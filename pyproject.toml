[project]
name = "homelab-monitor-workspace"
version = "0.0.0"
requires-python = ">=3.12"
description = "Homelab monitor — workspace root (not a package)"

[tool.uv]
workspace = { members = ["apps/monitor"] }
