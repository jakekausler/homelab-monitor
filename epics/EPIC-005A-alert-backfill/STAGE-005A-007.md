# STAGE-005A-007: Build / config health + self-disk-shrink rules

## Status: Complete

## Overview

A cluster of small "a subsystem silently stopped working / the monitor is acting on its own storage" health signals that EPIC-001/003 emit but never alert on. All four metrics below EXIST live and are one-rule-away from coverage. Severity is low (mostly warning/info) — these surface silent degradation, not emergencies.

Per-slot self-disk fill is OUT of this stage (it's in STAGE-005A-002); this stage covers the self-disk *shrink action* event plus the build/config health gauges.

**Verified live (scoping session):**
- `homelab_self_disk_shrink_total` EXISTS (counter, label `tier`) — emitted by `self_disk.py` when `used_pct > 95%` and the 5-min cooldown elapsed (the auto-shrink DECISION fired; the actual VM-retention reduction is a future TODO, currently metric-only signaling). NO alert.
- `homelab_build_sources_config_loaded` EXISTS (1 series, gauge) — health indicator for the build-sources config load. NO alert.
- `homelab_docker_compose_readable` EXISTS (1 series, gauge) — whether the monitor can read the compose file. NO alert.
- `homelab_build_source_hash_skipped_total` EXISTS (gauge despite the `_total` name, labels `reason`/`name`) — emitted by `local_build_update_collector.py` when a local-build update check skips (context_missing / context_too_large / permission_denied). NO alert.

## Stage Flags

- Has Input Forms: No.
- Touches secrets / production data: No (rules over existing self-metrics).
- Host-integration concerns: Yes — the build/config-readable gauges depend on the real host compose/build-context paths being mounted (validated on the prod rig, 3a + 3b). The shrink rule needs a planted critical-disk condition to test.

## Dependencies

- EPIC-001 (`self_disk` collector → `homelab_self_disk_shrink_total`).
- EPIC-003 (build-sources / local-build-update collectors → `homelab_build_*`, `homelab_docker_compose_readable`).
- vmalert-metrics + Alertmanager.

## Concrete deliverables

A new rule file `deploy/vmalert/metrics/self_and_build.yaml` (+ `__tests__/self_and_build.tests.yaml`) containing:

1. **Self-disk auto-shrink fired (user: include).** `increase(homelab_self_disk_shrink_total[10m]) > 0` → warning (or error), `{{tier}}`. The monitor's self-disk hit critical and the auto-shrink decision triggered — "my own storage is in trouble." Note: this fires at the same 95% tier as `SelfDiskCritical`, but is a DISTINCT event (the shrink decision, with the `tier` label) vs the threshold crossing. Design confirms whether warning or error and whether to dedupe vs SelfDiskCritical (they convey different things; both firing is acceptable, but confirm).
2. **Build-sources config load failure (user: include).** `homelab_build_sources_config_loaded == 0` for `5m` → warning. The build-sources config failed to load → the local-build-update feature is broken.
3. **Compose file unreadable (user: include).** `homelab_docker_compose_readable == 0` for `5m` → warning. The monitor can't read the compose file → degraded Docker features (locally-built image detection, etc.).
4. **Build-source hash skipped (user: include).** `homelab_build_source_hash_skipped_total == 1` (or `count(...) > 0`) for `5m` → info/warning, `{{name}}`/`{{reason}}`. A local-build update check couldn't compute the source hash (context missing / too large / permission denied) → the operator should fix the build context.

## Decisions to confirm in Design

- **D-005A-007-SHRINK-SEVERITY** — warning vs error for the shrink-fired rule; dedupe-vs-SelfDiskCritical decision (likely keep both — distinct signals).
- **D-005A-007-FOR-WINDOWS** — `for:` windows for the `== 0` health gauges (5m proposed — avoid firing on a transient single-tick blip).
- **D-005A-007-SKIPPED-SEVERITY** — info vs warning for the build-hash-skipped rule; whether to alert per-reason or aggregate.
- **D-005A-007-GAUGE-SEMANTICS** — confirm `build_source_hash_skipped_total` is a gauge (resets per tick) so the rule uses `== 1`/`count() > 0`, not `increase()` (the `_total` name is misleading; verify against the collector).

## Acceptance criteria

- Test mirror (plants config-load-failure / compose-unreadable / skip / shrink → fires; healthy → silent).
- The config-readable rules validated on the prod rig where the real compose/build-context paths ARE mounted (so the gauges read 1 at baseline, 0 only on real failure).
- Shrink rule does NOT spuriously fire (it only emits on a real >95% critical-disk event).
- Severity vocab + `source_tool: vmalert-metrics` + appropriate `target_kind`/`integration` labels.

## Design Phase

**Status**: [x] Design (Complete)

### Design Notes

**Verified live (prod rig + source) — corrections to card hints:**
- `homelab_self_disk_shrink_total` — COUNTER (`write_counter`, +1), label `tier="v1"`. ABSENT live (emits only on >95% + cooldown). Confirmed via self_disk.py:152-156.
- `homelab_build_sources_config_loaded` — GAUGE, NO metric-specific labels (only scrape-target instance/job/service). Live value = 1 (healthy). Card hinted `{tier}` — WRONG, no such label.
- `homelab_docker_compose_readable` — GAUGE, no metric-specific labels. Live value = 1.
- `homelab_build_source_hash_skipped_total` — GAUGE despite `_total` (`write_gauge`, SET to 1.0), labels `reason` (context_missing/context_too_large/permission_denied) + `name`. ABSENT live. Confirmed gauge via local_build_update_collector.py:339-371.
- None of the 4 metrics has an existing vmalert rule (verified live `/api/v1/rules`).

**Decisions:**
- **D-005A-007-SHRINK-SEVERITY** → CHOSEN: `warning`, keep both rules firing (do NOT dedupe vs SelfDiskCritical at the rule layer). Shrink event is a remediation-action trail; SelfDiskCritical remains the critical state alert. Distinct signals (shrink carries `tier`). REJECTED: critical (would duplicate SelfDiskCritical urgency as noise); rule-layer dedupe (loses the distinct action signal — defer any notification dedupe to Alertmanager).
- **D-005A-007-FOR-WINDOWS** → CHOSEN: `for: 5m` for both `== 0` health gauges. Suppresses config-reload/file-swap single-tick blips; 5m latency acceptable for degraded-feature warnings. Matches card + sibling convention. REJECTED: `for: 0s` (risks paging on a reload race).
- **D-005A-007-SKIPPED-SEVERITY** → CHOSEN: `info` severity, per-`{reason,name}` granular via `count by(reason,name)(...) > 0`. All 3 skip reasons are operator-fixable config issues, not emergencies → info. Granular preserves the actionable container+reason; fan-out bounded. REJECTED: `warning` (over-escalates a fixable config issue); single aggregate `count(...) > 0` (loses the actionable reason/name labels).
- **D-005A-007-GAUGE-SEMANTICS** → CONFIRMED: `build_source_hash_skipped_total` is a GAUGE (per-tick SET to 1.0), so the rule uses `count by(reason,name)(...) > 0` (presence-based, value-agnostic), NOT `increase()`. `self_disk_shrink_total` IS a real counter → `increase(...[10m]) > 0` is correct.

**Final rule plan (`deploy/vmalert/metrics/self_and_build.yaml`, label set: `severity` + `source_tool: vmalert-metrics` + `target_kind`):**
1. **SelfDiskShrinkFired** — `increase(homelab_self_disk_shrink_total[10m]) > 0` · `for: 0s` · `severity: warning` · `target_kind: self_disk` · annotates `{{ $labels.tier }}`.
2. **BuildSourcesConfigLoadFailed** — `homelab_build_sources_config_loaded == 0` · `for: 5m` · `severity: warning` · `target_kind: build_config`.
3. **DockerComposeUnreadable** — `homelab_docker_compose_readable == 0` · `for: 5m` · `severity: warning` · `target_kind: build_config`.
4. **BuildSourceHashSkipped** — `count by(reason,name)(homelab_build_source_hash_skipped_total) > 0` · `for: 5m` · `severity: info` · `target_kind: build_config` · annotates `{{ $labels.reason }}` / `{{ $labels.name }}`.

**Validation approach:** All 4 exprs are simple single-metric threshold/aggregate comparisons — NO subqueries, NO `label_value`, NO cross-metric binary ops → ALL promtool-validatable, NONE require live-replay. Test mirror plants present-series for the two absent-until-event metrics (#1 counter: rising window so `increase()>0`; #4 gauge: SET to 1.0 per `{reason,name}`) plus no-series negative cases asserting no alert.

## Build Phase

**Status**: [x] Build (Complete)

### Build Notes

Created two files:
- `deploy/vmalert/metrics/self_and_build.yaml` — 4 rules (group `self_and_build`, interval 30s): SelfDiskShrinkFired (`increase(homelab_self_disk_shrink_total[10m]) > 0`, for:0s, warning, target_kind self_disk), BuildSourcesConfigLoadFailed (`== 0`, for:5m, warning, build_config), DockerComposeUnreadable (`== 0`, for:5m, warning, build_config), BuildSourceHashSkipped (`count by(reason,name)(...) > 0`, for:5m, info, build_config). Label set per rule: `severity` + `source_tool: vmalert-metrics` + `target_kind`. Header uses the current severity-vocab comment (info/warning/critical; 'error' removed STAGE-005A-003).
- `deploy/vmalert/metrics/__tests__/self_and_build.tests.yaml` — 8 promtool test blocks (FIRE + NO-FIRE per rule). Absent-until-event metrics planted: shrink counter via rising `0 0 1 1 1` window (increase>0); hash-skip gauge SET to 1.0 per {reason,name}.

Pre-checks (main agent, direct):
- `promtool check rules self_and_build.yaml` → SUCCESS: 4 rules found.
- `promtool test rules __tests__/self_and_build.tests.yaml` → SUCCESS (all 8 FIRE/NO-FIRE blocks pass). This satisfies Refinement 3a (fire/no-fire on planted conditions).
- `make verify` → PASS. Backend 4928 pass / 2 environmental skips (test_scheduler hash-collision [flapping], test_vector_template vector-not-on-PATH) / 100% kernel coverage. Frontend 1541 vitest pass, tsc clean, build smoke pass. pyright 0, ruff clean. No Python/UI source touched (YAML-only), so no regressions.

All 4 exprs are promtool-validatable (no subqueries / label_value / cross-metric binary ops) — NO live-replay needed.

## Refinement Phase

- [x] 3a — rules fire/don't-fire on planted failure conditions (test mirror) — promtool `test rules __tests__/self_and_build.tests.yaml` → SUCCESS (all 8 FIRE/NO-FIRE blocks).
- [x] 3b — config-readable gauges validated against the real mounted paths on the prod rig — reloaded bind-mounted file; 4 rules `health=ok`/empty `lastError`; no parse errors; NO false-fire at baseline; gauges read 1 (healthy) live.
- [x] Regression Items Added

**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests) — Opus code-reviewer: APPROVED, zero findings.
- [x] Tests Written — promtool test mirror (8 FIRE/NO-FIRE blocks) written in Build; SUCCESS.
- [x] Code Review (post-tests) — N/A: no implementation logic changed after first review (clean approval; only new YAML files).
- [x] Documentation Updated — changelog entry added; regression.md entry added.
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: 394b9a6
- Changelog: 848edeb
- Tracking: (this commit)

**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize (Complete)
