# Regression Checklist - EPIC-005A: Alert backfill for EPICs 001-004

(Items added per stage during Refinement.)

## STAGE-005A-001 — Host machine threshold + anomaly rules

- `deploy/vmalert/metrics/host_anomalies.yaml` loads on the live vmalert with all 11 rules `health=ok` (check `docker exec homelab-vmalert-metrics sh -c "wget -qO- http://127.0.0.1:8880/api/v1/rules"`). No false-fire at a healthy baseline (check `/api/v1/alerts` — no `target_kind=host` alert firing/pending).
- Memory-ratio rules must use `/ ignoring(type)` — a bare `homelab_host_memory_bytes{type="used"} / homelab_host_memory_bytes{type="total"}` matches nothing and the rule silently never fires. Applies to HostCriticalMemory, HostMemoryPressure, HostMemoryAnomalous (host_anomalies.yaml) AND HostHighMemory (host.yaml). Regression check: query the division on the live VM and confirm it returns a value (not empty).
- `homelab_host_disk_io_bytes_total` / `homelab_host_net_bytes_total` must read SANE absolute values (kernel-truth ~TB/GB, not PB/EB) and `rate()` in MB/s (not TB/s). Regression check: `rate(homelab_host_disk_io_bytes_total[5m])` per-series should be plausible disk throughput. If inflated, the host collector regressed to passing psutil cumulative values through `write_counter` (.inc) instead of `write_counter_absolute` (Gauge set).
- Rule-test mirror `__tests__/host_anomalies.tests.yaml`: run `docker run --rm --entrypoint promtool -v $(pwd)/deploy/vmalert/metrics:/rules prom/prometheus:v2.47.0 test rules /rules/__tests__/host_anomalies.tests.yaml` — the 9 promtool-validatable rules must pass; the 2 MetricsQL-only rules (HostDiskIOAnomalous, HostNetThroughputAnomalous, which use a `rate(...)[w:s]` subquery) show `got:[]` under promtool by design (validated via live-MetricsQL replay instead). The load rules (HostHighLoad/HostCriticalLoad) use `scalar(count(...))` and ARE promtool-validatable.

## STAGE-005A-002 — Disk-fill rules (host fs + per-slot budgets)

- `deploy/vmalert/metrics/disk_fill.yaml` loads on the live vmalert with all 4 rules `health=ok` and does NOT false-fire at the real baseline (`/` ~46%, `/storage` ~9%, self-disk slots <3%, all under 85/95%). Regression check: `/api/v1/rules` shows the 4 rules health=ok; `/api/v1/alerts` shows no disk_fill alert firing/pending.
- Host-fs rules MUST use `/ ignoring(type)` (used/total differ on `type` → bare division empty). Per-slot rules MUST NOT use ignoring (used_bytes and budget_bytes share the `{slot}` label → native join) and MUST keep `{slot!="vl"}` (vl has dedicated VictoriaLogsDisk* rules) + the `(homelab_self_disk_budget_bytes > 0)` divide-by-zero guard. Regression check: query both exprs on the live VM — host-fs returns a value per mountpoint, per-slot returns a value per non-vl slot with `slot="vl"` ABSENT.
- Rule-test mirror `__tests__/disk_fill.tests.yaml`: `docker run --rm --entrypoint promtool -v $(pwd)/deploy/vmalert/metrics:/rules prom/prometheus:v2.47.0 test rules /rules/__tests__/disk_fill.tests.yaml` → SUCCESS (all 4 rules promtool-validatable; FIRE + NO-FIRE + 2 vl-exclusion cases). FIRE-case `exp_annotations` assert BOTH the summary AND the rendered `description` (which embeds the computed % value).
- Do NOT double-fire the `vl` slot: the generic SelfDiskSlotFill rules exclude `slot="vl"`; `VictoriaLogsDiskWarning/Critical` in `vl_health.yaml` own the vl slot. Regression check: a high-usage vl slot fires VictoriaLogsDisk* but NOT SelfDiskSlotFill*.

## STAGE-005A-003 — Collector health rules

- `deploy/vmalert/metrics/collector_health.yaml` loads on the live vmalert with all 4 rules `health=ok` (collector_failure_rate_elevated, collector_silent_death, collector_silent_death_slow, collector_duration_anomaly) and does NOT false-fire at the real baseline. Regression check: `/api/v1/rules` shows the 4 rules health=ok; `/api/v1/alerts` shows no collector_health alert firing/pending (all 31 collectors succeeding regularly; none failing >50%; image_update_checker NOT flagged despite its 6-7h cadence).
- `collector_failure_rate_elevated` MUST use `sum without(reason)(rate(failure_total[15m]))` (NOT a bare `rate(failure_total)`): `failure_total` carries `{name,reason}` while `success_total` carries `{name}`, so a bare `rate(success)+rate(failure)` matches nothing → empty → rule never fires (the STAGE-005A-001 label-mismatch class). Regression check: query the denominator `rate(success_total[15m]) + sum without(reason)(rate(failure_total[15m]))` on the live VM — it must return a value (non-empty) for collectors that have failed.
- `collector_silent_death` is a CADENCE-CLASS PAIR with `collector_silent_death_slow`. The fast rule `{name!~"noop|image_update_checker"}[1h]` covers the ~30 collectors running ≤5min; the slow rule `{name=~"image_update_checker"}[12h]` covers the ~6-7h registry checker. `image_update_checker` runs every ~6-7h BY DESIGN (rate-limited) — it MUST stay excluded from the fast rule or it chronically false-fires. Regression check: `image_update_checker` does NOT appear in firing/pending collector_silent_death alerts. A future >1h-cadence collector must be added to BOTH rules (fast exclusion + slow inclusion).
- `collector_duration_anomaly` uses a `[6h:15m]` subquery → NOT promtool-validatable (shows empty under promtool by design); validated via live-VictoriaMetrics replay (fires on a mean-duration spike > clamp_min(3×6h-baseline, 1s); silent below the 1s floor). The other 3 rules ARE promtool-validatable: `docker run --rm --entrypoint promtool -v $(pwd)/deploy/vmalert/metrics:/rules prom/prometheus:v2.47.0 test rules /rules/__tests__/collector_health.tests.yaml` → SUCCESS.

## STAGE-005A-004 — Container lifecycle + resource rules + probe enablement

- `deploy/vmalert/metrics/container_lifecycle.yaml` loads on the live vmalert with all 6 rules `health=ok`, empty `lastError`, state inactive (ContainerRestartLoop, DockerImageUpdateCheckSkipped, ContainerHighCpuShare, ContainerHighMemoryShare, ContainerCriticalMemoryShare, DockerProbeSlow) and does NOT false-fire at the real baseline. Regression check: `/api/v1/rules` shows the 6 rules health=ok; `/api/v1/alerts` shows none of them firing/pending.
- The two memory-share rules use a single-series-denominator join `container_memory_working_set_bytes{name!=""} / on() group_left() homelab_host_memory_bytes{type="total"}` — VERIFIED non-empty on the live VM (the STAGE-005A-001 label-mismatch class). `on()` (empty match) broadcasts the single host-total series across all per-container numerator series; `group_left()` keeps the numerator `{name}` label. Regression check: query the bare division on live VM — must return ~36 non-empty series (fractions 0..1).
- ContainerHighCpuShare's denominator `count(homelab_host_cpu_percent{cpu!="all"})` (=16 cores) is a `count()`-scalar broadcast → NOT promtool-validatable (returns got:[] by design; its `__tests__` block is commented out with the planted-series recipe preserved). Validated via live-VictoriaMetrics replay: fires at 0.5625 when a container uses ~9 of 16 cores. The R1/R2/R4/R5/R6 cases ARE promtool-validatable: `docker run --rm --entrypoint promtool -v $(pwd)/deploy/vmalert/metrics:/rules prom/prometheus:v2.47.0 test rules /rules/__tests__/container_lifecycle.tests.yaml` → SUCCESS.
- Resource thresholds are absolute share-of-host (no per-container limits exist): CPU warn >0.5 (15m), mem warn >0.5 (15m), mem critical >0.85 (10m). The long `for:` is the anti-false-fire guard. Healthy baseline is tiny (largest mem container ~0.01 of host) so huge headroom — confirmed no false-fire on the real rig.
- PROBE ENABLEMENT (dormant→live transition): `deploy/compose/docker-compose.yml` carries `homelab-monitor.http.health` labels on monitor (`http://container:9090/api/healthz`), grafana (`http://container:3000/api/health`), victoriametrics (`http://container:8428/health`), alertmanager (`http://container:9093/-/healthy`). `homelab_probe_up` went from 0 series (dormant) to 4 series all =1 after recreating the 4 labeled containers; `homelab_probe_duration_seconds` 2–10ms (well under the DockerProbeSlow 2s threshold). `DockerProbeFailing`/`DockerProbeSlow` correctly silent. Regression check: after any rebuild/recreate, `homelab_probe_up` must show 4 series=1 for the first-party sidecars; if a sidecar's labels are lost the probe goes dormant again. NOTE: labels only apply on container RECREATE (not a bind-mount reload) — a `docker compose up -d` that recreates the labeled services is required for the probe discoverer to pick up label changes.
- `fixture.yaml` (FixtureHostHighCPU) was KEPT (NOT removed): it is an intentional test-only artifact used by `apps/monitor/tests/integration/test_canonical_e2e.py` as the canonical end-to-end smoke test; it stays inactive in prod because `fixture_cpu_percent` never exists there. Do NOT delete it.

## STAGE-005A-005 — Cron / heartbeat run-duration anomaly

- `deploy/vmalert/metrics/cron_duration.yaml` (RULE 1 `CronRunDurationAnomalous`) and `deploy/vmalert/metrics/cron_duration_cadence.yaml` (RULE 2 `CronRunDurationNearCadence`) both load on the live vmalert-metrics with `health=ok`, empty `lastError`, state inactive, and do NOT false-fire at the real baseline. Regression check: `/api/v1/rules` shows both rules health=ok; `/api/v1/alerts` shows neither firing/pending. The 2 real wrapped jobs (backup.sh daily, rtlamr-watchdog.sh 5-min) are both well under threshold (backup 23.6% of cadence, rtlamr ~10%, rtlamr natural max 74s = 24.7% — all under the 50%-of-cadence rule and the 120s floor).
- RULE 2 (cadence-relative) uses VM-only `label_value(metric, "cadence_seconds")` → promtool CANNOT parse it. A rule file containing `label_value()` is rejected WHOLE by promtool, so RULE 2 lives in its OWN file (`cron_duration_cadence.yaml`) — keeping `cron_duration.yaml` (RULE 1) promtool-validatable. Regression check: `promtool test rules __tests__/cron_duration.tests.yaml` → SUCCESS (tests RULE 1 only; RULE 1's file must NOT gain a label_value rule or promtool breaks). RULE 2 is validated via live-replay only. Both files load in prod vmalert (the `*.yaml` glob is per-file; vmalert supports label_value — confirmed health=ok on the live rig).
- RULE 1 rolling-baseline `homelab_heartbeat_last_duration_seconds{wrapper_installed="yes"} > clamp_min(3 * avg_over_time(same[1d]), 120)`: the floor=120s is the load-bearing anti-false-fire guard — it keeps rtlamr-watchdog.sh's natural 74s max silent (74 < 120). `homelab_heartbeat_last_duration_seconds` is a PERSISTED LAST-VALUE GAUGE (re-emitted every 30s until the next run) so for the daily job avg_over_time[1d] ≈ the current value (near self-comparison); the rule mainly fires on a fresh in-window spike or sustained degradation. Live-replay: fires at 500 (baseline 100), silent at flat 100.
- RULE 2 cadence-relative `homelab_heartbeat_last_duration_seconds{wrapper_installed="yes"} > 0.5 * label_value(same, "cadence_seconds")`: live-replay proved fire backup@50000 (>43200), silent rtlamr@30 and @74 (<150 — no false-fire on natural variance), fire rtlamr@200 (>150 — catches a genuine hang). This is the robust hang detector (works identically for the stable daily job and the variable 5-min job). Series WITHOUT a `cadence_seconds` label correctly return NaN from label_value() and are excluded.
- No overlap with existing heartbeat rules: the 7 `homelab.heartbeats` rules fire on staleness/overdue/failure/flapping; these 2 fire on run-DURATION while the job still succeeds. Labels: severity warning, source_tool: vmalert-metrics, target_kind: cron, routing_channel: cron-health (matching the cron family). Too-fast rule DROPPED (Design finding — noise on rtlamr's 0.16× natural fast exits).
