# STAGE-003-002: Vector container-log ingestion fix + opt-out exclude_containers wiring

## Status: Complete

## Overview

Fix the long-standing gap from STAGE-001-016: the vector `docker_logs` source hardcodes `include_containers = []` which means **zero containers are tailed**. Per-container stdout/stderr is NOT actually flowing into VictoriaLogs despite STAGE-001-016 claiming the deliverable was complete. This stage fixes that — vector tails ALL containers on the docker socket by default. An opt-out `exclude_containers` env var is provided for noise reduction.

Container LOG INGESTION is the prerequisite for STAGE-003-011 (per-container log viewer) and for EPIC-004's anomaly-detection work. EPIC-003.md cross-stage criterion `Docker log requirements` (added 2026-05-21) #1 lives here.

**Vertical slice:** after `make dev-prod` (or equivalent prod restart), querying VictoriaLogs for any running container's logs returns recent stdout/stderr lines. E.g.: `_stream_id:* AND service:"homeassistant"` (or whatever stream/service labels vector assigns) returns log entries. Specifically — for every container currently running on the host, VL contains at least one line within ~30s of stack restart.

**Spec references:** §3.2 (sidecars — vector); §4.3 (Logs data flow); §6.3 (VictoriaLogs streams); STAGE-001-016 (the original vector setup; its docker_logs source). EPIC-004.md Docker log requirements §1.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (read-only vector access to docker socket; vector already runs)
- Host-integration concerns: [x] Yes — relies on the real docker socket access from vector + real VictoriaLogs ingest. Refinement is **3a AND 3b**.

## Cross-cutting standards (from EPIC-003 §Cross-stage acceptance criteria)

100% kernel coverage maintained; ruff + black + pyright `--strict`; `make verify` green before commit and at every phase exit. Per-stream caps from STAGE-001-016 (50 lines/sec, 500 MB/day defaults) MUST still hold — this stage SHOULD NOT remove the rate caps; only the include/exclude container filter changes. Vector config rendering preserves the atomic-write + group-own pattern from `kernel/cron/render.py`.

## Concrete deliverables

### 1. Vector template fix — `deploy/vector/vector.toml.template`

The current template has:
```toml
[sources.docker_logs]
type = "docker_logs"
include_containers = []
exclude_containers = []
auto_partial_merge = true
```

`include_containers = []` is interpreted by vector as "include zero containers". Remove the `include_containers` line entirely (or set it to `null`, depending on vector's TOML semantics — Build phase verifies). With `include_containers` omitted, vector tails ALL containers visible via the docker socket — which is what we want for auto-discovery.

Replace `exclude_containers = []` with a template placeholder that the render-on-boot path substitutes:
```toml
[sources.docker_logs]
type = "docker_logs"
# include_containers omitted: tail ALL containers visible via the docker socket.
exclude_containers = ${VECTOR_DOCKER_EXCLUDE}
auto_partial_merge = true
```

The placeholder substitution renders to a TOML array (e.g., `[]` for the default, or `["noisy-container-1", "another"]` if the user has set the env var).

### 2. Render-on-boot substitution

Extend `apps/monitor/homelab_monitor/kernel/cron/render.py` (or rename to a more general `kernel/render/vector.py` if appropriate — Build phase decides) to handle the new placeholder.

Current behavior: single placeholder `${CRON_EVENTS_INGEST_TOKEN}` substituted at render-on-boot time.

New behavior: also substitute `${VECTOR_DOCKER_EXCLUDE}`. Source:
- Read env var `VECTOR_DOCKER_EXCLUDE` (CSV string, default empty).
- Parse into a list (e.g., `"foo,bar"` → `["foo", "bar"]`).
- Render as TOML array literal: `["foo", "bar"]` or `[]` if empty.

If multiple template placeholders are needed in this stage and future ones, refactor the render function to a dict-based substitution (`{"CRON_EVENTS_INGEST_TOKEN": ..., "VECTOR_DOCKER_EXCLUDE": ...}`) — cleaner than chained `.replace()` calls.

### 3. Environment variable wiring

Add to `deploy/dev/dev.env.example`:
```
# Comma-separated list of container names to EXCLUDE from log ingestion.
# Default: empty (tail every container). Use to silence noisy/sensitive containers.
VECTOR_DOCKER_EXCLUDE=
```

Add to `deploy/compose/.env.example` (if it exists; otherwise document in CLAUDE.md):
```
VECTOR_DOCKER_EXCLUDE=
```

The compose file (`deploy/compose/docker-compose.yml`) MUST pass this env var to the monitor container so render-on-boot can read it. Verify the existing `environment:` block on the monitor service includes `VECTOR_DOCKER_EXCLUDE=${VECTOR_DOCKER_EXCLUDE:-}`.

### 4. Tests

`apps/monitor/tests/test_vector_render.py` (new OR extend existing test):
- Empty VECTOR_DOCKER_EXCLUDE → `exclude_containers = []` in rendered TOML.
- `VECTOR_DOCKER_EXCLUDE=foo` → `exclude_containers = ["foo"]`.
- `VECTOR_DOCKER_EXCLUDE=foo,bar` → `exclude_containers = ["foo", "bar"]`.
- Malformed input (extra whitespace, trailing commas) handled gracefully (strip + filter empty).
- Atomic-write pattern preserved (tempfile + os.replace).
- Group-own + mode 0o640 preserved.

Integration test (extend `apps/monitor/tests/integration/`):
- After bringing up the test rig + vector + a known fixture container, query VL for that container's logs within a bounded wait — confirm log lines arrive.

### 5. Verify the gap is fixed

Document an acceptance probe in the stage's Build phase:
1. Bring up prod stack: `make dev-prod`.
2. Wait ~30s.
3. Query VL: `docker compose exec victorialogs wget -qO- 'http://127.0.0.1:9428/select/logsql/field_values?query=*&field=service&limit=50'`.
4. Confirm container names appear (not just journald `service` values like `docker.service`, `cron.service`).

If you set `VECTOR_DOCKER_EXCLUDE=homelab-cadvisor`, the cadvisor container's logs should NOT appear in VL after restart. (Cadvisor's logs are noisy; excluding it from log ingestion is a likely default in the host-overrides repo.)

## Out of scope for this stage

- Per-container log viewer UI (STAGE-003-011).
- Log anomaly / pattern detection (EPIC-004).
- Inclusion glob patterns (e.g., `include_containers = ["compose-*"]` with wildcards) — vector supports them but adds config complexity; defer to a follow-up if a real need surfaces.
- Per-container rate caps differing from the global default (STAGE-001-016 sets per-stream caps via vector's transforms; this stage doesn't touch them).
- Editing `exclude_containers` from the UI — env-driven only in v1.
- Hot-reload of `exclude_containers` without restart — render-on-boot only. Users must restart the stack for changes to take effect (documented).

## Decisions in this stage

The Design phase confirms the exact env var name, the parsing rules (CSV vs JSON vs other), whether to support glob patterns, whether to refactor the render function to dict-based substitution, and whether the env var is monitor-side OR vector-side (i.e., does the monitor render the config, or does vector read the env directly?).

## Locked Design Decisions

D-OPT-OUT-MODEL — vector tails ALL containers by default (include_containers omitted); `VECTOR_DOCKER_EXCLUDE` is the opt-out mechanism. NOT an opt-in (include) model. User decision: "we can just rely on exclude." (EPIC-003 brainstorm 2026-05-21.)

D-RENDER-AT-BOOT — `VECTOR_DOCKER_EXCLUDE` is substituted at render-on-boot time, NOT dynamically. Containers come/go without re-rendering; vector's docker_logs source watches the docker events stream and auto-attaches to new containers, auto-detaches when containers die. Re-rendering only needed when the EXCLUDE list itself changes.

D-NO-INCLUDE-PATTERNS-V1 — wildcard patterns in include_containers (e.g., `homelab-*`) are NOT supported in v1. Either a container is excluded by exact name or it's included. Glob support deferred.

D-NO-HOT-RELOAD — changing `VECTOR_DOCKER_EXCLUDE` requires a stack restart (monitor process re-renders the config, vector restarts to pick it up). Documented; no inotify watcher in v1.

D-FIXES-STAGE-001-016 — this stage fixes the gap from STAGE-001-016 where the deliverable "tails Docker container stdout/stderr" was declared complete but actually shipped non-functional (empty include_containers). The fix is small but is BLOCKING for STAGE-003-011 (log viewer) and EPIC-004 (log anomaly detection).

## Design Phase

- **Approaches to present:** parse rules for `VECTOR_DOCKER_EXCLUDE` (CSV vs JSON array vs YAML list); render-function refactor (chained `.replace()` vs dict-based substitution); test rig fixture container to use for the integration test.
- **UI Options Presented:** None (no UI in this stage).
- **User Choice:** See "Selected Approach" section below.
- **Seed Data Agreed:** acceptance test — bring up prod stack, query VL for container service names, confirm at least one container's logs are present within 30s.
- **Session Notes:** Design phase decisions locked 2026-05-21.

**Status**: [x] Design (Complete)

## Selected Approach (Design phase exit, 2026-05-21)

### Tactical decisions (3)

| # | Question | Decision |
|---|---|---|
| T-PARSE-FMT | `VECTOR_DOCKER_EXCLUDE` parse format | **CSV** (comma-separated container names, e.g. `homelab-cadvisor,homelab-vmagent`). Trim whitespace per entry. Empty / unset → renders to `[]`. |
| T-RENDER-SHAPE | Render-function refactor | **Dict-based substitution map** in `kernel/cron/render.py`. Replaces current single `template.replace()` with a registry of `placeholder → value-producer` callables. Future-proofs for STAGE-003-006+. |
| T-TEST-FIXTURE | Integration-test fixture container | **No new fixture container.** Unit-test the CSV→TOML-array converter in isolation. Real-container ingestion verified by Refinement 3b against prod stack. |

### Additional clarifications confirmed in Design

- **D-INVALID-ENTRY-HANDLING** — entries containing `"` or `\` are skipped silently with a WARNING log naming the offending entry. Vector still starts. Spirit: don't break boot for one bad name.
- **D-DEFAULT-VALUE** — `VECTOR_DOCKER_EXCLUDE` defaults to empty (tail ALL containers). Matches D-OPT-OUT-MODEL. Users opt-out specific containers via env var. Required for STAGE-003-011 log viewer to see everything by default.

### Deliverable buckets (4)

1. **`deploy/vector/vector.toml.template`**
   - Remove the `include_containers = []` line entirely (vector defaults to all containers when omitted).
   - Replace `exclude_containers = []` with `exclude_containers = ${VECTOR_DOCKER_EXCLUDE}` (substituted value is the full TOML array literal: `["name1","name2"]` or `[]`).

2. **`apps/monitor/homelab_monitor/kernel/cron/render.py`**
   - Refactor to dict-based substitutions (registry of placeholder→producer callables).
   - Add `VECTOR_DOCKER_EXCLUDE` env reader.
   - Add CSV-to-TOML-array converter helper. Defensive: skip entries containing `"` or `\`, log WARNING naming the entry.
   - Preserve atomic-write + group-own pattern (no behavior change to file IO).

3. **Env wiring**
   - `deploy/dev/dev.env.example`: add commented-out `# VECTOR_DOCKER_EXCLUDE=` example with a short comment explaining usage.
   - `deploy/compose/.env.example` (if exists): same.
   - `deploy/compose/docker-compose.yml`: ensure monitor service receives `VECTOR_DOCKER_EXCLUDE` env var.

4. **Tests**
   - Extend `apps/monitor/tests/test_vector_render.py` (create if not present) with unit tests for:
     - CSV parser: empty, single, multi, whitespace-padded, trailing comma, malformed (containing `"` or `\`)
     - Dict-based render: existing `${CRON_EVENTS_INGEST_TOKEN}` substitution still works; new `${VECTOR_DOCKER_EXCLUDE}` substitution works; both work together
     - Defensive WARNING log emitted when an entry is dropped
   - Maintain 100% kernel coverage gate.

### Refinement plan (host-integration stage → 3a AND 3b)

**Driver:** Main agent dispatches subagents (user does NOT manually drive). Confirmed during Design.

**3a (dev rig):**
- Subagent runs `make dev`.
- Inspects rendered vector config — confirms no `include_containers` line, expected `exclude_containers = [...]`.
- Restarts vector.
- Waits 30s. Queries VictoriaLogs `/select/logsql/field_values?query=*&field=service&limit=50`.
- Asserts at least one running container's name appears (i.e., `service` field includes a container name, not just journald units like `docker.service`).

**3b (prod):**
- Subagent runs `make dev-prod` (or ensures prod stack is up via `docker compose -f deploy/compose/docker-compose.yml ps`).
- Queries VictoriaLogs after 30s; confirms container logs present.
- Sets `VECTOR_DOCKER_EXCLUDE=homelab-cadvisor` in `deploy/compose/.env`, restarts stack.
- Queries VL again; confirms cadvisor logs no longer appear in new ingestion.

### Out-of-scope (re-confirmed)

- Per-container log viewer UI → STAGE-003-011
- Log anomaly / pattern detection → EPIC-004
- Wildcard include/exclude patterns → deferred (D-NO-INCLUDE-PATTERNS-V1)
- Hot-reload of `VECTOR_DOCKER_EXCLUDE` without stack restart → deferred (D-NO-HOT-RELOAD)
- UI for editing `exclude_containers` → env-driven only in v1

## Build Phase

- **Components Created:** Dict-based substitution context in `kernel/cron/render.py`, CSV-to-TOML-array converter helpers.
- **API Endpoints Added:** None (backend-only stage).
- **Placeholders Added:** `${VECTOR_DOCKER_EXCLUDE}` in `deploy/vector/vector.toml.template`.
- **Session Notes:** 2026-05-21 — Build phase complete; `make verify` passed.

**Status**: [x] Build (Complete)

## Build Phase Notes (2026-05-21)

### Verification result

`make verify` → PASS (pass 9, after 8 failed iterations during ruff/black/pyright/test-migration cycle).

- Backend: ruff + format + pyright clean; 2280 tests passed, 1 skipped; **100.0% kernel coverage** (gate satisfied).
- Frontend: 35 ESLint warnings (baseline), 0 errors; 316 vitest passing; Vite build OK.

### Files modified/created in Build

- `apps/monitor/homelab_monitor/kernel/cron/render.py` — refactored to dict-based substitutions; added `SubstitutionContext` dataclass and `csv_to_toml_array()` helper.
- `apps/monitor/tests/test_vector_render.py` — NEW file; ~250 lines covering CSV parser edge cases, render_config new signature, render_on_boot env-var integration.
- `apps/monitor/tests/test_cron_render.py` — migrated 8 `render_config(token=...)` call sites to `render_config(context=SubstitutionContext(...))`.
- `deploy/vector/vector.toml.template` — removed `include_containers = []`; replaced `exclude_containers = []` with `exclude_containers = ${VECTOR_DOCKER_EXCLUDE}`.
- `deploy/dev/dev.env.example` — added `VECTOR_DOCKER_EXCLUDE=` documented opt-out var.
- `deploy/compose/.env.example` — added `VECTOR_DOCKER_EXCLUDE=` documented opt-out var.
- `deploy/compose/docker-compose.yml` — added `VECTOR_DOCKER_EXCLUDE` to monitor service env.

### Notes for Refinement

- Refinement is host-integration (3a + 3b). Main agent drives both via subagents.
- 3a: bring up `make dev`; inspect rendered config; restart vector; query VictoriaLogs `/select/logsql/field_values?query=*&field=service&limit=50` after 30s; confirm at least one running container's name appears in `service` field.
- 3b: `make dev-prod` (or verify prod stack is up via `docker compose -f deploy/compose/docker-compose.yml ps`); query VL after 30s for container names; then set `VECTOR_DOCKER_EXCLUDE=homelab-cadvisor` in `deploy/compose/.env`, restart, confirm cadvisor logs no longer flow.
- KNOWN: project's prod stack may currently be running with dev port mappings if `make dev` was last bring-up. Check `docker compose ps` first.

## Refinement Phase

- [ ] Desktop Approved (N/A — no UI in this stage)
- [ ] Mobile Approved (N/A — no UI in this stage)
- [x] Regression Items Added
- **Feedback Round 1:** Host-integration stage → Refinement is **3a AND 3b**. 3a confirms container logs flow to VL in dev rig. 3b confirms the same in prod against the user's real containers, AND that the `VECTOR_DOCKER_EXCLUDE` env var correctly silences a designated container.

**Status**: [x] Refinement (Complete)

## Refinement Phase Notes (2026-05-21)

### Approach actually taken

The Refinement plan envisioned a 3a/3b split (dev rig + prod stack). On finding the prod stack was already running and healthy with 12 containers + ~2h uptime, the main agent + user agreed to consolidate into a single prod-stack validation pass (combining 3a+3b). Rationale: there's no synthetic-data delta to validate — vector tails the docker socket, so any real container ecosystem is the actual test surface.

### Validation results — all 8 scenarios passed

| # | Scenario | Result |
| --- | --- | --- |
| 1 | BEFORE state baseline (confirms bug): 31 journald sources, **zero container names** in VL | PASS |
| 2 | Monitor image rebuild | PASS |
| 3 | Monitor + vector restart with new render.py + vector.toml.template | PASS |
| 4 | AFTER state: container logs now flowing — homelab-monitor, homelab-karma, homelab-victorialogs, zigbee2mqtt, rtlamr2mqtt, homeassistant, podcast-feed all present | PASS |
| 5 | `VECTOR_DOCKER_EXCLUDE=homelab-karma` added to prod .env, monitor restarted, vector restarted | PASS |
| 6 | Rendered config inside vector container contains `exclude_containers = ["homelab-karma"]` (verbatim verification) | PASS |
| 7 | Karma logs suppressed: 0 new entries in `_time:30s` post-restart; pre-existing buffered logs flush once on restart but no NEW logs ingest | PASS |
| 8 | Other containers (control): monitor logs still flowing (5 entries in `_time:1m`) — confirms exclusion is targeted, not global | PASS |
| Safety | .env restored to pristine state (md5 `8f3433e6de37fabb700f0d35c94d7177` matches exact pre-change hash); stack healthy post-restore | PASS |

### Locked decisions validated

- **D-OPT-OUT-MODEL** — vector tails ALL containers when no exclude is set ✓
- **D-RENDER-AT-BOOT** — VECTOR_DOCKER_EXCLUDE substituted at monitor boot via dict-based render; vector docker_logs auto-attached to all new container names ✓
- **T-PARSE-FMT (CSV)** — single-name exclude rendered correctly as `["homelab-karma"]` ✓

### Known gotcha (documented for future stages)

`docker compose up -d monitor vector` does NOT restart vector if its image/env didn't change — only monitor was recreated (its image was rebuilt). The freshly-rendered config sits on the shared volume but vector continues reading the old one. **Vector restart must be explicit**: `docker compose restart vector` (or `up -d --force-recreate vector`). This applies to ANY future stage that modifies the vector config via render.py — the deployment runbook must include both a monitor rebuild AND an explicit vector restart.

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: `1fbad04`
- Changelog: `1fbad04`
- Tracking: `1fbad04`

**CHANGELOG Entry**: [x] Added

## Finalize Phase Notes (2026-05-21)

### Code review

First pass (code-reviewer Opus): 10 findings (0 critical, 2 important, 5 minor, 3 nitpick). All applied per project policy.

Important findings addressed:
- Structlog event names: `cron_events.render.*` → `vector.render.*` (no operator-facing consumers — verified via grep across runbooks/vmalert/grafana/docs)
- Identifier scope: `SubstitutionContext` → `VectorRenderContext` (more accurately named for use in vector render context; prevents collision with potential future Alertmanager/other render contexts)

Other findings:
- Module docstring updated to reflect STAGE-003-002 extension scope
- `csv_to_toml_array` removed from `__all__` (internal helper)
- Dataclass docstring uses formal Attributes: section
- TOML array literal uses `", "` separator (matches spec example style)
- Env example files comment out `VECTOR_DOCKER_EXCLUDE=` per project convention
- `tmp_dir` test fixture removed; tests now use pytest's built-in `tmp_path` directly
- `capture_logs()` assertions added to verify D-INVALID-ENTRY-HANDLING WARNING contract
- Added populated-exclude-list test in `test_vector_template.py`
- `EXPECTED_ALL_INVALID_WARNINGS` constant extracted to satisfy PLR2004

Second pass (abbreviated, code-reviewer Opus): APPROVED for commit — no new critical/important issues.

### Final verification

`make verify` → PASS:
- Backend: ruff/format/pyright clean
- Backend: 2282 pytest tests passed (8 warnings) — 100% kernel coverage
- Frontend: 35 ESLint warnings (baseline), 0 errors; 316 vitest tests passing; Vite build clean

### Implementation commit

`1fbad04` — feat(STAGE-003-002): vector container-log ingestion fix + VECTOR_DOCKER_EXCLUDE opt-out

**Status**: [x] Finalize (Complete)
