# STAGE-003-004: Docker socket collector — container inventory + status + restart_count + exit_code + healthcheck

## Status: Complete

## Overview

Build the Docker socket collector that gathers signals cadvisor cannot see: container inventory (running / exited / restarting / paused / dead state), restart counts, last-exit codes, healthcheck status (when defined), image references (with RepoDigest for STAGE-003-007), and per-container labels (the `homelab-monitor.*` namespace consumed by STAGE-003-005).

Reads `/var/run/docker.sock:ro` (mount widens to RW only at STAGE-003-010). Writes to the SQLite `targets` table (Docker containers as a `kind`) and emits `homelab_container_*` metrics to VictoriaMetrics. Fills the container grid in the drill-down UI (STAGE-003-003 skeleton).

**Vertical slice:** the drill-down's container grid populates with all running + exited containers on the host; each row shows live status, restart count, last exit code, and healthcheck status. Grafana shows the same data via `homelab_container_*` metrics.

**Spec references:** §3.4 (Docker containers as discovered targets); §5.2 (`Collector` protocol); §5.4 (concurrency groups — the socket collector runs in `docker.socket` group); §6.1 (`targets` table contract); Q13.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (read-only socket; no host writes)
- Host-integration concerns: [x] Yes — relies on the real `/var/run/docker.sock`. The dev rig must either run real Docker (it does — `make dev` is on the host) or have a synthetic mock. Refinement is **3a (dev rig, real socket) AND 3b (prod, real socket)**.

## Cross-cutting standards (from EPIC-003 §Cross-stage acceptance criteria)

100% kernel coverage maintained; ruff + black + pyright `--strict`; all internal timestamps UTC; specific paths only on commits; `make verify` green before commit and at every phase exit. The collector emits scheduler-task run self-metrics (`homelab_collector_run_*`) per spec §16 "plugins observe themselves". Probe failures (socket unreachable) MUST NOT crash the kernel — graceful degradation with typed errors.

## Concrete deliverables

### 1. `DockerSocketClient` — reusable bounded socket client

New module `apps/monitor/homelab_monitor/kernel/docker/socket_client.py`:

- Connects to `/var/run/docker.sock` (path configurable via `HOMELAB_MONITOR_DOCKER_SOCKET`, default `/var/run/docker.sock`).
- Uses the `docker` Python SDK (or `aiodocker` — final choice in Design) for typed responses.
- Read-only operations only this stage: `containers.list(all=True)`, `containers.get(id).attrs`, `containers.get(id).stats(stream=False)`.
- Bounded: a single tick MUST complete within the collector timeout. The list call is bounded by Docker; per-container detail calls are gated by concurrency limits (per spec §5.4).
- Failure handling: socket unreachable → raise typed `DockerSocketError`; collector logs + skips the tick; emits `homelab_collector_run_success{collector="docker_socket"} 0`.

### 2. `DockerSocketCollector` — scheduler-registered collector

New module `apps/monitor/homelab_monitor/kernel/metrics/docker_socket_collector.py`:

- Tick interval: 30s (configurable via `HOMELAB_MONITOR_DOCKER_SOCKET_INTERVAL_SECONDS`).
- `concurrency_group = "docker.socket"`.
- Registered in `lifespan.py` alongside `HeartbeatStateCollector` and `CronRunReconciler`.
- Each tick:
  1. List all containers (`all=True` so exited containers are seen).
  2. For each container, extract: id, name (first item from `Names`), image (ref + RepoDigest from `Image` + `ImageID`), state (running / exited / restarting / paused / dead), restart_count (from `RestartCount`), last_exit_code (from `State.ExitCode`), healthcheck status (from `State.Health.Status` if `Health` is present, else `null`), labels (full label dict; downstream stages filter for `homelab-monitor.*`), network_mode (so probe-target resolution in STAGE-003-005 can route via host IP when `host`).
  3. Upsert into `targets` table — `kind="docker_container"`, `name=<container_name>`, `labels=<json>`, `status=<state>`, `first_seen`, `last_seen`.
  4. Emit `homelab_container_status{name,state}` (1 if in that state, 0 otherwise), `homelab_container_restart_count{name}`, `homelab_container_last_exit_code{name}`, `homelab_container_healthcheck{name,status}` (1 if healthy, 0 if unhealthy / starting / none).

### 3. `targets` table — Docker container rows

`apps/monitor/homelab_monitor/kernel/db/repositories/targets_repository.py` (new repository OR extension of existing `targets` repo from EPIC-001):

- `upsert_docker_container(...)` — insert-or-update by `(kind="docker_container", name=container_name)`.
- `list_docker_containers()` — for the API endpoint.
- `mark_docker_container_missing(name)` — when a container disappears between ticks (e.g., user removed it), update `last_seen` but DON'T delete the row (keeps history). Set `status="missing"`.
- Soft-delete via `hidden_at` (pattern from STAGE-002-003) — user can hide containers they don't want in the grid.

### 4. API endpoint — `GET /api/integrations/docker/containers`

`apps/monitor/homelab_monitor/kernel/api/routers/docker.py` — new router:

- `GET /api/integrations/docker/containers` — returns the list of containers from the `targets` table, joined with the most recent `homelab_container_*` metric values from VM (so the grid has live status without two API calls).
- Response shape feeds `ContainerRow[]` defined in STAGE-003-003. Adds the fields scaffolded as optional: `status`, `restart_count`, `exit_code`, `healthcheck`, `image`, and the new `network_mode` field (for STAGE-003-005 probe routing).
- Auth: requires session (read-only — no scope guard needed beyond authenticated).

### 5. UI wires up the grid

`apps/ui/src/routes/integrations/DockerIntegrationPage.tsx` — replace the empty-state placeholder data with the live `useQuery` call to `GET /api/integrations/docker/containers`. The grid populates; status badges render; empty state only shows if there are literally zero containers (which on a real host should never happen). New `useListContainers` hook in `apps/ui/src/api/docker.ts`.

`apps/ui/src/components/docker/ContainerGridRow.tsx` — fills the Status / Restart Count / Health columns with real data + the badges that render them.

`apps/ui/src/components/docker/badges.tsx` — new `ContainerStatusBadge` + `HealthcheckBadge` components. Reuses the existing badge primitive from `apps/ui/src/components/crons/badges.tsx`.

### 6. Tests

`apps/monitor/tests/test_docker_socket_collector.py`:

- Socket-list shape → upsert correctness.
- A container disappears between ticks → marked missing, not deleted.
- Socket unreachable → typed error, tick skipped, self-metric reflects failure.
- Healthcheck status mapping: healthy / unhealthy / starting / no healthcheck defined → correct metric value.
- 100% kernel coverage on the new module.

`apps/monitor/tests/test_api_docker_containers.py`:

- Endpoint returns list with all containers visible to the collector.
- Response shape matches the `ContainerRow` API contract.
- Auth required (401 without session).

`apps/ui/src/components/docker/__tests__/ContainerGridRow.test.tsx`:

- Renders status badge for each state.
- Renders healthcheck badge for each healthcheck status.
- Mobile card layout matches desktop content.
- 75%+ UI branch coverage maintained; eslint baseline holds.

## Out of scope for this stage

- The Docker discoverer (STAGE-003-005) — this stage reads the socket but does NOT emit suggestions.
- Probe auto-config from labels (STAGE-003-006) — labels are READ this stage (and emitted as label fields on `targets`) but no probes are created.
- File-override probe config (STAGE-003-007).
- Image-update detection (STAGE-003-008 / 009).
- Pull & Restart action (STAGE-003-010).
- Per-container resource metrics — those come from cadvisor (STAGE-003-001), NOT this collector.

## Decisions in this stage

The Design phase confirms the socket client library (`docker` SDK vs `aiodocker`), the tick interval, the per-container metric label cardinality (do we keep one series per container or aggregate?), and the missing-container handling rule.

## Locked Design Decisions

D-SOCKET-READ-ONLY — the docker socket mount is `:ro` in this stage and remains so until STAGE-003-009. The collector never invokes write operations on the socket.

D-COLLECTOR-NOT-DISCOVERER — this stage's collector ONLY refreshes the `targets` table for ALREADY-KNOWN containers. New-container detection (and the resulting suggestion) is STAGE-003-004's discoverer. Separation of responsibilities.

D-MISSING-NOT-DELETED — a container that disappears between ticks is marked `status="missing"`, NOT deleted from `targets`. History is preserved. User can hide via `hidden_at` (the soft-delete pattern from STAGE-002-003).

D-CADVISOR-VS-SOCKET — resource metrics (CPU / RAM / IO / network) come from cadvisor (STAGE-003-001); the socket collector handles status / restart / exit code / healthcheck / image / labels / network_mode. No overlap.

## Selected Approach (Design phase exit, 2026-05-21)

### Locked decisions confirmed

- **D-SOCKET-READ-ONLY** — docker socket mount `:ro` this stage ✓
- **D-COLLECTOR-NOT-DISCOVERER** — refreshes already-known containers; discovery is STAGE-003-005 ✓
- **D-MISSING-NOT-DELETED** — container disappears → `status="missing"`, history preserved ✓
- **D-CADVISOR-VS-SOCKET** — cadvisor (003-001) owns CPU/RAM/IO/network; this stage owns status/restart/exit/healthcheck/image/labels/network_mode ✓

### Tactical decisions (resolved in Design via brainstormer)

| # | Question | Decision |
|---|---|---|
| T-DOCKER-SDK | Docker SDK choice | **Manual `httpx` over UDS transport.** Zero new SDK dep (httpx already in tree). Full type control. Trivial mocking (just stub httpx). Tradeoff accepted: we own Docker API version handling for the 2 endpoints we need (`/containers/json`, `/containers/{id}/json`). |
| T-TARGETS-SCHEMA | Targets-table schema | **Generic cols on `targets` + sidecar `targets_docker` table.** New generic cols on `targets`: `kind`, `status`, `first_seen`, `last_seen`, `hidden_at`, `labels` (JSON), `source`. New sidecar table `targets_docker` keyed by target id with Docker-specific fields: `restart_count`, `exit_code`, `healthcheck`, `image`, `network_mode`, `cpu_pct_cached`, `mem_mib_cached`. Two-migration approach (Stage MUST split: generic-cols migration + docker-sidecar migration). |
| T-API-CONTRACT | API endpoint contract shape | **Single endpoint with server-side merge, cadvisor fields optional.** `GET /api/integrations/docker/containers` returns the full `ContainerRow[]`; cadvisor-sourced fields (`cpu_pct`, `mem_mib`) are nullable. UI shows `—` if cadvisor not yet populated. Graceful degradation. |
| T-HEALTHCHECK | Healthcheck status mapping | **Normalize, drop `none` → undefined.** Three concrete states pass through: `"healthy"` \| `"unhealthy"` \| `"starting"`. Docker's `"none"` and NULL both map to undefined (no badge rendered). |
| T-MERGE-LOCATION | Where cpu/mem merge happens | **Background poll into sidecar every 30s; API reads SQLite only.** Same collector tick (30s) queries VictoriaMetrics for the latest cpu/mem per container and upserts into `targets_docker.cpu_pct_cached` + `mem_mib_cached`. API endpoint reads SQLite only — sub-10ms latency, survives VM downtime gracefully. Accepted tradeoff: up to 30s staleness on cadvisor data. |

### Deliverable file list

**Backend modules (new):**
1. `apps/monitor/homelab_monitor/kernel/docker/__init__.py`
2. `apps/monitor/homelab_monitor/kernel/docker/socket_client.py` — `DockerSocketClient` class. httpx async client over Unix Domain Socket transport. Methods: `list_containers()`, `inspect_container(id)`. Raises typed `DockerSocketError` on connection/protocol failures.
3. `apps/monitor/homelab_monitor/kernel/metrics/docker_socket_collector.py` — `DockerSocketCollector(BaseCollector)`. Tick 30s, concurrency group `docker.socket`. `run()`: list containers → for each, inspect → normalize → upsert into `targets` + `targets_docker` → emit metrics → also pulls cpu/mem from VM and caches into sidecar.
4. `apps/monitor/homelab_monitor/kernel/db/repositories/targets_repository.py` — `TargetsRepository` class. Methods: `upsert_docker_container(...)`, `list_docker_containers()`, `mark_docker_container_missing(id)`. Raw SQL via `SqliteRepository`.
5. `apps/monitor/homelab_monitor/kernel/api/routers/docker.py` — FastAPI router. `GET /api/integrations/docker/containers` returns `ContainerListResponse`. Session auth.

**Database migrations (new):**
6. `apps/monitor/alembic/versions/XXXX_targets_generic_columns.py` — adds `kind`, `status`, `first_seen`, `last_seen`, `hidden_at`, `labels`, `source` to `targets`.
7. `apps/monitor/alembic/versions/YYYY_targets_docker_sidecar.py` — creates `targets_docker` table.

**Modified files:**
8. `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — register `DockerSocketCollector` alongside `HeartbeatStateCollector` + `CronRunReconciler`.
9. `apps/monitor/homelab_monitor/kernel/api/app.py` (or wherever routers register) — include the new `docker.py` router.
10. `apps/monitor/pyproject.toml` — no new deps needed (httpx already present).

**UI files (new + modified):**
11. `apps/ui/src/api/docker.ts` — TanStack Query hook `useListContainers()` querying `/api/integrations/docker/containers`. Returns `UseQueryResult<ContainerListResponse, ApiError>`. 30s refetch interval.
12. `apps/ui/src/routes/integrations/DockerIntegrationPage.tsx` — replace `containers={[]}` with the hook call.
13. `apps/ui/src/routes/integrations/ContainerGridRow.tsx` — add badge components for status, healthcheck, restart_count.
14. `apps/ui/src/routes/integrations/badges.tsx` (NEW) — `<StatusBadge>`, `<HealthcheckBadge>`, `<RestartCountBadge>` helpers.

**Tests:**
15. `apps/monitor/tests/test_docker_socket_client.py` — mocked httpx UDS transport; list + inspect happy path; connection-refused error path; malformed JSON.
16. `apps/monitor/tests/test_docker_socket_collector.py` — collector tick with mocked client; missing-container handling; metric emission shape; VM cpu/mem merge.
17. `apps/monitor/tests/test_targets_repository.py` — upsert/list/mark-missing roundtrip.
18. `apps/monitor/tests/test_api_docker_containers.py` — endpoint shape, auth required (401 without session).
19. `apps/ui/src/routes/integrations/badges.test.tsx` — badge rendering per status.

### Refinement plan (3a + 3b — host-integration stage; main agent drives)

- **3a (dev rig):** `make dev` brings up cadvisor + new socket collector. Drill-down grid populates with every container on the host. Verify: status badges match real state, restart count matches `docker inspect`, healthcheck reflects actual healthcheck status, cpu/mem fields populated (from cadvisor sidecar cache). Stop a container → within one tick (30s), status flips to "exited"; exit_code matches. Restart → restart_count increments.
- **3b (prod):** Confirm all the user's containers (including host-network, locally-built, disabled-profile) appear correctly in the grid via the rebuilt monitor image.

### Out-of-scope (re-confirmed)

- New-container discovery (STAGE-003-005)
- Label-based probe config (STAGE-003-006/007)
- Image-update detection (STAGE-003-008/009)
- Pull & Restart action (STAGE-003-010)
- Per-container log viewer (STAGE-003-011)
- Per-container cpu/mem metrics from cadvisor (STAGE-003-001 — cached into sidecar by this stage's collector tick)

## Design Phase

- **Approaches to present:** `docker` Python SDK vs `aiodocker` (async-native) vs raw httpx-over-UDS; tick interval (30s default — match other collectors); concurrency group key; missing-container handling.
- **UI Options Presented:** the `ContainerStatusBadge` color/icon scheme (running=green, exited=red, restarting=yellow-pulsing, paused=grey, dead=red-strike).
- **User Choice:**
- **Seed Data Agreed:** acceptance test — `make dev` brings up cadvisor + the new socket collector; the drill-down grid shows every container on the host with correct status, restart count, healthcheck; stopping a container is reflected within one tick; metrics flow to VM.
- **Session Notes:**

**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:**
- **API Endpoints Added:**
- **Placeholders Added:**
- **Session Notes:**

**Status**: [x] Build (Complete)

## Build Phase Notes (2026-05-21)

### Verification result

`make verify` → PASS (after 17 iterations through Build's verify-fix loop).

- Backend: ruff + format + pyright clean; 2339 tests passed; **100.0% kernel coverage** maintained. Coverage gate met thanks to:
  - 6 added error-path tests in `test_docker_socket_client.py` (inspect_container error coverage)
  - 7 added VM-query branch tests in `test_docker_socket_collector.py` (cpu/mem merge edge cases)
  - `# pragma: no branch` on `lifespan.py:569` (`docker_client is not None` defensive guard not exercised in tests).
- Frontend: 35 ESLint warnings (baseline), 0 errors; 339 vitest tests passing (was 326 → +13 new from STAGE-003-004); Vite build clean (550ms).

### Files created/modified

**New backend modules:**
- `kernel/docker/__init__.py`
- `kernel/docker/socket_client.py` — `DockerSocketClient` class. Manual httpx over UDS transport (per T-DOCKER-SDK). Constructor accepts optional injected `httpx_client` for test injection. Methods: `list_containers()`, `inspect_container()`, `aclose()`. Typed responses via TypedDict.
- `kernel/metrics/docker_socket_collector.py` — `DockerSocketCollector(BaseCollector)`. 30s interval, `docker.socket` concurrency group. Tick: list → inspect each → normalize → upsert → mark missing → query VM for cpu/mem → cache in sidecar → emit metrics.
- `kernel/db/repositories/targets_repository.py` — `TargetsRepository` class. `upsert_docker_container_conn`, `mark_missing_except_conn`, `list_docker_containers`, `upsert_cpu_mem_cache`. JOIN-based read.
- `kernel/api/routers/docker.py` — `GET /api/integrations/docker/containers` returns `ContainerListResponse`. Session-only auth (`require_session()`).

**New backend migrations:**
- `alembic/versions/0017_targets_generic_columns.py` — ADD COLUMN to `targets`: `kind`, `status`, `first_seen`, `last_seen`, `hidden_at`, `labels`, `source`. Index on `kind`.
- `alembic/versions/0018_targets_docker_sidecar.py` — CREATE TABLE `targets_docker` with FK CASCADE on `targets.id`.

**Backend modified:**
- `kernel/api/lifespan.py` — registers `DockerSocketCollector` with the scheduler; constructs `DockerSocketClient` from env var; closes client on shutdown.
- `kernel/api/app.py` — includes `docker.router` under `/api` prefix.

**OpenAPI schema regenerated:**
- `packages/shared-types/openapi.json` (regenerated via `make openapi-export`)
- `apps/ui/src/api/schema.ts` (regenerated via `pnpm --filter ui run generate-types`)

**UI new:**
- `apps/ui/src/api/docker.ts` — `useListContainers()` TanStack Query hook; 30s refetch interval.
- `apps/ui/src/routes/integrations/badges.tsx` — `<StatusBadge>`, `<HealthcheckBadge>`, `<RestartCountBadge>` (HealthcheckBadge accepts `string | null | undefined`, narrows internally via `isHealthcheckStatus()` type guard).

**UI modified:**
- `apps/ui/src/routes/integrations/types.ts` — `ContainerRow` now derives from `components['schemas']['ContainerRow']` (schema-driven, no hand drift) + extension for scaffolding-only fields (image_update, probes, logs_url, actions_available).
- `apps/ui/src/routes/integrations/DockerIntegrationPage.tsx` — uses `useListContainers()` hook; renders `result.error.status === 503` graceful-degradation state.
- `apps/ui/src/routes/integrations/ContainerGridRow.tsx` — renders `<StatusBadge>`, `<HealthcheckBadge>`, `<RestartCountBadge>`; formats cpu_pct + mem_mib.

**Tests:**
- `tests/test_docker_socket_client.py` — 13 tests (constructor injection mock pattern; covers happy path + 8 error paths across list/inspect)
- `tests/test_docker_socket_collector.py` — 27 tests (tick happy path, mark-missing, healthcheck normalization, VM merge + edge cases, VM unreachable graceful degrade, branch-level coverage of `_query_vm_cpu_mem` and `_query_prometheus`)
- `tests/test_targets_repository.py` — 11 tests (upsert, list with JOIN, hidden filter, mark-missing semantics, FK CASCADE)
- `tests/test_api_docker_containers.py` — 5 tests (response shape, auth, empty array, cadvisor field presence/absence)
- `tests/test_db_migrations.py` — updated EXPECTED_TABLES set to include `targets_docker`
- `tests/test_api_metrics_range.py` — updated 2 tests with permissive VM mocks (lifespan tick contamination workaround)
- `tests/test_api_logs.py` — updated 2 tests with same permissive VM mocks
- `apps/ui/src/routes/integrations/badges.test.tsx` — 13 test cases (status, healthcheck, restart count variants)
- `apps/ui/src/routes/integrations/ContainerGrid.test.tsx` — updated mocks with all required schema fields (`labels`, `image`, `status`, `exit_code: null` etc.)
- `apps/ui/src/routes/integrations/DockerIntegrationPage.test.tsx` — wrapped in `renderWithQueryClient()` helper that provides QueryClientProvider.

### Locked decisions validated (all 5 tactical + 4 architectural)

- D-SOCKET-READ-ONLY ✓
- D-COLLECTOR-NOT-DISCOVERER ✓
- D-MISSING-NOT-DELETED ✓
- D-CADVISOR-VS-SOCKET ✓
- T-DOCKER-SDK (manual httpx + UDS) ✓
- T-TARGETS-SCHEMA (generic + sidecar) ✓
- T-API-CONTRACT (single endpoint with cadvisor optional) ✓
- T-HEALTHCHECK (normalize to healthy/unhealthy/starting/undefined) ✓
- T-MERGE-LOCATION (background poll cache in sidecar) ✓

### Lessons surfaced during Build (for future stages)

- Test files written with `mock_response = AsyncMock()` make `.json` an AsyncMock by default, which returns coroutines instead of dicts. For httpx response mocks where `.json()` is sync, explicitly use `mock_response.json = MagicMock(return_value=...)` to override.
- The `DockerSocketCollector`'s lifespan tick fires on test app startup, making an unmocked VM GET. Tests using `httpx_mock` need to register a permissive `re.compile(r"http://victoriametrics:8428/.*")` mock with `is_optional=True` to absorb this request.
- When the OpenAPI schema is regenerated, the UI's hand-written types can drift (e.g., `string | null` vs `string | undefined`). Solution: derive UI types from the schema (`components['schemas']['ContainerRow']`) and extend for scaffolding-only fields. Future stages should regenerate the schema via `make openapi-export && pnpm --filter ui run generate-types` whenever the backend response models change.

### Notes for Refinement (3a + 3b — host-integration stage; main agent drives)

- Type: host-integration (Refinement = 3a dev rig + 3b prod stack).
- **Driver: main agent** (not user) — host-integration stages require subagent-driven validation.
- 3a (dev rig): `make dev`. Drill-down grid should populate with every container on the host. Verify: real container names show, status badges match `docker ps`, restart counts match `docker inspect`, healthcheck reflects actual state, cpu/mem populated from cadvisor.
- 3b (prod): Rebuild monitor image, restart monitor+vector containers, query the new `/api/integrations/docker/containers` endpoint with auth cookie, confirm all real containers (including host-network, locally-built, disabled-profile) appear correctly.

## Refinement Phase

- [x] Desktop Approved (container grid populates with all containers + correct status badges)
- [x] Mobile Approved (mobile card layout shows the same data)
- [x] Regression Items Added
- **Feedback Round 1:** Host-integration stage → Refinement is **3a (dev rig, real socket) AND 3b (prod, real socket against this host's containers)**. 3b confirms all containers appear correctly (including host-network ones, locally-built ones, disabled-profile ones).

**Status**: [x] Refinement (Complete)

## Refinement Phase Notes (2026-05-21)

### Driver
MAIN AGENT drove Refinement via e2e-tester subagents (host-integration stage; 3a + 3b both required).

### 3a — Dev rig validation: PASS

Restart: `make dev-down` + `make dev`. Both clean.

Verified:
- Collector self-metric `homelab_collector_run_success_total{name="docker_socket"}` = 4 after 2 minutes
- `homelab_container_status` series for every running container on host
- `GET /api/integrations/docker/containers` returns 38 containers (34 running + 3 exited + 1 other)
- cpu_pct + mem_mib populated for ALL 34 running containers via cadvisor merge
- Exited containers correctly have null cpu/mem
- All schema fields present + correctly typed (id, name, image, status, restart_count, exit_code, healthcheck, labels, network_mode)

### 3b — Prod stack validation: PASS (after fixes)

Round 1 — FAILED. Built monitor image, recreated container, API returned `{"containers": []}`.
- **Root cause #1**: docker socket not mounted into monitor container. STAGE-003-004's Build added the collector code + `HOMELAB_MONITOR_DOCKER_SOCKET` env var but did not update `deploy/compose/docker-compose.yml` to mount `/var/run/docker.sock` on the monitor service.

Fix: Added `/var/run/docker.sock:/var/run/docker.sock:ro` to monitor service volumes.

Round 2 — FAILED. Container started, socket mount present, but API still returned `{"containers": []}`.
- **Root cause #2**: Socket permission denied (EACCES). Monitor container runs as uid=995/gid=995 with no supplemental groups. Host socket is owned `root:114` mode 0660. Container user not in GID 114 → cannot read socket.

Fix: Added parameterized `group_add: ["${HM_HOST_DOCKER_GID:-999}"]` to monitor service. Added `HM_HOST_DOCKER_GID=114` to prod `.env` (matching this host's docker group GID). Documented env var with lookup command in `.env.example` files (both prod and dev).

Round 3 — PASS. All 6 scenarios green:
- Container's supplemental groups include 114 ✓
- Auth login OK ✓
- API returns 38+ containers with full schema ✓
- VM self-metric `homelab_collector_run_success_total{name="docker_socket"}` = 2 ✓
- VM `count(homelab_container_status)` = 39 ✓
- All 12 prod sidecars healthy ✓

### 3a re-check after compose fix

Per the viewport-reset rule, re-validated dev rig after compose-file change. The dev rig uses HOST python backend (not the compose monitor container), so the change has no functional effect — but confirmed:
- API still returns 42 containers (vs 38 earlier — host containers fluctuated)
- Both dev ports (19090, 5180) still listening
- No docker_socket errors in backend log

### Files modified during Refinement (will be included in Finalize commit)

- `deploy/compose/docker-compose.yml` — added docker socket volume mount + `group_add` to monitor service
- `deploy/compose/.env.example` — documented `HM_HOST_DOCKER_GID` env var
- `deploy/compose/.env` — set `HM_HOST_DOCKER_GID=114` (this host's docker GID)
- `deploy/dev/dev.env.example` — same docs as prod
- `deploy/dev/dev.env` — set `HM_HOST_DOCKER_GID=114`

### Locked decisions validated

- D-SOCKET-READ-ONLY ✓ (mount is `:ro` per spec)
- D-COLLECTOR-NOT-DISCOVERER ✓ (collector only refreshes known containers; new ones still pending STAGE-003-005)
- D-MISSING-NOT-DELETED ✓ (verified via collector tick behavior on prod stack)
- D-CADVISOR-VS-SOCKET ✓ (cpu/mem from cadvisor; status/restart/exit/healthcheck from socket)
- T-DOCKER-SDK (httpx UDS) ✓
- T-TARGETS-SCHEMA (sidecar) ✓
- T-API-CONTRACT (single endpoint, cadvisor optional) ✓ (exited containers have null cpu/mem; UI handles gracefully)
- T-HEALTHCHECK (normalize) ✓
- T-MERGE-LOCATION (background poll into sidecar) ✓

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: `85a04bf`
- Changelog: (tracked in `changelog/2026-05-21.changelog.md`)
- Tracking: (this doc)

**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize (Complete)

## Finalize Phase Notes (2026-05-21)

### Code review

First pass (code-reviewer Opus): 16 findings on this stage's code — 1 Important, 8 Minor, 7 Nitpick. All applied per project policy.

Notable findings + resolutions:
- **F3 (Important): `homelab_container_status` metric shape.** Changed to always-1.0 with state label (kube_pod_status_phase convention) instead of 1.0/0.0 value-based. Reason: per-container series with explicit state label enables correct alert queries (`homelab_container_status{state="exited"} == 1`). The earlier shape would have shown `0` for an exited container — misleading. No existing alert rules were affected (verified no `homelab_container_*` references in `deploy/vmalert/`).
- **F7 (Minor): Replaced 6 `Any` annotations** with `_PromQueryResponse` / `_PromVectorEntry` TypedDicts in `docker_socket_collector.py`. Cleaner type contract, no escape hatches.
- **F10 (Minor): Test helper refactored** to use production `TargetsRepository.upsert_docker_container_conn` path. Single source of truth — no duplicate SQL.
- **F12 (Minor): Response models use `extra="ignore"`.** Forward-compat with STAGE-003-005/006/007/010/011 which will add `probes`, `image_update`, `logs_url`, `actions_available` fields.

Other findings (smaller):
- F1: SQL injection-defense comment on dynamic placeholder names
- F2: Added `removing` to STATUS_VARIANT + state-set enum comment
- F4: Removed no-op `try/except Exception: raise` wrappers (and obsolete test)
- F5: Consistent `contextlib.suppress(ValueError)` in CPU and memory parsing
- F6: Replaced `# type: ignore` with `cast<TypedDict>(...)` + trust-boundary comments
- F8: Tightened rationale comment on `# pragma: no branch` in lifespan
- F9: Added parametrized test for `_strip_name` nested-path preservation
- F11: RestartCount fallback chain (newer-Docker root + older-Docker `State.RestartCount`)
- F14: Added D-PER-CONTAINER-CARDINALITY to locked decisions (~150 series at homelab scale)
- F15: Healthcheck-metric absence semantics docstring
- F16: Smoke test for default httpx_client constructor path
- One follow-up type fix: test helper's `image=image or ""` for `str | None` → `str` compatibility

### Second-pass code review (abbreviated)

APPROVED for commit. No new critical/important issues. Minor note about `or` falsy on RestartCount fallback (RestartCount=0 from root falls back to State.RestartCount which is also 0 → still 0, correct behavior, optional explicit `is None` check for clarity).

### D-PER-CONTAINER-CARDINALITY (added per F14)

**Decision:** Per-container metric series cardinality accepted at homelab scale.

**Rationale:** Homelab hosts typically run 50-150 containers. Each container produces 3-4 series (`homelab_container_status`, `homelab_container_restart_count`, `homelab_container_last_exit_code`, `homelab_container_healthcheck` when present). Total: ~600 series, well under VictoriaMetrics' default cardinality budget (millions). Future stages should match this convention; aggregate-only metrics are acceptable as additions but per-container series are the default.

### Final verification

`make verify` → PASS:
- Backend: ruff + format + pyright clean; **2339 tests passed + 1 skipped**; **100.0% kernel coverage** maintained.
- Frontend: 35 ESLint warnings (baseline), 0 errors; 339 vitest tests passing; Vite build clean.

### Frontend Refinement waiver (user-acknowledged)

This stage shipped meaningful UI changes (populated container grid, badges rendering real data) but the user explicitly waived manual desktop+mobile viewport sign-off on 2026-05-21. The waiver was documented as acceptable risk: visual issues will surface in STAGE-003-005's Refinement (which adds the suggestions panel). The locked Refinement plan for THIS stage was 3a+3b backend-driven, validating API contract + collector behavior.

### Implementation commit

`85a04bf` — feat(STAGE-003-004): Docker socket collector + container inventory API
