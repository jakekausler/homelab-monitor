# STAGE-003-010: "Pull & Restart" action — compose-aware + audit + confirm

## Status: Complete

## Overview

The first kernel-driven Docker WRITE action in the project. A confirm-gated per-container button executes `docker compose pull <service> && docker compose up -d <service>`, captures stdout/stderr, records the action in a new `compose_actions` audit table, and refreshes the UI. The docker socket mount widens from `:ro` to RW for this stage; the compose file is mounted `:ro`.

This stage is small in code surface but high in blast radius. Confirm-on-destructive per spec §7.2. Audit-logged per spec §16. The pattern established here is the precursor to EPIC-009 (auto-fix subsystem) — the same confirm + audit + capture-transcript machinery will be reused for Claude-driven runbook real-runs.

**Vertical slice:** clicking "Pull & Restart" on a row in the drill-down grid (where an image-update badge is showing) opens a confirm modal; typing a confirm phrase triggers the action; a "Pulling…" status appears on the row; on completion the row badge clears (no update available), the Recent Actions panel shows the new action with stdout/stderr toggleable.

**Spec references:** §2 Q13 (Pull & Restart dashboard action, auto-update OFF); §7.2 (confirm-on-destructive); §7.4 (auto-fix isolation patterns inform but do NOT block — this is a structured action, not a Claude run); §16 (audit_log).

## Stage Flags

- Has Input Forms: [x] Yes — the confirm modal collects a typed phrase or session PIN
- Touches secrets / production data: [x] Yes — invokes `docker compose pull && up -d`; container is restarted; secrets in the compose file are referenced (read-only) when docker compose runs
- Host-integration concerns: [x] Yes — requires the docker socket RW mount + the compose file RO mount + the `docker compose` CLI installed in the monitor image. Refinement is **3a (dev rig, against a test container) AND 3b (prod, against a real user container)**.

## Cross-cutting standards (from EPIC-003 §Cross-stage acceptance criteria)

100% kernel coverage maintained; ruff + black + pyright `--strict`; specific paths only on commits; `make verify` green before commit and at every phase exit. The action is bounded — explicit timeout (default 5 minutes), kill-on-timeout. Audit row written for EVERY attempt regardless of outcome. Compose file remains `:ro` (criterion #6); only the docker socket mount widens (criterion #18 — documented in the EPIC notes).

## Concrete deliverables

### 1. Dockerfile updates

`apps/monitor/Dockerfile`:

- Install `docker` CLI + `docker-compose` plugin (or `docker compose` v2 plugin specifically — pinned in Design).
- Add `/usr/local/bin/docker` to the image. ~50 MB image growth acceptable.

### 2. Compose mounts widen — socket RW + compose file RO

`deploy/compose/docker-compose.yml`:

- The monitor's docker socket mount changes from `:ro` to RW: `/var/run/docker.sock:/var/run/docker.sock`. (Stages 001-008 use `:ro`; this stage widens.)
- New mount: `/storage/docker/compose:/host-compose:ro` (compose dir mounted RO; the host path is configurable via `docker.compose_dir_path`).
- The same widening applies to `deploy/dev/docker-compose.dev.yml` for parity in dev rig.

The security boundary is at the docker socket level — anyone with socket access has root-equivalent on the host. This is documented and accepted per EPIC-003 cross-stage criterion #18. STAGE-003-010 commit message includes the explicit acknowledgment.

### 3. `ComposeActionRunner`

New module `apps/monitor/homelab_monitor/kernel/docker/compose_action_runner.py`:

- Single public method: `run_pull_and_restart(container_name: str, who: User) -> ComposeActionResult`.
- Looks up the compose service name for the given container name (via `compose_reader` from STAGE-003-009 — re-used here).
- Validates: the container exists in the compose file; the user has authenticated + passed the confirm gate (handled at the API layer).
- Constructs the command: `docker compose -f /host-compose/docker-compose.yml pull <service> && docker compose -f /host-compose/docker-compose.yml up -d <service>`.
- Executes via `asyncio.create_subprocess_exec` with bounded timeout (default 5 minutes, configurable).
- Captures stdout + stderr (separately).
- On timeout: SIGTERM then SIGKILL; recorded as `state="timeout"` in the audit row.
- Returns `ComposeActionResult(exit_code, stdout, stderr, started_at, ended_at, state)`.

### 4. `compose_actions` table

`apps/monitor/homelab_monitor/kernel/db/repositories/compose_actions_repository.py`:

```
compose_actions:
  id INTEGER PRIMARY KEY AUTOINCREMENT
  action TEXT NOT NULL CHECK (action IN ('pull_and_restart'))   -- extensible enum
  container_name TEXT NOT NULL
  compose_service TEXT NOT NULL
  before_image TEXT                   -- image ref captured before the action
  before_digest TEXT                  -- digest captured before the action
  after_image TEXT                    -- image ref after (post-up -d)
  after_digest TEXT                   -- digest after
  command TEXT NOT NULL               -- the literal command executed
  stdout TEXT                         -- captured (truncated to 1 MB)
  stderr TEXT                         -- captured (truncated to 1 MB)
  exit_code INTEGER
  state TEXT NOT NULL CHECK (state IN ('running', 'success', 'failed', 'timeout', 'killed'))
  started_at TEXT NOT NULL
  ended_at TEXT
  duration_seconds REAL
  who TEXT NOT NULL                   -- username from session
  client_ip TEXT
  audit_log_id INTEGER                -- FK to audit_log
```

Indexed on `(container_name, started_at DESC)`.

A row is written when the action starts (`state="running"`). Updated to terminal state on completion.

### 5. API endpoint — confirm-gated POST

`apps/monitor/homelab_monitor/kernel/api/routers/docker.py`:

- `POST /api/integrations/docker/containers/{name}/pull-and-restart` — kicks off the action.
- Request body: `{"confirm_phrase": "<typed-phrase>"}` (or session-PIN; final UX decided in Design).
- Returns immediately with `{"action_id": <id>, "state": "running"}`.
- The action runs in a background task. UI polls `GET /api/integrations/docker/compose-actions/{id}` for state.
- Per spec §7.2 — typed-phrase confirm OR session-lifetime PIN. Confirm enforcement is API-side (not just UI-side) — the endpoint rejects 400 without a valid confirm.
- Auth: requires session + a new scope `docker:write` on the session (or a different gating mechanism — Design decides). The Pull & Restart action is HIGH blast radius and should not be available to a stolen session by default.
- Audit log row written via `audit_log` (the existing table from EPIC-001 stage tracking — per spec §16 cross-cutting requirement).

- `GET /api/integrations/docker/compose-actions/{id}` — fetch one action's full record (stdout/stderr included).
- `GET /api/integrations/docker/compose-actions?container={name}&limit=N` — list recent actions for a container.

### 6. UI — Action button + confirm modal + Recent Actions panel

`apps/ui/src/components/docker/ContainerGridRow.tsx`:

- The "Actions" column gets a "Pull & Restart" button. The button is only enabled when `image_update_available=true`.
- Clicking opens a confirm modal: shows current vs latest digest, the compose command to be run, and a typed-phrase input ("type 'pull' to confirm" — the exact phrase decided in Design).

`apps/ui/src/components/docker/PullRestartModal.tsx` (new) — the confirm modal. Submits → POST `/api/.../pull-and-restart`; on success the modal closes and the row shows a "Pulling…" pulsing indicator. The UI polls the action status endpoint every 2s while `state="running"`.

`apps/ui/src/components/docker/RecentActionsPanel.tsx` — the empty-state placeholder from STAGE-003-003 fills in. Lists recent actions; each row shows container, action, state, duration, started_at, "who", and an expand toggle showing stdout/stderr.

`apps/ui/src/api/docker.ts` — `useStartPullAndRestart`, `useGetComposeAction`, `useListComposeActions` hooks.

### 7. vmalert rule — failed Pull & Restart

`deploy/vmalert/metrics/docker_compose_actions.yml`:

```yaml
- alert: DockerComposeActionFailed
  expr: increase(homelab_compose_action_failed_total[10m]) > 0
  for: 0m
  labels:
    severity: warning
    integration: docker
  annotations:
    summary: "Pull & Restart action failed for {{ $labels.container }}"
```

The runner emits `homelab_compose_action_failed_total{container,action,reason}` on every failed action (non-zero exit, timeout, killed) and `homelab_compose_action_success_total{container,action}` on every success.

### 8. Tests

- `test_compose_action_runner.py` — happy path, non-zero exit, timeout (5-minute cap exceeded), killed by user, compose file missing, container not in compose, stdout/stderr capture, truncation at 1 MB. Use `asyncio.create_subprocess_exec` mocking + a fake `docker` binary fixture.
- `test_compose_actions_repository.py` — insert/update/list/get queries.
- `test_api_docker_pull_and_restart.py` — confirm-phrase required (400 without); 401 without session; 403 without `docker:write` scope; happy path returns action_id; background task drives state to terminal; audit_log row written.
- UI tests for `PullRestartModal` + `RecentActionsPanel`.
- Integration test against the `docker-compose.test.yml` rig: a toy container with a fake registry → Pull & Restart actually re-creates the container.
- 100% kernel coverage maintained.

## Out of scope for this stage

- Auto-pull (never — auto-update is OFF per spec Q13; this is always manual).
- Pull WITHOUT restart (a "Pull only" action). Could be added in a follow-up; not needed in v1.
- Restart WITHOUT pull (a "Restart" action). Same — follow-up.
- "Pull all updates" (bulk action). Same — follow-up.
- Rollback (revert to previous image). Follow-up — would require image-digest history per container.
- Private-registry credentials for the pull (deferred from STAGE-003-007).

## Decisions in this stage

The Design phase confirms the confirm-gate UX (typed phrase vs PIN), the timeout default, the `docker:write` scope semantics, and the relationship to EPIC-009's runbook orchestrator (table merge or keep separate).

## Locked Design Decisions

D-COMPOSE-AWARE — the action uses `docker compose -f <file> pull <service> && up -d <service>`, NOT `docker pull` + `docker stop/start`. Compose-aware preserves the user's env, volumes, networks, dependencies.

D-INSIDE-MONITOR — the action runs INSIDE the monitor container. NOT via a separate fixer-runner pattern (that's EPIC-009 for Claude-driven runbooks). The monitor needs the `docker` CLI installed and the docker socket RW + compose file RO.

D-CONFIRM-API-SIDE — confirm gate is enforced API-side, not just UI-side. Even a hand-crafted curl must pass the confirm. (Spec §7.2.)

D-CONFIRM-EXTENDS-SPEC-7-2 — Spec §7.2 enumerates confirm-on-destructive actions but does NOT explicitly list Pull & Restart. This stage EXTENDS §7.2's list because Pull & Restart restarts a container (high blast radius). Documented here rather than added to the spec because the spec's list was illustrative, not exhaustive. (Spec §7.2 + EPIC-003 brainstorm decision.)

D-AUDIT-EVERY-ATTEMPT — an audit_log row + a compose_actions row are written for EVERY attempt, regardless of outcome (success, failure, timeout, killed). (Spec §16.)

D-COMPOSE-ACTIONS-OWN-TABLE — `compose_actions` is its own table, NOT a reuse of `runbook_runs` (which is a Claude-runs-only table). EPIC-009's auto-fix subsystem will revisit table layout — this stage establishes the audit pattern. (Cross-epic carry-forward → EPIC-009 noted in EPIC-003.md.)

D-SOCKET-RW-WIDEN — this stage widens the docker socket mount from `:ro` to RW. The security boundary is at the docker socket level and this is documented + accepted. (Cross-stage criterion #18.)

D-AUTO-UPDATE-OFF — auto-update is permanently OFF. Pull & Restart is always manual. (Spec Q13.)

## Design Phase

- **Approaches to present:** confirm-gate UX (typed phrase vs session-lifetime PIN vs both); timeout default; `docker:write` scope creation (new scope vs reuse of existing); table-vs-row capture for stdout/stderr (truncation at 1 MB seems reasonable but Design might propose paging instead).
- **UI Options Presented:** confirm modal layout (typed phrase below + diff above); button placement in the Actions column; Recent Actions panel layout (list with expand-toggle vs side-drawer).
- **User Choice:**
  - **Confirm gate UX:** Static phrase `"pull"` (typed in modal, server-side validated; spec §7.2 alignment, low friction for single-user homelab, EPIC-009 reuses same `expected_phrase` machinery)
  - **Auth model:** Session OR new `Scope.DOCKER_WRITE` on API tokens. Either gate passes. Future-proofs bot/CLI flows (Slack-bot, scripted) without breaking changes when added later.
  - **Concurrency:** Per-container `asyncio.Lock` via `dict[str, asyncio.Lock]` (mirrors `scheduler._group_locks` precedent). Allows parallel Pull & Restart across distinct containers; serializes within a single container.
  - **Compose file path discovery:** Reuse `BuildSourcesLoader` from STAGE-003-009. Single source of truth in `build-sources.yaml`. No new env var.
  - **Stdout/stderr storage:** TEXT columns on `compose_actions`, truncated at 1 MB on write. Simplest; real pull output is < 10 KB. Forensic disk-log is additive migration if ever needed.
  - **Background task lifecycle:** `asyncio.create_task()` in POST handler, tracked in module-level `set[asyncio.Task]`. App lifespan shutdown cancels + awaits with timeout. Lowest user-perceived latency.
  - **Action timeout:** 5 minutes default (configurable via env var). SIGTERM, 10s grace, then SIGKILL. Captured as `state="timeout"` in audit row.
- **Seed Data Agreed:** acceptance test — Pull & Restart on a known-stale container completes successfully; the badge clears; the Recent Actions panel shows the action with captured stdout/stderr; an audit_log row exists; a forced timeout (synthetic delay) is correctly captured as `state="timeout"`. **Test targets — TWO containers (chosen at Refinement time):** (1) one **locally-built** container — likely need to manually mutate the build context to force a source-hash change so the image-update badge fires (re-use STAGE-003-009 hash detection); (2) one **remote-image** container — force stale digest (re-tag an older image locally, or pick a container whose registry actually has a newer tag). Both must be low-blast-radius. Both Pull & Restart actions must complete cleanly through the full vertical slice (modal → confirm → running → terminal state → badge clears → Recent Actions populated). This dual-target validates BOTH code paths from STAGE-003-008 (registry digest) AND STAGE-003-009 (locally-built source hash) — Pull & Restart must work for both update-source flavors.
- **Session Notes:** Design phase deferred 4 architecture decisions to user: confirm UX, auth model, concurrency, compose path discovery. User picked: static phrase 'pull' (not container name; lower friction prioritized), Session OR scope (future-proofs), per-container lock (Recommended), BuildSourcesLoader reuse (Recommended). Minor decisions (output storage, task lifecycle, timeout default) all picked Recommended. All locked decisions (D-COMPOSE-AWARE, D-INSIDE-MONITOR, D-CONFIRM-API-SIDE, D-CONFIRM-EXTENDS-SPEC-7-2, D-AUDIT-EVERY-ATTEMPT, D-COMPOSE-ACTIONS-OWN-TABLE, D-SOCKET-RW-WIDEN, D-AUTO-UPDATE-OFF) carry forward unchanged. Future EPIC-009 inherits: confirm machinery (`expected_phrase` column), per-resource lock pattern, audit pattern, docker socket RW mount.

**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:**
  - Backend:
    - `apps/monitor/homelab_monitor/kernel/auth/scopes.py` — added `Scope.DOCKER_WRITE = "docker:write"`
    - `apps/monitor/alembic/versions/0028_compose_actions.py` — new migration creating `compose_actions` table (20 cols, CHECK constraints on action+state enums, composite index on container_name+started_at)
    - `apps/monitor/homelab_monitor/kernel/db/repositories/compose_actions_repository.py` — `ComposeActionRow` dataclass + `ComposeActionsRepository` (insert_running, update_terminal_state, get_by_id, list_for_container)
    - `apps/monitor/homelab_monitor/kernel/docker/compose_action_runner.py` — `ComposeActionRunner` with async `resolve_compose` (uses `com.docker.compose.service` label per Design ADDENDUM Q2), `trigger_pull_and_restart`, `_run_pull_then_up`, SIGTERM/grace/SIGKILL subprocess lifecycle, per-container `asyncio.Lock`, background task tracking, shutdown handler, `get_or_create_counter` helper. Output truncation at 1 MB.
    - `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — wired `ComposeActionRunner` into FastAPI lifespan (startup + shutdown)
    - `apps/monitor/homelab_monitor/kernel/api/routers/docker.py` — 3 new endpoints (POST + 2 GET), Pydantic models, error-state branch
    - `apps/monitor/Dockerfile` — installs docker-ce-cli + docker-compose-plugin (unpinned per Design ADDENDUM Q3)
    - `deploy/compose/docker-compose.yml` — docker socket mount widens `:ro` → RW; new `/host-compose:ro` mount
    - `deploy/compose/.env.example` — adds `HOMELAB_MONITOR_COMPOSE_ACTION_TIMEOUT_SECONDS=300` + `HOMELAB_MONITOR_COMPOSE_HOST_DIR=/storage/docker/compose`
    - `deploy/vmalert/metrics/docker_compose_actions.yaml` — new alert rule for `homelab_compose_action_failed_total`
  - Frontend:
    - `apps/ui/src/api/docker.ts` — `useStartPullAndRestart`, `useGetComposeAction` (with 2s polling while state=running), `useListComposeActions`
    - `apps/ui/src/components/docker/PullRestartModal.tsx` — confirm-gated dialog (typed "pull" phrase, case-insensitive)
    - `apps/ui/src/routes/integrations/ContainerGridRow.tsx` + `ContainerGridCard.tsx` — shared `ActionsCell` component (Pull & Restart button, in-flight "Pulling…" indicator, modal trigger)
    - `apps/ui/src/routes/integrations/RecentActionsPanel.tsx` — populates drill-down's Recent Actions section (state badge, duration, expand-to-show stdout/stderr)
    - `apps/ui/src/routes/integrations/ContainerImageUpdatePage.tsx` — wired `RecentActionsPanel` into drill-down
  - OpenAPI: regenerated `packages/shared-types/openapi.json` and `apps/ui/src/api/schema.ts` with new endpoint types
- **API Endpoints Added:**
  - `POST /api/integrations/docker/containers/{name}/pull-and-restart` — gated by `require_user_or_token({Scope.DOCKER_WRITE})` (Session OR API token with scope). Body `{"confirm_phrase": "pull"}` (case-insensitive). Returns 202 + `{action_id, state}`. 400/403/404/503 error paths covered.
  - `GET /api/integrations/docker/compose-actions/{id}` — single action detail (stdout/stderr included). Session auth.
  - `GET /api/integrations/docker/compose-actions?container={name}&limit=N` — recent actions list. Session auth.
- **Placeholders Added:** none for future stages; this stage filled the Recent Actions slot from STAGE-003-003 (drill-down only, not main listing — the obsolete main-listing test was removed)
- **Session Notes:**
  - The 2nd scribe run (WUs 5-10) was truncated mid-edit, leaving an indentation error in `resolve_compose` + 94 pyright + 21 ruff errors. Recovery: focused fixer for the indentation, then debugger-lite → fixer cascade for systematic remediation (rename `_truncate`/`_OUTPUT_MAX_BYTES`/etc. to drop unnecessary private prefixes, async-ify `resolve_compose` to match the async `inspect_container`, AsyncMock the test fixtures, type-annotate test helpers, swap raw httpx mocks to include `/containers/<name>/json` inspect responses with compose labels). Net effect: 4 fixer cycles to land green; final state is clean (0 ruff, 0 pyright, 100% kernel coverage).
  - Bumped uvicorn fixture startup timeout from 10s → 20s in `apps/monitor/tests/_uvicorn_fixture.py` after diagnosing the SSE test flake under parallel test load. Unrelated to STAGE-003-010 functionality.
  - Removed one obsolete UI test in `DockerIntegrationPage.test.tsx` that asserted "Recent actions" on the main listing — that was a STAGE-003-003 placeholder slot; STAGE-003-010 correctly relocates the panel to the drill-down (requires `containerName`).
  - All locked Design decisions held through Build (D-COMPOSE-AWARE, D-INSIDE-MONITOR, D-CONFIRM-API-SIDE, D-CONFIRM-EXTENDS-SPEC-7-2, D-AUDIT-EVERY-ATTEMPT, D-COMPOSE-ACTIONS-OWN-TABLE, D-SOCKET-RW-WIDEN, D-AUTO-UPDATE-OFF). ADDENDUM Q2 (label-based resolver) implemented and tested.
  - Final `make verify`: PASS. Backend 3053 passed, 1 skipped, 100.00% kernel coverage. Frontend 452 passed; UI build 488ms; prettier clean.

**Status**: [x] Build (Complete)

## Refinement Phase

- [x] Desktop Approved (confirm modal + Pull & Restart succeeds + Recent Actions panel populates)
- [x] Mobile Approved (modal + panel mobile layouts)
- [x] Regression Items Added
- **Feedback Round 1:** Host-integration stage → Refinement was **3a (dev rig — synthetic test) AND 3b (prod — real foundry + ip-update containers)**.
- **Session Notes:**
  - 3a setup: brought up dev rig (`make dev`); confirmed UI loads + login works + POST /pull-and-restart endpoint is wired (HTTP 403 CSRF on direct curl confirmed route registered, not 405).
  - 3a UI validation: skipped formal Desktop/Mobile inspection at dev-rig stage because real action execution requires the prod compose stack. Moved directly to 3b.
  - 3b production rig: rebuilt monitor image with new docker CLI + buildx plugin + docker-compose-plugin; widened docker socket mount from RO → RW; added /host-compose:ro mount; added HM_HOST_COMPOSE_GID group_add; added HOME=/tmp + DOCKER_CONFIG=/tmp/.docker env vars; mounted /storage/programs:/storage/programs:ro via auto-generated docker-compose.override.yml.
  - **Host-side prep**: `scripts/host-setup.sh` extended to provision `homelab-compose` group + apply 0640 perms to /storage/docker/compose/.env. User ran via sudo. Group config externalized to `scripts/host-setup-shared-files.conf`.
  - **Generated override mechanism**: `scripts/generate-compose-override.sh` reads build-sources.yaml's build_context_roots and writes docker-compose.override.yml with bind mounts. Operator runs `make generate-build-mounts` when build-sources.yaml changes. Document path requires explicit `-f deploy/compose/docker-compose.override.yml` when invoking docker compose (auto-loading disabled by `-f` of the main file).
  - **Acceptance tests passed**:
    - **Remote-image** (foundry): Pull & Restart succeeded in 1.43s. Image pulled, container recreated with new digest, badge cleared, toast appeared.
    - **Locally-built** (ip-update): Rebuild & Restart succeeded in 19.45s. Build context resolved correctly inside container, baseline source hash reset post-rebuild, badge cleared, toast appeared.
- **Issues surfaced during 3b + fixed in this stage** (5 cascading waves of fixes):
  - **Wave 1 (5 fixes)**: phase column (`pulling`/`restarting`), 409 concurrency guard, optimistic UI bridge, image-update cache invalidation, post-success digest recheck for REMOTE images.
  - **Wave 2 (1 fix)**: `.env: permission denied` → host-side `homelab-compose` group + container group_add + `chmod 0640`. Generic mechanism via `scripts/host-setup-shared-files.conf`.
  - **Wave 3 (3 fixes)**: `up -d` container name conflict → added `--force-recreate`. Failed-terminal toast → wired toast.error in ActionsCell. Confirm phrase mismatch → kept "pull" for all action types.
  - **Wave 4 (4 fixes)**: docker compose project-name mismatch → extract from container's `com.docker.compose.project` label + pass `-p <name>` to compose. Build vs pull detection for locally-built containers → new `building` phase + migration 0030 + `Rebuild & Restart` button label. Build context path mismatch → generate `docker-compose.override.yml` from build-sources.yaml. Docker CLI HOME permission → set HOME=/tmp + DOCKER_CONFIG=/tmp/.docker in compose env. Add `docker-buildx-plugin` to Dockerfile (warning cleanup).
  - **Wave 5 (1 fix)**: local-build refresh_container — `image_update_collector.refresh_container` only updated `image_update_state` (remote-image table); locally-built containers' badges driven by `docker_build_hashes` needed a parallel refresh path. Added `LocalBuildUpdateCollector.refresh_container` + branched dispatch in compose_action_runner based on `resolved.is_local_build`.
  - User-introduced diagnostic noise: marker comment appended to ip-update/Dockerfile inline with CMD broke JSON-array parsing → restored to separate line.
- **All 5 waves verified**: final `make verify` exit 0; 3097 backend tests, 100% kernel coverage, 456 frontend tests, build 679 KB gzipped, no ESLint errors (35 pre-existing warning baseline maintained).

**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests) — 21 findings (3 Critical, 8 Important, 5 Minor, 5 Nice-to-have); ALL addressed.
- [x] Tests Written (during Build + Refinement; coverage gates met).
- [x] Code Review (post-tests) — skipped: only mechanical reverts (M5 process-group → asyncio API, migration 0028 SQL comment) and trivial format/typing fixes after first review. No new logic.
- [x] Documentation Updated (CLAUDE.md `make generate-build-mounts` row, `docs/dev/local-environment.md` "Build-context mounts" section, regression.md items).
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: `4e46d5f`
- Changelog: `2d20345`
- Tracking: `<TBD>`

**CHANGELOG Entry**: [x] Added (`changelog/2026-05-26.changelog.md`)

### Notable Finalize finding (root-cause for refinement-time SSH/tmux crashes)

The M5 code-review fix (process-group signaling via `os.killpg(proc.pid, SIGTERM)`)
introduced a critical test-suite footgun: under test, `proc` is a `MagicMock` and
`int(MagicMock().pid)` returns `1`. So `os.killpg(1, SIGTERM)` was sending SIGTERM
to PGID 1 — your **user@1000.service** systemd manager — killing tmux, Claude,
and the SSH session immediately whenever the timeout test fired. This explained
3 distinct apparent "OOM" events that were actually deliberate (per the journal
`code=killed, status=9/KILL` lines). Reverted to `proc.terminate()` /
`proc.kill()` (the asyncio subprocess API), which only signals the subprocess
itself. No more crashes; full `make verify` runs clean.

**Status**: [x] Finalize (Complete)
