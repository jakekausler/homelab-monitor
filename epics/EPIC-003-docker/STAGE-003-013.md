# STAGE-003-013: Container drill-down unification + integration-page simplification

## Status

Status: Complete (2026-05-27)

## Goal

Unify all container-level information and actions into a single per-container drill-down page with tab navigation. Slim the Docker integration page to a compose-grouped list with badges and resource metrics. All container actions (Pull & Restart, probe management, logs viewing, image update review) move out of the integration page proper into the per-container drill-down.

This is a structural refactor — no new backend endpoints required. All existing routes survive; a new parent route is added to host shared layout + tab navigation.

## Phase Status

- [x] Design (Complete 2026-05-27)
- [x] Build (Complete 2026-05-27)
- [x] Refinement (Complete 2026-05-27, Desktop + Mobile approved)
- [x] Finalize (Complete 2026-05-27)

## Success Criteria

1. Integration page (`/integrations/docker`) renders a compose-grouped list. Each compose file's containers appear under a non-collapsible header showing the compose file's parent directory basename. Containers WITHOUT a compose file appear under an "Ungrouped" header. Within each group, containers sort alphabetically by name.
2. Each list row shows: container name, image (subtitle), 4 status badges (status, image-update, probes, healthcheck) + restart-count chip (24h, only if >0) + CPU% + RAM MiB + a small "→" affordance indicating the row is clickable.
3. Clicking a row navigates to `/integrations/docker/containers/$name` (the new parent route).
4. New parent route renders a shared sticky header (name + status badge + image as subtitle + back-link to integration page) and a horizontal scrollable tab strip with 4 tabs: **Overview**, **Probes**, **Logs**, **Recent Actions**.
5. Default child route (when user visits `/$name` with no specific tab) redirects to `/$name/overview`.
6. **Overview tab** contains: image update detail (current/latest digest, source, timestamps, error reasons) + Pull & Restart button (visible only when `update_available === true`) + container metadata block (image, status, exposed ports, healthcheck command, compose info) + restart/exit history block + CPU & RAM block with 15-min sparklines.
7. **Probes tab** contains the existing `ContainerProbesCard` content (active + suggested + add new), styled to fit the tab's layout.
8. **Logs tab** contains the existing `DockerContainerLogsViewer` content.
9. **Recent Actions tab** contains the existing `RecentActionsPanel` content.
10. All four tabs share the page header + tab strip; switching tabs is fast (route-level navigation; React Query caches persist across tab switches).
11. Mobile (< sm breakpoint): horizontal scrollable tab strip; sticky header collapses to name + status badge; tab content is full-width.
12. All existing routes `/probes`, `/image-update`, `/logs` continue to work (children of the new parent). `/image-update` is RENAMED to `/overview` per the new structure (the image update detail moves into Overview). All internal links to `/image-update` get updated.
13. Old `DockerIntegrationPage` sections (the desktop grid, the mobile cards, the ProbesPanel section) are DELETED. Old `ContainerGridRow.tsx` + `ContainerGridCard.tsx` + `ContainerGrid.tsx` are DELETED.
14. The ProbesPanel (just shipped in STAGE-003-012) is REMOVED from the integration page and its content (`ContainerProbesCard`) becomes the Probes tab on the unified per-container page.
15. `make verify` passes green with 100% backend kernel coverage maintained and UI coverage gates met.

## Concrete Deliverables

### 14.1 New + modified components — Integration page

| File | Action | ~LOC | Purpose |
|------|--------|------|---------|
| `apps/ui/src/routes/integrations/DockerIntegrationPage.tsx` | MODIFY | -150 / +50 | Strip grid + probes panel; render new `<ContainerList>` only |
| `apps/ui/src/routes/integrations/ContainerList.tsx` | CREATE | ~120 | Wraps `useListContainers()`; groups by compose; renders headers + `<ContainerListRow>` |
| `apps/ui/src/routes/integrations/ContainerListRow.tsx` | CREATE | ~100 | Row with name + image + 4 badges + restart-chip + CPU/RAM + "→". Entire row is a `<Link>` |
| `apps/ui/src/routes/integrations/ContainerGrid.tsx` | DELETE | -50 | Replaced by ContainerList |
| `apps/ui/src/routes/integrations/ContainerGridRow.tsx` | DELETE | -200 | Replaced by ContainerListRow |
| `apps/ui/src/routes/integrations/ContainerGridCard.tsx` | DELETE | -150 | Same row works for both viewports |
| `apps/ui/src/routes/integrations/ProbesPanel.tsx` | DELETE | -50 | Moves to Probes tab on per-container page |

Notes:
- ContainerListRow renders the SAME on desktop and mobile (responsive with stacked metadata on narrow viewports). Replaces the desktop-table / mobile-card duplication.
- Compose grouping uses `extractComposeBasename(c.compose_file_path)` from `composeBasename.ts`. Containers with null `compose_file_path` go into an "Ungrouped" group. Group order: alphabetical by basename. Within group: alphabetical by container name.
- The 4 badges and restart-chip ONLY render when state warrants. Healthcheck badge only when `container.healthcheck !== null`. Restart chip only when `restart_count_24h > 0`. Image-update badge only when an update is available (reuse `<ImageUpdateBadge>`).
- The "→" affordance is a small chevron icon at the right of the row.

### 14.2 New + modified components — Per-container unified page

| File | Action | ~LOC | Purpose |
|------|--------|------|---------|
| `apps/ui/src/routes/integrations/ContainerPage.tsx` | CREATE | ~150 | Parent route: shared header + tab strip + `<Outlet>` |
| `apps/ui/src/routes/integrations/ContainerOverviewTab.tsx` | CREATE | ~250 | Overview tab content: image update + Pull&Restart + metadata + restart history + CPU/RAM with sparklines |
| `apps/ui/src/routes/integrations/ContainerProbesTab.tsx` | CREATE | ~50 | Thin wrapper around `<ContainerProbesCard>` for in-tab rendering |
| `apps/ui/src/routes/integrations/ContainerLogsTab.tsx` | CREATE | ~50 | Thin wrapper around `<DockerContainerLogsViewer>` (or refactor existing viewer to accept the layout context) |
| `apps/ui/src/routes/integrations/ContainerActionsTab.tsx` | CREATE | ~50 | Thin wrapper around `<RecentActionsPanel>` |
| `apps/ui/src/routes/integrations/ContainerProbesPage.tsx` | DELETE | -100 | Replaced by ContainerProbesTab (the route moves to be a child of ContainerPage) |
| `apps/ui/src/routes/integrations/ContainerImageUpdatePage.tsx` | DELETE | -150 | Content moves into ContainerOverviewTab |
| `apps/ui/src/routes/integrations/DockerContainerLogsViewer.tsx` | MODIFY | -20 (refactor to a body component, drop its own page wrapper) | Accept layout context (no own sticky header / back-link; parent provides) |
| `apps/ui/src/routes/integrations/RecentActionsPanel.tsx` | MODIFY (small) | +0 | Already a panel; no major change. Move from being imported by ContainerImageUpdatePage → imported by ContainerActionsTab |
| `apps/ui/src/components/tiles/Sparkline.tsx` | REUSE | +0 | Already exists per the look-back report. Use for CPU/RAM sparklines in Overview |

### 14.3 New tabs nav component

| File | Action | ~LOC | Purpose |
|------|--------|------|---------|
| `apps/ui/src/routes/integrations/ContainerTabsNav.tsx` | CREATE | ~80 | Horizontal scrollable tab strip with active-state highlighting; renders 4 `<Link>` items |

The tab strip uses TanStack Router's `<Link>` with `activeProps` to highlight the active tab. The 4 tabs are: Overview, Probes, Logs, Recent Actions (in that order, locked decision).

### 14.4 Router changes

In `apps/ui/src/router.tsx`:

```typescript
// NEW: parent route for per-container drill-down
const containerPageRoute = createRoute({
  getParentRoute: () => protectedLayoutRoute,
  path: '/integrations/docker/containers/$name',
  component: ContainerPage,
})

// MODIFIED: existing routes become children
const containerOverviewRoute = createRoute({
  getParentRoute: () => containerPageRoute,
  path: 'overview',
  component: ContainerOverviewTab,
})

const containerProbesRoute = createRoute({
  getParentRoute: () => containerPageRoute,
  path: 'probes',
  component: ContainerProbesTab,
})

const containerLogsRoute = createRoute({
  getParentRoute: () => containerPageRoute,
  path: 'logs',
  component: ContainerLogsTab,
})

const containerActionsRoute = createRoute({
  getParentRoute: () => containerPageRoute,
  path: 'actions',
  component: ContainerActionsTab,
})

// Index redirect: visiting /containers/$name with no sub-path → /overview
const containerIndexRoute = createRoute({
  getParentRoute: () => containerPageRoute,
  path: '/',
  beforeLoad: () => redirect({ to: '/integrations/docker/containers/$name/overview', from: ... })
})

// Or use a simpler redirect component if router redirect API is tricky
```

The old `/image-update` route is RENAMED to `/overview` — internal link updates required (see §14.5).

### 14.5 Internal link updates

Five locations point to old child routes. All migrate to the new structure:

1. `ContainerListRow.tsx` (NEW) — link to `/integrations/docker/containers/$name/overview` (the default tab)
2. ImageUpdateBadge.tsx — link from `/image-update` → `/overview` (the new home of image-update detail)
3. ProbesBadge.tsx (two locations) — link from `/probes` → `/probes` (path stays the same, but parent route changed)
4. DockerContainerLogsViewer's back-link — back-link target unchanged (still `/integrations/docker`)
5. Old `containerImageUpdateRoute` deleted entirely from router.tsx

### 14.6 Backend changes

**NONE.** All required data is already exposed via existing endpoints:

- `GET /api/integrations/docker/containers` — container list with metadata, healthcheck, restart counts, CPU/RAM, compose info
- `GET /api/integrations/docker/containers/{name}/probes` — active probes
- `GET /api/integrations/docker/probes/summary` — probes summary (failing counts)
- `GET /api/integrations/docker/containers/{name}/image-update` — image update detail (for Overview tab)
- `GET /api/integrations/docker/image-updates/summary` — update-available summary
- `GET /api/integrations/docker/containers/{name}/logs` — log viewer
- `GET /api/integrations/docker/compose-actions` — recent actions
- `POST /api/integrations/docker/containers/{name}/pull-and-restart` — Pull & Restart action
- `GET /api/metrics/range` (or similar VictoriaMetrics proxy) — for the CPU/RAM sparklines on Overview tab. NOTE: confirm during Build phase that a metrics-range query endpoint exists.

**Potential backend gap to confirm at Build start**: the CPU/RAM 15-min sparkline needs a time-range metrics query. Look for an existing `useMetricsRange()` hook or VictoriaMetrics proxy endpoint. If absent, the simplest fallback is to skip the sparkline this stage and display only current values (matches the "current values only" fallback). DO NOT block the stage on adding new metrics endpoints — defer to a follow-up if needed.

### 14.7 Tests

| Test file | Action | Coverage target |
|-----------|--------|-----------------|
| `__tests__/ContainerList.test.tsx` | CREATE | groups by compose, ungrouped fallback, empty state, error state |
| `__tests__/ContainerListRow.test.tsx` | CREATE | name + image render, all 4 badges, restart chip conditional, CPU/RAM rendering, full-row link |
| `__tests__/ContainerPage.test.tsx` | CREATE | parent layout renders header + tabs + Outlet; index redirect to overview; mobile tab scrolling |
| `__tests__/ContainerTabsNav.test.tsx` | CREATE | active-tab highlighting, 4 tab links, horizontal-scrollable styling |
| `__tests__/ContainerOverviewTab.test.tsx` | CREATE | image update render, Pull&Restart visibility logic, metadata block, restart/exit history, CPU/RAM with sparkline OR fallback |
| `__tests__/ContainerProbesTab.test.tsx` | CREATE | renders ContainerProbesCard with the right container prop |
| `__tests__/ContainerLogsTab.test.tsx` | CREATE | renders log viewer body content (without standalone page chrome) |
| `__tests__/ContainerActionsTab.test.tsx` | CREATE | renders RecentActionsPanel for the right container |
| `__tests__/ContainerGridRow.test.tsx` | DELETE | replaced by ContainerListRow tests |
| `__tests__/ContainerGridCard.test.tsx` | DELETE | replaced by ContainerListRow tests |
| `__tests__/ContainerGrid.test.tsx` | DELETE | replaced by ContainerList tests |
| `__tests__/ProbesPanel.test.tsx` | DELETE | section removed from integration page |
| `__tests__/ContainerProbesCard.test.tsx` | KEEP | component still in use (now inside ContainerProbesTab) |
| `__tests__/DockerContainerLogsViewer.test.tsx` | MODIFY | test the body component (after refactor to drop page wrapper) |

## Locked Design Decisions

### D-NESTED-ROUTES
Use nested TanStack Router routes for the unified per-container page. Parent route `/integrations/docker/containers/$name` provides shared header + tab strip + `<Outlet>`. Child routes `/overview`, `/probes`, `/logs`, `/actions` render in the Outlet. Browser back/forward + deep links work naturally. URLs are stable.

### D-COMPOSE-GROUP-NON-COLLAPSIBLE
Integration list groups containers by compose file basename. Each group has a non-collapsible header. Standalone containers (no compose) go under an "Ungrouped" group. Within group: alphabetical by container name. Between groups: alphabetical by group name. No localStorage state, no expand/collapse interaction this stage (could be added in a future iteration if user requests).

### D-FULL-ROW-LINK-WITH-CHEVRON
Each integration list row is a single `<Link>` element wrapping name + image + badges + CPU/RAM + a trailing `→` chevron icon. No inline action buttons in the list. All actions live on the unified per-container page.

### D-RESPONSIVE-LIST-ROW
ContainerListRow renders the same on desktop and mobile. Layout: name + image stacked vertically on the left; badges + CPU/RAM stacked on the right on narrow viewports, inline on wide viewports. NO separate desktop-grid vs mobile-card components.

### D-TAB-ORDER-LOCKED
4 tabs in fixed order: **Overview → Probes → Logs → Recent Actions**. Image update info lives on Overview (not its own tab). Pull & Restart button lives on Overview (only when `update_available === true`).

### D-OVERVIEW-CONTENTS
Overview tab shows (all four blocks):
1. **Image update block** — full image-update detail (source, current/latest digest, timestamps, error reasons). Pull & Restart button at the bottom of this block when `update_available === true`.
2. **Container metadata block** — image, status, exposed ports (from inspect if available), healthcheck command, compose info (project, service, file path) if applicable.
3. **Restart/exit history block** — restart_count_24h, cumulative restart_count, last exit_code, last_recreated_at timestamp.
4. **Resource usage block** — current CPU% + RAM MiB with 15-min sparklines (see D-OVERVIEW-SPARKLINES).

### D-OVERVIEW-SPARKLINES
CPU and RAM on the Overview tab include 15-minute sparklines from VictoriaMetrics. Series: `homelab_container_cpu_percent` and `homelab_container_memory_mib` (or whatever the actual metric names are — confirm at Build start). Reuse the existing `<Sparkline>` component from `apps/ui/src/components/tiles/Sparkline.tsx`. If the metrics-range query endpoint doesn't exist, Build phase falls back to current-values-only and adds a follow-up TODO for sparklines.

### D-STICKY-HEADER
Per-container page header (sticky on all tabs):
- Container name as h1
- Status badge inline next to name
- Image as muted subtitle (truncated if long)
- "← Back to Docker integration" link
- Tab strip below the header (also sticky)

Restart-count chip + healthcheck chip in header → DEFERRED to a future iteration. The integration list row already shows these badges; once user is on the per-container page, they're focused on one container's tabs.

### D-MOBILE-TAB-STRIP-HORIZONTAL-SCROLL
Tab strip is horizontally scrollable on narrow viewports. No collapse-to-dropdown. Same navigation pattern across breakpoints.

### D-OVERVIEW-IS-DEFAULT-TAB
Visiting `/integrations/docker/containers/$name` with no sub-path → redirect to `/$name/overview`. Bookmarkable; consistent landing.

### D-NO-BACKEND-CHANGES
This is a pure structural refactor. No new endpoints. No new Pydantic models. No new repo methods. All data already available via existing hooks.

### D-DELETION-LIST-LOCKED
Deletions are scoped exclusively to the integration-page surface that's being replaced:
- `ContainerGridRow.tsx`, `ContainerGridCard.tsx`, `ContainerGrid.tsx` (replaced by ContainerList + ContainerListRow)
- `ProbesPanel.tsx` (the section disappears from integration page; ContainerProbesCard moves to Probes tab)
- `ContainerProbesPage.tsx`, `ContainerImageUpdatePage.tsx` (replaced by tab components under the new parent route)
- Their test files

Everything else stays. `ContainerProbesCard`, `AddProbeModal`, `EditProbeModal`, `RecentActionsPanel`, `DockerContainerLogsViewer` (refactored to drop its own page wrapper), `ImageUpdateBadge`, `ProbesBadge`, `StatusBadge`, `HealthcheckBadge`, `Sparkline` all survive.

### D-DEFER-LIST-INLINE-ACTIONS
No inline Pull & Restart button in the integration list rows (originally an option from the brainstorm). All actions live on the per-container page. The badge tells the user "update available"; they click the row to drill in and act on it.

## Out of Scope

- New backend endpoints (the stage is a pure UI refactor)
- Persistent state for compose-group expand/collapse (groups are non-collapsible this stage)
- Container metadata that isn't already in `useListContainers()`'s response (no new fields)
- Inspect-driven data on Overview (e.g., exposed ports from docker inspect) UNLESS the existing endpoint already returns it; do NOT add a new inspect call for the metadata block. If a piece of metadata isn't available, just don't show it.
- Bulk operations (Pull & Restart all in a compose group, etc.)
- Reordering containers within a group
- Custom column visibility / per-user grid preferences
- Per-container favorites / pinning
- Container creation / removal (Docker-side operations beyond Pull & Restart)
- Settings / configuration tab on the unified page
- Notifications tab
- Live tail in the Logs tab (still deferred to EPIC-004)

## Refinement Plan

Refinement = 3a only (frontend-only stage with no host-integration delta). Desktop AND Mobile viewport approval required.

Validation flow:
1. After rebuild + prod restart, user visits `http://127.0.0.1:29090/integrations/docker`
2. Confirm: containers are grouped by compose file; each group has a header; rows show name + image + badges + CPU/RAM + chevron
3. Click a container row → navigates to `/containers/$name` → redirects to `/overview`
4. Confirm Overview tab shows all 4 blocks (image update, metadata, restart history, resource usage with sparklines)
5. Click each of the other 3 tabs (Probes, Logs, Recent Actions) → confirm content renders correctly, no console errors
6. Repeat on mobile viewport (~375px width)
7. Both viewports approved before exit gate

**Critical pre-Refinement build step (DO NOT FORGET)**: After source changes, run `pnpm --filter ui run build` BEFORE `docker compose build monitor`. The Dockerfile copies pre-built `apps/ui/dist/`. Stale dist = stale UI in container even after restart. (Documented gotcha from STAGE-003-012 Refinement.)

## Build Order of Operations

1. **Router updates first** — add parent route + children, wire to existing components as placeholders. Verify routing works (manual or unit test).
2. **ContainerPage shell** — header + tab strip + Outlet. Wire tabs to placeholder content. Verify navigation.
3. **ContainerOverviewTab** — port content from ContainerImageUpdatePage + add metadata block + restart-history block + CPU/RAM block.
4. **CPU/RAM sparklines** — verify backend endpoint exists; build sparkline integration. If endpoint missing, fall back to current values + TODO.
5. **Tab content wrappers** — ContainerProbesTab (wrap ContainerProbesCard), ContainerLogsTab (wrap DockerContainerLogsViewer's body), ContainerActionsTab (wrap RecentActionsPanel).
6. **Refactor DockerContainerLogsViewer** — extract body so it doesn't carry its own page chrome; the tab provides chrome.
7. **Integration page slim-down** — ContainerList + ContainerListRow + compose grouping logic. Delete old grid components.
8. **Link updates** — update all `Link to="/integrations/docker/containers/..."` references (ImageUpdateBadge, ProbesBadge).
9. **Test files** — new tests for new components; delete obsolete tests.
10. **Verify cycles** — make verify; fix any coverage/lint/format issues.

## Known Gotchas (carry from prior stages)

- **Stale dist bug**: `pnpm --filter ui run build` BEFORE `docker compose build monitor`. The Dockerfile copies pre-built dist/.
- **`as never` not `as any`** in vitest mock returns (ESLint enforces).
- **noUncheckedIndexedAccess**: optional chaining `arr[i]?.field` on any array indexing.
- **TanStack Router params**: typed access via `useParams({ from: ... })` or `useParams({ strict: false })`. Match existing patterns in DockerContainerLogsViewer.tsx.
- **Tests that render components with `<Link>`** need `RouterProvider` wrapper + async `findBy*` queries (NOT sync `getBy*`).
- **Coverage gates**: backend 100% kernel (no backend changes this stage; gate trivially met). Frontend functions 80% / statements 80% / branches 75%.
- **ApiError constructor signature**: `new ApiError({status, code, message, retryAfterSeconds, details})`. NOT positional args.

## Forward References

- EPIC-011 STAGE-011-001 (when opened) inherits all carry-forwards previously documented (PendingSuggestionsPanel-replacement fate, endpoint path migration, per-default-probe ignore persistence). This stage doesn't add new carry-forwards; the in-epic probes panel is now a tab on the per-container page rather than a section on the integration page, so the "remove vs link" decision shifts to "the Probes tab on /containers/$name will be subsumed when the global Suggestions inbox lands."

## Session Notes

### 2026-05-27 — Build phase complete
- All 8 new components implemented per spec: ContainerPage, ContainerOverviewTab, ContainerProbesTab, ContainerLogsTab, ContainerActionsTab, ContainerTabsNav, ContainerList, ContainerListRow.
- DockerContainerLogsViewer refactored: page chrome split out into DockerContainerLogsViewerBody (rendered inside ContainerLogsTab).
- PullRestartButton extracted from old ContainerGridRow ActionsCell logic; reused on Overview tab.
- Deletions: ContainerGrid.tsx, ContainerGridRow.tsx, ContainerGridCard.tsx, ProbesPanel.tsx, ContainerProbesPage.tsx, ContainerImageUpdatePage.tsx, DockerContainerLogsViewer.tsx + their tests.
- Router restructured: new parent route `/integrations/docker/containers/$name` with 4 child routes (overview/probes/logs/actions) + index redirect to overview.
- ImageUpdateBadge link target migrated from `/image-update` to `/overview` per D-NESTED-ROUTES (consolidated tabs).
- CPU/RAM 15-min sparklines wired via useMetricsRange (endpoint pre-existing); same pattern as HostCpuTile.
- Verify: ALL GREEN. Backend 100% kernel coverage (3182 tests). Frontend 80% functions / 77.8% branches / 83.69% statements / 85.27% lines (508 tests). ESLint baseline 35 warnings maintained. UI build clean.
- Iteration count: ~4 fix waves needed (initial tsc errors → test wrapper RouterProvider misuse → assertion mismatches → coverage push). All resolved.

### 2026-05-27 — Stage opened + Design phase complete (same session)
- Stage created as a follow-on to STAGE-003-012 (which closed EPIC-003 at 12/12). User surfaced new structural scope: unify all container actions/info into per-container drill-downs; slim integration page to a compose-grouped list with badges + CPU/RAM.
- EPIC-003 status reverted from Complete to In Progress to accommodate this work.
- Design phase ran immediately in the same session, while STAGE-003-012 context was fresh. All architectural decisions locked via AskUserQuestion:
  - Nested routes with shared parent layout (D-NESTED-ROUTES)
  - Compose-grouped non-collapsible list (D-COMPOSE-GROUP-NON-COLLAPSIBLE)
  - Full-row link + chevron, no inline actions (D-FULL-ROW-LINK-WITH-CHEVRON)
  - Same nav pattern on desktop and mobile (D-MOBILE-TAB-STRIP-HORIZONTAL-SCROLL)
  - Tab order: Overview → Probes → Logs → Recent Actions (D-TAB-ORDER-LOCKED)
  - Overview includes image update + metadata + restart history + CPU/RAM with sparklines (D-OVERVIEW-CONTENTS, D-OVERVIEW-SPARKLINES)
  - Default tab = Overview (D-OVERVIEW-IS-DEFAULT-TAB)
  - Pull & Restart lives on Overview when update available (no separate Actions tab for that)
- Build phase will execute next session.

### 2026-05-27 — Refinement phase complete (prod rig, both viewports approved)
- Validated against full prod stack at http://127.0.0.1:29090 after rebuild + restart.
- User feedback iterations during Refinement (all addressed in-phase):
  1. "Cards look bad — want more icons." → Added leading lucide-react icons to all 4 badges (Status, ImageUpdate, Probes, Healthcheck), restart chip, CPU/RAM metrics, and compose group headers (Boxes icon + count chip).
  2. "ImageUpdateBadge should be a badge, not a button — and no '—' clutter." → Refactored ImageUpdateBadge + ProbesBadge to use the canonical <Badge> component pattern (StatusBadge/HealthcheckBadge style). No more <Link> wrappers. Loading / no-data / no-active states render null instead of "—".
  3. "Rename 'Check failed' → 'Update Check Failed'" → done; aria-label updated to match.
  4. "Mobile cards need 4 stacked wrappable rows." → ContainerListRow now uses `flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between` so mobile stacks name / image / badges / metrics vertically; desktop keeps side-by-side.
  5. "Container drill-down page: content scrolls behind sticky header — make whole page scroll." → Removed `sticky top-0 z-10` + backdrop-blur from ContainerPage header; header now scrolls naturally with content.
- Final verify (post-iteration): make verify ALL GREEN. Backend 100% kernel coverage. Frontend ≥80% functions, ≥75% branches. ESLint 35-warning baseline maintained.
- Bundle live in container: confirmed via `docker exec homelab-monitor ls /app/ui/assets/` matches host `apps/ui/dist/assets/` after each rebuild cycle.
- Desktop + Mobile approved by user (2026-05-27).

### 2026-05-27 — Finalize phase complete; EPIC-003 closed (13/13)
- Pre-test code review (Opus): 0 critical, 4 important, 9 minor/style/test findings. All addressed.
  - Important: deleted dead PullRestartButton.tsx + test (extracted during Build, never imported by production code).
  - Important: fixed unmount-safe pull-restart timeout via useRef + useEffect cleanup (was leaking setState on unmounted component).
  - Important: fixed silent no-op query invalidation — was `['listContainers']` literal, now `dockerQueryKeys.containers` (real bug, would have caused row metrics to not refresh after Pull & Restart).
  - Minor: renamed `iconProps` → `iconClass` in badges.tsx; dropped redundant optional chains inside `{row && ...}` guards; renumbered "Block 3/4/5" comments to "Block 2/3/4"; extracted `PULL_RESTART_TIMEOUT_MS = 30_000`; defensive `?? []` on Map lookup in ContainerList.
  - Test gaps closed: ContainerList non-503 error path; ContainerPage Outlet render; ContainerListRow singular "1 restart (24h)".
- Post-test code review (Opus, abbreviated): ready to commit; cosmetic items non-blocking.
- Final verify: ALL GREEN. Backend 100% kernel coverage (3182 tests). Frontend 80.43% functions / 78.38% branches / 84% statements / 85.58% lines (508 tests). ESLint 35-warning baseline.
- Commit cascade:
  - Implementation: 97da570 — feat(STAGE-003-013): unified per-container drill-down + integration-page slim-down
  - Changelog: 8fb7fbc — chore: add commit hash to STAGE-003-013 changelog
  - Tracking: (this commit) — chore: mark STAGE-003-013 Complete; EPIC-003 done (13/13 stages)
