# STAGE-005-041: Grafana dashboard review & readability pass (System + Home Assistant) — interactive

## Status: Complete

## Overview

A collaborative, interaction-driven stage to make sure EVERY metric we collect is surfaced on Grafana correctly, completely, and READABLY — for BOTH the System/host-overview dashboard AND the Home Assistant dashboard. Unlike the other stages, this stage's center of gravity is a back-and-forth working session WITH THE USER: the agent inventories what metrics exist vs what each dashboard panel shows, proposes panel additions/edits/reorganizations, applies them to the dashboard JSON, rebuilds/reloads the rig so the user can view the real rendered dashboards, and ITERATES on the user's feedback until the user is satisfied that everything is surfaced and easy to read.

This stage runs AFTER the audit-gap-closure stages (034–040) so the dashboards under review already contain the new panels/data those stages add (script cadence, registry-filtered availability, etc.). It is the final polish/sign-off on observability surfacing for EPIC-005's domain and the system dashboard.

**Depends on:** STAGE-005-034 through STAGE-005-040 (all gap-closure stages — the dashboards must contain the final metric set before the readability pass). Hard dependency: this is the LAST stage; it reviews the finished surface.

## Stage Flags

- Has Input Forms: No.
- Touches secrets / production data: No (Grafana dashboard JSON + queries over existing metrics; no new data emission).
- Host-integration concerns: Yes — panels are validated against REAL metric data on the prod rig; the user visually reviews the live-rendered dashboards. Refinement is the interactive review loop itself (real rig, real data).

## Interaction model (how this stage runs — READ THIS, it is non-standard)

This stage does NOT follow the usual "build to a fixed spec then verify" shape. Its Build and Refinement phases are a SINGLE interactive loop:

1. **Inventory pass (agent):** enumerate every `homelab_*` metric family relevant to (a) the System/host-overview dashboard and (b) the Home Assistant dashboard. For each, determine: is it on the dashboard? In a readable panel (right viz type, units, thresholds, legend)? Or missing / buried / mis-typed? Produce a gap-and-readability report PER DASHBOARD.
2. **Propose (agent → user):** present the proposed changes per dashboard (new panels, panel-type changes, unit/threshold/legend fixes, row reorganization, removal of redundant/empty panels) and DISCUSS with the user — the user steers priorities and taste.
3. **Apply (agent):** edit the dashboard JSON (`deploy/grafana/dashboards/*.json`), rebuild/reload the rig so the change is live.
4. **Review (user):** the user views the real dashboards (System tab + Home Assistant tab via the in-app Metrics page, and/or Grafana directly) and gives feedback.
5. **Iterate:** repeat 2–4 until the user signs off on BOTH dashboards.

Because this is user-driven, the agent must PROMPT the user throughout (this is an explicit, sanctioned interactive stage). Both dashboards must be signed off — this is the System+HA equivalent of the Desktop+Mobile dual sign-off.

## Concrete deliverables

1. A metric→panel coverage report for the System/host-overview dashboard: every relevant `homelab_*` system/host/container metric mapped to its panel (or flagged missing/unreadable).
2. A metric→panel coverage report for the Home Assistant dashboard: every `homelab_ha_*` metric mapped to its panel (or flagged missing/unreadable). Includes the families that were Grafana-only or unsurfaced before this slate (script cadence, z-score, registry-filtered availability).
3. Dashboard JSON edits to `deploy/grafana/dashboards/` (system dashboard + `home-assistant.json`) implementing the agreed additions/edits/reorganizations: correct panel types, units, thresholds, legends, row grouping, and removal of redundant/empty panels.
4. Each edited dashboard validated rendering REAL data on the prod rig and visually signed off by the user (both dashboards).
5. Regression notes capturing the agreed "every metric is surfaced and readable" invariant per dashboard.

## Decisions in this stage (Design phase will confirm)

- D-DASH-SCOPE — confirm exactly which dashboards are in scope (host-overview/system + home-assistant; any others the user wants, e.g. a dedicated logs or alerts dashboard).
- D-DASH-COMPLETENESS-BAR — what "everything surfaced" means: every metric family on a panel, vs a curated subset with the rest reachable via explore/ad-hoc (user's call on signal-vs-noise).
- D-DASH-READABILITY-RUBRIC — the readability criteria to apply (panel type fit, units, thresholds with sensible colors, legend clarity, row organization, mobile/narrow rendering of the embedded dashboards).
- D-DASH-INTERACTION-CADENCE — confirm the interactive loop format with the user (how changes are batched per review round).

## Design Phase

**Status**: [x] Design (Complete)

### Design Notes

Interactive stage. All four decisions confirmed WITH the user (sanctioned prompt exception):

- **D-DASH-SCOPE**: Both dashboards in scope — `host-overview.json` (System) and `home-assistant.json` (HA). Starting with System.
- **D-DASH-COMPLETENESS-BAR**: Each dashboard should surface all the meaningful (non-internal) data it can — not a curated subset. Prometheus internals (`*_created`, histogram `_sum`/`_count`) excluded from dedicated panels.
- **D-DASH-READABILITY-RUBRIC**: Right viz type for the data shape; correct units (bytes/percent/seconds → human units); sensible thresholds with colors; clear legends (friendly labels, not raw series); logical row grouping; titles readable at a glance; dense timeseries broken up so they are not unreadable "blobs."
- **D-DASH-INTERACTION-CADENCE**: User reviews the live-rendered dashboards (bind-mounted Grafana, ~30s hot-reload) and iterates with the agent until dual sign-off.

**SCOPE EXPANSION (user directive):** This stage is no longer Grafana-only. During the System-dashboard review the user identified that the disk panel (`homelab_host_disk_bytes`) shows ~19 meaningless container-internal mountpoints (the metric is collected inside the `homelab-monitor` container, so it labels by container bind-mount paths — none named `/` or `/storage`). The user directed fixing BOTH the dashboard AND the collector in THIS stage (not deferring the collector to a new stage).

- **Collector fix** (`apps/monitor/homelab_monitor/plugins/collectors/builtin/host.py`, `_collect_disk_usage()` + `HostCollectorConfig`): add a `disk_mountpoint_relabel: dict[str, str]` config field, default `{"/config": "/", "/host-compose": "/storage"}` (the project's own compose bind-mount conventions: `/config` = host `/`, `/host-compose` = host `/storage`). When the map is non-empty, emit disk metrics ONLY for mountpoints in the map, relabeled to the friendly host path. Empty map = emit-all (preserves current behavior; open-source-safe). Baked as the default in `HostCollectorConfig` because no live per-collector config injection path exists yet (the "STAGE-014" YAML-config TODO in the code is a dangling/aspirational reference with no owning tracked stage; the existing `extra_mountpoints`/`exclude_disk_devs` defaults use the same baked-default pattern).

**System (host-overview) dashboard round-1 edits (agreed with user):**
1. CPU: split the single messy 17-line panel into two — "CPU % (all)" (`homelab_host_cpu_percent{cpu="all"}`, single line) and "CPU % per core" (`homelab_host_cpu_percent{cpu!="all"}`, legend `core {{cpu}}`).
2. Load average: unchanged (user: "this one is good").
3. Memory: replace the "No data" gauge with a timeseries — `used` (`homelab_host_memory_bytes{type="used"}`) + swap-used (`homelab_host_swap_bytes{type="used"}`) + `total` (`homelab_host_memory_bytes{type="total"}`) as a flat reference line; unit bytes (IEC). (No `available` series — headroom is visible from the gap to the total line. VM only emits available/total/used for memory and total/used for swap; no cached/buffers/virtual series exist.)
4. Disk: bargauge showing percent-full, reading the relabeled series (`/` and `/storage` only — no `/rackstation`); thresholds amber >85, red >95.
5. Network: drop loopback (`iface!="lo"`), separate rx/tx as download/upload (`rate(homelab_host_net_bytes_total[5m])` is correct — the metric is a monotonic counter), legend by direction/interface, unit bytes/sec.
6. Uptime: friendly format via Grafana `dtdurations` unit (renders "X weeks Y days Z hours …"; Grafana has no native strict two-tier rollup — `dtdurations` accepted by the user).

HA dashboard review (round 2) deferred until after System sign-off — same interactive loop.

## Build Phase

**Status**: [x] Build (Complete)

### Build Notes

Implemented in three files:
- `apps/monitor/homelab_monitor/plugins/collectors/builtin/host.py` — added `disk_mountpoint_relabel: dict[str, str]` field to `HostCollectorConfig` (default `{"/config": "/", "/host-compose": "/storage"}`); read in `run()` via getattr; threaded into `_collect_disk_usage()`, which now allowlists+relabels mountpoints when the map is non-empty (empty = emit-all, prior behavior). Extracted a `_emit_disk_mountpoint()` helper to keep branch count under ruff PLR0912 while preserving the error asymmetry (partitions-loop failures recorded; extra_mountpoints failures silently skipped). `disk_usage()` still called with the raw mountpoint; only the emitted label is the friendly value; `seen` dedups on friendly labels.
- `apps/monitor/tests/test_host_collector.py` — added `disk_mountpoint_relabel={}` to 8 existing disk tests (they assert raw `/` labels which the new default map would filter); added 4 new tests (default allowlist+relabel, empty-map emit-all, allowlisted-key disk_usage failure handled, extra_mountpoint-in-map emitted).
- `deploy/grafana/dashboards/host-overview.json` — System dashboard round-1 edits: CPU split into "CPU usage (all cores)" (id=1, `cpu="all"`) + new "CPU usage per core" (id=7, `cpu!="all"`); Load average moved to y=8; Memory gauge→timeseries "Memory & swap" (RAM used + Swap used + RAM total dashed reference line, unit bytes); Disk bargauge thresholds 85/95 + relabeled `/`+`/storage` series; Network expr `iface!="lo"`; Uptime unit `dtdurations`. Full re-layout, jq-validated, no gridPos overlaps (7 panels).

Verification: `make verify` PASSED — backend 4913 pass / 1 skip / 100% kernel coverage (4 new disk tests); vitest 1527; eslint 94 warnings baseline; pyright + ruff + tsc clean. Dashboard JSON edits validated by jq; live-render validation happens in Refinement.

## Refinement Phase

- Interactive dual sign-off: System dashboard AND Home Assistant dashboard (the System+HA analogue of Desktop+Mobile).
- [x] System dashboard reviewed & approved
- [x] Home Assistant dashboard reviewed & approved
- [x] Regression Items Added

**Status**: [x] Refinement (Complete)

### Refinement Notes

Interactive dual sign-off (System + HA dashboards) plus a user-directed scope expansion. The stage grew well beyond Grafana-only into a multi-dashboard + frontend deliverable:

**System (host-overview) dashboard** — iterated to user sign-off: CPU split (all / per-core with numeric `sort_by_label_numeric` legend ordering); Load split into "Load average (1m)" + "Load average (1m/5m/15m)" (ordered legends); Memory & swap timeseries with total reference line; Disk bargauge percent-full reading the relabeled `/`+`/storage` series (collector fix); Network split into separate "Network download" + "Network upload" charts, each rate[1m]/[5m]/[15m]; Uptime dtdurations. (irate[30s]→rate[1m] after the user observed irate sawtooth was a sampling artifact; `\d`→`[0-9]` after Grafana's parser 422'd on the backslash escape.)

**FOUR new Grafana dashboards + Metrics-page tabs** (user-directed: surface the non-host metric families): `containers.json` (uid containers), `collectors.json`, `heartbeats.json`, `storage-logs.json` — each embedded as its own tab inserted BETWEEN System and Home Assistant (order: System, Containers, Collectors, Heartbeats, Storage & Logs, Home Assistant). New tab components + routes + TABS entries + tests. Timeseries-first, no tables/bare-number-stats. Established the reusable BINARY UP/DOWN stat pattern (green "Up"/red "Down" word, no digit) on the Storage & Logs "VictoriaLogs" panel.

**Home Assistant dashboard rework** — to user sign-off: id=2 "HA Up"→"Status" binary Up/Down; new "WebSocket" binary Up/Down; new "Registered entities" + "Integrations not loaded" stats; "Entities Unavailable" and "Stale Entities" converted to timeseries; **fixed the `sum(homelab_ha_entity_available == 0)` always-0 bug → `count(...== 0)` (now correctly shows ~198, matching the Status page)**; "Low/Critical Batteries" bargauge → "Batteries below 20%" timeseries; "Collector Run Health" (always-empty error-age/failure queries) → "HA collector run rate" (11 ha_* collectors); count stats threshold-colored (green 0 / amber <10 / red ≥10; Config Entry Errors + Integrations not loaded green 0 / red ≥1); full grid reflow.

Refinement bugs fixed in-loop: Grafana 422 on `\d` regex (→`[0-9]`), legend ordering (→`sort_by_label_numeric`), iframe-cache staleness (open iframe URL directly + hard refresh; `:13000` is the dev-rig port and not running — prod Grafana is only reachable via `:29090`), network sawtooth (irate→rate[1m]), and the invalid Grafana `color.mode:"none"` (→`"fixed"`) that blocked HA dashboard load.

All changes validated against REAL data on the prod rig (VM queries + Grafana API). Dashboards bind-mounted (30s provisioning poll); new tabs required a UI rebuild (served bundle `index-n6HTTbev.js`).

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: c74d2a3
- Changelog: 6c912c6
- Tracking: (this commit)

**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize (Complete)

### Finalize Notes

Two Opus code-review passes (the second triggered because dashboard logic + a frontend file changed after the first review). Both approved. Findings dispositioned:
- Binary Up/Down stat panels (HA Status, HA WebSocket, storage-logs VictoriaLogs) changed from `color.mode: "fixed"` (renders the Up/Down text but a static background) to `color.mode: "thresholds"` (steps null→red, 1→green) so the background actually renders green/red. User re-confirmed live.
- Mobile tab overflow: the Metrics tab nav now uses `overflow-x-auto` + `-mx-2` (matching the codebase `ContainerTabsNav` pattern) so 6 tabs scroll horizontally instead of forcing page-wide overflow. User confirmed on mobile.
- Disk-collector non-empty default and CPU legend zero-padding: reviewed as intended deliverables, no action.

`make verify` PASSED (backend 4913 pass / 1 skip / 100% kernel coverage; vitest 1531; eslint 94 warnings baseline; pyright/ruff/tsc clean). 3-commit flow: impl c74d2a3, changelog 6c912c6, tracking (this commit).
