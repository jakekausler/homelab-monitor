# STAGE-005-038: Script-cadence Grafana panel + automation/script cadence API + Status-tab widget

## Status: Complete

## Overview

`homelab_ha_script_last_triggered_seconds` has NO Grafana panel, NO alert, NO API, NO UI (the Grafana dashboard has automation idle/disabled/oldest panels at lines 682/741/800 but nothing for scripts). Automation last_triggered/enabled exist only on Grafana — there is no integration-panel API or UI for cadence. This stage surfaces cadence beyond Grafana. At MINIMUM it adds the missing script-cadence Grafana panel (parity with the automation panels). OPTIONALLY it adds a cadence detail endpoint (mirroring `/entities` / `/batteries`) and a Status-tab widget/drill listing idle/disabled automations + idle scripts. Because the alert noise from disabled automations is fixed by STAGE-005-034, this stage MUST be numbered after 034 so the idle data it surfaces is clean.

**RECOMMENDED SPLIT.** The Grafana-panel work is small (config-only); the cadence API + Status-tab widget is substantial (new endpoint, response models, OpenAPI regen, new widget + drill + tests). If Design confirms the API+UI scope, SPLIT this into two stages:
- **STAGE-005-038a** (this number) — Grafana script-cadence panel only (config; 3a+3b). Small, ships parity immediately.
- A FOLLOW-ON stage (next free number at split time) — cadence detail endpoint + Status-tab widget/drill (backend 3a+3b + frontend 3a Desktop+Mobile).

The split decision is made in this stage's Design (D-CADENCE-SURFACE-SPLIT). The card below covers the full scope; if split, the API+UI deliverables move to the follow-on card. (NOTE: splitting bumps N from 7 to 8 and the epic total from 40 to 41 — the later session updates the counter math accordingly and inserts the follow-on row.)

**Depends on:** STAGE-005-034 (clean idle data)

## Stage Flags

- Has Input Forms: No.
- Touches secrets / production data: No (script/automation entity_ids + ages; same posture as existing cadence metrics).
- Host-integration concerns: Yes for the backend/Grafana portion — Grafana panel + cadence API validated against real script/automation metrics; Refinement 3a + 3b. The widget portion (if not split) → 3a Desktop + Mobile.

## Concrete deliverables

**Minimum (always):**
1. Grafana: add a "Idle Scripts" (and/or "Oldest-Triggered Scripts") panel to `deploy/grafana/dashboards/home-assistant.json` over `homelab_ha_script_last_triggered_seconds`, mirroring the automation panels (Idle Automations >24h line 682, Oldest-Triggered Automations line 800). Place in the existing "Automation Cadence" row (line 669) or a renamed "Automation & Script Cadence" row. Validate it renders against real script metrics in 3b.

**Optional (confirm scope in Design; move to follow-on if split):**
2. Cadence detail endpoint (e.g. `GET /api/integrations/home-assistant/cadence`) returning typed rows for automations (entity_id, enabled, last_triggered_age) + scripts (entity_id, last_triggered_age), sourced from VM per-series gauges (mirror `_sample_float` / the `/entities` VM-selection pattern in the endpoint router). New response models → OpenAPI regen → schema.ts → drill test-mock cascade (the 031 schema-cascade gotcha applies).
3. Frontend Status-tab widget + drill (mirror `HaEntitiesDrill` / `HaUpdatesDrill`): list idle (>threshold) and disabled automations + idle scripts. New `useHomeAssistantCadence` hook in `apps/ui/src/api/home_assistant.ts`; new `types.ts` exports; widget + drill + tests.

## Decisions in this stage

### Design phase will confirm

- D-CADENCE-SURFACE-SPLIT — ship Grafana-panel-only (this stage) and defer API+UI to a follow-on stage, OR do all three here? (Lean: SPLIT — the API+UI is substantial enough to be its own session-sized stage; the Grafana panel ships parity immediately and is config-only.)
- D-CADENCE-API-SOURCE — cadence rows from VM per-series (like `/entities`) vs live-HA re-query. (Lean: VM, consistent with the locked D-DETAIL-CONSUMES-VM.)
- D-CADENCE-IDLE-THRESHOLD — what "idle" threshold the widget/panel uses (the existing Grafana automation panel uses 24h; reuse it).
- D-CADENCE-SCRIPT-DISABLED — scripts have no enabled flag (per 034) — the script row carries only idle-age, no enabled column. Confirm.

## Design Phase

**Status**: [x] Design (Complete)

### Design Notes

**D-CADENCE-SURFACE-SPLIT → SPLIT (locked).** STAGE-005-038 ships the script-cadence **Grafana** panel ONLY (config-only; real-HA 3a+3b; no user prompt). The cadence detail API + Status-tab widget are split into a NEW card **STAGE-005-042** (epic denominator bumped 41 → 42). Rationale: the API+UI is ~8 files / ~600 LOC + the OpenAPI schema-cascade + its own dual-signoff refinement surface — session-sized on its own, and bundling it would let optional risk block the trivial parity panel. Ordering: 042 runs AFTER 041 with NO forward-dependency violation, because 041 reviews the **Grafana** dashboard surface only (its deliverables are Grafana JSON edits + coverage reports), and the in-app cadence widget is NOT a Grafana panel — it is outside 041's scope. The script-cadence Grafana panel that 041 DOES review is built here in 038, before 041. (Verified 041's deliverables are Grafana-dashboard-scoped during Design.) Boundary notes added to both the 041 epic-table row and card 042.

**D-CADENCE-API-SOURCE → VM per-series (locked, deferred to 042).** Consistent with locked D-DETAIL-CONSUMES-VM; mirror the `_sample_float` / `vm_instant_query` pattern in `integrations_home_assistant.py`.

**D-CADENCE-IDLE-THRESHOLD → 24h / 86400s (locked).** Matches the existing automation Grafana panels' `> 86400` literal so the new script panel and the (future) widget agree.

**D-CADENCE-SCRIPT-DISABLED → confirmed (locked).** Scripts have NO enabled flag (no `homelab_ha_script_enabled` metric); script cadence is idle-age-only. No disabled-scripts panel; the future cadence row model gives scripts an idle-age column only.

**Grafana panels for 038 (this stage):** add TWO panels to `deploy/grafana/dashboards/home-assistant.json`, mirroring the automation pair, into the "Automation Cadence" row (rename to "Automation & Script Cadence"):
- "Idle Scripts (>24h)" — `stat`, `count(homelab_ha_script_last_triggered_seconds > 86400)`, unit `short` (mirror automation panel id=15).
- "Oldest-Triggered Scripts" — `timeseries`, `topk(10, homelab_ha_script_last_triggered_seconds)`, unit `s`, legend `{{entity_id}}` (mirror automation panel id=17).
New panel ids 21 + 22 (highest existing id = 20). Layout: y=32 is full (id=15 x0w6, id=16 x6w6, id=17 x12w12), so the two new panels go at a NEW y=40 (stat x0 w6 h4 + timeseries x6 w12 h8) — shifting the "Anomalies & Collector Health" row (id=18) and all downstream panels (ids 18,19,20) DOWN by +8 in gridPos.y.

**Real-data verification (Design):** VM confirms `count(homelab_ha_script_last_triggered_seconds)` = 19 series; labels on each series are `entity_id`, `instance`, `job`, `service` — NO `friendly_name` label (panel legend uses `{{entity_id}}`). Grafana provisioning polls the dashboards dir every 30s (`updateIntervalSeconds: 30`, `allowUiUpdates: false`), so editing the JSON hot-reloads in ~30s — NO Grafana container restart needed.

**Scope for 038:** Grafana panel ONLY (2 panels). No backend, no API, no OpenAPI regen, no frontend. NOT a frontend stage → NO Desktop+Mobile user prompt; real-HA 3a+3b validates the panels render against the 19 real script series.

## Build Phase

**Status**: [x] Build (Complete)

### Build Notes

Edited ONE file: `deploy/grafana/dashboards/home-assistant.json`. Added two HA script-cadence Grafana panels mirroring the automation pair, renamed the cadence row, and shifted the downstream row block down to make room. Five edits:
- Renamed row id=14 title "Automation Cadence" → "Automation & Script Cadence".
- Added panel id=21 "Idle Scripts (>24h)" — `stat`, `count(homelab_ha_script_last_triggered_seconds > 86400)`, unit `short`, at gridPos {h:4,w:6,x:0,y:40} (mirrors automation panel id=15).
- Added panel id=22 "Oldest-Triggered Scripts" — `timeseries`, `topk(10, homelab_ha_script_last_triggered_seconds)`, unit `s`, legend `{{entity_id}}`, at gridPos {h:8,w:12,x:6,y:40} (mirrors automation panel id=17).
- Shifted "Anomalies & Collector Health" row (id=18) gridPos.y 40 → 48, and its children id=19 "Top Anomaly z-scores" + id=20 "Collector Run Health" gridPos.y 41 → 49. (The two new panels occupy a fresh y=40 row of height 8; the +8 shift keeps the downstream block contiguous and non-overlapping.)

**Verification:** `jq` confirms the dashboard JSON is valid (22 panels, was 20) with the intended layout (id=21/22 at y=40; id=18 row at y=48; id=19/20 at y=49; no overlaps). `make verify` fully green: backend 4908 passed / 2 skipped (both known-environmental: vector-binary-absent + scheduler hash-collision) / 100% kernel coverage; pyright 0 errors; vitest 1527 passed; eslint 94 warnings (unchanged baseline); tsc + UI build clean. No Grafana-dashboard validation test exists in the suite — JSON-structure-only change validated via `jq` + the real-rig render in Refinement.

**Scope:** Grafana panel only (config). No backend / API / OpenAPI / frontend changes (the cadence API + Status widget are STAGE-005-042). Refinement = real-HA 3a+3b (panel renders against the 19 real script series), NO Desktop+Mobile user prompt.

## Refinement Phase

**Status**: [x] Refinement (Complete)

### Refinement Notes

Backend/Grafana stage → real-HA 3a+3b validation against the running prod rig (Grafana container `homelab-grafana` healthy; dashboards dir bind-mounted `deploy/grafana/dashboards -> /var/lib/grafana/dashboards`, re-provisioned every 30s — no rebuild/restart needed). NO Desktop+Mobile user prompt (not a frontend stage). e2e-tester (report-only) results, all PASS:
- Script metric live in VM: `count(homelab_ha_script_last_triggered_seconds)` = **19** series.
- "Idle Scripts (>24h)" stat expr `count(homelab_ha_script_last_triggered_seconds > 86400)` = **15** (15 scripts idle >24h — real non-empty, meaningful at-a-glance signal).
- "Oldest-Triggered Scripts" timeseries expr `topk(10, homelab_ha_script_last_triggered_seconds)` returns 10 series with the `{{entity_id}}` legend populated (e.g. `script.enable_doorbell_motion` ≈ 414 days) and real age-in-seconds values.
- Grafana container reads the updated JSON (grep confirms "Idle Scripts" + "Automation & Script Cadence" present); dashboard uid=home-assistant active; NO provisioning errors in the last 5m of Grafana logs.

3a (synthetic/dev) is subsumed by the prod-rig real-HA check. No bugs found; no code changes during Refinement.

## Finalize Phase

- [x] Code Review (pre-tests) — Opus code-reviewer APPROVED, zero findings (JSON validity, mirror-fidelity vs id=15/id=17, gridPos shift complete/no overlaps, PromQL+units correct, split-decision artifacts consistent)
- [x] Tests Written — N/A (Grafana-config-only; no test surface, no dashboard-validation test in the suite; JSON validated via `jq`)
- [x] Code Review (post-tests) — N/A (no impl/source logic changed after first review; config-only, zero findings)
- [x] Documentation Updated — changelog entry + STAGE-005-042 split card + regression entry
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: 73184af
- Changelog: 2f91595
- Tracking: (this commit)

**CHANGELOG Entry**: [x] Added

### Finalize Notes

Grafana/config-only stage. Opus code-reviewer reviewed the full change set (dashboard JSON diff + the split-decision artifacts: STAGE-005-042 card + EPIC-005 counter/row edits) and APPROVED with ZERO findings across all 6 review areas. No tests written (no test surface for a provisioned-dashboard JSON; validity covered by `jq` + the real-rig render in Refinement). No second review pass (config-only; no impl/source logic changed after review — only the changelog/tracking edits, which do not trigger a second pass). 3-commit flow: impl `73184af` (dashboard JSON), changelog `2f91595`, tracking (this commit).

**Status**: [x] Finalize (Complete)
