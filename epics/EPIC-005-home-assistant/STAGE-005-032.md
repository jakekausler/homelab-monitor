# STAGE-005-032: HA config-entry state-distinction metric (collector change)

## Status: Not Started

## Overview

STAGE-005-027's `/config-entries?filter=error` endpoint can report that a config entry is "in error" but CANNOT distinguish setup_error from setup_retry (and migration_error / failed_unload). The `ha_config_entry` collector collapses all four non-loaded error states into a single `homelab_ha_config_entry_setup_error` gauge (value 1), so the specific state is not recoverable from VM per-series labels. 027 therefore ships `HaConfigEntryRow.state` as the coarse literal `"error"` and defers the precise distinction here.

This is a COLLECTOR-LAYER fix (not an API/live-HA fix): add a `state` label (or per-state gauges) to the `ha_config_entry` collector so VM CAN distinguish setup_error vs setup_retry vs migration_error vs failed_unload. The cardinality cost is negligible (config entries number in the tens). This keeps config-entry detail VM-sourced, consistent with the locked D-DETAIL-CONSUMES-VM.

Vertical slice: after this stage, the `ha_config_entry` collector emits the specific config-entry state as a VM label, the 027 `/config-entries` endpoint returns the precise `state`, and (if added) a vmalert rule / dashboard panel can distinguish transient retry from hard error.

## Deferred from

STAGE-005-027 Design (2026-06-13), D-DETAIL-VM-LABELS-PRESENT. The `ha_config_entry` collector (`apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_config_entry.py`, ~lines 49-69) collapses 4 error states into one gauge; the specific state is not a VM label.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (config-entry domain/title/state are operational metadata).
- Host-integration concerns: [ ] Yes — collector change validated against real HA config entries; Refinement 3a (dev) + 3b (prod, real config entries incl. a real setup_error/retry if present).

## Cross-cutting standards

- Strict pyright; `homelab_collector_run_*` self-metrics preserved; VM metric schema contract documented; the new label must not break existing vmalert rules (`deploy/vmalert/metrics/home-assistant.yaml`) that reference `homelab_ha_config_entry_setup_error`.

## Concrete deliverables

(To be detailed in this stage's Design. High level:)
1. Add a `state` label to `homelab_ha_config_entry_setup_error` (or emit per-state gauges) so the specific state is queryable. Decide label-vs-per-state in Design.
2. Update 027's `/config-entries` endpoint to return the precise `state`.
3. Keep existing vmalert rules working (the error condition must still alert).
4. Collector tests + (if endpoint changes) OpenAPI regen.
5. real-HA 3a + 3b.

## Decisions in this stage

### Design phase will confirm

- D-CFGSTATE-SHAPE — a `state` label on one gauge vs separate per-state gauges (cardinality + vmalert-compat).
- D-CFGSTATE-VMALERT — how existing alert rules adapt to the new shape.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

**Status**: [ ] Finalize (Not Started)
