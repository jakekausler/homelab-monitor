# STAGE-007-015: vmalert-METRICS rules (the ~24 metric alerts)

## Status: Not Started

## Overview

Author the Unifi metric alert rules (`deploy/vmalert/metrics/unifi.yaml`) — the ~24 alerts in the EPIC-007
catalog (5 critical / 16 warning / 5 info). Needs ONLY the Wave-B/C collectors (all present by now); it does
NOT depend on the syslog stream (those are the separate log rules in 017).

**Vertical slice:** the rules file provisions into vmalert-metrics; each rule is promtool-validated where
possible; a representative critical rule (`UnifiControllerDown`) and an anomaly rule are demonstrated firing
against the live/test data path → Alertmanager.

**Spec references:** spec §3.1 (vmalert metrics), §4.1 (metric flow). EPIC-007 "Alert catalog" (the metric
table). EPIC-005-005 (user-authored-rule machinery — these default rules coexist with user-tunable ones).
EPIC-006 STAGE-006-016 (sibling metric-rules stage; promtool idioms; the §4.1 cross-metric empty-join trap).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (rules over already-collected metrics). Refinement 3a (promtool
  validation + rule unit tests where the idiom allows) AND 3b (a rule actually fires end-to-end on the prod
  rig: collector → VM → vmalert → AM).
- Host-integration concerns: [x] Yes — rule firing validated against live metrics on the prod rig.

## Cross-cutting standards

- Severity vocab `info|warning|critical` ONLY.
- Anomaly rules use the rolling-baseline `clamp_min(K*avg_over_time(metric[window]), floor)` idiom with
  warm-up; absolute-threshold rules (down/probe) carry the load immediately.
- Avoid the §4.1 cross-metric empty-join trap (a binary op between two different metrics returning empty =
  dead rule). promtool can't eval `[w:s]` subqueries / `label_value()` — those validate via live-replay.
- Annotations carry the device/client name so an alert says WHICH device.

## Concrete deliverables

### 1. `deploy/vmalert/metrics/unifi.yaml`

The ~24 rules from the catalog:
- **Critical:** UnifiControllerDown, UnifiWanDown, UnifiGatewayDown, UnifiDeviceDown (per-device),
  UnifiDnsSteeringDrift (`dhcp_dns_primary != 192.168.2.148`).
- **Warning:** UnifiDeviceFlapping, UnifiSwitchPortErrors, UnifiPoePortFault, UnifiApHighChannelUtil,
  UnifiApLowSatisfaction, UnifiClientPoorExperience, UnifiWanLatencyHigh, UnifiWanPacketLoss,
  UnifiThreatDetected (structured alarm path — the only threat alert), UnifiDeviceHighTemp,
  UnifiDeviceHighCpuMem, UnifiTeleportDown, UnifiDhcpPoolExhaustion, UnifiSwitchUplinkSaturation.
- **Info:** UnifiFirmwareUpdateAvailable, UnifiNewClient, UnifiSpeedtestDegraded, UnifiApiSlow,
  UnifiClientCountAnomaly, UnifiSsidClientCountAnomaly.

### Deferred Implementation Details

**1. UnifiDhcpPoolExhaustion & UnifiDnsSteeringDrift rule mechanics (Deferred from STAGE-007-011 Design — Decision 2)**

The `UnifiDhcpPoolExhaustion` rule computes DHCP pool occupancy as a PromQL join: `homelab_unifi_client_count_by_network{network}` (per-network active count, emitted by STAGE-007-007 active_client.py) divided by `homelab_unifi_dhcp_pool_size{network}` (emitted by STAGE-007-011 networkconf collector). STAGE-007-011 deliberately emits pool_size ONLY; the occupancy ratio/recording-rule + the exhaustion alert live HERE. Reference `homelab_unifi_client_count_by_network`, NOT a non-existent `dhcp_pool_used` series. When the opt-in SSH lease collector (STAGE-007-012) is enabled, `homelab_unifi_dhcp_lease_count` is the AUTHORITATIVE occupancy numerator that sharpens this (active-client count is the API approximation when leases are off). Also: the `UnifiDnsSteeringDrift` rule compares the `dns` label of `homelab_unifi_dhcp_dns_primary{network,dns}` against the configured expected Pi-hole IP (192.168.2.148) and needs an `absent()` companion clause to catch a vanished metric.

**2. DPI per-counter spike clamp (Deferred from STAGE-007-009 Design — Decision 3)**

The DPI per-counter spike clamp: the `homelab_unifi_client_dpi_bytes{client,app,cat}` collector (STAGE-007-009) emits RAW cumulative bytes; the clamp for Ubiquiti's known per-counter spike bug is a PromQL safeguard that lives HERE (a `clamp_max(...)` / rate-sanity guard in the DPI-consuming rules/recording rules), NOT in the collector. Apply it in any 015 rule that computes rate()/increase() over the DPI byte series so a spurious counter jump doesn't produce a false anomaly.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — promtool validates the rules file; per-rule expressions checked (no empty-join trap); annotations name the device/client
- [ ] 3b — a critical rule (`UnifiControllerDown`) + an anomaly rule fire end-to-end on the prod rig (VM → vmalert → AM)
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation:
- Changelog:
- Tracking:

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
