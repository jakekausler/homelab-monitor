# STAGE-007-014: VPN/Teleport health collector

## Status: Not Started

## Overview

The VPN/Teleport collector — monitors WiFiman **Teleport** remote-access health (the user accesses the LAN
from their phone via Teleport). Emits Teleport session/connection state from the `stat/health` vpn subsystem
+ any Teleport-specific fields. The exact API field shape was NOT drilled in recon, so **confirming the live
field names is an explicit Design-phase step** for this stage (don't guess metric names ahead of Build).

**Vertical slice:** the collector emits `homelab_unifi_teleport_*` (sessions, up/state) against the live UDM;
visible in VM; feeds `UnifiTeleportDown`.

**Spec references:** spec §6.2, §2 Q11 (Unifi signals — VPN in scope). EPIC-007 "Metric families" (VPN/
Teleport row — "confirm field shape live at Design") + alert catalog (`UnifiTeleportDown`).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [x] Yes — live VPN/remote-access state. Refinement 3a (collector against
  a fixture of the confirmed field shape) AND 3b (real Teleport state live; exercise the active-session case
  by connecting from the phone if needed).
- Host-integration concerns: [x] Yes — real UDM API.

## Cross-cutting standards

- `unifi` group; 60s cadence.
- **Design-phase live recon** of the exact `stat/health` vpn / Teleport field names BEFORE Build (the recon
  flagged this as unverified). Capture a real payload, then design the metric shape.
- Graceful degrade if the vpn/Teleport block is absent or Teleport is idle (no active sessions ≠ error).
- Emits `homelab_collector_run_*`.

## Concrete deliverables

### 1. VPN/Teleport collector

Reads the `stat/health` vpn subsystem + Teleport fields (confirmed at Design); emits `teleport_up`,
`teleport_sessions` (and whatever the live shape supports). Feeds `UnifiTeleportDown` (unhealthy while
expected up). Surfaces in the Unifi panel Teleport/VPN widget.

## Design Phase

**Status**: [ ] Design (Not Started) — INCLUDES a live recon step to confirm the Teleport/vpn API field shape

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — collector unit-tested against a fixture matching the Design-confirmed field shape (up/idle/active-session; absent-block graceful degrade)
- [ ] 3b — real Teleport state against the live UDM (idle + at least one active session if reproducible)
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation:
- Changelog:
- Tracking:

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
