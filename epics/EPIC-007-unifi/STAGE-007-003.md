# STAGE-007-003: Persistent `unifi_clients` registry table (MAC-keyed, time-stamped IP↔MAC observations)

## Status: Not Started

## Overview

Create the persistent client registry — the canonical client inventory that EPIC-007 OWNS and EPIC-006
ENHANCES. A new SQLite table (Alembic migration) keyed by **MAC** (the stable identity), holding the current
identity + connection facts, **plus time-stamped IP↔MAC observations** so EPIC-006's join is time-windowed
(never a static IP→device map — DHCP IPs rotate). The host `192.168.2.148` is a first-class row.

**Vertical slice:** the migration creates the `unifi_clients` table (+ observation history); a repository
facade can upsert a client and append an IP↔MAC observation; migrate up/down is tested; a seeded host row
(`192.168.2.148`) is present.

**Spec references:** spec §6.1 (registry/`targets` "single source of truth" principle; SQLAlchemy Core +
narrow repository facade), §6 (Alembic migrations). EPIC-007 "Persistent client registry" + "Client-join
contract". This is the data foundation the identity collector (007), the EPIC-006 enrichment, the digest
"what changed", and `UnifiNewClient` all read.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (schema + repo; real data lands via the 007 collector).
- Host-integration concerns: [ ] No (DB migration + repo). Refinement: 3a (migrate up/down; repo upsert +
  observation append; seeded host row). No 3b host-integration delta.

## Cross-cutting standards

- SQLAlchemy Core (NOT ORM) behind a narrow repository facade; pydantic/dataclass row shapes.
- Alembic migration; `HOMELAB_MONITOR_AUTO_MIGRATE` honored; migrate up/down both tested.
- All internal timestamps UTC.
- Cardinality: the registry is bounded by real device count (not metric cardinality), but the observation
  history needs a retention/cap policy (Design decides — e.g., keep last N observations per client or a
  time window) so it doesn't grow unbounded.

## Concrete deliverables

### 1. Migration + tables

`unifi_clients` keyed by MAC: current `ip`, `hostname`, `name`, `oui`/device-type, `network`/SSID,
`ap_mac`, `sw_mac`, `sw_port`, `fixed_ip`/`use_fixedip`, `online`, `first_seen`, `last_seen`,
`lease_expiry` (nullable — populated only by the opt-in lease collector 012). A satellite
`unifi_client_observations` table (or a bounded JSON history) recording **time-stamped IP↔MAC observations**
for the time-windowed join. Indexes for IP-at-time lookups (the EPIC-006 join) and `last_seen`.

### 2. Repository facade

`UnifiClientRepo` with upsert-by-MAC, append-observation, query-by-IP-at-time-window (the contract EPIC-006
calls), and a host-row guarantee (the configured `unifi_host_lan_ip`=`192.168.2.148` is always present as a
first-class row).

### 3. Host seed

Seed/ensure the host `192.168.2.148` row exists as a first-class device, marked appropriately so views never
hide it.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — migrate up/down clean; repo upsert-by-MAC + observation append + query-by-IP-at-time-window tested; host row seeded; observation retention/cap policy enforced
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation:
- Changelog:
- Tracking:

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
