# STAGE-010-003: Karma → outcome retrofit (periodic reconciliation)

## Status: Complete

## Overview

Prod-DB recon 2026-07-05 confirmed `alert_outcomes` is EMPTY (0 rows) despite 168k `alerts` rows. Root cause: Karma (the embedded alert-lifecycle UI) posts ack/silence actions directly to Alertmanager, never touching our monitor. Every alert Jake has acknowledged in the past ~7 weeks is invisible to the analyzer.

Per EPIC-010 Decision 2 (locked 2026-07-05), fix via periodic pull from Alertmanager rather than proxy interception: hourly scheduled job pulls `/api/v2/silences` + resolved-alert state, matches each silence/resolution to `alerts.fingerprint`, writes `AlertOutcome.ACKED` / `AlertOutcome.AUTO_RESOLVED` rows to `alert_outcomes`. Idempotent (INSERT OR IGNORE on `(alert_id, outcome)`) so re-runs never double-count.

**Retrofit for:** EPIC-001..009 + 017 (all completed epics that emit alerts).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No — read-only against Alertmanager, INSERT-only against our DB (idempotent).
- Host-integration concerns: [x] Yes — requires access to the running Alertmanager sidecar; Instance-A refinement will observe real silences translating to outcome rows.

## Concrete deliverables

1. New collector `karma_outcome_reconciler` under `apps/monitor/homelab_monitor/plugins/collectors/builtin/` (or `kernel/analyzer/` — decide during Design):
   - `interval: 1h`, `concurrency_group: analyzer`, `run_kind: ASYNC`, `timeout: 5m`.
   - Pulls `GET /api/v2/silences` from AM (auth: Alertmanager doesn't require auth in our compose — verify).
   - Pulls `GET /api/v2/alerts?resolved=true` (or equivalent) for alerts that have transitioned to resolved.
   - For each silence with matcher(s) that resolve to a specific `alerts.fingerprint` in our DB (matcher shape: label-value pairs; use the fingerprinting.py:21 same-algorithm to compute what the target fingerprint SHOULD be): write `AlertOutcome.ACKED` with `decided_by = "karma"`, `decided_at = silence.created_at`.
   - For each alert that transitioned to `resolved_at IS NOT NULL` since last successful reconciliation AND has NO `alert_outcomes` row yet: write `AlertOutcome.AUTO_RESOLVED` with `decided_by = "reconciler"`.
   - Records last successful reconciliation timestamp in a new `app_settings` key `analyzer.last_reconciliation_at` to bound the incremental scan window.
2. Idempotency: use `INSERT OR IGNORE INTO alert_outcomes (alert_id, outcome, ...) VALUES ...` — the natural UNIQUE key is `(alert_id, outcome)` (add index in this stage's migration OR piggyback on STAGE-010-002 by adding it there — decide in Design).
3. Match semantics: for silences with LABEL matchers (Karma's default) — reconstruct the label set and compute the fingerprint using `fingerprinting.py:compute_fingerprint`. For silences with FINGERPRINT matchers (rare) — use directly. Silences that don't uniquely resolve to ONE `alerts` row are logged and skipped (not an error — just noise).
4. Handles Alertmanager unavailability gracefully: if AM is unreachable, log + emit `homelab_collector_run_last_error_age_seconds`, don't crash, retry next tick.
5. Test coverage:
   - Silence with fingerprint matcher → writes ACKED row.
   - Silence with label matcher that resolves to one alert → writes ACKED row.
   - Silence with label matcher that resolves to multiple alerts → skipped with warning log.
   - Silence with label matcher that resolves to zero alerts → skipped silently.
   - Alert resolved between two reconciliation ticks → writes AUTO_RESOLVED row.
   - Re-run with same AM state → zero new rows written (INSERT OR IGNORE succeeds silently).
   - AM 503 / connection refused → collector run marked errored, no partial writes, next tick retries.

## Design Decisions

**Selected by:** implementing session (autonomous epic-stage-workflow loop), 2026-07-06.
**Locked upstream:** EPIC-010 Decision 2 (hourly Alertmanager pull, INSERT OR IGNORE on `(alert_id, outcome)`, retrofit EPIC-001..009 + 017).

### Sub-decisions (open in stage card, resolved here)

| # | Choice | Decision | Rationale |
|---|--------|----------|-----------|
| 1 | Reconciler module location | `apps/monitor/homelab_monitor/kernel/analyzer/karma_outcome_reconciler.py` (new `kernel/analyzer/` directory) | Matches existing reconciler idiom (`kernel/metrics/cron_run_reconciler.py`); reserves `kernel/analyzer/` for the future STAGE-010-006/007 aggregation jobs sharing the same `analyzer` concurrency group. |
| 2 | New alembic migration `0053`? | YES — required in this stage | STAGE-010-002's `0052` did not touch `alert_outcomes`. The `UNIQUE(alert_id, outcome)` index required for `INSERT OR IGNORE` MUST land here. |
| 3 | `alert_outcomes.decided_by` type conflict (INTEGER FK `users.id` vs. spec's `"karma"` / `"reconciler"` strings) | Widen `decided_by` to `TEXT` in `0053`; drop FK. Update `AlertRepository.insert_outcome` signature from `decided_by: int \| None` to `decided_by: str \| None`. | Provenance ("karma" vs "reconciler" vs future human ack via user id) is the whole point of tool-effectiveness scoring; encoding it via `audit_log.what` (fallback option) would force expensive joins in STAGE-010-006/007 aggregation. Prod has 0 rows in `alert_outcomes` (per handoff and stage overview) so no data migration needed — column change is free. Human-ack path (future) will store the username string instead of an FK id. |
| 4 | Idempotency implementation | Add `AlertRepository.insert_outcome_if_absent(alert_id, outcome, decided_by, decided_at) -> bool`. Uses SQLite `INSERT ... ON CONFLICT DO NOTHING RETURNING id`; returns `True` on real insert (and writes audit_log), `False` on skip (no audit_log write). | Keeps write path centralized (all other alert writes go through the repository). Conditional audit_log satisfies spec's "re-runs never double-count" and prevents audit_log spam on hourly no-op ticks. |
| 5 | HTTP client pattern | New module `apps/monitor/homelab_monitor/kernel/alertmanager/silences_client.py` (thin wrapper: `list_silences()`, `list_alerts()`). Reuses shared `ctx.http` (`httpx.AsyncClient`), same warn-on-failure shape as `AlertmanagerReloader`. | Groups AM HTTP concerns in one dir (`render.py`, `reload.py`, `silences_client.py`); keeps reconciler focused on match logic; testable in isolation via `httpx.MockTransport`. |
| 6 | Env var | Reuse existing `HOMELAB_MONITOR_ALERTMANAGER_URL` (default `http://alertmanager:9093`). | Already established convention in `lifespan.py`; no new env var. |
| 7 | Self-observation metrics | Rely on scheduler-emitted `homelab_collector_run_*` family (free). Emit domain counters `homelab_karma_reconciler_silences_matched_total`, `homelab_karma_reconciler_silences_skipped_total{reason=...}`, `homelab_karma_reconciler_resolutions_matched_total`, and gauge `homelab_karma_reconciler_last_reconciliation_lag_seconds`. | Scheduler covers the mandatory `_run_*` metrics; domain counters give match-failure observability for debugging (esp. multi-match / zero-match silences). |
| 8 | Last-reconciliation timestamp storage | New `app_settings` key `analyzer.last_reconciliation_at` (ISO 8601 UTC). `AppSettingsRepository.get/set`. Only updated on successful tick (both AM fetches succeeded). | Existing pattern. Bounds incremental resolved-alert scan to `WHERE resolved_at > :last`. Not updating on AM failure means a transient outage's resolutions get picked up on the next healthy tick. |
| 9 | Concurrency group | `analyzer` (new group, first user is this reconciler). | Reserved for STAGE-010-006/007 to share (they'll never run simultaneously with this reconciler → no scorecard-computation-during-write races). |
| 10 | Collector metadata | `interval_seconds: 3600` (1h), `timeout_seconds: 300` (5m), `run_kind: ASYNC`, `trust_level: BUILTIN`. Registered in `lifespan.py` alongside other builtin collectors. | Per stage-card contract + Decision 2 cadence. |

### Match semantics (restated from card)

**Silence → ACKED**
- Iterate `GET /api/v2/silences` results with `status.state IN ("active", "expired")` (both; expired silences represent past acks whose alerts may still be in DB).
- For each silence, only handle matchers with `isEqual=true, isRegex=false` (label equality); regex/negated matchers are logged + skipped (`skipped_total{reason="unsupported_matcher"}`).
- Special case: single matcher `name="fingerprint"` → use `value` verbatim as target fingerprint.
- General case: reconstruct label set from equality matchers → `kernel/alerts/fingerprinting.py:compute_fingerprint()` → target fingerprint.
- Query `SELECT id FROM alerts WHERE fingerprint = :fp` — expect exactly 1 row.
  - 0 rows → `skipped_total{reason="no_match"}` + debug log (alert may have been purged by STAGE-010-001).
  - 1 row → `insert_outcome_if_absent(alert_id, ACKED, "karma", silence.startsAt)`.
  - N>1 rows → `skipped_total{reason="ambiguous_match"}` + warn log (silence's matchers don't uniquely identify one alert row — should be rare given per-fire fingerprints).

**Resolved → AUTO_RESOLVED**
- Query monitor's own `alerts` table for rows with `resolved_at IS NOT NULL AND resolved_at > :last_reconciliation_at`.
- For each, `insert_outcome_if_absent(alert_id, AUTO_RESOLVED, "reconciler", resolved_at)` — the `_if_absent` handles the case where a silence already wrote ACKED (ACKED wins; AUTO_RESOLVED skipped as `(alert_id, outcome=ACKED)` is already present but `(alert_id, outcome=AUTO_RESOLVED)` is not — actually BOTH will be written because they're distinct outcome values on the same alert_id — this is CORRECT because the analyzer needs both facts: "ack'd" AND "eventually auto-resolved").
- Note: we don't call `/api/v2/alerts` for this side — the monitor already knows resolution state from its own alert-lifecycle path.

**AM unreachable**
- `httpx.HTTPError` from `silences_client.list_silences()` → log warning, return `CollectorResult(ok=False, errors=[<msg>])`, DO NOT update `app_settings.analyzer.last_reconciliation_at`. Scheduler auto-emits `_failure_total` and `_last_error_age_seconds`.

### Testability

Existing test conventions apply (`asyncio_mode=auto`, `MemoryRetainingMetricsWriter` for metric assertions, `httpx.MockTransport` for AM mocking, in-memory migrated DB via `repo` fixture). No new test infra.

### Explicitly out of scope

- Backfill of historical Karma acks (>7 weeks of silences that have already expired and been garbage-collected by AM) — belongs to STAGE-010-004 (Karma-scraped history backfill).
- Scorecard computation over the reconciled outcomes — STAGE-010-006/007.
- MAINTENANCE_SUPPRESSED outcome writes — EPIC-012 (not in EPIC-010 scope).

## Design Phase

**Status**: [x] Design Complete

## Build Phase

**Status**: [x] Build Complete

## Refinement Phase

- [x] Instance-A validation: bring up prod, verify the hourly job runs and populates outcome rows for currently-silenced prod alerts within one reconciliation cycle
- [x] Regression Items Added

**Status**: [x] Refinement Complete

### Refinement Summary (2026-07-06)

Validated against the LIVE prod compose stack (post-rebuild + redeploy).

**Rebuild**: `docker compose -f deploy/compose/docker-compose.yml build monitor` succeeded. Container `homelab-monitor` recreated via `up -d --no-deps --force-recreate monitor`, healthy in ~30s. Volumes preserved.

**Migration**: Alembic head transitioned 0051 → 0053 automatically at lifespan startup (STAGE-010-002's 0052 + this stage's 0053 applied in one shot). Verified:
- UNIQUE index `uq_alert_outcomes_alert_id_outcome ON alert_outcomes (alert_id, outcome)` present.
- `alert_outcomes.decided_by` widened to TEXT.

**Manual reconciler tick**: Triggered via `POST /api/collectors/karma_outcome_reconciler/retry`. Tick completed in ~31s.

**Results**:
- `alert_outcomes` row count: **0 → 13,973** (all `outcome='auto_resolved', decided_by='reconciler'`).
- `silences_matched`: 0 (Karma's active silences did not resolve to any alert row this tick — historical acks are older than AM's silence-retention window and belong to STAGE-010-004 backfill).
- `resolutions_matched`: 13,973.
- `app_settings.analyzer.last_reconciliation_at`: `2026-07-07T03:34:25.205963+00:00`.
- Metrics: `homelab_karma_reconciler_resolutions_matched_total = 13973`, `homelab_collector_run_success_total{name="karma_outcome_reconciler"} = 1`, no failures.
- Domain gauge `homelab_karma_reconciler_last_reconciliation_lag_seconds = 0.0` (first-run baseline).

**Verdict**: Refinement PASS. Reconciler works end-to-end against real prod data. Additive + idempotent writes; safe to re-run.

**Observation deferred to regression.md**: per-alert INFO log line count (1841) is significantly less than counter increment (13,973). Investigate whether logs are silently deduped/rate-limited — not a bug, but worth confirming before STAGE-010-004 relies on per-alert log volume.

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:

- Implementation: 5ef4aea
- Changelog: [pending]
- Tracking: [pending]

**CHANGELOG Entry**: [ ] Added

**Status**: [x] Finalize Complete

### Finalize Summary (2026-07-07)

- code-reviewer Opus pass 1: 1 Critical + 1 Important + 8 Minor findings; all implemented.
- code-reviewer Opus pass 2 (after implementation changes): APPROVED.
- `make verify` GREEN post-review: 100.00% coverage, 7400 backend tests, 2038 UI tests.
- Impl commit: `5ef4aea` (feat: karma → outcome periodic reconciler + alembic 0053).
- Prod already validated at Refinement — no further deploy step in Finalize.
