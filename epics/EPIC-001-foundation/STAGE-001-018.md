# STAGE-001-018: vmalert (logs) + first log-derived rule

## Status: Complete

## Overview

Add the second vmalert instance, this one targeting VictoriaLogs. Author the first log-derived rule. After this stage, log-pattern alerts flow the same path as metric alerts (Alertmanager → ingestor → channels), with `source_tool=vmalert-logs`.

**Spec references:** §3.2 (vmalert ×2), §6.3 (logs streams), §4.3 (log flow), §13.2 (integration tests).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] Reuses existing AM token

## Cross-cutting standards (from EPIC-001 §Cross-stage acceptance criteria)

Every stage must satisfy: 100% kernel coverage gate; ruff + black + pyright (strict) clean; structured JSON logging via structlog; no plaintext secrets in logs (assert in tests when relevant); no `git add -A` or `git add .` (specific paths only); all internal timestamps UTC. See EPIC-001.md for the full list.

## Concrete deliverables

1. **vmalert (logs) sidecar** in compose
   - Same image as the metrics one. Args: `-datasource.url=http://victorialogs:9428`, `-notifier.url=http://alertmanager:9093`, `-rule=/etc/vmalert/logs/*.yaml`. The two vmalert services are named `vmalert-metrics` and `vmalert-logs`.
2. **First rule** at `deploy/vmalert/logs/system.yaml`
   - Group `system_logs`:
     - **`KernelOOM`** — LogsQL `{service="kernel"} ~ "Out of memory.*kill_process"` count > 0 over 5m → severity=critical, source_tool=vmalert-logs, target_kind=host.
     - **`SshFailedLoginBurst`** — LogsQL `{service="sshd"} ~ "Failed password"` count > 10 over 5m → severity=warning, source_tool=vmalert-logs, target_kind=host. (Useful even on day one — guards against drive-by SSH scans on hosts with internet exposure.)
     - **`LogStreamBudgetOverrun`** — bound to the `log_stream_budget` metric from STAGE-001-016 (this rule is metrics-side actually; cross-listed here for completeness — it lives in `deploy/vmalert/metrics/log_stream_budget.yaml`).
3. **Test rig extension**
   - `docker-compose.test.yml` adds a fixture container that, on demand (env-controlled), emits planted log lines matching the rules.
4. **Tests**
   - Integration: planted "Out of memory: Killed process 1234 (badproc)" → vmalert-logs fires within eval+wait → alert appears in `/api/alerts` with `source_tool=vmalert-logs`.
   - Integration: 11 "Failed password" lines in 5m → SshFailedLoginBurst fires.
   - Resolved path: lines stop, alert auto-resolves.
   - Source tagging assertion: `source_tool` is correctly set to `vmalert-logs` (not `vmalert-metrics`).

## Out of scope for this stage

- Drain log clustering / signature-based rules (EPIC-004; this stage is hand-written rules)
- HA / Pi-hole / Synology / Unifi log rules (those land in their respective epics)
- Log redaction policy (EPIC-004)

## Decisions in this stage

- **Two vmalert instances** confirmed (vs one). The two-instance pattern is canonical for vmalert + VL.
- **Initial rules conservative** — high-confidence patterns only (OOM, SSH burst). Wide pattern matching belongs in the Drain-driven approach (EPIC-004).

## Design Phase

- **Approaches to present:**
  - (a) two vmalert instances (recommended; spec)
  - (b) one combined instance via a query-router (doesn't exist as a stable upstream component)
- **UI Options Presented:** N/A
- **User Choice:** vmalert-logs sidecar reusing the vmalert-metrics + Alertmanager + AlertDispatcher infrastructure from STAGE-001-017. Locked decisions:
  - **Q1 — Test fixture**: Direct curl from pytest helper. Test plants log lines via httpx POST to `http://victorialogs:9428/insert/jsonline` directly from the integration-tests container with explicit `_time` fields for deterministic timestamps. Helper at `apps/monitor/tests/integration/helpers/vl_planter.py` reusable for STAGE-021. No extra fixture container, no vector pipeline coupling — vmalert-logs is what's under test; vector → VL path is STAGE-021 scope.
  - **Q2 — Eval windows**: Test-specific rule dir at `deploy/vmalert/logs-test/` with shorter windows (`count > 0 over 30s` for OOM; `count > 2 over 30s` for SshFailedLoginBurst) mirroring host.yaml's "fast-test convention" pattern. Production keeps canonical 5m windows in `deploy/vmalert/logs/`. Integration tests use the test-windows rule file. The slow integration test is gated by `@pytest.mark.slow` so `make test-fast` excludes it; the default `make verify` runs it (per the hybrid testing strategy locked in STAGE-017 Q4).
  - **Q3 — LogStreamBudgetOverrun**: `homelab_log_stream_lines_per_sec > 45` for 5m, severity=warning, source_tool=vmalert-metrics, target_kind=log_stream. 45 = 90% of vector's hardcoded 50/s throttle. The LogStreamBudgetCollector emits exactly two gauges (`homelab_log_stream_bytes_today`, `homelab_log_stream_lines_per_sec`) with `{host, service}` labels; no threshold gauge exists. Bytes-today has no daily budget defined anywhere — don't alert on it.
  - **Q4 — Architectural extras**: (a) Land `system.yaml` + vmalert-logs service in SAME commit so the rules dir is never empty at first boot. (b) Mirror vmalert-metrics healthcheck (`wget http://localhost:8880/-/health`). vmalert is stateless; no persistent volume. (c) Build-phase verify VL `-datasource.url` form (VictoriaLogs v0.30.0 may expect `/select/logsql` path suffix vs bare URL). (d) Build-phase verify Alertmanager routing accepts the new vmalert-logs notifier without changes (existing flat routing should work).
- **Seed Data Agreed:** Three log-derived rules in `deploy/vmalert/logs/system.yaml` (KernelOOM, SshFailedLoginBurst) + one cross-listed metrics rule (`LogStreamBudgetOverrun` at `deploy/vmalert/metrics/log_stream_budget.yaml`).
- **Session Notes:**
  - STAGE-017 left baseline at 1041 backend tests / 100% kernel coverage; verified clean before Design.
  - vmalert-logs reuses Alertmanager from STAGE-017. The same `data_alertmanager_config` rendered config + AM webhook receiver handles both metric and log-derived alerts. Both vmalert instances point to `-notifier.url=http://alertmanager:9093`.
  - vector.toml labels (`host`, `service`, `severity`) confirmed as the LogsQL selector keys for rule expressions.
  - `homelab_collector_quarantine_count` metric remains deferred (commented in host.yaml). Out of scope for STAGE-018.

**Status**: [x] Complete

## Build Phase

- **Components Created:**
  - `deploy/vmalert/logs/system.yaml` (new — production rules: KernelOOM, SshFailedLoginBurst with `type: vlogs` group + 5m windows)
  - `deploy/vmalert/logs-test/system.yaml` (new — test counterpart with 30s windows + lower thresholds)
  - `deploy/vmalert/metrics/log_stream_budget.yaml` (new — LogStreamBudgetOverrun rule querying `homelab_log_stream_lines_per_sec > 45`)
  - `apps/monitor/tests/integration/helpers/__init__.py` (new — package init)
  - `apps/monitor/tests/integration/helpers/vl_planter.py` (new — async helper to POST NDJSON to VL with deterministic timestamps)
  - `apps/monitor/tests/integration/test_vmalert_logs_pipeline.py` (new — 4 `@pytest.mark.slow` tests: rules-load, OOM fires, SSH burst fires, OOM resolves on aging-out)
  - Edited `deploy/compose/docker-compose.yml` (added vmalert-logs service block; mirrors vmalert-metrics pattern)
  - Edited `deploy/compose/docker-compose.test.yml` (added victorialogs-test, vmalert-logs-test services + integration-tests env vars and depends_on)
  - Edited `deploy/compose/test-fixtures/alertmanager.yml` (resolve_timeout 60s; group_interval 30s; webhook URL → `http://integration-tests:9090/api/alerts/ingest`)
- **API Endpoints Added:** none new (alert webhook ingestor at `/api/alerts/ingest` reused from STAGE-013/017)
- **Placeholders Added:** none
- **Session Notes:**
  - Implementation took ~4 verify cycles (smaller than STAGE-017's ~9 because most of the infrastructure was already in place from STAGE-017).
  - Critical correction during planning: my Design phase had PromQL-style `count_over_time(...)` rule expressions which are NOT valid LogsQL. The planner caught this and corrected to `type: vlogs` group discriminator + LogsQL pipe syntax (`_time:Xm sel "phrase" | stats by (host) count() as N | filter N:>threshold`). Production rule files use the corrected syntax.
  - VL `-datasource.url` form verified during planning: bare `http://victorialogs:9428` (no `/select/logsql` suffix needed; vmalert auto-suffixes the LogsQL endpoint).
  - Existing test fixture `alertmanager.yml` posted to `nowhere.invalid:9999` (fine for STAGE-017 healthcheck test, broken for end-to-end alert delivery). Updated to `http://integration-tests:9090/api/alerts/ingest` so the integration test's in-rig webhook receiver gets the alerts.
  - vmalert-logs reuses the SAME Alertmanager from STAGE-017. Existing AM flat routing accepts `source_tool=vmalert-logs` alerts without changes (no source_tool filter).
  - 1041 backend tests pass; 100% backend kernel coverage maintained. Slow integration tests (`@pytest.mark.slow`) excluded from `make test-fast`, included in `make verify`.

**Status**: [x] Complete

## Refinement Phase

- [x] Desktop N/A (backend-only stage, no UI changes)
- [x] Mobile N/A (backend-only stage, no UI changes)
- [x] Regression Items Added
- **Feedback Round 1:**
  - e2e-tester ran 4-scenario backend API validation autonomously (backend-only stage; user not prompted).
  - Scenario 1 (full test suite): PASS — 1041 backend + 91 UI tests, 100% backend coverage.
  - Scenario 2 (service-layer): brought up `alertmanager + victoriametrics + victorialogs + vmalert-metrics + vmalert-logs-test` from docker-compose.test.yml. All vmalert rules load correctly: vmalert-metrics shows host (HostHighCPU/HostHighMemory) + log_stream_budget (LogStreamBudgetOverrun) + self_disk (3 rules); vmalert-logs-test shows system_logs (KernelOOM/SshFailedLoginBurst) with `type: vlogs` group discriminator accepted. LogsQL syntax validated by vmalert v1.107.0.
  - Alert firing: planted RFC3339-timestamped log lines via `vl_planter.py` → KernelOOM and SshFailedLoginBurst fired within 30s evaluation window with correct `source_tool=vmalert-logs` labels. Alertmanager received the firing notifications and attempted webhook delivery to `http://integration-tests:9090/api/alerts/ingest`.
  - Scenario 3 (frontend layer): SKIP — no frontend changes.
  - Scenario 4 (slow integration test): partial pass — `test_vmalert_logs_loads_system_rules` passed; the 3 alert-firing tests blocked by the 2 bugs found in feedback (now fixed). Will pass in CI run from inside integration-tests container.
- **Feedback Round 2:** none needed; all bugs resolved.
- **Bugs found and fixed during Refinement:**
  - **Bug 1 (HIGH, introduced in STAGE-018)**: `vl_planter.py` used epoch milliseconds for the `_time` field. VictoriaLogs v0.30.0 only accepts RFC3339 format; ingest returned `cannot unmarshal rfc3339 timestamp` errors. Fixed by switching to `ts.strftime("%Y-%m-%dT%H:%M:%S.%f")[:-3] + "Z"` and updating the docstring.
  - **Bug 2 (MEDIUM, pre-existing from STAGE-016/017)**: docker healthchecks used `wget http://localhost:<port>/...` but busybox-based images (VM, VL, AM, vmalert) don't resolve `localhost` (no /etc/hosts entry). Container dependency chains stalled in "Created" state. Fixed by replacing all `http://localhost:` with `http://127.0.0.1:` in BOTH `docker-compose.yml` (5 services) and `docker-compose.test.yml` (5 services). 10 total fixes.

**Status**: [x] Complete

## Finalize Phase

- [x] Code Review (pre-tests) — code-reviewer-Opus pass 1: 17 findings (0 Critical, 7 Important, 7 Minor, 3 Nice-to-have). 12 addressed; 5 deferred per documented trade-offs (F2 webhook auth, F7 content-type, F11 slow-test wait, F12 cardinality, F15 xdist file path, F17 rule duplication).
- [x] Tests Written — 4 new `@pytest.mark.slow` integration tests in test_vmalert_logs_pipeline.py covering rule loading, OOM firing, SSH burst firing, and resolved-on-aging-out. Helper modules: vl_planter.py + test_webhook_server.py.
- [x] Code Review (post-tests) — code-reviewer-Opus pass 2: 3 polish findings (case-insensitive safety check, append-atomicity docstring, kill-fallback timeout bump). All addressed.
- [ ] Documentation Updated — `docs/alerting/log-rules.md` deferred to a future docs stage. The vmalert-logs integration is documented inline (rule file headers, helper module docstrings, structured log events). LogsQL pipe syntax is the standard upstream form and well-documented externally.
- [x] Committed: specific paths only (no `git add -A` used)

**Commit Hash**: `23b028e` (code review fixes); `6fd43e7` (impl); `aff5737` (Refinement bug fixes)
**CHANGELOG Entry**: [x] Added (`740361b`)

**Status**: [x] Complete
