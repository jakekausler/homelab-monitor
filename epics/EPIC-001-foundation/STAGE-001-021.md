# STAGE-001-021: Full integration test rig + canonical e2e test

## Status: Not Started

## Overview

Finalize the integration test rig that future epics rely on. Expand `docker-compose.test.yml` to include every sidecar from EPIC-001 (VM, VL, vmagent, vmalert ×2, Alertmanager, Karma, kthxbye, Grafana, vector, monitor) plus a fixture target stack that produces predictable metrics, logs, and heartbeats. Author the canonical end-to-end integration test that exercises the entire alert path from collector → VM → vmalert → AM → ingestor → channel. Wire the test into CI as a required job.

After this stage, EPIC-001 is complete: the foundation is proven end-to-end with an automated test that catches regressions in the alert pipeline.

**Spec references:** §13.2 (integration tests — full list), §13.3 (e2e), §10.5 (CI), §15 (stage 021 acceptance).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] Test rig uses fixture creds only

## Cross-cutting standards (from EPIC-001 §Cross-stage acceptance criteria)

Every stage must satisfy: 100% kernel coverage gate; ruff + black + pyright (strict) clean; structured JSON logging via structlog; no plaintext secrets in logs (assert in tests when relevant); no `git add -A` or `git add .` (specific paths only); all internal timestamps UTC. See EPIC-001.md for the full list.

## Concrete deliverables

1. **`docker-compose.test.yml` finalization**
   - All sidecars from STAGE-001-015 through 020 included.
   - Plus a "fixture target" container (`fixture-host`): exposes a Prometheus `/metrics` endpoint with controllable values driven by env (e.g., `FIXTURE_CPU_PERCENT=95` makes the host-cpu metric report 95). Used to deterministically trip rules.
   - Plus a "noisy logger" container (from STAGE-001-016) that emits planted log lines on demand.
   - Plus an `integration-tests` container that runs pytest against the live rig and exits with the suite's exit code.
   - `--abort-on-container-exit --exit-code-from integration-tests` is the canonical run mode.
2. **Canonical e2e test** at `apps/monitor/tests/integration/test_canonical_e2e.py`
   - Steps:
     1. Boot the rig (handled by the docker-compose call; the test assumes services are up via healthchecks).
     2. Set the fixture `cpu_percent` to 95 via the fixture target's control endpoint.
     3. Wait up to 60s for the alert to appear in `/api/alerts` (poll).
     4. Assert: `severity=warning`, `source_tool=vmalert-metrics`, `alertname=HostHighCPU`.
     5. Reset `cpu_percent` to 5.
     6. Wait up to 60s for the alert to enter `resolved` state.
     7. Assert: `resolved_at` is populated and a `alert.resolved` SSE event was observed.
   - Test imports a small `Rig` helper that wraps polling, fixture control, and SSE consumption.
3. **Additional integration tests** (each is short, focused on one path)
   - **Heartbeat path stub** — POST to `/hb/test/ok` returns 401 without a token; with a valid token, returns 204 and updates a row. (The full heartbeat receiver behavior lands in EPIC-002, but the auth on the endpoint is here.)
   - **Logs path** — plant a log line via the noisy logger; assert `/api/logs/query` returns it.
   - **Logs alert path** — plant 11 SSH-failed-password lines; assert `SshFailedLoginBurst` fires and reaches `/api/alerts` with `source_tool=vmalert-logs`.
   - **Karma ack path** — programmatic ack via Karma's API creates a silence in AM; the alert moves to silenced state.
   - **Quarantine path** — induce a collector to fail repeatedly; verify the quarantine alert appears in `/api/alerts` (this exercises the path where the scheduler emits via the ingestor's internal API, not via vmalert).
4. **CI integration**
   - `ci.yml` adds an `integration` job that runs the rig and asserts exit code 0.
   - The `integration` job is required for PR merge (branch protection).
   - The job uses BuildKit cache for fast image builds.
5. **Tooling — centralized dev + prod docker workflows**

   **Single source of truth for ports/env:** `deploy/dev/dev.env.example` (checked in) + `deploy/dev/dev.env` (gitignored, user-generated from example). Lists every port number the dev stack uses (UI, backend, AM, Karma, kthxbye, VM, VL, vmagent, vector, vmalert ×2) plus all `HOMELAB_MONITOR_*` env vars and the master key. The same file is sourced by both the dev script and the test-rig script so port choices don't drift.

   **`scripts/reset-test-rig.sh`** — tears down `docker-compose.test.yml` volumes and rebuilds. Used both locally and (optionally) in CI when builds drift.

   **`scripts/dev-up.sh [--clean] [--prod]`** — single entrypoint for local development:
   - Reads `deploy/dev/dev.env` (creates from `.env.example` on first run; aborts if master key not set).
   - **Dev mode (default)**: hybrid stack. Brings up sidecars in docker (AM, Karma, kthxbye, VM, VL, vmagent, vmalert ×2, vector) on the dev network with port-publishing per `dev.env`. Runs the monitor backend on host via `uv run uvicorn ... --reload` so code edits hot-reload. Runs UI dev server on host via `pnpm dev`. Creates default `admin` user if none exists (idempotent). Prints status banner with all URLs + login creds.
   - **`--prod` mode**: no host processes; brings up the full `docker-compose.yml` stack with the monitor image built from local Dockerfile (`docker compose build monitor`). Validates that the prod image runs end-to-end. Used to verify deployment correctness before merge.
   - **`--clean` flag**: kills any existing dev processes (uvicorn, pnpm dev) AND tears down docker stack + volumes before bringing up. The "clear cached code" path.

   **`scripts/dev-down.sh`** — graceful tear-down of whatever `dev-up.sh` started. Detects mode from running containers/processes.

   **`Makefile` targets**:
   - `make dev` → `scripts/dev-up.sh` (dev mode, no clean)
   - `make dev-clean` → `scripts/dev-up.sh --clean` (kill + restart, dev mode)
   - `make dev-prod` → `scripts/dev-up.sh --prod` (prod-image variant)
   - `make dev-down` → `scripts/dev-down.sh`

   **Prod Dockerfile correctness — must be validated by `make dev-prod`:**
   - `.dockerignore` re-includes `apps/monitor/alembic/` (currently missing — discovered in STAGE-019 Refinement).
   - Container user (uid 1000) must own `/data` volume mount: add `RUN chown -R homelab:homelab /data` after USER switch in `apps/monitor/Dockerfile`, OR use a tini-based init script that chowns at startup.
   - `make dev-prod` MUST succeed end-to-end (login, navigate to /alerts, see Karma) as the acceptance test for the prod variant. Failure here blocks STAGE-021 completion.

   **Documentation:**
   - Update `CLAUDE.md` Local Refinement section to point at `make dev` / `make dev-prod` instead of the manual recipe.
   - Add `docs/dev/local-environment.md` covering: when to use dev vs `--prod`, port map, troubleshooting (port collisions, host-network containers, vmagent visibility).

## Out of scope for this stage

- Real external integrations (HA, Pi-hole, Synology, Unifi — those have their own epics' integration tests later)
- Auto-fix end-to-end testing (EPIC-009)
- Performance / load testing (separate hardening epic)

## Decisions in this stage

- **Test rig boots ALL sidecars** — yes. Future epics will add more services that need to participate; making this the canonical complete rig means future epics can extend without bringing up parallel rigs.
- **Polling-based assertions** with sensible timeouts — yes; alert pipelines are inherently asynchronous and arbitrary sleeps are flaky.
- **CI runtime budget** — target ≤ 5 minutes for the integration job; if it grows, parallelize sub-suites later.

## Design Phase

- **Approaches to present:**
  - (a) one big rig + many focused tests (recommended)
  - (b) per-test mini-rigs (faster individually, slower in aggregate, more boilerplate)
- **UI Options Presented:** N/A (not a UI stage)
- **User Choice:** (a) one big rig + many focused tests, with the following architecture decisions locked: (1) single big docker-compose.test.yml rig, (2) fixture-host as FastAPI Python custom image, (3) hybrid log injection — noisy-logger container for ONE vector→VL test + vl_planter elsewhere, (4) custom Grafana image with baked-in VL plugin published to GHCR via separate workflow, (5) BuildKit `type=gha` cache + serial test execution (defer xdist), (6) single `deploy/dev/dev.env` file with sections + chmod 600, (7) single `dev-up.sh` bash script with case statement for --prod/--clean, (8) remove `--ignore=tests/integration` from pyproject addopts and rely on `@pytest.mark.integration` marker + guardrail test, (9) three sequential Build specs A/B/C: A=rig core (custom Grafana image + fixture-host + noisy-logger + compose additions + Rig helper + 5 integration tests + pyproject change), B=dev/prod tooling (dev.env + dev-up.sh + dev-down.sh + Makefile targets + prod Dockerfile fixes), C=CI integration (ci.yml integration job + grafana-image.yml workflow). Documented all 10 brainstormer-Opus decisions with rationale.
- **Seed Data Agreed:** Fixture-host exposes Prometheus `/metrics` with `FIXTURE_CPU_PERCENT` env + `POST /control` to mutate at runtime. Noisy-logger exposes `POST /log` printing payload to stdout (vector tails via docker logs driver). Canonical e2e: set CPU=95 → poll `/api/alerts` ≤60s for `HostHighCPU` warning + `source_tool=vmalert-metrics` → set CPU=5 → poll ≤60s for `resolved_at` populated + `alert.resolved` SSE event.
- **Session Notes:** Brainstormer-Opus produced 10 architectural decisions (1500-2000 word output). User confirmed all 4 highest-stakes via AskUserQuestion: custom Grafana image, remove pytest --ignore, three-spec split, hybrid noisy-logger. Other 6 decisions accepted as recommended (single big rig, FastAPI fixture-host, runtime budget strategy, single-file dev.env, single bash dev-up.sh, CI cache strategy). Pre-existing infra debt to resolve in this stage: pyproject `addopts` ignores `tests/integration` (Spec A); prod Dockerfile lacks `chown -R homelab:homelab /data` (Spec B); `GF_SERVER_ROOT_URL` hardcoded in prod compose (Spec B); `.dockerignore` alembic re-include already done in STAGE-019 (verified).

**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:** All three Build specs (A, B, C) implemented and verified green via `make verify` (1125 backend tests + 100% kernel coverage + 110 UI tests).

  **Spec A (Rig core)** — 22 files: Custom Grafana image (deploy/grafana/Dockerfile + README), fixture-host FastAPI module + Dockerfile (apps/monitor/tests/fixtures/fixture-host/), noisy-logger FastAPI module + Dockerfile (apps/monitor/tests/fixtures/noisy-logger/), new vmalert rule (deploy/vmalert/metrics/fixture.yaml), test-rig configs (deploy/compose/test-fixtures/vmagent-scrape.yaml + vector.toml), heartbeat stub router (apps/monitor/homelab_monitor/kernel/api/routers/heartbeat.py + tests), AST guardrail test (test_integration_markers_guardrail.py), reset-test-rig.sh script, app.py heartbeat router registration, pyproject `addopts` change (--ignore=tests/integration → -m 'not integration'), expanded docker-compose.test.yml (added monitor + vmagent + vector + fixture-host + noisy-logger services + /shared tmpfs for token bootstrap), prod docker-compose.yml grafana switch, Rig helper module (apps/monitor/tests/integration/helpers/rig.py), and 5 new integration tests (test_canonical_e2e.py, test_heartbeat_path.py, test_vector_to_vl_path.py, test_quarantine_path.py, test_karma_ack_canonical.py).

  **Spec B (Dev/prod tooling)** — 9 files: deploy/dev/dev.env.example (single source of truth, 32 env vars + 13 ports), .gitignore additions, scripts/dev-up.sh (hybrid + --prod + --clean modes), scripts/dev-down.sh (graceful tear-down), Makefile dev/dev-clean/dev-prod/dev-down targets, prod Dockerfile /data chown fix (RUN mkdir -p /data && chown -R homelab:homelab /data before USER), docker-compose.yml GRAFANA_PUBLIC_URL/DOMAIN templating, deploy/compose/.env.example documentation, CLAUDE.md Local Refinement section rewrite (recommends `make dev` + preserves manual recipe as fallback), and docs/dev/local-environment.md (175-line operator guide).

  **Spec C (CI integration)** — 3 files: .github/workflows/ci.yml integration job (BuildKit cache pre-warm for 5 images via type=gha, GHCR login for karma/kthxbye rate-limit avoidance, 10min timeout), .github/workflows/grafana-image.yml (publishes ghcr.io/jakekausler/homelab-monitor-grafana:latest + immutable :12.3.3-vlplugin0.26.3 tag on changes to deploy/grafana/**), and .github/dependabot.yml docker ecosystem entry for /deploy/grafana.

  **Pre-existing infra debt resolved:** pyproject `addopts` --ignore=tests/integration removed (Spec A); prod Dockerfile `/data` chown added (Spec B); GF_SERVER_ROOT_URL hardcode replaced with env templating (Spec B); .dockerignore alembic re-include verified present (no change needed).

- **API Endpoints Added:** `POST /api/hb/{path}` (heartbeat stub) — token-only auth via Scope.HEARTBEAT_WRITE, returns 204 No Content with body discarded. EPIC-002 will add real persistence behavior.

- **Placeholders Added:** None — this stage closes EPIC-001. The heartbeat router is a documented stub for EPIC-002 (real receiver behavior); guardrail test covers integration tests in tests/integration/.

- **Session Notes:** Build executed across 3 sequential spec sessions (A → B → C) per Design decision #10. 5 verifier passes total (3 fix-cycles for Spec A: 11 lint+format+pyright fixes from initial pass, 5 PLR2004 leftovers in rig.py, 1 headers type error at line 37 of test_api_heartbeat.py, then guardrail bug discovery — guardrail incorrectly flagged a fixture named test_webhook because its name starts with test_; fix added fixture-detection branch via decorator AST inspection). Specs B + C: green on first verify pass each. Final verify: 1125 backend tests + 100% kernel coverage (4722 stmts, 0 missed) + 110 UI tests + zero lint/format/typecheck errors. 31 pre-existing UI lint warnings unchanged (React 19 forwardRef + set-state-in-effect noise, not introduced by this stage). The BCFC pattern from STAGE-019/020 (parallel scribe wave by file domain) worked well: 4 parallel scribes for Spec A Phase 1+2 + 2 parallel scribes for Spec A Phase 4 = wave-1 wave-2 wave-3 build pattern. Total Build files: 34 (22 Spec A + 9 Spec B + 3 Spec C). Pyright deprecation `Iterator -> Generator` for @contextmanager surfaced as a new gotcha (not in CLAUDE.md). Phase 2 scribe initially put `@pytest.mark.integration` on the wrong target (a fixture instead of test functions) — guardrail bug compounded this; both fixed.

**Status**: [x] Build (Complete)

## Refinement Phase

- [ ] Desktop Approved (full local rig run, all integration tests pass)
- [ ] Mobile Approved (N/A; document)
- [ ] Regression Items Added
- **Feedback Round 1:**
- **Feedback Round 2:**

**Status**: [ ] Complete

## Finalize Phase

- [ ] Code Review (pre-tests) — focus on test flakiness (poll-loops, sleeps, ordering), CI runtime, image cache configuration
- [ ] Tests Written (the suite is itself the deliverable)
- [ ] Code Review (post-tests)
- [ ] Documentation Updated: `docs/testing/integration.md` — how to add an integration test, how the rig is structured
- [ ] Committed: specific paths only

**Commit Hash**:
**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Complete
