# STAGE-001-017: Alertmanager + vmalert (metrics) + first rule

## Status: Build (Complete)

## Overview

Add Alertmanager and vmalert (metrics) as sidecars. Configure vmalert to evaluate rules against VictoriaMetrics and route to Alertmanager. Configure Alertmanager to webhook back to our `/api/alerts/ingest`. Author the first vmalert rule (host metric threshold). Verify the full path: synthetic threshold trip → vmalert → Alertmanager → ingestor → in-process dashboard channel → SSE → dashboard live update.

After this stage, real alerts flow end-to-end with no manual webhook posting.

**Spec references:** §3.2 (Alertmanager + vmalert sidecars), §4.1 (metric flow ending at Alertmanager), §4.4 (alert lifecycle), §10.1 (compose).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [x] Yes — Alertmanager calls our ingestor with an API token

## Cross-cutting standards (from EPIC-001 §Cross-stage acceptance criteria)

Every stage must satisfy: 100% kernel coverage gate; ruff + black + pyright (strict) clean; structured JSON logging via structlog; no plaintext secrets in logs (assert in tests when relevant); no `git add -A` or `git add .` (specific paths only); all internal timestamps UTC. See EPIC-001.md for the full list.

## Concrete deliverables

1. **Alertmanager sidecar** in compose
   - Image pinned (e.g., `prom/alertmanager:v0.27.x`).
   - Config at `deploy/alertmanager/alertmanager.yml` (template; rendered by the monitor on startup so secrets like the API token can be injected without baking them into the image):
     - One default route → webhook receiver.
     - Webhook receiver URL: `http://homelab-monitor-monitor:9090/api/alerts/ingest`.
     - `Authorization: Bearer ${ALERTMANAGER_INGEST_TOKEN}` header (token created via `hm api-token create --scope alerts:ingest:write` and stored in the secrets store; the monitor renders the AM config with the token at startup).
     - Default `group_by`, `group_wait`, `group_interval`, `repeat_interval` configured per spec §8.4.
2. **vmalert (metrics) sidecar**
   - Image pinned.
   - Args: `-datasource.url=http://victoriametrics:8428`, `-notifier.url=http://alertmanager:9093`, `-rule=/etc/vmalert/metrics/*.yaml`, `-evaluationInterval=30s`.
   - Volume `deploy/vmalert/metrics/` mounted at `/etc/vmalert/metrics/`.
3. **First rule** at `deploy/vmalert/metrics/host.yaml`
   - Group `host`:
     - **`HostHighCPU`** — `avg_over_time(homelab_host_cpu_percent{cpu="all"}[5m]) > 90` for 10m → severity=warning, source_tool=vmalert-metrics, target_kind=host, summary, description.
     - **`HostHighMemory`** — `homelab_host_memory_bytes{type="used"} / homelab_host_memory_bytes{type="total"} > 0.9` for 10m → severity=warning.
     - **`CollectorQuarantined`** — `homelab_collector_quarantine_count > 0` → severity=warning, source_tool=vmalert-metrics, target_kind=collector. Replaces (de-duplicates with) the synthetic alert the scheduler emits internally — the scheduler keeps its synthetic alert as a faster path; vmalert's version provides cross-restart durability via the persisted `consecutive_failures` counter.
4. **Alertmanager render-on-boot**
   - The monitor reads `alertmanager.yml.template`, substitutes `${ALERTMANAGER_INGEST_TOKEN}` from the secrets store, writes the result to a docker volume Alertmanager reads. Calls Alertmanager's `/-/reload` after.
   - On token rotation, the same flow re-runs.
5. **Ingestor lockdown**
   - `/api/alerts/ingest` now strictly requires the token's `alerts:ingest:write` scope. The dashboard's "synthetic test post" (used in STAGE-001-013) gets its own scope `alerts:ingest:dev` that's only minted in dev mode.
6. **Tests**
   - Integration: bring up the test rig; force `homelab_host_cpu_percent` high (use a fixture metric exporter); vmalert fires within evaluation+wait windows; Alertmanager webhook hits the monitor; alert appears in `/api/alerts`; SSE event delivered.
   - Resolved path: metric returns to normal; vmalert sends `status=resolved`; alert row updated; resolved SSE event delivered.
   - Auth: an unauthenticated POST to `/api/alerts/ingest` returns 401; a wrong-scope token returns 403.

## Out of scope for this stage

- Karma + kthxbye for alert lifecycle UI (STAGE-001-019)
- vmalert (logs) and log-derived rules (STAGE-001-018)
- External channels (HA push, Discord, SMTP — EPIC-005 / EPIC-013)
- Routing rule editor (EPIC-012)
- Maintenance windows (EPIC-012)
- Per-tag overrides (EPIC-012)

## Decisions in this stage

- **Token lives in secrets store** — yes; never in plaintext in alertmanager.yml on disk.
- **Reload on token rotation** — yes; the rotate flow includes re-rendering the Alertmanager config and POSTing to AM `/-/reload`.
- **Severity vocabulary** locked — `info`, `warning`, `error`, `critical` (per spec §8.2). Rules must use one of these.

## Design Phase

- **Approaches to present:**
  - (a) Alertmanager + vmalert (recommended; spec)
  - (b) skip Alertmanager and have vmalert webhook directly to the ingestor (loses dedup/grouping/silences — defeats the point)
- **UI Options Presented:** N/A
- **User Choice:** Approach (a) — Alertmanager + vmalert, with these specific architectural decisions:
  - **Q1 — AM render-on-boot**: Lifespan render. Monitor renders `deploy/alertmanager/alertmanager.yml.template` during FastAPI lifespan startup (after secrets resolver, before scheduler) into a shared named volume `data_alertmanager_config`. Compose `depends_on: monitor (healthy)` ordering for AM. New module at `apps/monitor/homelab_monitor/kernel/alertmanager/render.py` exposing `render_config(...)` and `AlertmanagerReloader.reload()`. On token rotation, re-render + POST `http://alertmanager:9093/-/reload`; if AM unreachable, log warning (file is source of truth).
  - **Q2 — Token bootstrap**: Auto-mint via system principal. On first boot, the render module checks for an existing API token named `alertmanager-ingest`; if missing, mints one with `Scope.ALERTS_INGEST_WRITE` using `who="system:alertmanager-bootstrap"` (distinguishable in audit log). Token plaintext held only in memory long enough to render the AM config; never logged.
  - **Q3 — vmalert reload**: File-watching via `-configCheckInterval=30s` arg. STAGE-017 ships static rules in `deploy/vmalert/metrics/host.yaml` mounted RO. 30s detection lag is well within the rules' 10m `for:` thresholds. Forward-compatible for later stages that render rule files dynamically.
  - **Q4 — Test strategy**: Hybrid. Keep handler-level e2e tests fast (POST AM v2 webhook payload directly to `/api/alerts/ingest`, assert SSE delivery — sub-second, runs in default suite). Add separate `@pytest.mark.slow` integration test extending `deploy/compose/docker-compose.test.yml` with vmalert + AM containers to validate YAML rule files compile and rendered AM config is accepted by AM. Full sidecar coverage deferred to STAGE-001-021.
- **Seed Data Agreed:** the three host rules above
- **Session Notes:**
  - Pre-existing baseline regressions discovered before Design: 5 categories fixed in commit 86fbbd9 (5 logs tests using stale `start="a"` placeholders, TS6 strict cast in HostCpuTile, 4 backend coverage gaps requiring 13 new tests + 1 pragma). `make verify` now green at 1012 tests / 100% coverage.
  - Alert ingestor at `apps/monitor/homelab_monitor/kernel/api/routers/alerts.py::ingest_alerts` already accepts Alertmanager v2 webhook JSON shape verbatim (auth: `Scope.ALERTS_INGEST_WRITE` OR session). No changes needed to the ingestor.
  - Existing AlertDispatcher already fans out to InprocDashboardChannel which publishes AlertFiringEvent / AlertResolvedEvent to SSE broker. End-to-end pipeline contract is already in place from STAGE-013/014.
  - vmalert rule file `deploy/vmalert/metrics/self_disk.yaml` already exists (forward-compat from STAGE-015A). STAGE-017 adds `host.yaml` with three rules: `HostHighCPU`, `HostHighMemory`, `CollectorQuarantined`. Deferred-firing self_disk rules will start firing once vmalert sidecar is added in this stage.

**Status**: [x] Complete

## Build Phase

- **Components Created:**
  - `apps/monitor/homelab_monitor/kernel/alertmanager/__init__.py` (new package init)
  - `apps/monitor/homelab_monitor/kernel/alertmanager/render.py` (new — `ensure_ingest_token`, `render_config`, `AlertmanagerReloader`, `render_on_boot`)
  - `deploy/alertmanager/alertmanager.yml.template` (new — webhook to monitor with Bearer token placeholder)
  - `deploy/vmalert/metrics/host.yaml` (new — 3 rules: HostHighCPU, HostHighMemory, CollectorQuarantined)
  - Edited `apps/monitor/homelab_monitor/kernel/api/lifespan.py` (call `render_on_boot` after secrets init)
  - Edited `apps/monitor/homelab_monitor/kernel/auth/repository.py` (added `get_api_token_by_name`, `delete_api_token_by_name`)
  - Edited `deploy/compose/docker-compose.yml` (added alertmanager + vmalert-metrics services + `data_alertmanager_config` volume)
  - Edited `deploy/compose/docker-compose.test.yml` (added alertmanager + vmalert-metrics for slow integration tests)
  - Edited `apps/monitor/tests/conftest.py` (added `HOMELAB_MONITOR_ALERTMANAGER_URL=disabled` env var to authenticated_client + api_token_client fixtures so tests skip AM render)
  - New tests: `tests/test_alertmanager_render.py`, `tests/test_lifespan_alertmanager.py`, `tests/test_alerts_ingest_e2e.py`, `tests/integration/test_vmalert_pipeline.py`
- **API Endpoints Added:** none new (existing `/api/alerts/ingest` reused; auth strictness via existing `Scope.ALERTS_INGEST_WRITE` already in place from STAGE-013)
- **Placeholders Added:** `deploy/compose/test-fixtures/` directory for integration test fixtures (used by the slow `@pytest.mark.slow` integration test)
- **Session Notes:**
  - Implementation took ~9 verify cycles. Friction points captured in lessons-learned.
  - Key gotcha resolved: `BoundLogger.subscribe()` API mismatch — tests called `broker.subscribe(0)` (positional) and used `async with` context manager pattern. `SseBroker.subscribe()` is actually a no-arg async generator; tests needed `sub = broker.subscribe(); evt = await sub.__anext__()` pattern.
  - Key gotcha resolved: `Pydantic BaseModel.kind` field is not accessible at the class level — tests must use literal strings (`"alert.firing"`, `"alert.resolved"`) instead of `AlertFiringEvent.kind`.
  - Key gotcha resolved: `ensure_ingest_token` orphan-pair handling. Initial impl assumed both token + secret rows always exist together; test exposed UNIQUE constraint on second mint when only secret was deleted. Fixed by deleting whichever orphan row exists before re-minting.
  - Coverage gate green at 100.00% (1039 backend tests + UI). 6 fix iterations during Build.

**Status**: [x] Complete

## Refinement Phase

- [ ] Desktop Approved (force a high-CPU loop, see the alert appear in the dashboard)
- [ ] Mobile Approved (alert visible on Overview banner — confirmed in tests)
- [ ] Regression Items Added
- **Feedback Round 1:**
- **Feedback Round 2:**

**Status**: [ ] Complete

## Finalize Phase

- [ ] Code Review (pre-tests) — focus on AM config rendering atomicity, token rotation reload, vmalert rule shape consistency
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated: `docs/alerting/rules.md` describing how to add a vmalert rule
- [ ] Committed: specific paths only

**Commit Hash**:
**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Complete
