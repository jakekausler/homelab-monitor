# STAGE-001-004: SQLite + Alembic + first migration

## Status: Complete

## Overview

Stand up the operational database. Add SQLite via SQLAlchemy Core (NOT the ORM — see spec §6.1) behind a narrow `SqliteRepository` facade, configure Alembic for migrations, and produce the first migration that creates the *initial* set of tables: `users`, `sessions`, `audit_log`, `api_tokens`, **plus minimal-schema stubs** for the tables future stages will use so additive migrations stay focused: `targets`, `collectors`, `crons`, `heartbeats_state`, `alerts`, `alert_outcomes`, `runbooks`, `runbook_runs`, `secrets`, `channels`, `routing_rules`, `digest_configs`, `maintenance_windows`, `suggestions`, `tool_scorecards`.

**"Minimal-schema stub" rule:** the initial migration creates each future table with ONLY its identity, name/key columns, and `created_at`. Behavioral or feature-specific columns are added by the stage that implements that behavior. Examples: `collectors` has `id, name, config, created_at` here; `quarantined_at`, `quarantine_reason`, `consecutive_failures` are added by STAGE-001-008's `0002_collector_quarantine.py` when the quarantine logic lands. This way every additive migration ships alongside the code that uses it, while still avoiding "table doesn't exist yet" headaches in tests that span subsystems.

**Spec references:** §6.1 (SQLite tables), §6.5 (config: `HOMELAB_MONITOR_AUTO_MIGRATE`), §16 (UTC timestamps).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (no real secrets stored yet — that's STAGE-001-005)

## Cross-cutting standards (from EPIC-001 §Cross-stage acceptance criteria)

Every stage must satisfy: 100% kernel coverage gate; ruff + black + pyright (strict) clean; structured JSON logging via structlog; no plaintext secrets in logs (assert in tests when relevant); no `git add -A` or `git add .` (specific paths only); all internal timestamps UTC. See EPIC-001.md for the full list.

## Concrete deliverables

1. **DB connection layer** at `apps/monitor/homelab_monitor/kernel/db/`
   - `engine.py` — creates an `AsyncEngine` for SQLite (URL: `sqlite+aiosqlite:///{data_dir}/homelab-monitor.db`); WAL pragma applied on connect; foreign keys ON; busy timeout 5000ms.
   - `repository.py` — abstract `SqliteRepository` facade. No raw cursor leakage to callers; methods return dataclasses or pydantic models.
   - `migrations.py` — wraps Alembic invocations: `migrate_to_head()`, `current_revision()`, `pending_revisions()`.
2. **Alembic** at `apps/monitor/alembic/`
   - `alembic.ini` checked in, with `script_location = alembic` and `sqlalchemy.url = ${HOMELAB_MONITOR_DB_URL}`.
   - `env.py` configured to read the DB URL from env (so tests use in-memory or temp-file DBs cleanly).
   - First migration: `0001_initial_schema.py` creating all tables described above with the columns from spec §6.1.
3. **Startup integration**
   - On app boot (a stub `main()` for now — actual FastAPI shell lands in STAGE-001-010), the migration runner is invoked. Behavior controlled by env `HOMELAB_MONITOR_AUTO_MIGRATE` (default `true`). When `false`, refuses to start if pending migrations exist; instructs user to run `hm migrate`.
4. **CLI** — extend `hm` with: `hm migrate` (apply pending), `hm migrate status` (show current vs head revisions), `hm migrate history` (list revisions).
5. **Tests** at `apps/monitor/tests/unit/db/`
   - Migrate up to head from empty DB; verify all tables exist with expected columns.
   - Round-trip: `alembic downgrade -1` then `alembic upgrade head`.
   - Repository facade smoke tests with a fresh in-memory DB.
6. **Audit log helper** — small utility `audit.write(who, what, before, after)` that the rest of the codebase uses for state-changing operations. Lands in the kernel; later stages route their writes through it.

## Schema notes

- **All timestamps stored as ISO-8601 UTC strings** (`TEXT` columns). Rationale: sqlite has no native timestamptz; storing as ISO strings avoids the timezone-loss problem with the `TIMESTAMP` type in many SQLite setups. Repository helpers convert to/from `datetime` objects.
- **Primary keys** — UUIDv7 strings everywhere except `users.id` (INTEGER autoincrement is fine for a tiny user table). UUIDv7 chosen so primary keys carry creation-time ordering (helpful for the audit log and runbook runs).
- **Indexes** — at least the indexes named in spec §6.1 (alerts.fingerprint, alerts(source_tool, opened_at), runbook_runs(runbook_id, started_at), targets(kind, name), crons(host, command)).
- **Foreign keys** declared on all relations; `PRAGMA foreign_keys = ON` enforced on every connection.

## Out of scope for this stage

- Authentication logic (STAGE-001-011)
- Secrets encryption (STAGE-001-005)
- Any business logic touching the schema (just create the schema and a smoke roundtrip)
- ORM-style models (we are explicitly NOT using ORM)

## Design Phase

- **Approaches to present:** (a) all-tables-up-front initial migration (recommended — matches spec intent), (b) just-what-we-need-now and additive migrations for every new table (more "agile" but produces 30+ migrations over the project)
- **UI Options Presented:** N/A
- **User Choice:** Option (a) — all-tables-up-front. Supplementary decisions locked for Build phase below.
- **Seed Data Agreed:** none in this stage; later stages will seed test data via fixtures
- **Session Notes:** Design phase completed 2026-05-05.

**Status**: [x] Complete

### Locked decisions (Design — 2026-05-05)

**Schema strategy:** Option (a) — all 19 tables created in `0001_initial_schema.py`. Behavioral columns added by later stages via additive migrations (e.g., STAGE-001-008 → `0002_collector_quarantine.py`).

**Tables fully defined per spec §6.1:** `users`, `sessions`, `audit_log`, `api_tokens`.

**Tables created as minimal stubs (id, name/key, created_at only):** `targets`, `collectors`, `crons`, `heartbeats_state`, `alerts`, `alert_outcomes`, `runbooks`, `runbook_runs`, `secrets`, `channels`, `routing_rules`, `digest_configs`, `maintenance_windows`, `suggestions`, `tool_scorecards`.

**Engine:** Async via `sqlalchemy.ext.asyncio.create_async_engine` + `aiosqlite`. Alembic env.py uses `connection.run_sync(...)` inside an async wrapper (the standard async-Alembic pattern).

**Pragmas applied at connect:** `journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000`. Wired via `@event.listens_for(engine.sync_engine, "connect")` listener.

**Timestamps:** All TEXT columns storing ISO-8601 UTC strings. Helper `kernel/db/time.py` provides `utc_now_iso() -> str`.

**Primary keys:** UUIDv7 (str) on every table except `users.id` (INTEGER autoincrement, since bcrypt-keyed sessions need a stable lookup target). UUIDv7 generated via the `uuid-utils` PyPI package — adds as a runtime dep in `apps/monitor/pyproject.toml`.

**Repository facade:** Thin generic `SqliteRepository` at `kernel/db/repository.py` with `execute(stmt) -> Result`, `fetch_one(stmt)`, `fetch_all(stmt)`, and `transaction()` async-context-manager. Each future stage adds its own narrow query helpers as plain functions in its own module — no per-table classes upfront.

**Audit helper:** `kernel/db/audit.py` exposes `async def audit_write(repo, who, what, before=None, after=None, ip=None) -> None`. Inserts one row into `audit_log`.

**Auto-migrate env var:** `HOMELAB_MONITOR_AUTO_MIGRATE` (default `"true"`). On boot, kernel checks Alembic head vs current DB. If equal, proceed. If pending and env is `"true"`, run `upgrade head` automatically. If pending and env is `"false"`, raise `MigrationsPendingError` and refuse to start.

**CLI subcommands** added to `homelab_monitor.cli.main`:
- `hm migrate` — runs `alembic upgrade head`
- `hm migrate status` — prints current revision + pending revisions
- `hm migrate history` — prints `alembic history`

**Module layout to create:**
```
apps/monitor/homelab_monitor/kernel/
├── __init__.py
└── db/
    ├── __init__.py
    ├── engine.py       # create_async_engine + pragma listener + DATABASE_URL resolution
    ├── repository.py   # SqliteRepository facade
    ├── migrations.py   # async runner that wraps alembic upgrade/check
    ├── audit.py        # audit_write helper
    ├── ids.py          # uuid7() wrapper around uuid_utils for type stability
    └── time.py         # utc_now_iso()
apps/monitor/alembic/
├── env.py
├── script.py.mako
└── versions/
    └── 0001_initial_schema.py
apps/monitor/alembic.ini
```

**New runtime dep:** `uuid-utils>=0.10` in `apps/monitor/pyproject.toml`.

**Test plan (Build phase):** in-memory SQLite fixture (URL `"sqlite+aiosqlite:///:memory:"`); async pytest fixtures via `asyncio_mode = auto`; round-trip test (upgrade head → downgrade -1 → upgrade head); repository facade smoke tests; audit_write integration test. 100% kernel coverage gate stays green.

## Build Phase

- **Components Created:** db/{engine,repository,migrations}.py; alembic/ scaffolding + 0001_initial_schema.py; hm migrate subcommands; tests.
- **API Endpoints Added:** none (api shell is STAGE-001-010)
- **Placeholders Added:** the data-only tables for later stages (no code touches them yet).
- **Session Notes:**

### Build Summary (2026-05-05)

**Implementation completed:**
- **Kernel + DB Package:** `homelab_monitor.kernel.db` with `engine.py` (AsyncEngine, WAL pragma, foreign keys), `repository.py` (SqliteRepository facade), `migrations.py` (Alembic wrapper), `audit.py` (audit_write helper), `ids.py` (UUIDv7 wrapper), `time.py` (utc_now_iso utility)
- **Alembic Infrastructure:** async `env.py` with DB URL env resolution, first migration `0001_initial_schema.py` with 19 tables (4 fully defined: users/sessions/audit_log/api_tokens; 15 stubs: targets/collectors/crons/heartbeats_state/alerts/alert_outcomes/runbooks/runbook_runs/secrets/channels/routing_rules/digest_configs/maintenance_windows/suggestions/tool_scorecards)
- **Indexes Created:** `idx_alerts_fingerprint`, `idx_targets_name` (other indexes deferred until later stages add referenced columns)
- **CLI Subcommands:** `hm migrate`, `hm migrate status`, `hm migrate history`
- **Tests Added:** 40 total backend tests (up from 4), 100% kernel coverage gate green
- **New Runtime Dependency:** `uuid-utils>=0.10` added to `apps/monitor/pyproject.toml`
- **Verification:** `make verify` green on first pass (ruff, black, pyright strict, pytest, tsc, vitest, UI build)

**Status**: [x] Complete

## Refinement Phase

- [x] Desktop Approved
- [x] Mobile Approved (N/A; CLI/backend stage)
- [x] Regression Items Added

**Refinement Summary (2026-05-05)**

e2e-tester ran 4 scenarios — all passed (GREEN verdict):

1. **CLI subcommand smoke** — `hm migrate`, `hm migrate status`, `hm migrate history` all work; 19 tables confirmed; pragmas (journal_mode=wal, foreign_keys=1, busy_timeout=5000) fire on connect; help text accurate
2. **AUTO_MIGRATE env var gate** — `HOMELAB_MONITOR_AUTO_MIGRATE=false` raises MigrationsPendingError correctly. Boot-time wiring deferred to STAGE-001-010 (FastAPI app shell) as expected
3. **Repository facade against real (non-pytest) tempfile DB** — fetch_one, audit_write, transaction rollback all work
4. **Alembic CLI round-trip** — upgrade head → downgrade -1 → upgrade head clean

**Observation:** Direct `uv run alembic current` from `apps/monitor/` requires a custom alembic.ini override or a `./data/` directory existing. The `hm migrate` CLI handles URL resolution properly via `HOMELAB_MONITOR_DB_URL` env var. This is documented in the README touch-up during Finalize, not a code change.

**Status**: [x] Complete

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated: `docs/architecture/db.md` (or ADR) explaining Core vs ORM choice and UTC string timestamps.
- [x] Committed: specific paths only

**Finalize Summary (2026-05-05)**

Code-review gate completed with 28 findings across 4 review passes:
- **Pre-test review:** 1 critical (async transaction pattern), 8 important (Alembic env config, time helper imports, repository typing), 7 minor
- **Post-test review (2nd pass):** 3 minor findings (docstring formatting, type annotation edge cases)

All findings addressed in commit `ac7c07b`. Key improvements:
- Async transaction context manager correctness verified and hardened
- Alembic env.py environment variable resolution and async mode properly configured
- Repository facade typing cleaned up; fetch_one/fetch_all return types explicit
- Test coverage expanded: 44 total backend tests (up from 40 during Build), 100% kernel coverage maintained
- pyright strict clean; ruff + black + pytest passing

Documentation updates:
- `apps/monitor/README.md` created with DB setup, migration running, CLI reference
- Top-level `README.md` touched to link to component READMEs
- `CLAUDE.md` gotcha added: "Direct `uv run alembic` from `apps/monitor/` requires `HOMELAB_MONITOR_DB_URL` set or `./data/` dir existing; use `hm migrate` commands instead."

Commits:
- Implementation: `ac7c07b`
- Changelog backfill: `b83f8b4`

**Commit Hash**: ac7c07b (+ b83f8b4 for changelog)
**CHANGELOG Entry**: [x] Added

**Status**: [x] Complete
