# STAGE-002-006: UI redesign — 4-panel detail page, hidden replaces archive, remote-banner, wrapper-installed display

## Status: Not Started

## Overview

Redesign the Cron detail page to reflect the derived-state model. After this stage, the cron detail page is split into four clearly-labeled panels: Disk source (read-only), Monitoring policy (editable), Heartbeat state (read-only with diagnostics), and Actions (Hide/Unhide, Install heartbeat — disabled in EPIC-002, enabled in STAGE-002-009 for local host only). The Crons toolbar replaces "include_archived" with "include_hidden"; replaces the `integration_mode` filter with a `wrapper_installed` filter. List view shows a "Remote (no disk source)" badge for rows where `source_path IS NULL`.

This stage closes the visual loop on the redesign — after it ships, the UI matches the derived-state mental model and downstream stages (007 discovery, 009 wrapper helpers) extend it without further structural changes.

**Spec references:** `docs/superpowers/specs/2026-05-11-cron-derived-state-redesign.md` §UI changes.

## Stage Flags

- Has Input Forms: [x] Yes (the "Monitoring policy" panel is a form for `name`, `expected_grace_seconds`, `enabled`)
- Touches secrets / production data: [ ] No

## Cross-cutting standards (from EPIC-002 §Cross-stage acceptance criteria)

100% kernel coverage maintained (no kernel changes in this stage); ruff + black + pyright strict; UI follows established patterns; UTC ISO-8601 timestamps; specific paths only on commits. ESLint + Prettier + tsc clean. UI tests cover the new panel layout + wrapper-installed badge + remote-banner.

## Concrete deliverables

1. **CronDetail.tsx redesign** at `apps/ui/src/components/crons/CronDetail.tsx`
   - Replace single-form layout with 4-panel grid:
     - **Header:** cron name + state badge + last-seen timestamps row.
     - **Panel 1 — Disk source (read-only):** host, source_path (with "Remote — disk source not available until cross-host discovery (EPIC-015/017)" callout if NULL), schedule (+ `schedule_canonical` tooltip if different), command, "Last discovered: <ISO ts>" derived from `crons.updated_at` for now (real "last discovered" timestamp comes in STAGE-002-007 with a new `last_discovered_at` column — flag for that stage).
     - **Panel 2 — Monitoring policy (editable):** form with `name` (text input), `expected_grace_seconds` (number input), `enabled` (checkbox). Save button calls `useUpdateCron`.
     - **Panel 3 — Heartbeat state (read-only):** current_state, current_streak, last_start_at, last_ok_at, last_fail_at, last_duration_seconds, last_exit_code, wrapper_installed_at — display as "Wrapper installed via UI/CLI on <date>" or "No wrapper installed (heartbeats from ad-hoc curl)".
     - **Panel 4 — Actions:** Hide button (replaces "Archive" — same soft-delete behavior, new label) / Unhide button (only if currently hidden). "Install heartbeat" button — disabled with tooltip "Local-host install ships in STAGE-002-009; remote-host install ships in EPIC-017 (SSH probe framework)". Wrapper-health badge (placeholder until STAGE-002-010 vmalert rule + monitoring-health channel exist).
   - The form on Panel 2 keeps the existing `useUpdateCron` mutation. The PATCH body matches the trimmed schema from STAGE-002-003/004 (`name`, `expected_grace_seconds`, `enabled`, `hidden_at`).
   - On successful PATCH, refetch the cron + show success toast; do NOT navigate away.

2. **CronsToolbar.tsx update** at `apps/ui/src/components/crons/CronsToolbar.tsx`
   - Replace `integration_mode` filter with `wrapper_installed: yes/no/all` filter (queries `wrapper_installed_at IS [NOT] NULL`).
   - Rename "include archived" → "include hidden" (UI label only; backend already uses `include_hidden` after STAGE-002-003 schema rename).
   - Search field unchanged.

3. **CronsTable.tsx update** at `apps/ui/src/components/crons/CronsTable.tsx`
   - Add a "Remote (no disk source)" badge next to the host column for rows where `source_path === null`.
   - Replace `Mode` column with a small `Wrapper` indicator (✓ if `wrapper_installed_at` is set, blank otherwise).
   - Other columns unchanged: Name, Host, Schedule, State, Last OK, Streak, Hidden.

4. **ConfirmDeleteModal.tsx → ConfirmHideModal.tsx**
   - Rename the file + component. Update the type-name confirmation text from "Archive" / "Soft-delete" to "Hide". Behavior is identical (PATCH `hidden_at = utc_now_iso()`).
   - Update all imports.

5. **API hooks update** at `apps/ui/src/api/crons.ts`
   - Rename `useSoftDeleteCron` → `useHideCron` (label change only; calls the same `DELETE /api/crons/{fingerprint}` endpoint which still soft-deletes).
   - Add `useUnhideCron` if not already supported by the existing `useUpdateCron({hidden_at: null})` flow (probably reuses `useUpdateCron`).
   - All hooks already updated to use `fingerprint` instead of `cron_id` after STAGE-002-003.

6. **Sidebar / toolbar copy update**
   - "Archive" / "Archived" labels everywhere → "Hide" / "Hidden".
   - "Soft-delete" verb → "Hide".

7. **Tests**
   - Rewrite `CronDetail.test.tsx` for the 4-panel layout. Verify:
     - Disk source panel renders all read-only fields.
     - Remote-banner appears when `source_path === null`.
     - Monitoring policy form submits via `useUpdateCron`.
     - Heartbeat state panel renders timestamps + wrapper-installed text.
     - Actions panel shows Hide/Unhide and disabled "Install heartbeat" with tooltip.
   - Rewrite `CronsToolbar.test.tsx` for the new wrapper_installed filter + include_hidden label.
   - Rewrite `CronsTable.test.tsx` for the wrapper-indicator column + remote-badge.
   - Rename `ConfirmDeleteModal.test.tsx` → `ConfirmHideModal.test.tsx` and update assertions.

8. **Documentation**
   - Update `docs/architecture/heartbeat-receiver.md` UI section.
   - Update `epics/EPIC-002-heartbeat-cron/regression.md` with new manual-test checklist for the 4-panel detail page.

## Out of scope for this stage

- Discovery integration (STAGE-002-007).
- Local-host "Install heartbeat" actually doing the install (STAGE-002-009).
- Wrapper-health alert wiring (STAGE-002-010).
- Cross-host UI affordances (deferred to EPIC-015 / EPIC-017).

## Decisions in this stage

These need brainstormer-Opus input during Design phase. Pre-recommended position in **bold**.

1. **4-panel grid layout: 2x2 vs 1x4 stacked vs sidebar-style?**
   - **Recommended: 2x2 on desktop (1280+px), 1x4 stacked on mobile (<768px).** Each panel gets its own card with a clear heading. Mobile collapses to vertical stack.
   - Alternative: tabbed (one tab per panel). Hides information; user can't see policy + state at the same time.
   - Alternative: sidebar (panels 1, 3, 4 in a sticky sidebar; panel 2 as the main content). Asymmetric; better for "policy is the action" model but doesn't reflect the equal weight of disk source + state.

2. **"Install heartbeat" button placement: in Actions panel vs as a separate prominent CTA on the detail page?**
   - **Recommended: in the Actions panel.** Consistent with the disabled-tooltip pattern; doesn't compete with Hide/Unhide for attention; once enabled in STAGE-002-009 it's still in the right place.
   - Alternative: prominent CTA in the header. Feels louder; harder to ignore.

3. **Wrapper-installed display granularity: just timestamp vs timestamp + install method?**
   - **Recommended: timestamp + install method ("Wrapper installed via UI on 2026-05-12 14:30 UTC" or "Wrapper installed via CLI on 2026-05-12 14:30 UTC" or "No wrapper installed").** Install method comes from a future column we don't have yet — STAGE-002-009 will add `wrapper_install_method ENUM('ui', 'cli', 'manual') NULL`. For now, just show "Wrapper installed on <ts>" or "No wrapper installed."
   - Alternative: just "Wrapper: yes/no". Loses the timestamp; user can't see when wrapper was installed/refreshed.

4. **Hide button confirmation: same type-name confirm pattern as current "Archive" vs simpler "Are you sure?" modal?**
   - **Recommended: keep the type-name confirmation.** "Hide" is reversible (Unhide button), but type-name confirmation matches existing UX for destructive-feeling actions and prevents accidental hides via misclick.
   - Alternative: drop to simple confirm. Faster; matches the "Hide is reversible, no big deal" semantic.

## Design Phase

- **Approaches to present:** 4-panel layout options; install-button placement; wrapper-installed display; hide confirmation pattern
- **UI Options Presented:** desktop 2x2 vs sidebar; mobile stacking order
- **User Choice:**
- **Seed Data Agreed:** Use STAGE-002-003's seed (4 demo crons). Tests add a 5th demo with `source_path = NULL` and `wrapper_installed_at = '2026-05-11T00:00:00+00:00'` to validate the remote-banner + wrapper-installed display in the same row.
- **Session Notes:**

**Status**: [ ] Design (Complete)

## Build Phase

- **Components Created:**
- **API Endpoints Added:** none new (this stage is pure UI)
- **Placeholders Added:**
- **Session Notes:**

**Status**: [ ] Build (Complete)

## Refinement Phase

- [ ] Desktop Approved (4-panel detail page; new toolbar; new table column)
- [ ] Mobile Approved (panels stack correctly; remote-banner readable; toolbar wraps cleanly)
- [ ] Regression Items Added (Hide/Unhide cycle; wrapper_installed filter; remote-banner only on NULL source_path)
- **Feedback Round 1:**
- **Feedback Round 2:**

**Status**: [ ] Refinement (Complete)

## Finalize Phase

- [ ] Code Review (pre-tests) — focus on the field-readonly enforcement (no editable disk-source fields), accessibility (panel headings + aria-labelledby), mobile reflow
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated: `docs/architecture/heartbeat-receiver.md` UI section; `epics/EPIC-002-heartbeat-cron/regression.md` rewritten for the new derived-state checks
- [ ] Committed: specific paths only

**Commit Hash**:
**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Complete)
