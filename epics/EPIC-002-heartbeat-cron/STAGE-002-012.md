# STAGE-002-012: wrapper rewrite — generic shared script, run-UUID capture, per-line tagging, Vector transform

## Status: Complete

**(New stage. EPIC-002 re-opened on 2026-05-19 — see `docs/superpowers/specs/2026-05-19-cron-run-logs-design.md`. Second of five appended stages STAGE-002-011 … STAGE-002-015.)**

## Overview

Rewrite the cron heartbeat wrapper so that A-mode (wrapper-installed) crons capture their stdout/stderr with exact per-run attribution, and make the wrapper a **generic shared script** so all future wrapper upgrades are a single file replacement.

Today the wrapper `cron-with-heartbeat.sh.tmpl` bakes the cron's fingerprint into the script body at install time, so the shared file `/usr/local/bin/cron-with-heartbeat.sh` is per-cron-specific in practice and cannot be upgraded in place. This stage makes the script **byte-identical for every cron** (fingerprint passed as argument `$1`, URL base read from a config file), adds run-UUID generation, per-line UUID prefixing of captured output, explicit start/end boundary markers, and `run_id` on the heartbeat calls — and adds the Vector transform that parses the per-line marker into a `run_id` field in VictoriaLogs.

It also delivers the one-time migration path for the single existing wrapped cron via a `wrapper_format_version` marker and the existing wrapper-health badge.

**Vertical slice:** install the upgraded wrapper on a test cron, let it run; journald shows `HM_RUN_START` / `HM_RUN_END` markers and the command's output lines, all under `SYSLOG_IDENTIFIER=hmrun`, each output line prefixed `HM_RUN=<uuid> `; Vector parses the prefix into a `run_id` field; VictoriaLogs is queryable by `SYSLOG_IDENTIFIER:hmrun AND run_id:<uuid>`; the heartbeat receiver receives `run_id` on `/start` and `/ok|/fail` and (via STAGE-002-011) writes a `cron_runs` row.

**Spec references:** `docs/superpowers/specs/2026-05-19-cron-run-logs-design.md` §5 (wrapper rewrite), §3.2 (A-mode data flow), §3.3 (components), D5, D6, D7.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [x] Yes — the wrapper reads the heartbeat bearer token; the install path writes host files. Real host crontab is touched on prod-rig refinement.
- Host-integration concerns: [x] Yes — bind-mounts, host file paths, real crontab, the install executor, journald/Vector. Refinement is **3a AND 3b**.

## Cross-cutting standards (from EPIC-002 §Cross-stage acceptance criteria)

100% kernel coverage maintained; ruff + black + pyright `--strict`; the wrapper shell script is `#!/bin/sh` POSIX with `set -u`; all internal timestamps UTC; specific paths only on commits; `make verify` green before commit and at every phase exit. The wrapper rewrite changes a shipped install artifact — Refinement MUST validate the real install + the format-migration path on the prod host (3b). New API/schema changes regenerate `openapi.json`.

## Concrete deliverables

### 1. Generic wrapper template rewrite

`apps/monitor/homelab_monitor/data/cron-with-heartbeat.sh.tmpl` reworked:

- **Calling convention:** the installed crontab line becomes `cron-with-heartbeat.sh <fingerprint> -- <original command>` — fingerprint is positional argument `$1`, NOT baked into the script body.
- **Generic body:** the script body no longer substitutes `{{FINGERPRINT}}`. The shared script `/usr/local/bin/cron-with-heartbeat.sh` is **byte-identical for every cron**.
- **URL base from config:** the heartbeat URL base moves out of the script into a config file `/etc/homelab-monitor/wrapper.env` (alongside the existing `heartbeat.token`), containing `HEARTBEAT_URL_BASE=...`. The script reads it at runtime. The token file path remains a fixed constant as today.
- **`WRAPPER_FORMAT_VERSION`** — a version constant embedded in the template.
- **Per-run behavior:**
  1. Generate `RUN_ID` — `cat /proc/sys/kernel/random/uuid`, with `uuidgen` as a fallback (use absolute paths — cron has a minimal PATH).
  2. `logger --tag hmrun "HM_RUN_START fp=<fp> run=<RUN_ID>"` — a journald start boundary marker. The `--tag` is the **stable constant `hmrun`**, never a per-run value.
  3. `POST /api/hb/<fp>/start?run_id=<RUN_ID>`.
  4. Run the real command. Its combined stdout+stderr is processed two ways: (a) the **original, un-prefixed** output is echoed to the wrapper's own stdout/stderr (so a human running the cron by hand, or an existing `>> logfile` redirect, still sees normal output); (b) a copy where **each line is prefixed with `HM_RUN=<RUN_ID> `** is piped through `logger --tag hmrun` (stable identifier) → journald → Vector → VictoriaLogs.
  5. Capture the real command's exit code and the run duration.
  6. `logger --tag hmrun "HM_RUN_END fp=<fp> run=<RUN_ID> exit=<n> duration=<s>"` — a journald end boundary marker.
  7. `POST /api/hb/<fp>/ok|fail?run_id=<RUN_ID>&duration=<s>&exit_code=<n>` — `exit_code` is sent on **both** the `/ok` call (`0`, the successful run's exit code) and the `/fail` call (the actual non-zero code), not just `/fail`.
- **Heartbeat POSTs remain best-effort and bounded** (`curl --max-time 5`, output discarded, `|| true`) — a network failure NEVER blocks or alters the real command. Unchanged principle from the current wrapper.

### 2. Wrapper edge-case handling (mandatory)

- **Exit-code preservation** — the original command's exit code is ALWAYS preserved and returned as the wrapper's exit code. The `logger` pipe must NOT mask it: use a pipe-status-safe shell construct (capture the command's own status explicitly — e.g. via a named pipe / process-substitution-free POSIX technique, or `{ cmd; echo $? >fd; } | logger`). This is a known shell footgun and a specific test target (see Tests).
- **`logger` missing or failing** — if `logger` is unavailable or errors, the original output still reaches the wrapper's stdout/stderr and the run still completes. Capture degrades gracefully; the cron never breaks.
- **UUID generation failure** — if both UUID sources fail, the wrapper still runs the command (capture degrades; no `run_id` sent).

### 3. New Vector transform — `hmrun` parsing

`deploy/vector/vector.toml.template` — a new transform branch:

- Match journald lines with `SYSLOG_IDENTIFIER == "hmrun"`.
- Parse the `HM_RUN=<uuid>` prefix out of the message body into a structured field named `run_id`. Strip the `HM_RUN=<uuid> ` prefix from the message body so the stored line shows the original command output.
- `run_id` is a **regular VictoriaLogs field — NOT a stream field** (a stream field per unique run would explode stream cardinality; VictoriaLogs explicitly warns against this).
- The `HM_RUN_START` / `HM_RUN_END` boundary-marker lines also carry `SYSLOG_IDENTIFIER=hmrun` and flow through normally (they are just messages — they do not need special parsing beyond being attributed to the run window).
- Confirm the new branch does not collide with the existing cron-events branch (which filters `SYSLOG_IDENTIFIER == "CRON" || "crond"`).

### 4. Install-executor / `hm-cron-apply.sh` changes

- `_rewrite_line` emits the new `<fingerprint> --` prefix shape.
- `_build_wrapper_content` stops substituting `{{FINGERPRINT}}` and `{{HEARTBEAT_URL_BASE}}` into the script body.
- A new host operation writes `/etc/homelab-monitor/wrapper.env` with `HEARTBEAT_URL_BASE=...` (written once by the install / host-setup path). The file is written with mode `644` — it contains only a non-secret URL base, unlike the `heartbeat.token` file (`600`).
- The unwrap logic (from STAGE-002-009A) updates to strip the new `<fingerprint> --` prefix shape.
- STAGE-002-009A decisions hold: the shared wrapper script is never removed on unwrap; the shared token file is never touched.

### 5. `wrapper_format_version` column + format-outdated badge

- A new `crons` table column `wrapper_format_version`. STAGE-002-012 has no other migration, so this column gets its own alembic migration — `0016_crons_wrapper_format_version.py` (the next free number after `0015_cron_runs.py` from STAGE-002-011; confirm the head at implementation time). Set at install time to the wrapper's `WRAPPER_FORMAT_VERSION`.
- Extend the `WrapperHealth` Literal type in `apps/monitor/homelab_monitor/kernel/cron/schemas.py` (currently `Literal["ok", "stale", "unknown"]`) with a new member `"format_outdated"`.
- The `_compute_wrapper_health` logic in `kernel/api/routers/crons.py` returns `"format_outdated"` when the cron is wrapper-installed but its `wrapper_format_version` predates the run-log-capable format.
- The CronDetail wrapper-health badge (from STAGE-002-010) renders the new state: **"wrapper format outdated — re-install to enable run logs."**
- `openapi.json` regenerates (the `WrapperHealth` Literal change flows into `CronWithStateOut`).
- **No silent crontab edits** — the user re-installs each outdated cron via the existing one-click install UI. (Only one wrapped cron exists on the prod host today.)

### 6. Tests

- `apps/monitor/tests/` — wrapper-content build tests (generic body, no fingerprint baked in, the new calling convention); install-executor tests for the `<fingerprint> --` rewrite, the `wrapper.env` write, the new unwrap-prefix stripping; `wrapper_format_version` column migration round-trip; `_compute_wrapper_health` returns `"format_outdated"` for an outdated wrapper; the `WrapperHealth` Literal extension.
- **Wrapper shell-script tests** — a test harness that runs the actual wrapper script against a fake command and asserts: the exit code is preserved through the `logger` pipe (run a command that exits non-zero; assert the wrapper exits with the same code); `HM_RUN_START` / `HM_RUN_END` markers are emitted; output lines are prefixed `HM_RUN=<uuid> `; the original (un-prefixed) output still reaches stdout/stderr; behavior when `logger` is absent (capture degrades, command still runs and exit code preserved).
- Vector transform — a config-validity test (the `vector.toml.template` still renders + validates) and, if feasible, a parse test that an `HM_RUN=<uuid> ...` line yields a `run_id` field.
- UI test — the CronDetail badge renders `"format_outdated"`.
- 100% kernel coverage maintained.

## Out of scope for this stage

- The `VictoriaLogsClient` and the `CronRunReconciler` (STAGE-002-013) — this stage produces the tagged log lines; consuming them is 013.
- The B-mode log-scrape `cron_runs` write path (STAGE-002-013).
- The run-history / run-log API (STAGE-002-014).
- Anomaly detection (STAGE-002-014).
- The run-history / run-log UI routes (STAGE-002-015) — only the format-outdated badge state is in scope here.
- Bulk auto-re-wrap of outdated crons — the migration is explicitly per-cron, user-initiated, no silent edits.

## Decisions in this stage

No open architectural decisions — locked by the design spec §13. The Design phase confirms the exact pipe-status-safe shell construct for exit-code preservation, the `WRAPPER_FORMAT_VERSION` value/format, whether `wrapper_format_version` gets its own migration or folds into another, and agrees the acceptance test + the prod-rig re-wrap plan for the one existing wrapped cron.

## Locked Design Decisions

D-GENERIC — the wrapper becomes a generic shared script: fingerprint is positional argument `$1`; the URL base is read at runtime from `/etc/homelab-monitor/wrapper.env`. `/usr/local/bin/cron-with-heartbeat.sh` is byte-identical for every cron, so every future wrapper-logic upgrade is a single shared-file replacement with no per-cron re-install and no crontab edit. (Spec D5.)

D-CAPTURE — the wrapper generates a `RUN_ID` UUID per invocation, prefixes every captured output line with `HM_RUN=<RUN_ID> `, pipes the prefixed copy through `logger --tag hmrun` (a STABLE identifier, never per-run), and emits `HM_RUN_START` / `HM_RUN_END` boundary markers. The original un-prefixed output is still echoed to the wrapper's own stdout/stderr. (Spec D6.)

D-VECTORFIELD — a new Vector transform parses the `HM_RUN=<uuid>` per-line prefix into a **regular** VictoriaLogs field `run_id` (NOT a stream field — avoids stream-cardinality explosion). The A-mode run-log query is `SYSLOG_IDENTIFIER:hmrun AND run_id:<run_id>`. (Spec D6, §6.1, §14.)

D-EXITCODE — the original command's exit code is always preserved and returned through the `logger` pipe via a pipe-status-safe shell construct. `logger` missing/failing degrades capture but never breaks the cron. (Spec §5.3.)

D-RUNID-API — the wrapper threads `run_id` onto the `/start|/ok|/fail` heartbeat calls, and sends `exit_code` on **both** `/ok` (exit `0`) and `/fail` (the non-zero code) — not just `/fail` (the receiver side ships in STAGE-002-011; this stage makes the wrapper actually send it).

D-FORMATVER — legacy wrappers are migrated via a `wrapper_format_version` column on `crons` + a new `"format_outdated"` member on the `WrapperHealth` Literal + the existing wrapper-health badge. The user re-installs per cron via the one-click UI. NO silent crontab edits — consistent with the EPIC-002 "no cron modified without dashboard confirmation" policy. (Spec D7.)

## Design Phase

- **Approaches to present:** the exit-code-preservation shell construct; the `WRAPPER_FORMAT_VERSION` format; the `wrapper_format_version` migration placement; the `format_outdated` badge wording.
- **UI Options Presented:** the wrapper-health badge wording for the `format_outdated` state.
- **User Choice (3 decisions):**
  1. **Exit-code preservation = the tempfile technique.** The wrapper runs the real command in `{ realcmd; echo $? > "$codefile"; } | <tee-and-logger loop>`, then reads the exit code back via `code=$(cat "$codefile")`. Pure POSIX `sh` (works in dash/busybox — no `mkfifo` dependency, no `pipefail`/`$PIPESTATUS`). The temp file is cleaned via a `trap` on `EXIT INT TERM`. This is necessary because the `logger` pipe runs the read-loop in a subshell, so the exit code cannot travel via a shell variable — it must go through a file. A missing/unreadable codefile falls back to a non-zero exit code.
  2. **`WRAPPER_FORMAT_VERSION` = a semver string.** The run-log-capable generic wrapper is format `1.0.0` (a clean baseline for the new generic wrapper). The pre-run-log baked-fingerprint wrapper has NO `wrapper_format_version` recorded → treated as outdated. The wrapper script itself does NOT parse semver; only the backend `_compute_wrapper_health` compares the installed `wrapper_format_version` against the current known format, using a proper Python semver/tuple comparison.
  3. **`format_outdated` badge wording = "Re-install to enable run logs".** The `WrapperHealth` Literal gains the enum member `"format_outdated"` (internal value); the cron-detail wrapper-health badge renders that state as the text "Re-install to enable run logs" in an amber/warning style. Build must ensure the ActionsPanel row handles the longer text gracefully at mobile width (truncate/wrap).
- **Confirmed in Design:** (1) the current wrapper template, the install machinery (`_build_wrapper_content`, `_rewrite_line`, `install_wrapper_local`/`uninstall_wrapper_local`, the `WRAPPER_INVOCATION_PREFIX`/`WRAPPER_PATH`/`TOKEN_FILE_PATH` constants in `wrapper_constants.py`), and `scripts/hm-cron-apply.sh` were all read — the host executor already supports the `<path> ... -- <command>` line shape, so the new `<fingerprint> --` calling convention needs only the prefix-string change. (2) STAGE-002-011 shipped the heartbeat-side `run_id` support — all three query classes accept the optional `run_id` (and `HeartbeatOkQuery` accepts `exit_code`). (3) `WrapperHealth` is currently `Literal["ok", "stale", "unknown"]` and `_compute_wrapper_health` is in `kernel/api/routers/crons.py` — both ready to extend with `format_outdated`. (4) The alembic head is `0015` — the new `wrapper_format_version` migration is `0016_crons_wrapper_format_version.py` (its own standalone migration, as locked in the stage file). (5) The Vector `cron-events` transform branch filters `SYSLOG_IDENTIFIER == "CRON" || "crond"`; the new `hmrun` transform branch filters `SYSLOG_IDENTIFIER == "hmrun"` — no collision; journald's `SYSLOG_IDENTIFIER` is available as `.SYSLOG_IDENTIFIER` in Vector events and the message body as `.message`.
- **Seed Data Agreed:** acceptance test — install the upgraded wrapper on a test cron, run it, and observe in journald the `HM_RUN_START`/`HM_RUN_END` boundary markers + the command output lines, all under `SYSLOG_IDENTIFIER=hmrun`, each output line prefixed `HM_RUN=<uuid> `; Vector parses the prefix into a `run_id` field; VictoriaLogs is queryable by `SYSLOG_IDENTIFIER:hmrun AND run_id:<uuid>`; the heartbeat receiver receives `run_id` and (via STAGE-002-011) a `cron_runs` row is written. Prod-rig (3b): re-wrap the one existing prod wrapped cron onto the new `<fingerprint> --` convention.
- **Session Notes:** Stage consistency check PASS. Design context gathered via an Explore subagent. Architecture otherwise fully locked by design spec `docs/superpowers/specs/2026-05-19-cron-run-logs-design.md` §5 + D5/D6/D7. Design phase 2026-05-19.

**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:** the rewritten generic wrapper template `cron-with-heartbeat.sh.tmpl` (POSIX `sh`, byte-identical for every cron — fingerprint passed as positional arg `$1`, URL base read at runtime from `/etc/homelab-monitor/wrapper.env`, run-UUID generation, per-line `HM_RUN=<uuid>` output prefixing through `logger --tag hmrun`, `HM_RUN_START`/`HM_RUN_END` boundary markers, the tempfile exit-code-preservation technique with a `trap`, `WRAPPER_FORMAT_VERSION=1.0.0`); `wrapper_constants.py` reworked (regex-based variable-fingerprint prefix matching, `build_invocation_prefix`, `is_wrapped`, `unwrap_command`, `wrapped_fingerprint`, `WRAPPER_ENV_PATH`, `WRAPPER_FORMAT_VERSION`); a new `WriteWrapperEnvOp` IPC op (+ `OP_WRITE_WRAPPER_ENV`); alembic migration `0016_crons_wrapper_format_version.py` (the `wrapper_format_version` TEXT column on `crons`); the `hmrun` Vector transform branch in `vector.toml.template`; the `WrapperHealth` Literal extended with `format_outdated` + the `_compute_wrapper_health` semver logic.
- **API Endpoints Added:** None. Existing behavior changed: the wrapper now threads `run_id` (+ `exit_code` on `/ok` and `/fail`) onto the heartbeat calls; the cron-detail response's `wrapper_health` field can now return `format_outdated`; `openapi.json` regenerated.
- **Placeholders Added:** None.
- **Session Notes:** Build implemented via planner (Opus) spec + multi-pass scribe/test-writer (the implementation truncated twice and was completed across passes). `make verify` reached GREEN after an extended fix cycle that surfaced TWO genuine implementation bugs (not test drift): (1) `_find_matching_line` in install.py consumed `parse_one_line`'s output as the pre-unwrap command, but `parse_one_line` already unwraps — so `is_wrapped()` was always False (already-wrapped lines undetected; every uninstall wrongly raised NotWrappedError). Fixed: `parse_one_line` gained an `unwrap: bool = True` param; `_find_matching_line` now returns a `line_is_wrapped` bool computed from the genuine pre-unwrap command. (2) the remote-installer `_run_uninstall` recomputed the fingerprint instead of extracting it from the crontab line — fixed to use `wrapped_fingerprint()`. The remaining ~44 verify failures were legitimate STALE TESTS (the new `<fingerprint> --` prefix, the new 4-op install with `write-wrapper-env`, the generic wrapper's 3 placeholders replacing the old 5, the `format_outdated` health state, a `parents[4]`→`parents[3]` path bug in a new test) — all updated. Final `make verify` GREEN: 2006 backend tests passed (1 environmental skip), 100.00% kernel coverage, 245 UI tests, 79.96% UI branch coverage, 35 eslint warnings (baseline). Build phase 2026-05-19.

**Status**: [x] Build (Complete)

## Refinement Phase

- [x] Desktop Approved (the `format_outdated` wrapper-health badge on the cron-detail ActionsPanel — user-approved against the prod instance)
- [x] Mobile Approved (the badge at mobile viewport — user-approved; the longer "Re-install to enable run logs" text wraps/truncates gracefully)
- [x] Regression Items Added
- **Feedback Round 1:** Host-integration stage → Refinement was **3a (dev rig) AND 3b (prod rig)**. Both complete. Three bugs found and fixed during Refinement; `make verify` GREEN after the fixes (2027 backend tests, 100% kernel coverage, 245 UI tests). The `format_outdated` UI badge was user-approved at desktop and mobile viewports.

**Refinement Results (3a + 3b):**

**3a — Dev rig (synthetic data):** e2e-tester validated the generic wrapper against the dev rig. PASS: journald `HM_RUN_START`/`HM_RUN_END` boundary markers + per-line `HM_RUN=<uuid>` output prefixing under `SYSLOG_IDENTIFIER=hmrun`; the Vector `hmrun` transform config (static validation); the heartbeat receiver writes `cron_runs` rows with `run_id`; exit-code preservation through the `logger` pipe (the tempfile technique — verified with non-zero exit codes 7 and 42 and exit 0). Two items (live Vector→VictoriaLogs processing, the VL query) were honestly deferred to 3b — the dev rig could not write the Vector config volume without root.

**3b — Prod rig (real host, full compose stack):** the prod stack was rebuilt with STAGE-012 code. **Three bugs found and fixed:**
1. **Bugs B+C (STAGE-012 — the format-migration path was broken):** `is_wrapped`/`unwrap_command` recognized only the NEW wrapper format, so legacy (pre-STAGE-012, baked-fingerprint) wrapped crontab lines were invisible to discovery and install — the real `rtlamr-watchdog.sh` cron got soft-deleted, a phantom cron appeared, and install-wrapper double-wrapped. FIXED: `wrapper_constants.py` now recognizes BOTH formats (dual-format `is_wrapped`/`unwrap_command`, a new `is_legacy_wrapped` helper); `install.py`'s `_find_matching_line`/`build_install_kit`/`_rewrite_line` are legacy-aware (a legacy line is re-wrapped, not rejected, not double-wrapped); `scripts/hm-cron-apply.sh`'s `verify_wrap` accepts the legacy→current re-wrap. The fix AUTO-HEALS the prod DB on the next discovery scan (the real cron is restored, the phantom soft-deleted) — confirmed.
2. **Bug A (pre-existing — VictoriaLogs ingestion broken):** the Vector `[sinks.vl]` URI was missing `?_msg_field=message`, so VictoriaLogs silently discarded every event (VL only indexes the `_msg` field; Vector emits `message`). FIXED in `vector.toml.template`. VictoriaLogs had had ZERO indexed events for ~28h until this fix.
3b RE-validation after the fixes — ALL PASS: prod stack rebuilt; DB auto-heal confirmed (`b3d04508` rtlamr restored, phantom `6e4b735c` soft-deleted on the first discovery scan); the `hmrun` journald→Vector→VictoriaLogs path works (VL now indexes events; `run_id` extracted, `HM_RUN=` prefix stripped from the stored body); the real `rtlamr-watchdog.sh` cron was re-wrapped via the format-migration path — crontab line moved to the new `<fingerprint> --` convention (single wrap, no double-wrap), `wrapper_health` `format_outdated`→`ok`, `/etc/homelab-monitor/wrapper.env` written, the host shared wrapper is now the generic STAGE-012 script; a real cron-scheduled run produced `hmrun` lines in VictoriaLogs + a `cron_runs` row.

**Deployment-procedure note (carry to Finalize docs):** the host cron-apply executor `/usr/local/sbin/hm-cron-apply` is a NON-containerized host file deployed by `scripts/host-setup.sh`. A `docker compose up --build` does NOT update it. STAGE-012 changed `scripts/hm-cron-apply.sh`, so deploying STAGE-012 requires re-running `sudo bash scripts/host-setup.sh` on the host (it is idempotent — only reinstalls changed files). `docs/deploy/host-prerequisites.md` currently implies host-setup.sh is one-time-only; Finalize must update that doc to state it must be re-run when `hm-cron-apply.sh` changes. Also: a monitor-only `docker compose up --build monitor` does NOT recreate Vector — `docker compose restart vector` is needed for Vector to pick up a re-rendered config. **(Superseded by STAGE-007-016A: vector now runs with `--watch-config` and auto-reloads after the monitor re-renders — explicit vector restart is now only a fallback.)**

**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests) — code-reviewer (Opus): APPROVED, 0 Critical, 0 Important, 4 Minor findings.
- [x] Tests Written — written during Build + extended during Refinement's bug fixes; one additional test (`test_install_public_url_with_newline_raises`) added in Finalize for the `public_url` newline guard.
- [x] Code Review (post-tests) — second-pass code-reviewer (Opus): APPROVED, all 4 first-pass findings correctly fixed, no regressions.
- [x] Documentation Updated — `docs/deploy/host-prerequisites.md` updated to close the deploy-procedure gap found in Refinement (host-setup.sh must be re-run when the non-containerized host scripts change). `install-heartbeat.md` / `remote-install.md` / `cron-identity.md` checked — accurate as-is.
- [x] Committed: specific paths only

**Commit Hash**: 22b7517 (impl), df29fc9 (changelog)
**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize (Complete)

Finalized 2026-05-19 — code review (2 passes, 4 minor findings fixed: stale docstring, misleading test fixtures, a `public_url` newline guard + its test, a Vector comment), `docs/deploy/host-prerequisites.md` deploy-gap fix, make verify green (2029 backend / 100% kernel coverage / 245 UI), commits 22b7517 (impl) + df29fc9 (changelog).
