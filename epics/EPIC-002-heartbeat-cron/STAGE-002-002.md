# STAGE-002-002: Cron registry CRUD API + Inventory "Crons" tab UI

## Status: Not Started

## Overview

Build the user-facing surface for the cron registry: a typed CRUD API for `crons` rows (list, get, create, update, soft-delete) plus a "Crons" tab in the Inventory screen that shows every known cron with last-seen state, expected next run, current streak, and integration mode. Lets the user manually add a cron the auto-discovery missed (e.g., a cron on a remote host that the local discoverer can't see), edit cadence/grace, flip integration mode between `observe`/`heartbeat`/`both`, and soft-delete crons that have been retired.

Vertical slice: from a logged-in browser, navigate to Inventory → Crons, see all rows from STAGE-002-001's seed data, click a row to open detail, edit the grace period to 600s, save, and see the updated row in the list. The cron's `last_seen_state` continues to update as heartbeats arrive (proves the STAGE-001 path still works through the new UI surface).

**Spec references:** §3.1 (UI screens — Inventory tab), §6.1 (`crons` table contract), §10.4 (audit log on edits), §10.5 (CSRF on mutating endpoints), §16 (timezone handling — display in local TZ).

## Stage Flags

- Has Input Forms: [x] Yes (cron create + edit forms — exhaustive form validation tests required)
- Touches secrets / production data: [ ] No (no secrets stored on `crons` rows; tokens live elsewhere)

## Cross-cutting standards (from EPIC-002 §Cross-stage acceptance criteria)

100% kernel coverage; ruff + black + pyright (strict); structlog JSON logs; no plaintext secrets in logs; specific paths only. UI follows the patterns established in EPIC-001's UI shell (TanStack Router + Query, Radix + Tailwind, React Hook Form + Zod for forms). All cron schedule expressions parsed in host-local TZ; all internal timestamps UTC; display layer converts on render.

## Concrete deliverables

1. **Expand the `crons` router** at `apps/monitor/homelab_monitor/kernel/api/routers/crons.py` (created in STAGE-002-001):
   - `GET /api/crons` → paginated list (default 100, max 500); query filters: `host`, `integration_mode`, `enabled`, `state` (filters on `last_seen_state`), `q` (substring on `command` or `name`)
   - `GET /api/crons/{cron_id}` → full row + joined `heartbeats_state`
   - `POST /api/crons` → create; body validates against `CronCreate` schema (host, command, schedule, name, cadence_seconds OR schedule, expected_grace_seconds, integration_mode); 409 if `(host, command)` already exists; CSRF required
   - `PATCH /api/crons/{cron_id}` → partial update; body validates against `CronUpdate`; 404 if missing; CSRF required; emits audit log entry with `before`/`after` JSON of changed fields only
   - `DELETE /api/crons/{cron_id}` → soft-delete (sets `deleted_at`); CSRF required; cascades to disable `heartbeats_state` row (does NOT delete it; preserves history)
   - All endpoints session-auth (not token-auth — this is dashboard-facing)

2. **Schemas** at `apps/monitor/homelab_monitor/kernel/schemas/cron.py`
   - `CronOut` (id, host, name, command, schedule, cadence_seconds, expected_grace_seconds, integration_mode, enabled, last_seen_state, created_at, updated_at, deleted_at, heartbeat: HeartbeatStateOut | None)
   - `CronCreate` (host: str, name: str, command: str, schedule: str | None, cadence_seconds: int | None, expected_grace_seconds: int = 300, integration_mode: Literal["observe", "heartbeat", "both"] = "observe", enabled: bool = True). Validator: exactly one of `schedule` OR `cadence_seconds` must be set.
   - `CronUpdate` (all fields optional; same validators).
   - `CronListResponse` (items: list[CronOut], total: int, page: int, page_size: int)

3. **Soft-delete migration** at `apps/monitor/alembic/versions/XXXX_stage_002_002_crons_soft_delete.py`
   - ALTER TABLE `crons` ADD COLUMN `deleted_at TIMESTAMP` (NULL = not deleted)
   - ALTER TABLE `crons` ADD COLUMN `name TEXT` (display name; defaults to derived from command on create)
   - ALTER TABLE `crons` ADD COLUMN `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`
   - ALTER TABLE `crons` ADD COLUMN `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`
   - All `GET /api/crons*` endpoints automatically filter `deleted_at IS NULL` unless `?include_deleted=1` is passed (admin recovery path).

4. **Croniter integration** at `apps/monitor/homelab_monitor/kernel/cron/schedule.py`
   - `parse_schedule(expr: str, tz: str) -> CronSchedule` (validates the expression; raises `InvalidCronExpression` with helpful message)
   - `next_run_after(expr: str, after: datetime, tz: str) -> datetime` (returns UTC)
   - `prev_run_before(expr: str, before: datetime, tz: str) -> datetime`
   - Handles `@reboot` specially (returns sentinel; no `next_run_after` value).
   - Uses `croniter` library (added to `pyproject.toml`).

5. **UI: Crons tab in Inventory** at `apps/ui/src/pages/inventory/CronsTab.tsx`
   - Route: `/inventory/crons` (TanStack Router; nested under `/inventory`)
   - Table columns: Name (linked), Host, Schedule (or cadence display), Mode (badge: observe/heartbeat/both), Last seen state (badge: ok/fail/stale/unknown), Last OK (relative time tooltip absolute), Next due (relative time, "OVERDUE" badge if past), Streak
   - Top toolbar: filter dropdowns (host, mode, state), search input (debounced 250ms), "+ Add cron" button
   - Row click → opens detail panel (right drawer or row expansion; Design phase decides)
   - Polling: TanStack Query with 30s `refetchInterval` (matches the heartbeat metrics emitter cadence)

6. **UI: Cron detail/edit form** at `apps/ui/src/pages/inventory/CronDetail.tsx`
   - Renders all fields editable: name, schedule, cadence_seconds, expected_grace_seconds, integration_mode, enabled
   - Schedule editor: text input with live validation (debounced call to `POST /api/crons/validate-schedule` — a tiny endpoint that wraps `parse_schedule`); preview of next 3 expected runs
   - Mode toggle: tri-state segmented control (observe / heartbeat / both); changing to `heartbeat` or `both` shows a callout: "Heartbeat mode requires the cron to POST to /hb/{id}. See STAGE-002-005 wrapper installer."
   - Save: PATCH; success toast; optimistic update via TanStack Query
   - Delete: red "Soft-delete" button with `<ConfirmDialog>` requiring typing the cron name (matches the "confirm-on-destructive" pattern from STAGE-001-011)

7. **UI: Add-cron modal** at `apps/ui/src/pages/inventory/CronAddModal.tsx`
   - Fields: name, host (defaulted to current host hostname), command, mode (default `observe`), one-of (schedule string OR cadence_seconds with unit dropdown s/m/h/d), expected_grace_seconds
   - Submit → POST → close modal → invalidate query → row appears in table

8. **API client + types** auto-generated via `apps/ui/src/api/schema.ts` (existing OpenAPI codegen path)
   - Run `pnpm generate-types` after backend changes; commit the regenerated types alongside the UI changes

9. **Tests**
   - **Backend** at `apps/monitor/tests/kernel/api/routers/test_crons.py`:
     - List with pagination + filters (host, mode, state, q substring)
     - GET 404 for missing
     - POST creates row + audit entry; 409 on dup; CSRF required
     - PATCH updates fields + audit `before`/`after` shows only changed fields
     - DELETE sets `deleted_at`; subsequent GET excludes by default; `?include_deleted=1` includes
     - Schedule validation: invalid expression returns 400 with field-level error
     - Exactly-one-of (schedule | cadence_seconds) validator works
   - **Backend** at `apps/monitor/tests/kernel/cron/test_schedule.py`:
     - `parse_schedule("17 * * * *", "America/New_York")` succeeds
     - `parse_schedule("invalid")` raises `InvalidCronExpression` with line/column hint
     - `next_run_after("0 4 * * *", at_now, "America/New_York")` returns next 04:00 local in UTC
     - DST spring-forward handling (cron at 02:30 on the spring-forward day → skips that day, returns next valid)
     - `@reboot` returns sentinel; `next_run_after` raises NoNextRun
   - **UI** at `apps/ui/src/pages/inventory/__tests__/CronsTab.test.tsx`:
     - Table renders rows from mock API
     - Filter changes URL search params
     - Search debounces 250ms
     - "+ Add cron" opens modal
     - Mode badge color reflects mode
   - **UI** at `apps/ui/src/pages/inventory/__tests__/CronDetail.test.tsx`:
     - Form pre-fills from API
     - Schedule preview updates on input
     - Save sends PATCH with only changed fields
     - Delete requires typing name
   - **Integration** at `apps/monitor/tests/integration/test_crons_crud_e2e.py`:
     - Create cron via POST → POST `/hb/{id}/ok` → GET `/api/crons/{id}` shows `last_seen_state=ok` (proves STAGE-001 + STAGE-002 path together)

## Out of scope for this stage

- Cron auto-discovery (STAGE-002-003) — manual create only
- B-mode log-scrape (STAGE-002-004)
- C-mode wrapper helpers + dashboard "Install heartbeat" action (STAGE-002-005) — the mode toggle in this stage just sets the column; STAGE-002-005 makes the heartbeat mode actually useful
- vmalert rules (STAGE-002-006)
- Bulk operations on crons (multi-select; defer to a follow-up if needed)
- Cron run history view (last 100 runs with timestamps + exit codes — needs B-mode in place; defer to STAGE-002-004 or a polish stage)

## Decisions in this stage

These need brainstormer-Opus input during Design phase. Pre-recommended position in **bold**.

1. **Cron schedule storage format: cron expression string vs structured (minute/hour/day/month/dow) parsed columns?**
   - **Recommended: cron expression string.** Matches what the user types in `crontab -e`. Structured columns are easier to query but lose round-trip fidelity (`@hourly`, `*/5`, `MON-FRI` all canonicalize lossy-ly). croniter parses the string at use-time; we don't query schedule fields directly anyway (filtering is on host/mode/state).
   - Alternative: parse to structured columns, render back to canonical string. Defensible if we expect a "show me all crons that run on weekends" query later, but no such query is in scope for EPIC-002 or near-term.

2. **Soft-delete vs hard-delete?**
   - **Recommended: soft-delete (`deleted_at` column).** Audit-log shows the delete; `heartbeats_state` row preserved for history; admin can un-delete via `?include_deleted=1` + PATCH. Only downside: query writers must remember the filter (added by default in the repo layer, opt-out via `include_deleted=True`).
   - Alternative: hard-delete with audit-log capture of the deleted row. Simpler queries, harder recovery.

3. **Audit entry granularity on PATCH: full-row diff vs changed-fields only?**
   - **Recommended: changed-fields only.** Reduces audit_log row size; the `before`/`after` JSONs only include the keys that changed. Reconstructing full state from the audit log requires walking back through entries, but that's acceptable — audit-log is for "what happened?" not "what was the state at time T?".
   - Alternative: full-row snapshots. Easier point-in-time reconstruction, but bloats audit_log fast (rows are wide).

4. **Detail view: drawer (slide-in from right) vs row expansion vs separate route page?**
   - **Recommended: separate route page at `/inventory/crons/{id}`.** Bookmarkable, deep-linkable from alerts ("alert on cron X" → click → land on detail page). Drawer is faster but breaks deep-linking. Row expansion looks cool but limits the detail UI to ~400px height.
   - Alternative: drawer with URL state preserved. More modern feel, more JS.

## Design Phase

- **Approaches to present:** [TBD]
- **UI Options Presented:** mockups for the Crons tab table layout; the detail page; the add-cron modal — each at desktop (1440px) and mobile (390px) breakpoints
- **User Choice:**
- **Seed Data Agreed:** STAGE-002-001's three seed crons (one per mode) extended with `name`, `created_at`, `updated_at`. Add a fourth seed cron in `observe` mode with `last_seen_state=stale` to verify the stale badge renders.
- **Session Notes:**

**Status**: [ ] Design (Complete)

## Build Phase

- **Components Created:**
- **API Endpoints Added:**
- **Placeholders Added:**
- **Session Notes:**

**Status**: [ ] Build (Complete)

## Refinement Phase

- [ ] Desktop Approved (Crons tab + detail page + add-cron modal at 1440px)
- [ ] Mobile Approved (Crons tab + detail page + add-cron modal at 390px; table collapses to card layout)
- [ ] Regression Items Added (heartbeat path through STAGE-001-021 still works via new UI surface; existing alert pipeline unaffected)
- **Feedback Round 1:**
- **Feedback Round 2:**

**Status**: [ ] Refinement (Complete)

## Finalize Phase

- [ ] Code Review (pre-tests) — focus on form validation completeness, CSRF coverage, soft-delete query default, audit log diff granularity
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated: `docs/ui/inventory-crons.md` — screenshots + workflow walkthrough
- [ ] Committed: specific paths only

**Commit Hash**:
**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Complete)
