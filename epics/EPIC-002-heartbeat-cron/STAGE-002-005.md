# STAGE-002-005: `/api/hb/{fingerprint}/register` endpoint + heartbeat receiver behavior change

## Status: Complete

## Overview

Add the `/register` endpoint that creates `crons` rows from heartbeat-side metadata. After this stage, a wrapper script (or any heartbeat client) can POST `{host, source_path, schedule, command, wrapper: bool}` to `/api/hb/{fingerprint}/register` and the server creates the matching cron row idempotently. The heartbeat receiver continues to 404 on unknown fingerprints for `/start`, `/ok`, `/fail` — `/register` is the only creation path on the heartbeat side.

This stage formalizes the "heartbeat-first" creation path that was previously rejected in STAGE-002-001 D1 (404 on unknown). The reversal is justified by the new derived-state model where the heartbeat wrapper is a legitimate authority on what it's wrapping.

**Spec references:** `docs/superpowers/specs/2026-05-11-cron-derived-state-redesign.md` §Heartbeat receiver behavior, §API surface changes.

## Stage Flags

- Has Input Forms: [ ] No (server-side only; the wrapper is a separate client)
- Touches secrets / production data: [ ] No

## Cross-cutting standards (from EPIC-002 §Cross-stage acceptance criteria)

100% kernel coverage maintained; ruff + black + pyright strict; structlog JSON logs; UTC ISO-8601 timestamps; specific paths only on commits. EPIC-002 cross-stage criterion #9 ("the heartbeat receiver does NOT create cron rows on first ping") is **superseded** by this stage's introduction of `/register`. EPIC-002.md is updated.

## Concrete deliverables

1. **New API endpoint** at `apps/monitor/homelab_monitor/kernel/api/routers/heartbeat.py`
   - `POST /api/hb/{fingerprint}/register`
   - Auth: `Scope.HEARTBEAT_WRITE` (token-only).
   - Path param: `fingerprint` — 64-char SHA256 hex (regex-validated).
   - Body (Pydantic `RegisterCronBody`):
     ```python
     class RegisterCronBody(BaseModel):
         host: str = Field(min_length=1, max_length=255)
         source_path: str | None = Field(default=None, max_length=500)
         schedule: str = Field(min_length=1, max_length=200)
         command: str = Field(min_length=1, max_length=2000)
         wrapper: bool = False
         model_config = ConfigDict(extra='forbid')
     ```
   - Behavior:
     1. Compute `expected_fingerprint = SHA256(f"{body.host}|{body.source_path or ''}|{body.schedule}|{body.command}").hexdigest()`.
     2. If `expected_fingerprint != path_fingerprint` → 422 with detail `{"fingerprint_mismatch": True}`.
     3. Validate `body.schedule` via croniter; reject 422 on invalid.
     4. Compute `cadence_seconds` and `schedule_canonical` from schedule.
     5. Default `name` to command basename.
     6. Try INSERT. If row exists with same fingerprint:
        - Compare ALL stored metadata fields to body. If identical → 200 idempotent (no audit row, return existing CronOut).
        - If any field differs → 409 with detail `{"fingerprint_collision": True, "expected": <stored fields>, "received": <body>}`. (Should never happen by construction; defensive check.)
     7. If new row created: set `wrapper_installed_at = utc_now_iso()` if `body.wrapper`. Audit verb `crons.register` with full row in `after`.
   - Returns 201 (created) or 200 (idempotent existing).

2. **Heartbeat receiver behavior unchanged**
   - `POST /api/hb/{fingerprint}/{start,ok,fail}` still returns 404 for unknown fingerprints. No change to the existing logic from STAGE-002-001 (now reading via fingerprint per STAGE-002-003).

3. **CronRepo extension** at `apps/monitor/homelab_monitor/kernel/cron/repository.py`
   - New method `async def register_cron(self, body: RegisterCronBody, *, who: str, ip: str | None) -> tuple[CronRecord, bool]` returning `(record, created)`. The `created` boolean indicates 201 (true) vs 200 (false-idempotent).
   - The audit verb is `crons.register`. The `who` field in audit is the token name (per the existing pattern from STAGE-002-001).
   - Same atomic-transaction discipline as `update_cron`: INSERT + audit_log INSERT in one transaction.

4. **Pydantic schemas** at `apps/monitor/homelab_monitor/kernel/cron/schemas.py`
   - Add `RegisterCronBody` (above).
   - The response from `/register` reuses `CronOut` (no new response schema).

5. **Tests**
   - `apps/monitor/tests/test_api_register.py` (NEW):
     - 201 on first registration; payload roundtrips correctly.
     - 200 idempotent on second identical registration; no audit row written second time.
     - 422 on fingerprint mismatch (URL hash != computed body hash).
     - 422 on invalid schedule.
     - 422 on missing required field.
     - 401 without token; 403 with wrong scope token.
     - `wrapper: true` sets `wrapper_installed_at`.
     - `wrapper: false` leaves `wrapper_installed_at = NULL`.
     - 422 on unknown extra field (extra='forbid' check).
     - Heartbeat receiver still returns 404 for unknown fingerprints AFTER /register exists (parametrized check that they're separate code paths).
   - `apps/monitor/tests/test_cron_repo.py` extended:
     - `register_cron` happy path; idempotent re-register; audit verb correct.
   - `test_fingerprint_computation.py` (from STAGE-002-003) extended:
     - Server-computed fingerprint matches a hand-computed reference for known inputs.

6. **Documentation**
   - Update `docs/architecture/heartbeat-receiver.md`:
     - Document the `/register` endpoint contract.
     - Document the "404 on unknown for /start//ok//fail; call /register first" protocol.
     - Add diagram showing the wrapper's interaction sequence.
   - Update `docs/architecture/cron-identity.md` (from STAGE-002-003):
     - Document the conflict-resolution policy (409 on metadata mismatch — defensive only).

## Out of scope for this stage

- The wrapper script itself (STAGE-002-009) — this stage only ships the server endpoint that the wrapper will call.
- UI redesign — the existing UI is unaware of `/register`. UI changes in STAGE-002-006.
- Discovery integration — the discovery path also creates rows (with audit verb `crons.discover`); discovery itself is in STAGE-002-007.

## Design Decisions Locked

The following ten decisions have been locked by design review on 2026-05-12:

| # | Decision | Locked Choice | Rationale |
|---|----------|---------------|-----------|
| D1 | Idempotent response | 201 Created on first insert; 200 OK on idempotent re-register. Both return CronOut body. | REST-idiomatic upsert; status code carries create-vs-existed signal. |
| D2 | `wrapper: true` on re-register | Refresh `wrapper_last_seen_at` on every successful re-register with `wrapper: true`. | STAGE-002-010 WrapperPossiblyStale rule needs the freshest timestamp. Audit log preserves history. |
| D3 | Audit `who` attribution | Token name. | Consistency with existing `heartbeat.start/ok/fail` audit rows. |
| D4 | Rate limiting | Same per-fingerprint 60/min bucket as `/start/ok/fail`. Reuse `cron_rate_limiter` singleton. | One policy, simpler operator model. /register is rare. |
| D5 | Hidden cron behavior | `hidden_at IS NOT NULL` means **display + notification suppression ONLY**. Data capture continues for hidden crons. `/register`, `/start`, `/ok`, `/fail` all operate normally on hidden fingerprints. **In-stage fix:** remove `AND hidden_at IS NULL` from `_SELECT_CRON_SQL` in `kernel/heartbeat/repository.py`; update existing tests asserting 404-on-hidden. | Hidden is presentation/notification, not soft-delete. Operator wants to silence noise without losing data. Notification suppression is a STAGE-002-010 concern (vmalert rules gate on hidden). |
| D6 | Defensive 409 | NO defensive 409 path. 422 on URL-vs-body fingerprint mismatch is sufficient. **In-stage doc edit:** delete the "409 metadata mismatch" paragraph from `docs/architecture/cron-identity.md`. | Fingerprint IS the metadata hash; mismatch is impossible without hash bug. Defensive dead code obscures intent. |
| D7 | Idempotent semantics for policy fields | `/register` sets `name = derive_name(command)`, `expected_grace_seconds = 300`, `enabled = true` ONLY on first insert. Re-register NEVER touches `name`, `expected_grace_seconds`, `enabled`, or `hidden_at`. Only `wrapper_last_seen_at` can mutate on re-register (per D2). | Identity (wrapper) vs policy (operator) split preserved. Wrapper re-install must not clobber operator's UI edits. |
| D8 | Auth scope | Reuse `Scope.HEARTBEAT_WRITE`. | Wrapper threat model is unitary; no scope separation value. |
| D9 | Status code matrix | 201 Created (new); 200 OK (idempotent re-register, with or without `wrapper_last_seen_at` refresh); 401 (no token); 403 (wrong scope); 422 with `{"fingerprint_mismatch": True}` (URL hash ≠ computed body hash); 422 with `{"invalid_schedule": True, "reason": ...}` (croniter rejects); 422 (body validation: missing/extra/length via Pydantic default envelope); 429 (rate limit). **No 404** (hidden no longer triggers it); **no 409** (per D6); **no 503**. | Locked. |
| D10 | Audit row format | **Path 3 — audit every wrapper handshake.** Every `/register` that creates a row OR mutates `wrapper_last_seen_at` writes a `crons.register` audit row. NULL→ts AND ts→ts refresh both audited. Re-register with `wrapper: false` writes NO audit row (true no-op). | Full wrapper handshake history visible to operator; UI in STAGE-002-006 renders this timeline. If wrapper /register chattiness becomes excessive, debouncing is a 002-009 concern. |

**Audit row format reference (D10):**
- `who` = token.name (per D3)
- `what` = "crons.register"
- For first insert: `before_json` = null, `after_json` = full CronOut serialization
- For ts→ts refresh: `before_json` = {wrapper_last_seen_at: <old_ts>}, `after_json` = {wrapper_last_seen_at: <new_ts>}
- For NULL→ts on previously-discovered row: `before_json` = {wrapper_last_seen_at: null}, `after_json` = {wrapper_last_seen_at: <ts>}
- `ip` = request peer IP

## Decisions in this stage (DEPRECATED — see Design Decisions Locked above)

These need brainstormer-Opus input during Design phase. Pre-recommended position in **bold**.

1. **Idempotent semantics: 200 OK + same body vs 200 OK + `{idempotent: true}` flag vs 304 Not Modified?**
   - **Recommended: 200 OK + same CronOut body.** Cleanest REST semantics; client doesn't need special-case handling.
   - Alternative: 304 Not Modified. Cache-friendly but weird for a POST; clients expect 304 only for GET.
   - Alternative: 200 OK + `{idempotent: true}` envelope. Useful if clients want to log "actually created vs already-existed" separately, but most clients don't care.

2. **`wrapper: true` semantics if `wrapper_installed_at` was already set: refresh the timestamp or no-op?**
   - **Recommended: refresh the timestamp on every successful /register with `wrapper: true`.** A re-install (e.g., wrapper script updated) should bump the install time.
   - Alternative: only set on first registration; ignore subsequent. Preserves "when was this first wrapper installed" history but loses "when was it last refreshed."

3. **Token attribution in audit `who` field: token name vs token id vs both?**
   - **Recommended: token name (current pattern from STAGE-002-001).** Human-readable in audit log; matches existing audit rows.
   - Alternative: token id (UUID). More precise but unreadable; requires JOIN to make sense.

4. **Rate limiting on `/register`: same per-cron bucket as `/start//ok//fail` (60/min) vs separate / stricter?**
   - **Recommended: same bucket per fingerprint.** /register is rare in practice (once per cron install + occasional refresh); the existing limit is more than sufficient and avoids two different rate-limit configs.
   - Alternative: stricter (e.g., 5/min per fingerprint). Defensive against /register being abused for table-spam, but the body validation + 422 already filters most spam.

## In-Stage Scope Additions

Locked 2026-05-12 during Design phase closure. These additions are required for STAGE-002-005 Build to complete:

1. **Alembic migration** (new revision after 0008): rename `crons.wrapper_installed_at` → `crons.wrapper_last_seen_at`. Update all code references in `kernel/cron/`, `kernel/heartbeat/`, `kernel/api/`, all tests, and auto-regenerated UI schema (`apps/ui/src/api/schema.ts` via `generate-ui-types` hook).

2. **Fix `_SELECT_CRON_SQL` in `kernel/heartbeat/repository.py`**: remove `AND hidden_at IS NULL` filter; update the existing `/start/ok/fail` tests that asserted 404-on-hidden so they instead assert that operations succeed and write audit rows.

3. **Update `docs/architecture/cron-identity.md`**: delete the 409 metadata-mismatch paragraph; replace with the locked `/register` flow (idempotent semantics, status codes, audit row behavior, hidden-cron behavior).

## Cross-Stage Edits Applied

The following tracking documents were edited directly during this Design phase to record scope additions derived from the 10 locked decisions:

- **STAGE-002-006.md:** Added wrapper handshake history panel scope + `wrapper_last_seen_at` column rename notifications (per D2/D10).
- **STAGE-002-010.md:** Added hidden-cron notification suppression via vmalert gating + `hidden` label on metrics (per D5) + `wrapper_last_seen_at` column rename (per D2).

## Design Phase

- **Approaches to present:** idempotent response shape; wrapper timestamp refresh policy; rate-limit reuse vs separate
- **UI Options Presented:** N/A (backend-only stage)
- **User Choice:** All 10 decisions locked per Design Decisions Locked table (above).
- **Seed Data Agreed:** Integration test creates a heartbeat:write token, computes a fingerprint for `(host="test-host", source_path="/etc/crontab", schedule="*/5 * * * *", command="/usr/bin/true")`, posts `/register` with that body, asserts 201 + correct row in DB; reposts identical body, asserts 200 + no new audit row.
- **Session Notes:** Design decisions locked 2026-05-12; scope additions recorded.

**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:**
  - `apps/monitor/alembic/versions/0009_wrapper_last_seen_at_rename.py` (column rename migration)
  - `apps/monitor/tests/test_api_register.py` (20+ tests covering /register endpoint)

- **API Endpoints Added:**
  - `POST /api/hb/{fingerprint}/register` with idempotent semantics (201 created, 200 idempotent)

- **Files Modified:**
  - `apps/monitor/homelab_monitor/kernel/cron/schemas.py` (+RegisterCronBody schema; CronOut field rename)
  - `apps/monitor/homelab_monitor/kernel/cron/repository.py` (+register_cron method with 3-branch transaction logic; column rename)
  - `apps/monitor/homelab_monitor/kernel/heartbeat/repository.py` (removed `AND hidden_at IS NULL` from `_SELECT_CRON_SQL`; column rename in queries)
  - `apps/monitor/homelab_monitor/kernel/api/routers/heartbeat.py` (+receive_register POST route)
  - `apps/monitor/homelab_monitor/kernel/api/routers/crons.py` (column rename in _record_to_out)
  - `docs/architecture/cron-identity.md` (rewrote /register section per locked decisions D1-D10; removed 409 metadata-mismatch paragraph; corrected hidden semantic)
  - `apps/monitor/tests/test_api_heartbeat.py` (renamed hidden-cron tests to assert 204 + audit row; helper rename)
  - `apps/monitor/tests/test_cron_repo.py` (+4 register_cron tests; helper rename)
  - `apps/monitor/tests/test_db_migrations.py` (+2 migration 0009 tests; updated 0008 expected-columns)
  - `apps/monitor/tests/test_api_crons.py` (helper rename to wrapper_last_seen_at)
  - `apps/monitor/tests/test_heartbeat_repo.py` (helper rename to wrapper_last_seen_at)

- **Verify Result:** GREEN
  - Backend: 1314 pytest passed, 0 failed, 100.00% kernel coverage gate held
  - UI: 170 vitest passed, ESLint warnings (36 pre-existing baseline — React 19 forwardRef + missing effect deps), UI build success

- **Verify Cycles:** 2
  - Cycle 1: 50 failures from two root causes — test helper rename misses + wrong response envelope assumption
  - Cycle 2: GREEN after surgical fixes
  - Fix-up: ruff lint cleanup via `ruff check --fix` + small PLR2004 magic-number fix

- **Session Notes:** All 10 locked Design decisions implemented; idempotent POST /register endpoint complete; hidden-cron behavior fixed (data captured, display/notification suppressed); wrapper_last_seen_at timestamp refresh on re-register working; audit row strategy (Path 3 — audit every wrapper handshake) fully operational.

**Status**: [x] Build (Complete)

## Refinement Phase

- [x] Backend e2e validation (curl-based: register → start → ok → register-again-idempotent → late-state via vmalert in 002-010 not yet wired)
- [x] Regression Items Added (heartbeat receiver still 404s on unknown for /start/ok/fail; /register is the only creation path on the heartbeat side)

### Refinement Phase Outcome

**Status:** Complete
**Method:** Backend-only stage. e2e-tester drove validation; no user prompts (per Design D5/D7 — backend-only).

**Validation summary:**
- 18 e2e scenarios executed against real running backend + DB
- 17/18 passed on first run; 1 failure was a missing `response_model=CronOut` declaration on the `/register` route (OpenAPI schema showed empty `{}` for response shape — runtime behavior was correct)
- Fix applied: added `response_model=CronOut, responses={201: {model: CronOut}, 200: {model: CronOut}}` to the route decorator
- 18/18 passing after fix; `make verify` GREEN (1314 backend / 100.00% coverage / 170 UI / 0 errors)

**Scenarios validated:**
- 201 on new fingerprint
- 200 idempotent re-register
- 200 with wrapper_last_seen_at refresh + audit row (D2, D10 Path 3)
- 200 with wrapper:false re-register, NO new audit row (D10)
- 422 fingerprint mismatch with `fingerprint_mismatch: true` detail flag (D6, D9)
- 422 invalid schedule with `invalid_schedule: true, reason: <str>` detail flag (D9)
- 422 extra field (extra='forbid' per D7)
- 401 without token (D9)
- **Hidden cron operates normally** — start/ok/fail/register all succeed; audit rows still written (D5, the architectural change)
- Re-register preserves operator's PATCH'd name + expected_grace_seconds (D7)
- Full wrapper lifecycle: install → start → ok → start → fail → reinstall
- OpenAPI schema correctly references CronOut for both 200 and 201 responses

**Bug found and fixed during Refinement:**
- Missing `response_model=CronOut` declaration on `/register` route — would have caused UI's openapi-typescript codegen to produce untyped response for the endpoint. Caught by scenario 3 (frontend-layer schema check).

**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests) — focus on fingerprint validation (URL vs body), idempotent semantics, audit-row uniqueness across re-registers
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated: `docs/architecture/heartbeat-receiver.md`, `docs/architecture/cron-identity.md`
- [x] Committed: specific paths only

**Commit Hash**: 5ce8a2a
**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize (Complete)

### Finalize Phase Outcome

**Status:** Complete
**Implementation commit:** 5ce8a2a
**Changelog commit:** 259d8a8
**Tracking commit:** (this commit)

**Code review summary:**
- First pass (Opus, pre-test): 0 Critical, 5 Important, 5 Minor findings
- All findings implemented via 3 parallel fixer dispatches:
  - Wave 1: kernel/cron/repository.py — extracted _cron_to_audit_after helper; split _UPDATE_WRAPPER_LAST_SEEN_SQL bind params; added TODO marker for pre-existing update_cron combined-flip branch
  - Wave 2: heartbeat/crons routers + schemas.py — extracted shared cron_record_to_out helper (TYPE_CHECKING import pattern for circular-import avoidance); renamed envelope_response_with_record → _cron_json_response; extended OpenAPI responses dict with 401/403/422/429
  - Wave 3: test files + docs — renamed wia bind param to wlsa across helpers; tightened test_register_missing_required_field_returns_422 to assert error.detail body shape; cleaned up cron-identity.md stale section + last 409 mention
- Second pass (Opus, post-implementation): APPROVED, no regressions

**Bug caught during Finalize verify loop:** Minor #9's initial assertion path used `body["error"]["details"]["errors"]` (project's custom error envelope) but FastAPI's Pydantic body-validation errors bypass that envelope and use `body["detail"]` instead. 3 tests failed; debugger-lite diagnosed; fixer corrected to `body["detail"]`.

**Verify cycles in Finalize:** 3 (cycle 5 surfaced 2 ruff errors from the refactor — F401 unused CronRecord import + UP037 quoted forward-ref; cycle 6 surfaced the body shape regression in the parametrized missing-field test; cycle 7 GREEN.)

**Final verify result:** `make verify` GREEN — 1314 backend pytest passed, 100.00% kernel coverage, 170 UI vitest passed, UI build success, lint/format/pyright clean.
