# STAGE-002-004: API removal — drop POST /api/crons + AddCronModal + CronCreate + create-mode of CronForm

## Status: Complete

## Overview

Strip the manual-create path from the cron registry. After this stage there is NO way to create a cron row except via auto-discovery (STAGE-002-007) or `/register` (STAGE-002-005). The `POST /api/crons` endpoint is removed; the `CronCreate` Pydantic schema is removed; the `AddCronModal` UI component is removed; the "+ Add cron" button is removed; `CronForm` collapses to edit-only mode.

This is the "no add" enforcement stage. After it ships, manual-add cannot return without a deliberate code change reverting the deletion.

**Spec references:** `docs/superpowers/specs/2026-05-11-cron-derived-state-redesign.md` §API surface changes.

## Stage Flags

- Has Input Forms: [ ] No (REMOVES the only input form for cron creation)
- Touches secrets / production data: [ ] No

## Cross-cutting standards (from EPIC-002 §Cross-stage acceptance criteria)

100% kernel coverage maintained; ruff + black + pyright strict; structlog JSON logs; UTC ISO-8601 timestamps; specific paths only on commits. EPIC-002 cross-stage criterion #9 is **superseded** by this stage's deletion + STAGE-002-005's `/register` endpoint — EPIC-002.md is updated in lockstep.

## Concrete deliverables

1. **Backend removal**
   - Delete `POST /api/crons` handler from `apps/monitor/homelab_monitor/kernel/api/routers/crons.py`. Keep `GET /api/crons`, `GET /api/crons/{fingerprint}`, `PATCH /api/crons/{fingerprint}`, `DELETE /api/crons/{fingerprint}`, both `preview-runs` endpoints.
   - Delete `CronCreate` Pydantic schema from `apps/monitor/homelab_monitor/kernel/cron/schemas.py`. Keep `CronUpdate` (already trimmed in STAGE-002-003), `CronOut`, `CronListResponse`, `CronWithStateOut`, `CronListQuery`, the preview-runs schemas.
   - Delete `CronRepo.create_cron()` method (was a placeholder raising NotImplementedError after STAGE-002-003).
   - Delete the `mode='create'` branch of `update_cron()` if any (likely none — `update_cron` was always edit-only).
   - Delete tests targeting POST /api/crons + CronCreate from `test_api_crons.py` and `test_cron_repo.py`.
   - Update `CronListResponse` to retain its existing shape (no field changes; only the create endpoint is removed).
   - Audit verb `crons.create` removed from the codebase. Replaced by `crons.register` (STAGE-002-005) and `crons.discover` (STAGE-002-007).

2. **Frontend removal**
   - Delete `apps/ui/src/components/crons/AddCronModal.tsx`.
   - Delete `apps/ui/src/components/crons/__tests__/AddCronModal.test.tsx`.
   - Delete the "+ Add cron" button from `apps/ui/src/components/crons/CronsToolbar.tsx`.
   - Delete `apps/ui/src/components/crons/__tests__/CronsToolbar.test.tsx` assertions about the add button (or keep the file with reduced test surface).
   - Update `CronForm.tsx`:
     - Remove `mode: 'create' | 'edit'` prop; `mode` is always implicitly edit.
     - Remove the schedule + cadence radio toggle (schedule is read-only after creation in derived-state model).
     - Remove the schedule input field (read-only display only).
     - Remove the cadence_seconds input field (read-only display only).
     - Remove integration_mode select (field already dropped in STAGE-002-003).
     - Keep editable fields: `name`, `expected_grace_seconds`, `enabled`. Display read-only: `host`, `source_path`, `command`, `schedule`, `cadence_seconds`.
     - Form's submit handler hits `PATCH /api/crons/{fingerprint}` with the editable fields only.
   - Delete the `useCreateCron` hook from `apps/ui/src/api/crons.ts`. Keep all other hooks.
   - Update `CronDetail.tsx`:
     - Remove the "+ Add cron" affordances if any leak from CronForm.
     - The existing detail page wiring continues to use the same `useUpdateCron`, `useSoftDeleteCron`, etc.

3. **Type regeneration**
   - `pnpm --filter ui run generate-types` after backend ships → `CronCreate` type disappears from `schema.ts`. Imports in UI code that referenced `CronCreate` must be deleted.

4. **Tests rewritten**
   - `test_api_crons.py`: delete all "create" test functions; keep list/get/patch/delete/preview tests.
   - `test_cron_repo.py`: delete `create_cron` tests; keep update/get/list/soft-delete/atomicity tests.
   - UI tests: delete `AddCronModal.test.tsx`; rewrite `CronForm.test.tsx` for edit-only mode; rewrite `CronsToolbar.test.tsx` to reflect missing add button.

5. **Documentation**
   - Update `docs/architecture/heartbeat-receiver.md` to clarify the new "discovery + /register only" creation paths.
   - Update `epics/EPIC-002-heartbeat-cron/regression.md` to remove obsolete manual-create regression checks; add new check: "POST /api/crons returns 405 Method Not Allowed" (or 404 — depending on how FastAPI handles the missing route).

## Out of scope for this stage

- The `/api/hb/{fingerprint}/register` endpoint (STAGE-002-005).
- UI redesign for the new 4-panel detail page (STAGE-002-006). The detail page in this stage continues to use the old layout with the now-trimmed CronForm.
- Discovery integration (STAGE-002-007).
- Wrapper-health alerts (STAGE-002-010).

## Decisions in this stage

These need brainstormer-Opus input during Design phase. Pre-recommended position in **bold**.

1. **Removed POST /api/crons response: 405 Method Not Allowed vs 404 Not Found vs leave-route-but-return-410-Gone?**
   - **Recommended: just delete the route. FastAPI returns 405 Method Not Allowed automatically because GET /api/crons still exists.** Cleanest.
   - Alternative: explicitly route POST to a 410 Gone handler with deprecation message. Helpful for clients still calling the old endpoint, but adds a kept-for-deprecation handler that pollutes the codebase.

2. **CronForm trim level: drop create-mode branch entirely vs keep skeleton with dead `if mode === 'create'` branches?**
   - **Recommended: drop the create branch entirely.** The mode prop is removed; the form is a single-branch edit form. Simpler, less dead code.
   - Alternative: keep the skeleton with `// future: re-enable create if discovery model changes`. Defends against a hypothetical reversal but adds clutter.

3. **Test cleanup approach: delete tests for removed code vs keep skipped with reason?**
   - **Recommended: delete the tests outright.** The removed code IS the contract being tested; keeping skipped tests pretends a removed feature is "temporarily disabled." If the feature comes back later, write fresh tests.
   - Alternative: pytest.skip with `reason="POST /api/crons removed in STAGE-002-004"`. Preserves test history but is process noise.

## Design Phase

- **Approaches to present:** 3 pre-doc'd decisions (D1-D3) + 4 brainstormer-surfaced findings (D4-D7)
- **UI Options Presented:** N/A — backend deletion + UI tree pruning; visual UX delta is "+ Add cron" button disappears + CronForm renders only name/grace/enabled
- **User Choice:** all 7 decisions confirmed (autonomous Build approval; spec gap on CronsList.tsx caught + addressed)
- **Seed Data Agreed:** No new seed data — relies on STAGE-002-003's seed.
- **Session Notes:**

### Design decisions locked (2026-05-12)

| # | Decision | Choice |
|---|---|---|
| D1 | Removed POST /api/crons response | **Just delete the route** — FastAPI emits 405 Method Not Allowed with `Allow: GET` header automatically. No tombstone handler. Regression check: assert exactly 405 (not "405 or 404"). |
| D2 | CronForm trim level | **Drop create-mode branch entirely; remove `mode` prop.** Keep-skeleton (Option B) is structurally impossible — the dead branch references `CronCreate` (type-deleted) and `useCreateCron` (deleted). Form becomes pure edit form. |
| D3 | Test cleanup | **Delete tests outright.** pytest.skip cannot work — tests import `CronCreate` which becomes an `ImportError` at module collection time. Git history preserves them for forensics. |
| D4 | CronForm field-removal scope | **Remove schedule/cadence inputs entirely from CronForm** (not RO-display, not disabled). STAGE-002-006's 4-panel layout will surface schedule/cadence in a "Disk source" RO panel sourced directly from `cron`, not from a form field. RO-in-form would be throwaway markup STAGE-002-006 rewrites. Caveat: if 002-006 slips, the detail page looks bare; defer-to-006 is the explicit accepted trade-off. |
| D5 | `useCreateCron` deletion blast radius | **Spec gap caught**: `apps/ui/src/routes/inventory/CronsList.tsx` (addOpen state at line 13, `<AddCronModal />` render at line 95) is NOT in the spec's deletion list but MUST be deleted alongside AddCronModal. Also `CronsList.test.tsx:20` vi.mock entry should be pruned. Build phase will include these in the deletion manifest. |
| D6 | `CronCreate` TypeScript fan-out | **Coordinated deletion**: backend ships → `pnpm generate-types` regenerates schema.ts → 4 UI sites compile-fail (`api/crons.ts`, `CronForm.tsx`, `CronDetail.tsx`, `AddCronModal.tsx`) → fix in same commit. Standard Stage convention. |
| D7 | `derive_name` retention | **Keep** in `kernel/cron/fingerprint.py` as forward-looking infra. STAGE-002-005 (`/register`) + STAGE-002-007 (discovery) both need it. Tests stay; 100% kernel coverage gate keeps it honest. |

### Cross-decision interactions

- **D2 × D6**: Dropping `mode` and the create branch in CronForm (D2) is what makes the `CronCreate` import deletion (D6) clean. Keeping the dead branch (D2 Option B) would require stubbing `CronCreate` to satisfy the type system — manufactured-fake-code defeats the deletion's purpose.
- **D3 × D6**: Skipping (vs deleting) tests would force `pytest.skip` at module collection time, but the tests' imports of `CronCreate` fail at the import statement BEFORE any decorator runs. Reinforces D3 → delete.
- **D4 × STAGE-002-006 sequencing**: D4 Option A (remove fields entirely) assumes STAGE-002-006 follows shortly. If 002-006 slips by more than 1-2 stages, the detail page form will look bare. Accepted trade-off.
- **D5 spec gap**: Brainstormer caught `CronsList.tsx` omission from spec. Build deliverable list extended to include it.
- **D1 ↔ regression.md**: Regression check becomes "POST /api/crons returns 405 Method Not Allowed with Allow: GET header" (deterministic, not "405 or 404").
- **D7 ↔ audit verb scrub**: `derive_name` retention is unrelated to the `crons.create` audit verb removal. After deletion, `grep "crons.create"` should return zero hits in the codebase.

### Build deliverable manifest (consolidated)

**Backend deletions:**
- POST /api/crons handler in `apps/monitor/homelab_monitor/kernel/api/routers/crons.py` (and its docstring reference to `crons.create`)
- `CronCreate` Pydantic schema from `apps/monitor/homelab_monitor/kernel/cron/schemas.py`
- `CronRepo.create_cron()` method from `apps/monitor/homelab_monitor/kernel/cron/repository.py` (and its audit insert + docstring reference)
- All tests targeting POST /api/crons in `test_api_crons.py` (including the `crons.create` audit assertion)
- All `create_cron` tests in `test_cron_repo.py` (including atomicity-under-audit-failure test if it specifically tests create)

**Frontend deletions:**
- `apps/ui/src/components/crons/AddCronModal.tsx`
- `apps/ui/src/components/crons/__tests__/AddCronModal.test.tsx`
- "+ Add cron" button block in `apps/ui/src/components/crons/CronsToolbar.tsx`
- `useCreateCron` hook in `apps/ui/src/api/crons.ts` (+ its `CronCreate` type alias)
- `mode` prop, create branch, `scheduleMode` radio, schedule input, cadence_seconds input from `apps/ui/src/components/crons/CronForm.tsx`
- `addOpen` state + `<AddCronModal />` render + AddCronModal import in `apps/ui/src/routes/inventory/CronsList.tsx` **(spec gap caught by brainstormer)**
- `useCreateCron` mock entry in `apps/ui/src/routes/inventory/__tests__/CronsList.test.tsx` line 20
- `CronCreate` union narrowing in `apps/ui/src/components/crons/CronDetail.tsx` (handleSave type `CronCreate | CronUpdate` → just `CronUpdate`)
- Update `CronForm.test.tsx` for edit-only mode (test "create mode" cases deleted; "edit mode" cases stay)
- Update `CronsToolbar.test.tsx` to remove "+ Add cron" button assertions

**Generated artifacts:**
- `packages/shared-types/openapi.json` regenerated (`CronCreate` schema disappears)
- `apps/ui/src/api/schema.ts` regenerated (gitignored)

**Documentation:**
- Update `epics/EPIC-002-heartbeat-cron/regression.md`: remove obsolete manual-create checks; add "POST /api/crons returns 405 Method Not Allowed with Allow: GET header"

**Brainstormer + user input:**
- Brainstormer (Opus) produced 7 decisions with 2-3 options each + cross-decision interactions. All 3 pre-doc'd recommendations validated; 4 additional findings surfaced.
- User input: autonomous Build approval per session direction; D4 trade-off (Option A vs B) accepted with awareness of STAGE-002-006 slip risk.
- **No deviation from brainstormer recommendations: all 7 decisions match the brainstormer's recommended position.**

**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:** none new (all deletions + edit-only refactor)
- **API Endpoints Removed:** `POST /api/crons` (FastAPI emits 405 + `Allow: GET` automatically)
- **Placeholders Added:** none
- **Session Notes:**

#### Build summary (2026-05-12)

**Implementation:** Spec at `/tmp/spec-stage002-004-1778613668.md`, implemented in 2 scribe passes (first pass got truncated mid-CronForm.tsx rewrite; second pass completed UI work + regression.md).

**Files deleted (3 total):**
- `apps/ui/src/components/crons/AddCronModal.tsx`
- `apps/ui/src/components/crons/__tests__/AddCronModal.test.tsx`
- `apps/monitor/tests/test_cron_schemas.py` already deleted in STAGE-002-003; no further test files deleted this stage

**Files modified (backend production):**
- `apps/monitor/homelab_monitor/kernel/api/routers/crons.py` — deleted `POST /api/crons` handler + its docstring reference to `crons.create`
- `apps/monitor/homelab_monitor/kernel/cron/schemas.py` — deleted `CronCreate` Pydantic schema; deleted `IntegrationMode` literal already gone
- `apps/monitor/homelab_monitor/kernel/cron/repository.py` — deleted `CronRepo.create_cron()` method + `_audit_after_for_create` helper; removed 6 unused imports (`Mapping`, `compute_fingerprint`, `canonicalize_schedule`, `compute_average_interval_seconds`, `CronCreate`, `_INSERT_CRON_SQL`)
- Audit verb taxonomy: `crons.create` scrubbed from codebase (grep returns 0 hits)

**Files modified (backend tests):**
- `apps/monitor/tests/test_api_crons.py` — deleted all POST /api/crons tests
- `apps/monitor/tests/test_cron_repo.py` — deleted create_cron tests; 15 setup-only-create tests converted to use `_seed_cron` (audit count assertions adjusted accordingly)

**Files modified (frontend production):**
- `apps/ui/src/api/crons.ts` — deleted `useCreateCron` hook + `CronCreate` type alias; CronCreate import removed
- `apps/ui/src/components/crons/CronForm.tsx` — **full rewrite to edit-only** (~100 lines down from ~370): dropped `mode` prop, scheduleMode radio, schedule input, cadence_seconds input, lastScheduleRef/lastCadenceRef + their useEffect hooks, CronCreate import. Zod schema collapsed to `{name, expected_grace_seconds, enabled}`. Submit always returns CronUpdate.
- `apps/ui/src/components/crons/CronsToolbar.tsx` — deleted "+ Add cron" button block; onAddClick prop removed
- `apps/ui/src/components/crons/CronDetail.tsx` — dropped `CronCreate` type import; narrowed `handleSave` from `body: CronCreate | CronUpdate` → `body: CronUpdate`; dropped `mode="edit"` prop from `<CronForm>` invocation
- `apps/ui/src/routes/inventory/CronsList.tsx` — **spec gap fix per D5**: deleted `addOpen` useState, `<AddCronModal />` render, AddCronModal import

**Files modified (UI tests):**
- `apps/ui/src/components/crons/__tests__/CronForm.test.tsx` — rewritten for edit-only: 4 create-mode tests deleted; kept tests for field rendering, validation, submit shape (now always CronUpdate)
- `apps/ui/src/components/crons/__tests__/CronsToolbar.test.tsx` — removed "+ Add cron" button assertions
- `apps/ui/src/routes/inventory/__tests__/CronsList.test.tsx` — removed `useCreateCron` mock entry at line 20; removed unused `userEvent` import

**Files modified (generated artifacts):**
- `packages/shared-types/openapi.json` — regenerated (`CronCreate` schema disappears)
- `apps/ui/src/api/schema.ts` — regenerated (gitignored)

**Files modified (regression.md):**
- Removed 3 obsolete STAGE-002-002 entries (POST-related)
- Updated STAGE-002-003 entries to use `hidden_at` consistently
- Added new STAGE-002-004 section with 4 regression items: 405 on POST, no AddCronModal, no "+ Add cron" button, CronForm edit-only

**Verify GREEN (4 cycles to GREEN):**
1. Cycle 1: scribe pass 1 (backend + most UI; truncated mid-CronForm.tsx) → ruff format fail (1 file unformatted)
2. Cycle 2: ruff format auto-fix → reran verify
3. Cycle 3: scribe pass 2 (CronForm.tsx rewrite + CronDetail.tsx narrow + CronForm.test.tsx + regression.md) → 3 UI files need prettier
4. Cycle 4: prettier auto-fix → GREEN

**Final verify GREEN:** 1282 backend pytest pass (down from 1300 — 18 create-related tests deleted) + 100.00% kernel coverage + 177 UI tests pass (down from 185 — 8 create/AddCronModal tests deleted) + ruff/black/pyright/tsc/eslint/prettier/UI-build clean. UI lint warnings: 35 (pre-existing baseline; was 36; one orphan warning resolved by deletions).

**Notable scope notes:**
- D5 spec-gap fix executed: `CronsList.tsx` was NOT in the spec's deletion list but contained `addOpen` state + AddCronModal render; brainstormer caught + Build included.
- D4 trade-off accepted: CronForm.tsx now renders only {name, expected_grace_seconds, enabled}. The detail page will look noticeably bare between this stage and STAGE-002-006 (which adds back schedule/cadence in a 4-panel RO display). Documented in code review.
- `derive_name` from `kernel/cron/fingerprint.py` retained per D7 (forward-looking infra for STAGE-002-005 + 002-007).
- `crons.create` audit verb: 0 hits across the codebase after this stage (grep confirmed).

**Pre-existing failures: none.** All 1300/185 tests GREEN at HEAD `eeab004` (STAGE-002-003 close); this stage cleanly deleted 18+8 tests for the removed feature.

**Status**: [x] Build (Complete)

## Refinement Phase

- [x] Desktop Approved (Crons tab — no "+ Add cron" button; detail page loads + saves edits; Archive button works without confirmation modal) — 2026-05-12
- [x] Mobile Approved (Crons tab; toolbar layout reflows correctly) — 2026-05-12
- [x] Regression Items Added — see epics/EPIC-002-heartbeat-cron/regression.md (added 5 STAGE-002-004 items including the 4 from Design plus 1 for the no-confirm Archive flow)
- **Feedback Round 1:** User requested removal of typed-name confirmation on Archive flow ("We need to remove the confirmation on archiving"). Bundled into Refinement (Option a: no confirmation at all). Implemented: ConfirmDeleteModal.tsx + test DELETED; CronDetail.tsx Archive button now calls handleDelete() directly with isPending state; CronDetail.test.tsx test rewritten to assert direct mutateAsync call.
- **Feedback Round 2:** N/A — Round 1 fix verified on desktop+mobile; both approved.

#### Refinement summary (2026-05-12)

**Scope:** Frontend stage (UI changes from STAGE-002-004 Build). Changes are pure removals: "+ Add cron" button + AddCronModal gone, CronForm edit-only. Per user policy, prompted for desktop+mobile testing.

**Round 1 feedback (NEW SCOPE):**
- User requested removal of the typed-name confirmation on Archive flow.
- Decision: Option a — no confirmation at all. Archive button on CronDetail directly calls softDelete.mutateAsync(); button shows "Archiving…" while isPending; on success navigates to /inventory/crons with include_hidden=true so the archived row is visible.
- Files affected:
  - DELETED: `apps/ui/src/components/crons/ConfirmDeleteModal.tsx` + `__tests__/ConfirmDeleteModal.test.tsx`
  - MODIFIED: `apps/ui/src/components/crons/CronDetail.tsx` — removed ConfirmDeleteModal import, deleteOpen/deleteError state, ConfirmDeleteModal render block; simplified handleDelete (no setDeleteError; errors logged to console)
  - MODIFIED: `apps/ui/src/components/crons/__tests__/CronDetail.test.tsx` — replaced "opens delete modal when Archive button is clicked" test with "calls softDeleteCron mutateAsync when Archive button is clicked"
- Verify GREEN after Round 1 fix: 1282 backend tests + 100% kernel coverage + 170 UI tests pass (down from 177; 7 ConfirmDeleteModal tests deleted).

**Why no confirmation:** User context — "It is only ui, there is a future thing that happens with it that this will prepare for." Soft-delete is reversible (Restore button exists) so accidental Archive clicks are recoverable; UX optimizes for single-operator speed over over-cautious dialogs.

**Tests run:**
1. Desktop (1440px): Visit /inventory/crons → confirmed no "+ Add cron" button; detail page CronForm shows only {name, expected_grace_seconds, enabled}; Archive button immediately archives (no modal); Restore from archived view works.
2. Mobile (390px): Toolbar reflows cleanly without "+ Add cron"; CronForm fields render correctly.

User approval: Both desktop AND mobile explicitly approved on 2026-05-12.

**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests) — Opus, 3 Important + 6 Minor findings; 5 fixes implemented + 1 deferred (alembic 0007 docstring; immutable history convention)
- [x] Tests Written — Build covered the bulk; Finalize added 1 regression test (test_post_crons_returns_405_method_not_allowed) per Important finding
- [x] Code Review (post-tests) — Opus second pass; 1 cosmetic TODO placement nit addressed; APPROVED
- [x] Documentation Updated: `regression.md` (5 STAGE-002-004 items); `docs/ui/inventory-crons.md` stamped REWRITE PENDING (STAGE-002-006); `docs/architecture/cron-identity.md` `crons.create` moved to Removed-verbs subsection
- [x] Committed: specific paths only

**Commit Hash**: `9ff564f` (implementation + code-review fixes bundled); `adb277f` (changelog entry)
**CHANGELOG Entry**: [x] Added at `changelog/2026-05-12.changelog.md`

**Status**: [x] Finalize (Complete)
