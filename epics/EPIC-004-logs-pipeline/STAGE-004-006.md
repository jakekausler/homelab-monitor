# STAGE-004-006: Redaction pipeline (vector VRL + audit metric + yaml-driven patterns)

## Status: Not Started

## Overview

Strip well-known sensitive patterns (bearer tokens, JWTs, passwords-in-URLs, AWS access keys, generic API keys) from log lines BEFORE they reach VictoriaLogs. Redaction happens in vector at ingest, in-process, so no monitor latency is added and no secrets land in VL ever.

Pattern source is `homelab-monitor.yaml` under `logs.redact:`; rendered into the vector.toml VRL transform at boot. Per-pattern-type counts are emitted as `vector_redactions_total{pattern_type}` to VictoriaMetrics; the spec requires an audit trail of counts (never the redacted values themselves).

This lands BEFORE the Logs Explorer + any new UI work so we never expose pre-redaction secrets in any later surface.

**Vertical slice:** after this stage, planting a bearer token / JWT / AWS key in a log line at the source results in the VL record containing `[REDACTED]` instead. A metric `vector_redactions_total{pattern_type="bearer_token"}` increments per redaction. The audit log on the monitor side surfaces the counts but never the values.

**Spec references:** Master spec §3.1 (audit log: counts only, never values).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [x] Yes — handles known-sensitive patterns; redaction failure = secret leak. Refinement is **3a + 3b**.
- Has Input Forms: [ ] No

## Cross-cutting standards (from EPIC-004 §Cross-stage acceptance criteria)

100% kernel coverage. **CRITICAL: no raw log line that contains a known-sensitive pattern leaves the pipeline un-redacted — asserted in tests with planted credentials.**

## Concrete deliverables

### 1. `homelab-monitor.yaml` schema extension

`apps/monitor/homelab_monitor/kernel/config/schema.py` (or wherever the yaml schema lives) — add:

```yaml
logs:
  redact:
    - name: bearer_token
      pattern: '(?i)bearer\s+[A-Za-z0-9._-]+'
      replacement: 'bearer [REDACTED]'
    - name: jwt
      pattern: 'eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+'
      replacement: '[REDACTED_JWT]'
    - name: password_in_url
      pattern: '://[^:@/\s]+:[^@/\s]+@'
      replacement: '://[REDACTED]:[REDACTED]@'
    - name: aws_access_key
      pattern: 'AKIA[0-9A-Z]{16}'
      replacement: '[REDACTED_AWS_KEY]'
    - name: api_key_generic
      pattern: '(?i)(api[-_]?key|api[-_]?token|access[-_]?token|secret[-_]?key)["'']?\s*[:=]\s*["'']?[A-Za-z0-9._-]{16,}'
      replacement: '$1=[REDACTED]'
```

Loader validates each entry has `name`, `pattern`, `replacement`. Invalid entries fail at config load (don't ship a partially-broken redaction policy).

### 2. Render-on-boot expansion into vector.toml

`apps/monitor/homelab_monitor/kernel/cron/render.py` (or wherever the render-on-boot logic lives) — read `logs.redact:` from `homelab-monitor.yaml`, generate a `[transforms.redact]` VRL block that runs BEFORE the throttle / sink:

```toml
[transforms.redact]
type = "remap"
inputs = ["docker_enrich", "journald_enriched", "hmrun_parsed"]  # after enrichment, before throttle
source = '''
# Per pattern from yaml:
.message, redacted_bearer = replace(.message, r'(?i)bearer\s+[A-Za-z0-9._-]+', "bearer [REDACTED]")
if redacted_bearer > 0 {
  emit_metric("vector_redactions_total", {"pattern_type": "bearer_token"}, redacted_bearer)
}
# ... repeated per pattern, generated from yaml
'''
```

(Final VRL syntax confirmed in Design phase. Vector's `emit_metric` may have a different name; substitute the actual function.)

### 3. Metric: `vector_redactions_total{pattern_type}`

Vector's prometheus_exporter sink (or equivalent metric pathway) exposes the counter; vmagent scrapes it. The metric is queryable in MetricsQL.

### 4. Audit log on monitor side

`apps/monitor/homelab_monitor/kernel/audit/redaction.py` (new) — a small periodic task (5 min) that:

- Queries the `vector_redactions_total` counter from VM
- Logs a structured audit entry: `{ts, pattern_type, count_delta, total_today}`
- Audit lines go to the regular audit log destination (not VL — risk of recursion)

### 5. Tests

- `apps/monitor/tests/test_redaction_yaml_loader.py` — validates the schema loader, invalid-entry rejection, default-pattern set
- `apps/monitor/tests/test_render_redaction_block.py` — given a yaml policy, render-on-boot produces a syntactically-valid VRL block with all patterns
- `apps/monitor/tests/integration/test_redaction_end_to_end.py` — plant known credentials in log emissions, confirm VL records contain `[REDACTED]` and the counter incremented for the right pattern_type
- `apps/monitor/tests/test_redaction_audit.py` — audit log records counts only, never the planted values

### 6. Documentation

`docs/admin/redaction.md` (new) — how to add a new pattern: edit `homelab-monitor.yaml`, restart monitor (which re-renders vector.toml), verify metric increments.

### 7. Forward-references in EPIC-007 and EPIC-008

Add explicit notes (in their EPIC files, NOT this stage):

- EPIC-007 (Unifi): a stage adds UDM-specific token patterns to the redaction policy yaml when UDM syslog ingest lands.
- EPIC-008 (Synology): a stage adds Synology API token patterns to the redaction policy yaml when DSM syslog ingest lands.

(Both handled when we update epic files after writing stage files.)

## Out of scope for this stage

- UI for editing redaction patterns — yaml edit + restart is the v1 workflow
- Per-line audit (which line was redacted) — counts only, per master spec
- Pattern-effectiveness reporting (which patterns fire most) — implicit in the metric; no dedicated UI in v1
- Synology / Unifi-specific patterns — forward-referenced to EPIC-007 and EPIC-008
- Pre-redacted backfill of historical VL data — accept the gap

## Decisions in this stage

### Design phase will confirm

- **VRL `emit_metric` function name** — vector's exact API for emitting counter metrics from a transform. Validate against vector version pinned in deploy/compose/.
- **Where in the pipeline DAG to insert `[transforms.redact]`** — after enrichment, before throttle. Specifically: input list confirmed by reading current vector.toml.template DAG.
- **How frequently to query the redaction counter for audit** — recommendation: 5 min, configurable via env var if needed.
- **Pattern test corpus** — exact planted-credential set for the end-to-end test (cover all 5 default patterns + edge cases like a pattern at the very start of a line, multiple matches per line, no-match-anywhere line).

### Locked Design Decisions (inherited from brainstorming 2026-05-28)

- **D-REDACT-VECTOR-LAYER** — VRL in vector, not the monitor process. Vector stays on the log hot path; monitor never sees pre-redaction lines.
- **D-REDACT-YAML-CONFIG** — patterns live in `homelab-monitor.yaml` under `logs.redact:`; render-on-boot into vector.toml.
- **D-REDACT-AUDIT-COUNTS-ONLY** — audit log records per-pattern_type counts; NEVER the redacted values themselves (master spec requirement).
- **D-REDACT-V1-PATTERNS** — bearer tokens, JWTs, passwords-in-URLs, AWS keys, generic API keys. Synology/Unifi-specific patterns added when their epics ship.
- **D-REDACT-BEFORE-UI-WORK** — lands in foundation wave so no later UI surface (Explorer, Field Inspector, etc.) ever exposes pre-redaction secrets.

## Design Phase

**Design phase complete: TBD.**
**Status**: [ ] Design (Not Started)

## Build Phase

**Build phase complete: TBD.**
**Status**: [ ] Build (Not Started)

## Refinement Phase

- 3a (dev rig): plant credentials in test container output; verify redaction + counter
- 3b (prod rig): observe `vector_redactions_total` over real prod stream for 24h; spot-check a sample of VL records for known-sensitive patterns (should find none unredacted)
- [ ] Desktop Approved (N/A — backend; observed via metric query)
- [ ] Mobile Approved (N/A)
- [ ] Regression Items Added (planted-credential test in regression.md)

**Refinement phase complete: TBD.**
**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation: TBD
- Changelog: TBD
- Tracking: TBD

**CHANGELOG Entry**: [ ] Added

**Finalize phase complete: TBD.**

Stage Complete.
**Status**: [ ] Finalize (Not Started)
