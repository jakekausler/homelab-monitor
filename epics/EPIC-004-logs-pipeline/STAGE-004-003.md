# STAGE-004-003: `<LogViewer>` component extraction + cron/docker viewers refactor + embedding contract documented

## Status: Complete

## Overview

Extract a shared `<LogViewer>` component from the two existing per-context log viewers (`DockerContainerLogsViewerBody`, `CronRunLogViewerPage`) and refactor both to use it. The component takes a caller-provided React Query hook (or fetcher) so it can render logs from any source: docker per-container, cron per-run, generic LogsQL, or future syslog sources (Synology, UDM, HA, Pi-hole).

This stage explicitly documents the **embedding contract** — `<LogViewer>` must support drop-in use on future detail pages (probe detail, alert detail, target detail) with a caller-provided filter + time range. Per master spec §9.2 ("inventory detail pages include related logs (LogsQL pre-filtered)").

The Logs Explorer page (STAGE-004-010) will consume the same component.

**Vertical slice:** docker container log viewer + cron run log viewer both render through `<LogViewer>`. No user-visible change. The component is now reusable for the Explorer + future detail-page embedding.

**Spec references:** Master spec §9.2 (logs screen + related logs on detail pages).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [ ] No — frontend-only refactor; Refinement is **3a only** (Desktop + Mobile).

## Cross-cutting standards (from EPIC-004 §Cross-stage acceptance criteria)

Frontend coverage gates (functions ≥80% / branches ≥75% / statements ≥80%) maintained. ESLint baseline holds. Both existing viewers continue to render identically after refactor.

## Concrete deliverables

### 1. `<LogViewer>` component

`apps/ui/src/components/logs/LogViewer.tsx` (new) — shared component with these props:

```typescript
interface LogViewerProps {
  // Data hook the caller controls — returns lines + status + error + loading
  useLogs: () => {
    lines: LogLine[] | undefined
    isLoading: boolean
    isError: boolean
    error: ApiError | undefined
    logStatus?: 'available' | 'no_lines' | 'unavailable' | 'unknown' | 'expired' | 'running'
    truncated?: boolean
  }
  // Optional slot above the line list (e.g., since-picker + refresh button for docker viewer)
  headerSlot?: ReactNode
  // Optional slot for empty-state copy
  emptyStateCopy?: string
  // Optional slot for unavailable-state copy
  unavailableCopy?: string
  // Render timestamps in local time by default (UTC toggle added in STAGE-004-009)
  timezone?: 'local' | 'utc'
}
```

- Renders the line list with monospace formatting matching the existing docker viewer
- Handles all log_status states: `available`, `no_lines`, `unavailable` (503), `unknown` (404), `expired`, `running`
- Renders truncated banner when applicable
- Renders error states from `ApiError`
- Renders empty state with caller-supplied copy
- NO pagination yet (STAGE-004-007), NO custom range yet (STAGE-004-008), NO UTC toggle yet (STAGE-004-009), NO search yet
- Mobile-responsive: line list scrolls horizontally on narrow viewports OR wraps (Design decides)

### 2. Component tests

`apps/ui/src/components/logs/__tests__/LogViewer.test.tsx` — comprehensive coverage:

- Lines render with timestamp + severity + service + message
- Empty state with default + custom copy
- Unavailable state with default + custom copy
- Unknown / expired / running states render appropriate banners
- Truncated banner appears when `truncated: true`
- Error state renders via `ErrorDisplay` (project pattern)
- Loading state renders skeleton/spinner
- Caller-provided `headerSlot` renders above line list

### 3. Docker container log viewer refactor

`apps/ui/src/routes/integrations/DockerContainerLogsViewerBody.tsx` — strip down to a thin wrapper:

- Owns the `since` preset state + dropdown + refresh button (`headerSlot`)
- Calls `useContainerLogs(name, since)` and adapts the response to the `useLogs` hook shape `<LogViewer>` expects
- Passes container-specific empty / unavailable copy
- Same behavior as today; same tests assert same DOM (with possible class name changes)

### 4. Cron run log viewer refactor

`apps/ui/src/routes/inventory/CronRunLogViewer.tsx` — strip down similarly:

- Owns the run-state badge + duration + anomaly flags + refresh button (`headerSlot`)
- Calls `useCronRunLog(fingerprint, runId)` and adapts
- Passes cron-specific empty / unavailable copy
- Same behavior as today

### 5. Embedding contract documented

`apps/ui/src/components/logs/README.md` (new) — short doc that explains:

- How to embed `<LogViewer>` on a detail page (HA target, Pi-hole target, probe detail, alert detail)
- Example: caller provides a `useLogs` hook that wraps `useLogsQuery(filter, range)` with appropriate LogsQL
- Example: minimal embed code: ~10 lines of JSX
- Forward-references future epics that will use this (EPIC-005 HA detail, EPIC-006 Pi-hole detail, EPIC-007 Unifi detail, EPIC-008 Synology detail, plus future cron-detail / alert-detail polish stages)

### 6. Updated tests for both refactored viewers

- `apps/ui/src/routes/integrations/__tests__/DockerContainerLogsViewer.test.tsx` — same coverage, now exercising the wrapper-`<LogViewer>` split
- `apps/ui/src/routes/inventory/__tests__/CronRunLogViewer.test.tsx` — same

## Out of scope for this stage

- Pagination — STAGE-004-007
- Custom datetime range picker — STAGE-004-008
- Local-time toggle — STAGE-004-009
- Search box — STAGE-004-010
- LogsQL textarea — STAGE-004-011
- Stream picker sidebar — STAGE-004-012
- Histogram — STAGE-004-019
- Field inspector — STAGE-004-016
- Any actual embedding on detail pages — that's per-epic work (EPIC-005/6/7/8) — this stage only ships the contract + docs

## Decisions in this stage

### Design phase will confirm

- **Mobile line rendering:** horizontal scroll vs wrap. Recommendation: horizontal scroll with sticky timestamp + service columns (matches existing docker viewer behavior).
- **Line color coding by severity:** which severities get which colors (or none). Recommendation: subtle text-color tinting (`text-red-500` for error/critical, `text-yellow-500` for warn) with NO background colors so the line list stays readable in bulk.
- **`useLogs` hook shape:** literal shape locked above; Design phase confirms or adjusts based on first consumer needs.

### Locked Design Decisions (inherited from brainstorming 2026-05-28)

- **D-LOGVIEWER-COMPONENT** — single shared `<LogViewer>` used by all log-display surfaces in the app.
- **D-CALLER-PROVIDES-HOOK** — `<LogViewer>` is data-source agnostic; caller supplies `useLogs()`. This is the embedding contract for future detail pages.
- **D-EMBEDDING-DOCUMENTED** — explicit deliverable: written contract + example in `apps/ui/src/components/logs/README.md` so future epics (HA / Pi-hole / Unifi / Synology detail pages) drop in `<LogViewer>` without rebuilding infrastructure.
- **D-NO-BREAKING-CHANGES-TO-EXISTING-VIEWERS** — docker + cron viewers render identically after refactor. Tests assert this.

## Design Phase

**Design phase complete: 2026-05-29.**
**Status**: [x] Design (Complete)

**Selected approach: Option C — compound primitives + convenience `<LogViewer>` default.**

Rationale: The two existing viewers diverge STRUCTURALLY (cron renders loading/error inside a sticky metadata header; docker does a full-tree 404 early-return), not just in copy. Forcing both through one shared component (Options A/B) demonstrably breaks D-NO-BREAKING-CHANGES. Option C deduplicates only what is truly duplicated — the line list, per-line row, and banner boxes — and lets each wrapper keep its exact outer structure.

Component structure (`apps/ui/src/components/logs/`):
- `LogLineList.tsx` — shared primitive: `<pre>` line list, per-line `<span>{ts}</span> {msg}` row, severity tint, `emptyContent` slot, `testId` prop, owns `overflow-x-auto` mobile behavior.
- `LogBanner.tsx` — extracted amber/blue rounded banner box (`tone`, `testId`, `role`, children).
- `LogViewer.tsx` — convenience default composing primitives in docker's stacked layout; honors the locked `useLogs`/`headerSlot`/`emptyStateCopy`/`unavailableCopy`/`timezone` prop shape. This is the drop-in for future detail pages + the Explorer.
- `__tests__/LogViewer.test.tsx`, `__tests__/LogLineList.test.tsx`, `__tests__/LogBanner.test.tsx` — primitives unit-tested in isolation.
- `README.md` — embedding contract + ~10-line example.

Refactor strategy (preserves both viewers byte-for-byte):
- Docker wrapper → renders `<LogViewer>`; keeps the 404 `container_unknown` early-return above it; passes docker-specific `truncatedCopy`/`emptyStateCopy`.
- Cron wrapper → keeps its sticky `<header>`/body skeleton (sticky-header loading/error, expired-omits-body guard, running banner); swaps inline `<pre>` for `<LogLineList>` and banners for `<LogBanner>`.

Confirmed UX decisions (the three "Design will confirm" items):
1. Mobile rendering: keep current horizontal-scroll `overflow-x-auto <pre>` this stage (zero-visible-change mandate). Sticky timestamp/service column upgrade deferred with a TODO in `LogLineList`.
2. Severity color coding: subtle text tint in `LogLineList` keyed off typed `severity` field — `text-red-500` (error/critical/alert/emergency), `text-yellow-500` (warn), muted otherwise. No background colors. Renders as no-op if `severity` is null (still zero-visible-change).
3. `useLogs` hook shape: locked shape used for the default `<LogViewer>`; per-source raw status (`container_unknown`→`unknown`, `vl_unavailable`→`unavailable`) mapped to the normalized enum inside each wrapper's `useLogs` adapter.

Build-phase verification note: confirm both wrappers' adapters surface `severity` from the converged `LogLine` before wiring the tint.

Stage consistency check: PASS (description matches success criteria, no copy-paste artifacts). Backend contract: verified — LogLine returns real typed fields, both viewers already consume the converged shape (STAGE-004-002). Pure frontend refactor, Refinement 3a only.

## Build Phase

**Build phase complete: 2026-05-29.** `make verify` GREEN (exit 0).

Files created (`apps/ui/src/components/logs/`):
- `types.ts` — `LogLine`, `LogViewerStatus`, `UseLogsResult`, `LogViewerProps`. NOTE: `UseLogsResult.logStatus` and `.truncated` are typed `?: T | undefined` (explicit `| undefined`) to satisfy `exactOptionalPropertyTypes: true` — the docker adapter computes possibly-undefined values for both.
- `severity.ts` — pure `severityTintClass()` (red for error/critical/alert/emergency, yellow for warn, '' otherwise incl. null). Isolated for unit-test coverage; no-op when severity null.
- `LogLineList.tsx` — shared `<pre>` primitive: per-line `<span>{ts}</span> {msg}`, severity tint, `emptyContent` slot, `testId`, owns `overflow-x-auto`.
- `LogBanner.tsx` — amber/blue rounded banner box (`tone`/`testId`/`role`/children).
- `LogViewer.tsx` — convenience default composing primitives in docker's stacked layout; consumes the locked `useLogs` shape.
- `README.md` — embedding contract + example.
- `__tests__/LogViewer.test.tsx` (14), `__tests__/LogLineList.test.tsx` (15, incl. severity unit tests), `__tests__/LogBanner.test.tsx` (5). 34 new tests.

Files refactored:
- `DockerContainerLogsViewerBody.tsx` → renders `<LogViewer>`; keeps 404 `container_unknown` early-return + generic-error `<p role="alert">` above it (adapter returns isError:false for generic errors to avoid double error UI); inline `useLogs` adapter maps `vl_unavailable`→`unavailable`.
- `CronRunLogViewer.tsx` → keeps sticky header/expired-omits-body/running-banner; swaps inline `<pre>` for `<LogLineList>` and banners for `<LogBanner>`; does NOT call `<LogViewer>` (composes primitives directly per design).
- Existing viewer test files unchanged (every data-testid/copy string preserved).

No openapi/UI-types regen needed (no backend API change; schema.ts already carries severity).

Pre-existing defect fixed during Build (per CLAUDE.md "fix by default"): `apps/monitor/tests/test_api_cron_runs.py` lacked the `_mock_vm_lifespan_tick` autouse fixture that six sibling `test_api_docker_*.py` modules use. The `DockerSocketCollector` background task (hash("docker_socket")%30s offset) fires HTTP requests that probabilistically landed in this module's httpx_mock window, causing a teardown "unexpected requests" error (surfaced this run on `test_get_run_log_no_slack_when_disabled`). Added the autouse fixture (catch-all `is_optional`/`is_reusable` mocks for victoriametrics/localhost/victorialogs), and tightened two count-zero assertions (`test_get_run_log_expired_old_window`, `test_get_run_log_expired_null_vl_window_end`) to count only `vl-test` host requests so leaked background requests no longer break them. Latent since STAGE-002-014; unrelated to the frontend refactor.

Build fix waves: (1) 22 ESLint require-await (async-no-await test callbacks → drop async); (2) backend test-isolation fixture + ruff isort + prettier on new test files + the two assertion tightenings; (3) TS2375 exactOptionalPropertyTypes type widening in types.ts. Frontend tsc gotcha confirmed: the UI build's strict `tsc -b` (exactOptionalPropertyTypes) is STRICTER than vitest's tsc — the scribe's narrow checks passed but the full gate caught it.

**Status**: [x] Build (Complete)

## Refinement Phase

- 3a only (frontend-only refactor)
- [x] Desktop Approved (docker + cron viewers render identically)
- [x] Mobile Approved
- [x] Regression Items Added

**Refinement phase complete: 2026-05-29.** Frontend stage, 3a only (no host-integration → no 3b). Manually tested against `make dev` rig (UI 127.0.0.1:5180, backend 19090) on the Home Assistant docker logs view + cron run log view. User formally APPROVED Desktop AND Mobile.

SCOPE EXPANDED during Refinement by explicit user request (user owns WHAT to build; D-NO-BREAKING-CHANGES relaxed for this stage). The zero-visible-change extraction was completed, then four visible features were added to `<LogViewer>`/`LogLineList`:
1. ANSI SGR rendering (`ansi.ts` — hand-rolled parser, no dep; 8/16 color + bold/dim/underline → Tailwind literal classes; 256/truecolor consumed-and-ignored; no dangerouslySetInnerHTML). Docker HA logs with `\x1b[31m...` now render colored instead of raw escape text.
2. Wrap toggle (`WrapToggle.tsx` — native checkbox; default nowrap preserves prior overflow-x-auto; wired into both docker + cron viewers).
3. Timestamp column + vertical divider (per-line `grid grid-cols-[auto_1fr]` with `border-r` divider; coexists with wrap via items-start).
4. Friendly UTC timestamp (`formatLogTimestamp` in relativeTime.ts — `2026-05-29T12:53:53.162712958Z` → `2026-05-29 12:53:53 UTC`; STILL UTC, zone shift deferred to 004-009; raw passthrough on non-ISO).
Plus label title-casing per user: Wrap / Last: / Since:.

Severity tint note: docker container logs default to `severity:"info"` (Vector add_labels has no .level for docker_logs), so error lines embedded in message text show no tint — tracked as new STAGE-004-004A (docker log severity-level extraction).

MAJOR ADJACENT WORK (separate concern, done this stage at user request, separate commit): backend test-harness boot-once refactor. Tests previously booted the full app lifespan per-test (12-22s setup under xdist due to real-Docker-socket image_update_checker blocking run). New session-scoped `_shared_app` (lightweight test lifespan, no background machinery) + per-function DB isolation + per-function singleton reset/re-establish (`apps/monitor/tests/_test_lifespan.py` + conftest rewrite). Backend suite ~597s → ~360-380s (verified across 3 clean full runs, isolation-safe). NOT the 70% an intermediate measurement suggested — honest ~36-40%. Two regressions found+fixed during the refactor: (a) env-var leak (DISABLE_STARTUP_CRON_DISCOVERY persisting into real-lifespan tests) → split env into persistent (MASTER_KEY/HTTPS_ONLY_COOKIES, read at request time) vs transient (3 divergent vars restored after wiring); (b) lost incidental coverage on compose_action_runner.py:639->641 + image_update_collector.py:402-403 → added 3 targeted unit tests. Also fixed the earlier `_mock_vm_lifespan_tick` gap in test_api_cron_runs.py (from the first Build wave).

make verify: GREEN (3264 passed, 100% kernel coverage, frontend 0 eslint errors, build green).

**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:
- Frontend implementation: `2f56477`
- Backend test-infra: `f68b76e`
- Changelog: `094f3a3`

**CHANGELOG Entry**: [x] Added

**Finalize phase complete: 2026-05-29.**

Code reviewer (Opus) reviewed full diff → APPROVED. Four Minor findings all implemented: stale docstring in LogLineList, inert timezone prop doc comment, README wrap prop documentation, ANSI fg-fallback coupling comment. Verification: `make verify` GREEN (3264 backend tests passed, 100% kernel coverage maintained, frontend ESLint clean, build successful).

Stage Complete.
**Status**: [x] Finalize (Complete)
