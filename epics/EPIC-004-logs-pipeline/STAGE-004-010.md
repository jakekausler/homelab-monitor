# STAGE-004-010: Logs Explorer skeleton at `/logs` (plain-text search, presets, custom range, paginated results)

## Status: Not Started

## Overview

Add the top-level `/logs` route with the first usable iteration of the Logs Explorer. v1 surfaces:

- Plain-text **search box** at top: translates to `_msg:"..."` LogsQL under the hood (LogsQL advanced mode lands in STAGE-004-011)
- `<TimeRangeControl>` from STAGE-004-008 (since-presets + custom range)
- Run / Search button (or auto-run on edit-debounce — Design decides)
- Results panel: paginated log lines via `<LogViewer>` (inheriting cursor pagination from STAGE-004-007, local-time rendering from STAGE-004-009)
- URL state: search query + range deep-linkable

No sidebar yet (STAGE-004-012), no saved queries (STAGE-004-013), no query history (STAGE-004-014), no state persistence beyond URL (STAGE-004-015), no field inspector (STAGE-004-016), no histogram (STAGE-004-019), no export (STAGE-004-020), no live tail (STAGE-004-023+).

Just a usable Explorer page.

**Vertical slice:** user navigates to `/logs`, types a search like "connection refused", picks a time range, clicks Search, sees matching lines from VL across all sources. Cursor pagination + custom range work. Deep-link `/logs?q=connection%20refused&since=24h` reproduces the view.

**Spec references:** master spec §9.2 (Logs screen / LogsQL explorer).

## Stage Flags

- Has Input Forms: [x] Yes — search box + range picker
- Touches secrets / production data: [ ] No
- Host-integration concerns: [ ] No. Refinement is **3a only** (Desktop + Mobile).

## Cross-cutting standards (from EPIC-004 §Cross-stage acceptance criteria)

Frontend coverage gates maintained. Backend `/api/logs/query` already exists; this stage adds the route + page.

## Concrete deliverables

### 1. Top-nav entry

`apps/ui/src/components/TopNav.tsx` (or wherever main navigation lives) — add a "Logs" entry that routes to `/logs`. Iconography from lucide (e.g., `ScrollText`).

### 2. Route registration

`apps/ui/src/router.tsx` — register `/logs` as a child of `protectedLayoutRoute`. Component: `LogsExplorerPage`.

### 3. `LogsExplorerPage` component

`apps/ui/src/routes/logs/LogsExplorerPage.tsx` (new) — layout:

```
+--------------------------------------------------+
| Search box (plain text, with placeholder)        |
| [TimeRangeControl] [Search button]               |
+--------------------------------------------------+
| <LogViewer> with `useLogs` wrapping              |
|   useLogsQuery(currentLogsQL, currentRange)      |
+--------------------------------------------------+
```

State:
- `searchText: string` (the plain-text input)
- `range: TimeRange` (since-preset or custom)
- Both are URL-synced (query string)

Search button (or debounced auto-fire) triggers a new query.

### 4. Plain-text → `_msg:` translation

`apps/ui/src/lib/logsQlTranslate.ts` (new):

```typescript
function translateSearchToLogsQl(searchText: string): string {
  if (!searchText.trim()) return '*'  // empty search → match all
  // Escape double quotes, embed in _msg:"..."
  const escaped = searchText.replace(/"/g, '\\"')
  return `_msg:"${escaped}"`
}
```

Tests for: empty input, basic phrase, quote escaping, leading/trailing whitespace.

### 5. `useLogsExplorerQuery` hook

`apps/ui/src/api/logs.ts` — extend with a hook that takes `(logsQl: string, range: TimeRange)` and returns the pagination-capable shape (from STAGE-004-007).

### 6. Empty / error / unavailable states

`<LogViewer>` already handles these from STAGE-004-003; just supply Explorer-specific copy:

- Empty: "No matches in the selected range. Try a wider time range or a different query."
- Unavailable: "Logs backend (VictoriaLogs) is unavailable. Check service health."

### 7. Mobile layout

Same as existing viewers — search box stacked above range control on mobile.

### 8. Tests

- `apps/ui/src/lib/__tests__/logsQlTranslate.test.ts` — translation cases
- `apps/ui/src/routes/logs/__tests__/LogsExplorerPage.test.tsx` — renders search box + range + viewer; URL state syncs; click Search refetches

## Out of scope for this stage

- LogsQL advanced mode toggle — STAGE-004-011
- Stream picker sidebar — STAGE-004-012
- Saved queries — STAGE-004-013
- Query history — STAGE-004-014
- State persistence beyond URL — STAGE-004-015
- Field inspector — STAGE-004-016
- Nested field extraction in results — STAGE-004-017
- Filter-scope field discovery — STAGE-004-018
- Histogram — STAGE-004-019
- Export — STAGE-004-020
- Open-in-Explorer deep-link from per-context viewers — STAGE-004-021 (the inverse direction; this stage just makes Explorer exist so STAGE-004-021 has a target)
- Global retention settings UI — STAGE-004-022
- Live tail — STAGE-004-023, 024

## Decisions in this stage

### Design phase will confirm

- **Search trigger:** explicit button vs debounced auto-fire. Recommendation: explicit button for v1 (predictable, no surprising VL queries)
- **Default range** — recommendation: 1h
- **Empty search behavior** — `*` LogsQL (match all in range)? Or require a query? Recommendation: `*` so the user immediately sees the most recent lines and can refine
- **Search box affordances** — clear button, prior-query autocomplete (defers to STAGE-004-014 query history)

### Locked Design Decisions (inherited from brainstorming 2026-05-28)

- **D-EXPLORER-TOP-LEVEL** — `/logs` is a top-level route, peer to Dashboard / Integrations / Alerts.
- **D-EXPLORER-SKELETON-FIRST** — v1 is intentionally minimal: search + range + pagination + viewer. Everything else is later stages.
- **D-PLAIN-TEXT-DEFAULT** — search box defaults to plain text; LogsQL toggle is a later stage. Users who don't know LogsQL still find this useful immediately.

## Design Phase

**Design phase complete: TBD.**
**Status**: [ ] Design (Not Started)

## Build Phase

**Build phase complete: TBD.**
**Status**: [ ] Build (Not Started)

## Refinement Phase

- 3a only (frontend only)
- [ ] Desktop Approved
- [ ] Mobile Approved
- [ ] Regression Items Added

**Refinement phase complete: TBD.**
**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation: TBD
- Changelog: TBD
- Tracking: TBD

**CHANGELOG Entry**: [ ] Added

**Finalize phase complete: TBD.**

Stage Complete.
**Status**: [ ] Finalize (Not Started)
