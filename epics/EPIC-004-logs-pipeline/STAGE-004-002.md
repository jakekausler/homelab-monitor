# STAGE-004-002: Backend `LogLine` shape convergence across all 3 log-fetching endpoints

## Status: Complete

## Overview

Today three endpoints return log data with three different response shapes:

- `GET /api/integrations/docker/containers/{name}/logs` → `ContainerLogsResponse { lines: LogLine[], log_status, truncated, window_start/end }`
- `GET /api/crons/{fp}/runs/{run_id}/log` → `RunLogResponse { entries: LogEntry[], state, duration_seconds, line_count, anomaly_flags, log_status, truncated }`
- `GET /api/logs/query` → `LogsQueryResponse { lines: VictoriaLogsLine[] }` (raw VL pass-through)

The inner per-line types diverge (`LogLine` vs `LogEntry` vs `VictoriaLogsLine`) in field names, optionality, and field set. This blocks the shared `<LogViewer>` component (STAGE-004-003) because the component would need three rendering paths.

This stage converges the inner line type to ONE Pydantic model (`LogLine`) used by all three endpoints, while preserving each endpoint's outer-envelope metadata (container status, cron state + anomaly flags, etc.). UI doesn't change yet — `<LogViewer>` extraction is the next stage.

**Vertical slice:** the three endpoints all return the same line shape. Existing UI continues to work (the converged shape is a superset of what each viewer reads today). Tests for all three endpoints assert the converged shape.

**Spec references:** Master spec §6.3 (VL streams keyed by host/service/severity), §9.2 (Logs explorer screen).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [ ] No — backend-only Pydantic shape change; Refinement is **3a only**.

## Cross-cutting standards (from EPIC-004 §Cross-stage acceptance criteria)

100% kernel coverage maintained. OpenAPI regen (`make openapi-export` + `bash scripts/generate-ui-types.sh`) must run BEFORE the implementation commit because all three endpoints' response models change.

## Concrete deliverables

### 1. The converged `LogLine` model

`apps/monitor/homelab_monitor/kernel/api/models/logs.py` (new file or extend existing) — single Pydantic model used by all 3 endpoints:

```python
class LogLine(BaseModel):
    timestamp: datetime  # UTC, ISO-8601
    severity: str | None = None  # info / warn / error / debug / etc., normalized to lowercase
    service: str | None = None  # service label as set by vector add_labels transform
    host: str | None = None  # host label
    message: str  # the log message (post-multiline-stitch; may contain \n)
    fields: dict[str, Any] = Field(default_factory=dict)  # extra structured fields (compose_*, run_id, etc.)
```

- `fields` is the bag for everything not in the typed core (compose_project, compose_service, container_id, run_id, cron_fingerprint, image labels — STAGE-004-004 will start populating these)
- All optional fields are typed `... | None` not `Optional[...]` per project pyright style
- Tests cover serialization (Pydantic → JSON) + deserialization with realistic VL payload shapes

### 2. Endpoint 1: docker per-container logs

`apps/monitor/homelab_monitor/kernel/api/routers/integrations/docker_logs.py` — `ContainerLogsResponse.lines: list[LogLine]` (change from current inner type). Container-specific envelope fields (`log_status`, `truncated`, `window_start`, `window_end`) stay.

### 3. Endpoint 2: cron run logs

`apps/monitor/homelab_monitor/kernel/api/routers/crons.py` (or wherever the cron run log endpoint lives) — `RunLogResponse.entries: list[LogLine]` (rename the field from `entries` to `lines` is OPTIONAL — Design phase decides whether to break the field name now or in a follow-up; current recommendation: keep `entries` name to avoid frontend churn but change the type). Cron-specific envelope fields (`state`, `duration_seconds`, `line_count`, `anomaly_flags`, `log_status`, `truncated`) stay.

### 4. Endpoint 3: generic LogsQL query

`apps/monitor/homelab_monitor/kernel/api/routers/logs.py` — `LogsQueryResponse.lines: list[LogLine]`. Existing VL pass-through translation logic (`VictoriaLogsClient.query`) maps the raw VL response to `LogLine`.

### 5. Mapper functions

`apps/monitor/homelab_monitor/kernel/logs/line_mapping.py` (new) — pure functions that convert each upstream source to `LogLine`:

- `from_victorialogs_line(vl: VictoriaLogsLine) -> LogLine`
- `from_docker_socket_log(entry: dict) -> LogLine` — if the docker endpoint still bypasses VL (verify in Design)
- `from_hmrun_event(event: dict) -> LogLine` — for cron run logs

100% test coverage of these mappers with realistic inputs from each source.

### 6. Frontend adaptation (minimal)

`apps/ui/src/api/docker.ts`, `apps/ui/src/api/crons.ts`, `apps/ui/src/api/logs.ts` — TypeScript types regenerated from openapi.json. Existing viewers (`DockerContainerLogsViewerBody`, `CronRunLogViewerPage`) read the renamed/retyped fields. NO refactor to a shared component yet — that's STAGE-004-003.

### 7. Tests

- `apps/monitor/tests/test_log_line_model.py` — model serialization round-trip, optional field handling, `fields` bag arbitrary nesting
- `apps/monitor/tests/test_line_mapping.py` — each mapper function with realistic inputs (VL raw response samples in `tests/fixtures/`)
- `apps/monitor/tests/test_api_docker_logs.py` — updated to assert converged shape
- `apps/monitor/tests/test_api_cron_run_log.py` — updated to assert converged shape
- `apps/monitor/tests/test_api_logs_query.py` — updated to assert converged shape
- `apps/ui/src/routes/integrations/__tests__/DockerContainerLogsViewer.test.tsx` — assertions still pass with new shape
- `apps/ui/src/routes/inventory/__tests__/CronRunLogViewer.test.tsx` — same

## Out of scope for this stage

- The shared `<LogViewer>` component — STAGE-004-003
- UI changes beyond what the type change forces
- New endpoints — only existing three are touched
- VL ingest-shape changes — only API-layer translation changes
- Multi-line handling — that's STAGE-004-001 (precursor)

## Decisions in this stage

### Design phase will confirm

- **Whether to rename `RunLogResponse.entries` → `lines`** — semantic consistency but frontend churn. Recommendation: keep `entries` name, change inner type only. Design phase locks.
- **Whether `LogLine.fields: dict[str, Any]`** stays untyped or becomes a typed sub-model — recommendation: dict[str, Any] for v1 to absorb the heterogeneous bag. Typing it later is non-breaking.
- **Severity normalization scheme** — what's "warn" vs "warning" vs "WARN" vs syslog severity numeric? Recommendation: normalize to lowercase canonical set (`debug, info, notice, warn, error, critical, alert, emergency`) at ingest time.

### Locked Design Decisions (inherited from brainstorming 2026-05-28)

- **D-CONVERGED-LINE-SHAPE** — single `LogLine` model used by all log-fetching endpoints. Outer envelope (status, container metadata, cron run state) stays endpoint-specific.
- **D-SHAPE-CONVERGENCE-PRECEDES-COMPONENT** — backend shape convergence lands BEFORE the shared `<LogViewer>` extraction (STAGE-004-003) so the component is built against the final shape.
- **D-FIELDS-BAG-UNTYPED-V1** — `fields: dict[str, Any]` for v1; typed sub-models can come later without breaking the wire.
- **D-NO-FRONTEND-REFACTOR** — existing UI continues to work; only TypeScript types regenerate.

## Design Phase

- **Approaches presented:** brainstormer (Opus) produced 6 discrete decisions, each with 2-3 options + recommended default. Proceeded autonomously with the recommended combined approach (user asleep; decisions cohere + each has a well-reasoned default; backend-only stage, low ambiguity).
- **Converged model:** `LogLine = { timestamp: str, message: str, stream: str, severity: str | None, host: str | None, service: str | None, fields: dict[str, Any] }`
- **D1 canonical names:** `timestamp` + `message` (matches the internal `VlLogLine` source-of-truth type all 3 endpoints already funnel through, and cron's existing names). Docker viewer's `entry.line` → `entry.message` (1 site); LogsQL `ts`/`line` rename is free (no viewer yet).
- **D2 rename strategy:** HARD rename inner types (single-user app, no external API consumers — no transition aliases / no dual-field debt).
- **D3 cron outer field:** rename `RunLogResponse.entries` → `lines`; standardize ALL 3 envelopes on `.lines` so STAGE-004-003's `useLogs` hook has a uniform `response.lines` contract. Folds into the CronRunLogViewer edits D1 already requires.
- **D4 promote fields:** `severity`, `host`, `service` become typed top-level `str | None` on LogLine (extracted by the mapper from the VL `fields` dict with fallbacks: severity from `severity`; host from `host`/`_HOSTNAME`; service from `service`/`SYSLOG_IDENTIFIER`). Full `fields: dict[str, Any]` bag is RETAINED so nothing is lost.
- **D5 severity normalization:** normalize at the mapper to a canonical lowercase set: `debug, info, notice, warn, error, critical, alert, emergency`. Map syslog numerics 0-7 + aliases (`warning`→`warn`, `err`→`error`, `crit`→`critical`, `panic`/`emerg`→`emergency`); unknown → `info`. Preserve the raw value in `fields.severity_raw` for auditability.
- **D6 mappers:** ship ONLY `from_victorialogs_line(VlLogLine) -> LogLine` (all 3 endpoints already go through `VlLogLine`, so this covers 100% of current call sites). `from_docker_socket_log` / `from_hmrun_event` are YAGNI — deferred until a direct non-VL source lands (avoids untested code under the 100%-coverage gate).
- **UI Options Presented:** N/A — backend shape change + minimal frontend accessor adaptation only (no UI redesign; that's STAGE-004-003).
- **Seed Data Agreed:** none new — tests use synthetic VlLogLine inputs covering: each severity alias/numeric → canonical mapping, missing severity→info, host/service extraction + fallback, fields-bag preservation, all-3-endpoint shapes serialize.
- **Refinement:** 3a only (no host-integration concerns; backend shape change validated against dev rig + integration tests).
- **Session Notes:**
  - All 3 endpoints currently funnel raw data through `VlLogLine` (timestamp/message/stream/fields) in kernel/logs/victorialogs_client.py — convergence is natural, single mapper.
  - Current inner types diverge: ContainerLogLine {timestamp, line} (drops stream+fields); RunLogLine {timestamp, message, stream, fields}; LogsQueryEntry {stream, line, ts, fields}.
  - Frontend blast radius is small: DockerContainerLogsViewerBody.tsx (entry.line→entry.message) + CronRunLogViewer.tsx (data.entries→data.lines, line.message stays). No LogsQL viewer yet.
  - OpenAPI regen is MANUAL before commit: `make openapi-export` + `bash scripts/generate-ui-types.sh`. apps/ui/src/api/schema.ts is gitignored.

**Design phase complete: 2026-05-28.**
**Status**: [x] Design (Complete)

## Build Phase

- **Components Created:**
  - `apps/monitor/homelab_monitor/kernel/logs/models.py` (NEW) — `LogLine` Pydantic model + `from_victorialogs_line(VlLogLine) -> LogLine` mapper + `_normalize_severity` helper.
  - `apps/monitor/tests/test_logline_model.py` (NEW) — 36 tests covering every mapper branch (severity numerics 0-7, aliases, canonical passthrough, case-insensitivity, unknown→info, None/empty→None, host/service extraction + fallbacks, severity_raw preservation, input-not-mutated, serialization).
- **API shape changes:**
  - `ContainerLogsResponse.lines: list[LogLine]` (deleted `ContainerLogLine`).
  - `RunLogResponse.entries` → `lines: list[LogLine]` (deleted `RunLogLine`).
  - `LogsQueryResponse.entries` → `lines: list[LogLine]` (deleted `LogsQueryEntry`).
  - All 3 build sites use `from_victorialogs_line(...)`.
- **Frontend adaptation (minimal):**
  - DockerContainerLogsViewerBody.tsx: `entry.line` → `entry.message`.
  - CronRunLogViewer.tsx: `.entries` → `.lines`.
  - Test mocks updated: CronRunLogViewer.test.tsx (`entries:`→`lines:`), DockerContainerLogsViewer.test.tsx (inner `line:`→`message:`).
- **OpenAPI:** regenerated packages/shared-types/openapi.json (`make openapi-export`) + apps/ui/src/api/schema.ts (`bash scripts/generate-ui-types.sh`, gitignored).
- **Verification:** `make verify` GREEN — ruff + ruff format + pyright 0 errors + 3260 backend tests + 100% kernel coverage + 508 frontend tests + tsc + UI build. Integration deselected. (log: /tmp/verify-004-002-build-final-*.log)
- **Session Notes:**
  - All 3 endpoints already funneled through `VlLogLine`, so a single mapper covered 100% of call sites (D6: from_docker_socket_log / from_hmrun_event deferred as YAGNI).
  - Pyright caught `_normalize_severity` private-import in the test (reportPrivateUsage); fixed with `# pyright: ignore[reportPrivateUsage]` on the import (testing the helper's branch matrix directly is intentional).
  - Frontend vitest initially failed 16 tests: stale mocks still used old `entries:`/`line:` field names → `data.lines` undefined → `.length` threw. Component code was correct; only the test fixtures were stale. Fixed the mocks.
  - Scribe also updated STAGE-004-001's integration tests (test_vector_to_vl_path.py, test_vector_multiline.py) which asserted the old `entries`/`line` shapes — those would have broken under the rename.
  - LESSON applied: ran the AUTHORITATIVE `make verify` (root pyright config) as the gate, not a narrow per-file pyright — which is what caught the reportPrivateUsage the scribe's targeted check missed.

**Build phase complete: 2026-05-28.**
**Status**: [x] Build (Complete)

## Refinement Phase

- **Type:** Backend-only (API response-shape convergence). No host-integration concerns → **3a only** (no 3b).
- **3a (validated against REAL data):** `make integration` (full docker-compose.test.yml rig, in-network) — the converged `LogLine` shape works end-to-end against live VictoriaLogs:
  - `test_vector_to_vl_path.py::test_log_line_via_noisy_logger_reaches_logs_query` PASS — plants a real line → vector → VL → `/api/logs/query`, reads `.lines[].message`/`.timestamp` from the converged shape. THE key validation (real VL data through the real converged endpoint, not unit mocks).
  - `test_vector_multiline.py` (11 tests) ALL PASS — query `/api/logs/query` and read `.lines[].message` from real multi-line VL records.
  - No shape/serialization errors anywhere: no KeyError, no `entries` vs `lines` mismatch, no Pydantic ValidationError.
  - Overall: 30 passed, 2 failed (known pre-existing: cadvisor timing flake + heartbeat missing-seed cron — out of scope), 2 skipped. Wall-clock 6m15s.
  - Rig health gate behaved (tests ran, not falsely skipped, rig fully up).
- [x] Desktop Approved (N/A — backend only)
- [x] Mobile Approved (N/A — backend only)
- [x] Regression Items Added
- **Refinement validated what unit mocks could not:** real VictoriaLogs data flowing through the real converged endpoints, confirming the `LogLine` model serializes correctly from actual VL records (the unit tests use synthetic VlLogLine inputs).

**Refinement phase complete: 2026-05-28.**
**Status**: [x] Refinement (Complete)

## Finalize Phase

- [x] Code Review (pre-tests) — code-reviewer (Opus): **APPROVED**, no Critical/Important findings. Two Minor items, both documented as intentional skips (implementing either would regress against an established convention or a locked Design decision):
  - Minor #1 "`LogLine` not frozen": SKIPPED — sibling response models (ContainerLogsResponse, RunLogResponse) are not frozen either; making only LogLine frozen would be INconsistent with repo convention. Reviewer explicitly noted "matches repo convention, no change needed."
  - Minor #2 "`fields: dict[str, Any]` could be `dict[str, str]`": SKIPPED — `dict[str, Any]` is a LOCKED Design decision (D4: untyped fields-bag for v1) chosen for forensic forward-compat. STAGE-004-017 (nested JSONL extraction) will place non-string values in this bag, so tightening to `dict[str, str]` now would have to be reverted. Keeping `Any` per the locked decision.
- [x] Tests Written — during Build (test_logline_model.py, 36 tests covering the full mapper branch matrix; + updated endpoint shape assertions in test_api_logs/cron_runs/docker_container_logs + integration tests).
- [x] Code Review (post-tests) — N/A: no implementation code changed after the pre-test review (review was APPROVED with only documented skips), so no second review required per phase-finalize self-check.
- [x] Documentation Updated — local-environment.md / CLAUDE.md unchanged this stage (no new operator-facing surface); the converged shape + severity normalization are documented in the stage file + regression.md.
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: 90d336f
- Changelog: a879d1f
- Tracking: (this commit)

**CHANGELOG Entry**: [x] Added

**Finalize phase complete: 2026-05-28.**

Stage Complete.
**Status**: [x] Finalize (Complete)
