# STAGE-004-021: "Open in Explorer" deep-link from Docker + Cron viewers

## Status: Not Started

## Overview

Add a small "Open in Explorer" button to the Docker container log viewer + the Cron run log viewer. Clicking it navigates the user to `/logs?logsql=<pre-filled>&start=<...>&end=<...>` with filters pre-populated from the source viewer's context.

For the docker viewer: pre-fills `service:"<container_name>"` (or `compose_service:"<svc>"` if a richer match makes sense; Design picks).
For the cron viewer: pre-fills `cron_fingerprint:"<fp>"` AND `run_id:"<run_id>"` to scope to the exact run.

**Vertical slice:** open the docker container logs viewer for `home-assistant` → click "Open in Explorer" → land in `/logs` with the query pre-filled to `service:"home-assistant"` and the same time range. User can refine from there.

**Spec references:** master spec §9.2.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [ ] No. Refinement is **3a only**.

## Cross-cutting standards (from EPIC-004 §Cross-stage acceptance criteria)

Frontend coverage gates maintained.

## Concrete deliverables

### 1. Helper to build Explorer URL

`apps/ui/src/lib/explorerLink.ts` (new):

```typescript
function buildExplorerUrl(opts: {
  logsQl?: string
  plainText?: string
  selectedServices?: string[]
  sincePreset?: string
  rangeStart?: Date
  rangeEnd?: Date
}): string {
  // Produces `/logs?logsql=...&services=...&start=...&end=...` etc.
}
```

100% test coverage.

### 2. Docker viewer integration

`apps/ui/src/routes/integrations/DockerContainerLogsViewerBody.tsx` (or its header slot) — add the button:

```tsx
<Link to={buildExplorerUrl({
  logsQl: `service:"${name}"`,
  sincePreset: since
})}>Open in Explorer</Link>
```

Place near the existing time-range picker / refresh button.

### 3. Cron viewer integration

`apps/ui/src/routes/inventory/CronRunLogViewer.tsx` (or its header slot) — same idea:

```tsx
<Link to={buildExplorerUrl({
  logsQl: `cron_fingerprint:"${fingerprint}" AND run_id:"${runId}"`,
  rangeStart: run.started_at,
  rangeEnd: run.finished_at ?? new Date()
})}>Open in Explorer</Link>
```

### 4. Future-detail-page contract

This stage doesn't add the button to other detail pages (HA, Pi-hole, etc. — those are per-epic work). But it documents the helper so future epics can drop the button in trivially.

Update `apps/ui/src/components/logs/README.md` (created in STAGE-004-003) with a section showing the helper + an example.

### 5. Tests

- `apps/ui/src/lib/__tests__/explorerLink.test.ts` — URL composition
- Updated tests for docker + cron viewers asserting the link is present + has correct href

## Out of scope for this stage

- "Open in Explorer" button on alert detail pages — per-epic work; will be added when alert-detail UI gets touched
- "Back to Docker viewer" link from Explorer — speculative; YAGNI
- Bidirectional state sync between viewer and Explorer — no, just deep-link

## Decisions in this stage

### Design phase will confirm

- **Docker filter shape** — `service:"<name>"` vs `container_name:"<name>"` (after STAGE-004-004 enrichment lands, both work; pick the canonical one)
- **Cron range** — running runs have `finished_at == null`; use `now` as end OR send no end (open-ended)
- **Button placement** — header slot vs separate "actions" area

### Locked Design Decisions (inherited from brainstorming 2026-05-28)

- **D-OPEN-IN-EXPLORER** — every per-context log viewer gets a button to jump to the Explorer with filters pre-filled.
- **D-PRE-FILLED-FILTERS** — Docker → service / compose service; Cron → fingerprint + run_id.
- **D-FUTURE-PAGES-INHERIT** — helper documented so HA / Pi-hole / Synology / etc. detail pages can add the button cheaply when they land.

## Design Phase

**Design phase complete: TBD.**
**Status**: [ ] Design (Not Started)

## Build Phase

**Build phase complete: TBD.**
**Status**: [ ] Build (Not Started)

## Refinement Phase

- 3a only
- [ ] Desktop Approved
- [ ] Mobile Approved
- [ ] Regression Items Added

**Refinement phase complete: TBD.**
**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation: TBD
- Changelog: TBD
- Tracking: TBD

**CHANGELOG Entry**: [ ] Added

**Finalize phase complete: TBD.**

Stage Complete.
**Status**: [ ] Finalize (Not Started)
