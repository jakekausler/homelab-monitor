# Regression Checklist - EPIC-008: Synology

(Items added per stage during Refinement.)

## STAGE-008-001 — Synology DSM v7 client

- [ ] **Live auth + system info:** The real `SynologyRestClient` (base_url `https://192.168.2.4:5001`, account `homelab-monitor`, secret `synology_dsm_password`) authenticates against the live DSM (`SYNO.API.Auth` v7 login, **NO `session=` param**) and `system_info()` returns a `SynologyResponse` with real data (model `DS3622xs+`, firmware string, uptime, sys_temp) + a `took_seconds` float. (Sending a `session=` param returns DSM error 402 — do NOT add it.)
- [ ] **Session reuse:** A second client call reuses the `_sid` without re-login (only ONE `SYNO.API.Auth method=login` across two calls).
- [ ] **Logout frees the session:** `aclose()` issues `SYNO.API.Auth method=logout` and never raises even if the NAS is unreachable at shutdown.
- [ ] **119 re-auth:** A DSM body error code 119 (session expired, on HTTP 200) triggers exactly ONE re-auth + retry; a second 119 surfaces `SynologyError(reason="auth", status=119)`.
- [ ] **Self-signed TLS:** the dedicated Synology httpx client uses `verify=False` (DSM cert `CN=synology`).
- [ ] **DSM password never logged:** no Synology error message or log line ever contains the password or the `_sid`.
- [ ] **Cron fixtures present:** `apps/monitor/tests/data/cron_fixtures/{system_cron,user_crontab,reboot_only}.example` exist + are git-tracked (the `!apps/monitor/tests/data/` `.gitignore` exception) so the 3 cron-parser tests pass on a clean checkout. (Pre-existing failure fixed in this stage.)

## STAGE-008-002 — Synology integration bundle skeleton

- [ ] **Bundle registers:** `register_all` (called from lifespan after the Unifi registration) registers `synology_placeholder` via `loader.load_all()` with config name `synology_placeholder`, interval 60s, timeout 5s, concurrency_group `default` (NOT `synology`).
- [ ] **Defensive isolation:** a raising `loader.register` for one collector is logged (`synology_integration.collector_register_failed`) and does NOT propagate (other collectors still register).
- [ ] **Placeholder emits:** `SynologyPlaceholderCollector.run()` returns ok=True, metrics_emitted==1, emits `homelab_synology_bundle_loaded=1.0`, and never touches `ctx.synology`.
- [ ] **Placeholder is scaffolding:** `placeholder.py` + the `_SYNOLOGY_COLLECTORS` entry + `test_synology_placeholder_collector.py` are REMOVED in STAGE-008-005 (the first Wave-B collector). After 008-005, `homelab_synology_bundle_loaded` no longer emits.
- [ ] **No `_shared.py` yet:** `integrations/synology/_shared.py` is created by STAGE-008-003 (not 008-002).

## STAGE-008-003: Synology `_shared.py` helpers + cardinality-cap hook

- [ ] **`_shared.py` helper unit suite green:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_shared.py"` passes (currently 14+ tests, 100% branch coverage of `_shared.py`). Covers `client_unconfigured_result`, `failed_result`, `fetch_or_result` (success + error branches), `as_float` (all branches incl. bool/None/non-finite rejection), `bytes_field`/`percent_field`, `cap_for_synology`, `capped_emitter`.
- [ ] **API-latency metric name is stable:** `_shared.M_API_TOOK_SECONDS == "homelab_synology_api_took_seconds"`; `fetch_or_result` emits it with a single `{api=<endpoint>}` label on every successful fetch. Any Synology collector relying on this gauge must keep the name + label key in sync.
- [ ] **`ok=True`-when-NAS-sad convention preserved:** `fetch_or_result` returns the `SynologyResponse` (NOT a failed `CollectorResult`) whenever the client returns a `SynologyResponse`, regardless of degraded payload fields (e.g. volume status "has_unverified_disk"). Only a `SynologyError` produces `ok=False`. Collectors own the per-field state decision.
- [ ] **Cardinality cap is the default 500 (guardrail, not active limit):** `cap_for_synology("homelab_synology_<any>")` returns 500 (no `homelab_synology_*` entries in `_DEFAULT_CARDINALITY_FAMILIES` — intentional). `capped_emitter(...).emit_family(...)` always writes one `homelab_metric_family_dropped_series` gauge and appends a `SuggestionEvent(severity="warning")` only when series exceed the cap. Verified: 8 obs → 0 dropped; 600 obs → 100 dropped + 1 warning event.
- [ ] **3b live exercise owed by STAGE-008-005:** `_shared.py` helpers are first exercised against the LIVE DSM in STAGE-008-005 (storage collector). Confirm there.
- [ ] **Collector-author contract notes (for STAGE-008-005+):** use `emitted: list[int] = [0]` and accumulate across `fetch_or_result` calls (mutable-box is intentional); call `capped_emitter` once per `run()` tick with the same `events` list returned in the `CollectorResult`; count the always-written drop gauge toward `metrics_emitted` (e.g. `emit_family` return + 1 per family). `bytes_field`/`percent_field` are thin `as_float` aliases — the extension points if a field later needs unit conversion.

## STAGE-008-004: SSH `homelab-probe` dedicated-user target config (EPIC-017 framework)

- [ ] **ssh-probe CLI dedicated-user tests green:** `make uv ARGS="--directory apps/monitor pytest tests/test_cli_ssh_probe.py"` passes incl. `test_install_instructions_dedicated_user_with_script_id`, `test_test_dedicated_user_restriction_enforced`, `test_test_dedicated_user_restriction_broken`.
- [ ] **install-instructions renders the hardened forced-command line:** for a `dedicated-user` target with `user: homelab-probe`, `hm ssh-probe install-instructions <target>` renders `command="/home/homelab-probe/hm-probe.sh",no-port-forwarding,no-pty,no-X11-forwarding,no-agent-forwarding <pubkey> hm-probe-<target-id>` and an ADVISORY (skippable) sudoers step. NO private key is ever printed.
- [ ] **Docs subsection accurate:** the `docs/ssh-probe-setup.md` Synology subsection's CLI commands, config fields, secret name `ssh_probe_key_synology`, script path `/home/homelab-probe/hm-probe.sh`, and no-sudoers claim stay consistent with `cli/ssh_probe.py` + `kernel/ssh/config.py` + EPIC-008 Amendment 3.
- [ ] **authorized_keys comment is target-id-derived, NOT script_id:** the rendered comment is `hm-probe-<target-id>` (`hm-probe-synology`); `script_id` (`synology_probe`) does NOT appear in install-instructions output (it's for runtime probe dispatch). Don't expect script_id in the setup recipe.
- [ ] **ssh-probe CLI needs a migrated DB + master key:** `keygen`/`install-instructions`/`test` call `build_secrets_repo()`; on a fresh instance the backend must have started once (auto-migrate) before the ssh-probe CLI secret store is usable. (Operator gotcha.)
- [ ] **LIVE provisioning + `homelab-probe` DSM user creation owed by STAGE-008-014 (MAIN instance A):** the real keygen→capture-hostkey→install→`test` against the live Synology, and creation of the low-priv `homelab-probe` DSM user, are deferred to STAGE-008-014 and MUST be done in the main instance A (NOT instance B). Confirm there.

## STAGE-008-005: Synology storage collector (volumes + disks) + placeholder removal

- [ ] **Storage collector unit suite green:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_storage_collector.py"` passes (13 tests, 100% branch coverage of `storage.py`). The `SynologyPlaceholderCollector` + its test are GONE; `_SYNOLOGY_COLLECTORS` registers `SynologyStorageCollector`; `test_synology_integration_bundle.py` asserts the storage collector registers (name `synology_storage`, 300s interval, 30s timeout).
- [ ] **Live 3b emits real metrics:** running `SynologyStorageCollector().run(ctx)` against the live NAS (secret `synology_dsm_password` in B's store) yields `ok=True`, ~92 metrics, 0 errors — per-volume `homelab_synology_volume_*` (used/total/used_percent/status×2/fs_type + writable/encrypted/locked/inode_full) + per-disk `homelab_synology_disk_*` (temp{disk,model}/smart_status/status/unc_count/remain_life/sb_days_left/size_bytes/slot) for 8 disks.
- [ ] **Real load_info key contract (do not regress):** parse reads `volumes[].id`/`.status`/`.space_status.status`/`.size.used`/`.size.total`/`.fs_type`/`.is_writable`/`.is_encrypted`/`.is_locked`/`.is_inode_full`; `disks[].id`/`.model`/`.temp`/`.smart_status`/`.status`/`.unc`/`.remain_life.value`/`.sb_days_left`/`.size_total`/`.slot_id`. used_percent is DERIVED (no field). `remain_life` is an OBJECT (`.value`, -1 on HDDs emitted literally). Booleans parsed via `_bool_to_gauge` (NOT as_float, which rejects bools).
- [ ] **volume_status is a state-set (2 obs live):** emits `homelab_synology_volume_status{volume,status}=1` for BOTH the top-level `status` and the nested `space_status.status`. Don't expect a single observation.
- [ ] **Cardinality cap guardrail:** all 17 families cap-routed; `homelab_metric_family_dropped_series{family}` emitted (0.0 live — 8 disks/1 vol far under the default-500 cap).
- [ ] **No pool/RAID metrics here:** `storagePools[]` is STAGE-008-006's slice; 005 emits only volumes + disks from the shared `load_info` response.

## STAGE-008-006 — Synology Pool & RAID collector (`pool.py`)

- [ ] **Run the live 3b exercise:** build a real `SynologyRestClient` against `https://192.168.2.4:5001` (account `homelab-monitor`, secret `synology_dsm_password` from B's store) and call `await SynologyPoolCollector().run(ctx)` with a `MemoryRetainingMetricsWriter`. Expect `ok=True`, ~34 metrics, zero errors.
- [ ] **Verify the `homelab_synology_pool_status` family emits ≥2 series** (top-level `status` + nested `space_status.status`), `homelab_synology_pool_unverified_disk=1.0` while the live pool is unverified, `homelab_synology_raid_status{raid="/dev/md2"}=1.0` with normal/designed disk count 8/8, and `homelab_synology_pool_progress_percent=-1.0` when idle.
- [ ] **Unit:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_pool_collector.py"` passes with 100% branch coverage on `pool.py`; `tests/test_synology_integration_bundle.py` asserts `SynologyPoolCollector` registers (name `synology_pool`, interval 300, timeout 30).

## STAGE-008-007 — Synology System collector (`system.py`) + navigator extraction to `_shared.py`

- [ ] **Run the live 3b exercise:** build a real `SynologyRestClient` against `https://192.168.2.4:5001` (account `homelab-monitor`, secret `synology_dsm_password` from B's store) and call `await SynologySystemCollector().run(ctx)`. Expect `ok=True`, ~17 metrics, zero errors, and exactly 3 `homelab_synology_api_took_seconds` entries (system_info + fanspeed + need_reboot).
- [ ] **Verify `homelab_synology_info` emits with labels {model="DS3622xs+", serial, firmware="DSM 7.3.2-86009", cpu_series="D-1531"}, `homelab_synology_system_uptime_seconds` is a sane positive number (parsed from the "HHH:MM:S" up_time string), `homelab_synology_sys_temp_celsius` ~50, `homelab_synology_sys_temp_warning=0`, `homelab_synology_fan_status{state="cool_fan"}=1.0` / `{state="all_disk_temp_fail"}=0.0` / `{state=<mode>}=1.0`, `homelab_synology_need_reboot=0`.
- [ ] **Multi-fetch degradation:** if a SUPPLEMENTARY fetch (fanspeed/need_reboot) errors, the collector must stay `ok=True` with the error recorded in `result.errors` and the system_info families still emitted; only a PRIMARY (system_info) failure yields `ok=False`.
- [ ] **Navigator-extraction regression guard:** `as_dict`/`as_list_of_dicts`/`nested`/`bool_to_gauge` live in `_shared.py` (NOT re-defined locally in storage.py/pool.py/system.py). `SynologyStorageCollector` and `SynologyPoolCollector` must still emit live (storage ~92 metrics/19 families, pool ~34 metrics/18 families) — a regression here means the extraction broke a call site.
- [ ] **Unit:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_system_collector.py"` passes; `test_synology_integration_bundle.py` asserts `SynologySystemCollector` registers (name `synology_system`, interval 60, timeout 30); storage + pool + shared collector tests all still pass at 100% branch.

## STAGE-008-008 — Synology Utilization collector (`utilization.py`)

- [ ] **Run the live 3b exercise:** build a real `SynologyRestClient` against `https://192.168.2.4:5001` (account `homelab-monitor`, secret `synology_dsm_password` from B's store — retrieve with `-e HOMELAB_MONITOR_REVEAL=1` on the docker exec) and call `await SynologyUtilizationCollector().run(ctx)`. Expect `ok=True`, ~110 metrics, zero errors, exactly 1 `homelab_synology_api_took_seconds` entry (single fetch).
- [ ] **Verify the UNIT conversions:** `homelab_synology_cpu_load1` is a FLOAT load-avg (≈0.x, NOT the raw ×100 DSM int); `homelab_synology_mem_total_bytes` is ~16e9 bytes (KB×1024, NOT ~16e6 KB); `homelab_synology_nfs_total_max_latency_seconds` is in seconds (ms×0.001).
- [ ] **Verify the AGGREGATE labels:** `homelab_synology_disk_read_bytes_per_second` has 9 series (8 drives sda..sdh + `{device="total"}`); `homelab_synology_net_rx_bytes_per_second` has `{iface="total"}` + per-NIC; `homelab_synology_vol_io_read_bytes_per_second` has `{volume="total"}` + `{volume="volume1"}` (display_name preferred over `dm-1`).
- [ ] **Scope:** smb/lun/swap-io are deliberately NOT emitted (YAGNI scope-out). All 28 data families (6 cpu + 6 mem + 5 disk + 2 net + 3 vol + 6 nfs) must emit.
- [ ] **Unit:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_utilization_collector.py"` passes at 100% branch coverage on `utilization.py`; `test_synology_integration_bundle.py` asserts `SynologyUtilizationCollector` registers (name `synology_utilization`, interval 60, timeout 30).

## STAGE-008-009 — Synology UPS + SystemHealth collector (`ups.py`)

- [ ] **Run the live 3b exercise:** build a real `SynologyRestClient` against `https://192.168.2.4:5001` (account `homelab-monitor`, secret `synology_dsm_password` from B's store — retrieve with `-e HOMELAB_MONITOR_REVEAL=1` on the docker exec) and call `await SynologyUPSCollector().run(ctx)`. Expect `ok=True`, ~22 metrics, zero errors, exactly 2 `homelab_synology_api_took_seconds` entries (UPS + SystemHealth co-equal fetches).
- [ ] **Verify UPS:** `homelab_synology_ups_charge_percent`~100, `_ups_runtime_seconds` a positive number (seconds), `homelab_synology_ups_status{state="online"}=1.0` while the UPS is on AC, `_ups_on_battery=0.0`/`_ups_low_battery=0.0` (derived booleans consistent with the state), `_ups_connected=1.0`. CONFIRM `homelab_synology_ups_info` labels are STRIPPED: `{model="Smart-UPS_1500", manufacture="American Power Conversion"}` — DSM appends a trailing newline that the collector must `.strip()`.
- [ ] **Verify SystemHealth (null-means-healthy inversion):** when the NAS has an active rule, `homelab_synology_health_ok=0.0` + `homelab_synology_health_rule{rule=<id>}=1.0` (e.g. `storage_is_attention`) + `homelab_synology_health_priority` (~2.2); when fully healthy, `health_ok=1.0` and NO `health_rule`/`health_priority`.
- [ ] **Multi-fetch CO-EQUAL semantic:** `ok=False` ONLY if BOTH `ups_get()` and `system_health()` fail; a single-fetch failure → `ok=True` degraded with the error recorded in `result.errors` and the other API's families still emitted.
- [ ] **Scope:** UPS load%/input-voltage/output-voltage are NOT in the DSM API (owned by STAGE-008-014's SSH `upsc` probe); UPS config fields (enable/delay_time/snmp_*) and SystemHealth hostname/uptime/interfaces are deliberately not emitted (uptime is owned by STAGE-008-007).
- [ ] **Unit:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_ups_collector.py"` passes at 100% branch coverage on `ups.py`; `test_synology_integration_bundle.py` asserts `SynologyUPSCollector` registers (name `synology_ups`, interval 60, timeout 30).

## STAGE-008-010 — Synology Backup collector (`backup.py`)

- [ ] **Backup collector empty-path (live):** Run the `SynologyBackupCollector` against the live NAS (currently 0 Hyper Backup jobs/repos). Verify it emits `homelab_synology_backup_configured_count=0`, `homelab_synology_no_backup_configured=1`, `homelab_synology_backup_repository_count=0`, both `homelab_synology_api_took_seconds` (Task + Repository endpoints), NO per-job families, and `result.ok=True` with no errors. (3b validated: ok=True, 18 metrics, empty-path assertions pass.)
- [ ] **Backup collector populated-path field-name verification (TRIGGER: when a real Hyper Backup job is configured).** The populated-job parse uses INFERRED DSM field names (centralized in the `_FIELDS` constant in `backup.py`) and `_ERROR_STATES`, because 0 jobs were configured on the NAS at build time. When a real Hyper Backup job is added, re-run the live 3b exercise and verify the per-job families actually populate (`backup_info{job,name}`, `backup_status{job,state}`, `backup_last_result{job,result}` + `backup_last_result_ok`, `backup_data_size_bytes`, `backup_enabled`, `backup_last_run_timestamp`/`_age_seconds`, `backup_next_run_timestamp`). If field names are wrong, correct the `_FIELDS` / `_ERROR_STATES` constants (a one-line-per-field edit) and update the fixtures in `tests/test_synology_backup_collector.py` to match the real payload. This item OWNS the verification of the inferred field names — it is the tracked home for that known limitation (NOT a silent deferral). **NOTE:** STAGE-008-022 owns the `BackupFailed`/`BackupMissing`/`SynologyNoBackupConfigured` alert RULES that consume these metrics — when a real job exists, that stage's rules should also be validated against the populated series.
- [ ] **Backup collector unit fixtures:** `tests/test_synology_backup_collector.py` covers empty/success/failed/zero-size/multi-job/sparse-field/string-timestamp/both-fail/single-degraded/unconfigured cases at 100% branch coverage. Re-run on any change to `backup.py`.
- [ ] **Unit:** `make uv ARGS="--directory apps/monitor pytest tests/test_synology_backup_collector.py"` passes at 100% branch coverage on `backup.py`; `test_synology_integration_bundle.py` asserts `SynologyBackupCollector` registers (name `synology_backup`, interval 300, timeout 30).

## STAGE-008-011 — Synology Snapshot & Replication collector (`replication.py`)

- [ ] **Snapshot collector empty-path (live):** Run `SynologyReplicationCollector` against the live NAS (currently 14 shares, 0 snapshots, Btrfs Replication package NOT installed). Verify it emits `homelab_synology_snapshot_count{share}=0` for every enumerated share (~14), NO `snapshot_latest_age_seconds`, `homelab_synology_snapshots_configured=0`, `homelab_synology_replication_available=0`, both `homelab_synology_api_took_seconds` (share_list + per-share snapshot calls), and `result.ok=True` with only the non-fatal `SYNO.Btrfs.Replica.Core/list: DSM error 103` recorded in errors. (3b validated: ok=True, 35 metrics, 14 share series all 0, empty-path assertions pass.)
- [ ] **Snapshot populated-path field-name verification (TRIGGER: when a real Btrfs snapshot exists on a share).** The per-snapshot parse uses INFERRED DSM field names (centralized in `_SNAP_FIELDS` in `replication.py`: `time`/`create_time`/`snapshot`/`desc`/`lock`), because 0 snapshots were configured at build time. When a snapshot is created on any share, re-run the live 3b exercise and verify `snapshot_latest_age_seconds{share}` actually populates (the `time`/`create_time` field parses) and `snapshots_configured` flips to 1. If field names are wrong, correct `_SNAP_FIELDS` (one-line-per-field) + update the fixtures in `tests/test_synology_replication_collector.py`. This item OWNS the snapshot field-name verification (NOT a silent deferral).
- [ ] **Btrfs Snapshot Replication collection (TRIGGER: when the Snapshot Replication PACKAGE is installed on the DSM and `SYNO.Btrfs.Replica.Core list` v1 returns a data payload instead of ERR:103).** Build-time recon proved the package is NOT installed: `SYNO.Btrfs.Replica.Core` exists in the API registry (maxVersion=1) but `list`/`get` both return ERR:103 (stub methods); `SYNO.SnapshotReplication.*` is absent (ERR:102). The collector currently emits only the self-correcting `homelab_synology_replication_available=0` sentinel (which AUTO-FLIPS to 1 when the probe returns a dict payload). When the package is installed: confirm the sentinel flips to 1, then ADD the per-task replication families the card specified (`homelab_synology_replication_status{task,state}`, `_last_sync_timestamp{task}`, `_lag_seconds{task}`, direction/target labels) — capture the real `Btrfs.Replica.Core list` payload, build the parse + fixtures, and update STAGE-008-022's replication alert rules to consume them. This item OWNS the deferred Btrfs replication collection (NOT a silent deferral — the read method provably did not exist at build time).
- [ ] **Replication collector unit fixtures:** `tests/test_synology_replication_collector.py` covers empty/populated-snapshots/string-time/unparseable-time/create_time-fallback/mixed-shares/replication-probe-dict-and-non-dict/share-list-fail/per-share-fail/all-fail/unconfigured at 100% branch coverage. Re-run on any change to `replication.py`.

## STAGE-008-012 — Synology Updates collector (`updates.py`)

- [ ] **Updates collector live (POPULATED path):** Run `SynologyUpdatesCollector` against the live NAS. A DSM update is currently available, so verify it emits `homelab_synology_dsm_update_available=1`, `homelab_synology_dsm_update_is_security=1`, `homelab_synology_dsm_update_info{available_version,type}` (with real labels e.g. "DSM 7.3.2-86009 Update 3"/"nano"), `homelab_synology_package_info` per installed package (~17), `homelab_synology_package_count` (~17), `homelab_synology_package_update_available{package}` per package (0/1 — the derived join), `homelab_synology_packages_with_updates_count`, and 3 `homelab_synology_api_took_seconds`. `result.ok=True`, no errors. (3b validated: ok=True, 49 metrics, update_available=1, is_security=1, 17 packages, 4 with updates [FileStation, HybridShare, QuickConnect, SurveillanceStation].) NOTE: when the DSM update is eventually installed, `dsm_update_available` will flip to 0 — that is correct, not a regression.
- [ ] **Package running/stopped status state-set (TRIGGER: if a status-bearing DSM API is confirmed).** The card asked for `package_status{package,state}` (running/stopped) but the live `SYNO.Core.Package list` payload exposes NO per-package run-state field (only id/name/version/timestamp/install_type) — so the running/stopped state-set was SCOPED OUT (not fabricated against a nonexistent field). The collector instead emits `package_info{package,name,version}` + `package_count` (package presence/disappearance is alertable). When/if a status-bearing API is confirmed via live recon (e.g. `SYNO.Core.Package status`, or an `additional=["status"]` param on the `list` call that returns run-state), add the running/stopped `package_status{package,state}` state-set + a `package_running` always-emitted alertable boolean, with fixtures. This item OWNS the scoped-out package-status dimension (NOT a silent deferral).
- [ ] **Per-package update join:** `package_update_available{package}` is a DERIVED join (installed `SYNO.Core.Package list` × non-beta `SYNO.Core.Package.Server list` catalogue, matched on `id`, string-inequality on `version`). Re-validate the join when package versions change (a package update gets installed → its `update_available` should flip to 0; a new server version appears → flip to 1). Verify beta entries are excluded.
- [ ] **Updates collector unit fixtures:** `tests/test_synology_updates_collector.py` covers update-available/no-update (absent/false/non-dict)/version-missing/type-missing, package id-skip/label-absent, the join (match-different/match-same/no-match/beta-skip/server-missing-fields), all-fail/degraded/unconfigured at 100% branch coverage. Re-run on any change to `updates.py`.

Note in items 2/3 that STAGE-008-022 owns the alert RULES consuming these metrics.

## STAGE-008-013 — Synology Security collector (`security.py`)

- [ ] **Security collector live (POPULATED path):** Run `SynologySecurityCollector` against the live NAS. The security posture is currently "risk" (a userInfo finding), so verify it emits `homelab_synology_security_status=2.0`, `homelab_synology_security_safe=0.0`, the 25-cell `homelab_synology_security_findings{category,severity}` grid (only `{userInfo,risk}=1`, rest 0), `homelab_synology_security_findings_total{severity}` (risk=1, rest 0), `homelab_synology_security_last_scan_age_seconds` (>=0) + `homelab_synology_security_last_scan_timestamp`, `homelab_synology_active_connections` (~5), and 2 `homelab_synology_api_took_seconds`. `result.ok=True`, no errors. (3b validated: ok=True, 44 metrics, status=2.0, safe=0.0, connections=5.) NOTE: when the userInfo finding is resolved (e.g. 2FA enabled on the account), `sysStatus` flips to "safe" → `security_status=0.0`, `security_safe=1.0`, all findings 0 — that is correct, not a regression.
- [ ] **`items`-is-a-DICT parse invariant (regression guard):** the security `SYNO.Core.SecurityScan.Status system_get` `items` field is a category-keyed DICT (NOT a list) — parsed via `as_dict(nested(payload,"items"))` + `nested(items_dict,cat,"fail",sev)`. If a future DSM version changes `items` to a list, the findings grid would silently go all-zero (the dict-access returns nothing). Re-validate the 25-cell grid populates correctly (the live userInfo/risk cell present) on any DSM upgrade.
- [ ] **`active_connections` emit-on-success (deliberate design, regression guard):** `active_connections` is emitted ONLY when the connection fetch succeeds (NOT a seeded 0-baseline) — 0-connections is a real state, distinct from "fetch failed". Verify on a connection-fetch failure the `active_connections` series is ABSENT (not 0). Do NOT "fix" this to a seeded baseline (documented in the module docstring + Design Notes).
- [ ] **Security collector unit fixtures:** `tests/test_synology_security_collector.py` covers status safe/warning/risk/danger/unknown-clamp/non-str, the findings grid (missing severity key, missing category), scan-time epoch-string/datetime-string/garbage/absent, items-not-dict, security-fail, connection-fail (active_connections absent), total-absent, both-fail, unconfigured at 100% branch coverage. Re-run on any change to `security.py`.

Note in items 1/2/3 that STAGE-008-022 owns the alert RULES consuming these metrics (e.g. `security_safe==0` → SecurityAdvisorNotSafe), and the per-connection "who is connected" detail is a STAGE-008-027 panel view-time re-query (NOT a metric series).

## STAGE-008-014 — Combined `homelab-probe` SSH probe

- [ ] **Combined SSH probe live run.** Run the `synology-probe` SSH probe end-to-end against the live Synology (as user `homelab-probe`, port 53197, key `ssh_probe_key_synology`). Confirm `result.ok=True`, `homelab_ssh_up{target=synology}=1`, and the emitted families include `homelab_synology_smart_attr_raw/_worst/_threshold` (8 disks × ~7 whitelisted SMART Ids), `homelab_synology_smart_attr_failing` (8 disks, all 0.0 when healthy), `homelab_synology_smart_disk_present` (8 disks, value 1.0, model labels NON-EMPTY — e.g. WD221KFGX-68B9KN0), `homelab_synology_mdstat_array_degraded` (md0/md1/md2, all 0.0 when healthy), `homelab_synology_mdstat_disks_active/_total`, `homelab_synology_ssh_cpu_temp_celsius` (~45-55°C), `homelab_synology_ssh_cpu_core_temp_celsius` (6 cores), `homelab_synology_ssh_uptime_seconds`, `homelab_synology_ssh_load1/5/15`. Validated 2026-06-26: 205 metrics, 18 families, all checks pass.
- [ ] **No UPS metrics from the probe.** Confirm the probe emits NO `homelab_synology_ups_*` (upsc scoped out of the SSH probe — DSM-API `ups.py` is the sole UPS source). The `upsc ups` capture remains in the forced-command script for future UPS models but the parser discards it.
- [ ] **uptime-synology probe removed.** Confirm the old admin-account `uptime-synology` SSH probe is NO LONGER registered (consolidated into the combined synology-probe). Only `synology-probe` (homelab-probe) and `uptime-udm` should be registered.
- [ ] **DSM sshd_config revert risk (operational caveat).** The probe's auth depends on a manual DSM host config: `/etc/ssh/sshd_config` has a global `AuthorizedKeysFile .ssh/authorized_keys .ssh/authorized_keys2 /etc/ssh/authorized_keys.d/%u` line placed BEFORE the Match blocks, and the key + forced-command script live OUTSIDE the homelab-probe home (`/etc/ssh/authorized_keys.d/homelab-probe` dir 0755/file 0644 root:root; `/usr/local/bin/hm-probe-synology.sh` root:root 0755) to bypass the inherited home-dir deny-ACL. DSM MAY revert sshd_config on an SSH-service GUI toggle or a DSM update. If the probe starts returning SshAuthError / `homelab_ssh_up{target=synology}=0`, re-apply the sshd_config directive (before the Match blocks) and `synosystemctl restart sshd`. Full procedure: project memory `reference_synology_ssh_probe_provisioning.md`.

## STAGE-008-015 — Surveillance camera collector

- [ ] **Camera collector live run.** Run the `synology_cameras` collector end-to-end against the live Surveillance Station (`ss_camera_list` + `ss_info`). Confirm `result.ok=True` and the emitted families include, for the 3 cameras (Driveway/Backyard/Doorbell): `homelab_synology_ss_camera_connected{camera}` (all 1.0 when status==1), `_status`, `_info{camera,id,model,vendor,ip,mac}` (model "Generic_ONVIF", real IPs .103/.99/.215), `_fps` (15.0), `_resolution_pixels` (Driveway 8294400 / Backyard 3686400 / Doorbell 4915200), `_resolution{resolution}`, `_recording_keep_days` (30), `_recording_keep_size_mb` (1000), `_recording_retention_mode{mode="size"}`; plus seeded rollups `homelab_synology_ss_cameras_total/_connected_total/_disconnected_total` (3/3/0) and SS.Info scalars `ss_info_camera_number` (3) / `ss_info_license_used` (3) / `ss_info_license_max` (90) / `ss_info_version{version="9.2.4-11880"}`. Validated 2026-06-26: 52 gauges, 18 families, all checks pass.
- [ ] **No events/recordings or authoritative-license metrics from this collector.** Confirm `synology_cameras` emits NO `homelab_synology_ss_event_*` / `_recording_*` (those are STAGE-008-016) and NO `homelab_synology_ss_license_*` (STAGE-008-017's authoritative SS.License breakdown). 015 emits only the `ss_info_*`-prefixed cameras-vs-license sanity rollup.
- [ ] **No `_enabled` camera metric.** Confirm there is NO `homelab_synology_ss_camera_enabled` (the card's `_enabled` was dropped — no backing DSM field; the operational proxy is `_connected` + the `_recording_retention_mode` state-set).

## STAGE-008-016 — Surveillance events & recordings collector

- [ ] **Events & recordings collector live run.** Run the `synology_events` collector end-to-end against the live Surveillance Station (`ss_event_count_by_category` + `ss_recording_list`). Confirm `result.ok=True`, errors=[], and the emitted families: per-camera `homelab_synology_ss_events_total{camera}` (3 cameras Driveway/Backyard/Doorbell, all-time counts) + system rollups `ss_events_today` (system-wide, from the LOCAL today-date key), `ss_events_total_all` (grand total), `ss_recordings_total` (top-level total), `ss_recordings_bytes_total`; per-camera `ss_recordings_count{camera}` + `ss_recordings_bytes{camera}`. Validated 2026-06-26: 22 gauges / 9 families for 2650 records in 0.74s.
- [ ] **Aggregate-only (no per-event/per-recording series).** Confirm the collector emits a BOUNDED handful of gauges (≈22), NEVER one series per event or per recording (privacy/cardinality lock). For ~2650 recordings the gauge count stays ~22, not ~2650.
- [ ] **Aggregation sums reconcile.** Confirm the per-camera `ss_recordings_count{camera}` values SUM to `ss_recordings_total`, AND the per-camera `ss_recordings_bytes{camera}` values SUM to `ss_recordings_bytes_total` (validated 2026-06-26: 804+1098+748=2650 counts; 1023192+1023920+1025248=3072360 bytes — exact matches).
- [ ] **LOCAL-timezone today-key + no collision.** Confirm `ss_events_today` reads the NAS-LOCAL (America/New_York) date key from the `date` map (NOT a UTC key — a UTC key would mis-read during the nightly UTC/ET offset window). Also confirm NO `ss_camera_*`/`ss_info_*` (STAGE-008-015) and NO `ss_license_*` (STAGE-008-017) families emitted. NOTE: the collector pins `_DISPLAY_TZ = ZoneInfo("America/New_York")` — if the host's display TZ ever changes, update that constant (a follow-up to plumb display-TZ to collectors may be carded).

## STAGE-008-017 — Surveillance license collector

- [ ] **Collector emits the 10 named license/homemode gauges + 2 api_took series against the live NAS.** Re-run the `SynologyLicenseCollector` (synology_license) against the live NAS (192.168.2.4) and verify it emits: `homelab_synology_ss_license_total`, `_used`, `_max`, `_exhausted`, `_camera_count`, `homelab_synology_ss_homemode_on`, `_notify_on`, `_schedule_on`, `_rec_schedule_on`, `_streaming_on`, plus `homelab_synology_api_took_seconds{api="SYNO.SurveillanceStation.License/Load"}` and `{api="SYNO.SurveillanceStation.HomeMode/GetInfo"}`.
- [ ] **Client API version is "1" for SS.License.** Verify `SynologyRestClient.ss_license()` calls `SYNO.SurveillanceStation.License` version "1" (NOT "2"). Version "2" returns DSM error 103 (Unknown API) on this NAS (SS 9.2.4-11880). Guard against a regression that reverts the version.
- [ ] **`ss_license_exhausted` semantics: fires only on over-subscription.** Verify `homelab_synology_ss_license_exhausted` == 0 when used == total (the NORMAL fully-licensed state, currently 3/3), and would emit 1 only if used > total. It must NOT alert on a fully-but-validly-licensed NAS.
- [ ] **Always-emit / seeded-rollup degraded behavior.** Verify that if the License fetch fails (e.g. transient DSM error) but HomeMode succeeds (or vice-versa), the collector still emits all 10 seeded gauge families (the failed side seeded at 0.0) and `ok` stays True (co-equal: ok = license_resp is not None or homemode_resp is not None). The `metrics_emitted == len(gauges)` reconciliation invariant must hold in both the clean and the degraded path.
- [ ] **Naming reconciliation with STAGE-008-015's ss_info_license_*.** Verify the authoritative `homelab_synology_ss_license_*` (from SS.License.Load: total=purchased seats, max=supported ceiling) and the cheap SS.Info-derived `homelab_synology_ss_info_license_*` (015) BOTH still emit and do not collide / contradict.
- [ ] **KNOWN CLIENT BEHAVIOR (not a 017 regression, but observed during 017 Refinement — track for the SSH/DSM-API collectors generally):** `SynologyRestClient` can return a transient DSM **error 105 (permission denied)** on a cold/stale-SID first call that resolves on immediate re-run; the client's single-flight re-auth currently triggers on error 119, not 105. This is pre-existing client behavior (NOT introduced by STAGE-008-017). If error-105 flakiness becomes a problem for the live collectors, the owning fix would be in `apps/monitor/homelab_monitor/kernel/synology/client.py` (extend the re-auth trigger to cover 105) — note for a FUTURE client-hardening stage if it ever warrants one; no current stage owns it because it has not caused a real collector failure (the next scrape recovers).

## STAGE-008-018 — NFS mount-health collector

- [ ] **Collector emits the 4 per-mount gauges against real /rackstation mounts.** Re-run `SynologyMountHealthCollector` (synology_mount_health) against the live host with `synology_mounts` configured to real `/rackstation/*` paths and verify per configured mount: `homelab_synology_mount_up{mount}`=1 (responsive), `homelab_synology_mount_probe_seconds{mount}` (small, <5s), `homelab_synology_mount_free_bytes{mount}`>0, `homelab_synology_mount_total_bytes{mount}`>0.
- [ ] **Non-hanging / missing-mount semantics.** A configured mount path NOT present in `/proc/self/mountinfo` (or a wedged hard-NFS mount that times out at the 5s probe) must emit `mount_up{mount}`=0 (seed) and NOT hang the collector. Verified live with a bogus `/rackstation/DoesNotExist` → up=0, no probe/bytes. The probe runs in a dedicated `ThreadPoolExecutor(max_workers=4)` so a wedged mount leaks at most 4 threads.
- [ ] **Compose bind exposes /rackstation via rslave.** Verify the `monitor` service volume `${HM_RACKSTATION_SRC:-/dev/null}:/rackstation:ro,rslave` in deploy/compose/docker-compose.yml. With `HM_RACKSTATION_SRC=/rackstation`, recreating the monitor container exposes all 9 NFS submounts inside the container (`docker exec homelab-monitor cat /proc/self/mountinfo | grep -c rackstation` ≥ 9). The `rslave` propagation is REQUIRED — without it only the empty parent dir is visible. The public `/dev/null` default is inert (verified: container starts cleanly, collector defaults `synology_mounts=[]` so it never probes).
- [ ] **DEPLOYMENT CONFIG (instance-A, host-override — NOT in this public repo).** For the collector to actually probe in prod, the host-override must (a) set `HM_RACKSTATION_SRC=/rackstation` permanently (so monitor recreates keep the bind), and (b) configure `synology_mounts` with the real `/rackstation/*` mount list. Without these the collector no-ops (open-source-safe empty default). The prod monitor was recreated with the bind during STAGE-008-018 Refinement; confirm the env var is persisted in the override so a future `docker compose up -d` doesn't lose it.
- [ ] **FOLLOW-UP (tracked, owning file `Makefile` `test:` target) — stale-coverage-shard self-clean.** The full coverage gate (`parallel=true`) combines ALL `.coverage.*` shards under `apps/monitor/`. A leftover shard from a targeted `--cov=<single-module>` run (done during every stage's Build loop) produces a FALSE low-coverage failure on unrelated files (hit during STAGE-008-018 Build: a phantom 99.31% with all misses in lifespan.py; the debugger reproduced a clean 100% twice). Recommended fix: add `rm -f $(MONITOR_DIR)/.coverage $(MONITOR_DIR)/.coverage.*` to the Makefile `test` target so the gate is self-cleaning. Workaround until then: clean shards before running the full gate. OUT OF SCOPE for 018 (shared Makefile change); address in a future tooling-hardening stage.
- [ ] **FOLLOW-UP (tracked, owning file `builtin/host.py`) — host collector hangs on wedged NFS.** `host.py`'s `psutil.disk_usage("/rackstation")` (its `extra_mountpoints` probe) has NO timeout and will HANG the `host` collector on a wedged hard-NFS mount (the scheduler's 5s timeout records a "timeout" failure but the blocking psutil thread leaks). NOT introduced by 018; 018 added the timeout-safe liveness probe under a separate metric. A future host-collector-hardening stage should wrap host.py's blocking psutil calls the same way 018 wraps statvfs (mountinfo presence-check → bounded dedicated-executor probe). OUT OF SCOPE for 018.

## STAGE-008-019 — Synology vmalert rules + inhibition (mount-down suppresses Plex/PodcastFeed containers)

- [ ] **Synology vmalert golden tests pass.** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 test rules /rules/__tests__/synology.tests.yaml
  ```
  Expect: `SUCCESS`. Validates the 3 rules (SynologyMountDown source; PlexContainerDown + PodcastFeedContainerDown targets) fire on the down states and don't fire when healthy, with exact rendered annotations.
- [ ] **Alertmanager config + inhibit-rule structure valid.** Run:
  ```
  make alertmanager-check
  ```
  Expect: `SUCCESS` and `1 inhibit rules` in the "Found:" summary. Validates the inhibit_rules block in `deploy/alertmanager/alertmanager.yml.template` parses and its matchers are syntactically valid. (amtool tolerates the unsubstituted `${ALERTMANAGER_INGEST_TOKEN}` placeholder as an opaque bearer string — checks the raw template directly.)
- [ ] **Inhibition mechanics: mount-down suppresses rackstation-dependent containers.** Ephemeral throwaway-AM check (no prod mutation). Render the template (`sed 's/\${ALERTMANAGER_INGEST_TOKEN}/dummy-token/g' deploy/alertmanager/alertmanager.yml.template > /tmp/am.yml`), start `prom/alertmanager:v0.27.0` on a free 127.0.0.1 port with that config, POST via the v2 API a `SynologyMountDown` alert + a `PlexContainerDown` alert (labels include `depends_on_rackstation="true"`) + an unrelated `PiholeContainerDown` alert, then query `/api/v2/alerts`. Expect: PlexContainerDown `state=suppressed` with non-empty `inhibitedBy`; SynologyMountDown `state=active`; PiholeContainerDown `state=active` (not inhibited). `docker rm -f` the throwaway after. Last validated 2026-06-26.
- [ ] **DEPLOY DEPENDENCY (not a code regression — a deploy reminder).** Real end-to-end firing in prod (real /rackstation mount down → real Plex/PodcastFeed ContainerDown actually suppressed) requires: (i) the prod monitor image rebuilt so vmalert loads `synology.yaml`; (ii) STAGE-008-032's collector config + a host `synology_mount_health.yaml` listing the real /rackstation mounts so `homelab_synology_mount_up` has real series; (iii) the plex/podcast-feed containers actually transitioning to a down state. STAGE-008-019 validated rule + inhibition MECHANICS only, NOT real-data firing (per the locked Design's honesty note).

## STAGE-008-020 — Synology syslog parse + vector template + redaction

- [ ] **Synology syslog parse + redaction unit tests pass.** Run:
  ```
  make uv ARGS="--directory apps/monitor pytest tests/test_synology_syslog_parse.py tests/test_vector_template.py tests/test_redaction_config.py tests/test_render_redaction.py"
  ```
  Expect: all pass (131 tests, 1 skipped = the vector-validate slow test when no vector binary on PATH). Validates the pure-Python reference parser (category-word bucketing: Connection→synology-auth, Storage Manager→synology-smart, Package Center→synology-package, recognized-else→synology-system, parse-fail→synology-other), the 5 synology_* redaction patterns, and the vector-template structural asserts.

- [ ] **Rendered vector template passes `vector validate`.** With a real vector binary OR a `timberio/vector:0.41.1-debian` container: render the prod template (via the monitor's `render_config()`) and run `vector validate --no-environment` on it. Expect: `Validated`, no errors. This confirms the synology_parse VRL (incl. the `to_string(parsed.*) ?? ""` coalescing — valid, same idiom as the shipping UDM VRL) compiles. The `@pytest.mark.slow` `test_rendered_template_passes_vector_validate` is the in-suite version (runs when a vector binary is on PATH). Last validated 2026-06-27 against `timberio/vector:0.41.1-debian`.

- [ ] **Real-vector ingest: DSM datagrams → service="synology-*" + redaction applied.** Ephemeral throwaway-vector check (no prod mutation). Render the template, swap the vl sink for a console sink, start a throwaway `timberio/vector:0.41.1-debian` with the synology socket source on a test port, feed the real captured DSM lines (+ synthetic Storage Manager / Package Center / malformed lines) via UDP, read the container stdout. Expect: `Connection` login → `service="synology-auth"` with `User [REDACTED]` / `from [REDACTED]` / `via [REDACTED]`; `System` heartbeat → `synology-system` with the test-source IP redacted; `Storage Manager` → `synology-smart`; `Package Center` → `synology-package`; malformed → `synology-other` + `parse_failed=1`. `docker rm -f` after. Last validated 2026-06-27 (REAL data confirmed Connection→auth + System→system + all redactions; Storage Manager/Package Center still synthetic).

- [ ] **OPEN — confirm real SMART/package category words against a live capture.** (Owned by STAGE-008-020 Refinement follow-up; consumed by STAGE-008-023.) The `Storage Manager`→synology-smart and `Package Center`→synology-package mappings are currently validated only with SYNTHETIC fixtures (the NAS was idle during capture, so only `System` and `Connection` real lines exist). When the NAS next emits real SMART/disk or package-center events (e.g. a real SMART test, a package install/update, or a disk warning), capture them on host UDP 5515 and confirm: (a) the real DSM category word is exactly `Storage Manager` / `Package Center` (or adjust the synology_parse category map + fixtures if DSM uses different words), and (b) they bucket to synology-smart / synology-package. STAGE-008-023 (vmalert-logs over service="synology-*") depends on these buckets being correct — if the real words differ, fix the map in 020's vector template + fixtures before 023's rules rely on them. The parser is category-generic with a safe `synology-system` fallback, so unconfirmed categories are never DROPPED, only possibly mis-bucketed into synology-system until confirmed.

- [ ] **PROVISIONAL — `synology_synotoken` redaction pattern is defensive, not yet confirmed against a real token line.** No token-shaped secret (SynoToken/sid/api-token) appeared in any real captured DSM line (only bracket-wrapped User/from/via identity fields, which ARE redacted by synology_user/synology_src_addr/synology_auth_method). The `synology_synotoken` pattern is RETAINED as a defensive guard (an unmatched redaction pattern costs only a tiny regex eval; a missing one risks leaking a real secret). If a future real capture shows DSM never logs a synotoken-shaped value, it may be dropped; until then it stays. (Rationale: for redaction, false-negative = leaked secret, so err toward keeping the guard.)

## STAGE-008-021 — Synology alert rules (core: storage, hardware, system groups)

- [ ] **Synology core alert-rule golden tests pass (fast).** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 test rules /rules/__tests__/synology.tests.yaml
  ```
  Expect: SUCCESS in ~2s. Validates the 16 new core rules (storage/hardware/system groups) + the 3 existing 019 rules — fire/no-fire, exact labels + annotations. (Includes the SynologyApiSlow per-api exclusion of `SYNO.Storage.CGI.Storage/*`.)

- [ ] **SynologyScrubOverdue isolated slow golden test passes.** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 test rules /rules/__tests__/synology.scruboverdue.tests.yaml
  ```
  Expect: SUCCESS in ~2s. This rule needs eval_time≥91d (its threshold is `(time()-ts)>90d`), which forces a coarse `evaluation_interval: 6h` that can't coexist with the main file's fine-grained `for:`-duration tests — hence the separate file. Validates FIRE (ts=1, genuinely old) + NO-FIRE (recent ts; AND ts=0 "never/unknown" sentinel guarded out).

- [ ] **Rule file validity.** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 check rules /rules/synology.yaml
  ```
  Expect: SUCCESS, 19 rules found (3 from 019 + 16 from 021).

- [ ] **LIVE-VALIDATED against the real NAS (2026-06-27) — findings + fixes:** the 16 rules were evaluated against the live prod VictoriaMetrics series. Confirmed the critical-rule metrics FLOW on this NAS (health_ok, volume_writable, raid_status, disk_status, disk_smart_status, ups_on_battery/low_battery all present; `smart_attr_failing` is correctly absent-until-a-SMART-attr-fails — the SynologyDiskFailed rule still has its two always-present operands). Two FALSE-FIRE bugs were found + fixed in Refinement: (a) **SynologyApiSlow** false-fired on `SYNO.Storage.CGI.Storage/load_info` (characteristically ~3.7s, benign) — fixed by dropping `max()` → per-api with a `{api!~"SYNO\\.Storage\\.CGI\\.Storage/.*"}` exclusion (now names the slow api in the alert). (b) **SynologyScrubOverdue** false-fired on `scrub_last_done_timestamp==0` (DSM "never/unknown" sentinel → `time()-0` ≈ 1.78B s) — fixed by guarding `ts > 0`. Threshold reality-check on the live NAS: disk temps 38–54°C (55°C threshold sane), CPU 1% / mem 19% (90% threshold sane), no UPS-on-battery. Two rules legitimately FIRE on the real NAS as TRUE positives (not bugs): SynologyPoolUnverifiedDisk (the pool genuinely has an unverified disk) and SynologyScrubOverdue is now correctly silent on the ts=0 pool. RE-RUN periodically: re-evaluate the rules against live VM after major DSM changes; confirm no NEW false-fires.

- [ ] **OPEN — capture the real volume_status / pool_status / scrub error-state enum strings (owned by STAGE-008-021; the degraded/crashed/scrub-error secondary rules depend on them).** Live capture (2026-06-27) showed `volume_status` ∈ {fs_almost_full, has_unverified_disk}, `pool_status` ∈ {pool_normal, has_unverified_disk}, `pool_scrub_status` = {ready} — NO `degraded`/`crashed`/scrub-error string observed (healthy NAS). The critical degraded rules therefore use unambiguous boolean gauges (`volume_writable==0`, `raid_status==0`), not state-set enum matches, and SynologyScrubError uses the `pool_disk_failure_number>0` proxy. WHEN the NAS next enters a degraded/crashed/scrub-error state (or via a controlled test), capture the real DSM enum string and, if stable, add a secondary state-set rule (`{status=~"<real-degraded>"}==1` / `{state=~"<real-scrub-error>"}==1`) alongside the boolean/proxy. The boolean/proxy rules are the always-safe primary; the state-set rule would be belt-and-suspenders. (No silent gap: the critical conditions ARE covered by the boolean gauges today.)

## STAGE-008-032 — CollectorConfig YAML-loading mechanism

- [ ] **YAML config-loading populates a CollectorConfig subclass field.** A `/config/plugins/collectors/<name>.yaml` (e.g. `synology_mount_health.yaml` with `synology_mounts: ["/rackstation/Movies"]`) must flow through `loader.register()` so the registered collector's `LoadedCollector.config` is the SUBCLASS (e.g. `SynologyMountHealthCollectorConfig`) with the field populated. Verified live: register populated `synology_mounts` from YAML. Mechanism is GENERAL (any collector with a `config_class` ClassVar).
- [ ] **Open-source-safe default: absent YAML → defaults.** With no `<name>.yaml` present (or `HOMELAB_MONITOR_COLLECTOR_OVERRIDES_DIR` pointing at an empty dir), `load_collector_overrides` returns `{}` and the collector's config uses its `Field` defaults (synology_mounts == [], host extra_mountpoints == ["/rackstation"]). Behavior IDENTICAL to pre-032 for a generic user.
- [ ] **Malformed / typo guards.** A non-dict YAML root raises ValueError; an unknown YAML key for a subclass-configured collector raises a pydantic ValidationError via `extra="forbid"` (catches operator typos). Both unit-tested.
- [ ] **The `config_class` ClassVar on the Collector Protocol.** `BaseCollector` (and the `Collector` Protocol) carry `config_class: ClassVar[type[CollectorConfig]]`. ANY new collector that conforms to the Protocol STRUCTURALLY (without subclassing BaseCollector) MUST declare `config_class` or pyright-strict fails (a freestanding test helper hit this during 032 Build). Regression: after adding a Protocol member, run the WHOLE-PACKAGE canonical pyright (not file-scoped) to catch structural conformers in other files.
- [ ] **018 real mount_up series (the validation 018 could not perform — now UNBLOCKED by 032).** With `synology_mounts` configured to real `/rackstation/*` paths, `SynologyMountHealthCollector.run()` emits REAL `homelab_synology_mount_up{mount}`=1 + `mount_free_bytes`/`mount_total_bytes` for responsive NFS mounts and =0 for an absent path. Verified live against the NAS (192.168.2.4): Movies/Documents up=1 (~52 TB volume), a bogus path down=0. This is the series `SynologyMountDown` (STAGE-008-019) consumes — 019's real-data firing is now unblocked once 019 lands + the prod image carries 032.
- [ ] **DEPLOYMENT (instance-A, host-override — NOT in this public repo).** To activate in prod: (a) a rebuilt prod monitor image carrying the 032 code; (b) `HM_RACKSTATION_SRC=/rackstation` set in `deploy/compose/.env` (the bind); (c) a real YAML at `<HOMELAB_MONITOR_OVERRIDES_DIR>/plugins/collectors/synology_mount_health.yaml` (default host dir `/var/lib/homelab-monitor/overrides`) with the real `synology_mounts` list. The running prod image predated 032 at this stage's Refinement, so the real-prod monitor emits mount_up only after the Finalize-time image rebuild + the YAML placement. Confirm these three on the next prod deploy.

## STAGE-008-022 — Synology alert rules (protect/security/updates + surveillance-integrity)

- [ ] **Protect/security/updates golden tests pass (fast).** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 test rules /rules/__tests__/synology.tests.yaml
  ```
  Expect: SUCCESS in ~2s. Validates the 6 new rules (synology_protect: SynologyBackupFailed, SynologyNoBackupConfigured; synology_updates: SynologyUpdateAvailable, SynologyDsmSecurityUpdateAvailable, SynologyPackageUpdateAvailable; synology_security: SynologySecurityAdvisorNotSafe) alongside the existing 19 — fire/no-fire, exact labels + annotations. Includes the dsm-update info/warning mutual-exclusion (`and is_security==0` vs `and is_security==1`) and the `count(package_update_available==1)>0` rollup.

- [ ] **Surveillance golden tests pass (fast).** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 test rules /rules/__tests__/synology-surveillance.tests.yaml
  ```
  Expect: SUCCESS in ~2s. Validates the 3 surveillance rules (SurveillanceCameraOffline over `ss_camera_connected==0`; SurveillanceCameraRecordingDisabled over `ss_camera_recording_retention_mode{mode="none"}==1`; SurveillanceLicenseExhausted over `ss_info_license_used >= ss_info_license_max`).

- [ ] **Rule file validity.** Run:
  ```
  docker run --rm --entrypoint promtool -v "$(pwd)/deploy/vmalert/metrics:/rules" prom/prometheus:v2.47.0 check rules /rules/synology.yaml
  ```
  Expect: SUCCESS, 25 rules found (3 from 019 + 16 from 021 + 6 from 022). And `check rules /rules/synology-surveillance.yaml` → SUCCESS, 3 rules.

- [ ] **LIVE-VALIDATED against the real NAS (2026-06-27).** Every authored rule was evaluated against real metric data (live prod VM for updates/security; fresh throwaway-container capture for backup/surveillance, since the running prod image is stale and does NOT run the surveillance/backup collectors). Result: 9/9 rules behave correctly, ZERO false positives. On the healthy NAS the rules that correctly FIRE today are SynologyNoBackupConfigured (no HyperBackup jobs — `no_backup_configured=1`), SynologyDsmSecurityUpdateAvailable (real pending DSM 7.3.2-86009 Update 3, a security update), SynologyPackageUpdateAvailable (4 packages: FileStation/HybridShare/QuickConnect/SurveillanceStation — `count(...)`=4), and SynologySecurityAdvisorNotSafe (`security_safe=0`, 1 real userInfo/risk finding). Correctly SILENT: SynologyBackupFailed (no jobs → series absent), SynologyUpdateAvailable (suppressed by the `is_security==0` guard since the update IS security), and all 3 surveillance rules (3 cameras connected, all `mode=size`, license 3/90). Mutual exclusion confirmed: exactly one of SynologyUpdateAvailable/SynologyDsmSecurityUpdateAvailable fires.

- [ ] **OPEN — REG-008-LICENSE-METRICS: the dedicated `ss_license_*` metrics are permanently 0.0 (a trap for rule authors).** The `homelab_synology_ss_license_used/max/exhausted/total` metrics are hardcoded-dead because the `homelab-monitor` DSM account lacks the SurveillanceStation `License/Load` privilege (DSM error 103 on `SYNO.SurveillanceStation.License/Load`). The trustworthy license source is the CAMERAS collector's `ss_info_license_used/max` (SS.Info), which SurveillanceLicenseExhausted is correctly written over. The dead `ss_license_*` metrics MUST NOT back any rule. **Trigger to resolve:** if the DSM account is granted SS License privilege, decide whether to delete the four dead `ss_license_*` metrics from the collector (to remove the trap). **Owner:** the next Synology-collector stage that touches the SS collector.

- [ ] **OPEN — REG-008-REPLICATION-BLOCKED: snapshot-replication alerting is blocked on DSM permissions, NOT missing config.** `homelab_synology_replication_available=0` / `snapshot_count=0` are permissions artifacts (DSM error 103 on `SYNO.Btrfs.Replica.Core/list`; the `homelab-monitor` account lacks the Btrfs/Replication Service privilege), and `snapshot_latest_age_seconds` is ABSENT. These metrics CANNOT distinguish "no replication configured" from "can't read replication." **`SynologyReplicationLag` is intentionally NOT authored** because its sole data source is unreadable (authoring it would be silently dead). **Trigger to resolve:** when the `homelab-monitor` DSM account is granted the Btrfs/Replication Service privilege, verify the collector emits `snapshot_latest_age_seconds`, then author `SynologyReplicationLag` over `snapshot_latest_age_seconds > <threshold>` (suggested 26h, to allow a daily replica + slack) with a `for: 1h` debounce. **Owner:** this regression item; promote to a stage only if replication monitoring becomes a priority. Also revisit whether `replication_available`/`snapshot_count` should be suppressed/relabeled while unreadable.

- [ ] **OPEN — REG-008-SURVEILLANCE-RECORDING-TREND: connected-but-silently-not-recording detection is deferred.** SurveillanceCameraRecordingDisabled (shipped this stage) only catches the config-disabled case (`recording_retention_mode{mode="none"}`). The valuable "camera is connected but silently failing to record" signal needs a `recordings_count` delta or a `last_recording_age_seconds` metric that events.py does not yet emit. **Trigger to resolve:** a future stage adds the recordings-trend metric to events.py, then authors a `SurveillanceCameraNotRecording` trend rule over it. **Owner:** this regression item (promote to a stage if surveillance recording-health monitoring becomes a priority).
