# STAGE-008-011: Snapshot Replication collector (graceful-empty)

## Status: Not Started

## Overview

Collect Btrfs snapshot + Snapshot Replication state: per-share snapshot count/age (`SYNO.Core.Share.Snapshot
list` — param is `name=<share>`, NOT `share_name`) and replication-task lag (`SYNO.Btrfs.Replica.Core`).
Recon found **no snapshots and no replication configured**, and `Btrfs.Replica.Core` had no working read
method among the guessed names — so this stage's Design must nail down the correct read method live (or
document that replication is absent and the collector ships graceful-empty until configured).

**Vertical slice:** after this stage `homelab_synology_snapshot_*` (per-share count/age) +
`homelab_synology_replication_*` (lag/status, when present) are emitted, degrading cleanly to empty for the
current no-snapshot/no-replication state.

**Spec references:** §6.2. EPIC-008 "Verified deployment reality" (Share.Snapshot empty, `name=` param;
Btrfs.Replica.Core read method TBD).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [x] Yes — reads live snapshot/replication state. Refinement is 3a (fixtures
  incl. empty + a synthetic populated case) + 3b (live — currently empty; confirm `Btrfs.Replica.Core` read
  method or document absence). NO user prompt.

## Cross-cutting standards (from EPIC-008 cross-stage acceptance criteria)

- `synology` concurrency group; self-metrics + API latency; 5m.
- Graceful-empty: no snapshots/replication → empty emission, NOT a collector failure.
- Design MUST resolve the `Btrfs.Replica.Core` read-method question live before building (don't guess in
  code); if no replication exists to validate against, document the method choice + ship behind the
  graceful-empty path with a regression note to validate when replication is configured.
- All timestamps UTC.

## Concrete deliverables

### 1. Snapshot + replication collector

`replication.py`:
- Snapshots: iterate configured shares, call `client.share_snapshot_list(name=<share>)`, emit
  `homelab_synology_snapshot_count{share}`, `homelab_synology_snapshot_latest_age_seconds{share}` (UTC).
- Replication: call the confirmed `Btrfs.Replica.Core` read method, emit
  `homelab_synology_replication_status{task,state}`, `_last_sync_timestamp{task}`,
  `_lag_seconds{task}`, direction/target labels. Graceful-empty when no tasks.

### 2. Tests

3a fixtures: empty (live state), synthetic populated snapshots, synthetic replication task with lag. 3b live
(empty path + the confirmed read-method shape).

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — collector unit-tested against empty + synthetic-populated snapshot/replication fixtures
- [ ] 3b — real emission against the live NAS (empty path; `Btrfs.Replica.Core` read method confirmed or
  absence documented)
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation: [pending]
- Changelog: [pending]
- Tracking: [pending]

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
