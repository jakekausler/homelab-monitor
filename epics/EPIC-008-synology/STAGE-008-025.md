# STAGE-008-025: Synology page shell + sidebar registration + header status strip

## Status: Complete

## Overview

Build the Integrations → Synology page scaffold: register the sidebar nav entry (alphabetical among
Integrations entries) + the route, the page shell, and the header status strip (DSM up/down · SystemHealth
rule · volume usage · UPS). Establishes the page that 008-026/027 fill with widgets. Mirrors the
per-integration-panel pattern (HA 005-022, Unifi 007-020). FRONTEND — Desktop + Mobile viewport sign-off.

**Vertical slice:** after this stage the Synology entry appears in the sidebar (alphabetically), routes to a
page with a working header status strip fed by the 008-024 summary endpoint.

**Spec references:** §9.1/§9.2 (nav; Integrations panel). EPIC-005 STAGE-005-022 + EPIC-007 STAGE-007-020
(panel shell + sidebar/router registration — the precedent). EPIC-008 Amendment 7 (two pages; alphabetical).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [x] Yes (consumes the live summary endpoint). **FRONTEND** — Refinement
  requires the user's Desktop + Mobile viewport sign-off against the prod UI (rebuild dist + sha256-prove +
  test at the prod URL).

## Cross-cutting standards (from EPIC-008 cross-stage acceptance criteria)

- Sidebar entry placed ALPHABETICALLY among Integrations entries (…Pi-hole · Surveillance · Synology · …).
- Observe-only — no write controls anywhere on the page.
- Consumes the 008-024 typed summary endpoint (no inline data shapes).
- Header strip: DSM up/down · SystemHealth `rule` · volume usage% · UPS status.

## Concrete deliverables

### 1. Sidebar + route registration

Add the "Synology" `NAV_ITEMS` entry (alphabetical) + `/integrations/synology` route + page scaffold (mirror
the verified registration pattern).

### 2. Header status strip

A status strip consuming `GET /api/integrations/synology/summary`: DSM up/down, SystemHealth rule, volume
usage, UPS status. Graceful loading/empty states.

### 3. Tests + viewport sign-off

Component/vitest tests; Refinement Desktop + Mobile sign-off on the prod UI.

## Design Phase

**Status**: [x] Design (Complete)

**Design Notes**:

Selected approach (coordinator-selected per pre-authorization; brainstormer consulted for the three real UX/architecture choices):

**Mirror precedent:** Synology page shell mirrors the Unifi integration page exactly — `UnifiLayout.tsx` (parent layout: header h1+description → status strip → tab `<nav>` of `<Link>`s → `<Outlet/>`), the `router.tsx` parent-layout-route + index-redirect + child-tab-route pattern, the `useUnifiSummary` react-query hook (`apiClient.GET` + `unwrap` + 30s `refetchInterval`), and the `<Badge>` variants (ok/warn/critical/muted). Sidebar entry added in `SidebarNav.tsx` ALPHABETICALLY between "Pi-hole" and "Unifi" (label "Synology", route `/integrations/synology`).

**Component/file map (named exports, mirroring Unifi):**
- `apps/ui/src/components/SidebarNav.tsx` — add the "Synology" NAV_ITEMS entry (between Pi-hole and Unifi).
- `apps/ui/src/router.tsx` — add `synologyLayoutRoute` (`/integrations/synology` → SynologyLayout), `synologyIndexRoute` (index → redirect `/integrations/synology/overview`), and four child tab routes (overview/hardware/ops/metrics), registered via `addChildren`.
- `apps/ui/src/api/synology.ts` (NEW) — `useSynologySummary()` react-query hook hitting `/api/integrations/synology/summary` (typed `Schema<'SynologySummary'>`), 30s refetch, + `synologyQueryKeys`.
- `apps/ui/src/routes/integrations/SynologyLayout.tsx` (NEW) — layout + inline status strip + TABS nav + Outlet.
- `apps/ui/src/routes/integrations/SynologyOverviewTab.tsx` (NEW) — the 025-filled tab (intro + empty-state note).
- `apps/ui/src/routes/integrations/SynologyHardwareTab.tsx` (NEW) — EmptyState placeholder citing STAGE-008-026.
- `apps/ui/src/routes/integrations/SynologyOpsTab.tsx` (NEW) — EmptyState placeholder citing STAGE-008-027.
- `apps/ui/src/routes/integrations/SynologyMetricsTab.tsx` (NEW) — EmptyState placeholder citing STAGE-008-030.
- `*.test.tsx` vitest files mirroring `UnifiLayout.test.tsx` (mock the summary hook + mock router Link/Outlet).
- Reuse existing: `components/EmptyState.tsx`, `lib/relativeTime.ts` (`formatRelative`), `components/ui/badge.tsx`.

**Choice 1 — System-health badge (resolves REG-008-024-DSM-UP-FLAG):** the header strip renders `dsm_up` as ONE badge — `System health: OK` (variant `ok`/green) when true, `System health: Degraded` (variant `warn`/amber) when false. NEVER "DSM down" language, NEVER red — the DSM REST API is reachable (other endpoints query it live); `dsm_up` is the `homelab_synology_health_ok` SystemHealth flag (currently false in prod for a real standing security-finding + unverified-disk reason), so red/"offline" would mislead.

**Choice 2 — Full 4-tab scaffold now:** tabs Overview / Hardware / Ops / Metrics shipped in 025; index route redirects to Overview. Overview is the only 025-filled tab (short intro + empty-state note). Hardware/Ops/Metrics ship as `EmptyState` placeholders THIS stage (in-scope, not silent deferral), each naming its future owner: Hardware→STAGE-008-026, Ops→STAGE-008-027, Metrics→STAGE-008-030. Later stages add tab CONTENT only (zero routing rework).

**Choice 3 — Header strip = 4 chips:** (1) System health (per Choice 1); (2) Volume — `Volume: NN%` with thresholds <80 `ok` / 80–90 `warn` / >90 `critical`, `null`→`Volume: —` `muted`; (3) UPS — composite of `ups_on_battery`+`ups_charge_percent`: on-battery → `UPS: on battery (NN%)` (`warn`, or `critical` if charge<20), else `UPS: online (NN%)` (`ok`), both unknown → `muted`, charge `null` drops the `(NN%)`; (4) Last seen — `formatRelative(last_seen)` rendered as `Updated <relative>` (`muted`; freshness, NOT a synthetic reachability signal). Loading/502/error branches mirror PiholeStatusStrip (`isPending`→"Loading…", `error.status===502`→amber "metrics temporarily unavailable" banner, other `isError`→`<ErrorDisplay>`).

**Backend contract (verified LIVE against prod):** `GET /api/integrations/synology/summary` → `SynologySummary` (8 fields, all present in `apps/ui/src/api/schema.ts`): `dsm_up`, `volume_used_percent_max`, `ups_on_battery`, `ups_charge_percent`, `update_available`, `security_safe`, `backup_configured`, `last_seen`. Live prod values: dsm_up=false, volume max ≈80.9, ups_on_battery=false, ups_charge_percent=100, update_available=true, security_safe=false, backup_configured=false. No contract mismatch.

**Card-vs-contract deltas recorded:** (a) The card lists header fields "DSM up/down · SystemHealth rule" as if two signals — the contract has ONE field (`dsm_up`=`health_ok`=SystemHealth); 025 renders them as one "System health" badge (REG-008-024-DSM-UP-FLAG). (b) Card named only "volume usage% · UPS status" for the strip — design adds System-health and Last-seen chips (freshness, mirroring Unifi).

**Scope-outs / deferrals (all tracked, NO silent drop):** summary fields `update_available`, `security_safe`, `backup_configured` are NOT in 025's strip — all three are consumed by STAGE-008-027 (backup/updates/security widgets). The Hardware/Ops/Metrics tab BODIES are owned by STAGE-008-026 / -027 / -030 respectively (025 ships named EmptyState placeholders for them). All 8 summary fields accounted for; nothing dropped.

**Refinement plan:** FRONTEND — rebuild `apps/ui/dist` + rebuild prod monitor image `--no-cache` + sha256-prove served==built, then prompt the user for Desktop + Mobile viewport sign-off at http://192.168.2.148:29090.

**Refinement design change (user-directed, 2026-06-28):** The Overview and Metrics tabs were REMOVED from the Synology page during Refinement. Final scaffold = TWO tabs: **Hardware** (→STAGE-008-026) and **Ops** (→STAGE-008-027); the index route `/integrations/synology` now redirects to **Hardware**. The header status strip (System health / Volume / UPS / Last seen) is unchanged and remains always-visible above the tabs. Rationale: Overview was a content-free intro/pointer (pointless); the Synology Grafana dashboard does NOT belong on a Synology-page Metrics tab — it belongs on the global Metrics page as a new dashboard tab. Files deleted: `SynologyOverviewTab.tsx`(+test), `SynologyMetricsTab.tsx`(+test). Vitest dropped from 1841→1839 tests accordingly; gate re-verified green.

**STAGE-008-030 scope correction (NO silent drop):** STAGE-008-030's Synology Grafana dashboard embed moves OFF the Synology integration page and ONTO the existing global Metrics page as a NEW dashboard tab (alongside the other Grafana dashboards). See the updated STAGE-008-030 card.

## Build Phase

**Status**: [x] Build (Complete)

**Build Notes**:

Implemented per spec `/tmp/spec-2026-06-28-09-26-47.md` (planner Opus → scribe). 13 frontend files (11 new, 2 modified), no backend changes.

**New files:**
- `apps/ui/src/api/synology.ts` — `useSynologySummary()` react-query hook (`apiClient.GET('/api/integrations/synology/summary')`, `unwrap`, 30s `refetchInterval`) + `synologyQueryKeys`; mirrors `api/unifi.ts`.
- `apps/ui/src/routes/integrations/SynologyLayout.tsx` — layout + inline `SynologyStatusStrip()` (4 chips: System health ok/warn never-red, Volume 80/90 thresholds, composite UPS, Updated/last-seen via `formatRelative`), loading/502/error branches mirroring `PiholeStatusStrip`, 4-tab nav + `<Outlet/>`.
- `apps/ui/src/routes/integrations/SynologyOverviewTab.tsx` — 025-filled index tab (intro + EmptyState).
- `apps/ui/src/routes/integrations/SynologyHardwareTab.tsx` / `SynologyOpsTab.tsx` / `SynologyMetricsTab.tsx` — EmptyState placeholders citing STAGE-008-026 / -027 / -030 (with SCAFFOLDING comments).
- 5 vitest files (`SynologyLayout.test.tsx` + one per tab) mirroring `UnifiLayout.test.tsx`.

**Modified files:**
- `apps/ui/src/components/SidebarNav.tsx` — added `HardDrive` lucide import + "Synology" NAV entry alphabetically between Pi-hole and Unifi.
- `apps/ui/src/router.tsx` — 5 component imports + `synologyLayoutRoute` (`/integrations/synology`) + index redirect to `/overview` + 4 child tab routes, registered via addChildren.

**Build-time fix:** the planner's UPS chip used an IIFE-in-JSX, which violates the project eslint rule `@eslint-react/unsupported-syntax`. Refactored (fixer) to compute the UPS chip into a const above the JSX (`const ups = result.data ? upsChip(...) : null`) and render `{ups && <Badge>}` — same output, no IIFE. Prettier --write applied to 3 files (pure formatter reflow, no logic change).

**Verification — BOTH gates GREEN:**
- Frontend (`make ui-verify`): eslint 0 errors (98 pre-existing warnings, acceptable), prettier clean, tsc strict clean, vitest 1841 tests passing (incl. all 5 new Synology test files), vite build OK.
- Backend (`pytest -m 'not integration' --cov-branch`, over unchanged Python): 6667 passed, 1 skipped (baseline `test_vector_template` — vector not on PATH), Total coverage 100.00%.

## Refinement Phase

- [x] 3 — FRONTEND: rebuild dist + sha256-prove served==built; user Desktop + Mobile viewport sign-off at
  the prod URL
- [x] Regression Items Added

**Status**: [x] Refinement (Complete)

**Refinement Notes**:

Rebuilt `apps/ui/dist` + rebuilt prod monitor image `--no-cache` + sha256-proved served==built (twice — once for the initial 4-tab shell, then again after the user-directed Overview/Metrics tab removal). Final served bundle sha256 `6d7ef7029f77b829e29ae19a29fdf4ca299fc420f965e9d6d59e3be4667335c8` (host == container MATCH); container healthy, HTTP 200; served bundle contains the `hardware`/`ops` synology routes, overview/metrics ABSENT.

**User-directed change during Refinement:** removed the Overview tab (content-free) and the Metrics tab (the Synology Grafana dashboard belongs on the global Metrics page, not a Synology-page tab). Final page = TWO tabs (Hardware→026, Ops→027), index redirects to Hardware, header status strip unchanged. Frontend gate re-verified green after the change (eslint 0 errors, tsc strict, vitest 1839 tests, build OK). STAGE-008-030 scope corrected accordingly (see its card).

**Viewport sign-off:** user approved BOTH Desktop AND Mobile against the final (2-tab) code state on the prod UI at http://192.168.2.148:29090 (2026-06-28).

## Finalize Phase

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests) — N/A: first review had ZERO findings (all PREMISE-CHECK confirmations, no code changed after review), so no second review was triggered (per workflow: zero-finding review → no 2nd review)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: `4428323`
- Changelog: `2ff7a41`
- Tracking: [pending]

**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize (Complete)

**Finalize Notes**:

code-reviewer (Opus) pre-test review: APPROVED with ZERO findings — all Minor items were PREMISE-CHECK confirmations (volume thresholds, UPS composite, never-red system-health, TS-strict cleanliness, clean tab deletion all verified correct, no change needed). No implementation/test files changed after the review, so no second code review was required (documented). Frontend gate green at commit time: eslint 0 errors (98 pre-existing warnings), prettier clean, tsc strict clean, vitest 1839 tests passing, vite build OK. Backend Python unchanged this stage (frontend-only) — Build's 100%-branch pytest gate (6667 passed) remains valid; openapi/ui-types pre-commit hooks correctly skipped (no kernel/api changes). 4/5-commit flow: impl `4428323`, changelog-hash `2ff7a41`, tracking `[pending]`.
