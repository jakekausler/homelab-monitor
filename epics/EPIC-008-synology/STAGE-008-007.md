# STAGE-008-007: System info + temps collector

## Status: Not Started

## Overview

Collect DSM system identity + thermal + reboot signals: `SYNO.Core.System` v3 `info` (model/serial/firmware/
uptime/`sys_temp`/fan status), plus `SYNO.Core.Hardware.NeedReboot` (a pending-reboot bool). Fan speed on
this box is status-strings-only (no numeric RPM — recon), so we emit the fan status enum, not RPM.

**Vertical slice:** after this stage `homelab_synology_system_*` (uptime, `sys_temp`, temp-warning flag,
firmware/model as labels) + `homelab_synology_need_reboot` are emitted for the live NAS.

**Spec references:** §6.2. EPIC-008 "Verified deployment reality" (`Core.System` v3 shape; `sys_temp=50`).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [x] Yes — reads live DSM system data. Refinement is 3a (fixture) + 3b (live
  emission). NO user prompt.

## Cross-cutting standards (from EPIC-008 cross-stage acceptance criteria)

- `synology` concurrency group; self-metrics + API latency.
- 60s cadence (temps/uptime are live-ish); `ok=True` on warn states.
- Model/serial/firmware as low-cardinality labels (not free strings in unbounded series).

## Concrete deliverables

### 1. System collector

`system.py`: calls `client.system_info()` (v3) + `client.need_reboot()` + (optionally)
`client.hardware_fanspeed()`, emits:
- `homelab_synology_system_uptime_seconds` (parse `up_time` HH:MM:SS), `homelab_synology_sys_temp_celsius`,
  `homelab_synology_sys_temp_warning` (1/0 from `sys_tempwarn`/`temperature_warning`),
  `homelab_synology_info{model,serial,firmware,cpu_series}` (gauge=1, identity labels).
- `homelab_synology_fan_status{state}` (from FanSpeed status strings; `all_disk_temp_fail`, `cool_fan`).
- `homelab_synology_need_reboot` (1/0).

### 2. Tests

3a fixture (recon `Core.System` v3 + FanSpeed + NeedReboot shapes) covering uptime-parse + temp + warn-flag +
reboot bool; 3b live emission.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — collector unit-tested against `Core.System` v3 + FanSpeed + NeedReboot fixtures
- [ ] 3b — real system/temp/reboot metrics emitted against the live NAS
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation: [pending]
- Changelog: [pending]
- Tracking: [pending]

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
