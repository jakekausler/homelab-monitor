# STAGE-008-031: Grafana `synology-surveillance.json` dashboard + Metrics-tab embed + readability pass

## Status: Not Started

## Overview

Author the `deploy/grafana/dashboards/synology-surveillance.json` dashboard and embed it as a Metrics tab on
the Surveillance page. Rows: cameras (online/recording status), events/recordings (per-camera activity,
events/day, recording footprint), license/HomeMode. The `.json` is provisioned-as-code; the Metrics-tab
embed is a `.tsx` change (FRONTEND — rebuild + sha256-prove + viewport sign-off). Readability pass. This is
the LAST stage of EPIC-008 — its tracking commit also closes the epic.

**Vertical slice:** after this stage `synology-surveillance.json` renders the surveillance metric surface and
is reachable as a Metrics tab; EPIC-008 is complete.

**Spec references:** §9.2. EPIC-005 STAGE-005-026/030 + EPIC-007 STAGE-007-025 (second dashboard + embed +
readability — the precedent). EPIC-008 Amendment 7.

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [x] Yes (renders live SS metrics). **MIXED:** the JSON is page-refresh-only;
  the Metrics-tab embed is a `.tsx` change → FRONTEND, rebuild + sha256-prove + Desktop + Mobile viewport
  sign-off.

## Cross-cutting standards (from EPIC-008 cross-stage acceptance criteria)

- Aggregate metrics; provisioned-as-code.
- Readability pass: every surveillance metric surfaced; glance-first.
- **Last stage of the epic** — the tracking commit advances EPIC-008 `## Status:` to the completed form
  (`Complete (31/31 stages — STAGE-008-001..031 Complete; EPIC-008 closed YYYY-MM-DD)`) and confirms ALL
  wave rows Complete.

## Concrete deliverables

### 1. `synology-surveillance.json` dashboard

Rows: cameras (online/recording status grid), events/recordings (per-camera events/day, recording
footprint), license/HomeMode.

### 2. Metrics-tab embed

Wire `synology-surveillance.json` as a Surveillance Metrics tab (`.tsx`).

### 3. Readability pass + sign-off + epic close

Iterate panels against live data; rebuild + sha256-prove; Desktop + Mobile viewport sign-off; mark EPIC-008
complete in the tracking commit.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3 — Grafana JSON readability iterated against live data; Metrics-tab embed rebuilt + sha256-proven +
  user Desktop + Mobile viewport sign-off
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only
- [ ] EPIC-008 marked Complete (last stage)

**Commit Hashes**:
- Implementation: [pending]
- Changelog: [pending]
- Tracking: [pending]

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
