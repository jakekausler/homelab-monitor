# STAGE-008-016: Surveillance events & recordings collector

## Status: Not Started

## Overview

Collect Surveillance Station activity: per-camera event/motion counts (`SYNO.SurveillanceStation.Event
CountByCategory` — recon: per-camera + per-day breakdowns, e.g. Driveway 745 / Backyard 804 / Doorbell 1097)
and recording totals/size (`SYNO.SurveillanceStation.Recording List` — 2645 clips). Kept as AGGREGATE
counters only (never per-event series). This feeds the "camera online but not recording / went silent"
integrity signal (STAGE-008-022).

**Vertical slice:** after this stage `homelab_synology_ss_events_total{camera}` + `_events_today` +
`homelab_synology_ss_recordings_total` + `_recordings_bytes{camera}` are emitted for the live SS at 5m.

**Spec references:** §6.2. EPIC-008 "Verified deployment reality" (Event.CountByCategory per-camera/per-day;
Recording.List total/size). EPIC-008 cardinality decision (SS events as aggregate counters only).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No
- Host-integration concerns: [x] Yes — reads live SS activity. Refinement is 3a (fixtures) + 3b (live
  per-camera counts). NO user prompt.

## Cross-cutting standards (from EPIC-008 cross-stage acceptance criteria)

- `synology` concurrency group; self-metrics + API latency; 5m.
- **Aggregate counters only** — per-camera + events-today + totals; NEVER per-event series (cardinality).
- All timestamps UTC.

## Concrete deliverables

### 1. Events & recordings collector

`ss_events.py`: calls `client.ss_event_count_by_category()` + `client.ss_recording_list()`, emits:
- `homelab_synology_ss_events_total{camera}` (per-camera cumulative), `homelab_synology_ss_events_today`
  (and per-camera-today if cleanly available), `_events_per_day` rollup.
- `homelab_synology_ss_recordings_total`, `homelab_synology_ss_recordings_bytes{camera}` (per-camera
  recording footprint).

### 2. Tests

3a fixtures: `Event.CountByCategory` (per-camera + per-day) and `Recording.List` (total + per-camera sizes),
plus a "camera dropped to 0 events" synthetic case; 3b live emission.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — collector unit-tested against Event.CountByCategory + Recording.List fixtures (+ zero-events case)
- [ ] 3b — real per-camera event/recording counters emitted against the live SS
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation: [pending]
- Changelog: [pending]
- Tracking: [pending]

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
