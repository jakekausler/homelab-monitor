# STAGE-006-020: Backend panel data endpoint(s)

## Status: Not Started

## Overview

The backend that powers the Integrations → Pi-hole panel: typed FastAPI endpoint(s) aggregating the panel's
read data (status strip, blocking state, gravity/adlists, FTL messages, upstreams+unbound, clients,
recent-blocked, versions, privacy level) — some served from stored metrics, some live-queried from the
Pi-hole API at view-time (per the Tier-2 live-query decision). Mirrors EPIC-005 STAGE-005-021 (HA panel
data endpoint).

**Vertical slice:** `GET /api/integrations/pihole/*` returns the panel's data (typed, OpenAPI-described);
the frontend (006-021+) can render against it; live-query fields (top clients, recent blocked, messages
text) reflect current Pi-hole state.

**Spec references:** spec §9.2 (Integrations = plugin-provided panel), §3.1 (API layer typed routes +
OpenAPI). EPIC-006 "Operator panel". EPIC-005 STAGE-005-021 (panel data endpoint precedent).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [x] Yes — RO session for live-query fields; returns per-client data
  (Tier 2 — the user's own devices; live-queried, not newly stored).
- Host-integration concerns: [x] Yes — live-query fields hit the real Pi-hole API. Refinement 3a (endpoint
  tested with mock client + stored-metric fixtures) AND 3b (real panel data on prod rig).

## Cross-cutting standards

- Typed routes; OpenAPI schema regenerated; `require_user_or_token` (read scope) on the read endpoints.
- Live-query fields use the RO session; respect the refresh cadence (the frontend drives refetch; the
  backend doesn't poll on its own beyond serving requests).
- Privacy-level field included so the frontend can conditionally show the privacy banner.

### Cross-stage deliverable notes (from upstream stages)

- **(from STAGE-006-011 Refinement)** The `pihole_version` collector (and the other slow-cadence pihole collectors: e.g. `pihole_gravity` 30s is fine, but `pihole_version` at 3600s) does NOT run on scheduler startup — its metrics are absent for up to ~1h after a monitor restart, so the version widget will show no version data until the first scheduled run. The sibling update-checkers (`image_update_checker`, `local_build_update_checker`) run on startup via explicit `await_immediate_run()` blocks in `kernel/api/lifespan.py` (a ~15-line per-collector block; there is NO one-line ClassVar opt-in). When building the version widget / panel-data endpoint, EVALUATE whether to add a startup-run hook for `pihole_version` (so the widget has data promptly after a restart) — either by adding the lifespan block for `pihole_version`, or by refactoring to a `run_on_startup: ClassVar[bool]` opt-in on BaseCollector (the cleaner cross-cutting fix). This is a startup-latency UX concern, not a correctness bug (the collector works correctly when it runs).

## Concrete deliverables

### 1. Panel data endpoint(s)

`GET /api/integrations/pihole/overview` (status strip + blocking + versions + gravity summary + messages
count + privacy level), and the live-query sub-endpoints the widgets need: `.../clients` (top talkers,
live), `.../recent-blocked` (live feed), `.../messages` (full text, live), `.../adlists` (per-adlist health),
`.../upstreams` + `.../unbound` (resolver card). Design decides the exact split (one fat endpoint vs several
focused ones — EPIC-005 used focused ones for drill-downs).

### 2. Data sourcing

Aggregate-from-stored-metrics where history matters (block %, query volume), live-query the Pi-hole API
where "now" matters (top clients, recent blocked, messages text, blocking timer). Document which field
comes from where.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — endpoints tested with mock Pi-hole client + stored-metric fixtures; typed shape + OpenAPI correct
- [ ] 3b — real panel data on prod rig; live-query fields reflect current state
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation:
- Changelog:
- Tracking:

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
