# STAGE-006-022: Blocking control + gravity/adlist + FTL-messages widgets (+ write buttons + privacy banner)

## Status: Complete

## Overview

The first set of panel widgets, including the first WRITE controls in the UI. Blocking control (state +
timer + enable/disable buttons calling the 006-018 write endpoint with confirm), gravity/blocklist widget
(count, last-update age, per-adlist health table + "Update gravity now" button), FTL diagnostic-messages
widget (live message list), and the conditional privacy banner (shown only when privacy level is elevated).

**Vertical slice:** the panel shows live blocking state with working enable/disable buttons (confirm-gated,
audited), the per-adlist table (the 2 live failing adlists shown by name) with an "Update gravity now"
button, the live FTL-messages list, and — only if privacy is up — the privacy banner.

**Spec references:** EPIC-006 "Operator panel" (widgets 1-3 + privacy banner). Consumes 006-018 (write
endpoints), 006-020 (panel data), 006-007 (adlists), 006-010 (messages). Q9 = write-capable + privacy
banner only when elevated. Confirm-on-destructive per spec §9.3.

## Stage Flags

- Has Input Forms: [x] Yes — the blocking enable/disable + gravity-update controls (write actions).
- Touches secrets / production data: [x] Yes — triggers the 006-018 write actions (mutate live Pi-hole).
- Host-integration concerns: [ ] No new backend; consumes validated endpoints. FRONTEND stage: Desktop +
  Mobile viewport approval. (The write actions themselves were 3b-validated in 006-018.)

## Cross-cutting standards

- Write buttons use confirm-on-destructive (typed phrase / session PIN per spec §9.3) and surface the audit
  outcome; reflect the new state live after the action.
- Privacy banner renders ONLY when privacy level elevated (conditional; hidden otherwise) — decided.
- FRONTEND stage: Desktop + Mobile testing + explicit approval; any code change resets both viewports.
- Per-adlist table reuses the app's table conventions; failing adlists visually distinct.

## Concrete deliverables

### 1. Blocking control widget

State + live countdown + enable/disable buttons → `POST /api/integrations/pihole/blocking` (confirm +
audit). Optimistic/refetch update so the new state shows immediately.

### 2. Gravity / blocklists widget

Domain count + last-update age + per-adlist health table (name/domains/enabled/status; the live 2 failing
adlists shown by name) + "Update gravity now" button → `POST /api/integrations/pihole/gravity/update`
(confirm + audit).

### 3. FTL diagnostic-messages widget

Live list from `.../messages` (text + type) — the "Pi-hole is unhappy" panel.

### 4. Privacy banner

Conditional: if privacy level elevated, show "query logging restricted — data may be incomplete"; else
render nothing.

## Design Phase

### Design Notes

**Selected approach (recon-verified against live source + brainstormer-assisted; coordinator selected).** Mirror the EPIC-007 docker write pattern + the `PullRestartModal` typed-phrase confirm precedent; self-fetching widgets like the HA/Unifi panels. Replaces the three 021 Overview-tab placeholder `<section>`s (Blocking control / Gravity & adlists / Messages) with real widgets + adds a conditional privacy banner. Pure frontend consuming the LIVE 018 write + 020 read endpoints (no backend change).

**Decision 1 — Confirm-on-write UX: extract a generic `ConfirmPhraseDialog` (Option A).** Factor the proven `components/docker/PullRestartModal.tsx` typed-phrase logic (input disabled until `input.trim().toLowerCase() === expectedPhrase.toLowerCase()`; expected phrase shown in label; 400-error surfaces the backend message) into `apps/ui/src/components/ConfirmPhraseDialog.tsx`, reused by all 3 write actions. Do NOT retrofit docker this stage (deferred as non-blocking tech-debt; a comment notes docker could adopt it later). Honors spec §9.3 typed-phrase confirm-on-destructive. Exact phrases (verified in `integrations_pihole.py`): blocking enable→`"enable"`, disable→`"disable"`, gravity→`"update"` (backend compares `.strip().lower()`).

**Decision 2 — Blocking control: refetch-on-success (Option A).** On mutation success, `invalidateQueries(piholeQueryKeys.overview)`; state (blocking_enabled + timer + live countdown via the EXISTING `useReenableCountdown` hook) re-derives from the refetched `/overview` (single source of truth; LAN refetch is sub-second). Timer collected via a PRESET dropdown (`30s / 5m / 1h / Indefinite`; "Indefinite" omits `timer` from the request). Contextual single primary action: Disable (+ timer preset) when enabled; Enable + live countdown when disabled. No optimistic-update code (YAGNI; minimal React-purity surface).

**Decision 3 — Gravity widget: pending button + result Dialog for `log_tail` (Option A).** The "Update gravity now" → `POST /gravity/update` is SLOW (streaming gravity rebuild, up to ~120s) → button shows a pending/disabled spinner via the mutation's `isPending`; on success, a result Dialog shows the `log_tail` lines (multi-line — a Dialog reads better than a toast; the action is also server-audited). Invalidate `/adlists` + `/overview` on success (gravity_domains changes). The status→badge mapping is a PURE exported function `piholeStatus.ts` (`adlistStatusToBadgeVariant`): `ok`→`ok`; contains `fail`/`error` (download_failed/parse_failed)→`critical`; empty/`unknown`/null→`muted`; else→`warn`. Failing rows visually distinct (badge + subtle row treatment). Responsive: real `<table>` on `sm+`, stacked cards on mobile.

**Decision 4 — Self-fetching widgets; privacy banner in the tab (Option A).** Each widget owns its read hook (Blocking→`usePiholeOverview`, Gravity→`useAdlists`, Messages→`useMessages`) — mirrors HA/Unifi; React Query dedupes the double `usePiholeOverview` (the banner also reads it). The privacy banner lives in `PiholeOverviewTab` ABOVE the sections, `warn` styling, shown ONLY when `privacy_level > 0` (else renders nothing; Pi-hole levels 0=show all … 3=anonymous).

**Files to CREATE:** `apps/ui/src/components/ConfirmPhraseDialog.tsx` + test; `apps/ui/src/routes/integrations/PiholeBlockingWidget.tsx` + test; `PiholeGravityWidget.tsx` + test; `PiholeMessagesWidget.tsx` + test; `piholeStatus.ts` (pure status→badge fn) + test.
**Files to MODIFY:** `apps/ui/src/api/pihole.ts` (add `useAdlists`, `useMessages`, `useBlockingMutation`, `useGravityUpdateMutation` + extend `piholeQueryKeys` with `adlists`/`messages`; mutations mirror docker's `useStartPullAndRestart` — `unwrap(await apiClient.POST(...))` + `invalidateQueries`; blocking→invalidate overview, gravity→invalidate adlists+overview; CSRF auto-attached by `csrfMiddleware`); `apps/ui/src/routes/integrations/PiholeOverviewTab.tsx` (replace the 3 placeholder sections' contents with the widgets + add the conditional privacy banner).

**Backend contract:** the 018 write endpoints (`POST /blocking`, `POST /gravity/update`) + 020 reads (`/adlists`, `/messages`) are LIVE + prod-validated; all request/response + read models are already in `apps/ui/src/api/schema.ts`. No backend/openapi change → openapi-export/generate-ui-types hooks NO-OP.

**Lint reminder (cost 3 attempts on 021):** `apps/ui` enforces React-Compiler purity (no set-state-in-effect / refs-during-render / Date.now()-during-render) as eslint ERRORS — run the frontend fast pre-flight (tsc + eslint) before the full verify. The blocking countdown REUSES the existing `useReenableCountdown` hook (already lint-clean).

**Refinement plan:** FRONTEND stage → 3a Desktop + Mobile viewport sign-off on the PROD stack — the USER tests in their own browser (NOT Playwright); `rm -rf apps/ui/dist` before `make dev-prod`, hash-prove served==built first. An agent pre-check (incl. exercising the write actions against the rig with CSRF) MAY catch bugs before hand-off but is NOT the sign-off. Any code change resets BOTH viewport approvals. Has Input Forms: Yes; the writes mutate the LIVE Pi-hole (confirm-gated + audited).

**Status**: [x] Design

## Build Phase

### Build Notes

Implemented per the planner spec (`/tmp/spec-2026-06-24-13-26-08.md`), mirroring the docker `useStartPullAndRestart` mutation + `PullRestartModal` confirm precedents; self-fetching widgets.

**Files created:**
- `apps/ui/src/components/ConfirmPhraseDialog.tsx` — generic typed-phrase confirm dialog (factored from PullRestartModal; reused by all 3 write actions; docker retrofit deferred non-blocking).
- `apps/ui/src/routes/integrations/piholeStatus.ts` — pure `adlistStatusToBadgeVariant` (ok→ok; *fail*/*error*→critical; empty/unknown→muted; else→warn).
- `apps/ui/src/routes/integrations/PiholeBlockingWidget.tsx` — blocking state + live countdown (reuses `useReenableCountdown`) + timer preset dropdown (30s/5m/1h/Indefinite; Indefinite omits timer) + contextual Enable/Disable → `ConfirmPhraseDialog` (phrase "enable"/"disable") → `useBlockingMutation` (invalidates overview).
- `apps/ui/src/routes/integrations/PiholeGravityWidget.tsx` — gravity count + last-update age (`formatAge`) + responsive per-adlist health table (failing rows tinted) + "Update gravity now" → confirm (phrase "update") → `useGravityUpdateMutation` (slow ~120s; pending spinner button; result Dialog shows `log_tail`; sonner toast on non-400 error; invalidates adlists+overview).
- `apps/ui/src/routes/integrations/PiholeMessagesWidget.tsx` — FTL diagnostic-messages list (type badge + text + optional timestamp/url; healthy empty state).
- Tests (colocated): ConfirmPhraseDialog, piholeStatus, PiholeBlockingWidget, PiholeGravityWidget, PiholeMessagesWidget, PiholeOverviewTab (privacy banner).

**Files modified:**
- `apps/ui/src/api/pihole.ts` — added `useAdlists`, `useMessages` (reads, 30s refetch) + `useBlockingMutation`, `useGravityUpdateMutation` (mutations mirror docker; CSRF auto-attached by middleware; invalidate on success) + `piholeQueryKeys.adlists`/`.messages`.
- `apps/ui/src/routes/integrations/PiholeOverviewTab.tsx` — replaced the 3 placeholder section bodies with the widgets + added the conditional privacy banner (shown only when `privacy_level > 0`). 023/024-owned sections + Logs tab untouched.

**Build fix-cycles (within budget):**
1. Frontend fast pre-flight caught 26 eslint errors + 3 vitest failures — ALL in TEST files (source was clean): unused imports, unsafe-`any` mutation-mock access, `require()`-style sonner import, Badge `role="status"` query (Badge has no role → assert by text), missing `beforeEach` import, sonner mock. Fixed per the codebase-canonical patterns (typed `MutateOptions` mocks, top-level `import { toast } from 'sonner'` + `vi.mocked`, text-content badge assertions).
2. **Pre-flight gap discovered:** my pre-flight `tsc --noEmit` PASSED but `make verify`'s `pnpm run typecheck` (`tsc -b`, with `exactOptionalPropertyTypes: true`) caught 15 stricter errors in the test files — `errorMessage={undefined}` on an optional prop (TS2375), `mock.calls[0][0]` possibly-undefined (TS2532), and the TanStack v5 `onSuccess`/`onError` 4-arg signature `(data, variables, onMutateResult, context)` (TS2554) + the `opts?:` contravariance (TS2322). Fixed all (omit explicit undefined; guarded call-args access; `opts?: MutateOptions<...>` + `?.` + 4th `undefined as never` arg). LESSON: use `pnpm run typecheck` (tsc -b), NOT plain `tsc --noEmit`, in the frontend pre-flight.

**Verification (cycle 2):** `make verify` GREEN — python `6029 passed, 1 skipped` (0 failed), **100.00% branch coverage**; frontend **vitest 1767 passed (170 files)** (+69), `tsc -b` typecheck PASSED, eslint **0 errors** (97 pre-existing warnings), vite build ✓. Pure frontend — NO backend/openapi/schema change (all types already in schema.ts); openapi/generate-ui-types hooks NO-OP.

**Status**: [x] Build

## Refinement Phase

- [x] 3a — Desktop viewport tested + approved (incl. confirm-gated write buttons)
- [x] 3a — Mobile viewport tested + approved
- [x] Regression Items Added

### Refinement Notes

FRONTEND stage — Desktop + Mobile viewport sign-off on the PROD stack (the USER tested in their own browser; not agent-driven). Prod image rebuilt (`rm -rf apps/ui/dist` then `make dev-prod`) to serve the freshly-built UI; served==built proven via bundle-hash change each rebuild.

**User approval:** Desktop (1280×720) AND Mobile (375×667) both explicitly approved against the corrected build (served bundle `index-DJTIldQd.js`).

**Agent-side write-action PRE-CHECK (bug-catch before hand-off, not the sign-off):** validated the live write wiring with CSRF against the rig — POST /blocking valid disable+enable → 200, wrong confirm_phrase → 400, missing CSRF → 403; Pi-hole blocking restored to original (enabled) each time; the gravity rebuild was NOT triggered (left to the user). Live read data confirmed: 2 NRD adlists `parse_failed`, 3 FTL messages, `privacy_level: 0` (banner correctly hidden).

**Bug found in user testing + fixed (2 attempts):** the blocking widget showed stale "Blocking is on" immediately after a successful Disable. Root cause: `/overview`'s `blocking_enabled` is VM-sourced (the `homelab_pihole_blocking_enabled` metric, refreshed by the `pihole_blocking` collector only every ~30s), so refetch-on-success re-read the stale pre-write metric. Fix attempt 1 (optimistic `setQueryData` patch from the authoritative `BlockingResponse`) was correct BUT was clobbered by the immediately-following `invalidateQueries` — which defaults to `refetchType: 'active'` and refetched the stale metric within the same tick (the hook unit test passed falsely because it never mounted an active `usePiholeOverview` observer). Fix attempt 2 (the real fix): `invalidateQueries({ ..., refetchType: 'none' })` — marks stale without an immediate clobbering refetch; the patch survives until the 30s reconcile. Added an active-observer regression test (mounts `usePiholeOverview` + mocks the stale GET) that reproduces the bug condition. Both the widget and the shared header status-strip badge now flip immediately and persist.

No backend change — pure frontend; the 018 write + 020 read endpoints + the audit contract are unchanged.

**Status**: [x] Refinement

## Finalize Phase

### Finalize Notes

**Code review (Opus, pre-test): CHANGES REQUIRED → all implemented.** 1 Important + 2 Minor, all premises verified:
- (Important) the gravity widget treated an HTTP-200 `GravityUpdateResponse.success: false` (rebuild RAN but FAILED — only PiholeError yields 502) as success → showed the green "complete" dialog. FIXED: `onSuccess` now branches on `data.success` — a `resultSuccess` state drives a "Gravity update failed" dialog title + an error toast on failure; log_tail renders in both cases. Added a `success: false` regression test.
- (Minor) `ConfirmPhraseDialog` `body` (ReactNode) renders inside `DialogDescription` (a `<p>`) → added a JSDoc "inline content only" note.
- (Minor) `PiholeMessagesWidget` url anchor not validated → gated to `^https?://` (blocks `javascript:`/`data:`/relative); added a non-http → no-anchor test.

**2nd code review (Opus, post-source-change): APPROVED.** The 3 fixes were real SOURCE edits after the 1st review → a 2nd review ran. It verified all 3 (the gravity success-branch incl. no stale-state bug across attempts; the JSDoc; the url regex) and confirmed no new issues. (The 1st review also empirically validated the Refinement blocking cache-clobber fix by reverting it and confirming the regression test catches it.)

**Verification (cycle 3, covering ALL 022 changes incl. the Refinement blocking fix + the review fixes):** `make verify` GREEN — python `6028 passed, 2 skipped` (0 failed; skips = `vector binary not on PATH` + the intermittent seed-dependent scheduler-decorrelation skip), **100.00% branch coverage**; frontend **vitest 1775 passed (171 files)** (+77), `tsc -b` typecheck PASSED, eslint **0 errors** (97 pre-existing warnings), vite build ✓.

**Refinement bug recap (carried into Finalize):** the user-found stale-"Blocking is on" bug was fixed via `setQueryData` optimistic patch + `invalidateQueries({ refetchType: 'none' })` (a default-active refetch was clobbering the patch with the 30s-lagged VM metric); an active-observer test was added that reproduces the clobber condition (a mutation-only hook test gave a false green).

**Pure frontend stage** — NO backend/openapi/schema change (all types already in schema.ts); the openapi-export/generate-ui-types pre-commit hooks correctly no-op'd.

**Commits:** implementation `a089b26`, changelog `fc6e5d6`, tracking `2193662`.

- [x] Code Review (pre-tests)
- [x] Tests Written
- [x] Code Review (post-tests)
- [x] Documentation Updated
- [x] Committed: specific paths only

**Commit Hashes**:
- Implementation: a089b26
- Changelog: fc6e5d6
- Tracking: 2193662

**CHANGELOG Entry**: [x] Added

**Status**: [x] Finalize
