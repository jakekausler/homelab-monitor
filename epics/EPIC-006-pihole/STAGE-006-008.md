# STAGE-006-008: Blocking-state collector

## Status: Not Started

## Overview

Poll `/api/dns/blocking` and emit the blocking-enabled state + any temporary-disable timer. This is the
data behind the "someone hit disable and walked away" alerts and the panel's live auto-re-enable
countdown. Small but high-value (with the user's forced-DNS topology, blocking-off = real protection loss).

**Vertical slice:** `homelab_pihole_blocking_enabled` (1/0) + `_blocking_timer_seconds` (remaining, or
absent when permanently enabled/disabled) in VM; reflects the live state.

**Spec references:** EPIC-006 "Metric families" (Blocking state). Alerts: `PiholeBlockingDisabled`
(>5m, warning), `PiholeBlockingDisabledIndefinitely` (no timer, >15m, warning).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [x] Yes — RO session.
- Host-integration concerns: [x] Yes — live API. Refinement 3a (fixtures: enabled / disabled-with-timer /
  disabled-indefinitely) AND 3b (real state; live `{blocking:"enabled", timer:null}` observed in recon).

## Cross-cutting standards

- Reuse client session; failure budget; self-metrics.
- Distinguish "disabled with a timer" (auto-re-enables) from "disabled indefinitely" (no timer) — the two
  alerts depend on it. Emit the timer as a separate metric (absent/null when not temporarily disabled).

## Concrete deliverables

### 1. Collector

Poll `/api/dns/blocking` (~30s). Emit `homelab_pihole_blocking_enabled` (1/0) and
`homelab_pihole_blocking_timer_seconds` (remaining seconds when temporarily disabled; not emitted / NaN
when there's no timer). The 15m `for:` on the indefinite-disable alert is set in 006-016; this stage just
emits the clean signal.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] 3a — fixtures: enabled, disabled+timer, disabled-indefinitely
- [ ] 3b — real blocking state on prod rig
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only

**Commit Hashes**:
- Implementation:
- Changelog:
- Tracking:

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
