# STAGE-009-013: `pihole-restart-loop` example runbook (exemplar-only) + end-to-end validation

## Status: Not Started

## Overview

Ship the first built-in example runbook as a folder (LOCKED Decision 2 structure: markdown intent/allowed-actions + a config manifest), and validate the whole auto-fix pipeline end-to-end against the fake claude. The `pihole-restart-loop` runbook's config declares a **docker capability** for the local container `pihole-unbound` with action `restart` (verified: Pi-hole runs as a local Docker container, `network_mode: host`, on this host — it acts via the docker socket, NOT SSH). Per LOCKED Decision 4, it ships in `runbooks/_examples/` and is **NOT registered/enabled** — a fresh install has zero active auto-fix. This is the **epic-closing stage** — its tracking commit closes EPIC-009.

**Vertical slice:** after this stage, the example runbook folder exists as a complete, correct exemplar (markdown + config); the full pipeline (registry → orchestrator → dry-run → fixer-runner exec → audit → history UI) is validated end-to-end against the fake claude using this runbook; the example is shipped disabled. EPIC-009 is complete.

**Spec references:** §7.4 (the whole safety model this runbook exercises), §9.2 (the runbook appears in the catalog once a user registers it). EPIC-009 LOCKED Decisions 2 (folder = markdown + config; docker capability) + 4 (exemplar only, NOT enabled, risky-by-default). EPIC-006 (Pi-hole — `pihole-unbound` local container).

## Stage Flags

- Has Input Forms: [ ] No
- Touches secrets / production data: [ ] No (the example is NOT enabled; end-to-end validation uses the fake claude)
- Host-integration concerns: [x] Yes — the end-to-end pipeline validation runs against the real stack with the fake claude (instance A).

## Cross-cutting standards (from EPIC-009 cross-stage acceptance criteria)

- **FAKE claude in CI** — the end-to-end validation uses the fake binary; no real Claude API call, no real pihole restart in CI.
- Non-negotiables exercised end-to-end: #1 (the runbook is registered-but-disabled by default — inert), #2 (its config declares exactly the docker capability for `pihole-unbound restart` and nothing broader; its markdown is the scoped CLAUDE.md), #4 (a run produces a full audit record), #5 (risky → dry-run by default).
- Open-source-safe: the example lives in `runbooks/_examples/`, NOT registered/enabled. The public release ships it as an exemplar only.
- **Epic-close:** this stage's Finalize tracking commit closes EPIC-009 (13/13).

## Concrete deliverables

### 1. `pihole-restart-loop` example runbook folder

In `runbooks/_examples/pihole-restart-loop/`: the markdown detail file(s) (the runbook's own CLAUDE.md-style intent + allowed/forbidden actions: "you may run `docker restart pihole-unbound` and tail its logs; you may NOT touch other containers") + the config manifest (match patterns for the pihole-crash-loop alert, `risk_tag: risky`, `dry_run_required: true`, rate-limit/cooldown, `scoped_capabilities: {docker: {container: pihole-unbound, actions: [restart]}}`).

### 2. End-to-end pipeline validation

Validate the full pipeline against the fake claude using this runbook: register → (enable in a test context) → match a synthetic pihole alert → dry-run plan → approval → real run (fake claude) → audited record → visible in the history UI. Confirm the shipped example is NOT enabled in the default config.

### 3. Tests

The example config validates against the STAGE-009-001 model; the docker capability resolves; end-to-end with the fake claude produces a complete audited run; the shipped example is inert (not registered/enabled) by default.

## Design Phase

**Status**: [ ] Design (Not Started)

## Build Phase

**Status**: [ ] Build (Not Started)

## Refinement Phase

- [ ] Instance-A validation: end-to-end pipeline with the fake claude using the example runbook; shipped-disabled confirmed
- [ ] Regression Items Added

**Status**: [ ] Refinement (Not Started)

## Finalize Phase

- [ ] Code Review (pre-tests)
- [ ] Tests Written
- [ ] Code Review (post-tests)
- [ ] Documentation Updated
- [ ] Committed: specific paths only
- [ ] EPIC-009 marked Complete (last stage)

**Commit Hashes**:
- Implementation: [pending]
- Changelog: [pending]
- Tracking: [pending]

**CHANGELOG Entry**: [ ] Added

**Status**: [ ] Finalize (Not Started)
