2026-06-19 075a93f EPIC-007/STAGE-007-015: vmalert-METRICS rules (EPIC-007 Unifi, Wave D)

New `deploy/vmalert/metrics/unifi.yaml`: single group `unifi`, interval 30s, 25 alert rules (5 critical / 15 warning / 5 info) over the Unifi collector metrics — controller/WAN/gateway/device down, DNS-steering drift + absent companion, device flapping/port-errors/PoE-fault/AP-channel-util/AP-satisfaction/client-experience/WAN-latency/WAN-loss/threat/temp/cpu-mem/teleport-down/DHCP-pool-exhaustion/uplink-saturation, firmware-update/new-client/speedtest-degraded/api-slow + two client-count anomaly rules.

New `deploy/vmalert/metrics/__tests__/unifi.tests.yaml`: promtool rule-unit tests for the 23 non-subquery rules (the 2 `[1d:30s]` anomaly rules are live-replay-only). Validated: `promtool check rules` -> 25 rules; `promtool test rules` -> SUCCESS (prom/prometheus:v2.47.0).

Deferred items resolved: UnifiDhcpPoolExhaustion (per-network `client_count_by_network / on(network) dhcp_pool_size`) + UnifiDnsSteeringDrift (label-NEQ vs Pi-hole 192.168.2.148 + absent() companion) BUILT here; the STAGE-007-009 DPI spike-clamp relocated to STAGE-007-024 (no DPI-rate consumer in 015).

Live prod-rig validation: reloaded vmalert-metrics; the `unifi` group loaded with all 25 rules health=ok, notifier wired to Alertmanager; `UnifiDnsSteeringMetricMissing` genuinely firing (demonstrates the full collector->VM->vmalert->AM path).

2026-06-19 19c6513 EPIC-007/STAGE-007-014: VPN/Teleport health collector (Wave C)

New `UnifiVpnTeleportCollector` (`unifi_vpn_teleport`, 60s, concurrency_group `unifi`): polls `stat/device` and derives Teleport health from the UDM gateway record's `teleport_version`. Live recon proved `stat/health` vpn.status is unconditionally "unknown" (so it is intentionally NOT used).

Emits `homelab_unifi_teleport_up` (label-free 1.0/0.0 — stable series for the future `UnifiTeleportDown` alert), `homelab_unifi_teleport_reason{reason}` (info-gauge; reasons ok/not_initialized/device_not_found/bad_response/not_configured + UnifiError pass-throughs unreachable/timeout/auth/rate_limited/http_error), `homelab_unifi_teleport_version{version}` (info-gauge, ok path only), and `homelab_unifi_api_took_seconds{endpoint="stat/device"}` (HTTP-response paths only).

Honest signal: teleport_up=1 means "gateway reports Teleport initialized", NOT "session establishable". teleport_sessions is NOT emitted — no session-count field exists on this firmware (the dedicated endpoints 404 even when configured); deferred pending an active-session probe.

Health-collector behavior: emits up=0 + reason on every failure path so the alert can fire even when unconfigured/unreachable.

No new client helper, migration, cardinality cap, or shared state. Registered in the unifi bundle.

Validated live against the UDM (idle Teleport): up=1.0, version="1", latency ~0.46s.

2026-06-19 538d840 EPIC-007/STAGE-007-013: Controller-up composite + API-latency collector (Wave C)

New `UnifiControllerUpCollector` (`unifi_controller_up`, 30s, `concurrency_group="unifi"`): polls `stat/sysinfo` and emits `homelab_unifi_up` (label-free 1.0/0.0 — stable series for the future `UnifiControllerDown` critical alert), `homelab_unifi_up_reason{reason}` (info-gauge; reasons: ok/unreachable/timeout/auth/rate_limited/http_error/bad_response/empty_data/not_configured), and `homelab_unifi_api_took_seconds{endpoint="stat/sysinfo"}` (latency, on HTTP-response paths only).

Health-collector behavior: emits `up=0` + reason on every failure path (incl. unconfigured/unreachable) so the controller-down alert can fire even when the integration is down.

No new client helper, migration, cardinality cap, or shared state. Registered in the unifi bundle.

Validated live against the UDM: success → up=1.0 + reason ok + latency; bad key (HTTP 401) → up=0.0 + reason auth.

2026-06-19 7b1caa3 EPIC-007/STAGE-007-012: SSH DHCP-lease collector (opt-in, default-OFF)

New `UnifiSshLeaseCollector` (`plugins/collectors/integrations/unifi/ssh_lease.py`, plain `BaseCollector`, 5m, `concurrency_group="unifi"`, registered in the unifi bundle): opt-in/default-OFF (gated on `UnifiConfig.ssh_lease_enabled`). When enabled, SSHes to the configured target (`UnifiConfig.ssh_lease_target_id`, default "udm") via the EPIC-017 framework's `ctx.ssh`, reads `cat /data/udapi-config/dnsmasq.lease`, parses the dnsmasq 5-field format, and enriches the `unifi_clients` registry with `lease_expiry` (never the identity source).

Registry enrichment via a new `UnifiClientRepo.set_lease_expiry_conn` (case-insensitive `WHERE LOWER(mac)=LOWER(:mac)` — registry stores API MACs verbatim, dnsmasq lowercases). Existing rows enriched; lease-only MACs get minimal `online=False` rows. No migration (`lease_expiry` column already existed).

Emits `homelab_unifi_dhcp_lease_count` (authoritative count that sharpens STAGE-007-011 pool occupancy) + probe-scoped health gauges `homelab_ssh_probe_up` / `homelab_ssh_probe_host_key_mismatch` / `homelab_ssh_probe_duration_seconds` / `homelab_ssh_last_success_age_seconds` (all `{target, probe="unifi_dhcp_lease"}`). The `_probe_`-infixed names deliberately avoid colliding with the SSH-bundle uptime probe's target-level `homelab_ssh_up{target}`. `last_success_age` is a real climbing age via per-instance monotonic state.

New `UnifiConfig.ssh_lease_target_id` field (env `HOMELAB_MONITOR_UNIFI_SSH_LEASE_TARGET_ID`, default "udm"). EPIC-017 framework untouched (plain BaseCollector, not an SshProbe subclass — SshProbe.parse() is pure with no DB hook).

ok-semantics: gate-off -> ok=True, 0 metrics, no SSH, no DB (inert). Transport error (host-key mismatch / auth / timeout) or non-zero exit -> ok=False, no lease_count, no DB write (so a probe failure is never misread as zero leases). `_parse_leases` skips malformed lines (<2 fields, non-int / non-positive / out-of-range epoch).

Opt-in setup is operator-provided (SSH key install on the UDM); the live read is inert until the operator configures the `udm` ssh_target + key. Refinement validated all code paths via synthetic SSH + temp DB; the live read is WAIVED pending operator key (tracked in regression).

~26 unit tests (gate-off, transport errors, nonzero-exit, empty/populated/combined success, case-insensitive enrichment, lease-only insert, climbing-age-after-success, `_parse_leases` edge cases) + config + repo test additions. Code review (2 rounds): made `last_success_age` a real climbing age, guarded `_parse_leases` against non-positive/overflow epochs.

2026-06-19 25058c0 EPIC-007/STAGE-007-011: DHCP config + DNS-steering collector (rest/networkconf)

New `UnifiNetworkconfCollector` (`plugins/collectors/integrations/unifi/networkconf.py`, `rest/networkconf`, 5m, `concurrency_group="unifi"`, stateless): per DHCP-enabled non-WAN network emits `homelab_unifi_dhcp_pool_size{network}` (address count), `homelab_unifi_dhcp_pool_start{network}` / `_pool_end{network}` (int(ip) as float), and `homelab_unifi_dhcp_dns_primary{network, dns=<dhcpd_dns_1>}=1.0` (info-gauge). ALWAYS emits `homelab_unifi_dhcp_enabled_network_count` (0.0 when none) + `homelab_unifi_api_took_seconds{endpoint="rest/networkconf"}`.

Pool gauges gated on both start+stop parsing to same-family IPs with stop >= start (inverted/mixed-family/malformed ranges skip pool gauges but the network still counts + emits its dns gauge — field-by-field graceful degrade).

`homelab_unifi_dhcp_dns_primary` is the DNS-steering signal: a `dns` label != 192.168.2.148 (the Pi-hole) means clients are no longer steered to filtered DNS. The expected value lives in STAGE-007-015's vmalert rule (the sole DNS-protection signal after the firewall DNS-force rules were deleted).

The always-present `dhcp_enabled_network_count` lets STAGE-007-015 avoid the absent() trap.

Tracked deferrals: `dhcp_pool_used`/occupancy -> STAGE-007-015 PromQL join (homelab_unifi_client_count_by_network / dhcp_pool_size); authoritative leases -> STAGE-007-012; `reservation_count{network}` -> STAGE-007-019 (added as a deliverable on that card).

Registered in the unifi bundle. No protocol/client/config changes (`rest_networkconf()` already existed).

~24 unit tests (all branches incl. `_parse_ip` 5 branches, WAN/disabled filtering, inverted + mixed-family pool ranges, missing DNS). Live-validated against the real UDM (populated path: pool_size=249, dns=192.168.2.148 confirmed). Code review: negative-pool-size-on-inverted-range + mixed-IP-family bugs found and fixed (same-family + stop>=start gate).
