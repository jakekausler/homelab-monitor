## [STAGE-005-003] integrations/ bundle skeleton + registration pattern

**Summary:** Establishes the `plugins/collectors/integrations/` collector-bundle directory structure and registration pattern — the exemplar that EPICs 006/007/008/021 copy. Introduces a generic kernel helper `config_from_classvars()` to derive registration config from ClassVars, eliminating hand-built-dict drift. Implements the real permanent Home Assistant reachability collector (`HaUpCollector`, 30s interval, `homelab_ha_up` gauge) using the REST client. Wires bundle registration into the kernel lifespan with a single call; future HA/non-HA integration stages require only directory structure + `__init__.py` exposing `register_all(loader)`.

**Key changes:**

**Kernel:**
- New `kernel/plugins/loader.py` helper: `config_from_classvars(cls: type[BaseCollector], **overrides) -> dict[str, object]` — derives registration config (`name`/`interval_seconds`/`timeout_seconds`) from a collector's ClassVars. Allows bundles to eliminate hand-built dicts; overrides layer for integration-specific tuning. Reused by all bundles, not copied per integration.
- `kernel/api/lifespan.py`: Added single `register_all(loader)` call after builtins, before `load_all()`. After this, Wave-B HA stages never touch lifespan again.

**Collectors:**
- New `plugins/collectors/integrations/` package hierarchy: `__init__.py` (empty exports), `homeassistant/__init__.py` (exposes `register_all(loader)` that loops `_HA_COLLECTORS` list, registering each via `config_from_classvars()`; per-collector `try/except` logs+continues, mirroring builtins failure isolation).
- New `integrations/homeassistant/ha_up.py`: `HaUpCollector` (`name="ha_up"`, interval 30s, timeout 10s, `concurrency_group="homeassistant"`). `run()` calls `await ctx.ha.get_config()`, emits gauge `homelab_ha_up` = 1.0 on success, 0.0 on `HaError` or `ctx.ha is None`; returns `CollectorResult(ok=True)` on both paths (a probe finding HA down is a successful run — the metric carries the signal; mirrors builtin probe semantics; 0.0 not absent so vmalert rules distinguish "HA down" from "collector never ran").

**Tests:**
- NEW: `tests/test_ha_up_collector.py` (4 test functions: ClassVars derive correctly, run() success→1.0, HaError→0.0, ctx.ha=None→0.0, all ok=True).
- NEW: `tests/test_ha_integration_bundle.py` (5 test functions: `config_from_classvars` derives/overrides/override-wins, `register_all()` happy-path, per-collector failure isolation with logging).
- UPDATED: `tests/conftest.py` — `_per_test_db` fixture now registers the HA bundle (`register_all(loader)`) so test app mirrors production; every HA collector flows in automatically.
- UPDATED: `tests/test_api_collectors.py` — added `ha_up` appears in `GET /collectors` response.

**Copyability:** New integration = a directory of one-class collector modules + `__init__.py` exposing `register_all` + one lifespan line; new collector within a bundle = one new module + two lines (import + append to bundle's collector list) — no lifespan edit, no hand-built config, no kernel change.

**Files:**
- NEW: apps/monitor/homelab_monitor/plugins/collectors/integrations/__init__.py, apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/__init__.py, apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_up.py, apps/monitor/tests/test_ha_up_collector.py, apps/monitor/tests/test_ha_integration_bundle.py
- MOD: apps/monitor/homelab_monitor/kernel/plugins/loader.py (+`config_from_classvars`), apps/monitor/homelab_monitor/kernel/api/lifespan.py (+`register_all(loader)` call), apps/monitor/tests/conftest.py (HA bundle registered in `_per_test_db`), apps/monitor/tests/test_api_collectors.py (+`ha_up` appears test)

**Refinement 3a Validation:** Live backend on real lifespan: `GET /api/collectors` lists `ha_up` (status healthy, interval_seconds=30); `homelab_ha_up=0.0` (no dev token, wiring confirmed) + `homelab_collector_run_success_total{name=ha_up}=1.0` in metrics snapshot — confirms full chain and ok=True-on-down semantic in production.

**Code Review:** Opus APPROVED. No Critical/Important findings. 2 Minor findings both reviewer-classified correct-as-designed with no-action recommended: (1) bundle's `register_all` swallows failures internally rather than folding into lifespan's `degraded` list — intentional (bundle owns isolation; folding would add coupling 4 downstream epics inherit); (2) log namespace differs (`ha_integration.collector_register_failed` vs `lifespan.collector_register_failed`) — correct (namespace should reflect bundle ownership; copies rename it).

**Verification:** `make verify` GREEN: pyright 0, pytest 4483 passed, **100% coverage** (all new files 100%), tsc/eslint/vitest/UI build clean. No OpenAPI/type regen (no new API models; `GET /collectors` already exists).

**Implementation:** (commit: bdc13cb)

---

## [STAGE-005-001] HA REST client + secret + lifespan wiring

**Summary:** Implements the Home Assistant REST client kernel package with async methods for configuration, entity state queries, error logs, and service calls. Replaces the empty `HomeAssistantClient` Protocol scaffold with concrete typed implementations. Uses a token-provider callback for secret rotation and post-startup recovery (token never stored). Integrates with the kernel lifespan to construct the client once at startup and inject it into request context. Error handling mirrors the existing `FetchError` pattern: all methods return `HaError` on failure (return-not-raise) with discriminated failure reasons (unreachable/timeout/auth/http_error/bad_response). Validated in Refinement 3b against the real HA instance at `http://192.168.2.148:8123`.

**Key changes:**

**Kernel:**
- New `kernel/ha/` package: `errors.py` defines `HaError` (frozen/slots discriminated union), `client.py` implements `HomeAssistantRestClient` with async methods: `get_config()`, `get_states()`, `get_error_log()`, `call_service(domain, service, data=None)`. Reuses shared `httpx.AsyncClient`; token-provider invoked per-request.
- `kernel/config.py`: Added `HaConfig` dataclass + `load_ha_config()` reading `HOMELAB_MONITOR_HA_URL` (default `http://192.168.2.148:8123`, trailing slash stripped).
- `kernel/api/lifespan.py`: HA client constructed once at startup, injected as `ctx.ha`. No-token case yields `HaError(reason="auth")` at call time (enables no-restart recovery).
- `kernel/plugins/io.py`: Updated `HomeAssistantClient` Protocol with 4 async methods (matching concrete implementation).

**Error Mapping:**
- ConnectError/ConnectTimeout → unreachable
- ReadTimeout/TimeoutException → timeout
- 401/403 → auth
- Other non-2xx → http_error (with status)
- JSON/shape failures → bad_response
- Bearer token never appears in error messages or logs.

**Tests:**
- NEW: `apps/monitor/tests/test_ha_client.py` (34 test functions, httpx fully mocked). Coverage: all 4 methods, all 5 error reasons, no-token branch, status mapping, token-not-leaked assertions (2), `load_ha_config`.
- UPDATED: `tests/test_plugins_context.py`, `tests/test_plugins_io.py` — updated `_FakeHa` stubs to satisfy now-non-empty Protocol.

**Files:**
- NEW: apps/monitor/homelab_monitor/kernel/ha/__init__.py, apps/monitor/homelab_monitor/kernel/ha/errors.py, apps/monitor/homelab_monitor/kernel/ha/client.py, apps/monitor/tests/test_ha_client.py
- MOD: apps/monitor/homelab_monitor/kernel/plugins/io.py, apps/monitor/homelab_monitor/kernel/config.py, apps/monitor/homelab_monitor/kernel/api/lifespan.py, apps/monitor/tests/test_plugins_context.py, apps/monitor/tests/test_plugins_io.py

**Code Review:** Opus APPROVED. No Critical/Important findings. 3 Minor findings (optional/cosmetic): (1) `call_service` silently drops non-dict entries — intentional & tested; (2) `get_states` fail-whole-batch on one malformed entry — documented contract; (3) inline-comment style — cosmetic.

**Verification:** `make verify` GREEN (pyright 0, pytest 4425 passed, 100% kernel coverage, tsc/eslint/vitest/UI build clean). Real HA validation: version 2026.2.1, 2496 entities, bad-token → HaError(auth,401), token not leaked. No OpenAPI/type regen.

**Implementation:** (commit: 1bac4de)

---

## [STAGE-005-002] HA websocket client + intent-replay reconnect + metrics

**Summary:** Implements a persistent, supervised Home Assistant `/api/websocket` client with multiplexed command/event handling, auto-reconnect with exponential backoff, and intent-replay (auto-resubscribe active subscriptions on reconnect). Provides `send_command()` one-shot RPC and `subscribe()` async-iterator handles. Includes observability: connection state, message timestamps, reconnect counts, and error reasons. Validates against the real HA instance; discovers constraint: bulk `get_states` exceeds HA's 1 MB frame limit—REST client required for entity sync. Fixes two production hangs found during Build (sequential-replay deadlock on reconnect; event-loop starvation in test mock).

**Key changes:**

**Kernel:**
- New `kernel/ha/websocket.py`: `HomeAssistantWebsocketClient` — persistent supervised client (start_task/stop_task mirroring BuildSourcesLoader) with async-iterator `Subscription` and injectable `connect` seam (minimal `_WsConnection` Protocol; single library-boundary cast; no `Any`).
- Auth handshake: `auth_required` → `auth` → `auth_ok`/`auth_invalid`; bearer token NEVER logged.
- Multiplexed protocol: single receive loop demuxes HA frames — `result` frames resolve command `Future`, `event` frames enqueue to matching subscription's bounded queue (drop-oldest + log on full), keyed by HA's monotonic per-connection `id`.
- `send_command(type_, **fields) -> dict | HaError` (one-shot RPC with timeout); `subscribe(type_, **fields) -> Subscription | HaError` (intent + bounded queue).
- Reconnect: exponential backoff (1s→60s); **intent-replay auto-resubscribes** active subscriptions CONCURRENTLY with receive loop (fixed sequential deadlock). Pending command futures failed on disconnect (no hangs). Per-connection subscription clear prevents stale-id routing. Transition-deduped auth_invalid logging.
- Metrics: `homelab_ha_websocket_connected` (gauge), `homelab_ha_websocket_last_message_timestamp_seconds` (gauge epoch), `homelab_ha_websocket_reconnect_total` (counter, RE-connections only), `homelab_ha_websocket_error_total{reason}` (counter).
- `kernel/api/lifespan.py`: WS client constructed after metrics writer; `start_task()` guarded on `ha_config.base_url`; injected as `app.state.ha_ws_client`; `stop_task()` in finally block.
- `websockets>=16.0` added as direct dependency (was transitive via uvicorn[standard]; declared explicit for honesty; zero install delta; `uv lock` run).

**Tests:**
- NEW: `apps/monitor/tests/test_ha_websocket.py` (48 test functions, injected fake WS, no real network/sleeps; **100% line+branch coverage** of websocket.py). Coverage: auth handshake/invalid/no-token, send_command (success/failure/not-connected/timeout/send-failure), subscribe/event/unsubscribe, reconnect+intent-replay, per-connection subscription-clear, unmapped-id drop, replay-subscribe-failure logging, disconnect-fails-pending, queue-full drop-oldest, backoff increment+reset, default-connect seam, log-transition dedup, fail-all-pending done-future skip.
- UPDATED: `tests/test_api_lifespan.py` — HA-disabled lifespan branch test (websocket start skipped).
- Build fixes: (1) Sequential-replay reconnect deadlock — moved intent-replay concurrent to receive loop; (2) `record_sleep` test mock not yielding to event loop — added `asyncio.sleep(0)` to progress concurrent tasks.

**Files:**
- NEW: apps/monitor/homelab_monitor/kernel/ha/websocket.py, apps/monitor/tests/test_ha_websocket.py
- MOD: apps/monitor/homelab_monitor/kernel/ha/__init__.py (exports), apps/monitor/homelab_monitor/kernel/api/lifespan.py (client construct/wiring), apps/monitor/tests/test_api_lifespan.py (HA-disabled branch), apps/monitor/pyproject.toml (+websockets>=16.0), apps/monitor/uv.lock

**Refinement 3b Validation:** Real HA instance (`ws://192.168.2.148:8123/api/websocket`): auth handshake succeeded → `get_config` returned HA version 2026.2.1; `subscribe_events` received real `state_changed` event; reconnect metrics incremented. **Constraint discovered:** WS `get_states` command exceeds HA's 1 MB frame limit (close code 1009) with ~2496 entities — bulk entity fetch MUST use REST client's `get_states()`, not WS (recorded for collector stages 006–012).

**Code Review:** Opus APPROVED (two passes). First pass: 5 findings — 2 Important (stale `_subscriptions` across reconnect → mis-route window; `reconnect_total` counting initial connect), 3 Minor (unsubscribe id reset; best-effort comment; replay-failure logging). All implemented and verified in second pass. No regressions.

**Verification:** `make verify` GREEN: pyright 0, pytest 4474 passed/1 skipped, **100% kernel coverage**, tsc/eslint/vitest/UI build clean.

**Implementation:** (commit: b6149f2)

---
