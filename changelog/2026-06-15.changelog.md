## [STAGE-005-041] Grafana dashboard review & readability pass (System + Home Assistant) + new metric tabs

**Summary**

Interactive readability pass over the Grafana observability surface that expanded (by user direction) from a Grafana-only review into a backend collector fix, a host-overview rework, four brand-new dashboards each embedded as its own Metrics-page tab, and a full Home Assistant dashboard rework. Dual sign-off (System dashboard + Home Assistant dashboard) plus Desktop/Mobile review of the new tabs.

**Host disk collector fix** — `homelab_host_disk_bytes` was emitting ~19 meaningless container-internal mountpoints (the collector runs inside the container, so `psutil.disk_partitions()` returns the container mount namespace). Added a `disk_mountpoint_relabel` allowlist+relabel config field (default `{"/config": "/", "/host-compose": "/storage"}`) so the dashboard shows only the real `/` and `/storage` filesystems with friendly labels. Empty map preserves emit-all behavior.

**System (host-overview) dashboard** — CPU split into "all cores" + per-core (numeric legend via `sort_by_label_numeric`); Load average split into "1m" + "1m/5m/15m"; Memory & swap timeseries; Disk bargauge percent-full on the relabeled `/`+`/storage` series; Network split into separate Download + Upload charts (each rate[1m]/[5m]/[15m]); Uptime `dtdurations`.

**Four new dashboards + Metrics tabs** — `containers`, `collectors`, `heartbeats`, `storage-logs` (auto-provisioned JSON), each embedded as a tab inserted between System and Home Assistant. Timeseries-first; introduced the reusable binary Up/Down stat pattern (green "Up"/red "Down" word, no digit) on the Storage & Logs VictoriaLogs panel.

**Home Assistant dashboard rework** — "Status" + "WebSocket" binary Up/Down; new "Registered entities" + "Integrations not loaded" stats; fixed the `sum(homelab_ha_entity_available == 0)` always-0 bug → `count(... == 0)` (now correctly shows ~198, matching the in-app Status page); "Entities Unavailable" / "Stale Entities" converted to timeseries; "Low/Critical Batteries" bargauge → "Batteries below 20%" timeseries; "Collector Run Health" (always-empty error queries) → "HA collector run rate"; count stats threshold-colored (green 0 / amber <10 / red ≥10; Config Entry Errors + Integrations not loaded green 0 / red ≥1); full grid reflow.

**Mobile fix** — the Metrics tab nav now scrolls horizontally (`overflow-x-auto`) instead of forcing page-wide overflow when the 6 tabs exceed viewport width.

**Modified files**

- `apps/monitor/homelab_monitor/plugins/collectors/builtin/host.py` — `disk_mountpoint_relabel` config + `_collect_disk_usage()` allowlist/relabel + `_emit_disk_mountpoint()` helper
- `apps/monitor/tests/test_host_collector.py` — 8 existing disk tests updated + 4 new tests
- `deploy/grafana/dashboards/host-overview.json` — full readability rework
- `deploy/grafana/dashboards/home-assistant.json` — full readability rework + sum→count bug fix + binary Up/Down
- `apps/ui/src/router.tsx` — 4 new metrics routes
- `apps/ui/src/routes/metrics/MetricsLayout.tsx` — 4 new tabs + horizontal-scroll nav
- `apps/ui/src/routes/metrics/MetricsLayout.test.tsx` — updated tab assertions

**New files**

- `deploy/grafana/dashboards/containers.json`, `collectors.json`, `heartbeats.json`, `storage-logs.json`
- `apps/ui/src/routes/metrics/MetricsContainersTab.tsx`, `MetricsCollectorsTab.tsx`, `MetricsHeartbeatsTab.tsx`, `MetricsStorageLogsTab.tsx` (+ their `.test.tsx`)

**Design decisions**

- Binary up/down panels use `color.mode: "thresholds"` (steps null→red, 1→green) + value-mappings for the "Up"/"Down" text. (`color.mode: "fixed"` shows the text but NOT the green/red background — caught in code review.)
- Disk relabel map baked as the `HostCollectorConfig` default (no live per-collector config injection path exists yet; matches the existing `extra_mountpoints`/`exclude_disk_devs` default pattern).
- `count(metric == 0)` not `sum(metric == 0)` for "how many match" — `sum(==0)` always yields 0.
- Grafana parser-safe regex `[0-9]` (not `\d`, which 422s) + `sort_by_label_numeric` for numeric legend order.
- New dashboards auto-provision from `deploy/grafana/dashboards/*.json`; tabs reuse the `GrafanaDashboardEmbed` kiosk-iframe pattern.

**Verification**

`make verify` PASSED — backend 4913 pass / 1 skip / 100% kernel coverage (4 new disk collector tests); vitest 1531 pass (4 new tab tests + updated layout test); eslint 94 warnings (baseline) / 0 errors; pyright + ruff + tsc clean. All 6 dashboards `jq`-valid, no gridPos overlaps, validated rendering REAL data on the prod rig (VM queries + Grafana API). Two Opus code-review passes, both approved; all findings dispositioned (binary-stat thresholds fix + mobile overflow fix implemented).

**Commit:** (commit: c74d2a3)

## [STAGE-005-042] HA cadence detail API + Status-tab idle cadence widget

**Summary**

The in-app HA cadence surface (split off from STAGE-005-038): a new `GET /api/integrations/home-assistant/cadence` endpoint + a Status-tab widget listing idle (>24h or never-triggered) automations and idle scripts, mirroring the `/entities` endpoint + `HaEntitiesDrill` pattern. Final stage of EPIC-005 (Home Assistant integration) — completes the epic at 42/42.

**Backend** — `GET /cadence` returns two named collections (`automations`, `scripts`) from VictoriaMetrics per-series gauges. Automation universe = `homelab_ha_automation_enabled` (LEFT JOIN `homelab_ha_automation_last_triggered_seconds`; absent → `last_triggered_age_seconds: null` = never-triggered). Include iff `enabled AND (age is None OR age > 86400)`. Scripts: universe = `homelab_ha_script_last_triggered_seconds`, include iff `age > 86400`. Sorted most-idle-first (null age first, then age DESC), top-N capped, `*_total`/`*_returned` per collection, `filtered_to="idle_24h"`. friendly_name enriched via one live HA `get_states()` (HA-down → null, never 502; VM-down → 502).

**Frontend** — `useHomeAssistantCadence` hook; `HaCadenceAutomationsDrill` + `HaCadenceScriptsDrill` (both consuming the one shared hook, TanStack-deduped), mounted as "Idle automations" + "Idle scripts" `PanelSection` cards in the HA Status tab. Null age renders "never".

**Refinement scope change (user)** — disabled automations EXCLUDED entirely (the card's "idle + disabled" became "idle only"): the predicate dropped the `not enabled` inclusion clause, `filtered_to` → `"idle_24h"`, the frontend "disabled" badge removed, the card retitled "Idle automations". Validated on the real prod rig: `automations_total` 24 (down from 59), ZERO disabled rows, scripts 16; friendly_name live-enriched.

**New files**

- `apps/ui/src/routes/integrations/HaCadenceAutomationsDrill.tsx` (+ `.test.tsx`)
- `apps/ui/src/routes/integrations/HaCadenceScriptsDrill.tsx` (+ `.test.tsx`)

**Modified files**

- `apps/monitor/homelab_monitor/kernel/api/routers/integrations_home_assistant.py` — `/cadence` handler + 3 Pydantic models + constants
- `apps/monitor/tests/test_api_ha_detail.py` — 7 new cadence tests
- `packages/shared-types/openapi.json` — regenerated (additive: 3 schemas + `/cadence` path)
- `apps/ui/src/api/home_assistant.ts` — `cadence` query key + `useHomeAssistantCadence` hook
- `apps/ui/src/routes/integrations/types.ts` — 3 cadence type re-exports
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.tsx` — two cadence cards
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.test.tsx` — mock the new `useHomeAssistantCadence` hook

**Design decisions**

- Option A: one endpoint, two named collections (vs two endpoints or a flat `rows`+`kind` list) — single fetch for one widget, respects the named-collection convention, and the two distinct row models structurally encode D-CADENCE-SCRIPT-DISABLED (scripts have no `enabled` field).
- `enabled` field KEPT on `HaCadenceAutomationRow` (always true for returned rows after the disabled-exclusion change; removing it would be needless schema churn).
- Never-triggered automations (37 on this rig) are treated as idle (`age = None`, sorts first); LEFT JOIN on `enabled` as the authoritative universe (verified: 80 enabled series vs 43 last_triggered).
- friendly_name shown to the authenticated owner (D-ENRICH-PRIVACY); VM carries only entity_id.

**Verification**

`make verify` PASSED — backend 4920 pass / 1 skip / 100% kernel coverage (7 new cadence tests + empty-entity_id guard coverage); vitest 1541 pass (4 new drill tests + tab-test hook mock); pyright + ruff + tsc clean; eslint 94 warnings baseline / 0 errors. Backend validated against REAL HA data on the prod rig (3a+3b subsumed by the prod rig). Frontend Desktop + Mobile both approved. Opus code review (post-Refinement-logic-change pass): approved, 1 minor finding (stale `filtered_to` test-fixture literal) fixed.

**Commit:** (commit: 47471cc)

---

## [STAGE-005A-001] Host machine threshold + anomaly rules

**Summary**

Backfilled 11 host-machine vmalert alert rules (the first EPIC-005A stage) over metrics EPIC-001's host collector already emits but had no alerts. Rules in new `deploy/vmalert/metrics/host_anomalies.yaml`: critical CPU/mem tiers (>95%, above the existing >90% warnings), load-vs-core-count (`scalar(2x/4x * count(cpu))`), a swap+RAM AND-gate (`HostMemoryPressure`), zombie-process count, reboot detection (uptime<600s), and rolling-baseline anomaly rules for CPU, memory, disk-IO, and net throughput (the house `clamp_min(K * avg_over_time(...), floor)` idiom). All carry `source_tool: vmalert-metrics` + `target_kind: host`; severity vocabulary info|warning|error|critical.

Two pre-existing bugs were root-caused and fixed correctly in-scope (not deferred):
- **Memory-division label mismatch:** `homelab_host_memory_bytes{type="used"} / {type="total"}` matches nothing in MetricsQL (operands differ on `type`), so the division returned empty and the rule could never fire. Fixed with `/ ignoring(type)` on the 3 new memory rules AND on the pre-existing `HostHighMemory` in `host.yaml` — the host's primary memory alert had never fired in production.
- **Cumulative-counter inflation:** the host collector passed psutil's absolute cumulative counter to `write_counter()` (which `.inc()`s) every tick, inflating `homelab_host_disk_io_bytes_total` / `homelab_host_net_bytes_total` to ~31 PB (rate ~2 TB/s). Fixed by adding a new `write_counter_absolute` MetricsWriter method (Gauge-backed SET semantics) across the writer subsystem and switching the 4 host disk/net emit sites to it. Post-rebuild the metric reads ~22 TB (kernel truth) with sane MB/s rates.

**Modified files**

- `deploy/vmalert/metrics/host_anomalies.yaml` (NEW) — 11 alert rules.
- `deploy/vmalert/metrics/__tests__/host_anomalies.tests.yaml` (NEW) — forward-compatible shortened-window test mirror (fire/no-fire per rule; NOT CI-run).
- `deploy/vmalert/metrics/host.yaml` — fixed pre-existing `HostHighMemory` label-mismatch (`/ ignoring(type)`).
- `apps/monitor/homelab_monitor/kernel/plugins/io.py`, `.../kernel/metrics/prometheus_writer.py`, `.../kernel/metrics/multiplex.py`, `.../kernel/plugins/process_context.py` — added `write_counter_absolute` (Gauge-backed SET) across the MetricsWriter protocol + all implementors.
- `apps/monitor/homelab_monitor/plugins/collectors/builtin/host.py` — switched 4 disk/net emit sites to `write_counter_absolute`.
- `apps/monitor/tests/test_metrics_prometheus_writer.py`, `test_plugins_io.py`, `test_process_context.py`, `test_metrics_multiplex.py`, `test_host_collector.py`, `test_heartbeat_collector.py` — new/updated tests for set-semantics (proving SET not INC) + the 2 type-checked test doubles.

**Design decisions**

- D-CPU-TIERS: critical tiers in the new file; `host.yaml` warning rules frozen. D-LOAD-MULTIPLIER: 15m period, 2x/4x cores, `scalar(count(...))` for engine portability. D-SWAP: reformulated to a swap+RAM AND-gate (the box idles at 93% swap / 20% RAM, so static swap-fullness would false-fire). D-PROCESS: zombie-only (`>20`). D-REBOOT: absolute `uptime<600`. D-ANOMALY-FORM: `clamp_min` idiom with floors (CPU 3x/40, mem 2x/0.6, IO/net 3x/50 MB/s). D-IO-NET: authored in-scope (tuned in 3b), NOT deferred. D-TEST-EXECUTION: forward-compatible mirror + manual promtool/live-replay validation (vmalert has no unit-test mode).

**Verification**

`make verify` PASSED — backend 4927 pass / 1-2 skip (known-environmental) / 100% kernel coverage (new `write_counter_absolute` branches fully covered); vitest 1541 pass; pyright + ruff + tsc clean; eslint 94 warnings baseline / 0 errors. vmalert rules validated on the live prod rig: all 11 + the host.yaml fix load `health=ok`, zero false-fires at the real baseline. Fire/no-fire: 9 rules pass under promtool; the 2 rate-subquery rules (HostDiskIOAnomalous/HostNetThroughputAnomalous) validated via live-VictoriaMetrics replay (9/9 scenarios). Collector fix validated on the rebuilt rig: disk-IO 31 PB → 22 TB, rates sane. Opus code review: 2 passes (initial + post-`scalar()`-logic-change), all findings dispositioned.

**Commit:** (commit: 8e57b75)

---

## [STAGE-005A-002] Disk-fill rules (host filesystems + per-slot self-disk budgets)

**Summary**

Added the second EPIC-005A stage: 4 disk-fill alert rules in a new `deploy/vmalert/metrics/disk_fill.yaml`, closing the user-requested "tell me when a volume is running low" gap. Host filesystem fill (`/`, `/storage`) had NO alert at all; per-slot self-disk budgets (vm/sqlite/runbook_transcripts) were only aggregate-alerted. Rules:
- `HostDiskFillWarning` (warning) / `HostDiskFillCritical` (critical) — `100 * homelab_host_disk_bytes{type="used"} / ignoring(type) homelab_host_disk_bytes{type="total"} > 85/95`, per-mountpoint, for 15m/10m. The `ignoring(type)` is mandatory (used/total differ only on the `type` label → a bare division matches nothing).
- `SelfDiskSlotFillWarning` (warning) / `SelfDiskSlotFillCritical` (critical) — `100 * homelab_self_disk_used_bytes{slot!="vl"} / (homelab_self_disk_budget_bytes > 0) > 85/95`, per-slot, for 15m/10m. Native `{slot}` join (no ignoring — both metrics share the slot label); `{slot!="vl"}` avoids double-firing the dedicated `VictoriaLogsDisk*` rules; `(budget > 0)` guards divide-by-zero.

No recording rules; no edits to existing files. No projection rule (user decision).

**Modified files**

- `deploy/vmalert/metrics/disk_fill.yaml` (NEW) — 4 alert rules.
- `deploy/vmalert/metrics/__tests__/disk_fill.tests.yaml` (NEW) — forward-compatible test mirror (FIRE + NO-FIRE per rule + 2 vl-exclusion NO-FIRE cases; all 4 rules promtool-validatable).

**Design decisions**

D-VL-OVERLAP: exclude `slot="vl"` (keep the dedicated VictoriaLogsDisk* 70/85 rules). D-PER-SLOT-FORM: a single generic inline-join rule covers all non-vl slots (DRY; vl's recording-rule precedent was justified only by its different thresholds). D-THRESHOLDS: uniform 85/95% for host-fs and per-slot, no free-bytes floor (% is the standard "running low" signal). D-FOR-DURATION: 15m warn / 10m crit (calm capacity signals). D-RUNBOOK-SLOT: confirmed `runbook_transcripts` has its own distinct budget series, covered natively.

**Verification**

`make verify` GREEN — backend 4927 pass / 1 skip (known-environmental) / 100% kernel coverage; vitest 1541 pass; pyright/ruff/tsc clean. All 4 rules load `health=ok` on the live prod rig. Fire/no-fire: `promtool test rules disk_fill.tests.yaml` → SUCCESS (all 4 rules promtool-validatable). 3b on real data: zero false-fire at the real baseline (`/` 46.4%, `/storage` 9.4%, self-disk slots <3%, all well under 85/95%); exprs confirmed returning real values with `slot="vl"` excluded. Opus code review: APPROVED; 2 minor comment-accuracy findings fixed (test-block-header threshold phrasing + a stale annotation-omission note).

**Commit:** (commit: 7a5a37f)
