2026-06-29 20c9346 EPIC-009 / STAGE-009-003: fixer-runner container (Dockerfile + static CLAUDE.md + compose wiring)

Built the `fixer-runner` container: the dedicated low-privilege runtime that hosts the `homelab-fixer` identity and the `claude` CLI for the auto-fix subsystem. Ships a minimal `debian:12-slim` Dockerfile (no Node) that creates `homelab-fixer` from build-arg UID/GID (default 1002:1002, adopting the STAGE-009-002 contract), installs the version-pinned native `claude` binary (default channel `stable`, auto-update disabled) via a `real|fake` build-arg switch, ends `USER homelab-fixer` (never root, no docker group, no sudoers — non-negotiable #3), and uses an idle-keepalive PID 1 (`tail -f /dev/null`) so `claude` is launched only via `docker exec` and the container is killable mid-run with `docker kill` (non-negotiable #7). A baked public-safe `CLAUDE.md` floor (container/non-interactive/deny-by-default; no host-specific targets) plus a read-only mount point for a host-overrides overlay. Compose wiring adds the service under a new `fixer` profile (OFF by default), a read-write transcript mount (monitor keeps `:ro`), an `ANTHROPIC_API_KEY` env passthrough, and a dedicated `fixer-egress` network (no published ports). A fake-`claude` CI harness + a rig-gated integration test (`test_fixer_runner.py`) prove the non-interactive exec shape (argv passthrough, transcript written + owned 1002:1002) and that `docker kill` terminates an in-flight exec — never hitting the real Claude API.

Validation: full backend pytest gate green (6735 passed, 2 benign skips, 100.00% coverage); ruff/ruff-format/pyright --strict clean; `docker compose config` + `docker build` clean. Refinement validated against real Docker (integration test 2/2) and the live host: `scripts/host-setup.sh` §3.9 provisioned the transcript dir + ACLs (monitor UID 995 r-x, fixer 1002 rwx, defaults), a fixer-writes/monitor-reads round-trip confirmed cross-container ACL correctness, and the prod monitor was recreated to activate STAGE-009-002's `:ro` transcript mount.

Deferred (tracked): exec-time vault `ANTHROPIC_API_KEY` injection + the orchestrator launching `claude` → STAGE-009-005; per-runbook egress destination filtering + the host allow/deny target overlay content → STAGE-009-008.

---

2026-06-29 4e746b3 EPIC-009 / STAGE-009-002: homelab-fixer provisioning + transcript ACLs

HOST-INTEGRATION stage. Established the low-priv `homelab-fixer` identity contract + transcript-directory ACLs so the future fixer-runner (STAGE-009-003) can write audit transcripts the monitor can read but not tamper with. Compose/script/docs-only (no Python). Changes: (1) monitor mounts the host transcript bind-dir at `/data/runbook-transcripts` READ-ONLY (`:ro`) — non-negotiable #4 audit integrity; (2) `HM_FIXER_UID`/`HM_FIXER_GID` (default 1002:1002) + `HM_FIXER_TRANSCRIPTS_SRC` env-var contract for the identity STAGE-009-003 adopts; (3) `scripts/host-setup.sh` section 3.9 provisions the transcript dir + POSIX default ACLs (monitor `r-x` read-only, fixer `rwx` write) with a setfacl-absent WARN-degrade to shared-GID + setgid `2770`, blast-contained so a setfacl failure cannot abort the rest of host-setup; (4) new public `docs/deploy/autofix-provisioning.md`. Verified the orchestrator's docker-exec path against the live prod monitor (socket RW + docker GID + sibling exec all work — no re-provisioning needed). Real-host prod-path provisioning + the fixer-runner container + in-container user deferred to STAGE-009-003 (tracked). All seven non-negotiables verified by code-review (Opus); #3 identity + #4 audit are load-bearing here.

---

2026-06-29 2a400c8 EPIC-009 / STAGE-009-001: Runbook schema & config-file contract — additive migration 0045 (8 runbooks + 10 runbook_runs columns, alert_id→alerts FK, conservative server-defaults risk_tag='risky'/dry_run_required=1/enabled=0/auto_trigger=0) + file-authoritative RunbookConfig pydantic model (match_patterns, scoped_capabilities docker/ssh-read/egress requiring ≥1 scope, risk_tag/dry_run/rate-limit/cooldown) + canonical-config content hash. enabled/auto_trigger are DB-only operator gates (not author-declarable). pyright --strict, 100% branch coverage.

---

2026-06-29 da34d7f EPIC-008 / STAGE-008-033: Host-collector hardening — non-hanging disk-usage probe

Hardened `HostCollector` (`apps/monitor/homelab_monitor/plugins/collectors/builtin/host.py`) so its blocking `psutil.disk_usage` probe cannot hang the collector or leak unbounded threads on a wedged hard-NFS mount. Mirrors the STAGE-008-018 `synology_mount_health.py` idiom: the probe now runs in a module-level bounded `ThreadPoolExecutor` (max_workers=4) under `asyncio.wait_for(timeout=5s)`, gated by a cheap `/proc/self/mountinfo` presence-check (read once per tick) that skips a path not currently mounted (no thread spawned). On `(TimeoutError, OSError)` the mountpoint is skipped and the error recorded; the collector continues.

Preserves host.py's existing two-loop ok-semantics: a disk_usage timeout in the `disk_partitions` loop records an error (collector `ok=False`); a timeout in the `extra_mountpoints` loop is discarded (`ok=True`, mount skipped). NO metric-surface change — host.py still emits only `homelab_host_disk_bytes{mountpoint,type}` (no probe-duration metric). `psutil.disk_partitions`/`disk_io_counters` left unwrapped (out of scope — they don't stat a wedged mount).

Typing: psutil 7.x ships no stubs and `sdiskusage` is a functional namedtuple (un-nameable under pyright --strict), so the seam uses a local `_DiskUsage(NamedTuple)` (total/used) + a runtime-noop `cast` — no bare `Any`, no `# type: ignore`. Real-host validation (instance A): rebuilt prod image, host collector still emits disk bytes for `/` and `/storage`, runs in ~10 ms (no hang), zero failures.

Verification: ruff + pyright --strict clean; full backend pytest gate 6691 passed / 1 skipped / 100.00% branch coverage (host.py 236 stmts, 60 branches, 0 missed). Tests cover the mountinfo helpers, presence-check skip, and the timeout branch in BOTH loops (partition→ok=False, extra→ok=True). EPIC-008 closed by this stage.

---

2026-06-29 9f422d7 EPIC-008 / STAGE-008-031: Surveillance Station Grafana dashboard + global Metrics-page tab

Authored `deploy/grafana/dashboards/synology-surveillance.json` (uid `synology-surveillance`, schemaVersion 40) — the Synology Surveillance Station metric surface — and embedded it as a new "Surveillance" tab on the global Metrics page (mirrors the STAGE-008-030 Synology dashboard + tab).

Dashboard (Option C, hybrid 3-row): a top non-collapsed "Alerts at a glance" stat strip (cameras connected/disconnected [disconnected ≥1 = the lone red alarm], license used [yellow@81/red@90 vs max 90], HomeMode [off = muted/grey, normal — not an alert], events today) + 3 collapsed rows — Cameras (per-camera table: Connected/Status/FPS/Resolution/IP), Recordings & identity (per-camera table: Recordings/Size/Keep-days/Keep-size/Retention/Model/Vendor + aggregate stats + storage-growth timeseries), and License/HomeMode/events (license stats, the 5-member HomeMode family [all off = grey], events stats + trend, Surveillance Station version). No barcharts; honest empty/edge states throughout.

Per-camera tables join on the `camera` label via `joinByField` (not `merge`): `merge` joins on the full shared field set, so the `homelab_synology_ss_camera_info` label-carrier (id/ip/mac/model/vendor) split each camera into duplicate rows; `joinByField {byField:"camera"}` collapses them to one row per camera (Backyard/Doorbell/Driveway) with all columns — including IP/Model/Vendor — populated. (Fixed during Refinement after the duplicate-row bug surfaced in prod sign-off.)

Frontend: new `MetricsSurveillanceTab.tsx` (1-component `GrafanaDashboardEmbed`, kiosk, src `/api/grafana/d/synology-surveillance/synology-surveillance?kiosk`) + its vitest; `MetricsLayout.tsx` adds the "Surveillance" tab after "Synology"; `router.tsx` wires `metricsSurveillanceRoute` (path `/metrics/surveillance`). The dashboard JSON is provisioned-as-code (bind-mounted, refresh-only); the `.tsx` tab is baked into the prod monitor image (rebuilt `--no-cache`, sha256 served==built proven). `homelab_synology_ss_events_total` is the per-camera breakdown of `homelab_synology_ss_events_total_all` (documented in the aggregate panel's description, not separately charted).

Verification: `make ui-verify` green (0 eslint errors, tsc clean, vitest 1885/1885, UI build OK); dashboard JSON valid + provisions in prod Grafana without errors. No backend/Python change.

---

2026-06-29 cab5493 EPIC-008 / STAGE-008-030: Synology Grafana dashboard + global Metrics-page tab

Authored the Synology Grafana dashboard (provisioned-as-code) + embedded it as a "Synology" tab on the global Metrics page.

Dashboard (deploy/grafana/dashboards/synology.json, uid "synology", schemaVersion 40, 40 panels):
- A non-collapsed "Alerts at a glance" stat strip (Health, DSM update, Backup, Security, SSH up — inverted thresholds, noValue "—") + 7 collapsible rows: health/storage, disks & SMART, system & UPS, backup/replication, security, mount-health, SSH-probe.
- Panel mix: stat-glance + table-detail (per-disk/mount/share/severity tables via organize transforms) + timeseries (CPU load, CPU breakdown, Memory usage). NO barcharts. Every panel + target uses the victoriametrics datasource.
- Honest edges: health_ok=0 → red "ALERT"; disk_remain_life=-1 → "n/a" (not "0% life"); all homelab_ssh_* panels filtered target="synology"; no synology uptime panel (only a udm series exists). Memory usage is a timeseries matching CPU (Refinement request).

Metrics-page tab (global Metrics page, NOT a Synology-integration-page tab): new MetricsSynologyTab.tsx (GrafanaDashboardEmbed src /api/grafana/d/synology/synology?kiosk) + TABS entry (after Pi-hole) in MetricsLayout.tsx + metricsSynologyRoute in router.tsx + a vitest.

Gates: synology.json valid + Grafana provisions cleanly (0 errors, dashboard renders against live data); frontend make ui-verify 0 eslint errors / 0 tsc errors / 1884 vitest / build OK; backend no-op (no Python changed). Code review: 1 Critical (Disks "Util %" column joined on `disk` but the metric carries `device` → permanently blank; dropped the column) + Important findings (dead {{priority}} legend, Status/SMART value mappings, alert-strip noValue) implemented; clean 2nd review. One review finding (Memory/row-40 y-coordinate) was an overturned premise — verified the nested collapsed-row coordinates don't actually collide (Grafana repacks; dashboard loaded clean) → no change. The synology.json is refresh-only (provisioned-as-code); only the .tsx tab needed a rebuild + Desktop+Mobile prod viewport sign-off.
