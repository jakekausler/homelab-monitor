2026-06-29 cab5493 EPIC-008 / STAGE-008-030: Synology Grafana dashboard + global Metrics-page tab

Authored the Synology Grafana dashboard (provisioned-as-code) + embedded it as a "Synology" tab on the global Metrics page.

Dashboard (deploy/grafana/dashboards/synology.json, uid "synology", schemaVersion 40, 40 panels):
- A non-collapsed "Alerts at a glance" stat strip (Health, DSM update, Backup, Security, SSH up — inverted thresholds, noValue "—") + 7 collapsible rows: health/storage, disks & SMART, system & UPS, backup/replication, security, mount-health, SSH-probe.
- Panel mix: stat-glance + table-detail (per-disk/mount/share/severity tables via organize transforms) + timeseries (CPU load, CPU breakdown, Memory usage). NO barcharts. Every panel + target uses the victoriametrics datasource.
- Honest edges: health_ok=0 → red "ALERT"; disk_remain_life=-1 → "n/a" (not "0% life"); all homelab_ssh_* panels filtered target="synology"; no synology uptime panel (only a udm series exists). Memory usage is a timeseries matching CPU (Refinement request).

Metrics-page tab (global Metrics page, NOT a Synology-integration-page tab): new MetricsSynologyTab.tsx (GrafanaDashboardEmbed src /api/grafana/d/synology/synology?kiosk) + TABS entry (after Pi-hole) in MetricsLayout.tsx + metricsSynologyRoute in router.tsx + a vitest.

Gates: synology.json valid + Grafana provisions cleanly (0 errors, dashboard renders against live data); frontend make ui-verify 0 eslint errors / 0 tsc errors / 1884 vitest / build OK; backend no-op (no Python changed). Code review: 1 Critical (Disks "Util %" column joined on `disk` but the metric carries `device` → permanently blank; dropped the column) + Important findings (dead {{priority}} legend, Status/SMART value mappings, alert-strip noValue) implemented; clean 2nd review. One review finding (Memory/row-40 y-coordinate) was an overturned premise — verified the nested collapsed-row coordinates don't actually collide (Grafana repacks; dashboard loaded clean) → no change. The synology.json is refresh-only (provisioned-as-code); only the .tsx tab needed a rebuild + Desktop+Mobile prod viewport sign-off.
