## [STAGE-005A-008] Watched-directory size collector + config-driven mounts + alerts + Grafana panel

**Summary**

Added the eighth EPIC-005A stage — the only non-alerting-only stage: a new collector measuring recursive directory sizes for a config list of watched dirs, config-driven `:ro` mounts so the collector can see host dirs, absolute-threshold alerts, and a Grafana panel. The existing disk metrics report per-FILESYSTEM usage but cannot size an arbitrary DIRECTORY (e.g. `/var`, which lives on `/`); this fills that gap. New `WatchedDirSizeCollector` (own 30s timeout, 60s interval — isolated from HostCollector's 5s timeout so a slow `/var` walk can't drop host CPU/mem/disk metrics) emits three gauges per watched dir: `homelab_host_directory_bytes{path}` (recursive byte total, friendly host path label), `homelab_host_directory_walk_truncated{path}` (1 if the 25s deadline was hit), `homelab_host_directory_unreadable_subdirs{path}` (count of subdirs skipped — permission-denied or vanished mid-walk). The walk is sync `os.walk` run in `run_in_executor` (doesn't block the async loop), with a monotonic per-directory deadline, `st_dev` pruning (no cross-filesystem descent, `du -x` semantics), and `os.walk(onerror=...)` graceful skip. Baked default config: `/tmp` (warn 1 GiB / crit 4 GiB), `/var` (warn 10 GiB / crit 25 GiB).

**Modified files**

- `apps/monitor/homelab_monitor/plugins/collectors/builtin/watched_dir_size.py` (NEW) — the collector + `WatchedDirectory`/`WatchedDirSizeCollectorConfig`.
- `apps/monitor/homelab_monitor/kernel/watched_dirs.py` (NEW) — the mount collision validator + `python -m` CLI.
- `apps/monitor/tests/test_watched_dir_size_collector.py`, `apps/monitor/tests/test_watched_dirs_validator.py` (NEW) — 100%-coverage tests.
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — collector registration (mirrors self_disk block).
- `scripts/generate-watched-dirs-mounts.sh` (NEW) — generator → `deploy/compose/docker-compose.watched-dirs.yml`.
- `deploy/compose/docker-compose.watched-dirs.yml` (NEW) — committed default generated mounts (`/tmp`→`/host-watch/tmp:ro`, `/var`→`/host-watch/var:ro`), static header / idempotent.
- `Makefile` — `generate-watched-dirs-mounts` + `test-generate-watched-dirs` targets; added `-f docker-compose.override.yml -f docker-compose.watched-dirs.yml` to compose-build/up/down/logs + verify-ci `config -q`.
- `scripts/dev-up.sh` — same `-f` additions on prod-mode `up --build` + `exec`.
- `scripts/host-setup.sh` — operator restart hint now points to `make compose-up` (was a bare `docker compose up` that would miss the overrides).
- `deploy/vmalert/metrics/watched_directories.yaml` (NEW) + `__tests__/watched_directories.tests.yaml` (NEW) — 4 per-path warn/crit rules + 8-block promtool mirror.
- `deploy/grafana/dashboards/host-overview.json` — new bargauge panel id 10 "Watched directory sizes".

**Design decisions**

D-005A-008-SPLIT: ONE stage (not split). NEW-vs-EXTEND: a NEW collector class with its own 30s timeout (isolates the slow walk from HostCollector's shared 5s budget). D-005A-008-MEASURE: `os.walk`+`st_size` in `run_in_executor` (reuses the existing `dir_size_bytes` idiom; no new `du` subprocess dependency) with monotonic deadline + `st_dev` prune + `onerror` graceful skip. D-005A-008-MOUNT-NAMESPACE: container target `/host-watch/<name>` (strip leading `/`, `/`→`-`; reject `/`); emit the friendly host path label. D-005A-008-COLLISION-RULES: a Python validator (pytest-covered) the bash generator shells out to — rules: duplicate path, root rejection, derived-name collision, all error-and-abort. (A Build-phase bug was found+fixed: the originally-specced "contains/contained-in an EXISTING MOUNT TARGET" rules were a NAMESPACE ERROR — watched dirs mount into the ISOLATED `/host-watch/` namespace and can never shadow existing container mounts; they wrongly rejected the default `/var`; removed.) D-005A-008-THRESHOLD-DELIVERY: PER-PATH rule entries — chosen over a companion-metric `> on(path)` generic join to AVOID the recurring cross-metric empty-join trap (a dead rule). D-005A-008-CONFIG-MECHANISM: baked `watched_directories` default via `getattr` (STAGE-014 YAML loading not live). D-005A-008-PERMISSIONS: graceful skip via `os.walk(onerror=...)`, no host setfacl. COMPOSE WIRING (user-approved scope add, fix-pre-existing): compose auto-merge of `docker-compose.override.yml` is disabled under explicit `-f`, so the build-mounts override (`/storage/programs:ro`, EPIC-003) NEVER applied in prod — this stage explicitly `-f`'s both override files at the 7 prod sites, fixing that latent bug; compose APPENDS volume lists (base mounts preserved); merge order base→override→watched-dirs. A Build-phase generator bug was also fixed (process-substitution hid the validator's non-zero exit, silently writing a 0-entry file → now captured + aborts).

**Verification**

`make verify` GREEN — backend 4957 pass / 1 known-environmental skip (test_vector_template.py "vector binary not on PATH") / 100% kernel coverage; frontend 1541 vitest + tsc + UI build; ruff/pyright clean; eslint baseline warnings, 0 errors. `promtool check rules watched_directories.yaml` → SUCCESS (4 rules); `promtool test rules watched_directories.tests.yaml` → SUCCESS (8 FIRE+NO-FIRE). Generator idempotence: regenerate == committed (byte-for-byte). Compose 3-file merge: `config` shows `/host-watch/tmp`+`/host-watch/var` mounts + the now-applied `/storage/programs` build override + base mounts preserved. 3b prod rig (monitor rebuilt + recreated with all 3 `-f` files): `/host-watch/tmp`+`/host-watch/var` mounted in the running container; `/storage/programs` build-mounts override NOW applies (pre-existing bug fixed); `homelab_host_directory_bytes` emits /tmp≈6.98 GiB, /var≈7.51 GiB (within ~0.5% of `du -sx` ground truth); walk_truncated=0 both; unreadable_subdirs /tmp=375 (systemd private tmp), /var=57 (root-only) — graceful skip working. Alerts: WatchedDirectoryTmpWarn+TmpCrit FIRE on the genuine ~7 GiB /tmp (real detection, NOT a false-fire — `du`-confirmed; NOTE this host's /tmp genuinely exceeds 1G/4G, so raise the thresholds in config if 7 GiB is acceptable), VarWarn/VarCrit inactive (7.5 GiB < 10 GiB warn — correct); 4 rules health=ok. Grafana panel provisioned + present in the bind-mounted file + datasource resolves. Opus code review: APPROVED; 1 Important (host-setup.sh stale restart hint → `make compose-up`) + 3 Minor (walk_dir param shadow → renamed `cpath`; unreadable_subdirs docstring accuracy; cadence docstring consistency) all FIXED; second review pass confirmed all 4 fixes, no new defects.

**Commit:** (commit: 8447df1)

---

## [STAGE-005A-007] Build / config health + self-disk-shrink rules

**Summary**

Added the seventh EPIC-005A stage: build/config-health + self-disk-shrink alerts filling EPIC-001/003 self-metric gaps. New `deploy/vmalert/metrics/self_and_build.yaml` (group `self_and_build`, interval 30s) with 4 rules — every rule labeled `severity` + `source_tool: vmalert-metrics` + `target_kind`:
- `SelfDiskShrinkFired` (warning, `target_kind: self_disk`) — `increase(homelab_self_disk_shrink_total[10m]) > 0` for 0s, `{{ $labels.tier }}`. The monitor's self-disk hit >95% and the auto-shrink decision fired (a real COUNTER, label `tier`). Distinct from the existing `SelfDiskCritical` (the threshold crossing) — both fire by design at the 95% condition; dedupe deferred to Alertmanager.
- `BuildSourcesConfigLoadFailed` (warning, `target_kind: build_config`) — `homelab_build_sources_config_loaded == 0` for 5m. The build-sources config failed to load → the local-build-update feature is broken.
- `DockerComposeUnreadable` (warning, `target_kind: build_config`) — `homelab_docker_compose_readable == 0` for 5m. The monitor can't read the compose file → degraded Docker features.
- `BuildSourceHashSkipped` (info, `target_kind: build_config`) — `count by(reason,name)(homelab_build_source_hash_skipped_total) > 0` for 5m, `{{ $labels.reason }}`/`{{ $labels.name }}`. A local-build update check skipped computing the source hash (context_missing / context_too_large / permission_denied).

GAUGE-vs-COUNTER (the load-bearing distinction): `homelab_self_disk_shrink_total` is a real COUNTER (`write_counter`) → `increase()`. BUT `homelab_build_source_hash_skipped_total` is a GAUGE despite its `_total` suffix (`write_gauge` SET to 1.0 per `{reason,name}`) → the rule uses `count by(reason,name)(...) > 0` (presence-based), NOT `increase()`. LABEL CORRECTION (card hint was wrong): `homelab_build_sources_config_loaded` and `homelab_docker_compose_readable` carry NO metric-specific labels (only scrape-target instance/job/service) — the card hinted `{tier}`, which does not exist; the `== 0` rules and annotations reference no per-metric label. All 4 exprs are single-metric threshold/aggregate comparisons (no subqueries, no `label_value`, no cross-metric binary ops) → fully promtool-validatable, NO live-replay needed.

**Modified files**

- `deploy/vmalert/metrics/self_and_build.yaml` (new) — 4 alert rules.
- `deploy/vmalert/metrics/__tests__/self_and_build.tests.yaml` (new) — promtool test mirror, 8 blocks (FIRE + NO-FIRE per rule). The two absent-until-event metrics are planted: shrink counter via a rising `0 0 1 1 1` window (`increase()>0`); hash-skip gauge SET to 1.0 per `{reason,name}`. `count by(reason,name)` drops instance/job, so rule-4 exp_labels = {severity, source_tool, target_kind, reason, name}.

**Design decisions**

- D-005A-007-SHRINK-SEVERITY → `warning`, keep both SelfDiskShrinkFired + SelfDiskCritical firing (distinct signals; dedupe to Alertmanager). Rejected: critical (duplicate-urgency noise), rule-layer dedupe.
- D-005A-007-FOR-WINDOWS → `for: 5m` for both `== 0` health gauges (suppresses config-reload/file-swap blips). Rejected: `for: 0s`.
- D-005A-007-SKIPPED-SEVERITY → `info`, per-`{reason,name}` via `count by(reason,name)(...) > 0` (operator-fixable config issues, not emergencies). Rejected: warning, single aggregate (loses actionable labels).
- D-005A-007-GAUGE-SEMANTICS → confirmed `build_source_hash_skipped_total` is a gauge → `count by()`, not `increase()`; `self_disk_shrink_total` is a real counter → `increase()`.

**Verification**

`promtool check rules self_and_build.yaml` → SUCCESS (4 rules). `promtool test rules __tests__/self_and_build.tests.yaml` → SUCCESS (all 8 FIRE/NO-FIRE blocks) — satisfies Refinement 3a. `make verify` → PASS (backend 4928 pass / 2 environmental skips / 100% kernel coverage; frontend 1541 vitest pass, tsc clean, build smoke pass; pyright 0, ruff clean; YAML-only change, no Python/UI touched). Refinement 3b (prod rig): reloaded the bind-mounted file; all 4 rules `health=ok`/empty `lastError`; no parse errors; NO false-fire at the real baseline (build_sources_config_loaded=1, docker_compose_readable=1 live; shrink + hash-skip metrics absent). Opus code review: APPROVED, zero findings.

**Commit:** (commit: 394b9a6)

---

## [STAGE-005A-006] Logging + Drain anomaly rules

**Summary**

Added the sixth EPIC-005A stage: log/Drain pipeline anomaly alerts filling the EPIC-004 logging gaps. New `deploy/vmalert/metrics/log_drain_health.yaml` with 5 rules (all `source_tool: vmalert-metrics`, `category: log-anomaly`, + per-rule `anomaly_kind`/`target_kind`):
- `CardinalityBudgetExceeded` (warning) — `homelab_log_signature_cardinality_warn == 1` for 5m. A service's distinct-signature count exceeded its budget (instance-level gauge, no per-service label).
- `DrainStall` (CRITICAL) — `increase(homelab_drain_cycle_duration_seconds_count[20m]) == 0 and on() (sum(homelab_log_stream_lines_per_sec) > 1)` for 5m. The Drain log-clustering loop ran zero cycles in 20m while logs are still arriving — the whole log-anomaly detection layer is blind.
- `DrainNoProgress` (warning) — `increase(homelab_drain_cycle_lines_total[20m]) == 0 and increase(homelab_drain_cycle_duration_seconds_count[20m]) > 0 and on() (sum(...) > 1)` for 5m. Cycles run but process zero lines (loop alive but stuck); the middle guard makes it mutually exclusive with DrainStall.
- `MetricFamilyDroppedSeries` (warning) — `homelab_metric_family_dropped_series > 0` for 5m, `{{ $labels.family }}`. The cardinality guard is shedding a family's series; operator is now blind to that family (meta-monitoring).
- `ServiceLogVolumeSpike` (warning) — `sum by (service_key)(sum_over_time(homelab_log_signature_count[5m])) > clamp_min(4 * avg_over_time((...)[7d:5m]), 50)` for 10m, `{{ $labels.service_key }}`. Per-service total log-signature volume spiked vs its own 7-day baseline.

THE `on()` GATE (Build-time refinement): the drain-stall rules gate on "logs arriving" via `sum(homelab_log_stream_lines_per_sec) > 1`. A bare `and sum(...)` is a VictoriaMetrics-only empty-label broadcast that promtool CANNOT evaluate (promtool's `and` needs label-set match; `sum()` yields an empty-label `{}` series). Changing the gate to `and on() (sum(...) > 1)` makes it work in BOTH engines (verified live) and keeps the LEFT operand's labels — so DrainStall/DrainNoProgress are PROMTOOL-VALIDATABLE (not live-replay-only). Only ServiceLogVolumeSpike (the `[7d:5m]` subquery) is live-replay-only.

`homelab_log_signature_count` is a per-cycle GAUGE → the volume rule uses `sum_over_time` (NOT `rate` — `_total` is also a gauge, not a counter). The drain runs on a FIXED 5-minute interval, so the stall window is `[20m]` (4 cycles of headroom; `[15m]` would be too tight and false-fire on a single slow/skipped cycle after a restart). The too-slow drain rule was DROPPED (Design finding — no SLA, noisiest signal). The LogsQL absolute-threshold spike form was REJECTED (noisy across order-of-magnitude-different per-service baselines); no `deploy/vmalert/logs/log_anomalies.yaml` was created. No double-fire with ServiceErrorRateSpike (error-rate axis), NewLogSignature, or SignatureWentSilent (distinct axes).

**Modified files**

- `deploy/vmalert/metrics/log_drain_health.yaml` (NEW) — 5 rules (group `log_drain_health`, interval 30s).
- `deploy/vmalert/metrics/__tests__/log_drain_health.tests.yaml` (NEW) — promtool mirror: FIRE+NO-FIRE for RULES 1-4 (CardinalityBudgetExceeded/DrainStall/DrainNoProgress/MetricFamilyDroppedSeries, all promtool-validatable incl. the on()-gated rules); RULE 5 (ServiceLogVolumeSpike, `[7d:5m]` subquery) commented as live-replay-only with the planted-series recipe preserved.

**Design decisions**

D-005A-006-CARDINALITY-WARN: `== 1` for 5m, warning. D-005A-006-DRAIN-STALL-FORM: ship DrainStall (critical) + DrainNoProgress (warning, mutually exclusive via the `increase(count) > 0` guard); `[20m]` window (drain's 5-min cadence + buffer); logs-arriving gate `and on() (sum(homelab_log_stream_lines_per_sec) > 1)`; too-slow rule dropped. D-005A-006-DROPPED-SERIES: `> 0` for 5m, warning, per-family (`for:` is the noise filter, not a count threshold). D-005A-006-SPIKE-FORM: per-service adaptive-baseline metrics rule (K=4×, floor=50, [7d:5m] subquery, live-replay-only); LogsQL absolute-threshold form rejected; single metrics file (no logs-rule file). Build-time: `on()` gate adopted to make RULES 2/3 promtool-validatable.

**Verification**

`make verify` GREEN — backend 4929 pass / 1 known-environmental skip (test_vector_template.py "vector binary not on PATH") / 100% kernel coverage; frontend 1541 vitest + tsc + UI build; ruff/black clean; eslint minimal warnings (baseline), 0 errors. (Stage adds only vmalert YAML — no Python/UI impact.) `promtool test rules log_drain_health.tests.yaml` → SUCCESS (RULES 1-4 FIRE+NO-FIRE, including DrainStall/DrainNoProgress via the on() gate); `promtool check rules log_drain_health.yaml` → SUCCESS (5 rules). 3a live-replay on a disposable VM: DrainStall fires on a planted flat-count stall + silent when the count rises; DrainNoProgress fires + mutually exclusive with DrainStall; ServiceLogVolumeSpike fires for a 300/5m service (threshold ~86), quiet service at 40 silent (floor 50). 3b real prod rig: all 5 rules health=ok, empty lastError; 4 of 5 silent at the real baseline (#1 criterion); ServiceLogVolumeSpike fired for homelab-grafana + homelab-vmagent — investigated and CONFIRMED a genuine sustained step-change (full 7d history; both stepped up ~20-30× at ~14:47 UTC and held; homelab-monitor steady at 1.24× correctly NOT firing) — the rule working as designed, NOT a false-fire. Opus code review: APPROVED, 3 Minor convention-notes dispositioned no-action (subquery duplication matches error_rate_spike idiom; 30s interval matches log_anomaly/signature_silent convention; fixture-not-CI-wired matches every sibling __tests__).

**Commit:** (commit: 60c9078)

---

## [STAGE-005A-005] Cron / heartbeat run-duration anomaly

**Summary**

Added the fifth EPIC-005A stage: cron run-DURATION anomaly alerts. EPIC-002's heartbeat monitoring already covers staleness/overdue/failure/flapping (`HeartbeatStale_*`, `HeartbeatFailed`, `CronRunFailed`, `HeartbeatFlapping`, `WrapperPossiblyStale`); the gap was a wrapped cron job suddenly running far longer than usual (hung/degraded) while still succeeding. Two complementary rules over `homelab_heartbeat_last_duration_seconds{wrapper_installed="yes"}` (all `source_tool: vmalert-metrics`, `target_kind: cron`, `routing_channel: cron-health`, severity warning):
- `CronRunDurationAnomalous` (rolling-baseline degradation) — `homelab_heartbeat_last_duration_seconds{wrapper_installed="yes"} > clamp_min(3 * avg_over_time(same[1d]), 120)` for 15m. House anomaly idiom; floor=120s is the load-bearing anti-false-fire guard (keeps the variable rtlamr-watchdog.sh's 74s natural max silent). Promtool-validatable.
- `CronRunDurationNearCadence` (cadence-relative hang backstop) — `homelab_heartbeat_last_duration_seconds{wrapper_installed="yes"} > 0.5 * label_value(same, "cadence_seconds")` for 15m. A job eating ≥50% of its own cadence interval is anomalous; robust for both the rock-stable daily backup.sh and the highly-variable 5-min rtlamr-watchdog.sh, and immune to the persisted-gauge baseline problem.

The two rules live in SEPARATE files: `cron_duration.yaml` (rule 1) and `cron_duration_cadence.yaml` (rule 2). Rule 2 uses VictoriaMetrics-only `label_value()` (reads the `cadence_seconds` label as a numeric value), which promtool CANNOT parse — and promtool rejects an ENTIRE rule file containing an unparseable function, which would block rule 1's promtool test. Splitting keeps rule 1 promtool-validatable; both files load independently in prod vmalert (the `-rule=.../*.yaml` glob is per-file; vmalert supports label_value — confirmed health=ok live).

`homelab_heartbeat_last_duration_seconds` is a PERSISTED LAST-VALUE GAUGE (re-emitted every 30s until the next run), so for the daily job `avg_over_time[1d]` ≈ the current value (a near self-comparison) — the rolling-baseline rule is the weaker signal and the cadence-relative rule is the robust hang detector. The [1d] window is the minimum that can hold two runs of a daily job.

The too-fast anomaly rule was DROPPED (Design finding, decided-not-to-build): rtlamr-watchdog.sh legitimately runs at 0.16× its average on every OK check, making any too-fast threshold unconditionally noisy; the one meaningful too-fast case (backup.sh finishing in a minute) is already covered by the existing heartbeat staleness/failure rules.

Also corrected a stale comment carried in three rule files: the severity-vocabulary header said `info / warning / error / critical`, but `error` was removed as an alert severity in STAGE-005A-003. Fixed in the two new files plus the pre-existing `heartbeats.yaml` (the prior commit left it stale there).

**Modified files**

- `deploy/vmalert/metrics/cron_duration.yaml` (NEW) — RULE 1 CronRunDurationAnomalous (rolling-baseline, promtool-validatable).
- `deploy/vmalert/metrics/cron_duration_cadence.yaml` (NEW) — RULE 2 CronRunDurationNearCadence (cadence-relative, VM-only label_value, live-replay-only).
- `deploy/vmalert/metrics/__tests__/cron_duration.tests.yaml` (NEW) — promtool mirror: RULE 1 FIRE (100s baseline ×2880 + 500s spike, eval 1456m → value 500) + NO-FIRE (flat 100s → silent); RULE 2 live-replay recipe preserved as a commented block.
- `deploy/vmalert/metrics/heartbeats.yaml` — corrected the stale `error`-in-severity-vocab header comment (pre-existing, fixed per code review).

**Design decisions**

D-005A-005-BASELINE: two complementary rules — cadence-relative (`0.5 * label_value(..., "cadence_seconds")`, live-replay-only) as the robust hang detector + rolling-baseline (`clamp_min(3 * avg_over_time([1d]), 120)`, promtool-validatable) for gradual degradation; floor=120s keeps rtlamr's 74s natural max silent; window [1d] (min to hold two daily runs). D-005A-005-TOO-FAST: dropped (noise on rtlamr's natural fast exits; backup-too-fast already covered by staleness rules). D-005A-005-SCOPE: `wrapper_installed="yes"` only (native on the metric; no cadence filter, no per-job exclusion). Build-time file split: rule 2 isolated because promtool can't parse label_value().

**Verification**

`make verify` GREEN — backend 4929 pass / 1 known-environmental skip (test_vector_template.py "vector binary not on PATH") / 100% kernel coverage; frontend 1541 vitest + tsc + UI build; ruff/black clean; eslint ~41 warnings (baseline), 0 errors. (Stage adds only vmalert YAML — no Python/UI impact.) `promtool test rules cron_duration.tests.yaml` → SUCCESS (RULE 1); `promtool check rules cron_duration.yaml` → SUCCESS (1 rule); `heartbeats.yaml` → SUCCESS (11 rules, comment fix didn't break it); cron_duration_cadence.yaml → valid YAML. 3a live-replay on a disposable VM: RULE 1 fires 500 / silent 100; RULE 2 fires backup@50000 (>43200), silent rtlamr@30 and @74 (<150), fires rtlamr@200 (>150). 3b real prod rig: both rules health=ok, empty lastError, inactive, NO false-fire at the real baseline (#1 criterion); CronRunDurationNearCadence loaded health=ok confirming vmalert accepts label_value(). Opus code review: APPROVED, all Minor findings (stale severity-vocab comments + test arithmetic precision) dispositioned.

**Commit:** (commit: 6161216)

---

## [STAGE-005A-004] Container lifecycle + resource rules + probe enablement

**Summary**

Added the fourth EPIC-005A stage: container lifecycle/resource alerts plus first-party probe enablement. EPIC-003 shipped the Docker collector, cadvisor, and a fully-built per-container probe subsystem, but none of those signals were alerted and no probes were configured (so `homelab_probe_up` was empty and `DockerProbeFailing` was dormant). New `deploy/vmalert/metrics/container_lifecycle.yaml` with 6 rules (all `source_tool: vmalert-metrics` + `target_kind: docker_container` + `integration: docker`):
- `ContainerRestartLoop` (warning, for 0m) — `increase(homelab_container_restart_count[1h]) > 3`. Container restarting repeatedly.
- `DockerImageUpdateCheckSkipped` (info, for 0m) — `homelab_image_update_check_skipped == 1`. Registry image-update check skipped (rate-limit/cooldown); per the locked epic decision this alerts on the SKIPPED signal, not `rate_limit_remaining`. Absent-until-event metric.
- `ContainerHighCpuShare` (warning, for 15m) — `rate(container_cpu_usage_seconds_total{name!=""}[5m]) / count(homelab_host_cpu_percent{cpu!="all"}) > 0.5`. One container using >50% of host cores.
- `ContainerHighMemoryShare` (warning, for 15m) — `container_memory_working_set_bytes{name!=""} / on() group_left() homelab_host_memory_bytes{type="total"} > 0.5`. One container using >50% host RAM.
- `ContainerCriticalMemoryShare` (critical, for 10m) — same join `> 0.85`. One container using >85% host RAM (OOM risk).
- `DockerProbeSlow` (warning, for 5m) — `homelab_probe_duration_seconds > 2`. Complements the existing `DockerProbeFailing`.

Container CPU/mem alerts are SHARE-OF-HOST (locked epic decision option b): no per-container limits exist (`container_spec_memory_limit_bytes`/`machine_cpu_cores` absent), so % is computed against host total cores / host total RAM. The long `for:` durations are the anti-false-fire guard (healthy baseline is tiny — largest container ~1% of host RAM).

PROBE ENABLEMENT: added `homelab-monitor.http.health` Docker labels to four first-party sidecars in `deploy/compose/docker-compose.yml` (monitor `http://container:9090/api/healthz`, grafana `http://container:3000/api/health`, victoriametrics `http://container:8428/health`, alertmanager `http://container:9093/-/healthy`). These are host-agnostic and ship in the public repo, so `homelab_probe_up` emits for every user and `DockerProbeFailing`/`DockerProbeSlow` go live. New `docs/admin/docker-probes.md` documents the first-party probes plus the operator host-override path (`homelab-monitor.<kind>.<name>` labels or `/config/plugins/docker/<container>.yaml` override files).

Two card deliverables were dropped as Design FINDINGS (not deferrals — evaluated and determined the rules should not exist):
- **Bad-exit-code rule DROPPED:** `homelab_container_last_exit_code` is a persisted gauge holding the LAST exit code (history, not a live event); it would false-fire forever on any container whose last exit was non-clean (live: gm-redis=128, campaign_playwright=1). The crash EVENT is already alerted by `ContainerCrashed`.
- **Stuck-non-running rule DROPPED:** "should be running" is not derivable from any metric (no restart-policy metric) and host-specific container names cannot ship in the public repo. A configured probe IS the open-source-safe "expected up" declaration — `DockerProbeFailing` covers it.

`fixture.yaml` (`FixtureHostHighCPU`) was KEPT (the card's "remove leaked fixture" premise was incorrect): it is an intentional test-only artifact used by `apps/monitor/tests/integration/test_canonical_e2e.py` as the canonical end-to-end smoke test; it stays inactive in prod because `fixture_cpu_percent` never exists there.

**Modified files**

- `deploy/vmalert/metrics/container_lifecycle.yaml` (NEW) — 6 alert rules.
- `deploy/vmalert/metrics/__tests__/container_lifecycle.tests.yaml` (NEW) — promtool test mirror (FIRE+NO-FIRE for R1/R2/R6; FIRE for R4/R5; R3 commented out as live-replay-only with the planted-series recipe preserved — its `count()`-scalar-broadcast denominator returns got:[] under promtool).
- `deploy/compose/docker-compose.yml` — added `labels:` blocks (first in the file) to monitor/grafana/victoriametrics/alertmanager enabling first-party probes.
- `docs/admin/docker-probes.md` (NEW) — probe enablement + host-override documentation.
- `docs/dev/local-environment.md` — reference to docker-probes.md.

**Design decisions**

D-005A-004-EXIT-ALLOWLIST: bad-exit-code rule dropped (persisted-gauge = history, not event; ContainerCrashed covers it). D-005A-004-RUNNING-WHITELIST: stuck-non-running rule dropped (no restart-policy metric; host names can't ship public; DockerProbeFailing is the open-source-safe expected-up signal). D-005A-004-RESOURCE-FRAC: share-of-host CPU warn >0.5 (15m), mem warn >0.5 (15m), mem critical >0.85 (10m); long `for:` as anti-false-fire guard. D-005A-004-PROBE-SCOPE: enable probes via public compose http labels on 4 first-party sidecars + document the /config override path; include DockerProbeSlow; NO 004a/004b split (probe subsystem already built in EPIC-003 → nothing to build). D-005A-004-FIXTURE-CLEANUP: keep fixture.yaml (intentionally test-only, used by test_canonical_e2e.py).

**Verification**

`make verify` GREEN — backend 4929 pass / 1 known-environmental skip (test_vector_template.py "vector binary not on PATH") / 100% kernel coverage; pyright/tsc/ruff clean; UI 1541 pass. `promtool test rules container_lifecycle.tests.yaml` → SUCCESS (R1/R2/R4/R5/R6). 3a live-replay on a disposable VM: R3 fires at 0.5625 (9 of 16 cores); R4 fires (memhog 0.6, silent for small 0.1); R5 fires (memhog2 0.9, silent for 0.6). 3b on the real prod rig: all 6 rules load `health=ok` with empty lastError and NO false-fire at the real baseline (the #1 acceptance criterion). Probe enablement dormant→live confirmed: after recreating the 4 labeled containers `homelab_probe_up` went 0→4 series all=1, durations 2–10ms (well under the 2s DockerProbeSlow threshold); DockerProbeFailing/DockerProbeSlow correctly silent. Opus code review: APPROVED, no findings (annotation labels cross-checked against probe_supervisor.py; override-path doc claim verified against lifespan.py/override_schema.py/label_parser.py).

**Commit:** (commit: dbe8b6f)

---

## [STAGE-005A-003] Collector health rules

**Summary**

Added the third EPIC-005A stage: collector self-monitoring alerts in a new `deploy/vmalert/metrics/collector_health.yaml`. The collector framework emits `homelab_collector_run_*` self-metrics for 31 collectors but none were alerted. 4 rules (all `source_tool: vmalert-metrics` + `target_kind: collector`, keyed on `name`):
- `collector_failure_rate_elevated` (warning, for 15m) — `sum without(reason)(rate(failure_total[15m])) / (rate(success_total[15m]) + sum without(reason)(rate(failure_total[15m]))) > 0.5`. Catches a collector failing >50% of runs (unhealthy but below the quarantine budget).
- `collector_silent_death` (critical, for 30m) — `rate(success_total{name!~"noop|image_update_checker"}[1h]) == 0`. Fast-cadence collectors (~30 run ≤5min); ~90min detection.
- `collector_silent_death_slow` (critical, for 30m) — `rate(success_total{name=~"image_update_checker"}[12h]) == 0`. The slow ~6-7h registry checker; [12h] window > cadence so it never false-fires; ~12.5h detection.
- `collector_duration_anomaly` (info, for 15m) — mean run duration `> clamp_min(3 * avg_over_time(mean[6h:15m]), 1)` (no histogram buckets exist → mean-via-sum/count; per-collector 6h baseline).

`last_error_age_seconds` evaluated and intentionally NOT alerted (redundant with the failure-rate rule — documented D-005A-003-LAST-ERROR-AGE).

Two bugs found + fixed during the stage:
- **Build — label mismatch in the failure-rate rule:** `failure_total` carries `{name,reason}`, `success_total` carries `{name}`; a bare `rate(success)+rate(failure)` matched nothing → empty → rule never fired. Fixed with `sum without(reason)(...)` collapsing failures to per-`{name}` (one alert per collector; the STAGE-005A-001 label-mismatch class).
- **Refinement — chronic false-fire on `image_update_checker`:** a single `[1h]` silent-death rule flagged this healthy collector, which runs every ~6-7h by design (rate-limited registry checks). Fixed by splitting into the fast/slow cadence-class pair above (the slow [12h] rule covers it without false-firing).

**Severity note:** the two silent-death rules were initially authored as `severity: error`, but the runtime `Severity` enum only supports `info|warning|critical` (`error` from any rule is silently coerced to `warning` at ingest — a latent system-wide bug). Per user decision: (1) the silent-death rules use VALID severities — a fully-dead collector is page-now → `critical` (vs `collector_failure_rate_elevated` warning, `collector_duration_anomaly` info); AND (2) `error` was removed as an alert severity EVERYWHERE in the codebase (not just this stage) — see the separate "[CHORE] Remove `error` as a valid alert severity" entry below. No `severity: error` remains in any vmalert rule file, Python alert-severity Literal, or the frontend.

**Modified files**

- `deploy/vmalert/metrics/collector_health.yaml` (NEW) — 4 alert rules.
- `deploy/vmalert/metrics/__tests__/collector_health.tests.yaml` (NEW) — test mirror (FIRE + NO-FIRE per rule; noop + image_update_checker exclusion cases; slow-rule [12h] cases; rule 4 by-design-empty under promtool).

**Design decisions**

D-005A-003-FAILURE-RATE: failure-ratio > 0.5 (cadence-normalized; catches unhealthy-not-quarantined). D-005A-003-SILENT-DEATH: cadence-class split (fast [1h] / slow [12h]) after Refinement found the single-window form false-fires on the 6-7h collector. D-005A-003-LAST-ERROR-AGE: dropped as redundant. D-005A-003-DURATION: per-collector mean-duration baseline anomaly (no histogram buckets). Silent-death severity = critical (user decision; error not a valid enum member).

**Verification**

`make verify` GREEN — backend 4926 pass / 2 known-environmental skips / 100% kernel coverage; pyright/tsc/ruff clean; UI 1541 pass. All 4 rules load `health=ok` on the live prod rig. Fire/no-fire: `promtool test rules collector_health.tests.yaml` → SUCCESS (rules 1+2a+2b promtool-validatable; rule 4 the `[6h:15m]` subquery validated via live-VictoriaMetrics replay — fires on a mean-duration spike, silent below the 1s floor). 3b on real data: zero false-fire at the real baseline (image_update_checker no longer flagged after the cadence-class split); rules confirmed watching real data (31 success series, 30 mean-duration series). Opus code review: 2 passes (initial + post-severity-change), all findings dispositioned. KNOWN EDGE: the slow rule's [12h] window reads 0 until 12h of history accrues — a one-time ~12.5h post-deploy window where it could fire spuriously (accepted/documented).

**Commit:** (commit: 5840efb)

---

## [CHORE] Remove `error` as a valid alert severity (codebase-wide)

**Summary**

Discovered during STAGE-005A-003 code review: the `Severity` enum (`kernel/alerts/types.py`) defines only `info|warning|critical`, but the spec §8.2 referenced `error` and ~9 vmalert rules (across prior epics) + several Python Literals + the alert-create form still used/allowed `error`. At ingest, `_resolve_severity` silently coerced `error` → `warning`, so every `error`-severity alert was degraded (stored/displayed as warning). Per user decision, `error` is removed as a valid ALERT severity everywhere (the canonical vocab is `info|warning|critical`); LOG-LINE `error` (the syslog level — signature ranking, log histogram buckets, LogsQL `severity:error` query tokens, log-viewer tint) is a separate concept and was left untouched.

**Severity remap (rules):** rules whose `error` middle tier would collapse to duplicate an existing warning sibling were bumped to `critical`; standalone error rules → `warning` (or `critical` if inherently page-now):
- → `critical`: `HaBatteryCritical`, `KernelOOM`, `SelfDiskError` (sibling SelfDiskWarn is warning), `HeartbeatStale_Error` (sibling HeartbeatStale_Warning is warning).
- → `warning`: `HaConfigEntryError`, `HaAutomationScriptFailure`, `HaIntegrationSetupFailure`, `HeartbeatFailed`, `CronRunFailed`.
Alert NAMES (SelfDiskError, HeartbeatStale_Error, etc.) and annotation prose were NOT changed (renaming breaks Alertmanager fingerprints/silences); only the `labels.severity` value changed.

**Modified files**
- vmalert rules (severity label only): `deploy/vmalert/metrics/home-assistant.yaml`, `deploy/vmalert/metrics/self_disk.yaml`, `deploy/vmalert/metrics/heartbeats.yaml`, `deploy/vmalert/logs/home-assistant.yaml`, `deploy/vmalert/logs/system.yaml`, `deploy/vmalert/logs-test/home-assistant.yaml`, `deploy/vmalert/logs-test/system.yaml`, `deploy/vmalert/metrics/__tests__/home-assistant.tests.yaml`.
- Python (drop `error` from alert-severity Literals/allowlist): `kernel/logs/user_rules_repo.py` (VALID_SEVERITIES), `kernel/api/schemas.py` (3 LogUserRule Literals), `kernel/api/routers/logs.py` (cast Literal), `kernel/plugins/types.py` (SuggestionEvent + AlertForwardEvent).
- DB migration: `apps/monitor/alembic/versions/0043_user_rule_error_severity_to_critical.py` (NEW — `UPDATE log_user_rules SET severity='critical' WHERE severity='error'`) + `apps/monitor/tests/test_db_migration_0043.py` (NEW — exercises the migration's real upgrade() on a pre-existing error row).
- Tests: `tests/test_log_user_rules_repo.py` + `tests/test_log_user_rules_api.py` (error now REJECTED — UserRuleValidationError / HTTP 422).
- Frontend: `apps/ui/src/components/logs/CreateAlertModal.tsx` (Zod enum drops 'error') + its test + `apps/ui/src/routes/alerts/__tests__/ManageRulesTab.test.tsx`.
- `packages/shared-types/openapi.json` (regenerated — 3 LogUserRule severity enums drop "error"; the core `Severity` enum was already info/warning/critical, unchanged).

**Verification**

`make verify` GREEN — backend 4928 pass / 2 known-environmental skips / 100% kernel coverage; pyright 0; UI tsc + 1541 tests + build pass. Live rig: both vmalert instances show ZERO error-severity rules; the changed rules show their new warning/critical severities, health=ok. Audit: zero `severity: error` LABELS remain in `deploy/vmalert/` (the `severity:error` LogsQL query tokens are correctly preserved). Migration 0043 round-trip tested. Opus code review (final pass over the full change set): approved, 1 minor test-quality finding (migration test now exercises the real upgrade()) fixed.

**Commit:** (commit: 5840efb)
