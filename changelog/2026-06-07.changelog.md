## [STAGE-004-031A] "Show surrounding logs" in the Logs Explorer

**Summary:** From a selected log line in the Logs Explorer, reconstruct incident context as an in-place "surrounding-logs mode" showing ~100 lines before + the anchor + ~100 after, with bidirectional accumulating load-older/newer, anchor highlighting, exit, and re-anchor. Completes the model→explorer→surrounding-logs debug flow (step 3 after STAGE-030/031).

**Key changes:**
- Backend `GET /api/logs/window` (anchor-centered, two-sided fetch via a `LogWindowFetcher` singleton; merge + dedup + `anchor_index`).
- `LogWindowFetcher.fetch()` reworked to oversample + sort ascending + keep the N lines NEAREST the anchor (side-aware), since VictoriaLogs returns newest-first and one-sided windows otherwise dropped anchor-adjacent lines.
- `LogWindowFetcher` singleton wired in lifespan + test lifespan; `get_log_window_fetcher` DI dependency; `LogWindowResponse` schema.
- Frontend in-place surrounding-logs mode in `LogsExplorerBody` (replaced the original modal): enter from the Field Inspector, exit button, bidirectional accumulating scroll, re-anchor, anchor highlight via the backend `anchor_index`.
- Surrounding window uses `expr='*'` (ignores the Explorer's active filter) so it shows full context regardless of the active query.
- Scope defaults to All services; only-this-service narrows only when the line has a raw `fields.service` + real `source_type` (never emits a dead `source_type:"unknown"` clause).
- `min-w-max` on the log row so the selected/hover highlight spans the full line width (also fixes normal-mode selection).
- Template "Open in Explorer" (`templateToLogsQl`) anchors on the single longest literal run instead of AND-chaining every segment (fixes empty results when a trailing template segment carried a non-printable ANSI byte). Applied to ModelsDebugPage + SignatureDetailPage.

**Files:**
- Backend: apps/monitor/homelab_monitor/kernel/logs/log_window_fetcher.py; kernel/api/routers/logs.py; kernel/api/schemas.py; kernel/api/dependencies.py; kernel/api/lifespan.py; tests/_test_lifespan.py; tests/test_log_window_fetcher.py; tests/test_api_logs_window.py (new); tests/integration/test_log_window_fetcher_integration.py; tests/integration/test_api_logs_window_integration.py (new).
- Frontend: apps/ui/src/routes/logs/LogsExplorerBody.tsx; components/logs/FieldInspectorPanel.tsx; components/logs/LogLineList.tsx; api/logs.ts; lib/logsQlTranslate.ts; routes/logs/ModelsDebugPage.tsx; routes/logs/SignatureDetailPage.tsx; + tests (LogsExplorerBody.test.tsx new, FieldInspectorPanel.test.tsx, api/__tests__/logs.surrounding.test.ts new, lib/__tests__/logsQlTranslate.test.ts, routes/logs/__tests__/ModelsDebugPage.test.tsx, explorerStatePersistence.test.tsx).
- Contract: packages/shared-types/openapi.json (new /api/logs/window endpoint + LogWindowResponse).

**Tests:** Backend two-sided window endpoint (mocked + real-VL integration), LogWindowFetcher side-aware-selection unit + integration regression (>limit lines), template longest-run translation, FE in-place mode (enter/exit/re-anchor/load-more/scope/anchor-highlight-with-divergent-identity). make verify GREEN: 100% backend kernel coverage, 3955 backend / 1212 FE tests, pyright 0 errors, eslint 0 errors.

**Implementation:** 7965060

## [STAGE-004-032] Container crash log correlation

**Summary:** Poll-based ContainerCrashReconciler (30s tick) detects docker containers that exited with a non-zero code, fetches a ~60s VictoriaLogs window anchored on the container's State.FinishedAt, persists crash enrichment rows (deduped on UNIQUE (logical_key, finished_at)), emits the counter `homelab_container_crash_total`, fires a vmalert rule, and renders the enrichment as a "Recent crashes" section in the Container Overview tab.

**Key changes:**
- New poll-based `ContainerCrashReconciler` (30s tick) mirroring CronRunReconciler; detection is poll-based because there is no container-stop event stream (the docker /events consumer ignores die/stop). Crash anchor = State.FinishedAt (with a stable `unknown:<container_id>` dedup key fallback when FinishedAt is absent, preventing an alert storm).
- New `container_crash_enrichments` table (migration 0036) + `targets_docker.finished_at` column; new `CrashEnrichmentsRepository` with INSERT OR IGNORE dedup + age/per-container prune.
- docker_socket_collector + ContainerState TypedDict extended to persist State.FinishedAt.
- Two new API endpoints: GET /api/integrations/docker/containers/{name}/crashes (summaries) and /{crash_id} (detail with log lines).
- New config CrashLogConfig + load_crash_log_config (HOMELAB_MONITOR_CRASH_LOG_WINDOW_S, HOMELAB_MONITOR_CRASH_ENRICHMENT_RETENTION_DAYS).
- vmalert rule deploy/vmalert/metrics/container_crash.yml (ContainerCrashed; fires on increase(homelab_container_crash_total[5m])>0; labels severity:warning, source_tool:vmalert-metrics, category:container).
- UI: RecentCrashesSection in ContainerOverviewTab — lists crashes, expands to render the captured log window via the unified <LogViewer> (with timestamps) + OpenInExplorer deep-link. Fixed LogLineList to apply min-w-max only when NOT wrapping (so wrapped log lines no longer overflow horizontally on mobile — benefits all LogViewer callers).
- Migration 0036 downgrade rebuilds targets_docker to its pre-0036 shape (drops finished_at) so cross-migration round-trips stay clean.

**Files:**
- Backend: apps/monitor/homelab_monitor/kernel/containers/container_crash_reconciler.py (new); kernel/containers/crash_enrichments_repo.py (new); kernel/config.py; kernel/targets_repository.py; kernel/collectors/docker_socket_collector.py; kernel/api/routers/docker.py; kernel/api/lifespan.py; alembic/versions/0036_container_crash_enrichments.py (new); tests/test_db_migrations.py; tests/test_container_crash_reconciler.py (new); tests/test_crash_enrichments_repo.py (new); tests/integration/test_container_crash_reconciler_integration.py (new); tests/integration/test_api_docker_crashes_integration.py (new).
- Frontend: apps/ui/src/components/docker/RecentCrashesSection.tsx (new); api/docker.ts; routes/integrations/ContainerOverviewTab.tsx; components/logs/LogLineList.tsx; components/docker/__tests__/RecentCrashesSection.test.tsx (new); api/__tests__/docker.test.ts (new); routes/integrations/__tests__/ContainerOverviewTab.test.tsx.
- Deploy: deploy/vmalert/metrics/container_crash.yml (new).
- Contract: packages/shared-types/openapi.json.

**Tests:** 4 new backend test files (100% coverage of new kernel modules), 2 new + 1 edited FE test files; backend e2e validated live on the prod rig (8/8 crash-chain checks). All `make verify` green (3994 backend passed, 100% kernel coverage, 1221 vitest passed).

**Implementation:** cebae37

## [STAGE-004-033] Healthcheck-failure log enrichment

**Summary:** Poll-based ContainerHealthcheckReconciler (30s tick) detects docker containers whose healthcheck has transitioned into "unhealthy", fetches a ~30s/30s VictoriaLogs window anchored on the transition time, persists healthcheck enrichment rows (deduped per episode), emits the counter `homelab_container_healthcheck_unhealthy_total`, fires a vmalert rule, and renders a "Recent healthcheck incidents" section in the Container Overview tab. Parallels STAGE-004-032 (crash enrichment).

**Key changes:**
- New poll-based `ContainerHealthcheckReconciler` (30s tick) — reads unhealthy containers with a stamped `healthcheck_changed_at`, enriches via LogWindowFetcher, dedups on (logical_key, healthcheck_changed_at).
- Collector transition detection: docker_socket_collector's upsert (targets_repository.py) extended to stamp NEW `targets_docker.healthcheck_changed_at` + `previous_healthcheck` columns on a transition INTO unhealthy (healthy→unhealthy, starting→unhealthy, first-sight-unhealthy with previous=null), preserving the stamp on non-edge ticks via the CASE-WHEN idiom. One stamp per episode; unhealthy→healthy→unhealthy re-stamps a new episode.
- NEW table container_healthcheck_enrichments (migration 0037; ALSO adds the 2 targets_docker columns; downgrade rebuilds targets_docker dropping them). NEW HealthcheckEnrichmentsRepository (INSERT OR IGNORE dedup, age + per-container prune).
- Two endpoints GET /api/integrations/docker/containers/{name}/healthcheck-incidents[/{incident_id}].
- NEW config HealthcheckLogConfig + load_healthcheck_log_config (HOMELAB_MONITOR_HEALTHCHECK_LOG_WINDOW_S, HOMELAB_MONITOR_HEALTHCHECK_ENRICHMENT_RETENTION_DAYS).
- vmalert deploy/vmalert/metrics/container_healthcheck.yml (ContainerUnhealthy; increase(homelab_container_healthcheck_unhealthy_total[5m])>0; labels severity:warning, source_tool:vmalert-metrics, category:container).
- UI: RecentHealthcheckIncidentsSection in ContainerOverviewTab (transition string + captured log window via unified <LogViewer> + OpenInExplorer); useContainerHealthcheckIncidents/Detail hooks.

**Files:**
- Backend: apps/monitor/homelab_monitor/kernel/containers/container_healthcheck_reconciler.py (new); kernel/containers/healthcheck_enrichments_repo.py (new); kernel/config.py; kernel/targets_repository.py; kernel/api/routers/docker.py; kernel/api/lifespan.py; alembic/versions/0037_container_healthcheck_enrichments.py (new); tests/test_db_migrations.py; tests/test_container_healthcheck_reconciler.py (new); tests/test_healthcheck_enrichments_repo.py (new); tests/integration/test_container_healthcheck_reconciler_integration.py (new); tests/integration/test_api_docker_healthcheck_incidents_integration.py (new); tests/test_targets_repository.py (transition tests).
- Frontend: apps/ui/src/components/docker/RecentHealthcheckIncidentsSection.tsx (new); api/docker.ts; routes/integrations/ContainerOverviewTab.tsx; components/docker/__tests__/RecentHealthcheckIncidentsSection.test.tsx (new); api/__tests__/docker.test.ts (new); routes/integrations/__tests__/ContainerOverviewTab.test.tsx.
- Deploy: deploy/vmalert/metrics/container_healthcheck.yml (new).
- Contract: packages/shared-types/openapi.json.

**Tests:** 4 new backend test files (100% coverage of new kernel modules) + 4 transition-detection cases in test_targets_repository + 2 new/1 edited FE test files; backend validated live on the prod rig (8/8 healthcheck-chain checks). make verify green (4033 backend passed, 100% kernel coverage, 1232 vitest).

**Implementation:** 81e4ac2

## [STAGE-004-034] Cron run failure log correlation

**Summary:** Cron run failure log correlation — when a cron run completes with state=fail, the existing CronRunReconciler's enrich loop now fetches the last ~50 lines of that run from VictoriaLogs (exact run_id selector), persists a row in a new cron_run_failure_enrichments table (deduped per (cron_fingerprint, run_id)), emits counter homelab_cron_run_failure_total, and fires a new vmalert rule CronRunFailed that deep-links to the cron run page. BACKEND-ONLY (no new UI — the deep-link targets the existing CronRunLogViewer). Parallels STAGE-004-032/033 but hooks into existing cron machinery rather than a standalone correlator.

**Key changes:**
- Failure-enrich step hooked INTO the existing CronRunReconciler._enrich per-run loop (gated on state=="fail"); threads ctx.vm (MetricsWriter) through run()→_enrich to emit the counter; degraded-VL still inserts + emits; per-run try/except isolation.
- NEW table cron_run_failure_enrichments (migration 0038, simple downgrade — drop table + indexes); NEW CronRunFailureEnrichmentsRepository (INSERT OR IGNORE dedup on (cron_fingerprint, run_id), age + per-fingerprint prune). The reconciler's _prune phase now ALSO prunes this table on its own 30d retention horizon (independent of cron_runs' prune — D-CRON-RETAIN-30D).
- Capped run-scoped VL fetch: last ~50 lines via the exact run_id selector (build_amode_query/build_bmode_query over the run's vl_window), a small-limit VictoriaLogsClient.
- NEW config fields on CronRunReconcilerConfig: cron_failure_enrich_max_lines (50), cron_failure_enrich_retention_days (30, env HOMELAB_MONITOR_CRON_ENRICHMENT_RETENTION_DAYS), cron_failure_enrich_max_rows_per_cron (100).
- NEW endpoint GET /api/crons/{fingerprint}/runs/{run_id}/failure-enrichment + CronRunFailureEnrichmentResponse + get_cron_run_failure_repo DI (wired on app.state in lifespan + _test_lifespan + conftest per-test-db fixture).
- NEW vmalert rule CronRunFailed in deploy/vmalert/metrics/heartbeats.yaml (NOT a new cron.yml): increase(homelab_cron_run_failure_total[5m])>0; labels severity:error, source_tool:vmalert-metrics, category:cron, target_kind:cron, routing_channel:cron-health; deep-links to /inventory/crons/{cron_fingerprint}/runs/{run_id}.

**Files:**
- Backend: apps/monitor/homelab_monitor/kernel/cron/cron_run_reconciler.py (the _enrich hook + vm threading + _prune failure-prune), kernel/cron/cron_run_failure_enrichments_repo.py (new); kernel/config.py; kernel/api/routers/crons.py; kernel/cron/schemas.py; kernel/api/dependencies.py; kernel/api/lifespan.py; tests/_test_lifespan.py; tests/conftest.py; alembic/versions/0038_cron_run_failure_enrichments.py; tests/test_db_migrations.py + new test files (test_cron_run_failure_enrichments_repo.py, test_api_cron_failure_enrichment.py, extended test_cron_run_reconciler.py + test_config.py).
- Deploy: deploy/vmalert/metrics/heartbeats.yaml.
- Contract: packages/shared-types/openapi.json.

**Tests:** new repo test + extended cron reconciler test (failure-enrich cases incl. the prune-the-failure-table wiring guard) + new endpoint test + config tests; both new modules + the reconciler at 100% coverage; backend validated live on the prod rig (9/9 cron-failure-chain checks). make verify green (4059 backend passed, 100% kernel coverage, 1232 vitest). Note: a Finalize code-review caught + fixed a Critical defect — the new table's prune was implemented but never invoked from the reconciler (retention env was inert); fixed + guarded by a mutation-proven reconciler test.

**Implementation:** 1f69f58

## [STAGE-004-035] Anomaly Type A: New signature detected

**Summary:** A new-log-signature anomaly alert. A backend collector (`NewSignatureCollector`) reads the `log_signatures` catalog every 60s and emits a gauge `homelab_log_signature_new{service_key, template_hash, severity}=1` for signatures that are first-seen within a configurable window (default 300s) AND not suppressed AND whose first-seen severity is in a configurable in-scope set (default error/critical/warning). A dumb vmalert-metrics rule `NewLogSignature` fires on `homelab_log_signature_new == 1`, carrying `category: log-anomaly` + `anomaly_kind: new_signature`. Suppression is folded into the collector's per-tick decision (no separate gauge) for atomicity, satisfying D-SUPPRESSED-SIGNATURES-NEVER-ALERT; the gauge is emitted via `replace_family` so aged-out / suppressed / out-of-scope signatures self-resolve.

**Key changes:**
- New migration `0039_log_signature_first_seen_severity` adds a nullable `first_seen_severity` column to `log_signatures` (column-add → table-rebuild downgrade; all 3 indexes recreated). EXPECTED_TABLES unchanged (column-add, not table-add).
- `signature_sync.py` persists `first_seen_severity` on INSERT only (preserved on UPDATE, like `first_seen_at`), choosing the highest-severity-wins representative when a signature's first cycle spans multiple severities (rank: critical > error > warning > notice > info > debug > unknown; ties alphabetical).
- New `NewSignatureCollector` (BaseCollector) — pure DB reader; decides window/severity/suppression in Python; emits `homelab_log_signature_new` via duck-typed `replace_family` + the mandatory `homelab_collector_run_new_signature{phase, result}` self-metric; registered + config-wired in `lifespan.py`.
- New config `NewSignatureConfig` + `load_new_signature_config()` — `HOMELAB_MONITOR_*` env, window clamped >= 1, default severities {error, critical, warning}, empty-env fallback to defaults.
- New vmalert-metrics rule `deploy/vmalert/metrics/log_anomaly.yaml` (`NewLogSignature`, `expr: homelab_log_signature_new == 1`, `for: 0m`) + a forward-compatible test fixture under `__tests__/`.
- DECISION (severity label): the rule's alert-level `severity: warning` collides with the gauge's line-`severity` label; in the firing alert Prometheus auto-renames the series label to `exported_severity` and the annotation `{{ $labels.severity }}` renders the LINE severity (validated live in prod e2e — summary shows the line severity, e.g. "(error)"). Accepted as-is (not renamed to `line_severity`); the line severity remains recoverable via `exported_severity` + the annotation text. Alert routing keys off `severity=warning` (the operational severity of a new-pattern event) by design.

**Files:**
- `apps/monitor/alembic/versions/0039_log_signature_first_seen_severity.py` (new)
- `apps/monitor/homelab_monitor/kernel/metrics/new_signature_collector.py` (new)
- `apps/monitor/homelab_monitor/kernel/logs/signature_sync.py`
- `apps/monitor/homelab_monitor/kernel/logs/signatures_repo.py`
- `apps/monitor/homelab_monitor/kernel/config.py`
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py`
- `deploy/vmalert/metrics/log_anomaly.yaml` (new)
- `deploy/vmalert/metrics/__tests__/log_anomaly.tests.yaml` (new)

**Tests:**
- `apps/monitor/tests/test_new_signature_collector.py` (new, 13 tests, 100% collector coverage — full decision table + self-metric on all paths + replace_family self-resolution + error-tick fail-safe that does NOT wipe a prior family)
- `apps/monitor/tests/test_signature_sync.py` (first_seen_severity persist-on-INSERT, preserve-on-UPDATE, highest-severity-wins, deterministic ranking)
- `apps/monitor/tests/test_signatures_repo.py` (first_seen_severity round-trip read)
- `apps/monitor/tests/test_db_migrations.py` (0039 upgrade/downgrade/re-upgrade round-trip; column + all 3 indexes survive)
- `apps/monitor/tests/test_config.py` (NewSignatureConfig defaults / window clamp / severity parsing / empty fallback)
- Validated live on BOTH dev rig (3a) and prod rig (3b): 5/5 e2e scenarios (new-error emits gauge + scrapes into VM; suppressed disappears + alert resolves; info out-of-scope absent; old outside-window absent; full chain NewLogSignature fires → Alertmanager).

**Implementation:** 0c5cbd1

## [STAGE-004-036] Anomaly Type B: Signature count spike vs baseline

**Summary:** A signature-count-spike anomaly rule, shipped as a parameterized vmalert metrics-rule TEMPLATE + a pure reusable Python render function. A rendered instance fires when a signature's windowed line-count exceeds `multiplier × max(rolling-baseline, min_baseline)`, using a 7-day rolling baseline for mature signatures and a 1-hour baseline during cold-start (signature younger than 7 days). Carries `category: log-anomaly` + `anomaly_kind: signature_spike`. This stage ships the TEMPLATE only — per-signature INSTANCES are rendered by STAGE-042 (persistence) and instantiated by STAGE-044 (UI) on user demand (D-USER-CURATED-RULE-INSTANCES); the `.tmpl` is never loaded live (vmalert globs `*.yaml`).

**Key changes:**
- New pure render function `render_signature_spike_rule(*, template_hash, service_key, multiplier=5, window="5m", min_baseline=10) -> str` (`kernel/logs/signature_spike_render.py`) — embeds the rule template as a `Final[str]` constant, substitutes `__UPPER_SNAKE__` sentinels via `str.replace()` (avoids colliding with vmalert's own `{{ $labels }}` / `{{ $value }}` Go-template syntax and with `string.Template`'s `$`), no DB/IO. Reusable by STAGE-042.
- Spike PromQL (corrected against the real metric): current volume = `sum_over_time(homelab_log_signature_count[window])` (NOT `increase()` — `_count` is a per-cycle GAUGE, not a counter); baseline = `avg_over_time(sum_over_time(...)[7d:window])` with a `max(baseline, min_baseline)` FLOOR for gap-robustness (the gauge is gappy — quiet cycles emit no sample, so a tiny noisy baseline can't hair-trigger). Cold-start gated on `time() - homelab_log_signature_first_seen_ts/1e9` (nanoseconds → seconds) vs 604800. Two age-guarded branches OR'd (`and` binds tighter than `or` → `(spike∧mature) ∨ (spike∧cold)`).
- Input validation: rejects `"`, `\`, AND any ASCII control char (newline/CR/tab) in template_hash/service_key (they land inside PromQL `"..."` quotes within the rendered YAML — a control char breaks out of both); multiplier>=1; min_baseline>=1; window matches `^\d+[smhd]$`. Alert slug `SignatureSpike_<sanitized_service_key>_<template_hash[:8]>` (always a valid alertname; raw labels disambiguate any slug collision).
- New template artifact `deploy/vmalert/metrics/signature_spike.yml.tmpl` (byte-identical to the embedded constant — a drift-guard test asserts equality) + a forward-compatible static fixture `__tests__/signature_spike.tests.yaml` (NOT CI-run).
- Added a `[tool.ruff.lint.per-file-ignores]` entry (`signature_spike_render.py` = `["E501"]`) — the embedded PromQL template lines are inherently long, same rationale as the existing `alembic/versions/*` E501 ignore.
- NO migration, NO openapi/Pydantic/route, NO collector, NO lifespan/config change.

**Files:**
- `apps/monitor/homelab_monitor/kernel/logs/signature_spike_render.py` (new)
- `deploy/vmalert/metrics/signature_spike.yml.tmpl` (new)
- `deploy/vmalert/metrics/__tests__/signature_spike.tests.yaml` (new)
- `apps/monitor/pyproject.toml` (per-file E501 ignore)

**Tests:**
- `apps/monitor/tests/test_signature_spike_rule.py` (new, 19 tests, 100% render-module coverage — render+parse, structural label/for asserts, `{{ $labels }}`/`{{ $value }}` collision regression, both cold-start guards + `/1e9` + `[7d:`/`[1h:`, min-baseline floor in BOTH branches, all validation-rejection paths incl. quote/backslash/control-char/multiplier/min_baseline/window, slug-collision-disambiguated-by-labels, valid-alertname-for-adversarial-keys, and `.tmpl`-matches-constant drift guard).
- Validated live (Refinement): a manually-rendered instance LOADED in vmalert (`health: ok` — PromQL accepted/evaluable) and FIRED on a planted spike (cold-start branch); steady-state did not fire. Mature-branch 7d-baseline live-fire deferred (impractical to backfill 7d) but its PromQL was accepted by vmalert.
- 2nd code review mutation-tested: 11/11 mutations killed; the C1 control-char injection fix has full teeth.

**Implementation:** fba8cb3

## [STAGE-004-037] Anomaly Type C: Service-wide error rate spike

**Summary:** Per-service error-rate spike detection. A stateless `LogErrorRateCollector` (a builtin BaseCollector) queries VictoriaLogs every 60s with ONE LogsQL `(severity:error OR critical OR fatal OR _msg:~"<patterns>") | stats by (service) count()` and emits `homelab_container_error_rate{name=<service>}` as a GAUGE (per-60s-window error count). A vmalert-metrics rule `ServiceErrorRateSpike` fires when a service's rate exceeds `clamp_min(5 × its 7-day rolling baseline, 10)` — both the 5× baseline AND the 10/min static floor must be cleared (D-STATIC-FLOOR-AND-BASELINE); the 10/min floor doubles as gap-robustness. Carries `category: log-anomaly` + `anomaly_kind: error_rate_spike`. Satisfies docker requirement #4. Backend-only (alert → Karma).

**Key changes:**
- New stateless `LogErrorRateCollector` (`plugins/collectors/builtin/log_error_rate.py`) — pure function of the VL response (no accumulator → restart-safe); self-builds a `VictoriaLogsClient` lazily from `ctx.http` + `HOMELAB_MONITOR_VL_URL` + `load_vl_query_limits()`; parse-skips rows with missing service / None / non-int count; VL-query or http-unavailable → `ok=False` (scheduler records `homelab_collector_run_failure_total`).
- New `LogsConfig` + `load_logs_config()` + `ErrorPattern(kind, regex)` + `DEFAULT_ERROR_PATTERNS` (panic/[Tt]raceback/[Ee]xception) + `ErrorRateOverride` in `kernel/config.py`, reading the `logs.error_patterns` section of the operator YAML (mirrors the `RedactPattern`/`load_redact_patterns` precedent). `error_rate_overrides` is parsed-but-UNUSED in v1 (reserved for STAGE-042).
- New vmalert-metrics rule `deploy/vmalert/metrics/error_rate_spike.yaml`.
- Deferred (documented): per-pattern `_pattern` metric, per-service override APPLICATION, per-service cold-start fallback (the 10/min floor covers young-service false-fires).
- NO migration, NO openapi/Pydantic/route, NO DB change.

**⚠️ TWO production bugs caught + fixed during LIVE Refinement (synthetic gates missed both):**
- **Rule file extension:** the rule was named `error_rate_spike.yml` but vmalert-metrics globs `-rule=/etc/vmalert/metrics/*.yaml` (`.yaml` ONLY) → it silently never loaded. Renamed to `error_rate_spike.yaml`. (INVARIANT: all vmalert-metrics rule files must end `.yaml`. NOTE: the pre-existing `container_crash.yml`/`container_healthcheck.yml` from STAGE-032/033 have the SAME latent bug and also do not load in prod — out of this stage's scope, flagged for follow-up.)
- **MetricsQL label collapse:** the expr `> max(5*avg_over_time(...),10)` NEVER fired — `max(vector, 10)` is an AGGREGATION that collapses all series to empty labels `{}`, so the `>` against the `{name}`-labeled gauge matched nothing (and aggregated baselines across services). Fixed to `> clamp_min(5*avg_over_time(...),10)` — element-wise, per-series, preserves `{name}`. Verified live against VM (original returned `[]` with gauge=716; clamp_min returns per-`{name}` series and the alert fires).

**Files:**
- `apps/monitor/homelab_monitor/plugins/collectors/builtin/log_error_rate.py` (new)
- `deploy/vmalert/metrics/error_rate_spike.yaml` (new)
- `deploy/vmalert/metrics/__tests__/error_rate_spike.tests.yaml` (new, forward-compat fixture, NOT CI-run)
- `apps/monitor/homelab_monitor/kernel/config.py` (LogsConfig + load_logs_config + ErrorPattern + DEFAULT_ERROR_PATTERNS + ErrorRateOverride + __all__)
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py` (collector register block)

**Tests:**
- `apps/monitor/tests/test_log_error_rate_collector.py` (new, 13 tests, 100% collector coverage — gauge-per-service emission, parse-skips incl. empty-string + non-int + missing-service + None count, empty result, VL-error→ok=False, http-unavailable→ok=False, `_resolve_client` from ctx.http, query assembly with/without/escaped patterns).
- `apps/monitor/tests/test_config.py` (load_logs_config: defaults, error_patterns + error_rate_overrides parse/null/not-list/entry-not-mapping/missing-field/non-numeric/bool-floor).
- Validated LIVE on the prod rig (3b): the collector emits per-service from REAL homelab error logs; a planted error burst FIRED ServiceErrorRateSpike (state firing, correct labels, reached Alertmanager); a below-floor service did not fire.
- 2nd code review mutation-tested: 12/12 mutations killed across the collector + config modules.

**Implementation:** 68841f5
