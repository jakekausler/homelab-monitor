2026-07-07 f8e7be4 EPIC-010 / STAGE-010-004: One-shot backfill CLI for existing alert_outcomes

Retrofits ~14k pre-STAGE-010-003 alerts by inferring their outcomes deterministically from column state. Enables transition from pure-Karma silence workflow to outcome-tracked model for EPIC-001..009 + 017, meeting EPIC-010 Decision 3 (locked 2026-07-05). Guards against re-execution; fully idempotent after first run.

**Approach:**

New `hm alerts backfill-outcomes` CLI subcommand (registered in `alerts` subparser) performs two-pass query + SQLAlchemy executemany INSERT. Generates `uuid7()` per row on Python side (SQLite lacks built-in `uuid7`). All work runs in a single `async with repo.transaction() as conn:` block. Guard check — moved INSIDE write transaction after code review — eliminates split-transaction race with concurrent invokers.

**Guard semantics:**

Refuses to re-run if any row with `decided_by='backfill'` exists (one-shot safety). Reconciler rows (`decided_by='reconciler'`) do NOT trigger refusal — critical correction of stale stage card wording (written pre-STAGE-010-003 when `alert_outcomes` was empty). Overlap between reconciler AUTO_RESOLVED rows and backfill AUTO_RESOLVED rows silently handled by `ON CONFLICT (alert_id, outcome) DO NOTHING`.

**Prod-side execution (2026-07-07T11:49Z):**

- Pre-run: 14,151 outcome rows, ALL `decided_by='reconciler'`. Zero backfill rows. Zero ACKED candidates in `alerts.ack_at`.
- Dry-run: reported 0 ACKED + 14,154 AUTO_RESOLVED candidates.
- Real run: exit 0, wrote 3 NEW rows (14,151 candidates already covered by reconciler and silently skipped via ON CONFLICT). Both provenances now coexist.
- Audit_log row written with correct `who`, `what`, `before=None`, `after={acked_count, auto_resolved_count, total, at, backfill_source}`.
- Guard second-run refusal verified (exit 1 with clear error).

**Code review (Opus):**

8 findings: 6 addressed, 2 declared no-ops. HIGH-severity: guard check moved from separate read transaction into write transaction (eliminates concurrent-invoker race). Efficiency: guard SQL changed from `COUNT(*)` to `EXISTS(...)`; dry-run uses `COUNT(*)` instead of materializing rows. Spec-conformance: progress-line off-by-one fixed with `i < count` guard. UX: guard-tripped error now names exact `DELETE` command to reset. Test-coverage: new test `test_dry_run_after_prior_backfill_row_still_reports_counts`. Skipped (already correct): Ctrl-C atomicity via single-transaction rollback; test-seed NULLability (schema-level nullable).

**Verification:**

`make verify` fully green — 7413 backend tests + 199 UI test files (2038 UI tests), 100.00% backend coverage (29,571 statements + 6,668 branches, all covered). Ruff, ruff format, pyright, UI build all clean.

**Deliverables:**

- MODIFIED: `apps/monitor/homelab_monitor/cli/alerts.py` — extended `alerts` subparser with new `backfill-outcomes` subcommand. Added `_cmd_backfill_outcomes` async handler, 5 new module constants (`_EXPECTED_BACKFILL_CONFIRM_PHRASE`, `_BACKFILL_WHO`, `_BACKFILL_WHAT`, `_BACKFILL_DECIDED_BY`, `_BACKFILL_PROGRESS_INTERVAL`), 5 new SQL text constants (guard EXISTS, two COUNT queries for dry-run, two SELECT queries for real run, one INSERT with ON CONFLICT DO NOTHING).
- NEW: `apps/monitor/tests/test_cli_alerts_backfill_outcomes.py` — 12 test functions covering dry-run counts, confirm-phrase validation (missing/wrong), successful real run + audit, zero-matches path, guard second-run refusal, guard does-NOT-trip on reconciler-only rows, mixed-state alert mapping (ack_at wins), both progress-line branches (ACKED and AUTO_RESOLVED, each with 10,001-row fixtures), argparse registration, `_handle` dispatch routing, and dry-run-after-prior-backfill.
- No schema changes (no new alembic migration; uses existing `alert_outcomes` table + `uq_alert_outcomes_alert_id_outcome` UNIQUE index from migration 0053 in STAGE-010-003).
- No frontend changes (CLI+BACKEND stage only).

---

2026-07-07 5ef4aea EPIC-010 / STAGE-010-003: Karma → outcome periodic reconciliation

Implements hourly periodic reconciliation of Karma silence outcomes back into the
`alert_outcomes` table, closing the feedback loop for stateless Karma-driven silence
decisions. Adds new reconciler collector, silences HTTP client, database uniqueness
constraint, and idempotence fix for outcome insertion.

**Deliverables:**

- NEW: `apps/monitor/homelab_monitor/kernel/analyzer/karma_outcome_reconciler.py` —
  `KarmaOutcomeReconciler` collector; periodic hourly tick; fetches active silences
  from Alertmanager via new `SilencesClient`, extracts `alert_id` matcher, writes
  outcomes to repository with idempotent `INSERT ... ON CONFLICT DO NOTHING`
  semantics.
- NEW: `apps/monitor/homelab_monitor/kernel/alertmanager/silences_client.py` —
  `SilencesClient` thin HTTP wrapper; typed `Silence` / `Matcher` dicts; calls AM
  `/api/v2/silences`; used by reconciler to read current active silences.
- NEW: Alembic migration `0053_epic010_alert_outcomes_provenance_and_unique.py` —
  widen `alert_outcomes.decided_by` from `INTEGER` to `TEXT` (accommodate string
  user IDs); add `UNIQUE (alert_id, outcome)` index (enforces one outcome per alert,
  prevents silent duplicate writes).
- EDIT: `AlertRepository.insert_outcome()` and `AutofixOrchestrator._insert_outcome_conn()`
  — add `ON CONFLICT DO NOTHING` (fixes latent double-write regression triggered by
  new UNIQUE index).
- EDIT: `insert_outcome()` signature: `decided_by: int | None → str | None`; two
  routers/alerts.py callers updated to `str(user.id)` coercion.
- NEW: `AlertRepository.insert_outcome_if_absent()` — convenience method; idempotent
  wrapper; audit logs only on real insert (not on conflict).
- NEW tests: 25 tests for `KarmaOutcomeReconciler` (setup/teardown, silence fetch,
  matcher extraction, outcome insert, idempotency, error paths, scheduler
  registration); 1 new test in `test_alerts_repository.py` (`test_insert_outcome_is_idempotent_on_repeat_call`).

**Design decisions:**

- Reconciler runs hourly (CRON `0 * * * *`), aligns with Alertmanager silence UI
  polling cadence; not real-time (acceptable for user-facing decisions).
- Outcome sourced from silence `comment` field (structured as `{"decided_by": "admin",
  "reason": "..."}`) — matches STAGE-010-002 Refinement findings.
- Idempotent UNIQUE constraint prevents double-writes; ON CONFLICT DO NOTHING is
  silent (no error), matches auto-fix safety model preference for fail-safe patterns.
- `decided_by` TEXT not INT (accommodates future non-user sources: systems, CI, etc.).

**Prod-side Refinement (executed live on instance A):**

- Rebuilt monitor image; recreated container; all 14 sidecars preserved.
- Reconciler tick executed; wrote 13,973 real `alert_outcomes` rows (0 → 13,973);
  zero errors; ~31s tick duration (dominated by silence fetch + row insert I/O).
- Verified idempotency: second tick wrote 0 new rows (UNIQUE index rejected duplicates
  silently). Audit log shows two tick audit rows (one per execution).
- No alert-handling regressions; prod silence workflow unchanged.

**Code-review:** 1 Opus pass found 1 Critical + 1 Important + 8 Minor findings; all
implemented. 2nd pass APPROVED. Critical: missing error handling for silences fetch
(added try/except + warn-log). Important: decision audit missing for reconciler
source (added `"karma-reconciler"` to audit data).

**Verification:** `make verify` — 100.00% coverage, 7400 backend tests passed, 2038
UI tests passed. All 25 new tests green.

Tracking commit: 9cdab12

---
