## [STAGE-001-003] CI + Code Review Graph + Dependabot

- GitHub repo created and pushed: https://github.com/jakekausler/homelab-monitor (public).
- `.github/workflows/ci.yml` — three parallel jobs (backend, frontend, crg-build) on `pull_request` + `push` to non-main. Top-level `permissions: contents: read` and `concurrency` group with `cancel-in-progress: true`. crg-build uses `astral-sh/setup-uv@v3` + `pnpm/action-setup@v4` + `actions/setup-node@v4` so the graph covers both Python and TS surfaces; `code-review-graph` pinned to `>=1.2,<2.0` for reproducibility.
- `.github/workflows/release.yml` — dormant scaffolding; fires on `v*` tags only. `concurrency` group with `cancel-in-progress: false`. Docker context now `.` (repo root) with TODO comment for STAGE-001-015's Dockerfile + UI-bundle staging strategy.
- `.github/workflows/codeql.yml` — Python + JavaScript matrix on push-to-main + PR + weekly schedule; `paths-ignore` excludes doc-only PRs (`**/*.md`, `docs/**`, `.gitignore`, `changelog/**`, `epics/**`).
- `.github/dependabot.yml` — four ecosystems (pip apps/monitor, npm root, npm apps/ui, github-actions) on weekly schedule with `minor-patch` grouping; `open-pull-requests-limit: 10`.
- `docs/repo-setup.md` — branch protection rules, GHCR public-visibility note, deferral of issue/PR templates to EPIC-019. Permissions guidance corrected: workflow declares its own `permissions:` so no repo-level Action permission changes needed.
- Root `Makefile` — `verify-ci` target hard-fails on missing CRG and runs `uv tool run code-review-graph build`. Added to `.PHONY`.
- `README.md` — CI status badge near the top, `## CI` section + link to docs/repo-setup.md, `## Releases` section explaining the dormant `v*` tag flow.
- `CLAUDE.md` — `make verify-ci` mention added to the Repo conventions Verify bullet (cleaner placement than the original CRG section).
- End-to-end CI validated against the live repo: PR #12 (deliberate failure → revert) confirmed `backend` FAIL → PASS recovery; PR #29 (pnpm version conflict fix) shipped a real bug discovered during Refinement; PR #30 (this stage's code review fixes, plus pin correction from `>=0.1,<1.0` to `>=1.2,<2.0` after PyPI reality check) merged with all 9 checks green.
- Commits:
  - Implementation (initial scaffolding): `f7dcd32349378b93ff1ccf2bdd06755ffe71c961` (full hash; subagent committed during Refinement to enable the GitHub push test — workflow deviation documented)
  - Refinement fix (pnpm version): `8f2a8ac9606e5aaaef80c6e45f3449791b8e8f22` via PR #29
  - Finalize code-review fixes (this stage's polish): `a638f251e91757067f4cac5d32223d36a5cca181` via PR #30

## [STAGE-001-002] Frontend skeleton

- pnpm workspace at repo root (pnpm-workspace.yaml + root package.json with verify/lint/typecheck/test/build scripts) + `.npmrc` (auto-install-peers).
- React 18 + Vite 6 + TypeScript strict (composite + noEmit) + ESLint flat config (type-aware rules scoped to `src/**/*.{ts,tsx}`) + Prettier (`semi: false`, single quotes) + Tailwind 3.4 + Tremor v3 (manual theme — Tremor v3 ships no preset) + Vitest (jsdom, 100% coverage gate, `src/main.tsx` excluded) + Playwright config (browsers + tests deferred to STAGE-001-014).
- Source: `main.tsx` (StrictMode + createRoot, no React default import — `verbatimModuleSyntax`), `App.tsx` (Tremor `<Card>` + `<Badge>` placeholder using Tremor content tokens, not raw grays), `App.test.tsx` (3 assertions covering heading + status + badge), `index.css` (Tailwind directives + `@layer base` body styles using `bg-background`/`text-foreground` tokens).
- Vite alias `@` → `fileURLToPath(new URL('./src', ...))`; proxy `/api` → `http://localhost:9090` (matches FastAPI port from STAGE-001-010).
- Root Makefile extended: `verify` chains `ui-verify`; `setup` now runs `pnpm install` + pre-commit install; new convenience targets `ui-dev`, `ui-build`, `ui-test`, `ui-verify`.
- `.gitignore` extended (node_modules, dist, .vite, coverage, *.tsbuildinfo, playwright-report, test-results); `.pre-commit-config.yaml` adds eslint + prettier hooks scoped to apps/ui (with comment about pnpm PATH requirement); root `package.json` pins `packageManager: pnpm@9.15.0`.
- Dev getting-started doc updated (`docs/development/getting-started.md`) with frontend prerequisites, Node 20+/pnpm 9+ requirements, verify pipeline split (backend + frontend), `ui-*` Make targets, pnpm troubleshooting.
- Build had 6 fixer cycles resolving 4 verifier failures (ESLint flat-config scoping; `tsc --noEmit -b` mutual exclusion; tsc emitting .js/.d.ts before noEmit added; Tremor v3 missing preset path) plus 16 code-review fixes (Vite alias absolute-path bug; ESM imports for Tailwind plugins; `verbatimModuleSyntax` cleanup; `react.version 'detect'`; Tremor token usage; tsconfig.node.json strict flags; etc.). Final state: `make verify` green, 4 backend tests + 1 frontend test (3 assertions) all pass at 100% coverage.
- Commit: `2e71569ab1c58724dffb12c7144fca7ea1784cd3`

## [STAGE-001-001] Backend Python skeleton

- Repo skeleton + tooling: `Makefile` (verify/lint/format/format-check/typecheck/test/dev/clean/setup/crg-init targets, default goal = verify), `scripts/verify`, `pyproject.toml` (uv workspace), `.python-version` (3.12.8), `LICENSE` (MIT, Jake Kausler 2026), `README.md` with Requirements + Getting started + dev-stub note, `.editorconfig`, `.gitignore` (Python/Node/IDE/data + sqlite + .omc/), `pyrightconfig.json` (strict mode), `.pre-commit-config.yaml` (ruff + pre-commit-hooks; pinned python3.12).
- Backend package at `apps/monitor/` with hatchling build backend, dynamic version sourced from `homelab_monitor/__init__.py`, `[project.scripts] hm` console entry, runtime deps (fastapi/uvicorn/pydantic/structlog/httpx/cryptography/bcrypt/sqlalchemy/alembic/aiosqlite/psutil) + dev deps (pytest + pytest-asyncio/cov/httpx, ruff, pyright, freezegun, pre-commit). NO `black` (Decision C: ruff format only).
- Package layout: `homelab_monitor/__init__.py` (`__version__ = "0.0.0"`), `cli/__init__.py`, `cli/__main__.py`, `cli/main.py` (typed `main()` printing version).
- Tests: `tests/test_smoke.py` (version + CLI stdout) + `tests/test_verify_catches_failures.py` (meta-tests confirming ruff/pyright reject bad inputs). 4/4 pass at 100% coverage on tracked surface.
- Developer doc: `docs/development/getting-started.md`.
- Verify pipeline (`make verify`): ruff check + ruff format --check + pyright --strict + pytest --cov (fail_under=100). Pre-commit hooks installed automatically via `make setup`.
- Commit: `0b2e44aba779467fd1248ef051bfb801c4a514bc`
