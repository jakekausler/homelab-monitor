2026-07-04 67b401c EPIC-009 / STAGE-009-015: fixer-runner egress enforcement (HTTPS proxy + host firewall)

Enforces the egress dimension of non-negotiable #2 (scope) at the network layer for auto-fix. Adds an HTTPS forward proxy sidecar (Squid 6) on a dedicated `fixer-egress` docker bridge + host-side nftables rule that DROPs all outbound traffic from the fixer-runner's `hm-fixegress` bridge except through the proxy. The orchestrator now writes an allow-list per real exec (baseline Anthropic hostnames ∪ runbook-declared `egress`) and calls `squid -k reconfigure`.

**Kernel changes:**
- New `_configure_egress_for_exec(grants)` on `AutoFixOrchestrator`: writes `/data/proxy/allowlist.txt` (inode-preserving truncate + write so Docker bind-mounts stay coherent), then `docker exec fixer-egress-proxy squid -k reconfigure` with 2s timeout. Fail-closed on any error via new `EgressConfigurationError(reason, detail)` with 5 reasons: `allowlist_write_failed`, `reconfigure_nonzero`, `reconfigure_timeout`, `reconfigure_docker_error`, `invalid_hostname`.
- New `_is_valid_hostname` module-level helper (RFC 1035-ish regex) validating both baseline and runbook-declared entries before writing.
- `_exec_claude` seam REPLACED at former `autofix.egress_unenforced` emission point: on non-dry runs, calls `_configure_egress_for_exec` inside `_transcript_lock`, after `_resolve_grants` and BEFORE `_current_run` publish + `exec_capture`. Dry runs skip the seam entirely.
- `FixerRunnerConfig` gains 4 new fields (`egress_proxy_container`, `egress_allowlist_path`, `egress_baseline_hostnames`, `egress_reconfigure_timeout_seconds`) with `HOMELAB_MONITOR_FIXER_EGRESS_*` env-var parsing + timeout > 0 validation.

**Audit event transition:**
- `autofix.egress_unenforced` REMOVED — was STAGE-009-008's warning-only audit; enforcement now lands so the warning is obsolete. Downstream consumers should switch to `autofix.egress_enforced`.
- NEW `autofix.egress_enforced` (informational): emitted per real exec after `_configure_egress_for_exec` succeeds. Payload: `{runbook_id, alert_id, run_id, egress, baseline}`.
- NEW `autofix.egress_configuration_error` (error): emitted on fail-closed. Payload: `{runbook_id, alert_id, run_id, reason, detail, egress, baseline}`.

**Compose changes (`deploy/compose/docker-compose.yml`):**
- `fixer-egress` network gets `driver_opts.com.docker.network.bridge.name: hm-fixegress` (host bridge name is deterministic for nftables rule targeting).
- NEW `fixer-egress-proxy` service (profile `fixer`, image `ubuntu/squid:6.6-24.04_beta`), on BOTH `fixer-egress` AND new `fixer-uplink` networks with `gw_priority: 100` on `fixer-uplink` so its outbound doesn't loop back through the drop rule.
- `fixer-runner` env: adds `HTTPS_PROXY=http://homelab-fixer-egress-proxy:3128`, `HTTP_PROXY=<same>`, `NO_PROXY=""` + `depends_on: [fixer-egress-proxy]`.
- Monitor service mounts proxy config dir RW at `/data/proxy` so orchestrator can write allowlist.txt.

**New files (`deploy/compose/proxy/`):**
- `squid.conf` — CONNECT-only allow-list via `dstdomain "/etc/squid/allowlist.txt"` + deny-all + Squid-writable log paths (initial `stdio:/dev/stdout` design broke under image's default `cache_effective_user proxy`).
- `allowlist.txt.example` — baseline 4 Anthropic hostnames.
- `allowlist.txt` — runtime file, seed = baseline; orchestrator overwrites per exec. Gitignored.

**Host setup (`scripts/host-setup.sh`):**
- New §3.10 (75 lines): installs `/etc/nftables.d/homelab-fixer.nft` with `table inet homelab_fixer { chain forward { priority filter - 10; iifname "hm-fixegress" oifname != "hm-fixegress" counter drop } }`. Reloads via `systemctl reload nftables` (fallback: `nft -f`). Idempotent via existing `do_or_check` helper. Bootstraps `deploy/compose/proxy/allowlist.txt` from `.example` if missing (cold-start safety).

**Tests:**
- NEW `apps/monitor/tests/kernel/autofix/test_egress_configuration.py` (11 tests: baseline-only + baseline+grants + dedup, reconfigure calls with correct args, all 5 EgressConfigurationError reasons exercised).
- EXTENDED `test_autofix_orchestrator.py`: `_FakeDockerClient` gained `squid_result`/`squid_raises` fields + squid short-circuit branch + `calls: list[_RecordedExecCall]` observer. `_make_orchestrator` sets `egress_allowlist_path` under a per-test tmp dir. New seam-integration test `test_egress_configuration_error_fails_closed_and_audits`. Two existing tests updated: `test_grant_resolution_dry_run_does_not_emit_egress_enforced` and `test_grant_resolution_empty_egress_still_emits_egress_enforced_with_baseline` (behavior flipped — real exec with empty grants.egress DOES emit egress_enforced because baseline is always configured).
- EXTENDED `test_feedback_orchestrator_wiring.py`: local `_FakeDockerClient` gained cmd[0] == "squid" short-circuit; `_make_orchestrator` sets `egress_allowlist_path`; two `_exec_capture_with_feedback` wrappers gained the same squid guard.
- EXTENDED `test_config.py`: 3 new tests for env-var parsing of `HOMELAB_MONITOR_FIXER_EGRESS_BASELINE_HOSTNAMES` + `HOMELAB_MONITOR_FIXER_EGRESS_RECONFIGURE_TIMEOUT_SECONDS`.

**Refinement-caught corrections (all in this commit):**
1. Squid log paths swapped from `stdio:/dev/stdout` to `/var/log/squid/access.log` (image's `cache_effective_user proxy` can't write to root-owned stdio).
2. Added `fixer-uplink` docker network so the proxy's own outbound uses a separate bridge, not affected by the nftables drop rule.
3. Changed compose network attachment from `priority: 100` to `gw_priority: 100` (correct Docker Compose 2.32+ field for gateway selection).
4. Orchestrator write strategy changed from `tempfile.mkstemp` + `os.replace` (atomic, but INODE-SWAPPING which breaks Docker bind mounts) to inode-preserving `open(path, "w")` truncate + write.

**Instance-A validation (all PASSED):**
- Baseline reach: fixer-runner → `api.anthropic.com` via proxy (HTTP 405 in 38ms).
- Off-list deny: fixer-runner → `example.com` blocked at Squid (`TCP_DENIED/403 HIER_NONE/-`).
- Raw TCP bypass: fixer-runner → `1.1.1.1` blocked at nftables (curl exit 28, timeout).
- Dynamic allow-list: append `example.com` to allowlist.txt, `squid -k reconfigure`, reach = HTTP 200; restore + reconfigure, block = fresh `TCP_DENIED/403`. Same inode preserved throughout on both host and container.
- Kill switch: `docker kill homelab-fixer-runner` returns ExitCode 137 despite proxy/firewall active.
- Idempotent host-setup: `--check` mode reports rule presence + `nft list table inet homelab_fixer` loaded.

**Seven non-negotiables PASS** (per code-reviewer Opus): #1 trigger (unchanged), #2 scope (enforced at proxy + nftables), #3 identity (fixer container has no host-firewall privs), #4 audit (enforced/config_error/existing grant_resolved all present), #5 dry-run (seam skipped on dry), #6 rate-limit (unchanged), #7 kill switch (docker kill still terminates fixer).

---

2026-07-04 6b41b5f EPIC-009 / STAGE-009-014: Docker intent gateway — schema, validator, executor

Landed the MECHANISM half of non-negotiable #2 (docker dimension) — the fixer emits a structured `docker-intent.json` file → the orchestrator parses, validates each intent against its runbook's `DockerCapability` envelope, and executes valid intents via `DockerSocketClient.restart_container(...)`. Invalid intents are audited and dropped. The fixer NEVER holds a docker socket.

**New module** `apps/monitor/homelab_monitor/kernel/autofix/intents.py`:
- `DockerIntent(BaseModel, extra="forbid")` — `container: str` (constrained to `^[a-zA-Z0-9][a-zA-Z0-9_.-]{0,254}$`, 1-256 chars), `action: Literal["restart"]` (MVP)
- `MalformedIntentFileError` with `reason` + truncated `detail` (≤1024 chars)
- `IntentAccept` / `IntentDeny(reason)` frozen dataclasses; `IntentValidation` union type alias
- `parse_intents(intent_path)` — tolerates missing file (returns `[]`); raises `MalformedIntentFileError` on JSON errors, non-list top-level, invalid entries, `OSError`, `UnicodeDecodeError`
- `validate_intent(intent, grants)` — envelope-check: docker capability absent / container mismatch / action not in allowed_actions

**New `ExecOutcome` dataclass** in `types.py` replaces `_exec_claude`'s 5-tuple return, bundling `exec_result`, `transcript_path`, `error_msg`, `errored`, `feedback_items`, `grants`. Enables post-exec consumers to access resolved grants without re-resolution.

**Orchestrator wiring** in `orchestrator.py`:
- `_execute_intents` method wired into both `_claim_and_exec` (dry=False) and `_claim_and_store_dry` (dry=True), between `_exec_claude` return and `_persist_outcome`
- Kill-switch batch decision at intent-gateway start (D6-C)
- `HM_RUN_ID` env var threaded into `exec_capture` so fixer knows the run's transcript subdir
- 8 distinct audit events: `autofix.intent_{executed, denied, exec_error, malformed, skipped_after_error, kill_switched, dry_planned, gateway_failed}`
- D3-A validate-all-first with per-intent audit transactions (I1 atomicity)
- D4-C halt on `DockerSocketConnectionError`; continue on `DockerSocketProtocolError`
- D7-B dry-run parse+validate+audit `intent_dry_planned`; never execute
- I3 try/except at both call sites → `intent_gateway_failed` audit; run always completes cleanly (no orphaned rows)
- Envelope-side `DockerCapability.container` pattern-constrained defense-in-depth (M7)

**Fake-claude extension** (`deploy/compose/fixer-runner/test/fake-claude`) — new `FAKE_CLAUDE_DOCKER_INTENT` env var writes intent JSON to `${TRANSCRIPT_DIR}/${HM_RUN_ID}/docker-intent.json`.

**Test coverage**: 17 unit tests in `test_intents.py` + 21 orchestrator-integration tests in `test_autofix_intent_gateway.py`. Backend coverage 100.00%. Instance-A real-host validation confirmed schema is live, `parse_intents`/`validate_intent` decision paths match, and `DockerSocketClient.restart_container` restarts a disposable target (StartedAt delta ~45s).

**Fixture housekeeping** (D8): migrated duplicated `secrets_repo_fixture`/`master_key_bytes` across 3 test files to top-level `secrets_repo`/`master_key` — byte-equivalent, cleaner testbench.

**Ancillary**: fixed pre-existing STAGE-009-012 UI fixture gap (`transcript_pruned_at: null` missing from 3 test fixtures) to unblock `make ui-verify`.

Seven non-negotiables re-verified PASS by code-reviewer (Opus). 10 findings from 1st review applied (I1-I4, M1-M7), then 4 more from 2nd review (N1-N4). All findings closed.

---

2026-07-04 aae69b8 EPIC-009 / STAGE-009-012: Audit immutability + runbook_hash enrichment + transcript rotation

Add transcript rotation (kept last N=100 per runbook, max age 365d — both configurable via `HOMELAB_MONITOR_FIXER_TRANSCRIPT_ROTATION_MAX_COUNT` / `..._MAX_AGE_DAYS`). Monitor scheduler runs a daily 03:00 UTC pass + one lifespan-startup pass; file deletion is delegated to `homelab-fixer-runner` via docker-exec (monitor's `/data/runbook-transcripts` mount stays `:ro` per STAGE-009-002 audit-integrity invariant). Rotation prunes transcript FILES only; the `runbook_runs` audit row is retained with a new `transcript_pruned_at TEXT NULL` column (migration 0051) and `transcript_path` NULLed. Audit_log emits `autofix.transcript_pruned` per prune with `reason: "count_limit" | "age_limit" | "both"`.

Add admin-only `POST /api/autofix/rotate-transcripts` endpoint (require_session + CSRF, NO destructive-cred gate — rotation is idempotent policy-driven housekeeping, not destruction of audit data). Returns `{files_pruned, runs_marked, runbooks_scanned, skipped_reason}`. If fixer-runner is not running, endpoint returns `skipped_reason="fixer_runner_not_running"` with 200 (graceful degradation).

Add TranscriptRotator with asyncio.Lock serializing scheduled + manual rotations. Add path-safety check rejecting non-absolute transcript_paths and paths outside the configured base dir. Add `mark_transcript_pruned_conn` (UPDATE-only, NEVER DELETE — preserves non-negotiable #4 audit immutability).

Regression tests assert: (a) no DELETE endpoint exists for `runbook_runs` or `audit_log`; (b) `POST /api/autofix/rotate-transcripts` requires admin session + CSRF, returns 401 unauth / 403 non-admin / 200 admin; (c) drift-hash detection at `orchestrator.execute_approved` correctly returns `DenialReason.RUNBOOK_CHANGED` on hash-drift and `RUNBOOK_MISSING` on runbook-deleted (no exec in either case).

Instance-A validation against prod monitor `192.168.2.148:29090` PASS on all 4 scenarios: DELETE endpoints rejected (405), rotate-endpoint auth gate (401 → 200 with counts), schema fields present in DB + Pydantic models + migration head at 0051.

Deferred (as owned regression items in `regression.md`):
- Whole-folder + markdown drift hash — STAGE-009-016 (new stage, added during 012 Design)
- Registry row pruning on `POST /api/runbooks/refresh` — STAGE-009-017 (new stage, added during 012 Design)

All non-negotiables (7 of 7) verified PASS in both code-reviewer passes.

EPIC-009 progress: 14/19 stages complete after this commit.

---
