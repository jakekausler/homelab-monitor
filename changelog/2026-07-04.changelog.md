2026-07-04 6b41b5f EPIC-009 / STAGE-009-014: Docker intent gateway — schema, validator, executor

Landed the MECHANISM half of non-negotiable #2 (docker dimension) — the fixer emits a structured `docker-intent.json` file → the orchestrator parses, validates each intent against its runbook's `DockerCapability` envelope, and executes valid intents via `DockerSocketClient.restart_container(...)`. Invalid intents are audited and dropped. The fixer NEVER holds a docker socket.

**New module** `apps/monitor/homelab_monitor/kernel/autofix/intents.py`:
- `DockerIntent(BaseModel, extra="forbid")` — `container: str` (constrained to `^[a-zA-Z0-9][a-zA-Z0-9_.-]{0,254}$`, 1-256 chars), `action: Literal["restart"]` (MVP)
- `MalformedIntentFileError` with `reason` + truncated `detail` (≤1024 chars)
- `IntentAccept` / `IntentDeny(reason)` frozen dataclasses; `IntentValidation` union type alias
- `parse_intents(intent_path)` — tolerates missing file (returns `[]`); raises `MalformedIntentFileError` on JSON errors, non-list top-level, invalid entries, `OSError`, `UnicodeDecodeError`
- `validate_intent(intent, grants)` — envelope-check: docker capability absent / container mismatch / action not in allowed_actions

**New `ExecOutcome` dataclass** in `types.py` replaces `_exec_claude`'s 5-tuple return, bundling `exec_result`, `transcript_path`, `error_msg`, `errored`, `feedback_items`, `grants`. Enables post-exec consumers to access resolved grants without re-resolution.

**Orchestrator wiring** in `orchestrator.py`:
- `_execute_intents` method wired into both `_claim_and_exec` (dry=False) and `_claim_and_store_dry` (dry=True), between `_exec_claude` return and `_persist_outcome`
- Kill-switch batch decision at intent-gateway start (D6-C)
- `HM_RUN_ID` env var threaded into `exec_capture` so fixer knows the run's transcript subdir
- 8 distinct audit events: `autofix.intent_{executed, denied, exec_error, malformed, skipped_after_error, kill_switched, dry_planned, gateway_failed}`
- D3-A validate-all-first with per-intent audit transactions (I1 atomicity)
- D4-C halt on `DockerSocketConnectionError`; continue on `DockerSocketProtocolError`
- D7-B dry-run parse+validate+audit `intent_dry_planned`; never execute
- I3 try/except at both call sites → `intent_gateway_failed` audit; run always completes cleanly (no orphaned rows)
- Envelope-side `DockerCapability.container` pattern-constrained defense-in-depth (M7)

**Fake-claude extension** (`deploy/compose/fixer-runner/test/fake-claude`) — new `FAKE_CLAUDE_DOCKER_INTENT` env var writes intent JSON to `${TRANSCRIPT_DIR}/${HM_RUN_ID}/docker-intent.json`.

**Test coverage**: 17 unit tests in `test_intents.py` + 21 orchestrator-integration tests in `test_autofix_intent_gateway.py`. Backend coverage 100.00%. Instance-A real-host validation confirmed schema is live, `parse_intents`/`validate_intent` decision paths match, and `DockerSocketClient.restart_container` restarts a disposable target (StartedAt delta ~45s).

**Fixture housekeeping** (D8): migrated duplicated `secrets_repo_fixture`/`master_key_bytes` across 3 test files to top-level `secrets_repo`/`master_key` — byte-equivalent, cleaner testbench.

**Ancillary**: fixed pre-existing STAGE-009-012 UI fixture gap (`transcript_pruned_at: null` missing from 3 test fixtures) to unblock `make ui-verify`.

Seven non-negotiables re-verified PASS by code-reviewer (Opus). 10 findings from 1st review applied (I1-I4, M1-M7), then 4 more from 2nd review (N1-N4). All findings closed.

---

2026-07-04 aae69b8 EPIC-009 / STAGE-009-012: Audit immutability + runbook_hash enrichment + transcript rotation

Add transcript rotation (kept last N=100 per runbook, max age 365d — both configurable via `HOMELAB_MONITOR_FIXER_TRANSCRIPT_ROTATION_MAX_COUNT` / `..._MAX_AGE_DAYS`). Monitor scheduler runs a daily 03:00 UTC pass + one lifespan-startup pass; file deletion is delegated to `homelab-fixer-runner` via docker-exec (monitor's `/data/runbook-transcripts` mount stays `:ro` per STAGE-009-002 audit-integrity invariant). Rotation prunes transcript FILES only; the `runbook_runs` audit row is retained with a new `transcript_pruned_at TEXT NULL` column (migration 0051) and `transcript_path` NULLed. Audit_log emits `autofix.transcript_pruned` per prune with `reason: "count_limit" | "age_limit" | "both"`.

Add admin-only `POST /api/autofix/rotate-transcripts` endpoint (require_session + CSRF, NO destructive-cred gate — rotation is idempotent policy-driven housekeeping, not destruction of audit data). Returns `{files_pruned, runs_marked, runbooks_scanned, skipped_reason}`. If fixer-runner is not running, endpoint returns `skipped_reason="fixer_runner_not_running"` with 200 (graceful degradation).

Add TranscriptRotator with asyncio.Lock serializing scheduled + manual rotations. Add path-safety check rejecting non-absolute transcript_paths and paths outside the configured base dir. Add `mark_transcript_pruned_conn` (UPDATE-only, NEVER DELETE — preserves non-negotiable #4 audit immutability).

Regression tests assert: (a) no DELETE endpoint exists for `runbook_runs` or `audit_log`; (b) `POST /api/autofix/rotate-transcripts` requires admin session + CSRF, returns 401 unauth / 403 non-admin / 200 admin; (c) drift-hash detection at `orchestrator.execute_approved` correctly returns `DenialReason.RUNBOOK_CHANGED` on hash-drift and `RUNBOOK_MISSING` on runbook-deleted (no exec in either case).

Instance-A validation against prod monitor `192.168.2.148:29090` PASS on all 4 scenarios: DELETE endpoints rejected (405), rotate-endpoint auth gate (401 → 200 with counts), schema fields present in DB + Pydantic models + migration head at 0051.

Deferred (as owned regression items in `regression.md`):
- Whole-folder + markdown drift hash — STAGE-009-016 (new stage, added during 012 Design)
- Registry row pruning on `POST /api/runbooks/refresh` — STAGE-009-017 (new stage, added during 012 Design)

All non-negotiables (7 of 7) verified PASS in both code-reviewer passes.

EPIC-009 progress: 14/19 stages complete after this commit.

---
