2026-06-18 73cb57c EPIC-007/STAGE-007-010: Alarms/threats collector (rest/alarm)

New `UnifiAlarmsCollector` (`plugins/collectors/integrations/unifi/alarms.py`, `rest/alarm?archived=false`, 60s, `concurrency_group="unifi"`, stateless): emits `homelab_unifi_threat_count{}` (distinct active alarm `_id` count, ALWAYS emitted incl. 0.0 on a quiet network), `homelab_unifi_threat{type}` (per-type distinct-`_id` count, present types only), and `homelab_unifi_api_took_seconds{endpoint="rest/alarm?archived=false"}`.

Within-poll dedup by alarm `_id` (duplicate `_id` counted once; records with no usable `_id` skipped). Cross-poll "don't re-page" is owned by STAGE-007-015's PromQL on the idempotent gauge.

`{type}` label = fallback chain `key` -> `subsystem` -> `"unknown"` (never empty). No cardinality cap (threat types are a bounded IDS enum).

The always-present `threat_count=0` series lets STAGE-007-015's `UnifiThreatDetected` alert avoid the `absent()` trap.

Registered in the unifi bundle. No protocol/client/config changes (`rest_alarm()` already existed).

18 unit tests (all branches incl. dedup, the 3-branch type fallback, empty/malformed payloads). Live-validated against the real UDM (empty path; populated path via synthetic fixture — live had zero alarms).

2026-06-18 1e1e529 EPIC-007/STAGE-007-009: Per-client DPI collector (capped top-N by volume)

Added `UnifiClientDpiCollector` (`plugins/collectors/integrations/unifi/client_dpi.py`, 5m interval, `concurrency_group="unifi"`): per-client per-application DPI breakdown emitting `homelab_unifi_client_dpi_bytes{client,app,cat}` (combined rx+tx cumulative gauge), `homelab_unifi_dpi_enabled`, `homelab_unifi_api_took_seconds{endpoint="stat/stadpi"}`, and `homelab_metric_family_dropped_series{family="homelab_unifi_client_dpi_bytes"}`.

By-volume cardinality cap (`cap_for("unifi_dpi")`=100, clamped to ≥0): keeps the highest-byte (client,app,cat) series, not lexically-smallest; explicit drop gauge + one warning SuggestionEvent over budget. Code review added metric-family label consistency: `family` uses the metric name (`homelab_unifi_client_dpi_bytes`), while the config lookup remains `unifi_dpi`.

Added `stat_stadpi()` to the `UnifiClient` Protocol + `UnifiRestClient`; registered the collector in the unifi bundle. Per-counter spike clamp deferred to STAGE-007-015 (PromQL); collector emits raw cumulative bytes.

Graceful degrade: empty/`[{}]`/malformed `stat/stadpi` → indicator + latency + drop gauge=0, no data series, ok=True. Unit tests (15 branches, by-volume ordering, over-cap drop/SuggestionEvent). Live-validated against the real UDM (graceful-empty path; populated path awaits accumulated DPI data).

2026-06-18 427d053 EPIC-007/STAGE-007-008: Per-client stats + WiFi-experience rollups collector (cardinality-capped)

Added `UnifiClientStatsCollector` (`plugins/collectors/integrations/unifi/client_stats.py`): one `stat/sta` fetch per 60s tick (concurrency_group="unifi") emitting 6 CARDINALITY-CAPPED per-client metric families via `CappedEmitter` with `cap_for("unifi_client_stats")` (=200), all labeled `{mac}`-only: `client_signal_dbm` (wireless), `client_tx_rate_bps`/`client_rx_rate_bps` (wireless, kbps->bps x1000), `client_uptime` (all), `client_tx_bytes`/`client_rx_bytes` (all; wireless `tx_bytes`/`rx_bytes` vs wired `wired-tx_bytes`/`wired-rx_bytes`). Each `emit_family` also writes a `homelab_metric_family_dropped_series{family}` gauge and appends ONE warning SuggestionEvent when over cap.

Bounded (NOT capped) WiFi-experience rollups computed over the full pre-cap list with `{threshold}` labels: `clients_poor_signal{threshold="-70"}`, `clients_poor_satisfaction{threshold="50"}`, `clients_high_retries{threshold="10"}` (lifetime-cumulative retry ratio `100*tx_retries/wifi_tx_attempts`, guarded `attempts>0`), and `ap_client_count{ap_mac}`. Plus `homelab_unifi_api_took_seconds{endpoint="stat/sta"}`.

Cap determinism (Decision A2): all 6 families share the identical `{mac}`-only candidate label set, so the sort-then-slice cap yields identical survivors within each candidate set (wireless-only families agree; all-client families agree). Wired graceful degrade (no signal/rate/retries for wired). Error/degrade paths: None-client/sta-error -> ok=False; malformed payload -> drop gauges (0) + rollups (0) + latency, ok=True.

Registered alongside device, wan, active_client. Unit tests (`test_unifi_client_stats_collector.py`) incl. cap enforcement, the A2 cross-family survivor invariant (same-candidate-set identity + mixed-roster wired-disjoint), rollup thresholds + div-by-zero guard, wired degrade, error paths. GLOBAL 100% branch coverage. Live-validated read-only against the real UDM Pro (45 wireless / 59 total clients, rates in bps magnitude, 6 drop gauges at 0, rollups + per-AP counts present).

Fixed a pre-existing flaky timing test `test_scheduler.py::test_tick_precision_short_interval` discovered during this stage's `make verify`: its tick-count lower bound (`14<=count<=16`) was too tight and dropped ticks under parallel-test load (the scheduler's by-design no-catch-up skip-forward); widened the lower bound to `10` (upper bound unchanged — still catches a catch-up-storm regression).

2026-06-18 b291dba EPIC-007/STAGE-007-007: Active-client identity collector -> registry upsert

The Wave-B active-client integration: a real-time `stat/sta` + `stat/alluser` fetcher driving identity upserts into the `unifi_clients` registry, with per-client and roster-level metrics.

- Added `UnifiActiveClientCollector` (`plugins/collectors/integrations/unifi/active_client.py`): one `stat/sta` + `stat/alluser` fetch per 60s tick (concurrency_group="unifi") driving the STAGE-007-004 `upsert_identity` helper inside a caller-owned transaction to populate the persistent `unifi_clients` registry (MAC<->IP<->hostname<->ap/sw-port<->fixed-ip, online/offline) + time-stamped IP<->MAC observations. Computes `now`/`cutoff` (now - 90d retention) from a single `datetime.now(tz=UTC)`, injected into the helper.
- New-client detection via a pre-upsert MAC-set snapshot diff (NOT `first_seen==now`, which would miss clients since the helper preserves the real first_seen): emits `homelab_unifi_new_client_total` (counter) + per-mac `homelab_unifi_new_client{mac,hostname,network}` gauge; the host `host:<ip>` sentinel is excluded.
- Roster rollups from the live `stat/sta` parse: `homelab_unifi_active_client_count` / `_known_client_count` / `_offline_client_count`, `ssid_client_count{ssid}`, `client_count_by_network/by_ap/by_band/by_link` (band ng->2.4ghz/na->5ghz/6e->6ghz; link wired/wireless). Records with a non-str/sentinel mac (which the upsert skips) are excluded from the counts for consistency.
- Self-metrics `homelab_unifi_identity_{clients_upserted,observations_appended,hosts_reconciled,skipped}` + per-endpoint `homelab_unifi_api_took_seconds{endpoint}`.
- D2 asymmetric degrade: `stat_sta` failure -> ok=False (no upsert, the online roster is irreplaceable); `stat_alluser` failure -> proceed with `[]`, `homelab_unifi_alluser_degraded`=1, ok=True; malformed payload -> no-op upsert, ok=True.
- Registered alongside `unifi_device` + `unifi_wan`. DB-backed unit tests (`test_unifi_active_client_collector.py`) against a real migrated SQLite; GLOBAL 100% branch coverage. Live-validated read-only against the real UDM Pro into an isolated DB: 86 clients upserted (59 online/27 offline), host reconciled, new-client set-diff confirmed idempotent on rerun, all rollups present.

2026-06-18 a6a0bc8 EPIC-007/STAGE-007-006: WAN/ISP + speedtest + failover collector (stat/health www)

The next Wave-B collector: a real-time `stat/health` fetcher for WAN/ISP state, speedtest results, and failover status.

- Added `UnifiWanCollector` (`plugins/collectors/integrations/unifi/wan.py`): one `stat/health` fetch per 30s tick (concurrency_group="unifi", the highest-priority WAN signal) parsing the `www` + `wan` subsystem entries into `homelab_unifi_wan_up` (status=="ok"), `_wan_latency_seconds` (ms->s), `_wan_drops`, `_wan_xput_down/up_bytes_per_sec` (live rx/tx byte rates), `_wan_failover_capable` (>1 WAN configured) / `_wan_failover_active` (secondary carrying traffic), `_wan_uptime_seconds`, `_speedtest_download/upload_mbps` + `_speedtest_ping_seconds` (only when a speedtest has run), `_speedtest_lastrun` (always; 0 = never run), and `homelab_unifi_api_took_seconds{endpoint="stat/health"}`.
- Never-run-speedtest graceful degrade: when `speedtest_lastrun==0`, the speedtest result metrics are NOT emitted (zeros are not reported as real measurements); `_speedtest_lastrun` is still emitted as the never-run signal.
- Extracted the shared parsing helpers (`as_float`/`as_bool`/`emit_numeric`) from `device.py` into a new `plugins/collectors/integrations/unifi/_parsing.py` module (made public) so both the device and WAN collectors (and future Wave-B/C collectors) import them; `device.py` refactored to use them with no behavior change.
- Registered `UnifiWanCollector` in the Unifi bundle alongside `UnifiDeviceCollector`.
- Tests: `test_unifi_wan_collector.py` (healthy/no-speedtest, completed-speedtest, absent-ping, single-WAN, active-failover, missing-www/wan, malformed, error/None-client) + `test_unifi_parsing.py` (pure-helper branch coverage). GLOBAL 100% branch coverage maintained. Live-validated read-only against the real UDM Pro (wan_up=1, latency in seconds, never-run speedtest correctly suppressed, failover capable=1/active=0).

2026-06-18 9479257 EPIC-007/STAGE-007-005: Combined Unifi device collector (health/firmware/ports/PoE/radios/PDU-relay/temp)

The primary Wave-B collector: a real-time `stat/device` fetcher yielding device state, firmware, port links, PoE power, radio airtime, and PDU relay state into the Unifi metrics family.

- Added `UnifiDeviceCollector` (`plugins/collectors/integrations/unifi/device.py`): one `stat/device` fetch per 60s tick (concurrency_group="unifi", 15s timeout) parsed into `homelab_unifi_device_*` (up/state/firmware_info/update_available/uptime/cpu%/mem%/load1/temperature), `homelab_unifi_port_*` (link/speed/PoE power-current-voltage/good/bytes/errors/drops/mac-table/link-down/satisfaction), `homelab_unifi_radio_*` (airtime cu_total/self_rx/self_tx/num_sta/tx_power/tx_retries_pct/satisfaction/channel/bandwidth), and `homelab_unifi_outlet_relay_state` (PDU relay state + name only — wattage intentionally omitted; HA owns it). Emits `homelab_unifi_api_took_seconds{endpoint="stat/device"}` from the client-measured latency.
- Graceful-degrade parsing: every nested field isinstance-guarded; STRING-encoded Unifi numerics (poe_*, cpu/mem, loadavg) float-parsed inside guards; bool excluded before int/float; absent/non-list tables and empty `sys_stats` skipped without error.
- Removed the STAGE-007-002 placeholder scaffolding: deleted `unifi/placeholder.py` + `test_unifi_placeholder_collector.py`; `_UNIFI_COLLECTORS` now registers the real device collector.
- Tests: `test_unifi_device_collector.py` — fixture-based unit tests across 4 device classes (UDM/PoE-switch/AP/PDU) + graceful-degrade/error/None-client/branch-edge cases (100% branch coverage on device.py). Live-validated read-only against the real UDM Pro (8 devices, real PoE/radio/PDU/temp values).

2026-06-18 713176a EPIC-007/STAGE-007-004: cardinality caps + identity-upsert helper (stat/sta + stat/alluser → registry)

The shared identity-normalization layer the Wave-B collectors build on: a PURE helper mapping live Unifi `stat/sta`+`stat/alluser` records into the `unifi_clients` registry, plus the cardinality-cap config reuse and the sentinel-host reconciliation deferred from STAGE-007-003.

- New `kernel/unifi/identity.py`: `upsert_identity(conn, *, stat_sta, stat_alluser, host_lan_ip, observation_cutoff, now) -> UpsertResult` — two-pass merge: PASS 1 (`stat/sta` = online) upserts + appends a time-stamped IP↔MAC observation + reconciles the host; PASS 2 (`stat/alluser` = all-known) upserts offline-only clients (using `last_ip`) for macs not seen online, no observation, no downgrade. Pure transform: `now`/`cutoff` are INJECTED (never calls utc_now). A `_extract` isinstance-guard converts the live shape (epoch-int `first_seen`/`last_seen` → ISO; `is_wired` wired/wireless; `use_fixedip`+`fixed_ip`; `name or hostname`) and skips records with no/non-str mac. `ExtractedClient`/`UpsertResult` frozen dataclasses; `UpsertResult{clients_upserted, observations_appended, hosts_reconciled, skipped}` feeds the collector's self-metrics. NOTE: 004 is the HELPER — the registered collector that calls it on the live UDM is STAGE-007-007.
- `UnifiClientRepo.upsert_client_conn` gained a `first_seen: str` param (INSERT-only, kept out of DO UPDATE SET → preserved on conflict) so the registry's `first_seen` comes from the RECORD's first_seen (epoch→ISO), not from last_seen.
- New `UnifiClientRepo.promote_to_host_conn(conn, *, real_mac, host_ip) -> bool`: the sentinel→real-MAC reconciliation deferred from STAGE-007-003 — when a record's ip == host_lan_ip, promotes the real-mac row to `is_host=1`, merges `first_seen` to the min, deletes the `host:<ip>` sentinel; idempotent.
- Cardinality-cap families `unifi_client_stats` (200) + `unifi_dpi` (100) added to the central caps config (`_DEFAULT_CARDINALITY_FAMILIES`). CONFIG-ONLY here — the per-client/DPI metric-series cap APPLICATION is STAGE-007-008/009. The registry itself is never capped (complete canonical inventory).
- Live shape was recon'd read-only against the real UDM to build accurate fixtures (the key gotcha: Unifi classic-API timestamps are epoch INTEGER seconds, converted to ISO TEXT by the helper). Validated end-to-end against a REAL migrated SQLite (two-pass merge, epoch→ISO first_seen-from-record, host reconciliation, no-downgrade, skip-counting). Full `make verify` green; global 100% branch coverage.

2026-06-18 2c53bd8 EPIC-007/STAGE-007-003: persistent unifi_clients registry table + UnifiClientRepo

Adds the canonical MAC-keyed client registry (EPIC-007 Wave A data foundation that EPIC-006 enhances): Alembic migration `0044` + a `UnifiClientRepo` facade + a config-driven host-row seed.

- Migration `0044_unifi_clients.py` (`down_revision 0043`) creates `unifi_clients` (MAC primary key; ip/hostname/name/oui/network/ap_mac/sw_mac/sw_port, use_fixedip/online/is_host INTEGER 0-1 booleans, first_seen/last_seen UTC-ISO TEXT, `lease_expiry` nullable — written later by STAGE-007-012, no migration needed) + `unifi_client_observations` (composite PK `(mac, ip)` span table for time-stamped IP↔MAC observations) with 4 indexes; idempotent `upgrade()`, explicit `downgrade()`.
- `UnifiClientRepo` (`kernel/db/repositories/unifi_clients_repository.py`): static transaction-composable `upsert_client_conn` (`ON CONFLICT(mac) DO UPDATE`, preserves `first_seen`, never touches `is_host`/`lease_expiry`) + `append_observation_conn` (span UPSERT on `(mac, ip)` + inline retention prune at a caller-supplied cutoff); instance `find_mac_by_ip_at(ip, at)` (the time-windowed IP→MAC join EPIC-006 calls), `get_client`, `list_clients`, `ensure_host_row` (idempotent sentinel `host:<ip>` row, `is_host=1`). Frozen `UnifiClientRow` dataclass.
- Observation retention is a 90-day time-window prune, configurable via a new `UnifiConfig.observation_retention_days` (default 90, env `HOMELAB_MONITOR_UNIFI_OBSERVATION_RETENTION_DAYS`); the prune runs inline on each observation append.
- Host-row guarantee: lifespan calls `ensure_host_row(unifi_config.host_lan_ip)` once at startup (config-driven, not a migration seed) so the host (192.168.2.148) is always a first-class `is_host=1` row.
- Validated against a REAL migrated on-disk SQLite via `UnifiClientRepo`: migration up/down, host-row idempotency, upsert first_seen-preservation, observation span collapse + 90d prune, and the time-window query (returns the right mac; None when no span matches). Full `make verify` green; global 100% branch coverage.
- **Known forward dependency:** the sentinel `host:<ip>` row's reconciliation into the real-MAC row (once the active-client collector learns the host's MAC) is STAGE-007-004/007 (collector-side identity logic; 003 only guarantees a first-class host row exists).

2026-06-18 08a0f73 EPIC-007/STAGE-007-002: integrations/unifi bundle skeleton + registration

Stands up the `integrations/unifi/` collector-bundle skeleton (EPIC-007 Wave A), mirroring the EPIC-005-003 HA integration-bundle pattern — the structural home for all Wave-B/C collectors.

- New `plugins/collectors/integrations/unifi/` package: `__init__.py` with `register_all(loader)` (mirrors `register_ha_collectors` — `_UNIFI_COLLECTORS` list + per-collector try/except isolation + `unifi_integration.collector_register_failed` warning), and `placeholder.py` (`UnifiPlaceholderCollector`).
- `UnifiPlaceholderCollector` is a genuine no-op SCAFFOLDING collector: emits one static gauge `homelab_unifi_bundle_loaded`=1.0, never calls `ctx.unifi`, `concurrency_group="default"` (not "unifi" — it makes no controller calls), 60s interval, always ok=True. It proves the bundle loads/wires into the scheduler/appears in the collector status API. REMOVED by STAGE-007-005 when the first real Wave-B collector lands (SCAFFOLDING comment names 007-005).
- Extended the existing `UnifiConfig` (`kernel/config.py`) with `host_lan_ip` (default `192.168.2.148`, env `HOMELAB_MONITOR_UNIFI_HOST_LAN_IP`, host attribution) and `ssh_lease_enabled` (default `False`, env `HOMELAB_MONITOR_UNIFI_SSH_LEASE_ENABLED`, the opt-in gate STAGE-007-012 reads). Bool-parse uses the project's standard truthy set `("1","true","yes")` (matches `load_drain_config`). `unifi_url` not duplicated; cardinality-cap fields deferred to STAGE-007-004.
- Wired `register_unifi_collectors(loader)` into lifespan alongside the HA/SSH bundle registrations.
- Refinement validated against a real running backend: `GET /api/collectors` lists `unifi_placeholder` (healthy, 60s) with no registration error. Full `make verify` green; global 100% branch coverage.

2026-06-18 3764f7c EPIC-007/STAGE-007-001: Unifi API client (key auth; classic + v1) + `unifi_api_key` secret + lifespan wiring + smoke

First foundation stage of the Unifi integration bundle (EPIC-007). Adds a read-only Unifi API client as kernel infrastructure (`kernel/unifi/`), mirroring the Home Assistant client pattern.

- New `UnifiRestClient` (`kernel/unifi/client.py`) reaching BOTH the official v1 Integrations API and the classic reverse-engineered API with a single read-only `X-API-KEY`; returns a frozen `UnifiResponse(payload, took_seconds, endpoint)` wrapper (per-request `time.monotonic()` latency captured for the future API-latency collector) or a typed `UnifiError` (discriminated union: unreachable/timeout/auth/rate_limited/http_error/bad_response). Read-only: GET helpers only. The API key never appears in any log line or error message.
- Self-signed UDM TLS handled via a dedicated `verify=False` httpx client created + torn down in lifespan (blast radius = the single UDM target; vanilla shared client keeps verification).
- `UnifiClient` Protocol added in `kernel/plugins/io.py`; `UnifiConfig`/`load_unifi_config()` (env `HOMELAB_MONITOR_UNIFI_URL`, default `https://192.168.2.1`) in `kernel/config.py`; `unifi` field added to `CollectorContext`; client constructed + injected (non-None) via the lifespan `ctx_factory`. The `unifi_api_key` secret is read per-request via the TTL-cached resolver.
- **Two site identifiers (discovered in live Refinement):** the classic API path `/api/s/{site}/` requires the SHORT site NAME (`"default"`) while v1 site-scoped paths `/sites/{id}/` require the site UUID. Client exposes `site_name` (classic) + `v1_site_id` (v1 UUID, resolved eagerly + NON-fatally from `v1/sites` at startup). A startup resolution failure does NOT crash the app.
- Integration README `docs/integrations/unifi.md` documents the read-only API-key generation steps and the `verify=False` threat model.
- 37 unit tests (mocked transport, 100% branch coverage on `kernel/unifi/`) + a lifespan success-branch test; validated LIVE read-only against the real UDM Pro (`v1_sites`, `stat_sysinfo`, `v1_devices` all 200 over self-signed TLS).

2026-06-18 5784d4f EPIC-017/STAGE-017-008: Cross-epic reconciliation (cron-SSH deferral notes + CronDetail text) — EPIC-017 COMPLETE

The final EPIC-017 stage — cross-epic docs/text reconciliation. EPIC-017 (the SSH-probe framework) is now complete (8/8 stages).

Changes:
- `apps/ui/src/components/crons/CronDetail.tsx`: reworded the disabled remote-cron "Install/Remove heartbeat wrapper" button tooltip from "...ships in EPIC-017 (SSH probe framework)." → "Remote-host {removal|install} over SSH is deferred — not yet available." Removes the now-false implication that EPIC-017 delivers remote-cron install (cron-SSH is deferred); no epic-jargon in user-facing UI. The button stays disabled; the conditional + remote-cron banner are unchanged.
- `apps/ui/src/components/crons/__tests__/CronDetail.test.tsx`: added two vitest assertions (install + removal cases) verifying the reworded tooltip text for a remote cron (`is_local: false`), with a locally-scoped `vi.mock` of the Tooltip module so Radix `TooltipContent` renders eagerly in jsdom.
- `epics/EPIC-002-heartbeat-cron/EPIC-002.md`: added a `## Deferred scope — remote-host cron SSH install (Option-B)` section recording the deferral, the write-path Option-B architecture (a scoped script consuming EPIC-017's `SshClientFactory` dedicated-user forced-command transport in its own cron-remote-management home, depends on 017's transport), the user-facing consequence, and the conditional/no-tracking-stage status.
- Verified (no edits needed): EPIC-017.md already holds the deferred cron-SSH Option-B note, the EPIC-007/008 consumer-rendering notes, and the EPIC-008 sudoers-hop contract.

Validation: the reworded tooltip is verified via the new vitest assertions (it renders only for remote crons / `is_local: false`); the live Desktop+Mobile viewport test was waived (user-approved — the user has no remote crons, so the element is unreachable in the live UI; validated via the render tests instead). `make verify` fully green: 5099 backend tests, 100% branch coverage, frontend tsc + vitest (1549, incl. both tooltip tests) + prettier + eslint + build, ruff + pyright clean.

2026-06-18 5cd2484 EPIC-017/STAGE-017-007: ssh.yaml framework-health alert rules

Added `deploy/vmalert/metrics/ssh.yaml` (group `ssh_health`) — the framework-health alert rules over the SSH-probe metrics. Three per-target rules, auto-loaded by vmalert via the `/etc/vmalert/metrics/*.yaml` glob:
- `SshTargetUnreachable` (warning) — `homelab_ssh_up == 0` for 5m; routes to the `monitoring-health` channel.
- `SshHostKeyMismatch` (critical) — `homelab_ssh_host_key_mismatch == 1`, fires immediately (`for: 0s`); default routing (security finding, not monitoring degradation).
- `SshProbeStale` (warning) — `homelab_ssh_last_success_age_seconds > 900` for 5m; routes to `monitoring-health`. Silent when the age series is absent (post-monitor-restart / never-succeeded) by design.

Each rule's `annotations.description` carries the card-required remediation (re-install authorized_keys incl. the appliance firmware-wipe note via `hm ssh-probe install-instructions <target>`; out-of-band host-key verification + `hm ssh-probe capture-hostkey <target>` for a mismatch; the in-memory-state-resets-on-restart note for stale). NO `absent()` anywhere — on a multi-target metric `absent()` collapses labels and breaks per-target alerting (it's valid in `vl_health.yaml` only because VictoriaLogs is single-target).

Validation: vmalert loads all 3 rules with `health=ok` (no parse error); a synthetic-injection live-replay on the prod rig proved `SshHostKeyMismatch` fires end-to-end to Alertmanager with the `{{ $labels.target }}`-rendered summary, `SshTargetUnreachable` goes pending on `homelab_ssh_up==0`, and `SshProbeStale` correctly stays silent with no age series. `make verify` green (no Python coverage impact — YAML only).
