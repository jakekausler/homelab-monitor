2026-06-20 6ba48de EPIC-007/STAGE-007-020: Integrations → Unifi panel (device table + drill-down + threats/DPI/Teleport/controller)

New Integrations → Unifi panel UI filling the STAGE-018 layout scaffold: persistent header status strip (controller/WAN/Teleport state, device up/total, threat count) + an Overview tab with a live device table and four widgets (threats, DPI top-apps, Teleport/VPN, controller/API health), all consuming the STAGE-019 backend endpoints via the generated openapi types.

Device drill-down at `/integrations/unifi/devices/$device` (route-param page, back-link) rendering data-driven sections: Ports/PoE (switches), Radios/airtime (APs), Outlet relays (PDU), plus an always-on System section (CPU/mem/load avg/temps).

New UI helpers: `formatUptime` (two-unit magnitude duration), `formatBitrate` (decimal bit-rate units), `formatDeviceKind` (AP/PDU/UDM/Gateway/Switch), `formatSatisfaction` (em-dash for the not-reported sentinel matching the cron-table convention).

Honest empty/loading/error states (200-empty vs 502 vs 404) per the established Home Assistant integration pattern; radio/port satisfaction "not reported" (-1/null) rendered as em-dash; controller API timing correctly labeled as per-endpoint Latency (not uptime).

Frontend-only; gate `make ui-verify` green (0 eslint errors, tsc strict, 1588 vitest tests, coverage all thresholds met, build success).

---

2026-06-20 42cb2cf EPIC-007/STAGE-007-019: backend Unifi data endpoints (panel + network + client contract)

Added the backend data-endpoint CONTRACT that frontend stages 020-023 consume: a new router `kernel/api/routers/integrations_unifi.py` with 13 GET endpoints under `/api/integrations/unifi/*` — `summary`, `devices`, `devices/{device}`, `threats`, `dpi`, `teleport`, `controller-health`, `network/wan`, `network/dhcp`, `network/wifi`, `network/dns-posture`, `clients` (paginated registry roster), `clients/{mac}` (registry+VM join). Mirrors the EPIC-005 Home Assistant router pattern (Pydantic response_models, `Depends(require_session())`, VM reads via `vm_instant_query`/`vm_count`, 502-on-VM-failure, empty/error states in-payload, 404 on unknown device/mac).

NEW collector metric `homelab_unifi_dhcp_reservation_count{network}` (active_client.py) — count of clients with `use_fixedip==true` per network (the deferred STAGE-011 item; Grafana-queryable for STAGE-025). No migration.

Client-detail carries the EPIC-006 DNS-enrichment extension slot (`dns: DnsEnrichment | None`, ships null until EPIC-006). `lease_expiry` + `is_host` surfaced in roster + detail (STAGE-022 needs these). Speedtest/WAN history is served by the existing `/api/metrics/range` proxy (019 serves current/instant only).

Design: granular endpoints + `/summary` aggregate; registry is source-of-truth for client `online`; reservation_count is a metric (not endpoint-computed) so Grafana can consume it.

Refinement (live prod-rig e2e against the real UDM): all 13 endpoints return real data (controller up, 8 devices, 86 clients). Discovered + fixed a `/network/dhcp` bug (`dhcp_enabled` queried a nonexistent metric → now derived from pool-metric presence). Required injecting the `unifi_api_key` secret into the local stack's DB for the collectors to scrape.

Code review (2 Opus passes): fixed PromQL label-injection (escape `device`/`mac` path params before interpolating into queries), wired up ~19 dropped port/radio metrics in `/devices/{device}` (collector emitted them but the endpoint hardwired None — contract gap for STAGE-020 drill-down), removed the never-populatable `UnifiDeviceRow.satisfaction`, and replaced the fragile device accumulator dict with a typed `_DeviceAccum` dataclass. Re-validated live: port/radio fields now populate with real values (speed_bps, poe, bytes, errors, satisfaction; radio cu/num_sta/tx_power/channel/bandwidth).

Verified: `make verify` GREEN (pyright --strict 0 errors, pytest 5466 passed, GLOBAL coverage 100.00% incl. the new router + collector metric, UI unchanged).

---

2026-06-20 8f92d80 EPIC-007/STAGE-007-018: nav restructure — remove Inventory, Crons under Integrations, add Unifi/Network

Removed the top-level Inventory page (a placeholder: header + single Crons tab). Relocated the Crons UI from `/inventory/crons*` to `/integrations/crons*` (full rename, no redirect; flat routes parented to `protectedLayoutRoute`; directory moved `routes/inventory/` → `routes/integrations/crons/`). Crons functionality preserved exactly — relocation, not rewrite.

Added `/integrations/unifi` (layout-with-child-tabs mirroring Home Assistant: layout + index + one Overview placeholder tab) and `/integrations/network` (flat placeholder mirroring Docker) as honest empty-state scaffolds for stages 020-023. NO fake data.

Sidebar: removed "Inventory"; under the "Integrations" group, indented alphabetical: Crons, Docker, Home Assistant, Network, Unifi (new icons: Clock/Network/Router).

Reused the EPIC-005 Home Assistant sidebar/router registration pattern; no new abstraction. No `/integrations` landing page (explicit non-goal).

Cross-epic provenance: Crons is an EPIC-002 cron-heartbeat UI surface, relocated here (closed epics aren't reopened). Spec §9.1 nav amendment.

Tests: moved + updated 9 cron test files (path/`from:`/href assertions), deleted Inventory.test.tsx, updated SidebarNav test, added 3 placeholder render tests (assert honest empty-state, no fake data).

Verified: `make ui-verify` GREEN (tsc 0 errors — the typed-router safety net confirmed no stale `/inventory` reference; vitest 1561 passed; build OK; 0 ESLint errors). User approved Desktop + Mobile on the PROD stack (rebuilt via `make dev-prod`).

---

2026-06-20 5660516 EPIC-007/STAGE-007-016A: vector config auto-reload on re-render (`--watch-config`)

Added `--watch-config` to the prod vector service command so the `homelab-vector` container auto-reloads its configuration when the monitor re-renders `vector.toml`, eliminating the manual `docker restart homelab-vector` step after any vector template/config change. Closes the deploy-propagation gap found during STAGE-007-016 Refinement (a fixed-and-valid config on disk did not take effect until vector was manually restarted).

Design: Selected Approach 1 (`--watch-config`) over Approach 2 (SIGHUP-on-render). SIGHUP is structurally redundant because render runs only at monitor boot and `vector depends_on monitor: service_healthy`, so vector starts (and reads fresh config) AFTER the render. The `--watch-config` flag enables file-watch-based reloads, ensuring any `vector.toml` swap (via atomic rename during re-render) triggers an in-process graceful reload without stopping the container.

Build: `deploy/compose/docker-compose.yml` vector `command:` updated to `["--config", "/etc/vector/vector.toml", "--watch-config"]`. Single edit covers prod, `make dev`, and `make dev-prod` (dev override only adds `ports:`; compose deep-merges command from base). Corrected build-phase assumption: vector 0.41.1 has only `--watch-config` (no `--watch-config-method poll` — that flag arrived in 0.43.0). The 0.41.1 default inotify watcher is already atomic-rename-safe (it re-arms its inode watch before reloading), so it correctly catches the monitor's `os.replace` write. No `poll` fallback needed. Updated `apps/monitor/homelab_monitor/kernel/cron/render.py` docstring to reflect auto-reload (no logic change).

Refinement validation (prod rig): 3 successive atomic-rename config swaps each produced `Vector has reloaded.` logs with the vector PID unchanged (in-process graceful reload, not a restart). No restart loop, no event loss, no dropped metrics. Reload latency ~12-21s (inotify on the overlayfs-backed named volume). Documentation consistency: updated `docs/admin/redaction.md` (operator runbook — restart monitor only; vector auto-reloads) and `epics/EPIC-003-docker/regression.md`; added STAGE-007-016A forward-pointers to historical "no hot-reload" notes in `epics/EPIC-003-docker/STAGE-003-002.md`, `epics/EPIC-002-heartbeat-cron/STAGE-002-012.md`, and `epics/EPIC-004-logs-pipeline/{regression.md,STAGE-004-004.md}`.

Files: deploy/compose/docker-compose.yml (command edit), apps/monitor/homelab_monitor/kernel/cron/render.py (docstring), docs/admin/redaction.md + epics/EPIC-003-docker/regression.md + epics/EPIC-003-docker/STAGE-003-002.md + epics/EPIC-002-heartbeat-cron/STAGE-002-012.md + epics/EPIC-004-logs-pipeline/regression.md + epics/EPIC-004-logs-pipeline/STAGE-004-004.md (documentation updates + forward-pointers).
