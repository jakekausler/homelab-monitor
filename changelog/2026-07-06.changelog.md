2026-07-06 afe0117 EPIC-010 / STAGE-010-002: Schema evolution — scorecards + new outcome enums + new supporting tables
2026-07-06 bd3c777 EPIC-010 / STAGE-010-001: SignatureWentSilent purge + rule removal

Hard-delete 158k+ noise alerts (94.5% of prod's `alerts` table) and remove the
vmalert rule that produced them. Adds `hm alerts purge` CLI subcommand
(dry-run + confirm-phrase gated), new `VmalertReloader` kernel module, deletes
`deploy/vmalert/metrics/signature_silent.yaml`. Executed live on prod
2026-07-06: 172,750 rows deleted, 1 audit_log row written, idempotent second
run confirmed (row_count=0 audit row). vmalert `/-/reload` succeeded on both
runs.

**Retrofit for:** EPIC-004 (log signature system). Underlying
`homelab_log_signature_*` metric collection is untouched; only the noisy
alerting rule is removed. Future stages may re-enable signature-silent
alerting with different thresholds.

**Deliverables:**

- NEW: `apps/monitor/homelab_monitor/cli/alerts.py` — `hm alerts purge`
  subcommand. Modes: `--dry-run` (report count, no write) OR `--confirm-phrase`
  (case-fold match against `"delete signature-silent noise"`). Single-
  transaction: `SELECT COUNT` → `insert_audit()` → `DELETE`; post-commit
  `VmalertReloader.reload()` (non-fatal on failure). `HOMELAB_MONITOR_VMALERT_URL`
  env var overrides default `http://vmalert-metrics:8880`.
- NEW: `apps/monitor/homelab_monitor/kernel/vmalert/{__init__,reload.py}` —
  `VmalertReloader` mirrors `AlertmanagerReloader`; owns its own
  `httpx.AsyncClient` lifecycle (CLI has no shared app-lifespan client);
  `httpx.Timeout(5.0, connect=2.0)`; warn-on-failure returns False, never
  raises.
- EDIT: `apps/monitor/homelab_monitor/cli/main.py` — register `alerts`
  subparser.
- DELETED: `deploy/vmalert/metrics/signature_silent.yaml` (file contained one
  group with one rule; whole-file delete cleaner than comment-out).
- NEW tests: 6 tests for `VmalertReloader` (success/failure/network-
  error/default+custom URL/client-injection), 11 tests for `hm alerts purge`
  (mode-validation × 2, phrase-mismatch, dry-run, success, case-fold,
  idempotency, isolation, reload-failure-non-fatal, dispatcher, argparse-
  reject-missing-alertname, empty-alertname-rejection).

**Design decisions (autonomous loop; recorded in STAGE-010-001.md
`Selected Approach`):**

- SQL uses `json_extract(payload_json, '$.labels.alertname')` — `alertname`
  is not a top-level column on `alerts` (verified against migrations
  0001/0005); it lives inside `payload_json.labels.alertname`. Correction
  to the stage card's originally-written SQL literal.
- Rule file deleted, not commented out (only rule in file).
- CLI is narrow (`--alertname` filter only, no generic-purge). Prevents
  foot-cannon; matches EPIC-010's scope discipline.
- Confirm-phrase text `"delete signature-silent noise"` follows the
  STAGE-009-007 kill-switch precedent (natural-language, case-fold).
- vmalert `/-/reload` in runtime code, not a manual Refinement step (matches
  STAGE-010-012's future Apply-action pattern).
- Idempotent second run still writes an audit row (row_count=0) — matches
  auto-fix safety model preference for explicit-attempted trace.

**Prod-side Refinement (executed live on instance A, container
`homelab-monitor`):**

- Rebuilt monitor image (`make compose-build`, sha256:9046cf9…); recreated
  container with `--no-deps --force-recreate monitor`; all 14 sidecars
  preserved.
- Pre-purge: 186,774 total alerts; 172,750 SignatureWentSilent (drift
  +8.8% from 2026-07-05 recon of 158,782, explained by 24h accumulation +
  ~28min post-`rm` before vmalert config-poll dropped the rule).
- Dry-run: reported count without deleting; no audit row.
- Real purge (confirm-phrase accepted): exactly 172,750 rows deleted;
  audit_log row `who=cli:stage-010-001-purge, what=alerts_bulk_purged,
  before={row_count:172750, alertname:SignatureWentSilent, source_tool:
  vmalert-metrics, alertgroup:signature_silent}, after=null`; vmalert
  `/-/reload` returned HTTP 200 (belt-and-suspenders — config-poll already
  dropped the rule earlier).
- Idempotent second run: 0 rows deleted, second audit row with row_count=0,
  vmalert reload also 200.
- Post-purge: 14,024 total alerts; other 9 alertnames in top-10 unchanged
  (CronRunFailed +1 is natural live inflow).
- Container health preserved throughout; no crashes.

**Regression items filed (`epics/EPIC-010-tool-effectiveness/regression.md`):**

- Prod monitor image lacks `sqlite3` binary — python3 fallback used;
  runbook implications.
- 589 orphaned `firing` alert rows with `last_seen_at` frozen at
  2026-06-22 — reconciliation gap when a rule disappears without an
  explicit resolved push. Not blocking STAGE-010-001 (all purged); may
  affect analyzer accuracy for other retired rules.

**Code-review:** 1 Opus pass found 0 Critical, 0 Important, 10 Minor
findings; 9 applied, 2 skipped with rationale (Finding 7: `alerts`
parent-parser `set_defaults` kept intentionally — removing it degrades
`hm alerts` bare UX; Finding 8: `_SUBCOMMANDS` tuple YAGNI). 2nd
review confirmed all fixes correct and skip rationales sound.

**Verification:** `make verify` — full backend gate green, 100.00%
coverage, 7362 passed + 1 expected `test_vector_template` skip
(vector binary not on PATH).

---
