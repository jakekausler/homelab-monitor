## [STAGE-004-012A] Service source_type field + grouped/collapsible stream picker

**Summary:** Extended STAGE-004-012 with source-type classification and grouping. Vector ingest now assigns an explicit `source_type` field to every log line: docker container logs → "docker", systemd/journald units → "systemd", cron (CRON/crond or the hmrun wrapper) → "cron", else "unknown" (via new `source_type_classify` VRL transform; cron classification checked before systemd, so raw cron.service journald lines classify as cron). `source_type` is a low-cardinality VictoriaLogs stream field.

**Frontend (grouped/collapsible stream picker):**
- **Changed:** `LogsServicesSidebar.tsx` (desktop) and `LogsServicesDialog.tsx` (mobile) now group services into collapsible sections by source type (docker, cron, systemd, others, unknown-last), each with aggregate count, collapse/expand toggle, and tri-state select-all/select-none control per section. A service logging under multiple source types appears in each relevant section.
- **Changed:** `LogsExplorerPage.tsx` (selection now per-identity: service + source_type pair). `LogsExplorerBody.tsx` (group rendering and collapse state). `router.tsx` (identity-qualified `services` URL param).

**Backend (per-identity service stats + filtering):**
- **New:** `source_type_classify` VRL transform in vector config (classify logs as docker/systemd/cron/unknown).
- **Changed:** `GET /api/logs/services` now returns `{services:[{service,source_type,count}], truncated}` — one row per (service, source_type) pair (no collapse); OpenAPI regenerated (ServiceCount gains source_type field).
- **Changed:** `GET /api/logs/query` `services` param is now identity-qualified e.g. `docker:nginx,cron:hmrun` (supersedes STAGE-004-012's bare-name `&services=a,b` format). Client side OR's selections; both halves quoted via `logsql_quote_phrase`.

**Verification:** Validated by real-VictoriaLogs 3a/3b e2e against REAL host data (source_type correctly classifies real docker/systemd/cron logs; per-identity rows + identity filtering confirmed; special-char quoting end-to-end). User Desktop+Mobile approval.

**Notes:** Inserted per user request after observing STAGE-004-012's flat sidebar mixed crons and docker logs, reducing clarity.

### Commits
- `Commit: 49f99a9d38d1b0d486d892ff772efae47c092654`

---

## [STAGE-004-012] Stream picker sidebar

**Summary:** Added a left-side "Services" sidebar to the Logs Explorer (`/logs`) listing distinct `service` values with line counts (window-scoped, sorted descending) for the current time range. Click to filter; selected services show as removable chips above the search box. Multiple services OR together and AND with the user's search/LogsQL query (works in both plain and advanced modes; the user's expression is never mutated). Selection is reflected in the URL as `?services=a,b` (shareable/reload-safe). Mobile: sidebar becomes an overlay dialog (shared Radix Dialog primitive) toggled by a "Services" button.

**Backend (VictoriaLogs stats + caching):**
- **New:** `GET /api/logs/services?start&end&limit=100` endpoint returns `{services:[{service,count}], truncated}` powered by VictoriaLogs `stats by (service) count()` with 30s cache and top-100 with truncated flag.
- **New:** Optional `services` CSV param on `GET /api/logs/query` that AND-composes `(service:"a" OR ...) AND (<expr>)` server-side via the canonical `logsql_quote_phrase` helper.
- **Tests:** Backend e2e validated against real VictoriaLogs (including special-char service-name quoting).

**Frontend (sidebar + dialog layout + URL state):**
- **New:** `LogsServicesSidebar.tsx` (desktop) and `LogsServicesDialog.tsx` (mobile overlay) showing service chips with line counts. `LogsServiceChip.tsx` (removable chip component with count badge).
- **Changed:** `LogsExplorerPage.tsx` (independent `selectedServices` state; `services` URL param). `LogsExplorerBody.tsx` (sidebar/dialog layout management; conditional rendering by viewport). `router.tsx` (`services` in `validateSearch`).
- **State:** Counts are window-scoped (refresh on range change only), independent of search/selection.
- **Mobile UX:** Sidebar toggle becomes a "Services" button; clicking opens overlay dialog (shared Radix Dialog primitive).

**Verification:** User Desktop+Mobile approval.

**Deferred:** Service grouping by type (STAGE-004-012A).

### Commits
- `Commit: 37a4a3fd1d0ebc18530c1e4da69447fee8ae9330`

---

## [STAGE-004-011] LogsQL advanced mode + syntax highlighting

**Summary:** Added an "Advanced (LogsQL)" toggle to the Logs Explorer (`/logs`). When enabled, the plain-text search box swaps for a CodeMirror 6 editor with basic LogsQL syntax highlighting; the raw LogsQL is sent to VictoriaLogs as the query `expr` (plain mode still translates to `_msg:"…"`). Toggling preserves both modes' text. Deep-linkable via `?logsql=<expr>` (mutually exclusive with `?q=`).

**Frontend (CodeMirror 6 integration + state management):**
- **New:** `apps/ui/src/components/logs/logsQlLanguage.ts` — CodeMirror `StreamLanguage` tokenizer + `HighlightStyle` for LogsQL keywords/fields/strings/numbers/operators. `LogsQlEditorImpl.tsx` (hand-wired CM6 EditorView; lazy-loaded chunk). `LogsQlEditor.tsx` (shared shell: plain `<textarea>` on narrow viewports / Suspense-wrapped CM6 on desktop). `AdvancedToggle.tsx` (toggle control).
- **Changed:** `LogsExplorerPage.tsx` (independent `advancedMode` / `plainText` / `logsQl` state; `logsql` URL param). `LogsExplorerBody.tsx` (toggle + conditional editor). `router.tsx` (`logsql` in `validateSearch`). `vitest.config.ts` (exclude CM6 EditorView impl from coverage — non-functional in jsdom, validated via textarea-fallback tests + manual Refinement).
- **Deps:** `@codemirror/state`, `@codemirror/view`, `@codemirror/language`, `@codemirror/commands`, `@lezer/highlight` (lazy-loaded; editor chunk loads only when advanced mode is first enabled on desktop). First lazy/Suspense usage in the UI.
- **Editor UX:** visible caret + click-anywhere-to-position (CM theme fills the box); Enter submits, Shift+Enter newline.

**Backend (time-window clock-skew fix + test infrastructure hardening):**
- **Fixed (clock-skew, backend):** `parse_and_validate_window` now allows a 5-second future grace (`FUTURE_SKEW_GRACE`) so an absolute `end == browser-now` is not rejected as `range_in_future` under browser/container clock skew; genuinely-future ranges still rejected. (`apps/monitor/.../kernel/logs/time_window.py` + 4 tests.)
- **Fixed (test infra):** `tests/_uvicorn_fixture.py` overrides `HOMELAB_MONITOR_DOCKER_SOCKET` to a nonexistent path so the SSE smoke test's startup collectors don't do real Docker/registry I/O (was flaking when a real Docker daemon is reachable).

**Docs:** `docs/admin/logs-explorer.md` extended with an "Advanced LogsQL mode" section.

**Out of scope (unplanned):** LogsQL autocomplete / query builder, syntax error highlighting.

**Tests:** logsQlLanguage.test.ts (tokenizer coverage for keywords/fields/strings/numbers/operators), LogsQlEditor.test.tsx (textarea fallback on narrow viewports, Suspense boundary, enter/shift+enter wiring), AdvancedToggle.test.tsx, LogsExplorerPage.test.tsx extended (toggle state preservation, `logsql` URL param hydration, mode-exclusive param behavior), time_window.test.py (clock-skew grace + boundary cases), _uvicorn_fixture.test.py (Docker socket override validation).

**Verification:** Pending.

### Commits
- `Commit: f77395c02a4e0b4d456d5479c3fb209ea0aa3472`

---
