2026-06-17 ff7d664 EPIC-017/STAGE-017-002: ssh_targets config model + per-target key secret convention

Declarative SSH target model for the EPIC-017 probe framework. Adds a pydantic-validated `ssh_targets:` config section and a config-backed resolver that fills the seam STAGE-017-001 left open (the lifespan factory's `resolve=lambda tid: None` placeholder).

**Components:**
- New `apps/monitor/homelab_monitor/kernel/ssh/config.py`:
  - `SshTargetConfig` (pydantic v2, `ConfigDict(extra="forbid")`) — per-target fields: `id`, `host`, `port` (1–65535, default 22), `user`, `account_mode` (`appliance` | `dedicated-user`), `key_secret_ref` (defaults to `ssh_probe_key_<id>`), `host_key` (bare OpenSSH pubkey; pinned later by 017-004), `forced_command`, `script_id`, `concurrency_group`.
  - Validation: `account_mode` hyphen form `dedicated-user` normalized to `dedicated_user`; `host_key` validated as a BARE OpenSSH pubkey; `forced_command` XOR `script_id`; duplicate `id` rejected.
  - `load_ssh_targets()` — zero-arg loader: self-reads `HOMELAB_MONITOR_CONFIG`, fallback cascade, wraps per-entry errors, returns `dict[str, SshTargetParams]`.
- `kernel/ssh/__init__.py`: exports `SshTargetConfig`, `load_ssh_targets`.
- `kernel/api/lifespan.py`: loads targets at startup and wires `resolve=ssh_targets.get` into `AsyncSshClientFactory` (replaces placeholder).
- Public default is EMPTY; host overrides repo declares real targets.
- Tests: 26 cases, 100% branch coverage on `config.py`. Verification green; refinement validated real loading path + factory resolver end-to-end.

All changes maintain 100% kernel coverage + full test suite passing.

2026-06-17 14:27 [pending-commit] EPIC-002: heartbeat-cron wrapper bugfixes

Post-completion bugfixes for EPIC-002 heartbeat-cron wrapper subsystem:

1. **UI wrapper-installed indicator** — CronDetail "Wrapper" row + CronsTable "Wrapper" column now show `wrapper_installed` (boolean state) instead of `wrapper_last_seen_at` (liveness). Displays "Installed"/"Not installed" with ✓/— badge. Old misleading text "No wrapper installed (heartbeats from ad-hoc curl)" removed.

2. **UI last-ok column wired** — CronsTable "Last OK" column previously hardcoded to "—"; now displays `last_ok_at` from backend LEFT JOIN heartbeats_state in list_crons. Regenerated openapi.json + UI schema.ts.

3. **Wrapper install/uninstall error mapping** — install.py now maps host-executor error codes `write_failed`/`crontab_missing` to CrontabWriteError (HTTP 500) instead of misleading CronLineNotFoundError (409), in both install and uninstall paths.

4. **Wrapper crontab writes + escape handling** — scripts/hm-cron-apply.sh:
   - /etc/crontab writes now work under systemd ProtectSystem=strict via Option-Z hybrid (scratch tempfile in RESULTS_DIR + rewrite-in-place, preserving snapshot/rollback).
   - apply_line_replace fixed awk escape-processing issue (lines with backslash escapes like Debian certbot's `\!` previously failed silently). Fixed by passing lines via awk ENVIRON[] instead of -v (no escape-processing).
   - Requires `sudo bash scripts/host-setup.sh` re-run to deploy the systemd unit change.

All changes maintain 100% kernel coverage + full test suite passing.

2026-06-17 f91863d EPIC-017/STAGE-017-001: asyncssh SSH transport (EPIC-017 SSH probe framework, foundation stage)

Filled the kernel's SSH stubs with a real asyncssh-based implementation:

1. **SSH transport package** — `kernel/ssh/` package (params/result/errors/client) providing `AsyncSshClientFactory` + `_AsyncSshConnection`, `SshTargetParams`, `SshCommandResult`, and the `SshTransportError` hierarchy (`HostKeyNotPinned`/`HostKeyMismatch`/`SshConnectionRefused`/`SshAuthError`/`SshTimeout`).

2. **Host-key pinning** — Per-target PINNED host-key verification (never blanket known_hosts; pinning a wrong key raises `HostKeyMismatch`). Open-per-run (no pool). Strictly key-only auth (agent + password disabled). Per-target ed25519 key read via a `secrets_for` seam. Connect/login/command all timeout-bounded.

3. **Protocol implementation** — Filled the `SshConnection` Protocol (`async run(command="") -> SshCommandResult`). Replaced the lifespan `_StubSshFactory` with the real factory (resolver seam for STAGE-017-002). Added `asyncssh>=2.18` (→2.23.1).

4. **Unit tests** — 22 unit tests via a real loopback asyncssh server (host-key accept/mismatch/not-pinned, auth, refused, timeout, error-mapping, secret-non-leak, agent-not-consulted). 100% coverage on kernel/ssh/.

5. **Refinement validation** — Read-only validation against the live UDM (appliance) + Synology (full-OS) with host-key pinning enforced.

All changes maintain 100% kernel coverage + full test suite passing.
