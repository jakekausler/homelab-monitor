# 2026-05-11

## STAGE-001-021: Full integration test rig + canonical e2e test

**Commit**: `84ededa`

**Commits**: 84ededa (docs), aea4e46 (review fixes), 426ef64 (rig stability), 92e37cc (CI fix), 2776eb1 (CI fix), c55f921 (CI fix), 0ac0d78 (diagnostic, reverted), 2530e63 (initial impl)

### Summary

Closes EPIC-001 (Foundation) with a complete integration test rig: containerized test services (logging, metrics, alerts, grafana, remediation) + heartbeat stub + integration test helpers + canonical e2e test. Enables ship-ready verification across all sidecars in one CI job (18/18 tests, 7m39s). Dev environment tooling (`dev-up.sh`, `dev-down.sh`, dev.env) and production Dockerfile fixes (alembic dir include, `/data` ownership) shipped.

### Highlights

- **Test rig core**: Rig class encapsulates container lifecycle + port management + healthcheck spinup + async context manager pattern. Heartbeat router stub (200 OK) for liveness probe. Test fixtures: `fixture-host` container (syslog + HTTP), `noisy-logger` (spam collector with metrics metadata). All fixtures in `deploy/compose/test-fixtures/` and mounted into rig.
- **Integration tests**: 5 test modules (fixtures, heartbeat, collector, plugins, remediation) covering service startup, metric collection from VictoriaMetrics, alert chain (vmalert → AM → Karma), log indexing in VictoriaLogs, Grafana provisioning, plugin sandbox, and mock fix-runner integration. All passing in CI (18/18).
- **Dev/prod tooling**: `scripts/dev-up.sh` (backend + ui + minimal rig sidecars), `scripts/dev-down.sh`, `deploy/dev/dev.env.example` (master key template, collector timeout tuning). Production Dockerfile fixed (alembic included, `/data` chowned to appuser). `docs/dev/local-environment.md` (single-command local dev setup). `docs/testing/integration.md` (rig architecture, adding tests).
- **CI integration**: `.github/workflows/ci.yml` integration job (runs full rig on Ubuntu 24.04, includes baseline sanity check before full suite). `.github/workflows/grafana-image.yml` job for independent grafana image build/push. CRG dependency added (stable graph on main).
- **Refinement fixes**: AM shared-GID alignment (spec §15.2.1), vector config node/pod/container metric subset (spec §10.5), `hm user create` stdin fallback (--password-stdin flag), master key validation (length ≥32 bytes), Alertmanager seed-config bootstrap pattern (email settings template).
- **Verification**: `make verify` green (1129 backend tests + 100% kernel coverage at 4741 stmts, 110 UI tests). CI integration job green (18/18 tests in 7m39s). All smoke tests passing.

### Code review

Two passes (Opus). Pass 1: 8 fixes applied + 8 items deferred to regression.md (I2, I4, I5, I7, I8, M3, M4, M6). Fixed items: C1 (Rig init error handling), C2 (fixture container health retries), I3 (heartbeat stub), I6 (integration suite fixture setup), I9 (test isolation), I10 (async cleanup), M2 (docstring clarity), M5 (magic number constants). Pass 2: 0 findings (clean delta).

### Deferred to regression.md (post-Foundation)

- I2: Parametrize rig service replicas
- I4: Helm/k8s integration test variant
- I5: Long-running rig stability test
- I7: Collector failure injection
- I8: Alert routing chaos test
- M3: Rig docstring examples
- M4: Test fixture metrics schema
- M6: CI job timeout tuning

### Acceptance

- `make verify` green: 1129 backend tests + 100% kernel coverage (4741 stmts) + 110 UI tests
- CI green: 4/4 jobs (backend, frontend, crg-build, integration)
- Integration job: 18/18 tests passing in 7m39s
- All smoke tests passing

### References

- Design spec §13.2 (rig architecture)
- Design spec §13.3 (integration test suite)
- Design spec §10.5 (collector metrics whitelist)
- Design spec §15 (remediation + alerting)
- EPIC-001 Foundation complete (21 of 21 stages shipped)
