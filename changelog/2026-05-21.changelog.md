# 2026-05-21

## STAGE-003-001 — cadvisor sidecar + scrape config + first container metrics in VM

First stage of EPIC-003 (Docker integration). Deploys cadvisor as a sidecar in prod, dev, and integration test rigs. Adds the curated scrape config + file_sd_configs extension point for STAGE-003-005+. Ships a dev-only `hm dev seed-container-metrics` CLI for synthetic container metrics during UI Refinement.

### Added

- **cadvisor sidecar** across all compose stacks:
  - `deploy/compose/docker-compose.yml` (prod)
  - `deploy/dev/docker-compose.dev.yml` (dev)
  - `deploy/compose/docker-compose.test.yml` (integration test)
  - Pinned to `gcr.io/cadvisor/cadvisor:v0.49.1` (D-CADVISOR-VERSION)
  - Non-privileged with no extra capabilities (D-CADVISOR-LEAST-PRIV — verified on Linux 5.15 cgroup v2)
  - Flags: `--docker_only=true`, `--store_container_labels=false`, `--whitelisted_container_labels=homelab-monitor.*,org.label-schema.*,com.docker.compose.*` (D-CADVISOR-FLAGS)
  - Dev rig published port `18081` (D-DEV-PORT-18081)
- **Curated scrape config** (`deploy/compose/vmagent/scrape.yaml`):
  - 13 metric families allowlist per D-CADVISOR-RELABEL-CURATED (CPU, memory, network, disk I/O essentials)
  - Filesystem-noise drop rule to exclude tmpfs/sysfs metrics
  - `file_sd_configs: ['targets.d/*.yml']` extension point for STAGE-003-005+ (D-SCRAPE-HYBRID)
- **Scrape config for test rig** (`deploy/compose/test-fixtures/vmagent-scrape.yaml`) — mirrors prod cadvisor job setup
- **Extension point directory** `deploy/compose/vmagent/targets.d/` with README documenting the contract for future stages
- **New `hm dev seed-container-metrics` CLI subcommand** (`apps/monitor/homelab_monitor/cli/dev.py`):
  - Syntax: `hm dev seed-container-metrics [--containers N] [--clear] [--force] [--vm-url URL]`
  - Deterministic seeding via RNG keyed on `(N, current_minute)` (D-SEED-CLI-DETERMINISTIC)
  - Labels all series with `homelab_synthetic="true"` for targeted cleanup via `--clear`
  - 5 shape variants (cpu-sawtooth, memory-step, network-bursts, idle, spiky) cycling on `idx%5`
  - Local-only (hostname gate via `socket.gethostname()` comparison)

### Changed

- **Hybrid dev-up script (`scripts/dev-up.sh`)** — added cadvisor to validated service list and port checks
- **Hostname validation** — replaced tautological `resolve_hostname()` call with `socket.gethostname()` for clearer intent (fixes gate path now exercised by STAGE-003-001 tests)
- **Prod compose invocations** — now explicitly pass `--env-file deploy/compose/.env` for deterministic env loading
- **Environment loader gating** — added early returns in `load_env()` and `assert_master_key_set()` to skip dev-only setup in prod mode
- **Port validation** — added cadvisor, vmagent, vector to free-port checks in dev-up

### Fixed

- **cadvisor missing from hybrid-mode service list** — added to `scripts/dev-up.sh` service validation
- **Tautological hostname gate** — `_validate_local_hostname` was reading the same env var it was checking against; replaced with OS-level `socket.gethostname()`
- **Missing env-file in prod compose** — explicit `--env-file deploy/compose/.env` in prod rig startup
- **Dev env sourced unconditionally** — added prod-mode early return to `load_env()`
- **Master key assertion in prod** — added prod-mode early return to `assert_master_key_set()`
- **Incomplete port validation** — added cadvisor (18081), vmagent (18429), vector (18686) to free-port checks

### Files Changed

NEW:
- `deploy/compose/vmagent/targets.d/README.md` — extension point contract documentation
- `apps/monitor/homelab_monitor/cli/dev.py` — new file housing `seed-container-metrics` subcommand
- `apps/monitor/tests/test_cli_dev_seed_container_metrics.py` — 24 tests for seed CLI
- `apps/monitor/tests/integration/test_cadvisor_scrape.py` — 3 integration tests for cadvisor metrics

MODIFIED:
- `deploy/compose/docker-compose.yml` — added cadvisor service definition
- `deploy/dev/docker-compose.dev.yml` — added cadvisor service, port 18081 binding
- `deploy/compose/docker-compose.test.yml` — added cadvisor service for test rig
- `deploy/compose/vmagent/scrape.yaml` — rewrote with curated metric allowlist + file_sd extension point
- `deploy/compose/test-fixtures/vmagent-scrape.yaml` — added cadvisor job matching prod
- `scripts/dev-up.sh` — cadvisor in service list, hostname gate refactor, port checks, env-file handling
- `apps/monitor/homelab_monitor/kernel/config.py` — hostname resolution refactored to use `socket.gethostname()`
- `apps/monitor/tests/test_cli_dev_seed_cron_runs.py` — updated monkeypatch for hostname gate refactor
- `packages/shared-types/openapi.json` — regenerated (no new API routes, but cadvisor metrics available in VM)

### Tests

- `apps/monitor/tests/test_cli_dev_seed_container_metrics.py`:
  - `test_seed_basic_shapes` — verifies 5 shape variants produced
  - `test_seed_deterministic_rng` — same N and minute produce identical series
  - `test_seed_clear_flag` — `--clear` removes only synthetic series
  - `test_seed_refuses_when_env_hostname_differs_from_os` — gate rejects mismatched hostname (previously broken path)
  - 20 additional edge-case tests
- `apps/monitor/tests/integration/test_cadvisor_scrape.py`:
  - `test_cadvisor_scrapes_real_containers` — verifies ≥35 container metrics collected in dev rig
  - `test_cadvisor_respects_allowlist` — confirms curated metric families only
  - `test_cadvisor_drops_filesystem_noise` — confirms tmpfs/sysfs exclusion

Backend test summary:
- 2261 backend tests pass; 100% kernel coverage; ESLint at baseline 35
- 316 UI tests pass

### Verification

- **3a (dev rig, synthetic data)** — cadvisor scrapes 35 real container series; CLI produces 5 synthetic series with distinct shapes; `--clear` removes synthetic only; hostname gate rejects mismatched env.
- **3b (prod rig, real data)** — `make dev-prod` brings up monitor on `0.0.0.0:29090` + sidecars container-internal; cadvisor scrapes 41 real containers; vmagent target healthy; no privilege errors.
- **Verify GREEN** — all linters/type-checkers clean, full test suite passing

### References

- Design spec: `docs/superpowers/specs/2026-05-04-homelab-monitor-design.md` (§5.2 Docker integration)
- Stage tracking: `epics/EPIC-003-docker-integration/STAGE-003-001.md`

### Commit

2c2d6f0
