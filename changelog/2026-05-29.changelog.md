## [STAGE-004-006] Redaction pipeline (Vector strips 5 sensitive patterns before VictoriaLogs)

Backend (Vector + config + metrics + tests):
- Added `apps/monitor/homelab_monitor/kernel/config.py` — `RedactPattern` dataclass + `DEFAULT_REDACT_PATTERNS` (5 well-known patterns: bearer tokens, JWTs, passwords-in-URLs, AWS access keys, generic API keys) + `load_redact_patterns`/`_validate_redact_entry` (validation: non-empty string fields, unique snake_case names, no regex lookarounds; absent `logs.redact` → defaults, explicit list REPLACES defaults).
- Added `apps/monitor/homelab_monitor/kernel/cron/render.py` — `build_redact_vrl` / `build_redact_strip_markers` / `build_redact_metric_entries` generators; `render_on_boot` loads patterns (config error → empty fallback + loud log) and renders the redact transforms into vector.toml.
- Updated `deploy/vector/vector.toml.template` — `redact_main`/`redact_hmrun` (raw-string `r'...'` VRL, `to_string(.message) ?? ""` coercion, integer markers `.rdt_<name>` 0/1), `strip_markers_*` (del markers before VL), `redaction_metric` (log_to_metric, `increment_by_value=true`, static `pattern_type` literal tags), `redaction_metrics` prometheus_exporter :9598. DAG: drop_noise→redact_main→strip_markers_main→throttle; hmrun_shaped→redact_hmrun→strip_markers_hmrun→vl.
- Updated `deploy/compose/docker-compose.yml` (vector expose 9598) + `deploy/compose/vmagent/scrape.yaml` (vector:9598 scrape job).
- Added `apps/monitor/homelab_monitor/kernel/metrics/redaction_audit.py` (new) + `lifespan.py` registration — `RedactionAuditCollector` (300s): queries `vector_redactions_total` from VictoriaMetrics, writes ONE counts-only `audit_write(what="logs.redaction_counts")` row per tick; never values, never to VL.
- Added `docs/admin/redaction.md` (new) — operator guide: default patterns, adding custom patterns (explicit list replaces defaults), validation rules, verifying via the metric, security notes.
- Tests: `test_redaction_config.py`, `test_render_redaction.py`, `test_redaction_audit.py` (new); extended `test_vector_template.py` / `test_vector_render.py`. Patterns enforce no-lookarounds, unique snake_case names. Vector-0.41.1 API corrections validated: VRL slash-literals `/.../ ` unusable → `r'...'` raw-string regex args; no VRL `emit_metric()` → `log_to_metric` transform; `$` in replacement → `$$`; `log_to_metric` ERRORS on absent field → integer markers ALWAYS-set 0/1 + `increment_by_value=true`; bearer_token floor `{20,}` (anti over-redaction).
- Secrets never reach VictoriaLogs — redaction happens INGEST (before VL sink). Per-pattern_type counter `vector_redactions_total` + a 5-min counts-only audit log. Patterns are operator-configurable via `homelab-monitor.yaml` `logs.redact:`, rendered into vector.toml at monitor boot.

Verification:
- `make verify`: GREEN (3341 backend passed, 100% kernel coverage; UI clean).
- docker `vector validate` (timberio/vector:0.41.1-debian): PASS.
- Real-rig Refinement (planted 5-pattern corpus on docker+journald): no raw secret reached VL (direct searches → 0); `vector_redactions_total{pattern_type}` populated in VictoriaMetrics; NOMATCH not over-redacted; markers stripped before VL; audit counts-only.

Locked decisions honored: D-REDACT-VECTOR-LAYER, D-REDACT-YAML-CONFIG, D-REDACT-AUDIT-COUNTS-ONLY (pattern_type label is a static literal — counts-only structurally enforced), D-REDACT-V1-PATTERNS, D-REDACT-BEFORE-UI-WORK.

### Commits
- `Commit: 63e9f2b` — feat(STAGE-004-006): redaction pipeline (Vector strips sensitive patterns before VictoriaLogs)

---

## [STAGE-004-005] Cron fingerprint enrichment (journald `HM_FP` → VictoriaLogs `cron_fingerprint`)

Backend (vector + tests + cron wrapper):
- Updated `apps/monitor/homelab_monitor/data/cron-with-heartbeat.sh.tmpl` to emit structured journald entries via `logger --journald` (fields: `SYSLOG_IDENTIFIER=hmrun`, `PRIORITY=5`, `HM_RUN=<uuid>`, `HM_FP=<64-char fingerprint>`, clean `MESSAGE`). Per-line output (HM_RUN_START / body / HM_RUN_END boundary markers) now carries the fingerprint. Legacy `HM_RUN=<uuid> ` text-prefix passthrough preserved for backward compatibility; stdout passthrough unchanged. Best-effort "never break the cron" safety model maintained.
- Updated `deploy/vector/vector.toml.template` `hmrun_shaped` transform to extract `.cron_fingerprint = .HM_FP` and `.run_id = .HM_RUN` (each `exists()`-guarded, independent). Retains legacy `HM_RUN=<uuid> ` text-prefix regex as a transitional fallback for in-flight lines. Strips raw `HM_FP`/`HM_RUN` journald fields after extraction to prevent double-shipping.
- Bumped `apps/monitor/homelab_monitor/kernel/cron/wrapper_constants.py` `WRAPPER_FORMAT_VERSION` from `1.0.0` → `1.1.0` (log-emission format change; re-apply wrapper to deploy).
- New tests: template-parse + wrapper-emission tests validating per-line fingerprint carry. Version-coupled test assertions refactored to reference `WRAPPER_FORMAT_VERSION` constant instead of hardcoded literals (future-proofs format bumps).
- Every hmrun log line (START / body / END) now carries a top-level, queryable `cron_fingerprint` field in VictoriaLogs. Foundation for Drain per-cron model keying (STAGE-004-025), the field inspector (016), deep-links (021), and cron-failure log correlation (034).

Verification:
- `make verify`: GREEN (3294 backend passed, 1 skipped, 100% kernel coverage; UI clean).
- Independent docker `vector validate` (timberio/vector:0.41.1-debian): PASS.
- Refinement 3a+3b (dev rig + real prod compose stack with real rtlamr-watchdog cron): 6/6 acceptance — every hmrun line carries full 64-char `cron_fingerprint`, clean `_msg`, cross-correlates exactly to `cron_runs.cron_fingerprint`; legacy fallback keeps `run_id` working on old-format lines.

Locked decisions honored: D-FINGERPRINT-PER-LINE (every line incl. boundary markers), D-ENRICHMENT-IS-ADDITIVE (independent run_id extraction + legacy fallback), full 64-char fingerprint (no truncation), fingerprint-only (no extra per-line fields).

### Commits
- `Commit: 4c5f2c9` — feat(STAGE-004-005): cron fingerprint enrichment via journald HM_FP → VictoriaLogs cron_fingerprint

---

## [STAGE-004-004A] Docker log severity-level extraction (vector docker_severity_extract)

Backend (vector + tests):
- Added `[transforms.docker_severity_extract]` VRL remap to `deploy/vector/vector.toml.template`, `inputs=["docker_enrich"]`, placed AFTER docker_enrich; drop_noise rewired to consume it; sinks.vl unchanged. Promotes leading severity tokens in docker log messages to canonical `.severity` values (queryable in LogsQL). Seven anchored patterns (emergency/alert/critical/error/warn/notice/debug), each accepting optional ANSI escape + optional ISO timestamp prefix + token with word-boundary, OR `level=<token>` logfmt form. Token mapping: FATAL→critical, PANIC→emergency, CRIT→critical, ERR→error, WARNING→warn. Guard: `(.severity == "info" || is_null(.severity)) && exists(.label) && exists(.message)` — only docker-sourced lines still at the add_labels default are re-parsed; journald lines (whose .severity came from .PRIORITY) are skipped. No regex lookarounds. Emits canonical lowercase strings only (every output in `_CANONICAL_SEVERITIES` 8-set).
- Closes STAGE-004-004's false-green VRL-syntax-error gap with a new authoritative `vector validate` gate: `@pytest.mark.slow test_rendered_template_passes_vector_validate` runs plain `vector validate` (NOT --no-environment) on the rendered template, skips gracefully when the vector binary is absent (local devs), runs in CI. The structural fix CAUGHT the very first VRL bug it was designed for during Build (E620 `match!` on infallible function), exactly as intended.
- 10 new template-parse tests + 1 renamed test (drop_noise rewire) in `apps/monitor/tests/test_vector_template.py`: transform present; inputs wiring on both ends of the rewire; guard present; canonical-value assignments via VRL-source string match; no-lookarounds; Python-`re` pattern compile smoke; programmatic match assertions for ERROR + WARN + CRITICAL (covering bare token, bracketed `[TOKEN]`, ANSI-then-timestamp form, `level=...` logfmt) plus mid-sentence rejection; FATAL→critical mapping; emitted-values-canonical via `_CANONICAL_SEVERITIES` from models.py (truth source). Pattern count locked at exactly 7 via `_EXPECTED_SEVERITY_PATTERNS = 7` — catches reordering/insertion regressions.
- Validated end-to-end against real prod rig (make dev-prod, real HA + journald + caddy containers, LogsQL): real HA `ERROR (MainThread)` lines (incl. `\x1b[31m...` ANSI form) → `severity:error AND compose_service:homeassistant` returns them; severity distribution sane (126 info / 10 error in 15min for HA); INFO lines and Traceback continuations stay info; mid-sentence "error" prose does NOT promote; journald/systemd lines preserve their PRIORITY-derived severity (the `exists(.label)` guard excludes them from docker_severity_extract). 5/5 acceptance scenarios PASS; logfmt scenario deferred (no active logfmt-emitting container in this rig).
- Two seed corrections from Design: extraction location is INGEST (not read-time) because three downstream stages hard-depend on severity being a queryable VL field (STAGE-004-037 error-rate, STAGE-004-039 severity-escalation alerts, STAGE-004-011 LogsQL advanced filters). VRL `match()` is INFALLIBLE — never use `match!` (Vector rejects with E620). Added to project VRL gotchas list.

make verify: GREEN (3284 backend passed, 1 skipped — the slow vector-validate test, 100% kernel coverage, frontend clean).

### Commits
- `Commit: c163de5` — feat(STAGE-004-004A): vector docker_severity_extract + authoritative vector-validate test gate

---

## [STAGE-004-004] Container label enrichment (vector docker_enrich)

Backend (vector + tests):
- Added `[transforms.docker_enrich]` VRL remap to `deploy/vector/vector.toml.template` (inputs=["parse_json"], placed after parse_json so trusted container metadata wins over app-JSON; drop_noise rewired to consume docker_enrich; sinks.vl unchanged). Promotes docker compose labels to top-level LogsQL-queryable fields: `compose_project`, `compose_service`, `image_revision` (from labels), and parses `image_name`/`image_tag`/`image_digest` from the `.image` ref (split-based, no regex; image_tag defaults "latest"; image_digest only when @sha256 present). journald entries no-op via exists(.label)/exists(.image) guards. Raw `.label.*` bag kept (D-KEEP-RAW-LABEL-BAG).
- 10 new template-parse tests in `apps/monitor/tests/test_vector_template.py` (transform presence, wiring on both ends of the rewire, field-name presence, snake_case enforcement, journald guards, no del(.label), no-lookarounds, sink unchanged).
- Two seed corrections folded in during Design: Vector's docker_logs emits only `.image` (parsed here, not pre-split); `org.opencontainers.image.revision` is a git revision (promoted as `image_revision`, NOT image_digest).
- Validated end-to-end against the prod rig (make dev-prod, real container logs, LogsQL): `compose_project:homelab-monitor` returns real lines with the enriched top-level fields; raw label bag kept; journald lines unenriched. (A VRL syntax error — dynamic array index `segs[length-1]`, illegal in VRL which requires integer-literal indices — was caught only by authoritative `vector validate` during Refinement, NOT by the template-parse tests; fixed with a for_each loop.)
- Fixed pre-existing test-isolation gap (per CLAUDE.md fix-by-default): added the `_mock_vm_lifespan_tick` autouse guard to `apps/monitor/tests/test_api_lifespan.py` (the last real-lifespan+httpx_mock module lacking it; a DockerSocketCollector background tick flakily tripped pytest-httpx teardown). Latent since STAGE-003-005.

make verify: GREEN (3274 backend passed, 100% kernel coverage, frontend clean).

### Commits
- `Commit: 5e74b50` — feat(STAGE-004-004): vector docker_enrich container label enrichment + pre-existing lifespan-guard fix

---

## [STAGE-004-003] Shared `<LogViewer>` component + log-display features + test-harness boot-once refactor

Frontend (commit 1):
- Extracted a shared `<LogViewer>` / `LogLineList` / `LogBanner` component family into `apps/ui/src/components/logs/` from the docker container + cron run log viewers (caller-provides-`useLogs()` embedding contract; README documents embedding for future detail pages).
- Added ANSI SGR rendering (`ansi.ts`): docker logs with `\x1b[..m` escapes render as colored React spans (8/16 color + bold/dim/underline; 256/truecolor consumed-ignored; no dangerouslySetInnerHTML; Tailwind-v4 literal classes).
- Added a Wrap toggle (native checkbox) to both viewers — switches long lines between soft-wrap and horizontal scroll (default nowrap).
- Restructured log lines into a timestamp column with a vertical divider (per-line CSS grid).
- Added friendly UTC timestamp formatting (`formatLogTimestamp`): `2026-05-29T12:53:53.162712958Z` → `2026-05-29 12:53:53 UTC` (still UTC; UTC→local conversion deferred to STAGE-004-009, which now carries explicit notes to convert BOTH row and header timestamps).
- Severity text-tint (warn→yellow, error/critical/alert/emergency→red) reading the converged `LogLine.severity`. (Docker logs default to info severity until STAGE-004-004A adds severity-level extraction — new stage created this session.)
- Title-cased the Wrap / Last: / Since: labels.

Backend test infrastructure (commit 2):
- Boot-once test harness: session-scoped lightweight test lifespan (`apps/monitor/tests/_test_lifespan.py`) + per-function DB isolation + per-function mutable-singleton reset, replacing per-test full-lifespan boot. Backend suite ~597s → ~360-380s (verified isolation-safe across multiple clean runs). Eliminates the per-test full-lifespan boot tax (real-Docker-socket image_update_checker blocking run under xdist). Real-lifespan e2e/lifespan tests intentionally retained on the real lifespan.
- Fixed a pre-existing test-isolation gap in `test_api_cron_runs.py` (added `_mock_vm_lifespan_tick` autouse guard; tightened two VL-host request-count assertions).
- Added 3 targeted unit tests restoring 100% kernel coverage on `compose_action_runner.py` + `image_update_collector.py` branches that lost incidental coverage when the harness stopped booting the full lifespan.

New stages created this session: STAGE-004-004A (docker log severity-level extraction). STAGE-004-009 annotated with dual-surface (row + header) timezone-conversion notes. EPIC-004 now 45 stages.

make verify: GREEN (3264 backend passed, 100% kernel coverage, frontend 0 eslint errors, build green).

### Commits

- `Commit: 2f56477` — feat(ui,logs): STAGE-004-003 Shared LogViewer component + ANSI rendering + timestamp formatting (Frontend commit 1)
- `Commit: f68b76e` — test(backend): STAGE-004-003 boot-once test harness + pre-existing isolation fixes + coverage restoration (Backend test infrastructure commit 2)
