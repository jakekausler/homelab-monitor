## [STAGE-001-013] Alert ingestor + first `inproc-dashboard` channel

New `kernel/alerts/` package with alert repository and fingerprinting logic. Module `types.py` defines `Severity` and `AlertStatus` StrEnums, `AlertOutcome` StrEnum (acknowledged/dismissed/resolved/escalated), `Alert` and `AlertmanagerV2Payload` Pydantic models. Module `fingerprinting.py` implements `compute_fingerprint(labels_dict) -> str` (deterministic JSON hash) and `quarantine_fingerprint(reason) -> dict` for structured audit. Module `events.py` defines `AlertFiringEvent` and `AlertResolvedEvent` as Pydantic BaseModel subclasses extending new `BaseEvent` base class (refactored from Protocol to BaseModel). Module `repository.py` implements `AlertRepository` with full CRUD: `insert(alert)`, `update_last_seen(alert_id)`, `mark_resolved(alert_id, resolved_at, decided_by)`, `set_ack(alert_id, ack_at, ack_by)`, `find_by_id(alert_id)`, `find_active_by_fingerprint(fingerprint)`, `find_active_quarantine_alert(fingerprint)`, `list_paginated(status/severity/source_tool/fingerprint, cursor, limit)`, `find_outcome_history(alert_id)`. Race-safe dedup via `ux_alerts_fingerprint_firing` UNIQUE PARTIAL INDEX on `(fingerprint) WHERE status='firing'` + IntegrityError handling on insert.

New `kernel/dispatch/` package with pluggable alert channels. Module `types.py` defines `Channel` Protocol (async `deliver(alert, severity) -> DeliveryResult`) and `DeliveryResult` dataclass. Module `dispatcher.py` implements `AlertDispatcher` with per-channel exception isolation, sequential delivery, and per-channel failure counter for observability. Module `channels/inproc_dashboard.py` implements `InprocDashboardChannel` publishing `AlertFiringEvent` and `AlertResolvedEvent` to `SseBroker`.

`BaseEvent` refactored from Protocol to Pydantic `BaseModel` with `kind: Literal[...]` and `ts: datetime` fields. `EventSink.publish()` now accepts any `BaseEvent` subclass. `SseBroker.publish()` uses event polymorphism with documented lock-contention caveat. `SchedulerTickEvent`, `AlertFiringEvent`, and `AlertResolvedEvent` all inherit `BaseEvent`; Pydantic v2 Literal kind overrides require `# pyright: ignore[reportIncompatibleVariableOverride]` per-line idiom (Protocol-style stub `model_dump(self) -> dict: ...` was silently overriding Pydantic's real implementation, returning None — caught by test failures).

New POST `/api/alerts/ingest` (Alertmanager v2 webhook receiver). Auth via `require_user_or_token({Scope.ALERTS_INGEST_WRITE})` — cookie path enforces CSRF, token path validates scope. Per-alert: compute fingerprint, `find_active_by_fingerprint(status='firing')` lookup; firing alerts insert OR update_last_seen; resolved alerts call `mark_resolved()` (idempotent); resolved-without-prior silently ignored. Returns 202 with `{received, ingested}` counts. Privacy: INFO logs include fingerprint + alert_id only, never full payload.

Four new API endpoints: `GET /api/alerts` (paginated, filterable by status/severity/source_tool/fingerprint), `GET /api/alerts/{id}` (detail view + outcome history), `POST /api/alerts/{id}/ack` (session-only, CSRF enforced), `POST /api/alerts/{id}/dismiss` (session-only, CSRF enforced). Malformed cursor raises `ValueError("invalid cursor format")` → mapped to 400 HTTPException.

Migration `0005_add_alerts_tables.py` adds 13 columns to `alerts` (source_tool, severity, status, opened_at, last_seen_at, resolved_at, ack_at, ack_by, runbook_id, payload_json, fingerprint) and 3 to `alert_outcomes` (outcome, decided_at, decided_by). Three indexes on `(source_tool, opened_at)`, `(status, opened_at)`, and `(alert_outcomes.alert_id)`. UNIQUE PARTIAL INDEX `ux_alerts_fingerprint_firing` on `(fingerprint) WHERE status='firing'` for race-safe dedup.

`FailureBudget` and `Scheduler` constructors widened with kw-only `alert_repo` and `dispatcher` parameters (default None for backward compat). Quarantine integration: insert/update active alert row + dispatch `AlertFiringEvent`. `Scheduler.clear_quarantine()` reordered: resolve alert FIRST (via fingerprint lookup using `_extract_last_reason()` helper) THEN clear budget. Ensures resolved-without-cleared-budget remains operator-recoverable since `mark_resolved()` is idempotent.

Lifespan constructs `AlertRepository`, `InprocDashboardChannel`, `AlertDispatcher`; threads through `Scheduler` + `FailureBudget`; sets `app.state.alert_repo` and `app.state.alert_dispatcher` for router access.

New test files: `test_alerts_repository.py` (full CRUD + filters + idempotency + race dedup), `test_dispatcher.py` (channel isolation + failure counter), `test_event_protocol.py` (BaseEvent conformance + SSE publish), `test_fingerprinting.py` (determinism + structured quarantine), `test_migration_0005.py` (additive migration + index usage verification), `test_api_alerts_ingest.py` (happy path + dedup + adversarial auth + privacy + token scope variants), `test_api_alerts_listing.py` (filters + pagination + 400 on malformed cursor), `test_api_alerts_outcomes.py` (ack + dismiss + CSRF + 404), `test_scheduler_quarantine_alert.py` (quarantine flow + clear-on-resolve + refire), `test_lifespan_alert_wiring.py` (app.state setup), `test_scheduler_quarantine.py` (extended with `_extract_last_reason` branches + defensive clear_quarantine paths).

Verification gate at exit: ruff PASS, pyright (strict) PASS, pytest 867 passed / 2 xfailed (SSE HTTP-level delivery deferred to STAGE-001-014) / 0 failed, 100% backend coverage maintained. UI verify clean.

Two-pass code review (code-reviewer Opus): first pass found 22 findings (3 Critical, 7 Important, 12 Minor) — split across Parts A/B/C for fixer-Haiku tool budget constraints. Second pass found 1 Critical (cursor ValueError → 500) + 1 Important (rfind doc) + 5 Minor — all addressed. Final verification: ALL GREEN.

Build split into Spec A (foundation, ~17 files) + Spec B (API + scheduler, ~15 files) per Lesson #65 (tool budget). Spec B scribe cut off at step 7 of 8; required 2 continuation dispatches. Refinement was e2e-tester only (24 checks across 4 scenarios, all PASS). Tester subagent hallucinated a fictional test (`test_session_extends_if_within_extension_window`); lesson extended from verifier to tester subagents (Lesson #50 don't trust mis-categorization).

Commits: 2328034 (feat: alert ingestor + dispatcher + scheduler quarantine wiring) + 065f07a (refactor: apply code review findings)

## [STAGE-001-014] UI shell + login + Overview live-tile + SSE-capable middleware

Backend ASGI middleware migration. RequestIdMiddleware, AccessLogMiddleware, AuthMiddleware rewritten from Starlette `BaseHTTPMiddleware` subclasses to pure ASGI callables (`__init__(app)` + `async __call__(scope, receive, send)`). BaseHTTPMiddleware buffered streaming responses, blocking SSE; pure ASGI lets bytes flow through unbuffered. Empty `Authorization: Bearer ` (with trailing space, empty token) now falls through to cookie auth instead of being treated as a malformed token (fixes silent auth-loss scenario).

In-process uvicorn pytest fixture. New `apps/monitor/tests/_uvicorn_fixture.py` (`UvicornTestServer` class, `uvicorn_server` async fixture). Required because `httpx.ASGITransport` ALSO buffers streaming responses (it accumulates body chunks and only returns control after the ASGI app completes — verified via httpx/_transports/asgi.py source). The 2 previously-xfailed SSE tests (`test_sse_http_endpoint_smoke`, `test_lifespan_e2e_sse_receives_tick`) rewritten to hit a real loopback socket and now pass naturally. Background-thread uvicorn with private temp DB; signal handlers skipped (non-main thread); WAL grace period before unlink.

SSE keepalive moved INSIDE the broker's `subscribe()`. New `SseKeepalive` sentinel class. `SseBroker.subscribe(keepalive_interval=...)` yields the sentinel on internal `wait_for(q.get(), timeout)` timeout, keeping the generator's `finally` block from unwinding (which would have discarded the subscriber from `_subscribers`). Critical fix: the prior approach (wrapping `__anext__()` in `wait_for` from the outer `gen()`) silently terminated the SSE subscription after each 15s keepalive in production. Caught by code review's second pass.

Concurrent sessions allowed at login. `auth.py::login` no longer calls `delete_all_user_sessions(user.id)` before minting a new session; multiple devices can stay logged in concurrently. `change-password` STILL revokes all sessions (post-incident posture preserved). Renamed `test_login_invalidates_prior_sessions` → `test_login_preserves_prior_sessions`; assertion inverted; CSRF preservation across the OLD session is verified (state-changing request with old session+csrf cookies returns non-401/403).

Pre-existing 1/16 probabilistic flake fixed in `test_verify_session_cookie_value_tampered_session_id` and `test_verify_session_cookie_value_tampered_hmac`. The "tamper" was overwriting the first hex char with `'a'`; when the original hex started with `'a'` (P=1/16), the tamper was a no-op. Fix: `('b' if parts[N][0] == 'a' else 'a') + parts[N][1:]` — guaranteed-different.

`HOMELAB_MONITOR_HTTPS_ONLY_COOKIES=false` now logs a startup warning. `_session_ttl_seconds()` env-var read uncached (lru_cache attempt reverted because it broke per-test env-var overrides). `AccessLogMiddleware` uses module-level `_access_log` logger to avoid per-request `structlog.get_logger()` overhead.

Frontend foundation: openapi-typescript + openapi-fetch typed client at `apps/ui/src/api/client.ts`; CSRF middleware reads `homelab_monitor_csrf` cookie and sets `X-CSRF-Token` on POST/PUT/PATCH/DELETE; `unwrap()` helper converts non-2xx envelopes into `ApiError` instances (with `status`, `code`, `message`, `details`, `retryAfterSeconds`). 401 from `/api/auth/me` returns null (auth-guard signal). TanStack Query hooks: `useCurrentUser`, `useVersion`, `useMetricsSnapshot`, `useLogin`, `useLogout`, `useCollectors`, `useAlerts`. `useMetricsSnapshot` uses `staleTime: Infinity` + `refetchOnWindowFocus: false` (live updates driven by SSE refetch).

Frontend routes: code-based TanStack Router with `/login` (public), `/overview` (protected), `/` redirects to `/overview`. Auth guard `ensureAuthenticated(queryClient)` reads cache first, then `queryClient.fetchQuery` — throws `redirect({to: '/login'})` (Response object) on null user.

Frontend AppShell: collapsible desktop sidebar (`hidden md:flex`) + full-screen mobile overlay sheet (`<aside role="dialog">` with backdrop, body scroll-lock, Esc-key close, initial focus on open). Two hamburger buttons via Tailwind responsive prefixes (`md:hidden` + `hidden md:inline-flex`). 13-item sidebar (Overview enabled; others disabled with "Coming soon" tooltip). User menu (theme toggle persisted to `localStorage['homelab-monitor:theme']`, sign out). Notifications button disabled with "Coming soon" tooltip. Search placeholder disabled. AppShell mounts BOTH desktop and mobile sidebar instances with distinct `aria-label`s ("Primary" vs "Mobile primary").

Login screen: centered card layout, React Hook Form + Zod validation, password reveal eye toggle (Eye/EyeOff lucide icons), 401/429/5xx error states with retry-after countdown, "Welcome — please run hm user create" variant when `/api/version` returns `users_configured: false`. `void onSubmit(e)` wrapped with `.catch(...)` for defense against unexpected synchronous-throw cases.

Overview screen: `HostCpuTile` displays the host collector's CPU% (label `cpu="all"`). Big-number + raw-SVG sparkline below. Sparkline pre-fills 60-point baseline (`Array.fill(initialValue)`) on mount so the line renders a flat baseline immediately rather than bumping/flatlining between sparse SSE ticks. Refetch on SSE tick uses latest-ref pattern (`refetchRef.current = snapshot.refetch`) to avoid the `[snapshot]` dep array creating a refetch loop. "Updated" timestamp formatted as `HH:MM:SS` in user's local TZ; date prefix `YYYY-MM-DD ` only when not today.

`useSSE<T>` hook with latest-ref pattern for unstable callbacks (`parserRef`, `factoryRef`); decoupled reconnect-effect keyed on `[status, failureCount, open]`; manual reconnect short-circuits one auto-backoff then clears on next open; stale-source guards on all 3 EventSource listeners (`if (sourceRef.current !== src) return`) prevent late events from a closed source mutating refs.

shadcn-style components copied into `src/components/ui/`: button, input, label, card, dropdown-menu, tooltip. Theme tokens extended in `src/index.css` (status/severity colors). FOUC mitigation: inline `<script>` in `index.html` reads `localStorage` and sets `data-theme` BEFORE React mounts; replaces dead `class="dark"` attribute.

`@hookform/resolvers` upgraded from `^3.10` to `^5.0.1` (now `5.2.2`) for Zod v4 compatibility. v3 was built for Zod v3 and silently failed to map the v4 ZodError shape, causing unhandled rejections in form validation tests.

Tooling: `scripts/generate-ui-types.sh` (regenerates `apps/ui/src/api/schema.ts` from `packages/shared-types/openapi.json`); pre-commit hook auto-regenerates on openapi.json change. `vite.config.ts` is now a function (uses `loadEnv`) reading `VITE_DEV_PORT`, `VITE_DEV_HOST` (default `0.0.0.0`), `VITE_API_PROXY_TARGET` (default `http://localhost:9090`); `vitest.config.ts` resolves the function for test mode.

Frontend coverage thresholds set to 80/75/80/80 (lines/branches/functions/statements) — kernel backend stays at 100%. UI build: 546KB JS / 171KB gzipped.

77 vitest tests across 12 files (api/csrf, api/client, api/queries, components/{AppShell,SidebarNav,TopBar,UserMenu,tiles/HostCpuTile,tiles/Sparkline}, lib/sse with 10 deterministic SSE tests including reconnect/backoff/manual-reconnect/cleanup/default-factory, routes/Login covering welcome+validation+all error states, routes/Overview smoke, router auth-guard tests using `isRedirect` from `@tanstack/react-router`).

Backend test count net change: +5 tests (now 870 total). New tests: SSE keepalive sentinel + frame survival, defensive empty-bearer `_resolve_token` regression pin (uses `AsyncMock(spec=AuthRepository)` to assert no DB call), CSRF preservation in concurrent-sessions test, lifespan HTTPS_ONLY warning assertion via caplog, plus the keepalive integration test publishing a real event after the keepalive yield.

Documentation: `docs/ui/README.md` covering frontend conventions, layout, auth/CSRF flow, theme system, testing patterns, and how to add a new screen.

Forward-planning: STAGE-001-015 deliverables list updated to include explicit "Frontend backfill" item — `useMetricsRange` hook + HostCpuTile.tsx update to seed the sparkline from `GET /api/metrics/range` on mount, with synthetic-baseline fallback. Identified during STAGE-001-014 Refinement when user noted the sparkline loses history on reload.

Two-pass code review (code-reviewer Opus): first pass found 7 Critical + 15 Important + 12 Minor across security, correctness, UX, observability, tests, and tooling — all 34 findings addressed in 5 fixer parts (A=critical security, B=critical UX, C=coverage+tests, D=frontend polish, E=tooling+minor). Second pass found 0 Critical + 4 Important + 6 Minor; the most consequential second-pass finding was the SSE keepalive subscriber-loss bug (the 1st-pass-approved fix used `wait_for(__anext__())` which cancels the iterator on timeout, terminating the subscription after every 15s keepalive). Architectural fix: keepalive owned by the broker's `subscribe()` method via `SseKeepalive` sentinel.

Build split into Spec A (backend ASGI) + Spec A.1 (uvicorn fixture) + Spec B (frontend, ~25 files). Refinement caught 8 user-facing issues including the sparkline rhythm, snapshot request storm, mobile sidebar overlay, concurrent sessions, password reveal, "Updated" formatting, notification button, and historical-backfill scope. Final verification gate: `make verify` exits 0 deterministically across 3 consecutive runs; backend 870 tests at 100% kernel coverage; frontend 77 tests; UI build successful.

Commits: 078010f (feat: UI shell + login + Overview live-tile + SSE-capable middleware), 0acd116 (chore: mark Build complete), e87fcf4 (refactor: apply Refinement feedback — 8 fixes), eaabf8c (chore(STAGE-001-015): scope frontend backfill into deliverables), a20d837 (chore: mark Refinement complete), 610f7f6 (refactor: apply code review fixes — 34 findings + 1 critical 2nd-pass)
