2026-06-24 8f16e2b EPIC-008/STAGE-008-013: Security Advisor + active-connections collector

Added `SynologySecurityCollector` (`plugins/collectors/integrations/synology/security.py`), a co-equal
multi-fetch collector (mirrors the Updates collector) for Security Advisor scan status + active-connection
count (1h cadence, `synology` concurrency group). Two co-equal fetches: `security_scan_status()`
(`SYNO.Core.SecurityScan.Status` v1 `system_get`) + `current_connection_list()`
(`SYNO.Core.CurrentConnection` v1 `list`). Emits `homelab_synology_security_status` (ordinal
safe=0/warning=1/risk=2/danger=3, unknown→3 fail-loud, seeded conservative 2.0 baseline) +
`homelab_synology_security_safe` (1 iff "safe") — both always-emitted alertable; a 25-cell
`homelab_synology_security_findings{category,severity}` grid (5 categories × 5 severities, all emitted, 0
included) + `homelab_synology_security_findings_total{severity}` rollup;
`homelab_synology_security_last_scan_age_seconds` + `_timestamp`; and `homelab_synology_active_connections`
(emit-on-success only — 0-connections is a real state distinct from a failed fetch). Follows the Wave-B
alerting philosophy so STAGE-008-022's `SecurityAdvisorNotSafe` rule (`security_safe==0`) can fire.

Validated live (security posture is genuinely "risk"): `ok=True`, 44 metrics, `security_status=2.0`,
`security_safe=0.0`, the 25-cell grid with only `{userInfo,risk}=1` non-zero, `active_connections=5`. 100%
branch coverage. Per Amendment 5, Security Advisor STATUS is API-sourced (a periodic scan result, not a log
event); login EVENTS are syslog→VL in STAGE-008-023. The per-connection "who is connected" detail is a
STAGE-008-027 panel view-time re-query, not a metric series. The `security_scan_in_progress`/`sysProgress`
signal was scoped out (YAGNI — no alert consumer; staleness is covered by `last_scan_age`). Two key design
choices documented in the module docstring + regression.md: the `active_connections` emit-on-success
(deliberate seed-0 break) and the `SecurityScan.Status` `items`-is-a-category-keyed-DICT parse invariant.
