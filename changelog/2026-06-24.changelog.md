2026-06-24 9c9c510 EPIC-006/STAGE-006-018: Pi-hole write endpoints (blocking / gravity) + query-logging metric

Added the first Pi-hole WRITE actions — `POST /api/integrations/pihole/blocking` (enable/disable with optional timer) and `POST /api/integrations/pihole/gravity/update` — in a new `kernel/api/routers/integrations_pihole.py`, mirroring the docker `pull-and-restart` precedent: `Scope.PIHOLE_WRITE`-gated (`require_user_or_token`), a `confirm_phrase` validated by a body-only FastAPI dependency that runs BEFORE the RW-client dependency (so a bad confirm returns 400, not 503), and a transactional `audit_log` row (who/what/before/after/ip) with an explicit `uuid7()` audit_id echoed in the response. A second long-lived RW `PiholeRestClient` (using the `pihole_api_password_rw` secret) is constructed in lifespan and injected only into the write router — never into the RO collector path. Added `set_blocking()` + `gravity_update()` (drains the streaming `text/plain` `pihole -g` output, ~120s timeout, positive `[✓] Done` success-marker heuristic) to the concrete client, and `config()` (GET `/api/config`) to both the client and the `PiholeClient` read Protocol. New `pihole_config` collector emits `homelab_pihole_query_logging_enabled` (bare fail-closed 1/0 from `config.dns.queryLogging`) — the metric deferred from STAGE-006-009. Hardened `PiholeRestClient._get()` (the second STAGE-006-009 deferral): a 200-response carrying an `{"error":{"key":"unauthorized"}}` body now shares the same single re-auth+retry guard as a real HTTP 401 (via a `_classify_response` helper), other error keys → `bad_response`; stays within the existing `PiholeErrorReason` Literal. Added `Scope.PIHOLE_WRITE`; regenerated `packages/shared-types/openapi.json`. Refinement 3b validated all paths against the live Pi-hole (blocking round-trip restored to original state, gravity ran with real output, confirm/scope/CSRF/audit all enforced) — leaving production state unchanged.

---
