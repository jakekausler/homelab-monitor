# 2026-05-12

## STAGE-002-002 — Cron registry CRUD API + Inventory "Crons" tab UI

### Added

- 7 new REST endpoints (GET /api/crons list with filters, GET /api/crons/{id}, POST /api/crons, PATCH /api/crons/{id}, DELETE /api/crons/{id}, GET /api/crons/{id}/preview-runs, POST /api/crons/preview-runs)
- `CronOut`, `CronCreate`, `CronUpdate`, `CronListResponse` Pydantic schemas (`apps/monitor/homelab_monitor/kernel/cron/schemas.py`)
- `CronRepo` for SQLite-Core access to `crons` (`apps/monitor/homelab_monitor/kernel/cron/repository.py`)
- `parse_schedule`, `next_run_after`, `prev_run_before` croniter wrappers (`apps/monitor/homelab_monitor/kernel/cron/schedule.py`); `croniter` dependency added to `pyproject.toml`
- Alembic migration 0007: adds `schedule_canonical TEXT` column to `crons`, adds partial index `idx_crons_active ON crons(name) WHERE archived_at IS NULL`, adds CHECK constraint enforcing "at-least-one" between `schedule` and `cadence_seconds`
- Inventory route shell + Crons tab UI: `routes/inventory/{Inventory,CronsList,CronDetailPage}.tsx`
- Crons UI components: `components/crons/{CronsTable,CronsToolbar,CronDetail,CronForm,AddCronModal,ConfirmDeleteModal,SchedulePreview,badges}.tsx`
- New Radix-based UI primitives: `components/ui/{dialog,badge,select}.tsx`
- `apps/ui/src/lib/text.ts` (Refinement code-review fix): shared `capitalize()` helper extracted from `badges.tsx`
- `apps/ui/src/lib/relativeTime.ts` for time formatting
- `Schema<K>` helper in `apps/ui/src/api/types.ts` (strips `T | undefined` from indexed-access type aliases under `noUncheckedIndexedAccess`)
- 11 new test files (backend: `test_api_crons`, `test_cron_repo`, `test_cron_schedule`; UI: 8 component + route tests)
- 14 regression items added to EPIC-002 regression.md
- `docs/ui/inventory-crons.md` operator guide

### Changed

- Cross-stage edit to STAGE-002-001 receiver: `kernel/heartbeat/repository.py::_SELECT_CRON_SQL` now filters `AND archived_at IS NULL` — heartbeat receiver returns 404 on archived crons (D2a decision).
- `query_model()` factory refactored to dynamically construct an `inspect.Signature` so OpenAPI introspection emits typed query parameters for all routes using it. STAGE-002-001's heartbeat routes benefit transparently.
- `kernel/api/{app.py, dependencies.py, lifespan.py}`: wired CronRepo + new router.
- `apps/ui/src/router.tsx`: 3 new routes (Inventory shell + Crons list + Cron detail).
- `apps/ui/src/components/SidebarNav.tsx`: Inventory sidebar link enabled.
- Generated artifacts updated: `packages/shared-types/openapi.json`, `apps/ui/src/api/schema.ts` (gitignored, regenerated by pre-commit hook), `pnpm-lock.yaml`, `uv.lock`.

### Decisions locked (recorded in stage tracking)

- D1 Hybrid schedule storage: raw `schedule TEXT` + denormalized `schedule_canonical TEXT` (deviates from brainstormer's "string-only" recommendation; preserves round-trip fidelity while enabling future query-ability).
- D2 Soft-delete via existing `archived_at` column (no new `deleted_at`). List/get filters `WHERE archived_at IS NULL` by default; `?include_archived=1` opts in. Restore via `PATCH {archived_at: null}` (no separate /restore endpoint).
- D2a Receiver 404s on archived crons via `_SELECT_CRON_SQL` filter + new regression test.
- D3 Changed-fields-only audit on PATCH: `before`/`after` JSONs contain only diffs. Empty-diff PATCH returns 200 with NO audit row. Audit verbs: `crons.create`, `crons.update`, `crons.delete`, `crons.restore`.
- D3a Schedule preview endpoint: both saved (`GET /api/crons/{id}/preview-runs`) and unsaved (`POST /api/crons/preview-runs`) variants; single croniter source.
- D4 Separate route `/inventory/crons/{id}` for detail (bookmarkable + deep-linkable); filter state preserved via TanStack Router `search` params.
- D4a Shared `<CronForm>` component with `mode=create|edit|promote` props (reused by AddCronModal, CronDetail, future STAGE-002-007 promote-suggestion flow).
- Pagination: offset/limit, default 100, max 500; `CronListQuery(extra='forbid')` rejects rogue keys.
- Xor loosened mid-Build: "at-least-one" between schedule and cadence_seconds (`schedule != '' OR cadence_seconds > 0`). Backend mirrors cadence from schedule via croniter average-interval helper, so the form ships values that satisfy both invariants.

### Deferred

- Cron auto-discovery → STAGE-002-007 (REWORKED post-redesign-brainstorm)
- B-mode log-scrape → STAGE-002-008
- Wrapper-helper install flow + "Install heartbeat" UI button → STAGE-002-009
- vmalert rules → STAGE-002-010
- Bulk operations (multi-select) on crons — not in EPIC-002 scope
- Cron run history view — STAGE-002-008 territory (depends on log-scrape)
- `cronsListSearch()` helper extraction (6 sites duplicate the same search-param object literal) — pre-existing from Build phase, deferred to STAGE-002-003
- Refresh `lastScheduleRef`/`lastCadenceRef` on same-id `defaultValues` change — only relevant when save flow stops navigating away (STAGE-002-006 territory)
- Controller-based radio group in CronForm (style refactor; current implementation works)
- AddCronModal desktop visual verification — defer to a polish stage
- Additional badges.test.tsx coverage for text/aria-label divergence

### Refinement summary

Six UI production files modified during Refinement to address user-tested behaviors:

- **Mode-swap correctness** (`CronForm.tsx`): React reconciliation reusing DOM nodes across mutually-exclusive ternary branches caused `type="number"` from cadence input to persist on schedule input, coercing cron expressions to "0". Fix: explicit `key="schedule-input"` and `key="cadence-input"` force remount. Also: Zod superRefine silent-block fix (`cadence_seconds=0` forced when scheduleMode='schedule'); RHF mode-swap handlers now `setValue('scheduleMode',...)` with `{shouldDirty,shouldTouch,shouldValidate}`; `lastScheduleRef`/`lastCadenceRef` (and post-review fixes seeding them from raw `defaultValues` to capture backend-mirrored cadence/schedule) preserve mode-swap defaults; `titleCase` used in mode dropdown options.
- **Save flow** (`CronDetail.tsx`): `navigate({to:'/inventory/crons'})` after successful PATCH so the user lands back on the list with their changes visible. Post-code-review: post-archive nav now sets `include_archived: true` so the just-archived row stays visible with its `archived` badge instead of vanishing.
- **Title-Case display** (`badges.tsx`, `CronsToolbar.tsx`, `CronForm.tsx`): badge text + dropdown labels capitalize the first character via a new shared `capitalize` helper at `apps/ui/src/lib/text.ts` (re-exported from `badges.tsx` as `titleCase` for backward compatibility). `aria-label` retains the lowercase enum value for screen readers and tests.
- **Archive terminology**: "Soft-delete" button + "Soft-delete cron?" heading renamed to "Archive" / "Archive cron?" / "Archiving...". Server-side action unchanged (`archived_at` column). Placeholder name — STAGE-002-006 renames to "Hide" with the derived-state model.
- **Mobile dialog overflow** (`components/ui/dialog.tsx`): `DialogContent` gets `max-h-[calc(100vh-2rem)] overflow-y-auto`.

Thirteen test-label updates across 4 test files kept verify GREEN.

### Code review

Two-pass code-reviewer (Opus). First pass: 6 Important + 9 Minor findings. Implemented: 4 fixes (lib/text.ts extraction, ref seeding from raw defaultValues, post-archive include_archived=true, explanatory comments on key props). Deferred with justification: 10 findings (style refactors, pre-existing duplications, polish coverage). Second pass: APPROVED with no new findings.

### Verify

`make verify` GREEN: 1287 backend pytest pass + 100.00% kernel coverage; 188 UI tests pass; ruff + ruff format + pyright (strict) + tsc + eslint + prettier all clean. UI lint emits 36 pre-existing warnings (same baseline as EPIC-001).

### Implementation commit

- 2b729a0 (implementation + code-review fixes + docs bundled)

### References

- Design spec §3.1 (UI screens — Inventory tab), §6.1 (`crons` table contract), §10.4 (audit log on edits), §10.5 (CSRF on mutating endpoints), §16 (timezone handling)
- Forward-looking redesign: `docs/superpowers/specs/2026-05-11-cron-derived-state-redesign.md` (drives STAGE-002-003 through STAGE-002-010)
- STAGE-002-002.md design + build + refinement summaries

---

## STAGE-002-003 — Cron schema redesign — fingerprint identity, drop integration_mode, rename archived_at→hidden_at

### Added

- New module `apps/monitor/homelab_monitor/kernel/cron/fingerprint.py` — `compute_fingerprint()` (SHA256 hex over JSON-canonical dict, `sort_keys=True`, `separators=(",",":")`, `ensure_ascii=False`) + `derive_name()` (basename of first token with `"cron"` fallback)
- New migration `apps/monitor/alembic/versions/0008_cron_fingerprint_redesign.py` — destructive both ways; new schema has `fingerprint PK`, `hidden_at`, `source_path`, `wrapper_installed_at`; drops `id`, `integration_mode`, `archived_at`; demo seeds gated behind `HOMELAB_MONITOR_INCLUDE_DEMO_SEEDS=1`; migration-local fingerprint helper + `_assert_seed_fingerprints_match()` guard
- New test file `apps/monitor/tests/test_fingerprint_computation.py` — verifies determinism, field-sensitivity, NULL ≠ `""` source_path, 64-char hex, Unicode safety
- New docs `docs/architecture/cron-identity.md` (202 lines) — operator guide to fingerprint identity, F19 precedent, sources of cron rows, lifecycle, audit verb taxonomy
- New API behavior: `PATCH /api/crons/{fp}` rejects non-whitelisted fields with 422 (`extra='forbid'` on `CronUpdate`)

### Changed

- `apps/monitor/homelab_monitor/kernel/cron/repository.py` — `CronRecord` renamed `id`→`fingerprint`; dropped `integration_mode`; renamed `archived_at`→`hidden_at`; added `source_path` + `wrapper_installed_at`; audit verb taxonomy now uses `crons.hide`/`crons.unhide` (not `crons.delete`/`crons.restore`)
- `apps/monitor/homelab_monitor/kernel/cron/schemas.py` — deleted `IntegrationMode` literal; trimmed `CronUpdate` to 4 fields (`name`, `expected_grace_seconds`, `enabled`, `hidden_at`); `CronCreate.source_path` requires `min_length=1`; `CronListQuery` drops `integration_mode`; renames `include_archived`→`include_hidden`
- `apps/monitor/homelab_monitor/kernel/heartbeat/repository.py` — `_SELECT_CRON_SQL` filters on `fingerprint = :fp AND hidden_at IS NULL`; method params renamed `cron_id`→`fingerprint`
- `apps/monitor/homelab_monitor/kernel/api/routers/heartbeat.py` and `apps/monitor/homelab_monitor/kernel/api/routers/crons.py` — all path params `{cron_id}`→`{fingerprint}`; deleted `_maybe_log_observe_warning` (integration_mode gone); duplicate fingerprint → 409
- UI (11 files modified for D7 minimal field-rename patches): `cron.id`→`cron.fingerprint`, `cron.archived_at`→`cron.hidden_at`, removed all `integration_mode` references; `ModeBadge` component DELETED including its tests; `CronForm.submit` branches on `mode`: `create` sends full payload, `edit` sends `CronUpdate` whitelist only (fixes 422 on save flow caught in code review)
- Generated artifacts: `packages/shared-types/openapi.json` regenerated; `apps/ui/src/api/schema.ts` (gitignored) regenerated by pre-commit hook
- 2 pre-existing alertmanager teardown errors fixed in `test_lifespan_alertmanager.py` via `@pytest.mark.httpx_mock(assert_all_responses_were_requested=False)` markers

### Decisions locked (recorded in stage tracking)

- D1 Full 64-char SHA256 hex fingerprint (matches `alerts/fingerprinting.py` precedent — same project, same hash function, same hex format)
- D2 NULL `source_path` for remote crons (matches kernel "absent" convention; `min_length=1` on `CronCreate` prevents empty-string confusion)
- D3 `derive_name = basename(command.split()[0])` with `"cron"` fallback
- D4 JSON-canonical dict with `sort_keys=True` + `separators=(",",":")` + `ensure_ascii=False` (F19 precedent; NULL `source_path` serializes as JSON `null`, distinct from empty string)
- D5 Migration seeds bypass `audit_log` entirely (migrations are infrastructure, not user actions)
- D6 Both upgrade and downgrade are destructive (symmetric with project precedent; prod rollback uses backup, not `alembic downgrade`)
- D7 Minimal UI field-rename patches in this stage (no UX redesign; STAGE-002-006 does the full UX pass)

### Deferred

- `cronsListSearch()` helper extraction — pre-existing from STAGE-002-002, defer to STAGE-002-003+ polish stage
- `derive_name` interpreter-aware fallback — defer; field is user-editable
- `soft_delete_cron` race condition — defer (single-operator app)
- Preview-runs missing-expr 404→422 — defer (contract intentional)
- `crons.archive`-style UI lexicon — defer to STAGE-002-006 ("Archive"→"Hide" rename)
- `@deprecated` marker on `create_cron` — defer to STAGE-002-004 removal

### Refinement summary

e2e-tester ran 6 backend scenarios on real DB (backend-only stage; UI changes are mechanical field renames with no UX delta):

1. **Fingerprint round-trip** — 64-char hex; same tuple → same fp; varying any of (host, source_path, schedule, command) → distinct fp; NULL ≠ `""` source_path
2. **Migration 0008 fresh DB** — schema correct; 4 seed rows present with env var set; 1 row has `source_path = NULL` (remote-host); `audit_log` has 0 `crons.discover` rows (D5)
3. **Heartbeat receiver on fingerprint URL** — 204 on success; `heartbeats_state` upsert + `crons.last_seen_state` mirror + `audit_log` row all correct
4. **Receiver 404 on unknown fingerprint** — all-zeros fingerprint returns HTTP 404 with `not_found` error code
5. **Hide → 404, unhide → 204** — D2a regression: `_SELECT_CRON_SQL` filters `AND hidden_at IS NULL`; unhiding restores 204
6. **API CRUD with fingerprint URLs + PATCH whitelist 422** — GET list/get use new field shape; PATCH accepts whitelisted fields; PATCH rejects non-whitelisted field (`host`) with HTTP 422 + `extra_forbidden`

All 6 PASS.

### Code review

Two-pass code-reviewer (Opus). First pass: 2 Critical + 5 Important + 5 Minor findings.

- Critical 1: `CronForm.submit` edit-mode was sending `CronCreate`-shaped payload → PATCH 422. **FIXED:** branch on `mode` prop (`create` sends full payload; `edit` sends `CronUpdate` whitelist only).
- Critical 2: `CronsList.test.tsx` mock had stale `id`/`integration_mode`/`archived_at` fields. **FIXED:** replaced mock with new schema shape + added `CronOut` type import.
- Important 1: `CronCreate.source_path` allowed `""`. **FIXED:** added `min_length=1`.
- 4 Important + 5 Minor findings deferred with justification.

Second pass: 2 cosmetic follow-ups (stale `id:` keys in 3 test spreads; missing test for `source_path=""` rejection) — **FIXED**. **APPROVED**.

### Verify

`make verify` GREEN: 1300 backend pytest pass + 100.00% kernel coverage + 185 UI tests pass + ruff + black + pyright (strict) + tsc + eslint + prettier + UI-build all clean. UI lint warnings: 36 (pre-existing baseline, non-blocking).

### Implementation commit

- 514b9af (implementation + code-review fixes + docs bundled)

### References

- Design spec §Identity, §Schema changes
- Forward-looking spec: `docs/superpowers/specs/2026-05-11-cron-derived-state-redesign.md`
- Cron identity guide (new): `docs/architecture/cron-identity.md`
- Stage tracking: `epics/EPIC-002-heartbeat-cron/STAGE-002-003.md`
- Migration: `apps/monitor/alembic/versions/0008_cron_fingerprint_redesign.py`
