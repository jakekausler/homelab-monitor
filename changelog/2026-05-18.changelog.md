# 2026-05-18

## STAGE-002-009 — Wrapper helpers: embed fingerprint at install time, call /register first, Install button local-host only

### Added

- **Heartbeat wrapper install feature** — bash wrapper template (`apps/monitor/homelab_monitor/data/cron-with-heartbeat.sh.tmpl`) wraps a host cron command and POSTs `/start` then `/ok`|`/fail` heartbeats to the monitor. The wrapper has the fingerprint, heartbeat URL base, and token file path baked in at install time.
- **Three install paths:**
  - UI "Install heartbeat wrapper" button on the cron detail page (`InstallHeartbeatModal.tsx` — dry-run preview → confirm flow)
  - CLI `hm cron install-wrapper`
  - Standalone remote-host CLI `install_wrapper_remote.py` for hosts not running the monitor stack
- **Host-side cron-apply executor architecture** — the monitor container has ZERO host-write capability. It writes only an IPC request file into `/var/lib/homelab-monitor/cron-apply/{requests,results}/`. A root systemd oneshot service (`scripts/hm-cron-apply.sh`, triggered by `homelab-monitor-cron-apply.path` watching the requests dir, with a `homelab-monitor-cron-apply.timer` 60s safety-net) performs all privileged host writes: the crontab rewrite, the wrapper script at `/usr/local/bin/cron-with-heartbeat.sh`, and the shared token at `/etc/homelab-monitor/heartbeat.token` (mode 0644, world-readable so the cron-runner user can read it). The executor only wraps an already-present crontab line, cross-checks the supplied wrapped line against its own re-derivation, writes to fixed paths only, and snapshots + rolls back on any failure.
- **Crontab-snapshot discovery fix** — fixed a latent STAGE-007 bug: the per-file user-crontab read ACL that discovery relied on broke vixie-cron (it rejects a crontab carrying an ACL with an "INSECURE MODE" error). Replaced with a root systemd timer + .path watcher (`hm-crontab-snapshot.service`/`.path`/`.timer`) that runs `crontab -l -u <user>` and writes a world-readable snapshot directory; the discoverer reads the snapshot instead of the 0600 spool. The old crontab-acl systemd units (`homelab-monitor-crontab-acl.path`/`.service`) and `scripts/refresh-crontab-acl.sh` were retired and deleted.
- **New API endpoint `GET /api/crons/wrapper-template`** (auth: `HEARTBEAT_WRITE` token) serves the raw wrapper template; the standalone remote CLI fetches the template from this endpoint rather than embedding its own copy.
- **New `is_local` computed field on `CronOut` schema** — gates the UI Install button: local-host crons enabled, remote crons disabled with an EPIC-017 tooltip.
- **New env var `HOMELAB_MONITOR_PUBLIC_URL`** (no default; wrapper install fails with HTTP 400 if unset) provides the heartbeat URL base baked into the wrapper.
- **Fingerprint convergence** — a wrapped crontab line is unwrapped before fingerprinting, so a cron keeps its original fingerprint across install (no spurious soft-delete).
- **Single system-managed `HEARTBEAT_WRITE` token** — shared across all wrapped crons (`ensure_heartbeat_wrapper_token`).
- **`host-setup.sh` updated** — installs the cron-apply + crontab-snapshot systemd units (including the new `cron-apply.timer`), idempotently retires the old crontab-acl units, strips stale ACLs.
- **Documentation** — new `docs/cron/install-heartbeat.md` and `docs/cron/remote-install.md` (install guides); updates to `docs/architecture/cron-identity.md` and `docs/deploy/host-prerequisites.md`.

### Changed

- `apps/monitor/homelab_monitor/cli/cron.py` — added `install-wrapper` subcommand.
- `apps/monitor/homelab_monitor/cli/install_wrapper_remote.py` (NEW) — standalone remote-host install CLI; fetches wrapper template from the public endpoint.
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — registers heartbeat wrapper token initialization.
- `apps/monitor/homelab_monitor/kernel/api/routers/crons.py` — added `GET /api/crons/wrapper-template` endpoint; added `is_local` computed field logic to `CronOut`.
- `apps/monitor/homelab_monitor/kernel/config.py` — added `HOMELAB_MONITOR_PUBLIC_URL` env var.
- `apps/monitor/homelab_monitor/kernel/cron/cron_apply_constants.py` (NEW) — shared IPC request/result paths and response models.
- `apps/monitor/homelab_monitor/kernel/cron/cron_apply_ipc.py` (NEW) — IPC request writer; generates request file with wrapped crontab line + cross-check digest.
- `apps/monitor/homelab_monitor/kernel/cron/heartbeat_wrapper_token.py` (NEW) — `ensure_heartbeat_wrapper_token()` initialization; stores token in database.
- `apps/monitor/homelab_monitor/kernel/cron/install.py` (NEW) — wrapper install orchestration; dry-run logic; result polling; error handling.
- `apps/monitor/homelab_monitor/kernel/cron/repository.py` — `get_for_install_wrapper()` query; `mark_wrapped()` method; unwrap-before-fingerprint logic.
- `apps/monitor/homelab_monitor/kernel/cron/schemas.py` — added `WrapperInstallRequest`, `WrapperInstallResult`, `is_local` field to `CronOut`.
- `apps/monitor/homelab_monitor/kernel/heartbeat/repository.py` — token storage and retrieval.
- `apps/monitor/homelab_monitor/data/cron-with-heartbeat.sh.tmpl` (NEW) — wrapper template with fingerprint, URL base, and token file path substitution placeholders.
- `apps/monitor/homelab_monitor/plugins/discoverers/cron_discoverer.py` — updated to read crontab snapshots instead of ACL-ed spool files; unwrap before fingerprinting.
- `apps/monitor/homelab_monitor/plugins/discoverers/cron_parser.py` — added unwrap logic; preserves original command in metadata.
- `apps/ui/src/api/crons.ts` — added wrapper install API calls; added `is_local` field handling.
- `apps/ui/src/components/crons/CronDetail.tsx` — added `InstallHeartbeatModal` component; conditionally renders Install button based on `is_local`.
- `apps/ui/src/routes/inventory/__tests__/CronsList.test.tsx` — updated tests for `is_local` filtering.
- `deploy/compose/.env.example` — added `HOMELAB_MONITOR_PUBLIC_URL` example.
- `deploy/compose/docker-compose.yml` — removed old crontab-acl systemd units; added cron-apply + crontab-snapshot units; added `/var/lib/homelab-monitor/cron-apply` volume.
- `deploy/dev/dev.env.example` — added `HOMELAB_MONITOR_PUBLIC_URL=http://127.0.0.1:19090` for dev.
- `deploy/systemd/homelab-monitor-cron-apply.path` (NEW) — .path unit watching `/var/lib/homelab-monitor/cron-apply/requests`.
- `deploy/systemd/homelab-monitor-cron-apply.service` (NEW) — oneshot service running `scripts/hm-cron-apply.sh`.
- `deploy/systemd/homelab-monitor-cron-apply.timer` (NEW) — 60s safety-net timer.
- `deploy/systemd/hm-crontab-snapshot.path` (NEW) — .path unit watching crontab files.
- `deploy/systemd/hm-crontab-snapshot.service` (NEW) — oneshot service running `crontab -l -u <user>` and writing snapshots.
- `deploy/systemd/hm-crontab-snapshot.timer` (NEW) — periodic snapshot timer.
- `scripts/hm-cron-apply.sh` (NEW) — host-side cron-apply executor; reads IPC request; validates; wraps crontab line; writes to fixed paths; snapshots + rolls back on failure.
- `scripts/host-setup.sh` — installs cron-apply + crontab-snapshot units; retires old crontab-acl units; strips stale ACLs.
- Deleted: `deploy/systemd/homelab-monitor-crontab-acl.path`
- Deleted: `deploy/systemd/homelab-monitor-crontab-acl.service`
- Deleted: `scripts/refresh-crontab-acl.sh`
- `docs/architecture/cron-identity.md` — updated with wrapper install and unwrap-before-fingerprint logic.
- `docs/deploy/host-prerequisites.md` — updated with cron-apply + crontab-snapshot systemd units; retired crontab-acl units.
- `docs/cron/install-heartbeat.md` (NEW) — user-facing guide for UI Install button flow.
- `docs/cron/remote-install.md` (NEW) — user-facing guide for standalone remote-host install CLI.
- `packages/shared-types/openapi.json` — regenerated (new endpoint, new field).

### Finalize-phase quality work

- **Two-pass Opus code review** — first pass: 4 Critical + 8 Important + 9 Minor findings, all fixed. Second pass: APPROVED.
- **Test coverage** — 1809 backend tests passed, 100% kernel coverage. 218 UI tests passed.
- **All lint/type checks** — ruff/black/pyright/tsc/prettier clean.

### Verify cycles

- **Build phase:** Backend tests + UI tests + lint/type checks all green.
- **Refinement phase (3a dev rig):** Validated wrapper install flow (dry-run preview → confirm), IPC request/result polling, error handling, unwrap-before-fingerprint convergence.
- **Finalize phase:** Code review + tests + commit.

### Deferred items

- **Heartbeat receiver UI label inconsistency** — the heartbeat receiver computes `expected_next_at` as last_run + cadence + grace (the "late threshold," ~last_run + 2×cadence when grace==cadence), but the UI labels this value "NEXT DUE", which is misleading. This predates STAGE-009. To be reconciled in STAGE-002-010 (which adds vmalert lateness rules and depends on this threshold) — either adjust the UI label or document the formula as intended.

### Migration status

No new alembic migration in this stage.

### Files touched (production)

46 files touched across CLI, backend, frontend, deploy, and docs.

- `apps/monitor/homelab_monitor/cli/cron.py`
- `apps/monitor/homelab_monitor/cli/install_wrapper_remote.py` (NEW)
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py`
- `apps/monitor/homelab_monitor/kernel/api/routers/crons.py`
- `apps/monitor/homelab_monitor/kernel/config.py`
- `apps/monitor/homelab_monitor/kernel/cron/cron_apply_constants.py` (NEW)
- `apps/monitor/homelab_monitor/kernel/cron/cron_apply_ipc.py` (NEW)
- `apps/monitor/homelab_monitor/kernel/cron/heartbeat_wrapper_token.py` (NEW)
- `apps/monitor/homelab_monitor/kernel/cron/install.py` (NEW)
- `apps/monitor/homelab_monitor/kernel/cron/repository.py`
- `apps/monitor/homelab_monitor/kernel/cron/schemas.py`
- `apps/monitor/homelab_monitor/kernel/heartbeat/repository.py`
- `apps/monitor/homelab_monitor/data/cron-with-heartbeat.sh.tmpl` (NEW)
- `apps/monitor/homelab_monitor/plugins/discoverers/cron_discoverer.py`
- `apps/monitor/homelab_monitor/plugins/discoverers/cron_parser.py`
- `apps/ui/src/api/crons.ts`
- `apps/ui/src/components/crons/CronDetail.tsx`
- `apps/ui/src/components/crons/__tests__/CronDetail.test.tsx`
- `apps/ui/src/components/crons/__tests__/CronsTable.test.tsx`
- `apps/ui/src/routes/inventory/__tests__/CronsList.test.tsx`
- `deploy/compose/.env.example`
- `deploy/compose/docker-compose.yml`
- `deploy/dev/dev.env.example`
- `deploy/systemd/homelab-monitor-cron-apply.path` (NEW)
- `deploy/systemd/homelab-monitor-cron-apply.service` (NEW)
- `deploy/systemd/homelab-monitor-cron-apply.timer` (NEW)
- `deploy/systemd/hm-crontab-snapshot.path` (NEW)
- `deploy/systemd/hm-crontab-snapshot.service` (NEW)
- `deploy/systemd/hm-crontab-snapshot.timer` (NEW)
- `scripts/hm-cron-apply.sh` (NEW)
- `scripts/host-setup.sh`
- Deleted: `deploy/systemd/homelab-monitor-crontab-acl.path`
- Deleted: `deploy/systemd/homelab-monitor-crontab-acl.service`
- Deleted: `scripts/refresh-crontab-acl.sh`
- `docs/architecture/cron-identity.md`
- `docs/deploy/host-prerequisites.md`
- `docs/cron/install-heartbeat.md` (NEW)
- `docs/cron/remote-install.md` (NEW)
- `packages/shared-types/openapi.json`

### Tests

- `apps/monitor/tests/test_api_install_wrapper.py` — endpoint schema validation, dry-run logic, result polling.
- `apps/monitor/tests/test_cli_cron_install.py` — CLI install-wrapper subcommand, IPC integration.
- `apps/monitor/tests/test_cron_apply_cons.py` — cron-apply IPC request/result models.
- `apps/monitor/tests/test_cron_parser.py` — unwrap logic, original command preservation.
- `apps/monitor/tests/test_cron_discoverer.py` — snapshot-based discovery, unwrap-before-fingerprint.
- `apps/monitor/tests/test_cron_repo.py` — `get_for_install_wrapper()`, `mark_wrapped()`, fingerprint convergence.
- `apps/ui/src/components/crons/__tests__/CronDetail.test.tsx` — InstallHeartbeatModal rendering, `is_local` gating.
- `apps/ui/src/components/crons/__tests__/CronsTable.test.tsx` — `is_local` field display.
- `apps/ui/src/routes/inventory/__tests__/CronsList.test.tsx` — `is_local` filtering in list view.

### References

- Design spec: `docs/superpowers/specs/2026-05-04-homelab-monitor-design.md` (§4.4 Wrapper install + cron-apply executor)
- Stage tracking: `epics/EPIC-002-heartbeat-cron/STAGE-002-009.md`
- Cron identity: `docs/architecture/cron-identity.md`
- Install guides: `docs/cron/install-heartbeat.md`, `docs/cron/remote-install.md`
- Host prerequisites: `docs/deploy/host-prerequisites.md`

### Commit

2043386

---
