2026-06-27 251fea2 EPIC-008 / STAGE-008-024: Backend panel data endpoints (Synology + Surveillance)

Added the backend FastAPI panel data endpoints that feed the Synology + Surveillance Integration pages (the frontend stages 025-029 consume these). All read VictoriaMetrics per-series gauges (+ one live DSM re-query for active connections); typed Pydantic response_models; honest-empty-state (absent series default to 0/None/[]/false; 502 only on VM transport failure).

- `kernel/api/routers/integrations_synology.py` (new): GET /api/integrations/synology/{summary, hardware, ops, disks/{disk}/smart-attrs, connections}. /summary = the 025 header strip; /hardware = the 026 cluster (volumes/pools/disks/SMART/system/UPS/ssh-probe); /ops = the 027 cluster (backup/replication/updates/security/mounts); /disks/{disk}/smart-attrs = per-disk SMART drill-down; /connections = a LIVE SynologyRestClient `current_connection_list()` re-query (the active-connection who-list — returned to the authenticated session only, never persisted; degrades to empty + data_available=false on DSM-unreachable, never 502s).
- `kernel/api/routers/integrations_surveillance.py` (new): GET /api/integrations/surveillance/{summary, cameras}.
- `kernel/api/app.py`: registered both routers.
- `packages/shared-types/openapi.json` + `apps/ui/src/api/schema.ts`: regenerated for the new endpoints (the openapi-export + generate-ui-types hooks; tsc compiles clean against the new types).

A per-section `data_available` flag (derived from the `homelab_collector_run_success_total{name=...}` self-metric for surveillance/ssh-probe/mount) distinguishes "collector not running" from "genuinely zero".

Verification: full make verify GREEN — backend pytest 6666 passed / 100.00% GLOBAL branch coverage (both new router modules at 100%); ruff/format/pyright 0/0/0; frontend gate (tsc against the new schema.ts) clean. Refinement live-validated all 8 endpoints against the real prod NAS (after a --no-cache prod image rebuild that brought the surveillance metrics live, cameras_total=3): 7/7 PASS, zero endpoint bugs; /connections returned 4 real active connections.

---

2026-06-27 9e5876b EPIC-008 / STAGE-008-023: vmalert-logs rules over synology-* + vector E651 fix + CI hardening

Authored vmalert-LOGS alert rules over the `service="synology-*"` VictoriaLogs syslog stream (forwarded by STAGE-008-020), and fixed several issues this work surfaced.

- `deploy/vmalert/logs/synology.yaml` (new): 4 LogsQL rules in group `synology_logs` — SynologyFailedLoginBurst (warning, `service:="synology-auth" "failed to sign in"` burst >10/5m), SynologySmartEventLog (warning, `service:="synology-smart" "SMART"`), SynologyPackageEventLog (info, `service:="synology-package" ("updated" OR "installed" OR "failed")`), SynologyLogErrorRate (info, `service:~"synology-.*" ("failed" OR "failure" OR "critical")` >20/10m). Labels source_tool: vmalert-logs, integration: synology, category: storage. (LogsQL phrase matching is token-based — `"failed"` matches the token `failed`, not the substring of a different word.)
- `deploy/vmalert/logs-test/synology.yaml` (new): the shortened-window mirror for the integration-test harness.
- `apps/monitor/tests/integration/test_vmalert_synology_logs_pipeline.py` (new): 5 integration tests planting synthetic synology-* lines via the VL ingest API + asserting each rule fires.

Issues fixed (all surfaced during this stage's live/integration validation):
- `deploy/vector/vector.toml.template`: removed 7 `to_string(...) ?? ""` coalesces in the `synology_parse` transform — a VRL E651 ("unnecessary error coalescing") that STAGE-008-020 shipped and that CRASH-LOOPED prod vector on redeploy (`to_string()` is infallible on parse_syslog's typed fields). `udm_parse`'s coalesces are load-bearing (over `parse_key_value() ?? {}`) and were left intact.
- `apps/monitor/homelab_monitor/kernel/logs/drain_consumer.py`: fixed a data-loss bug — the drain query had no sort, so a partial batch was an arbitrary subset and the watermark (max_ts_seen) could skip unprocessed lines. Now `* | sort by (_time)` makes partial batches oldest-N. Scoped to the drain path only (the window fetcher deliberately runs unsorted to avoid OOM).
- `apps/monitor/tests/integration/test_heartbeat_path.py`: fixed an orphaned test (STAGE-001-021 stub vs the STAGE-002-001 cron-resolution 404 gate) — now registers a cron fingerprint then asserts 204; added a 404-unknown-fingerprint test.

CI hardening (the vector E651 shipped because the only `vector validate` check skips when the binary isn't on PATH — every gate):
- `scripts/validate-vector-template.sh` (new): renders the template + runs `docker run --rm timberio/vector:0.41.1-debian validate`, exiting non-zero on any VRL error. Wired into `make verify-ci` (via a new `validate-vector-template` Makefile target) + the CI `integration` job (`.github/workflows/ci.yml`, which runs docker on the host). Proven to catch a re-introduced E651.
- `apps/monitor/tests/integration/test_vector_template_validate.py` (new): a self-contained vector-validate integration test for the host-pytest dev path.

Refinement live-validated: vector redeployed + restored healthy (UDM pipeline intact); 3 real failed DSM logins confirmed in synology-auth with the rule phrase matching real DSM wording exactly. Full make verify green (6631 passed / 100% coverage); full integration suite 0 failed.

---

2026-06-27 ef324b6 EPIC-008 / STAGE-008-022: vmalert-metrics protect/security/updates + surveillance-integrity rules

Authored the second tranche of Synology vmalert-metrics alert rules, grounded against the live production NAS (captured via a throwaway container running current collector source against the real DSM, since the running prod image predates these collectors). Hybrid severity model (info=standing advisory, warning=problem, critical=hardware/data-loss).

- `deploy/vmalert/metrics/synology.yaml`: +3 groups / +6 rules (existing 5 groups untouched). `synology_protect`: SynologyBackupFailed (critical, `backup_last_result_ok==0`), SynologyNoBackupConfigured (info, `no_backup_configured==1`). `synology_updates`: SynologyUpdateAvailable (info) and SynologyDsmSecurityUpdateAvailable (warning) — mutually exclusive via the `dsm_update_is_security` flag; SynologyPackageUpdateAvailable (info, `count(package_update_available==1)>0`). `synology_security`: SynologySecurityAdvisorNotSafe (info, `security_safe==0`). File now has 25 rules.
- `deploy/vmalert/metrics/synology-surveillance.yaml` (new): `synology_surveillance` group with SurveillanceCameraOffline (warning, `ss_camera_connected==0`), SurveillanceCameraRecordingDisabled (warning, `ss_camera_recording_retention_mode{mode="none"}==1`), SurveillanceLicenseExhausted (warning, `ss_info_license_used >= ss_info_license_max`). Separate file because the epic ships two dashboards / two integration pages (Synology + Surveillance).
- Golden tests: 12 stanzas appended to synology.tests.yaml + new synology-surveillance.tests.yaml (6 stanzas). All promtool check/test pass.

Card-vs-reality corrections from the live capture: the license rule fires over the cameras-collector `ss_info_license_used/max` (the dedicated `ss_license_*` metrics are permanently 0 — the DSM account lacks the License/Load privilege); the recording signal is `recording_retention_mode` (no direct "is-recording" metric exists); two card severities were downgraded to match the hybrid model (NoBackupConfigured warning→info, CameraOffline critical→warning).

Deferred (tracked in regression.md): SynologyReplicationLag NOT authored (REG-008-REPLICATION-BLOCKED — replication metrics unreadable: DSM account lacks the Btrfs/Replication privilege); SurveillanceRecordingStorageLow satisfied-by-reuse by the existing volume-fullness rules; the connected-but-silently-not-recording trend rule deferred (REG-008-SURVEILLANCE-RECORDING-TREND, needs a new recordings-trend metric); the dead `ss_license_*` metrics flagged (REG-008-LICENSE-METRICS).

Refinement: live-validated all 9 rules against real NAS data — 9/9 correct, zero false positives.

---

2026-06-27 b2c3ceb EPIC-008 / STAGE-008-021: Synology vmalert-metrics core — 16 storage/hardware/system alert rules

Added the core Synology metric alert rules (storage, disk/SMART, pool/RAID, scrub, temps, UPS, system, utilization, NeedReboot) over the always-emitted alertable gauges from the Synology collectors, with hybrid severity (advisory=info, problems=warning, hardware/data-loss=critical). The exprs are grounded in the REAL collector metric names (verified against source + the live NAS), and the rules were live-evaluated against the running prod VictoriaMetrics — which caught and fixed two false-fire bugs golden tests alone could not.

- EXTENDED `deploy/vmalert/metrics/synology.yaml` with 3 new groups (`synology_storage` / `synology_hardware` / `synology_system`), 16 rules: critical (SynologyDown=`health_ok==0`, VolumeDegraded=`volume_writable==0`, PoolDegraded=`raid_status==0`, DiskFailed=`disk_status==0 or disk_smart_status==0 or smart_attr_failing>0`, UpsOnBattery, UpsLowBattery); warning (VolumeAlmostFull≥90%, DiskHighTemp≥55°C, SysHighTemp=`sys_temp_warning==1`, HighCpuMem, NeedReboot, ScrubOverdue>90d, ScrubError=`pool_disk_failure_number>0`); info (VolumeNearFull≥80%, UnverifiedDisk, ApiSlow). The 019 groups (mount-down + container inhibit) are untouched.
- SSH rules and the connector-reachability rule are SATISFIED-BY-REUSE (the generic `ssh.yaml` SshHostKeyMismatch/SshProbeStale already fire for `target=synology`; `collector_health.yaml` `collector_silent_death` already covers the synology collectors) — deliberately not duplicated to avoid double-firing; documented in the synology.yaml header.
- Golden tests (`__tests__/synology.tests.yaml`) for all rules; the time()-based SynologyScrubOverdue tests are isolated into `__tests__/synology.scruboverdue.tests.yaml` with a coarse `evaluation_interval: 6h` (a fine interval × eval_time≥91d hangs promtool; a coarse interval can't coexist with the main file's `for:`-duration tests).
- Two false-fire fixes from live-validation: SynologyApiSlow is now per-api with a `{api!~"SYNO\\.Storage\\.CGI\\.Storage/.*"}` exclusion (the storage `load_info` API is characteristically ~3.7s but benign); SynologyScrubOverdue now guards `ts > 0` (DSM reports 0 = never/unknown, which an unguarded `time()-0` mis-reads as 54-years-overdue).

Validated: both promtool golden test files SUCCESS (fast); `promtool check rules` → 19 rules; full backend gate 6628 passed + 100% coverage; ui-verify green. Live NAS confirmed the critical-rule metrics flow (no silently-dead rule) and the judgment-call thresholds (55°C disk temp, 90% cpu/mem) are sane. Capturing the real degraded/crashed/scrub-error enum strings (only seen on a healthy NAS so far) is tracked as an open regression item.

---

2026-06-27 f8e82b3 EPIC-008 / STAGE-008-020: Synology DSM syslog pipeline — vector RFC-3164 source, service="synology-*" labels, redaction

Added the Synology DSM → vector → VictoriaLogs syslog pipeline, mirroring the EPIC-007 UDM pipeline but adapted for DSM's RFC-3164 (BSD) format. This is the sole log path for the Synology (no API log polling). Grounded on real captured DSM datagrams, which corrected the original card assumptions (DSM logs everything at PRI=14 so facility is useless for bucketing; the syslog TAG is a human-readable category word, not a Unix program name; the category delimiter is inconsistent — `Connection:` has a colon, `System Test…` does not; the multi-NIC NAS forwards from an unstable source IP).

- NEW vector blocks in `deploy/vector/vector.toml.template`: `[sources.synology_syslog]` (lossless `type="socket"` UDP, container :5515) → `[transforms.synology_parse]` (VRL `parse_syslog()` + category-word bucketing handling both colon and space delimiters: `Connection`→`synology-auth`, `Storage Manager`→`synology-smart`, `Package Center`→`synology-package`, recognized-else→`synology-system`, parse-failure→`synology-other` with `parse_failed=1`; `source_type="synology"`) → `[transforms.synology_throttle]` (50/s per service) → `[transforms.redact_synology]` → `[transforms.strip_markers_synology]`, plus a `synology_parse_failed_metric` so dropped/unrecognized lines are counted. Wired into the vl + redaction-metric sinks.
- NEW 5 `synology_*` redaction patterns in `apps/monitor/homelab_monitor/kernel/config.py` (the real PII is bracket/paren-wrapped: `User [x]`, `from [x]`, `via [x]`, `Synology Syslog Client from (x)`; plus a provisional `synology_synotoken` guard). Rust-regex-safe, no lookarounds.
- NEW pure-Python reference-parse test (`tests/test_synology_syslog_parse.py`) + fixture (`tests/fixtures/synology_syslog_samples.txt`, 2 real captured lines + labeled-synthetic SMART/package/malformed lines); structural asserts in `tests/test_vector_template.py`; redact-count bumps in `tests/test_redaction_config.py` + `tests/test_render_redaction.py`.
- Dedicated prod port `0.0.0.0:5515/udp` (dev `127.0.0.1:15515`) with `HM_SYNOLOGY_SYSLOG_*` env vars (compose + dev compose + both .env.examples); the dedicated port — not a source-IP filter — is the device discriminator (robust to the multi-NIC NAS). CLAUDE.md port-map row added (2nd documented LAN-syslog 0.0.0.0 exception). `docs/integrations/synology.md` gained the DSM-side Log Center enable steps + the Amendment-5 syslog-vs-metric coverage map.

Validated: 131 unit/structural/redaction tests pass; the rendered prod template passes `vector validate` (real `timberio/vector:0.41.1-debian`); a throwaway-vector ingest of the REAL DSM datagrams confirmed `Connection`→synology-auth (with `User`/`from`/`via` redacted), `System`→synology-system, and the synthetic SMART/package/malformed lines bucket correctly. Real category words for SMART/package remain confirmed-by-synthetic-fixture only (NAS idle during capture) — tracked as an OPEN regression item consumed by STAGE-008-023. Full backend gate: 6629 passed, 100% coverage; ui-verify green.
