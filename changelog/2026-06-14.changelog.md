## [STAGE-005-028] Home Assistant drill-list widgets (frontend)

**Summary:** Added 5 frontend "drill-list" widgets to the Home Assistant panel, consuming the STAGE-005-027 VM-sourced detail endpoints. Each drill-list renders the actual rows behind the existing count tiles (which entities are unavailable, which batteries are low, etc.) so the user can drill from "962 unavailable" into the specific entities. Also fixed a pre-existing latent layout bug in the HA panel that prevented the tab bodies from scrolling.

**New files:**
- `apps/ui/src/routes/integrations/DrillList.tsx` — generic presentational shell `<DrillList<T>>` (loading / empty / error / 502 states, a `max-h-80` scroll container above a row threshold, a conditional "Showing {returned} of {total} — {ordering}" caption when total > returned, and a `keyExtractor` prop for stable list keys).
- `apps/ui/src/routes/integrations/HaEntitiesDrill.tsx` — container component owning `useHomeAssistantEntities` TanStack Query hook + independent loading/empty/error/502 state; renders unavailable entities (entity_id, domain, age).
- `apps/ui/src/routes/integrations/HaBatteriesDrill.tsx` — container component owning `useHomeAssistantBatteries` TanStack Query hook + independent loading/empty/error/502 state; renders low/critical batteries (entity_id, level% with red <10 / amber 10–<20 tint).
- `apps/ui/src/routes/integrations/HaUpdatesDrill.tsx` — container component owning `useHomeAssistantUpdates` TanStack Query hook + independent loading/empty/error/502 state; renders pending updates (title-or-entity_id fallback).
- `apps/ui/src/routes/integrations/HaConfigEntriesDrill.tsx` — container component owning `useHomeAssistantConfigEntries` TanStack Query hook + independent loading/empty/error/502 state; renders config-entry errors (domain, title, state badge).
- `apps/ui/src/routes/integrations/HaRepairsDrill.tsx` — container component owning `useHomeAssistantRepairs` TanStack Query hook + independent loading/empty/error/502 state; renders active repairs (domain, issue_id, severity badge).
- `apps/ui/src/routes/integrations/DrillList.test.tsx` — render tests (loading/empty/error/502 states, caption rendering, key extraction).
- `apps/ui/src/routes/integrations/HaEntitiesDrill.test.tsx` — hook-mocked render test (entity list, age formatting, 502 handling).
- `apps/ui/src/routes/integrations/HaBatteriesDrill.test.tsx` — hook-mocked render test (battery list, level tinting, 502 handling).
- `apps/ui/src/routes/integrations/HaUpdatesDrill.test.tsx` — hook-mocked render test (update list, title fallback, 502 handling).
- `apps/ui/src/routes/integrations/HaConfigEntriesDrill.test.tsx` — hook-mocked render test (config-entry list, state badge, 502 handling).
- `apps/ui/src/routes/integrations/HaRepairsDrill.test.tsx` — hook-mocked render test (repair list, severity badge, 502 handling).
- `apps/ui/src/lib/relativeTime.test.ts` — tests for `formatAge(seconds)` helper (coarse single-unit "just now" / "Xm ago" / "Xh ago" / "Xd ago").

**Modified files:**
- `apps/ui/src/api/home_assistant.ts` — added 5 new TanStack Query hooks: `useHomeAssistantEntities`, `useHomeAssistantBatteries`, `useHomeAssistantUpdates`, `useHomeAssistantConfigEntries`, `useHomeAssistantRepairs` (each mirrors `useHomeAssistantSummary`: queryKey under `haQueryKeys`, `unwrap<...RowsResponse>`, `refetchInterval: 30_000`).
- `apps/ui/src/lib/relativeTime.ts` — added `formatAge(seconds)` helper (coarse single-unit "just now" / "Xm ago" / "Xh ago" / "Xd ago").
- `apps/ui/src/routes/integrations/types.ts` — re-exported the 10 generated drill-list row/response types: `HaEntityRow`/`HaEntityRowsResponse`, `HaBatteryRow`/`HaBatteryRowsResponse`, `HaUpdateRow`/`HaUpdateRowsResponse`, `HaConfigEntryRow`/`HaConfigEntryRowsResponse`, `HaRepairRow`/`HaRepairRowsResponse`.
- `apps/ui/src/routes/integrations/HomeAssistantHealthTab.tsx` — wired `<HaEntitiesDrill>` + `<HaBatteriesDrill>` below the existing count tiles; fixed pre-existing latent layout bug by adding `h-full overflow-y-auto` to the tab-body root div (establishes scroll region without affecting other tabs).
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.tsx` — wired `<HaUpdatesDrill>` + `<HaConfigEntriesDrill>` + `<HaRepairsDrill>` below the existing count tiles; fixed pre-existing latent layout bug by adding `h-full overflow-y-auto` to the tab-body root div.
- `apps/ui/src/routes/integrations/HomeAssistantHealthTab.test.tsx` — added render tests for drill-list wiring (states + empty-state rendering).
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.test.tsx` — added render tests for drill-list wiring (states + empty-state rendering).

**Deferred work:** Fields the VM cannot carry (battery device name, update versions/release_url, repair summary, precise config-entry state) are deferred to STAGE-005-031 (live-HA enrichment) and STAGE-005-032 (config-entry state collector change). Frontend-only; consumes existing endpoints (no backend/OpenAPI change).

**Layout bug:** The HA tab bodies could not scroll when content exceeded the viewport (the `HomeAssistantLayout` Outlet wrapper is `overflow-hidden` and the tab bodies had no scroll region). Fixed by adding `h-full overflow-y-auto` to both tab-body root divs — establishes the scroll region without affecting the iframe/LogViewer (Logs/Metrics) tabs.

**Design decisions:** D-DRILL-EXPAND = always-visible lists below the count tiles (entities list gets a `max-h-80` scroll cap). D-DRILL-CAP = conditional "Showing {returned} of {total} — {ordering}" caption when total > returned. D-DRILL-ROW-SHAPE = one generic `<DrillList<T>>` shell + per-list `renderRow` (categorical state/severity → Badge; numeric battery level → tinted span; updates title falls back to entity_id). D-DRILL-DATA-WIRING = pure shell + thin per-list container owning its own hook + independent loading/empty/error/502 state.

**Verification:** `make verify` GREEN — ruff clean, pyright 0, pytest 4829 pass / 1 skip, kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1515 pass, UI build OK. Refinement 3a: Desktop + Mobile both approved against the rebuilt prod-rig UI with REAL data (962 unavailable entities / 100 returned capped + "Showing 100 of 962" caption, 0 low/critical batteries, 0 pending updates, 3 config-entry errors [blink/frigate/shelly], 0 active repairs — empty states for the genuinely-empty lists are correct).

**Commit:** (commit: dc0152a)

## [STAGE-005-029] Home Assistant persistent-notification detail (live re-query + safe render)

**Summary:** Added a backend endpoint that live-queries Home Assistant for persistent-notification bodies (deliberately kept out of VictoriaMetrics for privacy) and a frontend widget that renders them safely. The HA panel's Status tab now shows the actual current notifications (title + body + relative time), sourced by a live HA WebSocket re-query, rendered text-only/escaped with no HTML-injection surface.

**New files:**
- `apps/monitor/homelab_monitor/kernel/ha/notifications.py` — shared notification parsing logic: `extract_notifications(data)` defensively coerces HA's persistent_notification/get response into a list of dicts with `notification_id` (required), `title` (null if missing), `message` ("" if missing), `created_at` (null if missing). Skips malformed notifications (non-dict, missing/empty notification_id).
- `apps/monitor/tests/test_api_ha_notifications.py` — endpoint tests (200 with live HA data, 502 on HA unreachable/timeout/http_error/bad_response/auth, defensive parsing skips malformed, bodies + token never logged — sentinel test confirms absence via grep).
- `apps/ui/src/routes/integrations/HaNotificationsDrill.tsx` — container component owning `useHomeAssistantNotifications` TanStack Query hook + independent loading/empty/error/502 state; renders active notifications (notification_id key, title fallback "(untitled)", relative time via `formatAge`, message rendered plain-text).
- `apps/ui/src/routes/integrations/HaNotificationsDrill.test.tsx` — hook-mocked render test (notification list, title/message/time rendering, safe-render malicious-string test: `<script>`/`<img onerror>`/`[x](javascript:)`/`**bold**` render as literal inert text).

**Modified files:**
- `apps/monitor/homelab_monitor/kernel/api/dependencies.py` — added `get_ha_ws_client` dependency (injected `ha_ws_client` from app.state).
- `apps/monitor/homelab_monitor/kernel/api/routers/integrations_home_assistant.py` — added `GET /api/integrations/home-assistant/notifications` endpoint (`Depends(require_session())` + `Depends(get_ha_ws_client)`). Live-queries HA via `ha_ws_client.send_command("persistent_notification/get")`. Returns `HaNotificationsResponse {rows: list[HaNotificationRow], total, returned}`. Maps `HaError` (unreachable/timeout/http_error/bad_response/auth) to HTTP 502 (upstream_unavailable); auth errors are 502 NOT 401 (bad HA token is a server config problem). Uses shared `extract_notifications` parser.
- `apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_persistent_notification.py` — refactored to use shared `extract_notifications` from `kernel.ha.notifications` (single parsing-truth).
- `apps/ui/src/api/home_assistant.ts` — added `useHomeAssistantNotifications` TanStack Query hook (mirrors 028 hooks: queryKey under `haQueryKeys`, `unwrap<HaNotificationsResponse>`, `refetchInterval: 30_000`).
- `apps/ui/src/routes/integrations/types.ts` — re-exported `HaNotificationRow` / `HaNotificationsResponse`.
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.tsx` — wired `<HaNotificationsDrill>` into the Status tab, reusing 028 `<DrillList<HaNotificationRow>>` shell.
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.test.tsx` — added a default `beforeEach` mock for `useHomeAssistantNotifications` so the auto-mocked new hook does not render `undefined` in the existing tab tests.
- `packages/shared-types/openapi.json` — regenerated with new models: `HaNotificationRow`, `HaNotificationsResponse`.

**Privacy & Safety (hard contracts, validated end-to-end):**
- **Bodies never logged.** Notification bodies + titles are returned only to the authenticated session and are NEVER logged. Verified on prod rig: `docker logs homelab-monitor | grep -iE 'ha_token|bearer|eyJ'` → 0 hits; `grep -i <distinctive-body-substring>` → 0 hits. Enforced by backend sentinel test.
- **Safe render.** Message + title render as plain-text React auto-escape (NO markdown lib, NO sanitizer, NO `dangerouslySetInnerHTML`). Enforced by frontend safe-render test: `<script>`, `<img onerror>`, `[x](javascript:)`, `**bold**` render as literal inert text — no DOM node, no handler fires.

**Design decisions:** D-NOTIF-SOURCE = WS `persistent_notification/get` via `app.state.ha_ws_client` (reuse the already-wired client + the shared `extract_notifications` helper; not REST, not VM). D-NOTIF-PRIVACY = full title+message+created_at to the authenticated session with enforced no-log/no-metric/no-error-leak (the STAGE-012 bodies-out decision was about METRICS, not the authenticated UI). D-NOTIF-SAFE-RENDER = plain-text React auto-escape + `whitespace-pre-wrap break-words` (no markdown lib, no sanitizer, no `dangerouslySetInnerHTML`). D-NOTIF-WIDGET-PLACEMENT = reuse the 028 `<DrillList<HaNotificationRow>>` shell with a rich renderRow in a new "Notifications" PanelSection on the Status tab. Degradation: HaError → 502 (incl. auth → 502, not 401).

**Verification:** `make verify` GREEN — ruff clean, pyright 0, pytest 4838 pass / 2 skip, kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1521 pass, UI build OK. Refinement 3a (frontend Desktop + Mobile approved) + 3b (prod rig real data: 2 live HA notifications rendered safely; token + bodies never logged — both greps returned 0).

**Commit:** (commit: 75c27bc)

## [STAGE-005-031] Home Assistant detail live-HA enrichment (battery device + update versions + repair description/url + entity friendly_name)

**Summary:** Added a live-HA enrichment layer to the STAGE-005-027 HA detail endpoints. VictoriaMetrics still SELECTS the rows (count-consistent, cardinality-bounded); live HA only fills in display-only fields VM per-series can't carry, joined onto the VM-selected rows. After this stage, the 028 drill widgets carry human-readable detail (friendly device names, update versions, repair descriptions).

**New files:**
- `apps/monitor/homelab_monitor/kernel/ha/enrichment.py` — shared enrichment helpers: `build_states_index(states) -> dict[str, HaState]` (entity_id-keyed index), `attr_str(state, key) -> str | None` (the SOLE reader of the untyped `state.attributes`; returns only if isinstance str), kernel-local `extract_issues(result)` (defensive bare-list / `{"issues":[...]}` shape; kernel does NOT import from plugins), `build_repairs_index(issues) -> dict[tuple[str,str], RepairEnrichment]` with a `RepairEnrichment` frozen dataclass `{description, learn_more_url}`.
- `apps/monitor/tests/test_ha_enrichment.py` — unit tests for enrichment helpers (states index correctness, attribute extraction strict-typing, repairs index keying, defensive malformed-issues parsing).

**Modified files:**
- `apps/monitor/homelab_monitor/kernel/api/dependencies.py` — added `get_ha_client` dependency (mirrors `get_ha_ws_client`, injects `ha_client` from app.state).
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — added `app.state.ha_client = ha_client` initialization (the REST client was built but previously not on app.state).
- `apps/monitor/homelab_monitor/kernel/api/routers/integrations_home_assistant.py` — enriched the 4 detail endpoints (`/batteries`, `/updates`, `/entities`, `/repairs`) with live-HA data joined onto VM-selected rows. `/batteries` → `device` (friendly_name); `/updates` → `installed_version` / `latest_version` / `release_url`; `/entities` → `friendly_name`; `/repairs` → `description` + `learn_more_url`. REST bulk `get_states()` call (one per endpoint) drives entity-keyed enrichment; WS `repairs/list_issues` snapshot drives repair enrichment keyed `(domain, issue_id)`.
- `apps/monitor/tests/test_api_ha_detail.py` — added enrichment integration tests (entity-keyed friendly_name match, device-keyed battery enrichment, graceful degradation on HA-down, null on not-found/non-str-attribute, never-logged sentinel test).
- `apps/ui/src/routes/integrations/HaBatteriesDrill.test.tsx` — updated test mocks to include new `device` field (null).
- `apps/ui/src/routes/integrations/HaEntitiesDrill.test.tsx` — updated test mocks to include new `friendly_name` field (null).
- `apps/ui/src/routes/integrations/HaRepairsDrill.test.tsx` — updated test mocks to include new `description` and `learn_more_url` fields (null).
- `apps/ui/src/routes/integrations/HaUpdatesDrill.test.tsx` — updated test mocks to include new `installed_version`, `latest_version`, and `release_url` fields (null).
- `packages/shared-types/openapi.json` — regenerated with new enriched model fields on `HaBatteryRow`, `HaEntityRow`, `HaRepairRow`, `HaUpdateRow`.

**Graceful degradation (load-bearing):** Any `HaError` (HA down, timeout, auth, bad response) → enrichment fields null, VM rows + 200 STILL returned, NEVER 502. Per-row not-found / attribute-missing / non-str attribute → null (individual field). Only VM-down is 502 (unchanged). Response shape NEVER wrapped or modified by enrichment.

**Privacy & Safety (hard contracts, validated end-to-end):**
- **Enriched values never logged.** Friendly names, device names, versions, repair descriptions, and URLs are returned only to the authenticated session, NEVER logged, NEVER in metrics, NEVER in error messages. HA errors swallowed to null, not raised. Enforced by backend sentinel test: friendly_name/device name/repair description in response, absent from all container logs. Validated on prod rig: `docker logs homelab-monitor | grep -iE 'friendly|device|description'` → 0 hits.
- **Attribute-reading strict.** `attr_str(state, key)` is the SOLE reader of `state.attributes` (untyped dict). Returns only if the attribute exists AND is a string; otherwise null. Defensive against malformed HA state schema.

**Repair field correction:** Stock HA `repairs/list_issues` response has NO `summary` key (the Design working-name); 031 reads the REAL `description` (str | null) and `learn_more_url` (str | null) keys per-issue.

**OpenAPI & frontend schema cascade:** Model fields are `str | None`. OpenAPI regenerated; `schema.ts` (gitignored) regenerated. The 028 drill widgets render new fields defensively (present-or-absent). Frontend-only test-mock updates to include null values (no widget rendering logic change this stage). Displaying `friendly_name` in the entities drill is tracked as STAGE-005-033.

**Deferred work:** Widgets rendering `friendly_name` in HaEntitiesDrill and other entity-aware drills → STAGE-005-033. Deferral increased epic denominator from 32→33 stages.

**Design decisions:** D-ENRICH-SOURCE = shared `kernel/ha/enrichment.py` helper functions + per-endpoint bulk `get_states()` REST call (entity_id-keyed) + WS `repairs/list_issues` (domain/issue_id-keyed). D-ENRICH-PRIVACY = full enriched fields to authenticated session, enforced never-logged/never-metric/never-error-leak. D-ENRICH-DEGRADE = enrichment fields `str | None`, null on HA-down/not-found/non-str-attribute, VM rows + 200 always preserved (never 502), no response-shape wrapper. D-ENRICH-ENTITY-FRIENDLY-NAME = add `friendly_name` to `HaEntityRow` backend model now; widget rendering deferred to STAGE-005-033.

**Verification:** `make verify` GREEN — ruff clean, pyright 0, pytest 4874 pass / 2 skip, kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1521 pass, UI build OK. Refinement 3b (prod rig, real HA): entities `friendly_name` populated on all 962 rows + batteries `device` populated ("Main Bathroom Toilet Motion Battery"); updates `installed_version` / `latest_version` populated on all 0 pending updates rows; repairs (0 active) graceful empty-state. Graceful degradation confirmed: HA-down simulation → all enrichment fields null, VM rows + 200 returned. Never-logged validated: `docker logs homelab-monitor | grep -iE 'bathroom|toilet|motion|battery|friendly'` → 0 hits; token/body grep 0.

**Commit:** (commit: 88d2088)

## [STAGE-005-032] HA config-entry state-distinction metric (collector `state` label)

**Summary:** Added a `state` label to the existing `homelab_ha_config_entry_setup_error` VictoriaMetrics gauge so VM can distinguish the four error states (`setup_error` / `setup_retry` / `migration_error` / `failed_unload`) that STAGE-005-027 collapsed into a single coarse "error". The STAGE-005-027 `/config-entries` endpoint now returns each row's PRECISE state from the new VM label instead of hardcoding "error". The `homelab_ha_config_entry_loaded` gauge is UNCHANGED, and the load-bearing `HaConfigEntryError` vmalert rule keeps firing with no expr change. Deferred from STAGE-005-027 (D-DETAIL-VM-LABELS-PRESENT).

**New files:**
- None.

**Modified files:**
- `apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_config_entry.py` — hoisted `state = _entry_state(entry)` to a loop-local and gave the error-gauge observation its own label dict `{**labels, "state": state}` (a fresh dict, no aliasing of the loaded gauge's `{domain,title}` dict); the `state` label is emitted on ALL error-gauge rows (value-1 AND value-0). `_state_gauges` / `_entry_labels` / `_entry_state` unchanged. Module docstring updated to document the new `{domain,title,state}` shape of the setup_error family.
- `apps/monitor/homelab_monitor/kernel/api/routers/integrations_home_assistant.py` — `get_ha_config_entries` now reads `state=s.labels.get("state", _FILTER_ERROR)` (precise state from the VM label; graceful fallback to "error" for pre-migration series with no `state` label). Updated the `HaConfigEntryRow.state` comment and the handler docstring to describe the precise-state behavior (removed the "coarse" / "deferred to STAGE-005-032" scaffolding language). No model field change (`state` was already `str`).
- `deploy/vmalert/metrics/home-assistant.yaml` — metric-contract comment updated to `homelab_ha_config_entry_setup_error{domain,title,state}  1/0`; the `HaConfigEntryError` rule `summary` annotation now renders the precise state ("HA config entry in error ({{ $labels.state }}): {{ $labels.title }} ({{ $labels.domain }})"). The rule `expr` is UNCHANGED (`homelab_ha_config_entry_setup_error == 1`).
- `deploy/vmalert/metrics/__tests__/home-assistant.tests.yaml` — updated the `HaConfigEntryError` test fixture: planted a `state` label on the input series, added `state` to `exp_labels`, and updated the expected `summary` to the new `({{ $labels.state }})` form.
- `apps/monitor/tests/test_ha_config_entry_collector.py` — extended three tests: error gauge carries the precise `state` on value-1 rows (parametrized over all 4 error states) AND on value-0 rows (loaded + neutral states); the loaded gauge never carries a `state` label.
- `apps/monitor/tests/test_api_ha_detail.py` — renamed `test_config_entries_coarse_error_state` → `test_config_entries_falls_back_to_error_without_state_label` (the no-label fallback case) and added `test_config_entries_precise_state_from_label` asserting precise per-row state passthrough from the VM label.
- `packages/shared-types/openapi.json` — regenerated; description-only change (the `/config-entries` endpoint `description` text propagated from the handler docstring edit). NO schema/field/type/required/enum change (verified via diff); `state` remains `str`.

**Graceful degradation:** The endpoint falls back to the coarse "error" string for any pre-migration series that lack a `state` label (e.g. series scraped before this deploy), so it never drops a row or crashes. VM-down remains 502 (unchanged).

**Privacy & cardinality:** `state` is a BOUNDED enum (HA `ConfigEntryState` + "" fallback) → safe as a metric label, no cardinality blow-up (each entry is in exactly one state per tick, so series count ≈ entry count — unchanged in practice). The free-text `reason` field is STILL never emitted as a metric label (the collector omits it; `test_reason_never_emitted_as_label` guards this).

**Design decisions:** D-CFGSTATE-SHAPE = add a `state` label to the EXISTING `homelab_ha_config_entry_setup_error` gauge (rejected per-state gauges + a new info-metric — both would force a vmalert rule rewrite and risk silently under-covering the error states). D-CFGSTATE-LABEL-SCOPE = emit `state` on ALL error-gauge rows (value-1 and value-0) via a single uniform `{**labels, "state": state}` code path (no branch on state); the resulting value-0 series churn on state transitions is invisible to both the endpoint and the vmalert rule (both filter `== 1`). D-CFGSTATE-VMALERT = no expr change — adding the label does not break the unfiltered `== 1` match and cannot double-count (one state per entry); only the summary annotation + contract comment changed.

**Verification:** `make verify` GREEN — ruff clean, pyright 0, pytest 4876 pass / 1 skip (vector binary not on PATH, environmental), kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1521 pass, UI build OK. Refinement 3a+3b (prod rig, real HA) 5/5 PASS: `/config-entries` returns precise `state="setup_retry"` for the 3 real error entries (blink / frigate / shelly); VM error gauge carries `state` on value-1 series; loaded gauge carries NO `state` label; vmalert `expr` unchanged + summary renders the precise state; `HaConfigEntryError` fired (after the `for: 5m` window restarted by the rule reload) and propagated to Alertmanager with "(setup_retry)" in the rendered summary; series identity verified stable across evals (no churn).

**Commit:** (commit: 4519c16)

## [STAGE-005-033] Entities drill renders friendly_name (frontend two-line row)

**Summary:** Wired the HA entities drill ("Unavailable entities" on the HA Health tab) to display the human-readable `friendly_name` that STAGE-005-031 added to `HaEntityRow`. Each entity row now renders as TWO lines — line 1 the `friendly_name` (primary) + relative age (right), line 2 the raw `entity_id · domain` (muted secondary) — so the list reads "Sliding Door" rather than just `binary_sensor.sliding_door`. Frontend-only follow-up to 031's backend enrichment. Deferred from STAGE-005-031 (D-ENRICH-ENTITY-FRIENDLY-NAME).

**New files:**
- None.

**Modified files:**
- `apps/ui/src/routes/integrations/HaEntitiesDrill.tsx` — `renderRow` rewritten to a two-line layout. Line 1: `friendly_name` primary (`min-w-0 truncate font-medium`) + age (`shrink-0 tabular-nums text-xs text-muted-foreground`, right). Line 2: `entity_id · domain` muted secondary (`truncate text-xs text-muted-foreground`), rendered only when a friendly_name is present. Primary uses the TS-safe inline ternary `row.friendly_name?.trim() ? row.friendly_name : row.entity_id` (narrows to `string`); a separate `hasName` boolean gates the secondary `<p>`. Separator is U+00B7 (·). `keyExtractor` unchanged (`row.entity_id`).
- `apps/ui/src/routes/integrations/HaEntitiesDrill.test.tsx` — null-case test updated (`getByText('sensor')` → `queryByText('sensor').not.toBeInTheDocument()`, since the domain is no longer a standalone node when friendly_name is null) and renamed to reflect the suppressed-secondary behavior; added a new test asserting friendly_name renders as the primary and `sensor.kitchen · sensor` renders as the combined secondary line. Empty / loading / 502 tests unchanged.

**Design decisions:** D-FRIENDLY-DISPLAY = render BOTH in a two-line row (Option B2), mirroring the established `HaNotificationsDrill` two-line primary/secondary precedent. Rejected Option A (friendly_name INSTEAD of entity_id — loses the actionable raw id the homelab owner needs). Rejected Option B1 (HaUpdatesDrill single-line clone with entity_id muted on the right — this drill already uses the right slot for age, so entity_id would contend for it and be clipped first on mobile). The secondary line is guarded by `hasName` so a null friendly_name (HA down / entity not found) shows entity_id as the primary with no redundant `entity_id`-over-`entity_id` double render.

**Verification:** `make verify` GREEN — backend 4876 pass / 1 skip (vector binary not on PATH, environmental) / 100% kernel coverage; frontend tsc clean, vitest 1522 pass (+1 new test), eslint 94 warnings / 0 errors (baseline), UI build OK. Refinement 3a (frontend, real rig): UI bundle rebuilt + served hash flipped `index-Dc1tdU-m.js` → `index-CpyGeCWl.js`; `/entities?filter=unavailable` confirmed 962 total / 100 returned all with populated `friendly_name`; user reviewed and APPROVED Desktop AND Mobile viewports.

**Commit:** (commit: 031f92a)

## [STAGE-005-034] Cadence: exclude disabled automations from last_triggered_seconds

**Summary:** The cadence collector emitted `homelab_ha_automation_last_triggered_seconds` for EVERY automation, including disabled (`state="off"`) ones — whose idle-age grows unbounded and is meaningless (they can never trigger). This stage gates that metric on `state == "on"`, so only ENABLED automations carry a triggered series; disabled / unavailable / unknown automations are excluded. `homelab_ha_automation_enabled` still carries all on/off automations (disablement detection preserved). This removes the noise that inflated the Grafana "Idle Automations (>24h)" panel and unblocks a clean idle alert in STAGE-005-039. First stage of the Wave I audit-gap-closure slate (GAP A). Backend-only.

**New files:**
- None.

**Modified files:**
- `apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_cadence.py` — the automation triggered-append `elif` gained `and state.state == _STATE_ON` (emit `homelab_ha_automation_last_triggered_seconds` only for enabled automations). The `if err: parse_errors += 1` branch is UNCHANGED and stays OUTSIDE the state gate — a disabled/unavailable automation with an unparseable `last_triggered` still increments `homelab_ha_cadence_last_triggered_parse_errors`. The enabled-state branch (on→1.0, off→0.0, else skip) and the entire script branch are UNCHANGED. Module docstring updated to state the triggered series is emitted only for enabled automations.
- `apps/monitor/tests/test_ha_cadence_collector.py` — +3 tests: (1) disabled automation with valid last_triggered → no triggered series, `automation_enabled`=0.0 still emitted; (2) unavailable automation with valid last_triggered → no triggered AND no enabled series (locks the `== on` form vs the weaker `!= off`); (3) disabled automation with UNPARSEABLE last_triggered → parse-error gauge still == 1.0 (locks parse-error-counting-outside-the-gate). No existing test required flipping.

**Design decisions:** D-CADENCE-OFF-VS-UNKNOWN = emit triggered only when `state == "on"` (the `== _STATE_ON` form — excludes off + unavailable + unknown), NOT `!= _STATE_OFF`; chosen because the downstream consumers (Grafana "Idle Automations" panel + the planned STAGE-005-039 `HaAutomationIdle` alert) query the triggered metric DIRECTLY without joining `homelab_ha_automation_enabled`, so series membership IS the filter and must encode the full "live + enabled" invariant. D-CADENCE-DISABLED-SCRIPTS = scripts UNCHANGED (a script's state is run-status, not an enable flag; no disabled notion to filter). D-CADENCE-METRIC-COUNT = no accounting change (`metrics_emitted` is a derived survivor count). Parse-error counting kept OUTSIDE the gate (data-quality signal independent of idle semantics).

**Verification:** `make verify` GREEN — backend 4879 pass (+3 new) / 1 skip (vector binary not on PATH, environmental) / 100% kernel coverage; frontend unchanged (tsc clean, vitest 1522, eslint 94 warnings / 0 errors); UI build OK. Refinement 3a+3b (prod rig, REAL HA) 5/5 PASS: 45 enabled-on / 35 disabled-off / 80 total automations (matches 009 baseline); 43 triggered series ≤ 45 enabled-on; DECISIVE — the disabled-automation entity_id set and the triggered-series entity_id set have an EMPTY intersection (zero disabled automations emit a triggered series); 35 disabled automations still carry `enabled==0`; 19 script triggered series unaffected.

**Commit:** (commit: 2446e85)

## [STAGE-005-035] UI: render update versions + release link in Updates drill

**Summary:** The HA Updates drill (`HaUpdatesDrill.tsx`) rendered only the title and entity_id; the `installed_version` / `latest_version` / `release_url` fields that STAGE-005-031 added to `HaUpdateRow` (and the `/updates` endpoint already returns) were fetched but never displayed. This stage renders them on a conditional secondary muted line, mirroring the STAGE-005-033 entities-drill two-line pattern (GAP C of the Wave I audit-gap-closure slate). Frontend-only — no backend, metric, or OpenAPI change.

**New files:**
- None.

**Modified files:**
- `apps/ui/src/routes/integrations/HaUpdatesDrill.tsx` — rewrote `renderRow` into a two-line `space-y-0.5` container. Primary line unchanged in content (title-or-`entity_id` label left, `entity_id` in the right slot). New conditional secondary line (`flex flex-wrap items-baseline gap-2 text-xs text-muted-foreground`) rendered only when version text exists OR `release_url` is non-null: a `tabular-nums` span with `{installed} → {latest}` (U+2192) — a single side shown alone when only one version is present, omitted when both null — and a `Release notes ↗` (U+2197) external link (`target="_blank" rel="noopener noreferrer" hover:text-foreground`) shown only when `release_url` is non-null.
- `apps/ui/src/routes/integrations/HaUpdatesDrill.test.tsx` — +3 tests: (1) fully-populated row renders `2.0.1 → 2.1.0` + a `Release notes` link with the correct href; (2) one-side-present row (latest only) renders `2.1.0` alone with NO arrow; (3) all-null row renders neither the arrow nor any link. Existing tests unchanged.

**Design decisions:** D-UPDATES-UI-LAYOUT = version transition + release link on a conditional secondary muted line below the title (Option A, mirroring STAGE-005-033); `entity_id` stays in the existing right slot (no unrequested relocation). Rejected Option B (version in the primary right slot) — version strings truncate badly on mobile, destroying the transition meaning. D-UPDATES-UI-PARTIAL = `installed && latest` → `{installed} → {latest}`; exactly one present → that one, no arrow (`latest ?? installed`); both null → omit the version span; entire secondary line omitted when no version text AND no `release_url`. D-UPDATES-UI-LINK-AFFORDANCE = `Release notes ↗` text+glyph (clearer + larger tap target than an icon-only link); hidden when `release_url` is null. TS narrowing via `installed && latest ? ... : (latest ?? installed ?? '')` (no stale-boolean-flag narrowing pitfall).

**Verification:** `make verify` GREEN — backend 4879 pass / 1 skip (vector binary not on PATH, environmental) / 100% kernel coverage; frontend tsc clean, vitest 1525 pass (+3 new), eslint 94 warnings / 0 errors (baseline), UI build OK. Refinement 3a (prod rig, REAL HA, no host-integration delta → no 3b): rig UI rebuilt (served bundle `index-CpyGeCWl.js` → `index-fDv32Tkm.js`); `/updates` returned 200; user reviewed the populated version-transition rows + release links on the live rig and APPROVED both Desktop and Mobile layouts.

**Commit:** (commit: c24e88c)

## [STAGE-005-036] UI: render repair description + learn-more link in Repairs drill

**Summary:** The HA Repairs drill (`HaRepairsDrill.tsx`) rendered only `issue_id` / `domain` / a severity Badge; the `description` (human-readable prose) and `learn_more_url` (doc URL) that STAGE-005-031 added to `HaRepairRow` (and the `/repairs` endpoint already returns) were fetched but never displayed. This stage renders them in a conditional secondary region, mirroring the STAGE-005-035 two-line pattern (GAP D of the Wave I audit-gap-closure slate). Frontend-only — no backend, metric, or OpenAPI change.

**New files:**
- None.

**Modified files:**
- `apps/ui/src/routes/integrations/HaRepairsDrill.tsx` — wrapped `renderRow` in a `space-y-0.5` container. Primary line unchanged in content (issue_id + domain + severity Badge). Added a conditional secondary region: `description` as a `line-clamp-2 text-xs text-muted-foreground` plain-text `<p>` (rendered only when non-null/non-whitespace; NEVER `dangerouslySetInnerHTML`), and a `Learn more ↗` (U+2197) external link (`target="_blank" rel="noopener noreferrer" hover:text-foreground`) on its OWN line as a FLAT sibling of the clamped `<p>` (so the 2-line clamp can never hide the link), rendered only when `learn_more_url` is non-null.
- `apps/ui/src/routes/integrations/HaRepairsDrill.test.tsx` — +2 tests: (1) a row with description + learn_more_url renders the description text + a `Learn more` link with the correct href; (2) an all-null row renders the primary line (issue_id + severity) with no description text and no link. Existing tests unchanged.

**Design decisions:** D-REPAIRS-UI-DESCRIPTION-RENDER = `line-clamp-2` plain-text node (two lines identify the repair while bounding row height in the compact scrolling list; `learn_more_url` carries the full story; never HTML). D-REPAIRS-UI-LAYOUT = description as a muted secondary `<p>` below the primary line, `Learn more ↗` on its OWN line below the prose (NOT inline-trailing) — rejected Option A (inline-trailing link) because a full 2-line clamp would truncate the prose tail AND the trailing link, hiding the link entirely; rejected Option C (single-line truncate) as too little prose to identify a repair and poor on Mobile. D-REPAIRS-UI-LINK-AFFORDANCE = `Learn more ↗` matching the 035 affordance. D-REPAIRS-UI-PRIVACY = render-only, no client logging (mirrors the STAGE-005-031 never-logged posture). Description/link gated independently (`description != null && description.trim() !== ''`; `learn_more_url != null`); TS narrowing via a derived `description: string | null` local (avoids the `Boolean(x?.trim())` non-narrowing pitfall).

**Verification:** `make verify` GREEN — backend 4878 pass / 2 skip / 100% kernel coverage (the 2 skips are environmental & unrelated to this frontend-only change: `test_scheduler.py:359` interpreter hash-collision, `test_vector_template.py:1051` vector binary not on PATH); frontend tsc clean, vitest 1527 pass (+2 new), eslint 94 warnings / 0 errors (baseline), UI build OK. `line-clamp-2` confirmed available (Tailwind v4.2.4, built-in). Refinement 3a (prod rig, REAL HA, no host-integration delta → no 3b): rig UI rebuilt (served bundle `index-fDv32Tkm.js` → `index-DoPoOqpe.js`); `/repairs` returned 200 (HA reported zero active repairs at the moment of the automated check — timing-dependent for a live endpoint); user reviewed the Repairs drill on the live rig and APPROVED both Desktop and Mobile layouts.

**Commit:** (commit: f7e89ea)

## [STAGE-005-037] HA entity-registry WS fetch + cache; exclude disabled/hidden (KEYSTONE)

**Summary:** No HA collector could see an entity's `disabled_by` / `hidden_by` / `entity_category` (only the WS command `config/entity_registry/list` carries them; `GET /api/states` does not), so disabled/hidden entities — which legitimately report `unavailable` — inflated the `homelab_ha_entity_available == 0` population and blocked the GAP-F availability alert (STAGE-005-039). This stage adds a kernel entity-registry cache (one WS fetch into an in-memory immutable snapshot, refreshed on a decoupled loop) and uses it to DROP disabled/hidden entities from `homelab_ha_entity_available` + `homelab_ha_entity_last_changed_seconds` and from z-score eligibility. KEYSTONE for STAGE-005-039. Backend/kernel/config only — no API model, no OpenAPI change.

**New files:**
- `apps/monitor/homelab_monitor/kernel/ha/entity_registry.py` — `HaEntityRegistryCache` (holds the ws_client + config + metrics writer + log; `snapshot()`, `async refresh()`, `start_task()`/`stop_task()` mirroring the WS client lifecycle, `_run_loop` refresh-first-then-sleep with a short initial backoff until the first populated fetch + a fail-open exception guard), `RegistrySnapshot` (frozen: `entries` + `fetched_at`; `is_populated`; `is_excluded(entity_id, cfg)` — fail-open to no-exclusion when not populated), `_RegistryWsClient` Protocol. Self-metrics: `homelab_ha_entity_registry_entries` (gauge), `homelab_ha_entity_registry_last_fetch_timestamp_seconds` (gauge), `homelab_ha_entity_registry_fetch_total{result}` (counter) — none carry an entity_id label.
- `apps/monitor/tests/test_ha_entity_registry.py` — parse-helper, config-loader, cache refresh/lifecycle, is_excluded toggle matrix, fail-open, initial-backoff-retry-until-populated, and privacy-sentinel tests.

**Modified files:**
- `apps/monitor/homelab_monitor/kernel/ha/enrichment.py` — added `RegistryEntry` (frozen: entity_id + disabled_by/hidden_by/entity_category, each `str | None`), `extract_registry` (defensive list/dict extraction mirroring `extract_issues`), `_entry_str` (isinstance-guarded reader mirroring `attr_str`), `build_registry_index`. Preserves the never-log/never-emit/never-raise purity.
- `apps/monitor/homelab_monitor/kernel/ha/websocket.py` — added `_WS_MAX_FRAME_BYTES: Final[int] = 32 MiB` and passed `max_size=_WS_MAX_FRAME_BYTES` to `connect()`. The real `config/entity_registry/list` response (~3031 entries) exceeds the websockets library's 1 MiB DEFAULT, which caused a 1009 "message too big" close that prevented the registry from EVER populating; a finite 32 MiB cap fixes it while keeping an OOM guard (not `max_size=None`).
- `apps/monitor/homelab_monitor/kernel/config.py` — added `HaRegistryConfig` + `load_ha_registry_config()` (env `HOMELAB_MONITOR_HA_REGISTRY_*`; `enabled` kill-switch, `exclude_disabled`/`exclude_hidden` default true, `exclude_categories` default empty, `refresh_seconds` clamp ≥60) + `__all__`.
- `apps/monitor/homelab_monitor/kernel/plugins/context.py` — added optional `ha_registry: HaEntityRegistryCache | None = None` field (TYPE_CHECKING import).
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — construct the cache after the WS client (config-gated `start_task()`), set `app.state.ha_entity_registry`, wire it through `ctx_factory`, and `stop_task()` it before the WS client in shutdown.
- `apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_entity_available.py` — DROP registry-excluded entities (the `continue` precedes both the available AND last_changed appends → equal cardinality); fail-open when `ctx.ha_registry` is None.
- `apps/monitor/homelab_monitor/plugins/collectors/integrations/homeassistant/ha_anomaly_zscore.py` — `_is_eligible` precedence: `extra_entity_ids` force-include FIRST → registry-exclude (when populated) → `excluded_entity_ids` → measurement/device-class heuristic.
- `apps/monitor/tests/test_ha_anomaly_zscore_collector.py`, `test_ha_entity_available_collector.py` — registry-exclusion + fail-open + override tests. `apps/monitor/tests/test_ha_websocket.py` — assert the `max_size` kwarg.

**Design decisions:** D-REGISTRY-CACHE-HOME = Option A, a self-fetching kernel cache on `app.state` with its own asyncio loop (mirrors the WS client's start/stop-task lifecycle; single shared fetch; immutable snapshot = loop-safe). D-REGISTRY-EXCLUDE-VS-LABEL = DROP, not label — the downstream STAGE-005-039 `homelab_ha_entity_available == 0` rule is UNFILTERED, so per the locked project lesson the collector's emit condition IS the filter; DROP keeps 039's rule maximally simple. D-REGISTRY-EXCLUDE-CATEGORIES = only disabled/hidden by default (diagnostic entities are often legitimately monitored). D-REGISTRY-FAIL-MODE = fail-OPEN gated on `snapshot.is_populated`; a failed/empty registry never blinds the metric, and the failure is observable via the self-metrics. Refinement added a startup-race initial-backoff and the WS `max_size` fix (both real-HA bugs).

**Verification:** `make verify` GREEN — backend 4908 pass (+30 new) / 1-2 skip (environmental: vector binary not on PATH; occasional interpreter hash-collision) / **100% kernel coverage**; ruff/black/pyright clean; frontend unchanged (tsc clean, vitest 1527, eslint 94 warnings / 0 errors); UI build OK. No OpenAPI change. Real-HA 3a+3b (prod rig, live HA): registry populated (`homelab_ha_entity_registry_entries`=3031, `fetch_total{result="ok"}`≥1, WS stable through the fetch), exclusion ACTIVE — `count(homelab_ha_entity_available)` stepped 1138→1116 (disabled/hidden /api/states entities DROPPED), `count(==0)` 202→198, `available`==`last_changed` cardinality (DROP confirmed), step-down temporally aligned with the registry populating, authenticated `/entities?filter=unavailable` total=198 matching VM.

**Commit:** (commit: 8e02a41)

## [STAGE-005-038] Script-cadence Grafana panel (parity with automation cadence) — surface split

**Summary:** `homelab_ha_script_last_triggered_seconds` was emitted by the cadence collector but had no Grafana panel (automation cadence had idle/disabled/oldest panels; scripts had nothing). This stage adds the missing script-cadence panels to the Home Assistant dashboard, achieving parity with the automation cadence panels. Grafana/config-only — no backend, no metric, no API, no OpenAPI, no frontend change. The Design phase resolved D-CADENCE-SURFACE-SPLIT by SPLITTING the optional in-app cadence API + Status-tab widget into a new card STAGE-005-042 (epic denominator 41→42); this stage ships the Grafana panel only.

**Modified files:**
- `deploy/grafana/dashboards/home-assistant.json` — renamed the cadence row id=14 title "Automation Cadence" → "Automation & Script Cadence"; added panel id=21 "Idle Scripts (>24h)" (`stat`, `count(homelab_ha_script_last_triggered_seconds > 86400)`, unit `short`, gridPos {h:4,w:6,x:0,y:40}) mirroring automation panel id=15; added panel id=22 "Oldest-Triggered Scripts" (`timeseries`, `topk(10, homelab_ha_script_last_triggered_seconds)`, unit `s`, legend `{{entity_id}}`, gridPos {h:8,w:12,x:6,y:40}) mirroring automation panel id=17 (the script timeseries sits at x=6, filling the slot the automation row gives its "Disabled Automations" panel — scripts have no enabled flag, so no disabled-script panel); shifted the downstream "Anomalies & Collector Health" row (id=18) gridPos.y 40→48 and its children id=19 "Top Anomaly z-scores" + id=20 "Collector Run Health" 41→49 (+8 = the new row's height) to keep the layout contiguous and non-overlapping.

**New files:**
- `epics/EPIC-005-home-assistant/STAGE-005-042.md` — split-off follow-on card for the in-app cadence API + Status-tab widget (runs after 041; the in-app widget is outside 041's Grafana-dashboard review scope, so no forward-dependency violation).

**Design decisions:** D-CADENCE-SURFACE-SPLIT = SPLIT — 038 ships the script-cadence Grafana panel only (config; real-HA 3a+3b; no user prompt); the cadence detail API + Status-tab widget move to new card STAGE-005-042. Rationale: the API+UI is ~8 files / ~600 LOC + an OpenAPI schema-cascade + its own dual-signoff refinement surface — session-sized on its own; bundling would let optional risk block the trivial parity panel. Ordering: 042 runs AFTER 041 with no forward-dependency violation because 041 reviews the GRAFANA dashboard surface only (its deliverables are Grafana JSON edits + coverage reports), and the in-app widget is not a Grafana panel — the Grafana script-cadence panel 041 does review is built here in 038, before 041. D-CADENCE-API-SOURCE = VM per-series (deferred to 042; consistent with locked D-DETAIL-CONSUMES-VM). D-CADENCE-IDLE-THRESHOLD = 24h/86400s (matches the automation Grafana panels). D-CADENCE-SCRIPT-DISABLED = confirmed: scripts have no enabled flag, so script cadence is idle-age-only (no disabled-script panel).

**Verification:** `make verify` GREEN — backend 4908 pass / 2 skip (environmental: vector binary not on PATH; interpreter hash-collision) / 100% kernel coverage; ruff/black/pyright clean; frontend unchanged (tsc clean, vitest 1527, eslint 94 warnings / 0 errors); UI build OK. No OpenAPI change (config-only). Dashboard JSON validated with `jq` (22 panels, was 20; no duplicate ids; valid structure). Opus code review APPROVED with zero findings (mirror-fidelity field-by-field vs id=15/id=17, gridPos shift complete with no overlaps, PromQL+units correct, split-decision artifacts consistent). Real-HA 3a+3b (prod rig, live HA, Grafana bind-mounted dashboards dir re-provisioned every 30s — no rebuild): script metric live (`count(homelab_ha_script_last_triggered_seconds)`=19); "Idle Scripts (>24h)" expr = 15; "Oldest-Triggered Scripts" `topk(10,…)` returns 10 series with the `{{entity_id}}` legend populated (e.g. `script.enable_doorbell_motion` ≈ 414 days) and real age values; Grafana loaded the updated dashboard (uid=home-assistant) with no provisioning errors.

**Commit:** (commit: 73184af)

## [STAGE-005-039] Activate availability/cadence vmalert rules — measured, kept all four OPT-IN

**Summary:** Four HA vmalert exemplar rules (HaEntityUnavailable, HaEntityStale, HaAutomationIdle, HaAutomationDisabled) were demoted to commented OPT-IN in STAGE-005-015 on a coarse "~924 of 1906 / ~50%" estimate. STAGE-005-037 (registry DROP of disabled/hidden entities) and STAGE-005-034 (disabled automations dropped from the idle metric) cleaned the underlying series, so this stage re-measured the real firing counts on the prod rig and decided, PER RULE, whether to activate. Outcome: ACTIVATE ZERO — all four stay opt-in. The stage's value is converting four stale estimates into measured fact and documenting WHY each stays opt-in (the "a noisy rule is a failed rule" discipline). Config/comment + test-fixture only; no rule activated, no `groups:` change.

**Modified files:**
- `deploy/vmalert/metrics/home-assistant.yaml` — COMMENT-ONLY edits (git diff confirms zero non-comment changes): updated the header deferral comment to post-037 reality (total ~1844→1117, unavailable ~924→197 / 17.6%; remainder still chronic integration/cloud noise; `for:` cannot reduce chronic state) + a D-ALERT-FILTER-FORM resolution note; updated the four exemplar rationale comments to cite the measured counts (HaEntityUnavailable 197/1117; HaEntityStale 865@1h / 618@24h / 525@7d; HaAutomationIdle 20@24h / 18@7d / 9@30d of 43 enabled; HaAutomationDisabled 35/80). No rule body uncommented; the five active HA rules (HaUnreachable, HaConfigEntryError, HaBatteryCritical, HaBatteryLow, HaUpdatesPending) are untouched.
- `deploy/vmalert/metrics/__tests__/home-assistant.tests.yaml` — appended four NO-FIRE regression tests (one per opt-in rule), each feeding input that WOULD trip a blanket form (entity_available=0; last_changed=172800/48h; last_triggered=2592000/30d; automation_enabled=0) and asserting `exp_alerts: []`. They pass because the rules are not loaded; if a future session uncomments a rule without re-grounding the noise analysis, that rule's test flips to firing → FAILS. (vmalert `*.tests.yaml` fixtures are not CI-wired — forward-compatible static fixtures, validated manually via docker vmalert.)

**Design decisions:** D-ALERT-ACTIVATE-UNAVAILABLE = KEEP-OPT-IN (197 chronic integration/cloud instances; a `for:` filters transient flaps but cannot reduce CHRONIC always-unavailable state, so 197 remains a mass-fire = failed rule). D-ALERT-STALE-BLANKET = KEEP-OPT-IN (no `device_class` label for a blanket threshold; 525–865 firing confirms a global rule is unworkable; 037 is orthogonal to the device-class problem). D-ALERT-AUTOMATION-IDLE = KEEP-OPT-IN (even the most favorable threshold 30d→9 cannot separate truly-dead from legitimately-seasonal automations; idle-ness is a per-automation user judgment → 005-005 user-rules). D-ALERT-AUTOMATION-DISABLED = KEEP-OPT-IN (disabling is intentional; 35/80 = pure noise as a default). D-ALERT-FILTER-FORM = BARE EXPR resolved by 037's DROP choice (active-form expr is `homelab_ha_entity_available == 0`, no `{registry_visible="1"}` filter; a future LABEL flip would require adding it). D-ALERT-FORDURATION = N/A (no rule activated; `for:` is near-useless for monotonic chronic state). Zero-activation is a complete, legitimate outcome — the reshaped deliverables (comment-only edits + no-fire fixture instead of move-to-groups + fire-tests) match the measured reality and are NOT a deferral (the rules' tracked state already IS opt-in).

**Verification:** `make verify` GREEN — backend 4908 pass / 2 skip (environmental: vector binary not on PATH; interpreter hash-collision) / 100% kernel coverage; pyright 0 errors; vitest 1527 pass; eslint 94 warnings / 0 errors; tsc + UI build clean. Both YAML files parse OK (validated via the project uv env). `git diff` confirms the rules file change is comment-only (zero non-comment diff lines). Opus code review APPROVED with zero findings (comment-only invariant confirmed via grep; no rule uncommented; measured counts internally consistent in both header + per-rule blocks; four NO-FIRE fixtures well-formed/name-matched/documented; existing tests intact; decision reasoning consistent). Real-HA 3a+3b (prod rig; rules dir bind-mounted into `homelab-vmalert-metrics`, `-configCheckInterval=30s` auto-reload — no rebuild): vmalert reloaded cleanly (no parse error in last-10m logs); the four candidate rules are ABSENT from `/api/v1/rules`; the five expected HA rules are present; none of the four candidates appear in `/api/v1/alerts` (firing/pending).

**Commit:** (commit: d91eef1)
