2026-07-02 b16fbe3 EPIC-009 / STAGE-009-010: Auto-fix UI — Runbooks catalog + pending approvals

Landed the FIRST auto-fix UI feature stage — the `/runbooks` top-level route replacing the "Coming soon" sidebar placeholder. Two stacked surfaces: (A) **Runbooks catalog** — one card per registered runbook with name (folder basename), config-folder path + copy button, alert-match-pattern chips, risk badge (safe / risky) + dry-run-required sub-badge, rate-limit + cooldown summary, live Enabled + Auto-trigger toggles (Radix-styled ToggleSwitch built inline — no shadcn Switch dependency added), em-dash placeholders for Last run + Success rate with info-icon tooltips pointing to STAGE-009-011 for the runs aggregation deliverable; and (B) **Pending approvals panel** — rows from `GET /api/autofix/approvals?status_filter=pending` with drift badges (server-computed), "View & Approve" opens Radix Dialog rendering full `plan_text` in a scrollable `<pre>` with copy-plan button, Reject (non-destructive, no confirm), and Approve (typed-phrase `"approve"` via ConfirmPhraseDialog reused from STAGE-009-006/007). Amber kill-switch banner atop the page when `useAutofixKillSwitch().data?.enabled === false` (reused from STAGE-009-007's `autofixSettings.ts`), which also disables Enabled + Auto-trigger toggles on every card and the Approve button on every pending approval. Friendly-error mapping for the 409 codes: `runbook_changed_since_plan`, `runbook_missing`, `kill_switch`, `approval_not_pending`, `rate_limit`, `cooldown`, `already_running`, `allow_list`, `ambiguous_match`, `claim_error`.

**Deferrals (all tracked with owner stages per no-silent-deferral):**
- Manual "Run fix" trigger endpoint + per-card Run-fix button → STAGE-009-010A (new). Bundled with backend defense-in-depth: reject `PATCH auto_trigger=true` on `risky` runbooks (today's safety net is exec-time only via `dry_run_required` routing to approval).
- Session-PIN confirm-on-destructive → STAGE-009-010B (new; STAGE-009-006 regression note).
- Last-run + success-rate + run-count on catalog cards → extended STAGE-009-011 card with new deliverable §3.5 "Runs aggregation endpoint".
- General-purpose transcript viewer for all runs → already in STAGE-009-011 card §2.

**Refinement discoveries + fixes (STAGE-009-004 gaps surfaced by 010's first-consumer role):**
- `deploy/compose/docker-compose.yml`: added `${HM_RUNBOOKS_SRC:-../../runbooks}:/runbooks:ro` bind-mount for the monitor service. The backend defaults `HOMELAB_MONITOR_RUNBOOKS_DIR` to `/runbooks` (in-container) per STAGE-009-004, but no compose mount was ever added — the `POST /api/runbooks/refresh` endpoint returned "runbooks root /runbooks is not a directory" until this fix.
- `apps/ui/src/components/SidebarNav.tsx`: added `overflow-y-auto` to the nav root — the shared sidebar (both mobile drawer and short-viewport desktop) clipped nav items at the bottom of tall lists.
- `apps/ui/vitest.config.ts`: added `src/api/runbooks.ts` to coverage exclude (entirely mocked out by tests via `vi.mock('@/api/runbooks')`; matches the existing `src/api/crons.ts` exclusion rationale).

**Non-negotiables preserved (code-reviewer pass verified all 7 PASS with explicit justification):**
1. **Trigger** — PASS. UI ships no new trigger surface; 010 deliberately defers manual "Run fix" to 010A. Approving a pending approval routes through the existing STAGE-009-006 backend gate ordering.
2. **Scope** — PASS. Read-only wrt folder identity; no rename/edit/move affordances; refresh rescans on-disk registry.
3. **Identity** — PASS. Zero exec-identity surface in this stage.
4. **Audit** — PASS. No delete/mutate affordance for `runbook_runs` or approvals audit rows; `plan_text` rendered as textContent (`<pre>`).
5. **Dry-run + approval** — PASS. ApprovalPlanDialog renders `plan_text` + `drift_detected`; approve sends literal `"approve"` string via ConfirmPhraseDialog; Approve disabled when kill-switch off; no auto-approval / bypass paths.
6. **Rate-limit + cooldown** — PASS. Displayed informationally; 409 codes surfaced as user-visible errors; no mutation surface.
7. **Kill switch** — PASS. Reuses `useAutofixKillSwitch()` (no fork); when off, disables Enabled + Auto-trigger toggles + Approve button; banner links to `/settings/autofix`; no kill-switch mutation surface exposed on `/runbooks`.

**Files created:** `apps/ui/src/api/runbooks.ts`; `apps/ui/src/routes/runbooks/RunbooksPage.tsx`; `apps/ui/src/routes/runbooks/RunbookCard.tsx`; `apps/ui/src/routes/runbooks/PendingApprovalsPanel.tsx`; `apps/ui/src/routes/runbooks/ApprovalPlanDialog.tsx`; and their `__tests__/` siblings (RunbooksPage.test.tsx, RunbookCard.test.tsx, PendingApprovalsPanel.test.tsx, ApprovalPlanDialog.test.tsx — 15+ test cases covering catalog render, toggle mutations, kill-switch integration, clipboard branches, drift badges, plan viewer, approve typed-phrase flow, reject, and every documented 409 error path). New tracking cards `STAGE-009-010A.md` + `STAGE-009-010B.md`.

**Files edited:** `apps/ui/src/components/SidebarNav.tsx` (enabled Runbooks nav + added `overflow-y-auto`); `apps/ui/src/router.tsx` (registered `runbooksRoute` under `protectedLayoutRoute`); `apps/ui/vitest.config.ts` (coverage exclude); `deploy/compose/docker-compose.yml` (runbooks bind-mount); tracking: `STAGE-009-010.md`, `STAGE-009-011.md`, `EPIC-009.md` (top-line 9/17), `regression.md`.

**Verification:** `make verify` GREEN — backend 7019 passed + 1 benign skip (test_vector_template) + 100.00% coverage; UI 190 test files / 1925 tests passed; Vite build ✓ in 905ms.

---

2026-07-02 65b095e EPIC-009 / STAGE-009-009: Claude→user improvement-feedback channel

Landed the FEEDBACK CHANNEL (LOCKED Decision 3): the fixer emits structured feedback via a `<pid>.feedback.json` sentinel in its transcript directory; the orchestrator parses it INSIDE `_transcript_lock` (race-safe, mirrors `_resolve_transcript`) and persists items into a NEW sibling table `runbook_run_feedback`. Purely additive — `runbook_runs` schema untouched, `audit_log` writes for `autofix.ran` / `autofix.exec_error` / `autofix.dry_run_stored` unchanged. UI + list endpoint deferred to STAGE-009-011 (tracked existing stage: its title is "Auto-fix history UI — filterable table + transcript viewer + feedback display" and Deliverable #3 explicitly owns the feedback display).

Non-negotiables preserved (code-reviewer pass 2 verified all 7 PASS):
- #1 (trigger allow-list): feedback surface is read-only telemetry, no new auto-trigger source.
- #2 (scoped capability + runbook folder): unchanged; scan is confined to the fixer's authorized transcript mount.
- #3 (identity `homelab-fixer`): unchanged; feedback file is written by the fixer inside its container, read by the monitor from the shared transcript mount.
- #4 (audit): primary audit trail intact — `_persist_outcome` ordering places `_insert_outcome_conn` BEFORE `_process_feedback` so a swallowed feedback exception cannot poison the primary audit inserts. `runbook_run_feedback` is a SIBLING table, `autofix.feedback_parse_error` is additive telemetry.
- #5 (dry-run + approval): dry-run path also processes feedback (feedback exists in the exec log); approval flow unchanged.
- #6 (rate-limit + cooldown): gate order untouched; feedback insertion is post-exec, cannot bypass or reset gates.
- #7 (kill switch): `kill_inflight` unchanged; `_process_feedback` runs from post-exec completion txns, cannot resurrect a killed run.

Design decisions (autonomous loop, recorded on the stage card):
- A1 (contract): sentinel `<pid>.feedback.json` file in the transcript dir. Rejected A2 (stdout marker — real claude spawns a Node subprocess whose stdout hygiene isn't under our control) and A3 (JSON-lines in transcript — overloads the audit artifact's semantic).
- B (schema): TEXT PK (UUIDv7), TEXT FK to `runbook_runs.id`, NO ondelete (matches 0045/0047 convention — bare FK). `FeedbackKind` StrEnum with 7 values (`missing_capability`, `config_change`, `runbook_gap`, `blocked`, `worked_around`, `other`, `parse_error`); loose JSON `structured_hint` (Pydantic dict[str, object]); `suggestion_text` truncated at 4096 chars with `\n...[truncated]` suffix.
- C3 (malformed): dual observability — one `parse_error` row PLUS one `autofix.feedback_parse_error` audit event referencing the run + truncated raw content. Rejected C1 (row only, weakens audit) and C2 (audit only, no user visibility).
- D1 (UI split): 009 backend-only. STAGE-009-011 owns the feedback display (its Deliverable #3). NOT a silent deferral — 011 is a tracked stage that already exists.

New public surfaces:
- `AutoFixOrchestrator.__init__(..., feedback_repo: RunbookRunFeedbackRepository | None = None, feedback_id_provider: Callable[[], str] = uuid7)`. Production wiring in `kernel/api/lifespan.py` passes `feedback_repo=RunbookRunFeedbackRepository(repo)`. Default `None` preserves backward compat.
- `_exec_claude` return tuple widened from 4-tuple to 5-tuple: adds `feedback_items: list[ParsedFeedbackItem] | None` as the 5th element. All callers updated (real path in `_claim_and_exec`, dry path in `_claim_and_store_dry`, grant-error early return returns `None` for the 5th slot).
- `FeedbackKind`, `RunbookRunFeedback` (frozen dataclass), `FeedbackParseError`, `ParsedFeedbackItem` in `kernel/autofix/types.py` + `kernel/autofix/feedback_parser.py`.
- New audit `what` value: `autofix.feedback_parse_error`.
- New table `runbook_run_feedback` + index `ix_runbook_run_feedback_runbook_run_id`.

Code-reviewer findings (all resolved):
- CRITICAL: original spec placed the feedback scan in `_process_feedback` (post-lock), creating a cross-run misattribution race identical to the one `_resolve_transcript` already avoids by running inside `_transcript_lock`. Fixed by hoisting the scan+parse INSIDE `_exec_claude` and returning `feedback_items` through the tuple. `_process_feedback` no longer scans — it just persists.
- IMPORTANT (order): moved `_insert_outcome_conn` BEFORE `_process_feedback` in `_persist_outcome` so that a `RuntimeError`/DB failure in feedback processing (swallowed by the bare except) cannot leave the SQLAlchemy async connection in `PendingRollbackError` state and revert the completion + `autofix.ran` audit.
- IMPORTANT (docstring): corrected `scan_transcript_dir_for_feedback` docstring to name the caller-must-hold-lock invariant (previously claimed the lock was implicit via PID prefix, which was wrong once the scan moved).
- IMPORTANT (migration test): `test_migration_0049_round_trip` now seeds parent rows and inserts a real `runbook_run_feedback` row, verifying all 6 columns roundtrip.
- NICE (errored-path swallow): added `test_feedback_repo_insert_exception_on_errored_path_swallowed_parent_txn_lands` to mirror the success-path swallow test, proving the errored path's txn also survives feedback insert exceptions.

Testing:
- 27 parser tests (all valid/missing/unknown/malformed/truncation/scan branches).
- 9 repo roundtrip tests (JSON, FK, kind persistence).
- 11 orchestrator wiring tests (end-to-end via `orch.handle_alert(alert)` with `_FakeDockerClient.exec_capture` writing real sentinel files; all 3 exec paths; exception swallow on both success + errored; backward compat with `feedback_repo=None`; custom id provider).
- 1 migration round-trip test.
- Full backend gate: 7018+ passed, 1 skipped (vector binary not on PATH), Total coverage 100.00%.

Also:
- `deploy/compose/fixer-runner/test/fake-claude` extended with `FAKE_CLAUDE_FEEDBACK` (writes payload verbatim) and `FAKE_CLAUDE_FEEDBACK_MALFORMED=1` (writes intentionally invalid JSON). Write occurs BEFORE the plan-mode `exit 0` so dry path is exercised.
- `apps/monitor/tests/test_db_migrations.py` `EXPECTED_TABLES` updated to include `runbook_run_feedback`.
- `apps/monitor/tests/test_autofix_orchestrator.py` — 14 pre-existing `_exec_claude(...)` unpack sites updated to receive the widened 5-tuple.

Deferrals (tracked stages, no silent drops):
- UI + `/api/autofix/runs` list endpoint → STAGE-009-011 (its Deliverable #3 explicitly reads "Show the runbook_run_feedback items (STAGE-009-009) for the selected run").

---

2026-07-02 1e77a6f EPIC-009 / STAGE-009-008: Per-runbook scoped-capability granting + fixer-runner egress control

Landed the POLICY LAYER for per-runbook scoped capabilities. `AutoFixOrchestrator._resolve_grants` re-reads `runbook.yaml` fresh at exec-start (file-authoritative — Decision 1B) and produces a `ResolvedGrants` envelope carrying declared docker/ssh/egress caps. On grant success: `autofix.grant_resolved` audit + (for real runs with declared egress) an `autofix.egress_unenforced` warning naming STAGE-009-015 as the future owner. On grant failure: `autofix.grant_failed` audit + early-return with `errored=True`; `_current_run` is never published — kill-switch invariants preserved.

Non-negotiables preserved:
- #2 (scoped capabilities): grant envelope exactly matches declared YAML; fail-closed on missing/malformed/schema-invalid YAML + unknown SSH `target_id`.
- #3 (identity `homelab-fixer`): no docker socket, no bind-mount, no cap_add added — fixer holds ZERO docker access this stage (Decision 2E-lite).
- #4 (audit): every grant translation writes a structured audit row.
- #7 (kill switch): STAGE-009-007's `_current_run` publish/clear frame and `_kill_lock` semantics untouched.

Deferrals (tracked stages):
- STAGE-009-014 — Docker intent gateway: schema, validator, executor (docker-capability enforcement mechanism).
- STAGE-009-015 — fixer-runner egress enforcement (HTTPS proxy sidecar + host iptables/nftables).

New public surfaces:
- `AutoFixOrchestrator.__init__(..., ssh_target_ids_provider: Callable[[], frozenset[str]] = frozenset)` — production wiring in `kernel/api/lifespan.py` passes `lambda: frozenset(load_ssh_target_configs().keys())`.
- `ResolvedGrants` and `GrantResolutionError` in `kernel/autofix/types.py`.
- New audit `what` values: `autofix.grant_resolved`, `autofix.grant_failed`, `autofix.egress_unenforced`.

Also:
- Converted `kernel/autofix/__init__.py` to lazy `__getattr__` for `AutoFixOrchestrator` to remove a cold-REPL circular-import trap discovered during Refinement.
- `_exec_claude` docstring updated to reflect grant-resolution audit writes.

Instance-A validation (2026-07-02): prod monitor rebuilt+redeployed; `_resolve_grants` exercised in-container against live SSH target registry (`udm`, `synology`). Happy path, missing-YAML, and unknown-SSH-target scenarios all validated end-to-end.

Code review (Opus): 2 IMPORTANT + 7 MINOR findings on first pass, all implemented. Second review pass: APPROVED, no new findings. All 7 auto-fix non-negotiables PASS.

Verification: `make verify` GREEN. Total coverage: 100.00%. 6971 passed, 1 expected skip (vector binary not on PATH). Frontend (typecheck, vitest 1896/1896, build) green.

EPIC-009 progress: 8/15 stages complete (STAGE-009-001..008; STAGE-009-014/015 added during 008 Design as owned deferrals).

---

2026-07-02 97c4b98 EPIC-009 / STAGE-009-007: Kill switch — pre-run gate + mid-run kill + dashboard control

Implements non-negotiable #7 (single dashboard control disables all auto-fix immediately). Layered onto the STAGE-009-005 orchestrator + STAGE-009-006 approval path.

**Backend**:
- Migration 0048: adds `killed_at TEXT NULL` to `runbook_runs`.
- `DockerSocketClient.kill_container(container_id, *, signal="SIGKILL", timeout_seconds=None)`: new UDS API method modeled on `stop_container`. 204 = killed, 409 = idempotent (already stopped, warning logged). Panic-path callers pass a short 5s timeout.
- `AutoFixOrchestrator.kill_inflight(*, reason, killed_by, ip=None) -> KillResult`: push-triggered from the toggle endpoint. Snapshots `_current_run` under new `_kill_lock`; writes pre-kill `autofix.kill_attempted` audit in a fresh txn; calls `docker.kill_container(..., signal="SIGKILL", timeout_seconds=5.0)`; on `DockerSocketError` audits `autofix.kill_failed` and re-raises. On success, re-verifies `_current_run` still matches the snapshot — if a concurrent `execute_approved` republished with a DIFFERENT `run_id`, audits `autofix.kill_wrong_run_race` and returns `killed=False, error="wrong_run_race"` without stamping. Otherwise stamps `runbook_runs.killed_at` + writes `autofix.killed` audit in ONE txn (SQL gates on `killed_at IS NULL` for idempotency). Then polls up to `_KILL_UNWIND_DEADLINE_SECONDS=5.0` for `_current_run` to clear; returns `unwind_warning="unwind_deadline_exceeded"` on timeout.
- `_exec_claude` publishes `_current_run` before `exec_capture` and clears in a finally block regardless of success/failure/timeout. Dry runs are NOT tracked.
- `_check_operational_gates` step-1 kill-switch check remains FIRST (unchanged from STAGE-009-005). Gate ordering preserved: kill_switch → allow_list → rate_limit → cooldown.
- New router `apps/monitor/homelab_monitor/kernel/api/routers/autofix_settings.py`: `GET /api/settings/autofix/kill-switch` returns `KillSwitchState(enabled, updated_at)` (single SELECT); `POST` accepts `KillSwitchToggleRequest(enabled, confirm_phrase)`. Noop POST (state==request) returns 200 with no audit (idempotent "current state alias"). Real transitions require case-insensitive confirm phrase: `"disable auto-fix"` (on→off) or `"enable auto-fix"` (off→on). Wrong/missing → 400. Toggle writes `autofix.kill_switch_toggled` audit. On on→off, invokes `orchestrator.kill_inflight(...)`; on `DockerSocketError` → 502. Response includes `killed_inflight_run_id` and `unwind_warning`.

**Frontend**:
- New route `/settings/autofix` (`SettingsAutofixPage.tsx`) with a sub-nav pattern in `SettingsLayout` (Logs | Auto-fix tabs using tanstack-router `Link` + `activeProps`).
- Card layout with Enabled/Disabled status label + destructive-styled toggle button. Reuses existing `ConfirmPhraseDialog` (case-insensitive match). Success responses render `unwind_warning` banner + `killed_inflight_run_id` info line.
- New `apps/ui/src/api/autofixSettings.ts`: TanStack Query `useAutofixKillSwitch` (query) + `useToggleAutofixKillSwitch` (mutation).
- `ConfirmPhraseDialog.tsx` widened `errorMessage?: string` to `errorMessage?: string | undefined` for `exactOptionalPropertyTypes: true` compatibility.

**Refinement** (host-integration + user viewport sign-off):
- Prod rebuild + redeploy on instance A (`homelab-monitor` on `192.168.2.148:29090`) — migration 0048 applied at startup; API endpoint registered; `runbook_runs.killed_at` column present.
- Live prod: `DockerSocketClient` UDS successfully inspects the running `homelab-fixer-runner` container.
- Mid-run kill LIVE-proven via host-run of `test_docker_kill_terminates_inflight_exec` — real `docker kill` fires against a fake-claude-configured fixer-runner container (with `FAKE_CLAUDE_SLEEP=600`); in-flight `docker exec` unwinds; 7.29s PASS.
- User viewport sign-off (Desktop + Mobile) APPROVED on prod. User exercised on→off and off→on transitions via the ConfirmPhraseDialog.

**Verification**: `make verify` GREEN — ruff + format + `pyright --strict` (0/0/0) + full backend pytest (6957 passed, 1 skipped [vector binary not on PATH], **100.00% coverage** across 27844 stmts + 6250 branches) + UI generate-types + lint + format-check + tsc + vitest (1896 tests passed) + vite build.

**Non-negotiables**: all 7 verified PASS by code-reviewer (Opus) with explicit per-non-negotiable sign-off. #7 (kill switch — owner) proven at unit-branch + host-integration levels. #4 (audit) — every toggle transition + every kill outcome writes an `audit_log` row (`autofix.kill_switch_toggled`, `autofix.kill_attempted`, `autofix.killed`, `autofix.kill_failed`, `autofix.kill_no_inflight`, `autofix.kill_wrong_run_race`). #1/#2/#3/#5/#6 regression checks PASS.

**Code review findings addressed** (all 3 Important + 6 Minor, per project workflow):
- Important #1: wrong-run race attribution — re-verify `_current_run` under lock after docker kill; audit `autofix.kill_wrong_run_race` and skip stamp on mismatch.
- Important #2: noop confirm-phrase bypass — dropped `autofix.kill_switch_noop` audit; noop is now audit-free (idempotent alias).
- Important #3: pre-kill audit — added `autofix.kill_attempted` in fresh txn BEFORE docker kill fires.
- Minor: `_UPDATE_KILLED_SQL` idempotency gate; `_read_state` single SELECT; `kill_container` 5s panic timeout; top-level `text` import; test renames + additions.

**Scope guards**:
- Sidebar top-level "Auto-fix" nav entry NOT added — owned by STAGE-009-010 (fuller Auto-fix Runbooks screen).
- Killed-row display in runbook_runs history table — owned by STAGE-009-010.
- Rate-limit-triggered auto-kill — out of scope; not tracked as EPIC-009 work.
- Role-gating on the toggle — homelab is single-user; intentional accept.

---

2026-07-02 7448057 EPIC-009 / STAGE-009-006: Dry-run mode + approval flow

**Implemented non-negotiable #5 (dry-run + explicit approval) on top of the STAGE-009-005 orchestrator.**

- Runbook with `dry_run_required=True` now triggers a PLAN-ONLY claude invocation (`claude -p <path> --permission-mode plan`, without `--dangerously-skip-permissions`); the plan is stored as a `runbook_runs` row with `mode='dry_run'`, and a PENDING `runbook_run_approvals` row is created in the same transaction. The orchestrator halts — no real exec fires.
- New API surface (auth via session, CSRF on mutating routes): `GET /api/autofix/approvals?status_filter=pending|approved|rejected`, `GET /api/autofix/approvals/{id}/plan`, `POST /api/autofix/approvals/{id}/approve` (typed confirm_phrase = "approve" required, byte-exact), `POST /api/autofix/approvals/{id}/reject`. Approve calls `AutoFixOrchestrator.execute_approved`, which re-validates operational gates (kill switch / rate limit / cooldown), re-validates drift (`content_hash != pinned_runbook_hash`), then reuses the same `_claim_and_exec` path as safe-runbook real runs.
- SQL-guarded race protection: `UPDATE runbook_run_approvals ... WHERE id=:id AND status='pending'` — concurrent approves are safely serialized (one wins, others receive `APPROVAL_NOT_PENDING`); on claim-denial post-approval, the approval reverts to `pending` for retry via `revert_to_pending_conn` with an `autofix.approval_reverted` audit.
- Drift split: `RUNBOOK_CHANGED` (content_hash mismatch) vs `RUNBOOK_MISSING` (runbook deleted) as distinct denial reasons + audit shapes; both surface as 409 with distinct API error codes.
- `autofix.ran` audit now records `approving_principal` when a real run was triggered via an approval, preserving the forensic chain from `autofix.approved` to `autofix.ran`.
- New migration 0047 adds `runbook_run_approvals` (id, dry_run_id, runbook_id, alert_id, pinned_runbook_hash, status, approved_by, decided_at, real_run_id, created_at) + indexes on status/dry_run_id/runbook_id.
- Fake claude script extended: `--permission-mode plan` argv → writes `.plan` marker + emits a recorded plan transcript, does NOT simulate execution. Two e2e integration tests (dry→approval→real; drift-invalidation) prove the safety chain against a real fixer-runner container.
- Backend gate: 6919 passed / 1 baseline skipped / Total coverage 100.00%. Full `make verify` (backend + UI: 1885 tests) green.
- Non-negotiable #5 formally verified: no code path lets a `dry_run_required=True` runbook produce a `--dangerously-skip-permissions` real exec without going through `execute_approved`, drift re-check, and operational-gate re-check.

**Deferrals (tracked, not silent):**
- Markdown/whole-folder drift detection → STAGE-009-012 (006's drift check is CONFIG-hash-only).
- Session-PIN confirm-on-destructive → STAGE-009-010/011 (006 ships typed `confirm_phrase` only).
- Approval UI (list pending, view plan, approve/reject buttons) → STAGE-009-010/011 (006 is API-only).
