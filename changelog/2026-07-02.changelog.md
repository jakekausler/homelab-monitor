2026-07-02 97c4b98 EPIC-009 / STAGE-009-007: Kill switch — pre-run gate + mid-run kill + dashboard control

Implements non-negotiable #7 (single dashboard control disables all auto-fix immediately). Layered onto the STAGE-009-005 orchestrator + STAGE-009-006 approval path.

**Backend**:
- Migration 0048: adds `killed_at TEXT NULL` to `runbook_runs`.
- `DockerSocketClient.kill_container(container_id, *, signal="SIGKILL", timeout_seconds=None)`: new UDS API method modeled on `stop_container`. 204 = killed, 409 = idempotent (already stopped, warning logged). Panic-path callers pass a short 5s timeout.
- `AutoFixOrchestrator.kill_inflight(*, reason, killed_by, ip=None) -> KillResult`: push-triggered from the toggle endpoint. Snapshots `_current_run` under new `_kill_lock`; writes pre-kill `autofix.kill_attempted` audit in a fresh txn; calls `docker.kill_container(..., signal="SIGKILL", timeout_seconds=5.0)`; on `DockerSocketError` audits `autofix.kill_failed` and re-raises. On success, re-verifies `_current_run` still matches the snapshot — if a concurrent `execute_approved` republished with a DIFFERENT `run_id`, audits `autofix.kill_wrong_run_race` and returns `killed=False, error="wrong_run_race"` without stamping. Otherwise stamps `runbook_runs.killed_at` + writes `autofix.killed` audit in ONE txn (SQL gates on `killed_at IS NULL` for idempotency). Then polls up to `_KILL_UNWIND_DEADLINE_SECONDS=5.0` for `_current_run` to clear; returns `unwind_warning="unwind_deadline_exceeded"` on timeout.
- `_exec_claude` publishes `_current_run` before `exec_capture` and clears in a finally block regardless of success/failure/timeout. Dry runs are NOT tracked.
- `_check_operational_gates` step-1 kill-switch check remains FIRST (unchanged from STAGE-009-005). Gate ordering preserved: kill_switch → allow_list → rate_limit → cooldown.
- New router `apps/monitor/homelab_monitor/kernel/api/routers/autofix_settings.py`: `GET /api/settings/autofix/kill-switch` returns `KillSwitchState(enabled, updated_at)` (single SELECT); `POST` accepts `KillSwitchToggleRequest(enabled, confirm_phrase)`. Noop POST (state==request) returns 200 with no audit (idempotent "current state alias"). Real transitions require case-insensitive confirm phrase: `"disable auto-fix"` (on→off) or `"enable auto-fix"` (off→on). Wrong/missing → 400. Toggle writes `autofix.kill_switch_toggled` audit. On on→off, invokes `orchestrator.kill_inflight(...)`; on `DockerSocketError` → 502. Response includes `killed_inflight_run_id` and `unwind_warning`.

**Frontend**:
- New route `/settings/autofix` (`SettingsAutofixPage.tsx`) with a sub-nav pattern in `SettingsLayout` (Logs | Auto-fix tabs using tanstack-router `Link` + `activeProps`).
- Card layout with Enabled/Disabled status label + destructive-styled toggle button. Reuses existing `ConfirmPhraseDialog` (case-insensitive match). Success responses render `unwind_warning` banner + `killed_inflight_run_id` info line.
- New `apps/ui/src/api/autofixSettings.ts`: TanStack Query `useAutofixKillSwitch` (query) + `useToggleAutofixKillSwitch` (mutation).
- `ConfirmPhraseDialog.tsx` widened `errorMessage?: string` to `errorMessage?: string | undefined` for `exactOptionalPropertyTypes: true` compatibility.

**Refinement** (host-integration + user viewport sign-off):
- Prod rebuild + redeploy on instance A (`homelab-monitor` on `192.168.2.148:29090`) — migration 0048 applied at startup; API endpoint registered; `runbook_runs.killed_at` column present.
- Live prod: `DockerSocketClient` UDS successfully inspects the running `homelab-fixer-runner` container.
- Mid-run kill LIVE-proven via host-run of `test_docker_kill_terminates_inflight_exec` — real `docker kill` fires against a fake-claude-configured fixer-runner container (with `FAKE_CLAUDE_SLEEP=600`); in-flight `docker exec` unwinds; 7.29s PASS.
- User viewport sign-off (Desktop + Mobile) APPROVED on prod. User exercised on→off and off→on transitions via the ConfirmPhraseDialog.

**Verification**: `make verify` GREEN — ruff + format + `pyright --strict` (0/0/0) + full backend pytest (6957 passed, 1 skipped [vector binary not on PATH], **100.00% coverage** across 27844 stmts + 6250 branches) + UI generate-types + lint + format-check + tsc + vitest (1896 tests passed) + vite build.

**Non-negotiables**: all 7 verified PASS by code-reviewer (Opus) with explicit per-non-negotiable sign-off. #7 (kill switch — owner) proven at unit-branch + host-integration levels. #4 (audit) — every toggle transition + every kill outcome writes an `audit_log` row (`autofix.kill_switch_toggled`, `autofix.kill_attempted`, `autofix.killed`, `autofix.kill_failed`, `autofix.kill_no_inflight`, `autofix.kill_wrong_run_race`). #1/#2/#3/#5/#6 regression checks PASS.

**Code review findings addressed** (all 3 Important + 6 Minor, per project workflow):
- Important #1: wrong-run race attribution — re-verify `_current_run` under lock after docker kill; audit `autofix.kill_wrong_run_race` and skip stamp on mismatch.
- Important #2: noop confirm-phrase bypass — dropped `autofix.kill_switch_noop` audit; noop is now audit-free (idempotent alias).
- Important #3: pre-kill audit — added `autofix.kill_attempted` in fresh txn BEFORE docker kill fires.
- Minor: `_UPDATE_KILLED_SQL` idempotency gate; `_read_state` single SELECT; `kill_container` 5s panic timeout; top-level `text` import; test renames + additions.

**Scope guards**:
- Sidebar top-level "Auto-fix" nav entry NOT added — owned by STAGE-009-010 (fuller Auto-fix Runbooks screen).
- Killed-row display in runbook_runs history table — owned by STAGE-009-010.
- Rate-limit-triggered auto-kill — out of scope; not tracked as EPIC-009 work.
- Role-gating on the toggle — homelab is single-user; intentional accept.

---

2026-07-02 7448057 EPIC-009 / STAGE-009-006: Dry-run mode + approval flow

**Implemented non-negotiable #5 (dry-run + explicit approval) on top of the STAGE-009-005 orchestrator.**

- Runbook with `dry_run_required=True` now triggers a PLAN-ONLY claude invocation (`claude -p <path> --permission-mode plan`, without `--dangerously-skip-permissions`); the plan is stored as a `runbook_runs` row with `mode='dry_run'`, and a PENDING `runbook_run_approvals` row is created in the same transaction. The orchestrator halts — no real exec fires.
- New API surface (auth via session, CSRF on mutating routes): `GET /api/autofix/approvals?status_filter=pending|approved|rejected`, `GET /api/autofix/approvals/{id}/plan`, `POST /api/autofix/approvals/{id}/approve` (typed confirm_phrase = "approve" required, byte-exact), `POST /api/autofix/approvals/{id}/reject`. Approve calls `AutoFixOrchestrator.execute_approved`, which re-validates operational gates (kill switch / rate limit / cooldown), re-validates drift (`content_hash != pinned_runbook_hash`), then reuses the same `_claim_and_exec` path as safe-runbook real runs.
- SQL-guarded race protection: `UPDATE runbook_run_approvals ... WHERE id=:id AND status='pending'` — concurrent approves are safely serialized (one wins, others receive `APPROVAL_NOT_PENDING`); on claim-denial post-approval, the approval reverts to `pending` for retry via `revert_to_pending_conn` with an `autofix.approval_reverted` audit.
- Drift split: `RUNBOOK_CHANGED` (content_hash mismatch) vs `RUNBOOK_MISSING` (runbook deleted) as distinct denial reasons + audit shapes; both surface as 409 with distinct API error codes.
- `autofix.ran` audit now records `approving_principal` when a real run was triggered via an approval, preserving the forensic chain from `autofix.approved` to `autofix.ran`.
- New migration 0047 adds `runbook_run_approvals` (id, dry_run_id, runbook_id, alert_id, pinned_runbook_hash, status, approved_by, decided_at, real_run_id, created_at) + indexes on status/dry_run_id/runbook_id.
- Fake claude script extended: `--permission-mode plan` argv → writes `.plan` marker + emits a recorded plan transcript, does NOT simulate execution. Two e2e integration tests (dry→approval→real; drift-invalidation) prove the safety chain against a real fixer-runner container.
- Backend gate: 6919 passed / 1 baseline skipped / Total coverage 100.00%. Full `make verify` (backend + UI: 1885 tests) green.
- Non-negotiable #5 formally verified: no code path lets a `dry_run_required=True` runbook produce a `--dangerously-skip-permissions` real exec without going through `execute_approved`, drift re-check, and operational-gate re-check.

**Deferrals (tracked, not silent):**
- Markdown/whole-folder drift detection → STAGE-009-012 (006's drift check is CONFIG-hash-only).
- Session-PIN confirm-on-destructive → STAGE-009-010/011 (006 ships typed `confirm_phrase` only).
- Approval UI (list pending, view plan, approve/reject buttons) → STAGE-009-010/011 (006 is API-only).
