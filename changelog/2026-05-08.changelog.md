## [STAGE-001-015] VictoriaMetrics + vmagent integration

Replaced the in-memory MetricsWriter with a multiplex writer that fans out to both an in-memory latest-value map AND a prometheus_client registry. vmagent scrapes the new unauthenticated `/metrics` endpoint and remote-writes to VictoriaMetrics; `/api/metrics/range` proxies range queries to VM. HostCpuTile optimistically backfills its sparkline with real history.

### Foundation epic progress: 14/22 → 15/22

### Backend

New `MultiplexMetricsWriter` (`kernel/metrics/multiplex.py`) fans out every `write_*`/`replace_family` call to a list of writers (in-memory + prometheus_client). New `PrometheusRegistryWriter` (`kernel/metrics/prometheus_writer.py`) backed by `prometheus_client>=0.21`. Caches metric objects keyed by `(name, frozenset(label_names))`. Different label-name sets log warning + skip. Thread-safe via `threading.RLock`.

New unauthenticated `GET /metrics` endpoint (`kernel/api/routers/observability.py`) serves Prometheus exposition format. Mounted at root path (NOT `/api/...`). New authenticated `GET /api/metrics/range` (`kernel/api/routers/metrics.py`) proxies to VM's `/api/v1/query_range`. Validates `expr <= 4096 chars` and `step` regex `/^\d+(ms|s|m|h|d)$/`. Surfaces VM `status="error"` and HTTP errors as 502. Uses shared `app.state.http_client`.

Lifespan attaches three new state attributes: `metrics_writer` (multiplex, for writes), `in_memory_metrics_writer` (for snapshot + inspection), `prom_registry` (for `/metrics`). `collectors.py` and `health.py` now inject `get_in_memory_metrics_writer` (resp. `_optional`) for inspection method calls (`last_tick_at_for`, `failures_in_window`, etc.) — multiplex no longer needs to forward those non-Protocol methods.

Pre-existing `AccessLogMiddleware` bug fixed: `exception=True` (boolean) tripped structlog's special exception-rendering path. Renamed to `had_exception=True`.

### Deployment

New `deploy/compose/docker-compose.yml` with monitor + VM v1.107.0 + vmagent v1.107.0. Monitor port bound to `127.0.0.1` by default (external access via upstream reverse proxy; `/metrics` exposure unauthenticated for in-network scraping only). New `deploy/compose/vmagent/scrape.yaml` — single static target (one job, one `/metrics` endpoint). New `deploy/compose/.env.example` documenting required env vars + security model.

New multi-stage `apps/monitor/Dockerfile` (uv sync from workspace lockfile → slim runtime with UI bundle from `apps/ui/dist`). New `.dockerignore` at repo root. New `make compose-build`, `compose-up`, `compose-down`, `compose-logs` targets. `make verify-ci` now includes `docker compose -f deploy/compose/docker-compose.yml config -q` for syntax validation.

### Frontend

New `useMetricsRange(expr, start, end, step)` TanStack Query hook in `apps/ui/src/api/queries.ts`. `HostCpuTile` optimistically backfills its sparkline with 600s of historical data from `/api/metrics/range`. On VM unreachable: silently retains synthetic baseline. SSE-driven appends continue regardless. Added `data-values` attribute to `Sparkline` for testable assertions.

### Refinement-phase fix

HostCpuTile race condition: SSE-tick effect was setting `seededFromHistoryRef.current = true`, killing the range backfill. Removed the SSE side effect on the ref. Renamed ref to `historyBackfilledRef` for clarity.

### Tests

22 new backend tests across 4 new files (`test_metrics_multiplex.py`, `test_metrics_prometheus_writer.py`, `test_api_metrics_endpoint.py`, `test_api_metrics_range.py`). 6 new frontend tests in `HostCpuTile.test.tsx` covering range backfill success, failure, empty, padding, race-with-SSE. 100% kernel coverage maintained. Backend: 901 tests passing. UI: 16 HostCpuTile tests + full UI suite passing.

### Code review

Two-pass code review with code-reviewer-Opus. Pass 1: 19 findings (3 Critical, 7 Important, 9 Minor) — all addressed. Pass 2 caught a deadlock introduced by Pass 1 fixes (`threading.Lock` was non-reentrant; `replace_family` calls `write_gauge` which also acquired the lock). Switched to `threading.RLock` (reentrant). Lesson: Pass 2 catches what Pass 1 misses.

### Commits

- `cb2601b` — feat(metrics): STAGE-001-015 VictoriaMetrics + vmagent integration
- `0645169` — fix(ui): backfill race in HostCpuTile (Refinement-phase fix)
- `15af968` — refactor(metrics): STAGE-001-015 code review fixes (2 passes)

(The `<finalize-impl-hash>` placeholder is for the upcoming Finalize commit; main agent will fill it in after committing.)

## [STAGE-001-015A] Backup hook + disk budget + minimal test rig

Layered operational concerns onto the VM sidecar from STAGE-001-015: a backup endpoint that triggers SQLite + VictoriaMetrics snapshots with audit logging, a disk-budget self-monitor with auto-shrink decision v1, and a minimal integration test rig that's reusable for future stages.

### Foundation epic progress: 15/22 → 16/22

### Key changes

**Backup endpoint:**
- New `POST /api/admin/backup` (cookie session OR API token with new `Scope.ADMIN_BACKUP_WRITE`). Orchestrates SQLite `.backup()` snapshot + VM `/snapshot/create` + `cp -al` snapshot files to backup root. Falls back to `cp -r` when backup is on a different filesystem than VM data. Two-row audit pattern: `requested` row written before work, `completed`/`failed` row written after.
- New `BackupService` at `kernel/backup/service.py`. Best-effort semantics: SQLite + VM steps independent; failures collected into `errors` list rather than raising. Pre-checks disk space (2x source DB size) before SQLite backup; cleans up partial files on failure. Validates VM snapshot ID against `^[A-Za-z0-9._-]+$` regex (path-traversal hardening). 5s VM HTTP timeout.
- New CLI: `hm backup {run, list, retention --keep N}`. Each subcommand bootstraps its own engine + repository for audit; disposes on exit. CLI runs write `who="cli:<euid>"` audit rows.
- Apply-retention: filters glob through name regex (`sqlite-YYYYMMDD-HHMMSS.sqlite` for SQLite, alphanumeric for VM dirs) to skip foreign files. Rejects `keep < 1`.

**Disk budget:**
- New `SelfDiskCollector` (60s interval) emits `homelab_self_disk_used_bytes`, `_budget_bytes` per slot (vm/vl/sqlite/runbook_transcripts), `_used_pct`, and `_shrink_total {tier}`. Uses `os.walk(followlinks=False)` for safety.
- Auto-shrink v1: at >95% used_pct, emits shrink counter + writes critical-severity audit row. 5-minute cooldown prevents flooding during sustained incidents (instead of 60 audit rows/hour, max 12). No actual data eviction in v1; deferred to future stage.

**Config loader:**
- New `kernel/config.py` — minimal `yaml.safe_load` helper. Loads `HOMELAB_MONITOR_CONFIG` (default `/config/homelab-monitor.yaml`). Returns `DiskBudgetConfig` with default ratios (60% VM / 30% VL / 10% SQLite) when YAML absent. `HOMELAB_MONITOR_DISK_BUDGET_GB` env overrides total. Validates ratios sum ~1.0.

**vmalert rules:**
- New `deploy/vmalert/metrics/self_disk.yaml`: SelfDiskWarn (70%/5m), SelfDiskError (85%/5m), SelfDiskCritical (95%/0s). Static YAML; vmalert deployment lands in STAGE-001-018.

**Test rig:**
- New `Dockerfile.test` (multi-stage with dev deps).
- New `deploy/compose/docker-compose.test.yml` (VM + integration-tests services on shared network).
- New `scripts/run-integration.sh` — `docker compose up --abort-on-container-exit --exit-code-from integration-tests`.
- New `apps/monitor/tests/integration/` with smoke test asserting VM `/health` reachable. Marked `@pytest.mark.integration`; excluded from default test runs via `--ignore=tests/integration` in addopts.
- Makefile: new `make integration` target.
- Compose: monitor service binds VM data dir RO at `/var/vm-data`.

### Tests

- 14 new backend tests across 5 new files: `test_backup_service.py` (12 tests), `test_api_admin_backup.py` (5), `test_cli_backup.py` (4), `test_config.py` (8), `test_self_disk_collector.py` (6).
- 1 new integration smoke test (excluded from default run).
- 100% kernel coverage maintained.
- 943 backend tests passing.

### Code review

Two-pass code-reviewer-Opus. Pass 1: 27 findings (6 Critical, 9 Important, 12 Minor) — all addressed. Pass 2 caught a CRITICAL regression: the Pass 1 fixes added a required `db=` parameter to BackupService but didn't update the 9 test constructions or the CLI bootstrap, breaking 8 tests + the entire CLI. Fixed in a final wave. Pass 2 verdict: APPROVED after the second-wave fixes.

### Commits

- `4ecc212` — chore(stage): STAGE-001-015A Design (Complete)
- `22e4b14` — feat(backup,disk): Build implementation (Spec A + B)
- `0645169` was STAGE-001-015 (unrelated)
- `a79145c` — chore(stage): Build (Complete)
- `89b3da0` — chore(stage): Refinement (Complete)
- `a8d1f91` — refactor(backup): code review fixes (2 passes)

(The `<finalize-impl-hash>` placeholder is for the upcoming Finalize commit; main agent will fill it in after committing.)

## [STAGE-001-016] VictoriaLogs + vector log pipeline

Stand up the log pipeline. Add VictoriaLogs and vector as sidecars. Replace the in-memory LogsWriter with a multiplex (in-memory + VL HTTP push). Vector tails Docker container stdout/stderr and journald, ships to VL with per-stream throttle. Adds /api/logs/query (LogsQL proxy) and /api/logs/streams (in-process state) endpoints.

### Foundation epic progress: 16/22 → 17/22

### Key changes

**Backend:**
- New `MultiplexLogsWriter` (kernel/logs/multiplex.py) fans out every ingest() call to a list of writers; mirrors STAGE-015 metrics multiplex.
- New `VictoriaLogsWriter` (kernel/logs/vl_writer.py) bridges sync ingest Protocol to async HTTP via bounded asyncio.Queue (10000 capacity) + background worker. Batches up to 100 events / 1s timeout; POSTs NDJSON to VL `/insert/jsonline`. Sentinel-driven graceful shutdown. Top-level try/except in flusher prevents worker crashes from silently killing log ingest.
- New `LogStreamBudgetCollector` (60s) queries VL stats, emits per-stream `homelab_log_stream_bytes_today` and `_lines_per_sec` gauges. Returns ok=False on VL outage; scheduler counts as failure for quarantine.
- New `LogStreamBudgetConfig` in kernel/config.py.
- Lifespan wires multiplex + flusher task; graceful shutdown drains.

**API:**
- `GET /api/logs/query` thin LogsQL proxy with auth; validates expr length (≤4096), step format, ISO-8601 start/end, range cap (≤30 days), tzinfo normalization on naive timestamps.
- `GET /api/logs/streams` returns dict-snapshot of in-process state.

**Compose & vector:**
- New `victorialogs` service (v0.30.0, internal-only, retention env-configurable).
- New `vector` service (v0.41.1) with config + Docker socket + journald RO mounts; depends on VL health.
- `data_vl` volume RW for VL, RO for monitor (SelfDiskCollector "vl" slot accounting).
- `deploy/vector/vector.toml`: docker_logs + journald sources → add_labels (host/service/severity, null-safe) → parse_json (preserves trusted top-level fields against malicious container JSON) → drop_noise → throttle (50 lines/sec/service) → http NDJSON sink with retry/buffer config.
- `HOMELAB_MONITOR_VL_RETENTION_DAYS` documented in `.env.example`.

### Tests

- 27 new backend tests across 4 new files (test_logs_multiplex.py, test_logs_vl_writer.py, test_api_logs.py, test_log_stream_budget_collector.py) + extended test_config.py.
- 100% kernel coverage maintained.
- 965 backend tests passing.

### Code review

Two-pass code review with code-reviewer-Opus. Pass 1: 17 findings (4 Critical, 10 Important, 13 Minor — all addressed). Pass 2: 1 Important fix (timezone normalization on start/end comparison) + verified all Pass 1 fixes correct. Approved.

### Commits

- `2d033c7` — chore(stage): STAGE-001-016 Design (Complete)
- `2b0e4d1` — feat(logs): VictoriaLogs + vector log pipeline (impl)
- `781be1c` — chore(stage): STAGE-001-016 Build (Complete)
- `daca0f3` — docs(env): HOMELAB_MONITOR_VL_RETENTION_DAYS in .env.example
- `6207382` — chore(stage): STAGE-001-016 Refinement (Complete)
- `9ba7f21` — refactor(logs): code review fixes (2 passes)

(The `<finalize-impl-hash>` placeholder is for the upcoming Finalize commit; main agent will fill it in after committing.)

---

## STAGE-001-017 — Alertmanager + vmalert (metrics) + first rule

### Summary

Wires the Alertmanager + vmalert (metrics) sidecars and bootstraps the alert pipeline. The existing alert ingestor + dispatcher (from STAGE-001-013/014) accept Alertmanager v2 webhook payloads; this stage adds the upstream pieces (AM render-on-boot, vmalert rule files, sidecar containers).

### New module

- `apps/monitor/homelab_monitor/kernel/alertmanager/render.py`
  - `ensure_ingest_token`: idempotent token mint with system-principal audit trail (`who="system:alertmanager-bootstrap"`); deletes orphan token/secret rows when the pair drifts apart.
  - `render_config`: atomic write (mkstemp + os.replace) of the AM template with Bearer token substitution.
  - `AlertmanagerReloader`: POST `/-/reload` with warn-on-failure semantics (file is source of truth).
  - `render_on_boot`: orchestrator called from lifespan; logs `ensure_ingest_token` failures at ERROR with traceback, render/reload failures at WARNING; never raises.

### AuthRepository additions

- `get_api_token_by_name(name) -> ApiToken | None`
- `delete_api_token_by_name(name) -> bool`

### Compose

- New `alertmanager` service: `prom/alertmanager:v0.27.0`, depends_on monitor (healthy), reads rendered config from new `data_alertmanager_config` volume.
- New `vmalert-metrics` service: `victoriametrics/vmalert:v1.107.0`, file-watching via `-configCheckInterval=30s`, evaluation interval 30s.
- Both internal-only (no host port exposure).
- TODO comment in alertmanager service block documenting unauthenticated `/-/reload` as accepted-for-now under single-tenant homelab trust model.

### vmalert rules

`deploy/vmalert/metrics/host.yaml`:
- `HostHighCPU`: `avg_over_time(homelab_host_cpu_percent{cpu="all"}[5m]) > 90` for 10m, severity=warning.
- `HostHighMemory`: `homelab_host_memory_bytes{type="used"} / homelab_host_memory_bytes{type="total"} > 0.9` for 10m, severity=warning.
- `CollectorQuarantined`: COMMENTED OUT pending `homelab_collector_quarantine_count` metric emission in STAGE-001-018+. The synthetic AlertFiringEvent in scheduler.failure_budget covers this case until the gauge is wired.

### Lifespan integration

- New section 8b in `apps/monitor/homelab_monitor/kernel/api/lifespan.py` calls `render_on_boot` after secrets resolver init.
- `HOMELAB_MONITOR_ALERTMANAGER_URL` env var: when set to `disabled`, `""`, `false`, `0`, or `none` (case-insensitive, whitespace-stripped), the AM `/-/reload` step is skipped. Used in tests + first-boot scenarios.

### Tests

- 4 new test files: `test_alertmanager_render.py` (token bootstrap, render module, reload paths, exception handling, plaintext-never-logged), `test_lifespan_alertmanager.py` (lifespan integration, idempotency, sentinel handling), `test_alerts_ingest_e2e.py` (firing+resolved → SSE delivery, auth tests), `integration/test_vmalert_pipeline.py` (`@pytest.mark.slow` placeholder).
- `conftest.py` updated: `HOMELAB_MONITOR_ALERTMANAGER_URL=disabled` in `authenticated_client` + `api_token_client` fixtures.
- 100% backend kernel coverage maintained.
- 1041 backend tests passing.

### Code review

Two-pass code review with code-reviewer-Opus. Pass 1: 16 findings (0 Critical, 4 Important, 8 Minor, 4 Nice-to-have). Pass 2: 3 Minor findings (stale docstrings, pyright-suppressed spy function refactored to typed signature). All findings addressed.

### Commits

- `bd4430f` — chore(stage): STAGE-001-017 Design (Complete)
- `3b1c9bd` — feat(alerts): STAGE-001-017 Alertmanager + vmalert (metrics) + first rule (impl)
- `954ca1c` — chore(stage): STAGE-001-017 Build (Complete)
- `9c7cc63` — chore(stage): STAGE-001-017 Refinement (Complete)
- `635bfcf` — refactor(alerts): STAGE-001-017 code review fixes (2 passes)

---

## STAGE-001-018 — vmalert (logs) + first log-derived rule

### Summary

Adds vmalert (logs) sidecar pointing at VictoriaLogs and the first three log-derived rules. Reuses Alertmanager + AlertDispatcher + AlertmanagerReloader infrastructure from STAGE-017; the same `/api/alerts/ingest` webhook receives both metric-derived (`source_tool=vmalert-metrics`) and log-derived (`source_tool=vmalert-logs`) alerts.

### New rule files

- `deploy/vmalert/logs/system.yaml` — production rules with `type: vlogs` group discriminator + LogsQL pipe syntax:
  - `KernelOOM` — count > 0 over 5m on `{service="kernel"}` matching "Out of memory.*kill_process"
  - `SshFailedLoginBurst` — count > 10 over 5m on `{service="sshd"}` matching "Failed password"
- `deploy/vmalert/logs-test/system.yaml` — test counterpart with 30s windows and lowered thresholds (>0 OOM, >2 SSH burst) for fast-firing integration tests
- `deploy/vmalert/metrics/log_stream_budget.yaml` — cross-listed metric rule: `LogStreamBudgetOverrun` fires when `homelab_log_stream_lines_per_sec > 45` (90% of vector's 50/s throttle) for 5m

### Compose

- `vmalert-logs`: victoriametrics/vmalert:v1.107.0, datasource VL, rule path `/etc/vmalert/logs/`, healthcheck on :8880/-/health, depends_on alertmanager + victorialogs
- `docker-compose.test.yml`: added victorialogs + vmalert-logs-test + integration-tests env vars (VL_URL, AM_URL, VMALERT_URL, VMALERT_LOGS_URL) + dependency wiring
- `test-fixtures/alertmanager.yml`: resolve_timeout 60s, group_interval 30s, webhook URL → `http://integration-tests:9090/api/alerts/ingest`
- All 10 docker healthchecks fixed to use `127.0.0.1` instead of `localhost` (busybox-based images don't resolve `localhost` without /etc/hosts entry)

### Tests

- `apps/monitor/tests/integration/helpers/vl_planter.py` — async helper to POST NDJSON to VL with deterministic RFC3339 timestamps; tz-aware safety + production-URL refusal
- `apps/monitor/tests/integration/helpers/test_webhook_server.py` — extracted FastAPI receiver for AM webhook tests
- `apps/monitor/tests/integration/test_vmalert_logs_pipeline.py` — 4 `@pytest.mark.slow` tests:
  - Rules load and are listed by vmalert
  - Plant 1 OOM line → KernelOOM fires + reaches webhook
  - Plant 3 SSH failed-password lines → SshFailedLoginBurst fires
  - After window expires + no new lines, OOM alert resolves

### Refinement-discovered bugs (fixed)

- `vl_planter.py` was using epoch milliseconds for VL `_time` field; VL v0.30.0 only accepts RFC3339 → switched to `ts.strftime("%Y-%m-%dT%H:%M:%S.%f")[:-3] + "Z"` form
- Docker healthchecks used `wget http://localhost:<port>/...` but busybox images don't resolve `localhost` → 10 healthchecks fixed to `127.0.0.1`

### Code review

Two-pass code review with code-reviewer-Opus. Pass 1: 17 findings (0 Critical, 7 Important, 7 Minor, 3 Nice-to-have). 12 addressed; 5 deferred per documented trade-offs. Pass 2: 3 polish findings (case-insensitive safety check, append-atomicity docstring, kill-fallback timeout bump). All addressed.

Verify: 1041 backend tests + 91 UI tests pass; 100.00% backend kernel coverage maintained.

### Commits

- `b448f94` — chore(stage): STAGE-001-018 Design (Complete)
- `6fd43e7` — feat(alerts): STAGE-001-018 vmalert (logs) + first log-derived rule (impl)
- `4054293` — chore(stage): STAGE-001-018 Build (Complete)
- `aff5737` — fix(test-rig): RFC3339 timestamps + 127.0.0.1 healthchecks (Refinement bug fixes)
- `cfe6e89` — chore(stage): STAGE-001-018 Refinement (Complete)
- `23b028e` — refactor(alerts): STAGE-001-018 code review fixes (2 passes)

---

## STAGE-001-019: Karma + kthxbye iframe integration

**Commit**: `4e9f60c` (implementation)

### Summary

Embedded the Karma alert UI at `/alerts` via a sandboxed same-origin iframe and added kthxbye to auto-extend `ACK!`-prefixed silences while alerts continue firing. The monitor reverse-proxies Karma at `/api/karma/{path:path}` with cookie-session auth (CSRF-exempt; substituted by Origin/Referer same-origin enforcement). Adds the first CSP middleware (`frame-ancestors 'self'`).

### Highlights

- **Backend**: new `karma` router (catch-all proxy, header allow-list, path validation, streaming response, reverse-proxy aware origin check via `HOMELAB_MONITOR_TRUST_FORWARDED_HEADERS=1`), new `require_session_no_csrf()` factory, new `CspHeadersMiddleware`. 100% kernel coverage maintained.
- **UI**: new `/alerts` route (full-bleed iframe), new shared `ErrorDisplay` component used by both root and router error boundaries (prevents React error #31 from raw HttpProblem objects).
- **Compose**: pinned `ghcr.io/prymitive/karma:v0.129` + `ghcr.io/prymitive/kthxbye:v0.16` as internal-only sidecars on `homelab-monitor-net`. kthxbye flag-ordering invariant documented (`extend-by > extend-if-expiring-in`).
- **Tests**: 31 backend unit tests + 2 slow integration tests + 8 UI tests added. Backend: 1076 passing, 100% coverage. UI: 104 passing, 77.29% branch coverage.
- **Documentation**: new `docs/alerting/lifecycle.md` (alert state machine, ack vs silence semantics, kthxbye timing math, proxy + iframe architecture).

### Code review

Two passes (Opus). Pass 1: 3 Critical + 9 Important + 12 Minor — all addressed (implementation or documented deferral). Pass 2: 0 Critical + 0 Important + 2 Minor (deferred — stylistic).

### Pre-existing fixes carried in

- `.dockerignore` re-includes `apps/monitor/alembic/` (the prod image was missing migrations; chown of `/data` for non-root user is deferred to STAGE-021).
- `tests/test_scheduler.py::test_process_run_kind`: 0.6s → 1.5s sleep (forkserver startup non-determinism).

### Deferred to STAGE-021

- Centralized dev-environment tooling (`scripts/dev-up.sh --clean / --prod`, `dev-down.sh`, `deploy/dev/dev.env` single source of truth).
- Prod Dockerfile correctness (`/data` chown for the non-root container user) so `make dev-prod` can run end-to-end.
- `make integration` script + `.dockerignore` integration-tests support (the `addopts --ignore=tests/integration` + `tests/` excluded from build context combination prevents the integration-tests container from collecting any tests today).
