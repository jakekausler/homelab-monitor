# 2026-05-26

## STAGE-003-010: Pull & Restart action — compose-aware + audit + confirm

**Epic:** EPIC-003 (Docker)
**Date:** 2026-05-26
**Commit:** `4e46d5f`

### Summary

First kernel-driven Docker WRITE operation. A confirm-gated per-container button executes `docker compose -p <project> -f <file> {pull|build} <service> && docker compose ... up --force-recreate -d <service>` from inside the monitor container against the host's compose file, captures stdout/stderr, writes a `compose_actions` audit row + `audit_log` row, emits Prometheus metrics, and refreshes the UI. The docker socket mount widens from `:ro` to RW; the compose file + build contexts are mounted RO. Confirm phrase is the literal string `"pull"` (server-validated). Concurrency: per-container `asyncio.Lock` + 409 Conflict on duplicate submit. Phase state machine: `pulling`/`building` → `restarting` → `success`/`failed`/`timeout`/`killed`. After a successful action, the appropriate image-update refresher (registry digest OR local-build source hash) is invoked so the badge clears within ~2s instead of waiting for the next scheduled tick. Auth: session OR new `Scope.DOCKER_WRITE`.

This stage establishes the audit + confirm + capture-transcript pattern that EPIC-009 (auto-fix subsystem) will reuse for Claude-driven runbook real-runs. The `compose_actions` table is kept separate from EPIC-009's future `runbook_runs` (cross-epic carry-forward documented).

### Behavior changes

- **NEW**: `POST /api/integrations/docker/containers/{name}/pull-and-restart` — triggers the action; gated by `require_user_or_token({Scope.DOCKER_WRITE})`; body `{"confirm_phrase": "pull"}` (case-insensitive, trimmed); returns 202 + `{action_id, state}`. Returns 409 if any row with `state in ('pulling','building','restarting','running')` already exists for this container.
- **NEW**: `GET /api/integrations/docker/compose-actions/{id}` — single action detail (stdout/stderr included). Session auth.
- **NEW**: `GET /api/integrations/docker/compose-actions?container={name}&limit=N` — list recent actions for a container. Session auth.
- **NEW**: `Scope.DOCKER_WRITE = "docker:write"` — admins can mint API tokens with this scope to trigger Pull & Restart programmatically.
- **NEW**: `homelab_compose_action_success_total{container,action}` + `homelab_compose_action_failed_total{container,action,reason}` Prometheus counters.
- **NEW**: `DockerComposeActionFailed` vmalert rule (warning severity, fires on failure increase in 10m window).
- **NEW**: `compose_actions` table (migration 0028) with `state` widened in 0029 (`pulling`/`restarting` instead of `running`) and again in 0030 (`building` added for locally-built containers).
- **NEW**: Per-container `asyncio.Lock` dict on `ComposeActionRunner` (mirrors `Scheduler._group_locks`). Lock acquired inside the background task; API-side 409 guard prevents two concurrent triggers from reaching the runner.
- **NEW**: `LocalBuildUpdateCollector.refresh_container(container_name)` — on-demand refresher invoked after a successful local-build rebuild; resets `baseline_source_hash` + `baseline_image_id` to current values and writes `update_available=0`. Mirrors `image_update_collector.refresh_container` (added in earlier wave for remote-image refresh after pull).
- **NEW**: `compose_actions.state` phase column. Initial state set to `pulling` (remote-image) or `building` (local-build). Transitions to `restarting` before `up --force-recreate -d` runs. Terminal: `success`/`failed`/`timeout`/`killed`.
- **NEW**: UI "Rebuild & Restart" button label for locally-built containers; "Pull & Restart" for remote-image. Confirm phrase is `"pull"` for both (API contract uniformity). Modal title/description reflect the action type.
- **NEW**: `useGetComposeAction(actionId)` polls every 2s while `state` is non-terminal; stops on terminal.
- **NEW**: Optimistic local state in `ActionsCell` bridges the gap between mutation success and the first poll showing the new row.
- **NEW**: On terminal state transition, `ActionsCell` invalidates `dockerImageUpdateQueryKeys.summary` + `dockerImageUpdateQueryKeys.detail(containerName)` so the badge clears immediately.
- **NEW**: Toast notifications on terminal state (success / failed / timeout / killed) — guarded by an `invalidatedRef` to fire at most once per `action_id`.
- **CHANGED**: Docker socket mount widens from `:ro` to RW in `deploy/compose/docker-compose.yml`. Compose file mounts RO at `/host-compose`. Security boundary is at the docker socket level; accepted per EPIC-003 cross-stage criterion #18.
- **CHANGED**: Monitor container `group_add` gains `homelab-compose` GID (default 994, configurable via `HM_HOST_COMPOSE_GID`). Container user needs read access to `/storage/docker/compose/.env` for compose env interpolation.
- **CHANGED**: Monitor container env adds `HOME=/tmp` + `DOCKER_CONFIG=/tmp/.docker`. Docker CLI requires a writable HOME for its config dir.
- **CHANGED**: `apps/monitor/Dockerfile` installs `docker-ce-cli`, `docker-compose-plugin`, `docker-buildx-plugin` from Docker's official APT repo (~50 MB image growth).

### Architecture decisions (locked in STAGE-003-010.md)

Original scope:
- D-COMPOSE-AWARE — `docker compose -f <file> pull <svc> && docker compose -f <file> up -d <svc>`, NOT raw `docker pull`. Preserves env, volumes, networks, dependencies.
- D-INSIDE-MONITOR — action runs INSIDE the monitor container. NOT via a separate fixer-runner pattern (reserved for EPIC-009 Claude runs).
- D-CONFIRM-API-SIDE — confirm gate enforced API-side, not just UI-side. Even a hand-crafted curl must pass.
- D-CONFIRM-EXTENDS-SPEC-7-2 — Pull & Restart extends spec §7.2's confirm-on-destructive list (the list was illustrative).
- D-AUDIT-EVERY-ATTEMPT — audit_log row + compose_actions row for EVERY attempt, regardless of outcome.
- D-COMPOSE-ACTIONS-OWN-TABLE — `compose_actions` is its own table, not a reuse of EPIC-009's future `runbook_runs`.
- D-SOCKET-RW-WIDEN — socket mount widens from `:ro` to RW this stage. Security boundary documented + accepted.
- D-AUTO-UPDATE-OFF — auto-update is permanently OFF. Always manual.

Design phase decisions (user-selected):
- Confirm UX: static phrase `"pull"` (typed in modal, server-validated). Lower friction for single-user homelab.
- Auth: Session OR new `Scope.DOCKER_WRITE` on API tokens. Future-proofs bot/CLI flows.
- Concurrency: per-container `asyncio.Lock` via `dict[str, asyncio.Lock]` (mirrors `scheduler._group_locks`).
- Compose path discovery: reuse `BuildSourcesLoader` from STAGE-003-009.
- Output storage: TEXT columns on `compose_actions`, truncated at 1M chars on write.
- Background task: `asyncio.create_task()` + module-level `set[asyncio.Task]` + shutdown cancellation.
- Timeout: 5 min default (`HOMELAB_MONITOR_COMPOSE_ACTION_TIMEOUT_SECONDS=300`). SIGTERM, 10s grace, then SIGKILL.

ADDENDUM Q2 — Container resolution uses `com.docker.compose.service` label via `socket_client.inspect_container`. Robust to docker-compose project-name prefixes + v1/v2 naming.

Refinement-wave decisions:
- 409 Conflict on duplicate submit (per-container, checked at API layer before reaching the runner).
- Phase column splits `running` into `pulling`/`building` (initial) + `restarting` (post-pull). Migration 0029 widens CHECK; migration 0030 adds `building`.
- Optimistic UI bridge — local React state stores `optimisticActionId` to show "Pulling…"/"Building…" instantly without waiting for first poll.
- Cache invalidation on terminal transition — invalidates `dockerImageUpdateQueryKeys.summary` + `.detail(name)` so badge clears within ~2s.
- Post-success refresher dispatched on `resolved.is_local_build` flag: registry-digest refresher for remote-image, source-hash refresher for local-build.
- `docker compose up -d` → `docker compose up --force-recreate -d` (handles already-running-container name conflicts).
- Compose project name extracted from container's `com.docker.compose.project` label (NOT the cwd basename). Passed as `-p <project>` flag.
- Build vs pull detection from `ResolvedCompose.is_local_build` (from the compose service's `build_context`). Local-build runs `docker compose build`; remote-image runs `docker compose pull`.
- Auto-generated `docker-compose.override.yml` from `build-sources.yaml`'s `build_context_roots` — bind-mounts host build-context paths AT the same path inside the container so `docker compose build` resolves them correctly. Operator runs `make generate-build-mounts` when `build-sources.yaml` changes; must invoke compose with both `-f` flags explicitly (auto-loading disabled by `-f` of the main file).
- Dedicated `homelab-compose` host group + `chmod 0640` on `.env` files listed in `scripts/host-setup-shared-files.conf`. Provisioned via `scripts/host-setup.sh`.

### Files added

**Backend:**
- `apps/monitor/alembic/versions/0028_compose_actions.py` — `compose_actions` table migration (20 cols, CHECK constraints, composite index)
- `apps/monitor/alembic/versions/0029_compose_actions_phases.py` — widens state CHECK to `pulling`/`restarting` (SQLite table-rename pattern)
- `apps/monitor/alembic/versions/0030_compose_actions_building_phase.py` — adds `building` to state CHECK
- `apps/monitor/homelab_monitor/kernel/docker/compose_action_runner.py` — `ComposeActionRunner` (per-container lock, SIGTERM/grace/SIGKILL, 1M-char output truncation, label-based service+project resolution, branch on `is_local_build` for build vs pull, post-success refresher dispatch); `ResolvedCompose` dataclass with `is_local_build` + `compose_project` fields; `ImageUpdateRefresher` + `LocalBuildRefresher` Protocols
- `apps/monitor/homelab_monitor/kernel/db/repositories/compose_actions_repository.py` — `ComposeActionsRepository` (insert_running, update_terminal_state, update_phase, get_by_id, get_active_for_container, list_for_container); `ComposeActionRow` dataclass; `ACTIVE_STATES` tuple

**Frontend:**
- `apps/ui/src/components/docker/PullRestartModal.tsx` — confirm-gated modal (dynamic label based on action type, static `"pull"` phrase, current/latest digest preview, command preview)
- `apps/ui/src/components/docker/__tests__/PullRestartModal.test.tsx`
- `apps/ui/src/routes/integrations/__tests__/RecentActionsPanel.test.tsx`

**Tests:**
- `apps/monitor/tests/test_compose_action_runner.py`
- `apps/monitor/tests/test_compose_actions_repository.py`
- `apps/monitor/tests/test_api_docker_pull_and_restart.py`

**Deployment:**
- `deploy/vmalert/metrics/docker_compose_actions.yaml` — `DockerComposeActionFailed` warning-severity rule
- `scripts/generate-compose-override.sh` — reads `build-sources.yaml`, writes `deploy/compose/docker-compose.override.yml` with one bind-mount per declared `build_context_roots[*].host_prefix`
- `scripts/host-setup-shared-files.conf` — list of host paths that need to be in the `homelab-compose` group with 0640 perms

### Files modified

**Backend:**
- `apps/monitor/homelab_monitor/kernel/auth/scopes.py` — adds `Scope.DOCKER_WRITE = "docker:write"`
- `apps/monitor/homelab_monitor/kernel/api/lifespan.py` — instantiates `ComposeActionRunner` with `build_sources_loader` + `socket_client` + `prom_registry`; wires `image_update_refresher` (registry digest) AND `local_build_refresher` (source hash) via setters after both collectors are constructed; registers shutdown
- `apps/monitor/homelab_monitor/kernel/api/routers/docker.py` — 3 new endpoints (POST + 2 GET); `PullAndRestartRequest`/`PullAndRestartAcceptedResponse`/`ComposeActionDetailResponse`/`ComposeActionListResponse` pydantic models; 409 Conflict guard using `actions_repo.get_active_for_container(name)` before `runner.trigger_pull_and_restart(...)`; ApiToken `who` formatted as `token:<name>`
- `apps/monitor/homelab_monitor/kernel/db/audit.py` — `insert_audit(...)` accepts optional `audit_id: str | None` so compose_action rows can link to a specific audit_log row via `audit_log_id`
- `apps/monitor/homelab_monitor/kernel/metrics/image_update_collector.py` — `refresh_container(container_name, image_ref, image_id)` for on-demand registry-digest recheck after a successful pull
- `apps/monitor/homelab_monitor/kernel/metrics/local_build_update_collector.py` — `refresh_container(container_name)` for on-demand local-build refresh after a successful rebuild; resets baseline + image_id to current

**Frontend:**
- `apps/ui/src/api/docker.ts` — `useStartPullAndRestart` + `useGetComposeAction` + `useListComposeActions`; exported `COMPOSE_ACTIVE_STATES` set; `dockerComposeActionQueryKeys` (with `limit` in key + `listAllForContainer` prefix for cross-limit invalidation)
- `apps/ui/src/routes/integrations/ContainerGridRow.tsx` — new `ActionsCell` (Pull & Restart / Rebuild & Restart button; optimistic state for in-flight indicator; terminal-state cache invalidation via `invalidatedRef`; toast on terminal; "Pulling…"/"Building…"/"Restarting…" phase labels)
- `apps/ui/src/routes/integrations/ContainerGridCard.tsx` — imports `ActionsCell` from `ContainerGridRow` for mobile parity
- `apps/ui/src/routes/integrations/RecentActionsPanel.tsx` — real implementation (state badge, duration, expand-to-show stdout/stderr); wired into drill-down
- `apps/ui/src/routes/integrations/ContainerImageUpdatePage.tsx` — mounts `<RecentActionsPanel containerName={name} />` below the digest detail
- `apps/ui/src/components/docker/__tests__/` — directory created

**Infrastructure:**
- `apps/monitor/Dockerfile` — installs `docker-ce-cli` + `docker-compose-plugin` + `docker-buildx-plugin` from Docker's official APT repo
- `deploy/compose/docker-compose.yml` — docker socket mount widens `:ro` → RW; new `/host-compose:ro` mount; new env vars (`HOME=/tmp`, `DOCKER_CONFIG=/tmp/.docker`, `HOMELAB_MONITOR_COMPOSE_ACTION_TIMEOUT_SECONDS`); new `group_add` entry (`HM_HOST_COMPOSE_GID`)
- `deploy/compose/.env.example` — new vars (`HM_HOST_COMPOSE_GID`, `HOMELAB_MONITOR_COMPOSE_ACTION_TIMEOUT_SECONDS`) with operator-targeted comments
- `scripts/host-setup.sh` — extended to provision `homelab-compose` group (idempotent), add desktop user + monitor user to it, apply 0640 perms to paths listed in `scripts/host-setup-shared-files.conf`
- `Makefile` — `generate-build-mounts` target invoking `scripts/generate-compose-override.sh`
- `.gitignore` — adds `deploy/compose/docker-compose.override.yml` (operator-managed; host-specific)
- `docs/dev/local-environment.md` — "Build-context mounts" section documenting `make generate-build-mounts` + the `-f` flag gotcha

**Tests (modified):**
- `apps/monitor/tests/test_auth_scopes.py` — `DOCKER_WRITE` scope tests
- `apps/monitor/tests/test_db_migrations.py` — adds `compose_actions` to `EXPECTED_TABLES`
- `apps/monitor/tests/test_api_lifespan.py` — refresher-wiring tests
- `apps/monitor/tests/test_image_update_collector.py` — `refresh_container` tests
- `apps/monitor/tests/test_local_build_update_collector.py` — `refresh_container` tests
- `apps/monitor/tests/_uvicorn_fixture.py` — startup timeout 10s → 20s (parallel-test load flake fix)
- `apps/ui/src/routes/integrations/ContainerGridRow.test.tsx` — new mocks for compose-action hooks
- `apps/ui/src/routes/integrations/ContainerImageUpdatePage.test.tsx` — RecentActionsPanel render
- `apps/ui/src/routes/integrations/DockerIntegrationPage.test.tsx` — removed obsolete placeholder assertion

**OpenAPI cascade:** `packages/shared-types/openapi.json` (+ generated `apps/ui/src/api/schema.ts`) regenerated with the 3 new endpoints + new types.

### Verification

- `make verify` PASS — 3097 backend tests + 456 frontend tests passing
- 100% kernel coverage (gate met — 11421 stmts / 0 missing)
- 35 ESLint warnings + 0 errors (baseline)
- pyright `--strict` clean
- UI production build green (679 KB gzipped, 390ms)

### Refinement

Host-integration stage → Refinement was **3a (dev rig) AND 3b (prod rig)**.

**3a setup:** brought up dev rig (`make dev`); confirmed UI loads + login works + POST `/pull-and-restart` route is wired. Moved directly to 3b for real action execution (dev rig backend runs on host; can't exercise docker socket RW or container build/restart semantics).

**3b production rig:** rebuilt monitor image with new docker CLI + buildx + compose plugins; widened docker socket mount; added `/host-compose:ro` mount; added `HM_HOST_COMPOSE_GID` group_add; added `HOME=/tmp` + `DOCKER_CONFIG=/tmp/.docker` env vars; mounted `/storage/programs:/storage/programs:ro` via auto-generated `docker-compose.override.yml`.

**Acceptance tests:**
- **Remote-image** (foundry, felddy/foundryvtt:release): Pull & Restart succeeded in 1.43s. Image pulled, container recreated with new digest, badge cleared within ~2s, success toast appeared.
- **Locally-built** (ip-update, build-only compose service): Rebuild & Restart succeeded in 19.45s. Build context resolved correctly inside container, baseline source hash reset post-rebuild, badge cleared, success toast appeared.

**5 cascading fix waves uncovered by 3b prod-only failure modes:**
1. **Wave 1 (5 fixes):** phase column (`pulling`/`restarting`), 409 concurrency guard, optimistic UI bridge, image-update cache invalidation, post-success digest recheck for REMOTE images.
2. **Wave 2 (1 fix):** `.env: permission denied` → host-side `homelab-compose` group + container `group_add` + `chmod 0640`. Generic mechanism via `scripts/host-setup-shared-files.conf`.
3. **Wave 3 (3 fixes):** `up -d` container name conflict → added `--force-recreate`. Failed-terminal toast → wired toast.error in ActionsCell. Confirm phrase mismatch → kept `"pull"` for all action types.
4. **Wave 4 (4 fixes):** docker compose project-name mismatch → extract from container's `com.docker.compose.project` label + pass `-p <name>`. Build vs pull detection for locally-built containers → new `building` phase + migration 0030 + `Rebuild & Restart` button label. Build context path mismatch → generate `docker-compose.override.yml` from `build-sources.yaml`. Docker CLI HOME permission → set `HOME=/tmp` + `DOCKER_CONFIG=/tmp/.docker`. Added `docker-buildx-plugin` to Dockerfile.
5. **Wave 5 (1 fix):** local-build `refresh_container` — `image_update_collector.refresh_container` only updated `image_update_state` (remote-image table); locally-built containers' badges driven by `docker_build_hashes` needed a parallel refresh path. Added `LocalBuildUpdateCollector.refresh_container` + branched dispatch in compose_action_runner based on `resolved.is_local_build`.

**UI (USER-approved):** Desktop + Mobile both approved.

### Code review

- Round 1: 21 findings (3 Critical, 8 Important, 5 Minor, 5 Nice-to-have) — ALL addressed.
- Notable correctness fixes from review:
  - **C1+C2 (audit linkage):** `_write_audit_row` was dropping its `audit_id` parameter on the floor. Extended `insert_audit(...)` to accept `audit_id: str | None`; pre-resolution failure path now generates `audit_id = uuid7()` upfront and passes it to BOTH `update_terminal_state` (via `audit_log_id`) AND `_write_audit_row` (via `audit_id`). Audit row + compose_actions row now correctly cross-reference end-to-end.
  - **I1 (state set drift):** `COMPOSE_ACTIVE_STATES` in `docker.ts` was missing `'building'` — local-build polling stopped immediately at the `'building'` state instead of running through to terminal. Fixed and exported as shared constant.
  - **I7 (relative path in conf):** `scripts/host-setup-shared-files.conf` referenced `docker-compose.override.yml` as a relative path; `readlink -f` resolved against wrong cwd. Fixed to absolute path.
  - **I8 (hardcoded desktop user):** `scripts/host-setup.sh` had `COMPOSE_GROUP_DESKTOP_USER="jakekausler"` hardcoded. Now env-overridable via `COMPOSE_GROUP_DESKTOP_USER=… sudo bash scripts/host-setup.sh`, defaulting to `$SUDO_USER` if available.
  - **M3 (queryKey collision):** `useListComposeActions(name, 1)` and `useListComposeActions(name, 10)` shared the same React Query key, causing one query to overwrite the other. Added `limit` to the key + new `listAllForContainer` prefix function for invalidation across limits.
  - **M5 (process group signaling):** `proc.send_signal(SIGTERM)` only killed the parent docker CLI, leaving buildkit/containerd shim children orphaned. Switched to `os.killpg(proc.pid, ...)` with `start_new_session=True` on subprocess.
  - **SQL inline comments in migration 0028:** `--` SQL comments inside a Python string literal commented out the rest of the table definition (no newline → SQLite saw "incomplete input"). Moved comments outside the string as Python `#` comments. This caused a 595-error cascade until fixed.

### Operational notes

- Operator runs `sudo bash scripts/host-setup.sh` once to provision the `homelab-compose` group + apply 0640 to `.env`.
- Operator runs `make generate-build-mounts` after editing `build-sources.yaml` to refresh `docker-compose.override.yml`.
- Operator MUST invoke docker compose with both `-f` flags explicitly: `docker compose -f deploy/compose/docker-compose.yml -f deploy/compose/docker-compose.override.yml ...`. The `-f` flag of the main file disables auto-loading of override files.
- `HM_HOST_COMPOSE_GID` in `.env` defaults to 994 (a sentinel matching the test host). Operators on other hosts MUST run `getent group homelab-compose | cut -d: -f3` after `host-setup.sh` and set the value accordingly.

### EPIC-003 progress

10/12 stages Complete. Current Stage advances to STAGE-003-011.

## STAGE-003-011 — Per-container log viewer

**Epic:** EPIC-003 (Docker)
**Commit:** `26dce07`

### Summary
Implements the per-container log viewer route at `/integrations/docker/containers/$name/logs`. Each container row in the Docker integration grid now has a "View logs →" link that opens a viewer showing recent log lines from VictoriaLogs (default: last 15 minutes, 500-line cap). Reuses `VictoriaLogsClient` (STAGE-002-013) for bounded queries; reuses container inventory from `targets` table (STAGE-003-004); replaces the placeholder component scaffolded in STAGE-003-003.

### What changed
**Backend** (`apps/monitor/`):
- New endpoint `GET /api/integrations/docker/containers/{name}/logs?since=<dur>&limit=<int>` returns the last 500 log lines for a container from VictoriaLogs over a configurable time window.
- New Pydantic models in `kernel/api/routers/docker.py`: `ContainerLogLine`, `ContainerLogsResponse`, `_ContainerLogStatus` (Literal of `available | no_lines | container_unknown | vl_unavailable`).
- Response status semantics: 200 (available/no_lines), 404 (container_unknown, body populated), 503 (vl_unavailable, body populated), 422 (invalid `since` format).
- Silent clamping: `limit` capped at 500, `since` capped at 7d. LogsQL filter uses `service:"<container_name>"` with `logsql_quote_phrase()` escaping.
- 17 new tests in `tests/test_api_docker_container_logs.py` covering all four log_status states, all clamping paths, all 422 paths, escape correctness, and auth.

**Frontend** (`apps/ui/`):
- New component `routes/integrations/DockerContainerLogsViewer.tsx` with: sticky metadata header (name, status badge, last-log-at timestamp, since picker, Refresh button), scrollable monospace log body, sticky truncation banner when `truncated=true`. Four log_status UX states each handled (lines, empty state, 404 page, vl_unavailable banner).
- New hook `useContainerLogs(name, since)` in `api/docker.ts` with `dockerLogsQueryKeys` factory. No `refetchInterval` (manual refresh only — per D-MANUAL-REFRESH-V1).
- Router updated: `/integrations/docker/containers/$name/logs` now renders the new viewer (was `ContainerLogsPlaceholder`).
- "View logs →" link added to `ContainerGridRow.tsx` (desktop) and `ContainerGridCard.tsx` (mobile).
- 12 new vitest cases in `routes/integrations/__tests__/DockerContainerLogsViewer.test.tsx`. 26 pre-existing grid tests converted to `async + findBy*` to work with the new `<Link>` requiring async router context hydration.
- Deleted `ContainerLogsPlaceholder.tsx` + its test.

**OpenAPI / shared-types:**
- `packages/shared-types/openapi.json` regenerated.
- `apps/ui/src/api/schema.ts` regenerated (gitignored, local only).

**Forward references:**
- TODO comments in `DockerContainerLogsViewer.tsx` AND `CronRunLogViewer.tsx` reference EPIC-004 STAGE-004-005 (cursor-based pagination + custom datetime range picker). Both log viewers will be updated together at that stage.
- `epics/EPIC-004-logs-pipeline/EPIC-004.md` STAGE-004-005 has explicit acceptance criteria for the deferred pagination + custom-range work.

### Locked design decisions
- D-LOG-LABEL-SERVICE: LogsQL filter `service:"<name>"`
- D-DEFAULT-WINDOW-15M: 15m default
- D-WINDOW-PRESETS-6: 5m / 15m / 1h / 6h / 24h / 7d
- D-TRUNCATION-BANNER-500: 500-line cap + sticky banner (no pagination this stage)
- D-DEFER-PAGINATION: pagination deferred to EPIC-004
- D-DEFER-CUSTOM-RANGE: custom datetime range deferred to EPIC-004
- D-LOGS-COLUMN-WIRING: link constructed client-side (no `logs_url` backend field)
- D-REFINEMENT-3A-ONLY: frontend-only; no host-integration delta requiring 3b

### Refinement
Tested against rebuilt prod stack at `http://127.0.0.1:29090`. Desktop AND Mobile both approved by user. No code changes during Refinement.

### Code review
Pre-test review: APPROVED (5 Minor + 5 Suggestion findings, no Critical/Important). 5 fixes applied (VlQueryLimits import hoisted, limit-cap comment, error-message prefix, "last:" ordering comment, test docstring correction). Post-fix re-review: APPROVED.

### Verification
- `make verify` exit 0.
- Backend: 3114/3114 tests pass, 100% kernel coverage gate maintained.
- Frontend: 468/468 vitest pass, eslint 35 warnings (baseline), tsc clean, build smoke pass.

## STAGE-003-012 — Per-container probe management + Docker drill-down completion

**Epic:** EPIC-003 (Docker)
**Commit:** `3c803b8`

### Summary
Completes EPIC-003 with a redesigned Probes section on the Docker integration page. Originally scoped as "wire Accept/Customize/Ignore buttons on the existing SuggestionCard panel + clean up drill-down stubs." During Refinement, scope expanded into a full UX redesign per user feedback: per-container probe cards (1:1 with the container grid) showing Active probes + Suggested defaults + Add new probe button, with optimistic mutations for instant feedback. New REST endpoints for direct probe-target CRUD bypassing the per-suggestion flow. Old SuggestionCard/CustomizeSuggestionModal/PendingSuggestionsPanel removed.

### What changed

**Backend** (`apps/monitor/`):
- **Suggestion-action endpoints** (initially the main work, but the UI built on top was later replaced):
  - `POST /api/integrations/docker/suggestions/{id}/accept` — accepts a suggestion, optionally inserts default probes from docker inspect (D-ACCEPT-INVENTORY-ONLY-WITH-CONSERVATIVE-PROBES: requires both exposed ports AND healthcheck for auto-probes)
  - `POST /api/integrations/docker/suggestions/{id}/customize` — accepts + upserts user-specified probe list
  - `POST /api/integrations/docker/suggestions/{id}/ignore` — archives suggestion (D-IGNORE-FROM-ANY-STATE)
  - `GET /api/integrations/docker/suggestions/{id}/default-probes` — returns probes that Accept would create (preview)
  - All endpoints idempotent at REST (Accept-already-accepted → 200 no-op; Customize-already-accepted upserts; Ignore-any-state). Customize on ignored/container_gone → 409.
  - All write paths emit audit_log entries.
- **Probe-target CRUD endpoints** (the endpoints the new UI actually uses):
  - `POST /api/integrations/docker/probe-targets` — create new probe row (`config_source="manual"`)
  - `PATCH /api/integrations/docker/probe-targets/{id}` — partial update of target_value / interval_seconds / timeout_seconds (kind + name + container_name immutable). Empty-body PATCH short-circuits before auditing.
  - `DELETE /api/integrations/docker/probe-targets/{id}` — physical delete (204). NOT a soft-hide.
- **New Pydantic models**: ProbeSpec, SuggestionAcceptRequest/Response, SuggestionCustomizeRequest/Response, SuggestionIgnoreResponse, SuggestionDefaultProbesResponse, CreateProbeTargetRequest, UpdateProbeTargetRequest.
- **New helper**: `_default_probes_from_inspect` — computes default probes from docker inspect dict (tcp-per-port + exec-for-healthcheck). Supports CMD/CMD-SHELL forms; correctly handles missing PortBindings or NONE healthcheck.
- **New repo methods**: `SuggestionsRepository.get_docker_suggestion_by_id`, `set_state_conn`; `ProbeTargetsRepository.update_probe_target_conn`, `delete_probe_target_conn`.
- **Tests**: 65+ new backend tests across `test_api_docker_suggestions_actions.py` (40+ cases including endpoint, helper unit, exception paths) and `test_api_docker_probe_targets_crud.py` (20 cases). 100% kernel coverage gate met.

**Frontend** (`apps/ui/`):
- **New components**: `ProbesPanel.tsx`, `ContainerProbesCard.tsx`, `AddProbeModal.tsx`, `EditProbeModal.tsx`.
- **New hooks**: `useSuggestionDefaultProbes`, `useAcceptDockerSuggestion`, `useCustomizeDockerSuggestion`, `useIgnoreDockerSuggestion`, `useCreateProbeTarget`, `useUpdateProbeTarget`, `useDeleteProbeTarget`. All CRUD mutations implement optimistic updates (onMutate cancels in-flight queries, snapshots cache, applies optimistic change; onError rolls back from snapshot; onSettled invalidates for refetch).
- **204 handling**: `useDeleteProbeTarget` short-circuits on 204 before calling unwrap (matches `useHideCron` pattern).
- **DockerIntegrationPage**: section header renamed "Probes"; `<ProbesPanel />` replaces `<PendingSuggestionsPanel />`. "Show missing" filter and its supporting state removed.
- **Tests**: 25+ new frontend tests (ProbesPanel, ContainerProbesCard, AddProbeModal, EditProbeModal, plus direct hook tests in `api/__tests__/docker.test.ts`). Coverage gates met (functions 83.6%, statements 80%+, branches 75%+).
- **Deleted** (replaced by new Probes section): `SuggestionCard.tsx`, `CustomizeSuggestionModal.tsx`, `PendingSuggestionsPanel.tsx` + their test files.
- **EPIC-011 cross-reference comments** added to `ProbesPanel.tsx`, `ContainerProbesCard.tsx`, `docker.ts`, and the backend `/suggestions/*` endpoints (orphaned by the UI refactor but kept on backend for EPIC-011 to subsume).

**OpenAPI / shared-types**: `packages/shared-types/openapi.json` regenerated; `apps/ui/src/api/schema.ts` regenerated.

**Audit cleanup**: removed stale `// SCAFFOLDING: STAGE-003-004` comments from ContainerGridCard.tsx; removed unused field documentation from types.ts.

### Locked design decisions
- D-ACCEPT-INVENTORY-ONLY-WITH-CONSERVATIVE-PROBES — Accept inserts probes only if container has both exposed ports AND healthcheck
- D-CUSTOMIZE-FORM-ROWS — repeatable probe rows in modal (superseded by D-PROBES-PANEL-REFACTOR which moved this into ContainerProbesCard's Add Probe modal)
- D-IGNORE-FROM-ANY-STATE — Ignore accepts suggestion in any state, does NOT touch probe_targets
- D-IDEMPOTENT-200 — Accept/Ignore on already-target-state returns 200 no-op
- D-CLIENT-IGNORE-STATE — per-default-probe Ignore is client-side React state only; persistence deferred to EPIC-011
- D-OPTIMISTIC-UPDATES — All probe mutations use React Query optimistic lifecycle
- D-PROBES-PANEL-REFACTOR (locked during Refinement) — per-container probe cards replace per-suggestion cards; 1:1 with container grid; no "show missing" filter

### Refinement
Two rounds of scope expansion during Refinement:
1. Default-probes preview endpoint + SuggestionCard preview text + CustomizeModal prefill (built then partly superseded by round 2)
2. Full UX refactor to per-container Probes section (final shipped design)

User testing: added 2 probes to `podcast-feed` via the new flow, verified cross-surface consistency with the per-container drill-down. Both Desktop AND Mobile approved against rebuilt prod stack on `127.0.0.1:29090` after the second iteration. Test probes deleted from DB after approval.

### Code review
Pre-test review: APPROVED with 6 Important + 12 Minor/Suggestion findings. 8 mechanical fixes applied (204 short-circuit bug fix, optimistic id collision-resistance, empty-body PATCH audit-clutter cleanup, disable optimistic-row buttons, loop variable rename, status constants, CSRF helper cleanup, pragma re-evaluation). Post-fix review: APPROVED with critical re-application of `# pragma: no cover` on the accept-loop (instrumentation gap is real; pragmas needed for 100% kernel coverage gate).

### Verification
- Final `make verify`: exit 0
- Backend: 3181/3181 tests pass; 1 skipped (test_scheduler hash-collision platform-dependent); 100% kernel coverage gate met
- Frontend: builds clean; ESLint 35 warnings (baseline); function coverage 83.57% (threshold 80%); UI build smoke pass
- Pre-existing flakes (NOT regressions): `test_lifespan_wires_docker_discoverer_to_app_state` (httpx_mock teardown leakage under xdist)

### Critical gotcha (CLAUDE.md candidate)
After ANY change to `apps/ui/src/*`, run `pnpm --filter ui run build` BEFORE `docker compose build monitor`. The Dockerfile at `deploy/compose/Dockerfile` (line 72) does `COPY apps/ui/dist /app/ui` — there's no UI build step in the image. Skipping this ships a stale UI bundle and the symptom is "the new layout doesn't show" even after the container restarts.

### Forward references
- EPIC-011 STAGE-011-001 (when opened) MUST decide: (1) PendingSuggestionsPanel-replacement fate; (2) endpoint path migration `/integrations/docker/suggestions/*` → `/suggestions/*`; (3) persistent per-default-probe ignore (currently client-side only).
- User intends a follow-up stage to unify all container actions into per-container drill-down pages (move probes/logs/image-updates from the integration page proper to `/integrations/docker/containers/$name/...` and slim down the integration grid). Discussion deferred to after this stage's Finalize.
