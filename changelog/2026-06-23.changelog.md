2026-06-23 1dc310c EPIC-006/STAGE-006-017: vmalert-logs FTL alert rules

Added 4 LogsQL alert rules over the Pi-hole/unbound container's docker-stdout stream (`service:="pihole-unbound"`) in `deploy/vmalert/logs/pihole.yaml` (group `pihole_logs`, `type: vlogs`), mirroring the EPIC-005 home-assistant container-logs sibling: `PiholeFtlRateLimit` (warning, matches `Rate-limiting`), `PiholeFtlError` (warning, matches the FTL `ERROR:` token), `PiholeGravityUpdateFailedLog` (warning, matches a gravity whole-run-failure phrase set — deliberately NOT the recurring benign `was inaccessible during last gravity run` baseline from the 2 permanently-dead NRD blocklists), `PiholeDbMaintenanceAnomaly` (info, matches SQLite/DB-failure phrases — disjoint from the routine `Optimized database` success line). Each rule ends in `| stats by (host) count() | filter :>0`; labels `severity` + `source_tool: vmalert-logs` + `target_kind: container` + `integration: pihole`. Added the test mirror `deploy/vmalert/logs-test/pihole.yaml` (shorter windows) and the integration test `apps/monitor/tests/integration/test_vmalert_pihole_logs_pipeline.py` (1 rules-load + 4 planted-line fire tests). Validated at Refinement: all 4 rules `health=ok` on the live vmalert-logs, silent at the real baseline (no false-fire), and all 4 fire on planted lines in the integration rig. Also fixed a pre-existing stale assertion in `test_vmalert_logs_pipeline.py::test_kernel_oom_log_alert_fires` (`error`→`critical`, left stale by STAGE-005A-003's severity-vocabulary refactor).

---

2026-06-23 5c266ec EPIC-006/STAGE-006-016: vmalert-metrics alert rules (22 Pi-hole/unbound/dns rules)

Added `deploy/vmalert/metrics/pihole.yaml` — 22 vmalert-metrics alert rules in three groups (`pihole` 16 / `unbound` 4 / `dns` 2) over the metrics emitted by collectors 005–015, plus a promtool unit-test mirror (`__tests__/pihole.tests.yaml`) with a FIRE + NO-FIRE case per rule. Severities: 8 critical / 10 warning / 4 info (info|warning|critical only). All rules carry `severity` + `source_tool: vmalert-metrics` + `category: dns` + `integration: pihole` (the EPIC-007 integration label convention).

Rules: critical — PiholeDown, PiholeContainerDown, PiholeBlockRateCollapsed, PiholeUpstreamAllDown, PiholeDnsProbeFailing, PiholeDnsSplitDivergence, UnboundServfailSpike, UnboundDnssecBogusSpike. warning — PiholeBlockingDisabled, PiholeBlockingDisabledIndefinitely, PiholeUpstreamDown, PiholeGravityStale, PiholeGravityDomainsDropped, PiholeAdlistFailing, PiholeMessagesPresent, PiholeDbTooLarge, PiholeClientFlooding, UnboundRecursionSlow. info — PiholeUpdateAvailable, PiholeBlockRateSpike, PiholeApiSlow, UnboundCacheHitLow.

Design notes for the load-bearing PromQL forms (proven by promtool fire-tests, not just syntax-checks): PiholeUpstreamAllDown uses `absent(homelab_pihole_upstream_queries{upstream!~"cache|blocklist"} > 0)` (NOT `count()==0`, which never fires on an empty vector); PiholeDnsSplitDivergence uses `...{path="pihole"}==0 and on() ...{path="direct"}==1` (the `and on()` is required — a bare `and` across differing path selectors yields empty); PiholeContainerDown uses `absent(...{state="running"}) or ...{state=~"exited|dead|restarting"}==1` over EPIC-003's `homelab_container_status`. Upstream rules exclude the always-populated `cache`/`blocklist` pseudo-upstreams. No `rate()` on Pi-hole's 24h-rolling gauges; `rate()` is used only on unbound's real counters (SERVFAIL/bogus). PiholeDnsSplitDivergence lives in the `dns` group because Pi-hole is the subject; EPIC-016 owns the `path="direct"`-fails (WAN) rules over the shared `homelab_dns_resolution_*` family.

Validated: promtool `test rules` SUCCESS (fire + no-fire per rule) + `check rules` SUCCESS (22 rules). Live 3b on the prod vmalert-metrics (bind-mounted rules dir, hot-reload via `-configCheckInterval=30s`): all 22 load `health=ok` with empty `lastError`, NO false-fire at the real baseline. Two thresholds were tuned at 3b against real data: PiholeClientFlooding `>50000`→`>300000` (the busiest real client runs ~77k queries/24h — `homelab_pihole_client_queries` is a 24h-rolling gauge, so that is normal daily activity), and UnboundCacheHitLow `<0.5`→`<0.01` (3b found unbound extended-stats ARE active, contrary to the initial assumption, and a ~4% unbound cache-hit ratio is structurally normal in a Pi-hole→unbound topology — Pi-hole's own dnsmasq cache absorbs the easy hits, so unbound mostly sees forwarded misses). Three rules correctly fire at the real baseline as TRUE positives on the user's Pi-hole (PiholeAdlistFailing — real parse-failed adlists; PiholeMessagesPresent; PiholeUpdateAvailable). Code review (Opus, Level 2) APPROVED. YAML-only stage; `make verify` GREEN with no Python/TS change (vmalert rule tests run via promtool, not `make verify`).

---

2026-06-23 4b453cb EPIC-006/STAGE-006-015: DNS split-check (Pi-hole vs direct 1.1.1.1)

Added the `pihole_dns_split` collector (`name="pihole_dns_split"`, 60s, `concurrency_group="pihole"`) — the disambiguation signal that distinguishes "Pi-hole broken" from "WAN/internet down". Each cycle it resolves `dns.google.com` via TWO independent paths from the monitor: the Pi-hole path (`:53` at the host LAN IP) and a DIRECT path to Cloudflare `1.1.1.1:53` that bypasses Pi-hole entirely. It reuses the STAGE-006-014 `resolve_a` UDP helper (one call per path) and is a stateless per-path emitter — the divergence logic lives in STAGE-006-016 PromQL, not here.

Emits a NEW shared, cross-epic metric family (deliberately `homelab_dns_*`, not `homelab_pihole_*`): `homelab_dns_resolution_up{path}` (gauge 1/0, always emitted for both paths; 1 iff the path resolves), `homelab_dns_resolution_seconds{path}` (latency, emitted only on a real round-trip; omitted on no-response), and `homelab_dns_resolution_probe_result{path,outcome}` (one-hot, always one series per path). `path ∈ {"pihole","direct"}`. STAGE-006-015 is the sole owner of this family; EPIC-016 STAGE-016-005 consumes it for WAN-health context and STAGE-006-016 alerts on it (`PiholeDnsSplitDivergence` = `up{path="pihole"}==0 and up{path="direct"}==1`). Both-fail (WAN down) emits both `up=0` and produces NO divergence alert (the `direct==1` term is false) — documented as a WAN problem, not a Pi-hole alert.

The three DNS-outcome constants (`PROBE_QNAME`, `OUTCOME_BY_ERROR`, `RESPONSE_OUTCOMES`) were extracted from the 014 collector into a new shared `kernel/dns/outcomes.py` (made public, re-exported via `kernel/dns/__init__.py`) so both the 014 and 015 collectors import one copy — no cross-collector duplication, no import cycle. Config adds `PiholeConfig.direct_dns_host` (env `HOMELAB_MONITOR_PIHOLE_DIRECT_DNS_HOST`, default `1.1.1.1`) + `direct_dns_port` (env `HOMELAB_MONITOR_PIHOLE_DIRECT_DNS_PORT`, default 53), giving the direct/WAN anchor a config escape hatch; the resolver hosts/ports are injected into the collector post-construction in `lifespan.py` (mirroring the 014 wiring). Backend-only; no migration, no API/frontend change. 100% branch coverage.

Validated live on the prod rig (`make dev-prod` + collector retry): both paths `up=1`, `outcome="ok"`, with pihole ~0.5ms (local resolver) and direct ~12.7ms (WAN round-trip to Cloudflare). The load-bearing WAN-`:53`-interception check passed — `dig @1.1.1.1 CH TXT id.server` returned `atl15` (Cloudflare Atlanta PoP), proving the "direct" probe genuinely reaches Cloudflare and is NOT silently DNAT-redirected to the local resolver (the ~25× latency gap independently corroborates). Code review (Opus, Level 2) APPROVED with zero findings.

---

2026-06-23 4c8c752 EPIC-006/STAGE-006-014: DNS health probe (composite up + direct :53 probe)

Added the `pihole_dns_health` collector (`name="pihole_dns_health"`, 30s, `concurrency_group="pihole"`) — the epic's truest "is Pi-hole actually resolving" signal. It sends a real DNS A-record query for `dns.google.com` from the monitor over UDP directly to Pi-hole's `:53` listener (host LAN IP `192.168.2.148:53`) and emits a DNS-decisive composite `homelab_pihole_up`. Because the probe originates from the monitor and is independent of the Pi-hole REST API, it catches the "API up but DNS wedged" failure that an API-only check would mask.

The DNS query is a new pure-stdlib async UDP helper in `kernel/dns/` (`resolve_a(resolver_ip, qname, *, port=53, timeout_seconds) -> DnsProbeResult`): it builds a minimal A-record query (12-byte header + QNAME-encoded question, QTYPE=A, QCLASS=IN), sends it over `asyncio` datagram transport, and parses only the QR/RCODE/TC bits + ANCOUNT (no answer-record decompression). It NEVER raises — timeout, socket error, malformed/too-short response, ID mismatch, and a non-ASCII qname all map to a typed `DnsProbeResult`. It is parameterized by resolver IP so STAGE-006-015's Pi-hole-vs-WAN split-check can reuse it unchanged.

Metrics: `homelab_pihole_up` (gauge 1/0, always emitted — 1 iff the probe resolves: QR set, RCODE NOERROR, ANCOUNT≥1, not truncated); `homelab_pihole_dns_probe_seconds` (latency gauge — emitted only on a real round-trip, i.e. when a response was received even SERVFAIL/NXDOMAIN/REFUSED/no-answer/truncated; omitted on timeout/socket-error/malformed/id-mismatch so latency graphs aren't polluted); and `homelab_pihole_dns_probe_result{outcome}` (one-hot gauge, exactly one series per run; outcome ∈ ok/timeout/servfail/nxdomain/refused/malformed/socket_error/id_mismatch/truncated/no_answer). The composite is DNS-decisive only — the API-reachability signal is NOT folded in (STAGE-006-016 builds the richer "DNS up but API down" view from the separate `homelab_collector_run_*` / `homelab_pihole_api_took_seconds` series).

Config: added `PiholeConfig.dns_host` (env `HOMELAB_MONITOR_PIHOLE_DNS_HOST`, deriving from the base_url hostname when empty) + `dns_port` (env `HOMELAB_MONITOR_PIHOLE_DNS_PORT`, default 53), giving 015 a clean home for a second resolver. The resolver host/port are injected into the collector post-construction in `lifespan.py` (mirroring the 013 UnboundStatsCollector wiring). Backend-only; no migration, no API/frontend change. 100% branch coverage; validated live on the prod rig (`make dev-prod` + collector retry → `homelab_pihole_up 1.0`, `outcome="ok"`, ~1ms latency against the real Pi-hole DNS, proving UDP egress from the bridge network works). Also fixed a pre-existing seed-dependent test-isolation flake in `tests/test_api_docker_pull_and_restart.py` (its lifespan-enabled tests start the full scheduler; on a `PYTHONHASHSEED` where the unbound collector's hash-offset is 0, its docker-exec tick fired inside the test's strict httpx mock window — the autouse mock now tolerates the `/containers/.../exec` + `/exec/.../start|json` endpoints, optional+reusable).

---

2026-06-23 aeaade6 EPIC-006/STAGE-006-013: Unbound recursive-resolver stats collector (docker-exec)

Added the `unbound_stats` collector (`name="unbound_stats"`, 60s, `concurrency_group="pihole"`) — the recursive-resolver visibility collector. Consumes the 006-003 access layer (`fetch_unbound_stats`), running `unbound-control stats_noreset` inside the `pihole-unbound` container via the shared Docker socket. Gracefully degrades to the default key set when extended-statistics is off.

Always emits (default set): `homelab_unbound_queries_total`, `_cache_hits_total`, `_cache_misses_total`, `_cache_hit_ratio` (derived; skipped on zero denominator), `_prefetch_total`, `_recursion_time_seconds{quantile="avg"|"median"}`, `_requestlist_current`, `_requestlist_exceeded_total`, the bundle-scoped flag `homelab_pihole_unbound_extended_stats_enabled` (1/0), and `homelab_pihole_api_took_seconds{endpoint="unbound/stats_noreset"}` (collector wall-clock — the access layer reports no timing). When extended-stats is on, ALSO emits histogram-derived `_recursion_time_seconds{quantile="0.5"|"0.95"|"0.99"}`, `homelab_unbound_query_type{type}`, `homelab_unbound_answer_rcode{rcode}` (incl. lowercase `nodata`), `homelab_unbound_answer_secure_total`, `homelab_unbound_answer_bogus_total`.

Recursion-time quantiles are derived in a pure helper by linear interpolation over unbound's log-scale `histogram.*` buckets (parsing the `histogram.{lo}.to.{hi}` second bounds, cumulative walk, clamp to bucket hi); the quantiles are skipped (not emitted as 0) on an empty/zero-total histogram while avg/median still emit from `total.recursion.time.*`. Source keys read `total.*` preferred with a `thread0.*` fallback. unbound counters are real monotonic counters (safe for `rate()`), unlike Pi-hole's 24h-rolling summary gauges.

Docker-socket access: the collector takes `socket_client`/`unbound_config` via `__init__` and is wired post-construction in `lifespan.py` (a new `isinstance` block reusing `app.state.docker_socket_client` + `load_pihole_unbound_config()`), mirroring the DockerSocketCollector/ProbeSupervisor injection pattern. When Docker is disabled (no socket) the run() guard degrades cleanly (`ok=False, errors=["client_unconfigured"]`, 0 emits). Backend-only; no migration, no API/frontend change. 100% branch coverage; validated live against the prod `pihole-unbound` container (all 10 metric families matched `unbound-control` ground truth; histogram quantiles ordered + plausible on real data).

---

2026-06-23 cb69549 EPIC-006/STAGE-006-012: Per-client (Tier-2) collector — top-N clients + top domains, capped + loopback-attributed

Added the `pihole_clients` collector (`name="pihole_clients"`, 60s, `concurrency_group="pihole"`) — the Tier-2 per-client DNS visibility collector. Polls 5 Pi-hole v6 endpoints per tick (`/api/stats/top_clients`, `?blocked=true`, `/api/stats/top_domains`, `?blocked=true`, `/api/network/devices`) with per-endpoint resilience (`ok = any` of the 4 primary endpoints; `network/devices` is MAC-enrichment-only and never fails the run). Requests `count = cap + 10` so the cardinality cap is exercisable.

Emits (all capped to top-N, default 50, via the EPIC-005 cap layer + 006-004 client classification): `homelab_pihole_client_queries{client_ip,client_name,client_kind,host_lan_ip,client_mac}`, `homelab_pihole_client_blocked{...same...}`, `homelab_pihole_top_blocked_domain{domain}`, `homelab_pihole_top_permitted_domain{domain}`, the shared `homelab_pihole_api_took_seconds{endpoint}` (5 new endpoint labels), and `homelab_metric_family_dropped_series{family}` (one per family, always emitted even when 0). Clients are classified ONCE on the unfiltered top-clients list (loopback structurally cap-exempt, attributed to `host_lan_ip` when configured); the same label set feeds both client families (symmetric membership), with the blocked count looked up by IP. MAC enrichment flattens `network/devices` `ips[].ip → hwaddr`.

Label set is a STABLE 5 keys per series (`host_lan_ip`/`client_mac` default to `""` when absent, NOT omitted) — required because the prometheus_writer pins the first-seen label-key set per metric and silently drops series with a divergent key set. (Note: VictoriaMetrics drops empty-string labels on storage, so stored series show a variable key set — verify the stable set at the exposition layer. `client_mac` may legitimately be FTL's synthetic `ip-<addr>` for MAC-less/loopback clients.)

Also: extended `PiholeRestClient.stats_top_clients`/`stats_top_domains` (+ the `PiholeClient` Protocol) with keyword-only `blocked: bool` / `count: int | None` params (blocked variants get distinct `*_blocked` api_took endpoint labels); added cardinality-cap families `pihole_client_queries: 50` and `pihole_top_domains: 50` to `_DEFAULT_CARDINALITY_FAMILIES`. Backend-only; no migration, no API/frontend change. 100% branch coverage; validated live against the prod Pi-hole (3b caught + fixed a real label-set drop bug).

---

2026-06-23 ff6c39d EPIC-006/STAGE-006-011: Pi-hole version / update-available collector

Adds `PiholeVersionCollector` (`pihole_version`), a single-endpoint collector polling Pi-hole v6 `/api/info/version` on a 1h cadence to surface, per component (core/web/ftl/docker), whether a software update is available.

Metrics emitted:
- `homelab_pihole_update_available{component}` (gauge 1/0) = `1.0` when the installed (local) version differs from the available (remote) version, `0.0` when equal. Plain string inequality (no semver — Pi-hole's `v`-prefix and 2-vs-3-segment versions make semver fragile). Emitted ONLY when BOTH local and remote are present non-empty strings; a missing/empty version → NO series for that component (absence = "unknown", so the `PiholeUpdateAvailable` alert never fires on a falsely-fabricated `0`).
- `homelab_pihole_version_info{component, version}` (info-gauge, value `1.0`, the installed LOCAL version string as the `version` label) — emitted whenever the local version is present (decoupled from remote, so the dashboard always shows what's installed). The remote/latest string is fetched at panel render-time, not stored as a metric.
- `homelab_pihole_api_took_seconds{endpoint="info/version"}` per run.

Iterates whatever components are PRESENT in `payload["version"]` (no hardcoded enum). A shape-driven `_extract_versions` / `_version_str` helper handles the API's shape divergence: core/web/ftl nest `local`/`remote` as objects with a `.version` key, but `docker`'s `local`/`remote` are BARE STRINGS — the helper branches on runtime shape (`isinstance str` vs `dict`), not on component name. No cardinality cap (4 components × slowly-rolling version labels = bounded; mirrors the Unifi `firmware_info` info-gauge precedent).

13 unit tests at 100% branch (both shapes × both outcomes, plus missing-remote / empty-string-remote / missing-local / non-dict-component / dict-without-version-subkey edge cases). Live-validated on prod against the real Pi-hole v6.6.2: web (`v6.5`→`v6.5.1`) and docker (`2026.05.0`→`2026.06.0`) correctly report `update_available=1`; core/ftl report `0`; `version_info` carries the correct installed-version labels including docker's bare-string version.

Tracked follow-up (STAGE-006-020, no silent deferral): the 1h-interval collector does not run on scheduler startup, so version metrics are absent for up to ~1h after a monitor restart. Adding a startup-run hook (like the sibling update-checkers' `await_immediate_run` blocks in `lifespan.py`) is a cross-cutting change to evaluate when the version widget is built — recorded as a deliverable note on STAGE-006-020 + a regression item.

---

2026-06-23 ce24b8b EPIC-006/STAGE-006-010: Pi-hole FTL diagnostic-messages collector

Adds `PiholeFtlMessagesCollector` (`pihole_ftl_messages`), a single-endpoint collector polling Pi-hole v6 `/api/info/messages` every 60s to surface FTL's diagnostic messages (inaccessible adlists, high-load warnings, config issues, etc.) as metrics.

Metrics emitted:
- `homelab_pihole_messages_count` (gauge) = total active-message count (`len(messages)`), ALWAYS emitted including `0` when there are no messages — so the planned `PiholeMessagesPresent` alert can fire on `> 0` without needing `absent()`.
- `homelab_pihole_messages{type}` (gauge) = per-type COUNT, grouped by each message's `type` field (duplicate types collapse: two `type="LIST"` messages → `{type="LIST"}=2`, not two series). Present types only (no zero-fill, no hardcoded FTL enum). A non-string/missing `type` falls back to `{type="unknown"}`; non-dict message entries are skipped.
- `homelab_pihole_api_took_seconds{endpoint="info/messages"}` per run.

The message text bodies (`plain`/`html`) are deliberately NOT emitted as metric labels (cardinality); they are fetched at panel render-time / alert-annotation render-time (a future UI/alert stage's concern). No cardinality cap (the FTL message-type enum is a small bounded internal set).

Design note: the card named the per-type metric `homelab_pihole_message{type}` (singular), which implied one series per message; live recon showed duplicate types must collapse to a count, so the metric was corrected to the plural `homelab_pihole_messages{type}` with per-type-count semantics. Documented semantic: `messages_count` = total list length (includes any malformed entries); the per-type breakdown groups only well-formed entries, so sum(per-type) may be < count if malformed entries are present.

10 unit tests at 100% branch (happy path, empty list, malformed payload/entry, unknown-type fallback, all failure paths). Live-validated on prod against the real Pi-hole v6.6.2: 3 messages → `messages_count=3`, correctly grouped to `{type="LIST"}=2` + `{type="LOAD"}=1`.

---

2026-06-23 ebd4e3f EPIC-006/STAGE-006-009: Pi-hole FTL-health + DB collector

Adds `PiholeFtlHealthCollector` (`pihole_ftl_health`), a two-endpoint collector polling Pi-hole v6 `/api/info/ftl` (FTL process health) and `/api/info/database` (long-term DB metrics) every 60s. Mirrors the `gravity.py` two-endpoint pattern with per-endpoint resilience (`ok = ftl_ok or db_ok`).

Metrics emitted:
- From `/api/info/ftl` (fields nested under the top-level `ftl` object): `homelab_pihole_ftl_uptime_seconds`, `_ftl_cpu_percent`, `_ftl_memory_percent`, `_privacy_level`, `_dnsmasq_cache_insertions`, `_dnsmasq_cache_evictions`.
- From `/api/info/database`: `homelab_pihole_db_size_bytes` and `homelab_pihole_db_queries_total` (the on-disk total `queries_disk`, ~11.6M live — not the in-memory window).
- `homelab_pihole_api_took_seconds{endpoint}` per endpoint.

Does NOT re-emit `homelab_pihole_gravity_domains` (STAGE-006-007 owns it) and does NOT emit host CPU/mem (node-level scope-out). 16 unit tests at 100% branch (full two-endpoint resilience matrix + a regression test pinning the `payload["ftl"]` nesting).

Refinement caught a real bug: the collector initially read FTL fields off the top-level payload, but Pi-hole v6 nests them under an `ftl` key, so all 6 FTL metrics were silently absent in production despite green unit tests (the fixtures were mis-shaped to match the buggy code). Fixed to descend into `payload["ftl"]`; fixtures re-nested; regression test added. Live-validated on prod (10/10) against the real Pi-hole v6.6.2: FTL process counters + the ~1.1 GB DB size + ~11.6M on-disk query count all confirmed.

Two follow-ups tracked (no silent deferral): `homelab_pihole_query_logging_enabled` deferred to STAGE-006-018 (lives in `/api/config`, not `/api/info/ftl`); and a latent `PiholeRestClient._get()` gap (does not detect a 200-response carrying an `{"error": ...}` body — 5-collector blast radius) recorded as a STAGE-006-018 deliverable + regression item.

---

2026-06-23 ee3239a EPIC-006/STAGE-006-008: Pi-hole blocking-state collector

Adds `PiholeBlockingCollector` (`pihole_blocking`), a single-endpoint collector polling Pi-hole v6 `/api/dns/blocking` every 30s to surface whether DNS blocking is currently active — a common foot-gun is disabling blocking temporarily and forgetting.

Metrics emitted:
- `homelab_pihole_blocking_enabled` — gauge `1.0` iff the API `blocking` field equals `"enabled"`, else `0.0` (fail-closed: `disabled`/`failed`/`unknown`/non-string/missing all map to `0.0`, i.e. "not protecting").
- `homelab_pihole_blocking_timer_seconds` — gauge of remaining seconds, emitted ONLY when a temporary-disable timer is active; OMITTED (never zeroed) when no timer is set, so the "disabled indefinitely" case is distinguishable from "disabled with auto-re-enable" via series presence/`absent()`. A legitimate `timer == 0` (0s remaining) IS emitted as `0.0`.
- `homelab_pihole_api_took_seconds{endpoint="dns/blocking"}` — per-call API latency (standard).

Mirrors the `upstreams.py` collector pattern (object-local → isinstance → cast payload narrowing, no source `# pyright: ignore`, `_parsing.as_float` reuse). Registered in the Pi-hole bundle `__init__.py`. 14 unit tests at 100% branch coverage, plus a `timer=0`-boundary contract test. Live-validated on the prod stack against the real Pi-hole (v6.6.2): `_blocking_enabled=1` matching the live enabled state, `_api_took` ~43µs, `_blocking_timer_seconds` correctly absent (live `timer=null`), self-metric `homelab_collector_run_success_total{name="pihole_blocking"}` incrementing.

Consumed by the planned STAGE-006-016 alerts `PiholeBlockingDisabled` (`_blocking_enabled == 0` for >5m) and `PiholeBlockingDisabledIndefinitely` (`absent(_blocking_timer_seconds)` for >15m — `for:` must exceed VM staleness; note recorded on the 016 card).

---
2026-06-23 a46d781 EPIC-008/STAGE-008-012: Updates & packages collector (DSM update + package-update join)

Added `SynologyUpdatesCollector` (`plugins/collectors/integrations/synology/updates.py`), a co-equal
multi-fetch collector (mirrors the Hyper Backup collector) for DSM update availability + installed-package
state (1h cadence, `synology` concurrency group). Three co-equal fetches: `upgrade_check()`
(`SYNO.Core.Upgrade.Server` v4), `package_list()` (`SYNO.Core.Package` v1), `package_server_list()`
(`SYNO.Core.Package.Server` v2 — the Synology catalogue). Emits always-on alertable signals
`homelab_synology_dsm_update_available` + `homelab_synology_dsm_update_is_security` (0-baselines seeded so a
failed fetch still emits the 0), a `homelab_synology_dsm_update_info{available_version,type}` identity series
when an update is available, `homelab_synology_package_info{package,name,version}` +
`homelab_synology_package_count` per installed package, and a derived
`homelab_synology_package_update_available{package}` (per installed package, from a join of the installed
list against the non-beta server catalogue, matched on id + string-inequality on version) +
`homelab_synology_packages_with_updates_count`. Follows the Wave-B alerting philosophy so STAGE-008-022's
update/package rules can fire.

Validated live (first populated live stage): `ok=True`, 49 metrics, `dsm_update_available=1`,
`is_security=1`, info labels "DSM 7.3.2-86009 Update 3"/"nano", 17 packages, and the version-diff join
correctly found 4 packages with updates (FileStation, HybridShare, QuickConnect, SurveillanceStation). 100%
branch coverage. The card's `package_status{package,state}` running/stopped state-set was SCOPED OUT — the
live `SYNO.Core.Package list` exposes no run-state field; package presence is covered by `package_info` +
`package_count`, and the dropped dimension is tracked in `regression.md` (STAGE-008-012) with a revisit
trigger. Also closed a pre-existing coverage gap from STAGE-008-011 (the `share_list`/`replica_core_list`
client helpers were missing from the `test_synology_client.py` scaffolding-coverage parametrize list).

---

2026-06-23 c6a3de2 EPIC-008/STAGE-008-011: Snapshot/Replication collector (graceful-empty + self-correcting replication sentinel)

Added `SynologyReplicationCollector` (`plugins/collectors/integrations/synology/replication.py`), a co-equal
graceful-empty multi-fetch collector (mirrors the Hyper Backup collector) for Btrfs snapshots + Snapshot
Replication (5m cadence, `synology` concurrency group). Enumerates shares via a new `share_list()` client
helper, then fetches per-share snapshots via `share_snapshot_list(name=<share>)` (each fetch isolated — one
share failing does not abort the others). Emits `homelab_synology_snapshot_count{share}` for every share
(0 included, so "share lost its snapshots" is alertable), `homelab_synology_snapshot_latest_age_seconds{share}`
(when a snapshot time parses), and a no-label `homelab_synology_snapshots_configured` sentinel (1 when any
share has snapshots). Follows the Wave-B alerting philosophy so STAGE-008-022's snapshot rules can fire.

Live recon proved Btrfs Snapshot Replication has NO working read method on this DSM — the package is not
installed (`SYNO.Btrfs.Replica.Core list` returns ERR:103; `SYNO.SnapshotReplication.*` absent). Per-task
replication collection is therefore scoped out, replaced by a SELF-CORRECTING
`homelab_synology_replication_available` sentinel: a new `replica_core_list()` probe sets it to 1 only if the
method returns a non-error dict payload, else 0 (currently 0). It auto-flips to 1 if the package is ever
installed. Validated live (14 shares, 0 snapshots, package absent): `ok=True`, 35 metrics, all 14
`snapshot_count` series at 0, `snapshots_configured=0`, `replication_available=0`; the probe's ERR:103 is
recorded but non-fatal. 100% branch coverage. Two inferred/deferred items are tracked in `regression.md`
(STAGE-008-011): snapshot field-name verification (when a real snapshot exists) and full Btrfs replication
collection (when the package is installed) — both with named triggers, NOT silent deferrals.

---

2026-06-23 a01d4a0 EPIC-008/STAGE-008-010: Hyper Backup collector (graceful-empty + NoBackupConfigured)

Added `SynologyBackupCollector` (`plugins/collectors/integrations/synology/backup.py`), a co-equal
multi-fetch collector (mirrors the UPS collector) that polls Hyper Backup task + repository state
(`SYNO.Backup.Task/list` + `SYNO.Backup.Repository/list`, 5m cadence, `synology` concurrency group). Emits
13 metric families: per-job `backup_info{job,name}`, `backup_status{job,state}` + always-emitted
`backup_status_error{job}`, `backup_last_result{job,result}` + always-emitted `backup_last_result_ok{job}`,
`backup_data_size_bytes`, `backup_enabled`, `backup_last_run_timestamp`/`_age_seconds`,
`backup_next_run_timestamp`; plus always-emitted aggregates `backup_configured_count`,
`no_backup_configured` (1 when Hyper Backup is running with 0 tasks), and `backup_repository_count`. Follows
the Wave-B alerting philosophy (always-emitted alertable gauge + labeled detail) so the STAGE-008-022
`BackupFailed`/`BackupMissing`/`SynologyNoBackupConfigured` rules can fire. Graceful-empty: 0 configured
jobs is a healthy state emitting `no_backup_configured=1`, not a collector failure — `no_backup_configured`
is set only when the task fetch SUCCEEDS and returns 0 tasks, so a fetch failure never masquerades as
"no backups configured."

Validated live against the DSM (0 jobs/repos configured): `ok=True`, 18 metrics, empty-path assertions
pass (`configured_count=0`, `no_backup_configured=1`, `repository_count=0`). 100% branch coverage. The
populated-job field names are inferred (no live job) and tracked for verification in
`regression.md` (STAGE-008-010 item #2, trigger: when a real Hyper Backup job is configured).

---

2026-06-23 2f5e064 EPIC-008/STAGE-008-009: UPS + SystemHealth collector (closes Wave B)

New `SynologyUPSCollector` (`apps/monitor/homelab_monitor/plugins/collectors/integrations/synology/ups.py`) is a two-API multi-fetch collector (`ExternalDevice.UPS get` + `SystemHealth get`) emitting 10 `homelab_synology_*` families across UPS power + DSM overall-health. UPS: `ups_charge_percent`, `ups_runtime_seconds`, `ups_status{state}` (state-set over online/on_battery/low_battery/charging, prefix-stripped) plus derived alertable booleans `ups_on_battery`/`ups_low_battery`, an always-emitted `ups_connected` presence gauge, and `ups_info{model,manufacture}` with `.strip()`'d labels (DSM appends a trailing newline). Health: an always-emitted `health_ok` (1=healthy/rule-absent, 0=rule-present) plus a `health_rule{rule=<id>}` state-set and `health_priority` when a concern is active — the null-means-healthy inversion made explicit so absence isn't conflated with collector-down. The two fetches are CO-EQUAL: `ok=False` only if BOTH fail (a new combine variant vs STAGE-008-007's primary-gated multi-fetch), reusing a local copy of 007's `_fetch` record-and-continue wrapper. Observe-only; cadence 60s, `concurrency_group="synology"`.

Validated live against the production NAS + APC Smart-UPS 1500: 22 metrics in 2.5s, zero errors — `ups_charge_percent=100`, `ups_runtime_seconds=4234`, `ups_status{state="online"}`, `ups_connected=1`, `ups_info` labels confirmed stripped (`model="Smart-UPS_1500"`, `manufacture="American Power Conversion"`), and the live health inversion (`health_ok=0` + `health_rule{rule="storage_is_attention"}` + `health_priority=2.2`); both co-equal fetches confirmed via 2 `api_took_seconds` entries. New `test_synology_ups_collector.py` (23 tests) holds 100% branch coverage on `ups.py` (achieved first-run via an explicit per-guard coverage-closure spec). The rule-of-three prediction (a shared status-mapper extraction) was investigated and did NOT hold — the per-collector mappers diverge by value-domain, so they stay local. `make verify` green (full gate, 9m14s; backend 5758 passed). This is the FINAL stage of Wave B (storage/pool/system/utilization/UPS+health — all five collectors complete and live-validated).

---

2026-06-23 493632e EPIC-008/STAGE-008-008: Utilization collector (CPU/mem/disk/net/volume/NFS)

New `SynologyUtilizationCollector` (`apps/monitor/homelab_monitor/plugins/collectors/integrations/synology/utilization.py`) is a single-fetch (`SYNO.Core.System.Utilization get`), multi-series collector emitting 28 `homelab_synology_*` metric families across six sections: CPU (`cpu_load1`/`_load5`/`_load15` as true load-average floats, `cpu_user`/`_system`/`_other_percent`), memory (`mem_usage_percent`/`swap_usage_percent`, `mem_total`/`available`/`cached`/`buffer_bytes`), per-disk IO (`disk_read`/`write_bytes_per_second{device}`, `disk_read`/`write_iops{device}`, `disk_utilization_percent{device}`), per-NIC throughput (`net_rx`/`tx_bytes_per_second{iface}`), per-volume LVM IO (`vol_io_read`/`write_bytes_per_second{volume}`, `vol_io_utilization_percent{volume}`), and NFS (`nfs_read`/`write`/`total_ops`, `nfs_read`/`write`/`total_max_latency_seconds`). DSM's encoded units are converted to Prometheus base units at the source via a local `_scaled` helper — load-average ÷100, KB→bytes ×1024, latency ms→seconds ÷1000 — and the per-second rate metrics are suffixed `_per_second` so consumers don't wrongly `rate()` an already-rated gauge. List aggregates (DSM's `total` disk/NIC/volume) are emitted under the same family with a `{device="total"}`/`{iface="total"}`/`{volume="total"}` label; the volume label prefers DSM's `display_name` ("volume1") over the raw device ("dm-1"). Observe-only; `ok=True` even when busy. Cadence 60s, `concurrency_group="synology"`. Empty/trivial sections (`smb`/`lun`/swap-IO) are deliberately scoped out (YAGNI).

Validated live against the production NAS: `SynologyUtilizationCollector` emitted 110 metrics in 3.3s with zero errors and the unit conversions confirmed against real data — `cpu_load1=0.27` (a float load average, not the raw ×100 int), `mem_total_bytes=1.654e10` (~16.5 GB, KB×1024), 9 disk series (8 drives + `{device="total"}`), and the volume series labeled `volume1`. New `test_synology_utilization_collector.py` holds 100% branch coverage on `utilization.py` (after a coverage-closure pass adding 6 field-absent edge tests for the emit-if-present guards); `make verify` green (split gate; backend 5733 passed).

---

2026-06-23 3216006 EPIC-008/STAGE-008-007: System info + temps collector + navigator extraction

New `SynologySystemCollector` (`apps/monitor/homelab_monitor/plugins/collectors/integrations/synology/system.py`) fetches THREE DSM APIs — `SYNO.Core.System` v3 `info` (PRIMARY), `SYNO.Core.Hardware.FanSpeed`, and `SYNO.Core.Hardware.NeedReboot` — and emits six `homelab_synology_*` families: `system_uptime_seconds` (parsed from the DSM "HHH:MM:S" up_time string), `sys_temp_celsius`, `sys_temp_warning` (OR of the three DSM warn-flag variants), `info{model,serial,firmware,cpu_series}` (identity gauge anchored on model), `fan_status{state}` (a state-set over cool_fan/all_disk_temp_fail/fan-mode, the box exposes status strings not RPM), and `need_reboot`. Multi-fetch uses a degraded-but-useful policy: a local `_fetch` wrapper records per-API errors without aborting; a PRIMARY (system_info) failure yields `ok=False`, but a supplementary (fan/reboot) failure keeps `ok=True` with the error recorded — matching the observe-only model. Cadence 60s, `concurrency_group="synology"`.

This stage also extracted the four defensive navigators (`as_dict`, `as_list_of_dicts`, `nested`, `bool_to_gauge`) from the STAGE-008-005 storage collector and STAGE-008-006 pool collector into the shared `_shared.py` module (rule-of-three: three consumers now), updating both collectors to import them and deleting their byte-identical local copies; the diverging per-collector status/value mappers stay local. The extraction is behavior-preserving — all Synology modules hold 100% branch coverage with zero test regression, and a live cross-check confirmed the storage (92 metrics) and pool (34 metrics) collectors still emit correctly against the production NAS. Validated live: `SynologySystemCollector` emitted 17 metrics in 0.76s (uptime ~1206h, sys_temp 54°C, model DS3622xs+, fan quietfan/cool, no reboot needed) across all three DSM APIs. New `test_synology_system_collector.py` (22 cases) at 100% branch; `make verify` green (split gate; backend 5702 passed).

---

2026-06-23 eaa4f29 EPIC-008/STAGE-008-006: Pool & RAID collector (load_info storagePools slice)

New `SynologyPoolCollector` (`apps/monitor/homelab_monitor/plugins/collectors/integrations/synology/pool.py`) parses the `storagePools[]` slice of the same `SYNO.Storage.CGI.Storage load_info` response that STAGE-008-005's storage collector reads (no second fetch, no new client helper). Emits `homelab_synology_pool_*` and `homelab_synology_raid_*` gauges: pool status (state-set, double-emit of top-level `status` + nested `space_status.status`), scrub status, a dedicated `pool_unverified_disk` flag (derived from `status`/`show_attention`/`show_danger`), rebuild `progress.percent` (-1=idle) + `progress.step`, writable + disk-failure-number, scrub capability flags + last-done/next-schedule timestamps, and per-RAID-set metrics (label = `raidPath`): mapped `raidStatus` (1=normal), normal/designed disk counts, `hasParity`, `raidCrashedReason`. Observe-only (`ok=True` even when degraded; `ok=False` only on a client-level `SynologyError`); every family is cap-routed via the shared `capped_emitter` guardrail. Mirrors the STAGE-008-005 structure (local navigators + `_bool_to_gauge` copied, plus a local `_raid_status_to_gauge`); wired into `_SYNOLOGY_COLLECTORS`. Cadence 5m, `concurrency_group="synology"`.

Validated live against the production NAS (`reuse_1`, RAID6): real `SynologyPoolCollector().run(ctx)` emitted 34 metrics across 18 families in 0.78s with zero errors — pool_status 2 series, scrub `ready`, unverified-disk 1.0, raid `/dev/md2` normal at 8/8 disks. New `test_synology_pool_collector.py` (40 cases) holds 100% branch coverage; `make verify` green.

---

2026-06-23 eaa6692 EPIC-008/STAGE-008-005: Synology storage collector (volumes + disks) + placeholder removal

First real Synology Wave-B collector. `SynologyStorageCollector` (synology_storage, 5-min, synology concurrency group) calls `SYNO.Storage.CGI.Storage load_info` and emits per-volume + per-disk metrics via the STAGE-008-003 `_shared.py` helpers, all cap-routed through the EPIC-005-004 cardinality cap (guardrail; 8 disks / 1 volume far under the default-500). Per-volume: `homelab_synology_volume_used_bytes`/`_total_bytes`/`_used_percent` (derived — DSM sends no percent), `_status` (state-set, one observation per condition — emits BOTH the top-level `status` and the nested `space_status.status`, so `has_unverified_disk` + `fs_almost_full` both surface), `_fs_type`, and the `_writable`/`_encrypted`/`_locked`/`_inode_full` flags. Per-disk (keyed by DSM disk id): `_temp_celsius{disk,model}`, `_smart_status`/`_status` (1=normal), `_unc_count`, `_remain_life` (read from the `remain_life.value` object; -1 emitted literally for HDDs), `_sb_days_left`, `_size_bytes`, `_slot`. The parse was grounded against the REAL captured `load_info` payload (the live DSM, secret now persisted in instance B's store), correcting six field-key/nesting assumptions from recon (the `is_*` boolean flag names, `remain_life` being an object, `size_total`/`slot_id`); booleans go through a `_bool_to_gauge` helper since `as_float` rejects bools. `ok=True` even when the NAS reports degraded volume/disk state; `ok=False` only on a client-level error. Removed the STAGE-008-002 placeholder collector (`placeholder.py` + its test deleted; `_SYNOLOGY_COLLECTORS` now registers the storage collector; the bundle test retargeted). 100% branch coverage of `storage.py`; live 3b against the real NAS emitted 92 metrics with zero fixture/reality mismatches. Pool/RAID (the `storagePools[]` slice of the same response) is STAGE-008-006. `make verify` green.

---

2026-06-23 51218ca EPIC-008/STAGE-008-004: SSH homelab-probe dedicated-user target config on the EPIC-017 framework

Configured the Synology as an SSH probe target on the (already-complete, generic) EPIC-017 framework, in dedicated-user mode with a low-priv `homelab-probe` user, a forced-command boundary, and NO sudoers (per EPIC-008 Amendment 3: synodisk/mdstat/upsc/hwmon/df all read unprivileged; btrfs-scrub comes from the DSM API, not SSH-root). Because EPIC-017 is generic + complete and the real `ssh_targets` config entry lives in the gitignored overrides repo, this stage's committed artifacts are docs + characterization tests, not new production code: a Synology subsection in `docs/ssh-probe-setup.md` walking the operator through the overrides-repo target entry + the keygen/capture-hostkey/install-instructions/test flow + the DSM-side `homelab-probe` user creation + the skip-sudoers rationale; and 3 tests in `apps/monitor/tests/test_cli_ssh_probe.py` closing verified coverage gaps (the dedicated-user `test` subcommand and `install-instructions` with `script_id` set on a dedicated-user target). The live provisioning (keygen→capture-hostkey→install→test against the real NAS) and the `homelab-probe` DSM user creation are deferred to STAGE-008-014, which runs in the MAIN instance A (it replaces the placeholder forced-command with the real combined probe body, so provisioning once there avoids a placeholder-then-replace round-trip). `make verify` green (5641 passed, 100% branch coverage).

---

2026-06-23 2724b72 EPIC-008/STAGE-008-003: Cardinality-cap reuse hook + shared parse / typed-error helpers (_shared.py) for Synology collectors

Added the shared helper layer every Synology collector (Waves B/C/E) will use, so each later collector stays a thin, single-purpose file. New `apps/monitor/homelab_monitor/plugins/collectors/integrations/synology/_shared.py` exposes: `client_unconfigured_result` / `failed_result` (typed-error → failed ok=False CollectorResult), `fetch_or_result` (centralizes `homelab_synology_api_took_seconds{api}` latency emission on success + the ok=True-when-NAS-sad convention — only a client-level SynologyError yields ok=False), the numeric field parsers `as_float` / `bytes_field` / `percent_field`, and the cardinality-cap helpers `cap_for_synology` / `capped_emitter`. The cardinality cap is wired as a guardrail via the reusable EPIC-005-004 cap: the `homelab_synology_*` families intentionally rely on the default 500-series cap (no `_DEFAULT_CARDINALITY_FAMILIES` entry — the bounded Synology series are far under it; a tighter cap would be a false guardrail). Generic enum→state mapping and a collector template were deferred to STAGE-008-005/009 (YAGNI). Covered by `tests/test_synology_shared.py` (100% branch coverage of `_shared.py`); Refinement exercised the helpers through a realistic collector lifecycle (42/42 checks incl. the cap guardrail at 600 observations). `make verify` green.
