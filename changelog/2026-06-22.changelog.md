2026-06-22 d49b880 EPIC-008/STAGE-008-001: Synology DSM v7 API client + secret + config + lifespan wiring + smoke

Built a real Synology **DSM v7** REST client (`SynologyRestClient`) and wired it into the collector context, mirroring the verified Pi-hole (session-auth) + Unifi (self-signed `verify=False`, monotonic latency) integration-client precedents. Observe-only foundation for the EPIC-008 Wave-B/C/E collectors.

The client authenticates via `SYNO.API.Auth` version 7 (login with `account`/`passwd`/`format=sid`, **NO `session=` param** — sending one returns DSM error 402), routing every call through `/webapi/entry.cgi` over a self-signed HTTPS endpoint (`verify=False`, cert `CN=synology`) on a dedicated `httpx.AsyncClient`. It reuses ONE session, re-authenticates once on **DSM body error code 119** (session expired, returned on HTTP 200 — not an HTTP 401), retries the call once, and logs out best-effort on shutdown. Errors are a return-not-raise typed `SynologyError` (reasons `unreachable`/`timeout`/`auth`/`rate_limited`/`http_error`/`bad_response`/`api_error`; `status` carries the DSM error code for `api_error` and DSM-derived `auth`); the DSM password and `_sid` never appear in any error message, log line, or the constructed login URL. Per-call latency is captured via a monotonic timer around only the data-returning request. The client exposes ~22 typed GET helpers — only `system_info()` (`SYNO.Core.System` v3 `info`) is exercised this stage; the rest are scaffolding for later waves.

Wiring: new `synology: SynologyClient | None` field on `CollectorContext`; `SynologyClient` Protocol in `plugins/io.py`; `SynologyConfig` + `load_synology_config()` (`HOMELAB_MONITOR_SYNOLOGY_URL` default `https://192.168.2.4:5001`, `HOMELAB_MONITOR_SYNOLOGY_ACCOUNT` default `homelab-monitor`); dedicated `verify=False` client constructed once in lifespan with `password_provider=lambda: ttl_resolver.current().get("synology_dsm_password")`, injected into `ctx_factory`, guarded best-effort `aclose()` on shutdown. Operator README at `docs/integrations/synology.md`. The DSM service account is an administrator (no read-only-admin role exists on DSM; observe-only code is the mitigation) created without 2FA for headless login.

Validated 3a (mock unit suite, 100% branch) + 3b (live login + `Core.System info` against the real DSM 7.3.2 → `DS3622xs+`, `DSM 7.3.2-86009`, sys_temp 51 °C, session reuse, clean logout, no password leak). Also fixed a pre-existing unrelated failure: 3 cron-parser tests were failing on a clean checkout because their fixtures (`apps/monitor/tests/data/cron_fixtures/*.example`) were gitignored by the global `data/` rule and never tracked — authored the fixtures and added a `!apps/monitor/tests/data/` `.gitignore` exception. `make verify` green (ruff, pyright --strict, pytest 5605 passed @ 100% branch, frontend). No migration, no OpenAPI/schema change, no frontend feature code.

---

2026-06-22 13:27 5b7cf43 parallel-instance (non-epic infra): dev-only second independent monitor on same host

Support a **second fully independent instance of homelab-monitor** on the same host (dev-only second prod compose stack alongside existing prod). New `HOMELAB_MONITOR_DOCKER_ENABLED` flag (default `true`) gates the Docker plugin (`DockerDiscoverer` + `DockerSocketCollector` + `ComposeActionRunner` + image-update loop + override-loader); second instance does no container monitoring. Compose parameterization: `HM_INSTANCE` (project/network/volume namespace) + `HM_CONTAINER_PREFIX` (container names, default reproduces existing names exactly) + host bind-mount SOURCE vars (`HM_CRON_IPC_SRC`, `HM_CRON_SNAPSHOT_SRC`, `HM_HOST_ETC`, `HM_HOST_PROC`) + `HOMELAB_MONITOR_OVERRIDES_DIR`; `cadvisor` + `vector` gated behind the `host-collectors` compose profile.

New `HM_DEV_BASE` parameterizes the dev-rig `/tmp/hm-dev` base path. **Fix (latent bug):** config-init now `chown`/`chmod` `/data` + `/storage/backup` to the runtime uid, so fresh instances (and a fresh existing-instance volume) can open the SQLite DB — previously relied on a coincidentally world-writable volume. **Fix:** monitor compose service now forwards `HOMELAB_MONITOR_DOCKER_ENABLED` into the container (was comment-only).

New operator guide: `docs/dev/parallel-instance.md`; `CLAUDE.md` port-map updated (`3xxxx` instance-B band, parallel to dev `1xxxx` and prod `2xxxx`). **Backward-compatible:** with no new env vars set, the existing instance renders byte-identical names/sources (one required change: existing instance must add `COMPOSE_PROFILES=host-collectors`).

Verified: `make verify` green (pyright strict, 5535 backend tests @ 100% kernel coverage, 1673 frontend tests); live two-instance coexistence validated on the host.

---

2026-06-22 9aca3fb EPIC-006/STAGE-006-001: Pi-hole v6 REST client + RO/RW secrets + lifespan wiring

First Pi-hole integration foundation. Adds a real Pi-hole **v6** session-auth REST client (`PiholeRestClient` in `kernel/pihole/`): `POST /api/auth` → `X-FTL-SID` session reused across requests, single-flight re-auth under an `asyncio.Lock`, 401-retry-once, 429 back-off, best-effort `DELETE /api/auth` logout on shutdown, and a typed `PiholeError` for every transport/HTTP failure (Pi-hole 5xx/timeout never propagates). Each response exposes its `took` float for the later API-latency metric. Wires a two-credential model (`pihole_api_password_ro` for collectors, `pihole_api_password_rw` for Wave-E write actions — Pi-hole v6 has a single app-password tier, so the same value is stored under both names, separated by purpose), a `PiholeConfig`/`load_pihole_config()` reading `HOMELAB_MONITOR_PIHOLE_URL` (default `http://192.168.2.148:8080`, the host LAN IP — the bridge-network container cannot reach `localhost`), a `PiholeClient` Protocol, a `CollectorContext.pihole` field, and lifespan construct/inject/shutdown on the shared httpx pool. Pins the vector `service` label `pihole-unbound` for downstream log rules + LogViewer. Includes an operator README (`docs/integrations/pihole.md`) and a full unit suite (mock-layer 3a). Validated live (3b) against the real Pi-hole v6.4.2: authenticated, read version (Core v6.4.2 / FTL v6.6.2), reused the session on a second call, and logged out. `make verify` green at 100% branch coverage. No migration (alembic HEAD stays `0044`); no API/openapi change; no frontend.

---

2026-06-22 ae473f2 EPIC-007/STAGE-007-026: DNS-posture drift badge — `expected_dns`/`drift` on the DNS-posture contract + ✓/drift UI badge

Completes the DNS-posture green-✓ / red-drift indicator deferred from STAGE-007-021 (final stage of EPIC-007). Backend: added `expected_dns: str | None` + `drift: bool` to `UnifiDnsHandout` (`apps/monitor/homelab_monitor/kernel/api/routers/integrations_unifi.py`); the `/api/integrations/unifi/network/dns-posture` handler now reads a single-source-of-truth config value and derives drift. New `UnifiConfig.expected_dns_steering_ip: str = "192.168.2.148"` config field (`apps/monitor/homelab_monitor/kernel/config.py`), env `HOMELAB_MONITOR_UNIFI_EXPECTED_DNS_STEERING_IP`, mirroring the `host_lan_ip` host-default precedent; the handler normalizes empty-string → None (`expected = config.expected_dns_steering_ip or None`) so an unset expected degrades to an honest "not configured" state (no false ✓). Per-handout `drift = expected is not None and dns != expected`.

Frontend: `NetworkDnsPostureWidget.tsx` renders a green "OK" badge (`variant="ok"`, CheckCircle2) when `expected_dns` is set and not drift, a red "Drift" badge (`variant="critical"`, AlertTriangle) when `drift`, and NO badge when `expected_dns` is null (honest no-false-✓). Empty-`networks` honest empty state preserved. No third frontend hardcode of the expected IP — single-sourced from the API.

Single-source-of-truth: expected DNS is sourced server-side from `UnifiConfig.expected_dns_steering_ip` for the API/badge. The `deploy/vmalert/metrics/unifi.yaml` `UnifiDnsSteeringDrift` rule still hardcodes `192.168.2.148` independently (static YAML, not templated from app config) — an acknowledged documented duplicate tracked as a POST-EPIC item in `epics/EPIC-007-unifi/regression.md`.

Tests: 3 backend handler tests (drift-true / drift-false-match / expected-unset) hold the global 100% branch-coverage gate; 2 config-loader tests (default + env override); frontend render test `NetworkDnsPostureWidget.test.tsx` (4 cases — empty / drift-red / ok-green / expected-null-no-badge), where the drift-red case is the viewport-waiver branch (unreachable on live data: the live UDM hands out the expected `192.168.2.148`). Regenerated `packages/shared-types/openapi.json` (response-model change). No migration (config-derived + computed). Refinement: backend live-recon confirmed `{network:"Default",dns:"192.168.2.148",expected_dns:"192.168.2.148",drift:false}` (✓ case) on prod `:29090`; Desktop + Mobile sign-off for the ✓ state.

---
