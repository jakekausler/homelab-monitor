2026-06-22 2a7eab8 EPIC-006/STAGE-006-006: upstreams collector (per-upstream query counts)

Adds the Pi-hole upstreams collector — the second real EPIC-006 collector, mirroring the STAGE-006-005 pattern. New `PiholeUpstreamsCollector` (`plugins/collectors/integrations/pihole/upstreams.py`, `name="pihole_upstreams"`, 30s, `concurrency_group="pihole"`) polls Pi-hole v6 `/api/stats/upstreams` via the inherited RO session and emits `homelab_pihole_upstream_queries{upstream}` — one 24h-rolling WINDOW-GAUGE per forward destination (NOT a counter; downstream alert rules must NOT `rate()` it) — plus `homelab_pihole_api_took_seconds{endpoint="stats/upstreams"}`. The `{upstream}` label is built per the live API shape: pseudo-upstreams (`port == -1`) → bare name (`cache` / `blocklist`); real resolvers → `ip#port` (e.g. `127.0.0.1#5335`, the unbound forwarder). `resp.payload` (typed `object`) is isinstance-narrowed and the `upstreams` list is iterated with per-entry defensive narrowing (entry-not-dict / ip-missing-or-not-str / port-missing-or-not-int-or-bool / non-numeric-count → skip that entry, others still emit); a missing/non-list `upstreams` degrades to ok=True with only api_took. Per the card, pseudo-upstream handling is DOCUMENTATION not a metric: the module docstring + stage record the contract that STAGE-006-016's `PiholeUpstreamAllDown`/`UpstreamDown` rules MUST exclude `upstream="cache"`/`"blocklist"` so the "all real upstreams down" alert fires only when the real resolver share → 0. Intentionally NOT emitted (YAGNI / no double-ownership): per-upstream `statistics.response`, the top-level `total_queries`/`forwarded_queries` (STAGE-006-005 owns the totals), and any `kind`/`is_pseudo` label. New local-`_parsing` reuse; one-line `_PIHOLE_COLLECTORS` registration. Added a literal-string contract-name test. Validated live (3b) against the real Pi-hole v6.6.2: all three `{upstream}` series present in VM incl. `127.0.0.1#5335`, values in-ballpark with the live API (unbound 7003 vs 6995, cache 36438 vs 36402, blocklist 49141 vs 49085). `make verify` green at 100% branch coverage (5655 backend tests). No migration (alembic HEAD `0044`); no API/openapi change; no frontend.

---

2026-06-22 40eab38 EPIC-006/STAGE-006-005: core query-stats collector (first real Pi-hole collector; placeholder removed)

Lands the first REAL Pi-hole collector and removes the STAGE-006-002 scaffolding. New `PiholeStatsSummaryCollector` (`plugins/collectors/integrations/pihole/stats_summary.py`, `name="pihole_stats_summary"`, 30s, `concurrency_group="pihole"`) polls Pi-hole v6 `/api/stats/summary` via the inherited RO session (`ctx.pihole.stats_summary()`) and emits the core query metrics as 24h-ROLLING WINDOW-GAUGES (NOT lifetime counters — downstream alert rules must NOT `rate()` them): `homelab_pihole_queries_total`, `_blocked_total`, `_forwarded_total`, `_cached_total`, `_percent_blocked`, `_query_frequency`, `_unique_domains`, `_active_clients`, `_total_clients`, the enum families `_query_by_type{type}` / `_query_by_status{status}` / `_query_by_reply{reply}` (emitted faithfully per-label, full bounded enum, no cardinality cap), and the API-latency gauge `homelab_pihole_api_took_seconds{endpoint="stats/summary"}` (mirrors the Unifi pattern). `resp.payload` (typed `object`) is isinstance-narrowed with per-group/per-entry defensive skips (non-dict payload, missing/non-dict queries/clients/enum, non-numeric values). `homelab_pihole_unique_clients` is RETRACTED (no source in Pi-hole v6 — `clients.active`/`clients.total` cover distinct-client info); gravity metrics are left to STAGE-006-007. New local `pihole/_parsing.py` (`as_float`/`emit_numeric`, copied from the unifi style, not cross-imported). REMOVES `placeholder.py` + `PiholePlaceholderCollector` + the `homelab_pihole_bundle_loaded` sentinel; `_PIHOLE_COLLECTORS = [PiholeStatsSummaryCollector]`. Refinement caught + fixed a metric-naming deviation (6 scalars had an erroneous `queries_` infix; corrected to the card names) and added a literal-string contract-name test so the self-referential constant asserts can't hide future drift. Validated live (3b) against the real Pi-hole v6.6.2: VM values match the live `/api/stats/summary` in-ballpark (queries.total 92170 vs 92140; percent_blocked 52.912% vs 52.909%; active/total clients exact). `make verify` green at 100% branch coverage (5639 backend tests). No migration (alembic HEAD `0044`); no API/openapi change; no frontend.

---

2026-06-22 e9a28d7 EPIC-006/STAGE-006-004: per-client cardinality cap + loopback-attribution helper

Adds the pure, synchronous client-classification helper that STAGE-006-012 (per-client collector), STAGE-006-027 (cross-epic Pi-hole+Unifi view), and EPIC-007 (Unifi) consume to attribute Pi-hole DNS clients and bound per-client metric cardinality. New `kernel/pihole/clients.py` (kernel-resident, mirroring `kernel/unifi/identity.py`; no I/O, no `CollectorContext`) exposes `classify_one(ip, name, *, host_lan_ip, resolver_names) -> (ClientKind, host_lan_ip|None)` and `classify_clients(raw, *, host_lan_ip, cap, resolver_names) -> ClientClassification`. `ClientKind = Literal["local","resolver_self","lan","unattributed"]`: loopback (`127.0.0.1`/`::1`/`::`/name `pi.hole`) is KEPT and STRUCTURALLY EXEMPT from the cap (partitioned out before the capper, so the host can never be evicted) — classified `resolver_self` (name in `resolver_names`, default `{"pi.hole","localhost"}`) or `local`, stamped with `host_lan_ip`; when `host_lan_ip` is empty (the public-release default) every loopback client degrades to `unattributed` with `host_lan_ip=None`, overriding the resolver/local distinction. LAN clients are deduped at source (one capper candidate per IP, first-wins) and capped via the REUSED `kernel/metrics/cardinality.py` `CardinalityCapper` (unchanged); `cap_domains` caps the top-domain family with no exemption. `ClassifiedClient` preserves the original-case name and passes `mac` through verbatim. New config `HOMELAB_MONITOR_PIHOLE_HOST_LAN_IP` (default empty) on `PiholeConfig`/`load_pihole_config`. Refinement was 3a-only (pure helper; the live 3b is owned by STAGE-006-012): 7/7 realistic e2e scenarios pass (real Pi-hole loopback forms; 200-client LAN population; cap-exemption at cap=50 and cap=0; deterministic eviction; duplicate-IP dedup). Code review caught and fixed a duplicate-IP survivor-mapping bug (dedup-at-source). `make verify` green at 100% branch coverage (5617 backend tests). No migration (alembic HEAD `0044`); no API/openapi change; no frontend; no secrets.

---

2026-06-22 ae8fa6b EPIC-006/STAGE-006-003: unbound-control access layer (docker-exec stats parser + extended-stats detect)

Adds the unbound resolver access layer the STAGE-006-013 collector will consume — it runs `unbound-control stats_noreset` inside the `pihole-unbound` container via the Docker SOCKET API, captures and de-multiplexes the output, parses it, detects extended-statistics, and graceful-degrades. New `DockerSocketClient.exec_capture()` (the stdout-capturing sibling of the exit-code-only `exec_in_container`, which is left untouched) attaches stdout/stderr and de-multiplexes Docker's `Tty:false` stream-frame protocol via a `_demux_stream` helper (8-byte header = 1 type byte + 3 pad + big-endian uint32 length, then payload; bounds-checked against truncated/zero/huge-length frames). New `kernel/pihole/unbound_control.py` exposes `fetch_unbound_stats(exec_backend, container) -> UnboundStats | UnboundError` (typed outcome, never raises into the collector): `parse_unbound_stats` splits `key=value` lines into a full `dict[str,float]`, sets `extended_enabled` from the presence of any `histogram.*`/`num.query.type.*` key, and maps failures to five reasons (`container_unreachable`/`socket_error`/`control_error`/`empty_output`/`parse_error`). The exec backend is injected via an `ExecCapture` Protocol so the parser/degrade logic tests against a fake without Docker. Config `HOMELAB_MONITOR_PIHOLE_UNBOUND_CONTAINER` (default `pihole-unbound`) via `load_pihole_unbound_config`. Two real captured fixtures (extended 154-key / default 60-key). Validated live (3b): the demux de-multiplexed the real 156-line multiplexed Docker exec stream with ZERO data loss (access-layer line count == direct `wc -l`), `extended_enabled=True`, and a bad container degraded to `container_unreachable`. `make verify` green at 100% branch coverage. No migration (alembic HEAD `0044`); no API/openapi change; no frontend; no secrets.

---

2026-06-22 2df2dc5 EPIC-006/STAGE-006-002: integrations/pihole/ bundle skeleton + registration

Establishes the Pi-hole collector-bundle layout + registration plumbing that the rest of EPIC-006 plugs into, mirroring the EPIC-005 (homeassistant) / EPIC-007 (unifi) integration-bundle exemplars. Adds `plugins/collectors/integrations/pihole/` with `__init__.py` exposing `register_all(loader)` — a per-collector `try/except` loop over `_PIHOLE_COLLECTORS` (`loader.register(cls, config_from_classvars(cls))`, isolating a bad register and logging `pihole_integration.collector_register_failed`) — wired into `lifespan.py` next to the HA/Unifi registration calls. Ships ONE intentional throwaway `PiholePlaceholderCollector` (`pihole_placeholder`, 60s, `concurrency_group="default"`, never touches `ctx.pihole`) emitting a static `homelab_pihole_bundle_loaded=1.0` sentinel — present only because an empty collector list can't satisfy the 100%-branch-coverage gate; it is removed in STAGE-006-005 when the first real collector (core query-stats) lands. Wave-B/C collectors now register by a one-line `_PIHOLE_COLLECTORS` edit, not by touching the loader. Validated live (3a) against the running monitor: clean registration (no `collector_register_failed`), `pihole_placeholder` healthy in `/api/collectors`, `homelab_pihole_bundle_loaded=1` in VictoriaMetrics. `make verify` green at 100% branch coverage. No migration (alembic HEAD `0044`); no API/openapi change; no frontend; no secrets.

---

2026-06-22 13:27 5b7cf43 parallel-instance (non-epic infra): dev-only second independent monitor on same host

Support a **second fully independent instance of homelab-monitor** on the same host (dev-only second prod compose stack alongside existing prod). New `HOMELAB_MONITOR_DOCKER_ENABLED` flag (default `true`) gates the Docker plugin (`DockerDiscoverer` + `DockerSocketCollector` + `ComposeActionRunner` + image-update loop + override-loader); second instance does no container monitoring. Compose parameterization: `HM_INSTANCE` (project/network/volume namespace) + `HM_CONTAINER_PREFIX` (container names, default reproduces existing names exactly) + host bind-mount SOURCE vars (`HM_CRON_IPC_SRC`, `HM_CRON_SNAPSHOT_SRC`, `HM_HOST_ETC`, `HM_HOST_PROC`) + `HOMELAB_MONITOR_OVERRIDES_DIR`; `cadvisor` + `vector` gated behind the `host-collectors` compose profile.

New `HM_DEV_BASE` parameterizes the dev-rig `/tmp/hm-dev` base path. **Fix (latent bug):** config-init now `chown`/`chmod` `/data` + `/storage/backup` to the runtime uid, so fresh instances (and a fresh existing-instance volume) can open the SQLite DB — previously relied on a coincidentally world-writable volume. **Fix:** monitor compose service now forwards `HOMELAB_MONITOR_DOCKER_ENABLED` into the container (was comment-only).

New operator guide: `docs/dev/parallel-instance.md`; `CLAUDE.md` port-map updated (`3xxxx` instance-B band, parallel to dev `1xxxx` and prod `2xxxx`). **Backward-compatible:** with no new env vars set, the existing instance renders byte-identical names/sources (one required change: existing instance must add `COMPOSE_PROFILES=host-collectors`).

Verified: `make verify` green (pyright strict, 5535 backend tests @ 100% kernel coverage, 1673 frontend tests); live two-instance coexistence validated on the host.

---

2026-06-22 9aca3fb EPIC-006/STAGE-006-001: Pi-hole v6 REST client + RO/RW secrets + lifespan wiring

First Pi-hole integration foundation. Adds a real Pi-hole **v6** session-auth REST client (`PiholeRestClient` in `kernel/pihole/`): `POST /api/auth` → `X-FTL-SID` session reused across requests, single-flight re-auth under an `asyncio.Lock`, 401-retry-once, 429 back-off, best-effort `DELETE /api/auth` logout on shutdown, and a typed `PiholeError` for every transport/HTTP failure (Pi-hole 5xx/timeout never propagates). Each response exposes its `took` float for the later API-latency metric. Wires a two-credential model (`pihole_api_password_ro` for collectors, `pihole_api_password_rw` for Wave-E write actions — Pi-hole v6 has a single app-password tier, so the same value is stored under both names, separated by purpose), a `PiholeConfig`/`load_pihole_config()` reading `HOMELAB_MONITOR_PIHOLE_URL` (default `http://192.168.2.148:8080`, the host LAN IP — the bridge-network container cannot reach `localhost`), a `PiholeClient` Protocol, a `CollectorContext.pihole` field, and lifespan construct/inject/shutdown on the shared httpx pool. Pins the vector `service` label `pihole-unbound` for downstream log rules + LogViewer. Includes an operator README (`docs/integrations/pihole.md`) and a full unit suite (mock-layer 3a). Validated live (3b) against the real Pi-hole v6.4.2: authenticated, read version (Core v6.4.2 / FTL v6.6.2), reused the session on a second call, and logged out. `make verify` green at 100% branch coverage. No migration (alembic HEAD stays `0044`); no API/openapi change; no frontend.

---

2026-06-22 ae473f2 EPIC-007/STAGE-007-026: DNS-posture drift badge — `expected_dns`/`drift` on the DNS-posture contract + ✓/drift UI badge

Completes the DNS-posture green-✓ / red-drift indicator deferred from STAGE-007-021 (final stage of EPIC-007). Backend: added `expected_dns: str | None` + `drift: bool` to `UnifiDnsHandout` (`apps/monitor/homelab_monitor/kernel/api/routers/integrations_unifi.py`); the `/api/integrations/unifi/network/dns-posture` handler now reads a single-source-of-truth config value and derives drift. New `UnifiConfig.expected_dns_steering_ip: str = "192.168.2.148"` config field (`apps/monitor/homelab_monitor/kernel/config.py`), env `HOMELAB_MONITOR_UNIFI_EXPECTED_DNS_STEERING_IP`, mirroring the `host_lan_ip` host-default precedent; the handler normalizes empty-string → None (`expected = config.expected_dns_steering_ip or None`) so an unset expected degrades to an honest "not configured" state (no false ✓). Per-handout `drift = expected is not None and dns != expected`.

Frontend: `NetworkDnsPostureWidget.tsx` renders a green "OK" badge (`variant="ok"`, CheckCircle2) when `expected_dns` is set and not drift, a red "Drift" badge (`variant="critical"`, AlertTriangle) when `drift`, and NO badge when `expected_dns` is null (honest no-false-✓). Empty-`networks` honest empty state preserved. No third frontend hardcode of the expected IP — single-sourced from the API.

Single-source-of-truth: expected DNS is sourced server-side from `UnifiConfig.expected_dns_steering_ip` for the API/badge. The `deploy/vmalert/metrics/unifi.yaml` `UnifiDnsSteeringDrift` rule still hardcodes `192.168.2.148` independently (static YAML, not templated from app config) — an acknowledged documented duplicate tracked as a POST-EPIC item in `epics/EPIC-007-unifi/regression.md`.

Tests: 3 backend handler tests (drift-true / drift-false-match / expected-unset) hold the global 100% branch-coverage gate; 2 config-loader tests (default + env override); frontend render test `NetworkDnsPostureWidget.test.tsx` (4 cases — empty / drift-red / ok-green / expected-null-no-badge), where the drift-red case is the viewport-waiver branch (unreachable on live data: the live UDM hands out the expected `192.168.2.148`). Regenerated `packages/shared-types/openapi.json` (response-model change). No migration (config-derived + computed). Refinement: backend live-recon confirmed `{network:"Default",dns:"192.168.2.148",expected_dns:"192.168.2.148",drift:false}` (✓ case) on prod `:29090`; Desktop + Mobile sign-off for the ✓ state.

---
