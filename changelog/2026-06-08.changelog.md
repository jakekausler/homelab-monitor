## [STAGE-004-042] Rule persistence model

**Summary:** Backend foundation for the user-authored alert flow (consumed by STAGE-043/044). Persists user-authored vmalert rules in SQLite (`log_user_rules`) and renders them to aggregate YAML files on shared writable volumes; vmalert picks them up via its 30s config poll. Backend-only vertical slice.

**Key changes:**

**Backend:**
- New SQLite table `log_user_rules` (migration `0041`): rule_name (unique), expr, expr_kind (logsql|metricsql), severity, summary, description, for_duration, source_kind/source_ref (source tracking for 043/044), enabled flag, timestamps.
- New `LogUserRulesRepository` (apps/monitor/homelab_monitor/kernel/logs/user_rules_repo.py) — CRUD with name-uniqueness (409 on conflict), `list_enabled()`, enable/disable; validate-before-persist (name regex, non-empty expr, expr_kind enum, for_duration format, value sanitization, and a render dry-run) so an unrenderable rule cannot be saved.
- New `user_rules_render.py` (apps/monitor/homelab_monitor/kernel/logs/user_rules_render.py) — pure `render_yaml(rules)->str` + atomic `render_all(repo, logs_path, metrics_path)->bool` that fully reconciles both aggregate files from enabled rules (orphan-free by full rewrite; disabled rules omitted). Logsql group emits `type: vlogs`; metricsql defaults to prometheus. Atomic write mirrors `kernel/cron/render.py` (mkstemp→os.replace, 0o640, config-group owned).
- New REST API under `/api/logs/user-rules`: GET (list, `?enabled=true` filters), GET by id, POST (create), PATCH (update), DELETE, POST enable/disable. CSRF on all mutations; session auth on all endpoints.
- Render-on-boot lifespan block (8d): re-renders all enabled rules from SQLite on startup.
- Two new shared named volumes `data_vmalert_user_logs` / `data_vmalert_user_metrics` (monitor rw, vmalert ro) + a second `-rule=` glob on each vmalert instance, in both prod and test compose. config-init/shared-init prepare the volumes (3777 root:amconfig); Dockerfile pre-creates the dirs.
- New `make verify-rules` target.

**Notes:**
- Rules become active within ~30s (vmalert config-check poll; no explicit reload — vector precedent).
- Valid vmalert logsql ALERTING expressions require a `| stats ...` pipe; expr-validity validation beyond YAML-render is intentionally out of scope for this stage (deferred).
- Two bugs caught + fixed during prod-rig refinement: new volumes needed permission prep (were root:root 0755, monitor uid couldn't write); logsql render group needed `type: vlogs` (else vmalert-logs treated it as prometheus → health=err).

**Files:**
- Backend: apps/monitor/alembic/versions/0041_log_user_rules.py (new); apps/monitor/homelab_monitor/kernel/logs/user_rules_render.py (new); apps/monitor/homelab_monitor/kernel/logs/user_rules_repo.py (new); apps/monitor/tests/test_log_user_rules_api.py (new); apps/monitor/tests/test_log_user_rules_repo.py (new); apps/monitor/tests/test_user_rules_render.py (new); apps/monitor/homelab_monitor/kernel/api/routers/logs.py; apps/monitor/homelab_monitor/kernel/api/schemas.py; apps/monitor/homelab_monitor/kernel/api/lifespan.py; apps/monitor/homelab_monitor/kernel/config.py; apps/monitor/tests/test_db_migrations.py; apps/monitor/Dockerfile; deploy/compose/docker-compose.yml; deploy/compose/docker-compose.test.yml; Makefile.
- Contract: packages/shared-types/openapi.json.

**Validation:** Backend verify 4 new test files (100% coverage of new kernel modules), extended schema tests, db migration tests; backend e2e validated on prod rig (render to yaml, vmalert picks up user rules within 30s, enabled/disabled flags control yaml presence, atomic writes prevent orphans). All `make verify` green: ruff/black clean, pyright 0 errors, pytest 4243 passed + 100% kernel coverage, tsc 0 errors, eslint 0 errors, vitest 1257 passed, UI build smoke.

**Implementation:** (commit: cea6d6c)

## [STAGE-004-038] Anomaly Type D: Log signature silence detection + allowlist

**Summary:** Log signature silence detection — when a recurring log signature stops appearing for 15-60 minutes (configurable), a backend collector emits an alert signaling a service/job may have stopped. An operator-managed allowlist suppresses the silence alert for expected silences (scheduled maintenance, cron jobs with known quiet windows, etc.). Suppression uses schedule kinds (always / window / cron) and is checked per-signature at collection time, ensuring suppressed signatures never fire alerts. Frontend Silence Allowlist tab provides full CRUD with an optional signature picker and responsive UI refinements.

**Key changes:**

**Backend:**
- New `SilenceDetectionCollector` (BaseCollector) — DB-reader that ticks every 60s, reads `log_signatures` + the new `log_signature_silence_allowlist` table, emits gauge `homelab_log_signature_silent{service_key, template_hash}=1` for signatures silent within `[silent_min_seconds=900, silent_max_seconds=3600]` (15-60 min, configurable) that are not suppressed and not allowlist-covered. Emits mandatory `homelab_collector_run_silence_detection` self-metric.
- New `is_silence_allowed(schedule_kind, schedule_value, expires_at)` evaluator (kernel/logs/silence_schedule.py) — schedule kinds always (silence always allowed) / window (ISO 8601 range, e.g. `2025-12-25/2025-12-26`) / cron (silence allowed EXCEPT within `cron_grace_seconds=900` after previous cron fire — i.e. alert can only fire just after a scheduled job's run window ends).
- New `SilenceAllowlistRepository` (kernel/logs/silence_allowlist_repo.py) + table `log_signature_silence_allowlist` (migration 0040) — fields service_key, template_hash, schedule_kind (enum: always/window/cron), schedule_value (JSON string), window_start, window_end (ISO strings for window kind), cron_expression, cron_grace_seconds, expires_at, created_by, created_at. UNIQUE (service_key, template_hash) enforces one allowlist entry per signature.
- New REST API (routers/logs.py): GET/POST (CSRF)/DELETE at `/api/logs/signatures/silence-allowlist`. SilenceAllowlistCreateRequest validates schedule_kind/value cross-consistency (e.g. window kind requires window_start+window_end, cron kind requires cron_expression), rejects unsupported `@reboot` cron, validates ISO window ranges, validates expires_at is in future. List response includes is_expired computed flag.
- New `SilenceDetectionConfig` in kernel/config.py (HOMELAB_MONITOR_SILENCE_*_SECONDS, HOMELAB_MONITOR_SILENCE_GRACE_SECONDS; defaults 900/3600/900).
- New vmalert-metrics rule deploy/vmalert/metrics/signature_silent.yaml (dumb `homelab_log_signature_silent == 1` with labels category=log-anomaly/anomaly_kind=signature_silent/severity=warning). Note: `.yaml` (the glob skips `.yml`).

**Frontend:**
- New SilenceAllowlistTab (Logs → Silence Allowlist) with rhf + zod form: schedule-kind-aware conditional fields (always kind shows just expires_at; window kind shows ISO window inputs; cron kind shows cron expression + grace_seconds input). List section displays service_key + signature, schedule summary (e.g. "every Tue 02:00-04:00"), expiry, delete button.
- Optional signature picker (autocomplete from GET `/api/logs/signatures`, autofills service_key + template_hash) — user can paste/search rather than copy-paste from Signatures tab.
- Refinement additions (user-requested per dual sign-off):
  - "Template Hash" → "Signature" relabel in Signatures and Models tabs (visible labels only; API/DB field `template_hash` unchanged).
  - Description blocks atop Silence Allowlist / Signatures / Models tabs (brief explainers of the feature's purpose).
  - Local-timezone helper text on window/expires datetime inputs (`Displayed in <system timezone>`).
- Bug fix: vertical-scroll clipping in the Logs tab-content area — SilenceAllowlistTab now uses internal `flex h-full min-h-0 flex-col` + `overflow-auto` scroll region (matching Signatures/Models tabs, inherited from LogsExplorerBody pattern).

**Important clarification:** The silence allowlist suppresses ONLY the "signature went silent" alert (`homelab_log_signature_silent` → vmalert rule `SignatureSilent`). It does NOT affect log storage/ingestion, signature catalog updates (NewLogSignature, SignatureSpike, ServiceErrorRateSpike, etc.), or any other system behavior — purely suppresses one alert type.

**Files:**
- Backend: apps/monitor/homelab_monitor/kernel/logs/silence_schedule.py (new); kernel/logs/silence_allowlist_repo.py (new); kernel/metrics/silence_detection_collector.py (new); kernel/config.py; kernel/api/routers/logs.py; kernel/api/lifespan.py; alembic/versions/0040_log_signature_silence_allowlist.py (new); tests/test_db_migrations.py; tests/test_silence_schedule.py (new); tests/test_silence_allowlist_repo.py (new); tests/test_silence_detection_collector.py (new); tests/test_api_silence_allowlist.py (new); tests/test_config.py (SilenceDetectionConfig).
- Frontend: apps/ui/src/routes/logs/LogsTab.tsx; routes/logs/tabs/SilenceAllowlistTab.tsx (new); api/logs.ts; lib/silenceScheduleFormat.ts (new, schedule summary renderer); components/logs/__tests__/SilenceAllowlistTab.test.tsx (new); api/__tests__/logs.test.ts; routes/logs/tabs/__tests__/SignaturesTab.test.tsx (relabel test); routes/logs/tabs/__tests__/ModelsTab.test.tsx (relabel test).
- Deploy: deploy/vmalert/metrics/signature_silent.yaml (new).
- Contract: packages/shared-types/openapi.json.

**Tests:** Backend 4 new test files (100% coverage of new kernel modules), extended test_config; frontend new tab + integration tests; backend e2e validated on prod rig (4/4: gauge emission → vmalert rule fires / allowlist suppresses for all 3 schedule kinds / cron grace window / expired entries ignored). All `make verify` green: ruff/black clean, pyright 0 errors, pytest 4207 passed + 100% kernel coverage, tsc 0 errors, eslint 0 errors, vitest 1257 passed, UI build smoke.

**Implementation:** 17b0833

## [STAGE-004-039] Severity escalation rules (L1) — critical-severity log line safety-net alert

**Summary:** A safety-net alert that fires when any log line at severity critical/emergency/alert appears from any service within the last 5 minutes. This closes the gap where nothing was watching the severity field at this level. The rule uses a static LogsQL query in vmalert-logs (mirroring the KernelOOM approach) with per-service override infrastructure deferred to STAGE-042.

**Key changes:**

**Backend:**
- New vmalert-logs rule `deploy/vmalert/logs/severity_escalation.yaml` — type: vlogs, alert `CriticalLogLine`, LogsQL expr `_time:5m severity:(critical OR emergency OR alert OR "0" OR "1" OR "2") | stats by (service) count() as crit_count | filter crit_count:>0`, for: 0m. Labels: category=log-anomaly, anomaly_kind=severity_escalation, severity=critical, source_tool=vmalert-logs, target_kind=container. Annotations include Explorer deep-link.
- Key correctness point: VictoriaLogs severity field is non-uniform — docker-origin lines carry WORD severities (critical/emergency/alert), journald/systemd lines carry NUMERIC syslog PRIORITY (0=emergency, 1=alert, 2=critical). The rule matches BOTH; matching only words would silently miss every journald-origin critical line. The dual-match is non-negotiable.
- Fast-fire twin rule `deploy/vmalert/logs-test/severity_escalation.yaml` (_time:30s, interval:5s) for integration testing.
- Config schema (parsed-but-unused until STAGE-042): new `SeverityFloor` dataclass + `LogsConfig.severity_escalation_excluded_services` + `LogsConfig.severity_escalation_floors` in kernel/config.py, parsing `logs.severity_escalation.excluded_services` and `.severity_floors` (mirrors the ErrorRateOverride parsed-but-unused pattern).
- Scope deferred: per-service excluded_services + severity_floors are config-schema-only in v1 (parsed but unused); enforcement logic lands in STAGE-042.

**Files:**
- Backend: deploy/vmalert/logs/severity_escalation.yaml (new); deploy/vmalert/logs-test/severity_escalation.yaml (new); apps/monitor/homelab_monitor/kernel/config.py (SeverityFloor, LogsConfig.severity_escalation_*); apps/monitor/tests/test_config.py (14 new tests).

**Validation:** Backend e2e 4/4 on the LIVE prod rig — rule loads in vmalert-logs from the .yaml file; word-severity line fires; numeric-severity "2" line ALSO fires — dual-match confirmed on real infra; info line does not fire. Verify GREEN: ruff/black clean, pyright 0 errors, pytest 4220 passed + 100% kernel coverage, tsc 0 errors, eslint 0 errors, vitest 1257 passed, UI build.

**Implementation:** d11965b

## [bugfix] vmalert .yml glob — container_crash / container_healthcheck rules were silently not loading

**Summary:** Fixed two vmalert-metrics rule files that were silently never loaded in production. vmalert globs `-rule=/etc/vmalert/metrics/*.yaml` (`.yaml` extension ONLY), so `container_crash.yml` (from STAGE-004-032) and `container_healthcheck.yml` (from STAGE-004-033) were silently skipped — the ContainerCrashed and ContainerUnhealthy alerts were DEAD in production since those stages shipped.

**Root cause:** Same `.yml`/`.yaml` glob mismatch that was fixed for `error_rate_spike` in STAGE-004-037; these two were the remaining instances, flagged in STAGE-037's notes as a follow-up.

**Key changes:**

**Backend:**
- Renamed `deploy/vmalert/metrics/container_crash.yml` → `container_crash.yaml` (alert ContainerCrashed, expr `increase(homelab_container_crash_total[5m]) > 0`) via git mv (history preserved). File contents unchanged — extension only.
- Renamed `deploy/vmalert/metrics/container_healthcheck.yml` → `container_healthcheck.yaml` (alert ContainerUnhealthy, expr `increase(homelab_container_healthcheck_unhealthy_total[5m]) > 0`) via git mv (history preserved). File contents unchanged — extension only.
- Added regression-guard test fixtures `deploy/vmalert/metrics/__tests__/container_crash.tests.yaml` and `container_healthcheck.tests.yaml` (fire + no-fire cases), matching the existing `*.tests.yaml` fixture format. These are static forward-compatible fixtures, consistent with the other `__tests__` fixtures — not yet CI-wired.
- Corrected the `.yml`→`.yaml` paths in `epics/EPIC-004-logs-pipeline/regression.md` (live operational doc).
- Added dated fix-notes to `STAGE-004-032.md` / `STAGE-004-033.md` / `STAGE-004-037.md` (historical docs; STAGE-037 had already flagged this as a known latent bug). The 2026-06-07 changelog entries are left as-is (accurate point-in-time record).

**Validation:** VERIFIED on the live prod rig — vmalert-metrics `/api/v1/rules` now shows ContainerCrashed AND ContainerUnhealthy loaded with health=ok from their `.yaml` files (both were ABSENT before the rename); all 13 other rule groups intact. `make verify` GREEN (check-yaml validates the renamed files + new fixtures; no Python/frontend touched, pytest 4220 + 100% kernel coverage unaffected).

**Files:**
- Backend: deploy/vmalert/metrics/container_crash.yaml (renamed from .yml); deploy/vmalert/metrics/container_healthcheck.yaml (renamed from .yml); deploy/vmalert/metrics/__tests__/container_crash.tests.yaml (new); deploy/vmalert/metrics/__tests__/container_healthcheck.tests.yaml (new); epics/EPIC-004-logs-pipeline/regression.md; epics/EPIC-004-logs-pipeline/STAGE-004-032.md; epics/EPIC-004-logs-pipeline/STAGE-004-033.md; epics/EPIC-004-logs-pipeline/STAGE-004-037.md.

**Implementation:** c95b90a

## [STAGE-004-040] Throttle/budget alerts (L2) — log-stream operational-health alerts + collector fix

**Summary:** Three vmalert-metrics alerts on log-stream operational health, each with anomaly_kind=log_budget_warning / category=log-anomaly. Addresses throttling visibility, budget overage, and unusual spike detection. Also fixes a critical pre-existing bug in the log_stream_budget collector (STAGE-001-018) that had been querying a fictional VictoriaLogs endpoint since it shipped — the collector had NEVER emitted any metrics against real VL. Rewrite to real `/select/logsql/stats_query` endpoint restores functional budget tracking and enables all three new alerts to fire correctly.

**Pre-existing bug fixed in-scope (stated prominently):** The log_stream_budget collector (from STAGE-001-018) had been querying a FICTIONAL VictoriaLogs endpoint `/select/logsql/stats` (returns 400, does not exist) and parsing an invented response shape — it had NEVER emitted any metrics against real VictoriaLogs since it shipped. Rewrote it to use the real `/select/logsql/stats_query` endpoint with two queries (today bytes+lines via `* | stats by (host,service) count() as lines, sum_len(_msg) as bytes_today` scoped to UTC-midnight; 5m line-rate baseline) and parse the correct Prometheus instant-vector response shape. This means `homelab_log_stream_bytes_today` / `homelab_log_stream_lines_per_sec` / `homelab_log_stream_bytes_budget` now actually populate (validated live: 177 real streams). The bug was invisible to unit tests because the tests mocked the same fictional shape (closed validation loop). Also: `config.py` `load_log_stream_budget_config` now raises contextual ValueError on malformed numeric values.

**Key changes:**

**Backend:**
- New vmalert-metrics rule file `deploy/vmalert/metrics/budget_threshold.yaml` (group budget_threshold):
  - `LogStreamApproachingBudget` — fires when a service's daily VL byte usage ≥80% of its budget (`homelab_log_stream_bytes_today / homelab_log_stream_bytes_budget > 0.80`), `for: 5m`, warning. Labels: category=log-anomaly, anomaly_kind=log_budget_warning, severity=warning.
  - `LogStreamThrottling` — fires when vector's throttle transform has events received-but-not-forwarded (`increase(vector_component_received_events_total{component_id="throttle"}[5m]) - increase(vector_component_sent_events_total{component_id="throttle"}[5m]) > 0`), `for: 2m`, warning. Labels: category=log-anomaly, anomaly_kind=log_budget_warning, severity=warning.
  - `LogStreamUnusualLineRate` — fires when a stream's lines/sec exceeds 5× its 1h baseline (clamp_min floor 1), `for: 5m`, info. Labels: category=log-anomaly, anomaly_kind=log_budget_warning, severity=info.
- New promtool test fixtures `deploy/vmalert/metrics/__tests__/budget_threshold.tests.yaml` (fire + no-fire cases for all 3 alerts).
- Collector rewrite: `apps/monitor/homelab_monitor/plugins/collectors/builtin/log_stream_budget.py` — now emits three gauges: `homelab_log_stream_bytes_today{host,service}` (daily cumulative bytes), `homelab_log_stream_lines_per_sec{host,service}` (5m rate baseline), `homelab_log_stream_bytes_budget{host,service}` (configured bytes_per_day_per_stream, default 500 MiB). New `__init__` param `default_budget_bytes_per_day_per_stream` defaults to `load_log_stream_budget_config()`. Endpoint fix: uses real VL `/select/logsql/stats_query` with two LogsQL queries scoped to UTC-midnight and current 5m window. Fixed HTTP error handling (no longer silently swallows 400s; now raises on malformed response).
- `kernel/config.py` — new `load_log_stream_budget_config()` method with contextual ValueError on malformed numeric values (e.g., non-integer bytes_per_day_per_stream). Validation moved from test mocks to production config load.
- Vector `deploy/vector/vector.toml.template` — added `[sources.internal_metrics]` (prometheus scrape self-diagnostics at :9598) routed into existing prometheus_exporter sink (vector:9598, already scraped by vmagent) — surfaces vector's component-level throttle counters (received_events_total / sent_events_total per component_id).
- Implementation approach: Option A (static rules) + Option B3 (log-stream budget + collector fix) + Option C (line-rate anomaly); per-service overrides deferred to STAGE-042.

**Tests:**
- Rewritten `apps/monitor/tests/test_log_stream_budget_collector.py` — mocks now use REAL VL response shape (Prometheus instant vectors), validates all 3 gauges emit correctly, validates budget_bytes config override, validates HTTP 200 / error paths.
- New `apps/monitor/tests/test_config.py` tests for malformed numeric values in log_stream_budget_config (non-integer bytes, negative values, missing keys).

**Validation:** Backend e2e on the LIVE prod rig — collector emits all 3 gauges for 177 real streams (bytes_budget=524288000 per service), HTTP 200 every tick (no more 400s from the fictional endpoint); LogStreamThrottling actively FIRING on real throttled events (received-sent=3090); all 3 rules health=ok. Verify GREEN: ruff/black clean, pyright 0 errors, pytest 4231 passed + 100% kernel coverage, tsc 0 errors, eslint 0 errors, vitest 1257 passed, UI build smoke.

**Files:**
- Backend: deploy/vmalert/metrics/budget_threshold.yaml (new); deploy/vmalert/metrics/__tests__/budget_threshold.tests.yaml (new); apps/monitor/homelab_monitor/plugins/collectors/builtin/log_stream_budget.py (collector rewrite + 3 gauges); apps/monitor/homelab_monitor/kernel/config.py (budget config access + contextual errors); apps/monitor/tests/test_log_stream_budget_collector.py (rewritten); apps/monitor/tests/test_config.py (malformed-value tests); deploy/vector/vector.toml.template (internal_metrics source).

**Implementation:** 03eb802

## [STAGE-004-041] VL backend health alerts (L3) — VictoriaLogs operational health

**Summary:** VictoriaLogs backend-health monitoring — detect VL problems before users see silently-empty log viewers. A health-probe collector emits up/latency/disk gauges, plus 4 vmalert-metrics alerts (all anomaly_kind=log_backend_health, category=ops) that fire on VL outages, latency spikes, and disk pressure. Reuses existing self_disk collector's per-slot infrastructure to avoid redundant disk scanning.

**Key changes:**

**Backend:**
- New `VlHealthCollector` (apps/monitor/homelab_monitor/plugins/collectors/builtin/vl_health.py) — 30s probe: GET {vl_url}/health, emits two gauges: `homelab_vl_up` (1.0 on 2xx, 0.0 on non-200/timeout/transport error), `homelab_vl_response_time_seconds` (last-probe latency, emitted always including on failure). Non-200 responses or transport errors → up=0.0 + collector ok=True (a down VL is a successful probe); ok=False only on http_client malfunction. Mirrors log_stream_budget's vl_url/http_client injection pattern.
- New config `VlHealthConfig` + `load_vl_health_config()` in kernel/config.py — parses `HOMELAB_MONITOR_VL_HEALTH_TIMEOUT_S` (default 5.0s).
- New vmalert-metrics rule file `deploy/vmalert/metrics/vl_health.yaml` — group vl_health:
  - Recording rule `homelab:vl_disk_used_pct = 100 * homelab_self_disk_used_bytes{slot="vl"} / (homelab_self_disk_budget_bytes{slot="vl"} > 0)` — reuses existing self_disk collector's per-slot bytes; the `>0` guard prevents divide-by-zero.
  - `VictoriaLogsDown` — `homelab_vl_up == 0 OR absent(homelab_vl_up)`, for: 1m, severity=critical. Labels: category=ops, anomaly_kind=log_backend_health.
  - `VictoriaLogsHighLatency` — `quantile_over_time(0.95, homelab_vl_response_time_seconds[5m]) > 2`, for: 5m, severity=warning. Labels: category=ops, anomaly_kind=log_backend_health. (Metrics writer lacks histogram support; latency as GAUGE + quantile_over_time per spec design constraint.)
  - `VictoriaLogsDiskWarning` — `homelab:vl_disk_used_pct > 70`, for: 10m, severity=warning. Labels: category=ops, anomaly_kind=log_backend_health.
  - `VictoriaLogsDiskCritical` — `homelab:vl_disk_used_pct > 85`, for: 5m, severity=critical. Labels: category=ops, anomaly_kind=log_backend_health.
- New promtool test fixtures `deploy/vmalert/metrics/__tests__/vl_health.tests.yaml` (fire + no-fire cases for all 4 alerts + recording rule).
- `kernel/config.py` — VlHealthConfig addition only (no backend integration logic moved; reuses existing per-stage VlDiskWarningConfig thresholds from STAGE-022 /settings/logs UI).
- `kernel/api/lifespan.py` — registers + injects VlHealthCollector with resolved vl_url + http_client.

**Design notes:**
- **Latency is a GAUGE** — VictoriaLogs health endpoint returns response time as metadata (not histograms); emitted as gauge, queried via quantile_over_time in PromQL (degraded but correct per spec infeasibility of true histograms in metrics writer).
- **VL disk pct via recording rule over self_disk** — no separate/redundant disk collector; recording rule leverages the existing slot-based bytes from self_disk. EPIC-014 forward-ref (VM + SQLite mirror) already documented this pattern.
- **source_tool: vmalert-metrics** on all 4 alerts — intentional deviation from spec's "homelab-monitor"; matches all other rule groups + Karma routing expectation.

**Files:**
- Backend: deploy/vmalert/metrics/vl_health.yaml (new); deploy/vmalert/metrics/__tests__/vl_health.tests.yaml (new); apps/monitor/homelab_monitor/plugins/collectors/builtin/vl_health.py (new); apps/monitor/homelab_monitor/kernel/config.py (VlHealthConfig + loader); apps/monitor/homelab_monitor/kernel/api/lifespan.py (register + inject); apps/monitor/tests/test_vl_health_collector.py (new); apps/monitor/tests/test_config.py (VlHealthConfig tests).

**Validation:** Backend e2e 4/4 on the LIVE prod rig — stopped the VictoriaLogs container, homelab_vl_up flipped to 0.0 and VictoriaLogsDown fired (pending→firing); restarted VL, homelab_vl_up recovered to 1.0 and alert resolved. Recording rule computed 1.05% disk_used_pct from real self_disk bytes; all 5 rules (recording + 4 alerts) health=ok; self-metric invariant confirmed (homelab_collector_run_success_total{name="vl_health"}=1). Verify GREEN: ruff/black clean, pyright 0 errors, pytest 4240 passed + 100% kernel coverage, tsc 0 errors, eslint 0 errors, vitest 1257 passed, UI build.

**Implementation:** fd94b00
