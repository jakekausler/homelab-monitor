2026-06-28 bdabd08 EPIC-008 / STAGE-008-028: Surveillance page shell + camera grid + license/HomeMode strip (+ CameraRow model/ip/vendor enrichment)

Added the Integrations -> Surveillance page (FRONTEND) plus a small backend enrichment to the surveillance /cameras endpoint.

Frontend:
- New "Surveillance" sidebar entry (Cctv icon) placed alphabetically between Pi-hole and Synology; route /integrations/surveillance redirects to /cameras.
- SurveillanceLayout: header + license/HomeMode/cameras status strip (Badge chips from useSurveillanceSummary, honest null/data_available handling) + single "Cameras" tab + Outlet (scaffolded for STAGE-008-029's Activity tab).
- SurveillanceCamerasTab: responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3) of camera cards showing name, connected badge, model/ip/vendor/status/recordings_count; honest empty states (data_available=false -> "no data yet"; null -> "—"; zero -> "0").
- New api hooks (surveillance.ts: useSurveillanceSummary + useSurveillanceCameras) and surveillanceFormat.ts (cameraConnectedBadge). Vitest tests for layout + cameras tab (semantic queries).

Backend (folded in, user-approved):
- CameraRow gains nullable model/ip/vendor; /cameras joins the homelab_synology_ss_camera_info metric by camera name (canonical camera set still from homelab_synology_ss_camera_connected; _info is enrichment-only). A _info_label helper coalesces the collector's empty-string labels ("") to None so the contract reports honest unknowns rather than empty strings masquerading as values (code-review finding).
- openapi.json regenerated for the CameraRow change.

Gates: backend 6681 passed, 1 skipped, 100.00% branch coverage; frontend 0 eslint errors / 0 tsc errors / 1877 vitest / UI build OK; pyright 0 errors. Code review: 1 Important finding (empty-string-vs-None contract leak) implemented + clean 2nd review. Refinement: Desktop + Mobile viewport sign-off on prod (sha256 served==built proven).

---

2026-06-28 4442147 EPIC-008 / STAGE-008-027: Synology Ops widgets + stale-read fix + mount-health wireup

Filled the Synology page Ops tab with backup/replication/updates/security/connections/mount-health widgets, and fixed two real issues found during Refinement (a correctness-critical backend stale-read bug + the mount-health prod-config no-op).

Frontend (Ops-tab widgets):
- `apps/ui/src/api/synology.ts`: added `useSynologyOps()` + `useSynologyConnections()` hooks + queryKeys.
- `apps/ui/src/routes/integrations/SynologyOpsTab.tsx`: 6 widgets — Backup (no-backup-configured warn banner / configured+last_result), Security (security_safe + per-severity findings via severityVariant; null-count→muted "—" chip, zero-count→derived "N other severities clear" caption — honest null-vs-zero), Updates (DSM-update badge + only update_available packages), Replication (replication_available banner + 14-share btrfs snapshot table), Connections (live DSM re-query + "live read — not stored" caption + graceful-empty), Mounts (mount_up/free table + graceful-empty). Connections in its own QueryState. Honest-empty captions for 4 card-vs-contract deltas.
- `apps/ui/src/routes/integrations/synologyFormat.ts`: added `severityVariant()` + row-type aliases.

Backend (stale-read fix — correctness-critical):
- `apps/monitor/homelab_monitor/kernel/api/routers/integrations_synology.py`: the `/summary` + `/ops` endpoints read metrics from two HOURLY collectors (synology_updates + synology_security, both 3600s) via bare instant query — VictoriaMetrics' 5-min staleness window returned EMPTY for ~58min of each hour, so the UI falsely showed "DSM up to date / Secure / no updates" when the NAS had a pending DSM update + 4 package updates + a security finding. FIX: wrapped 5 query constants (_Q_DSM_UPDATE_AVAILABLE, _Q_SECURITY_SAFE, _Q_PACKAGES_WITH_UPDATES, _Q_PACKAGE_UPDATE_AVAILABLE, _Q_SECURITY_FINDINGS_TOTAL) in `last_over_time(<metric>[2h])`. Updated the test mocks (keyed on query strings) + added a lock-in test asserting the constants contain "last_over_time". Confirmed live: all 3 DSM update APIs succeed (the bug was query-staleness, NOT permissions/auth/parse).

Prod config (host-specific, NOT in the public repo — see REG-008-027-MOUNT-CONFIG): mount-health monitoring activated by setting `HM_RACKSTATION_SRC=/rackstation` in deploy/compose/.env (gitignored) + a `synology_mount_health.yaml` override (9 mounts) in the gitignored overrides repo. Post-fix: 9 NFS mounts emit `homelab_synology_mount_up`, the Mounts widget shows them all Up.

Tests + viewport sign-off: backend pytest 6678 passed / 100% branch coverage; frontend make ui-verify (eslint 0 errors, tsc, vitest 1864, build) green. Desktop + Mobile approved.

---

2026-06-28 12fe858 EPIC-008 / STAGE-008-026: Synology hardware/storage widgets + SSH-probe-state + live probe fix + script canonicalization

Filled the Synology page Hardware tab with live hardware/storage widgets, extended the /hardware endpoint, fixed the production SSH probe end-to-end, and canonicalized the forced-command probe script in-repo. MIXED backend + frontend + deploy artifact.

Backend (`kernel/api/routers/integrations_synology.py`):
- `SynologySshProbe` += `up`, `host_key_mismatch` (bool, from homelab_ssh_up / homelab_ssh_host_key_mismatch) + `last_success_age_seconds`, `probe_duration_seconds` (float|None) — satisfies the EPIC-017 probe-state render mandate for the Synology page.
- `DiskRow` += `model` (str, from the homelab_synology_disk_temp_celsius{model} label, disk-id fallback) + `remain_life` (float|None; -1 = HDD N/A).
- `VolumeRow.status` / `PoolRow.status` changed `str` → `list[str]`: the collector emits one series per status condition, so the endpoint now dedups by name and aggregates all status labels into a list (fixes duplicate volume/pool rows).
- OpenAPI + UI types regenerated.

Frontend:
- `api/synology.ts`: `useSynologyHardware()` + lazy `useSynologyDiskSmart(disk)` hooks.
- `routes/integrations/SynologyHardwareTab.tsx`: System / Volumes / RAID-Pool / UPS / SSH-probe panels + an 8-disk SMART grid (disk/model/status/smart/temp/remain_life/failing + per-row SMART drill-down). Multi-chip status per volume/pool with horizontal scroll on mobile (overflow-x-auto). Honest-empty SSH state.
- `routes/integrations/SynologyDiskSmartDialog.tsx` (new): per-disk SMART-attr drill-down Dialog (lazy fetch, honest-empty states).
- `routes/integrations/synologyFormat.ts` (new): threshold/tone/badge + remain_life helpers (statusTone maps known-bad states to critical, unknown→warn).

SSH-probe-script canonicalization (durability):
- `deploy/ssh-probes/hm-probe-synology.sh` (new): canonical LF-clean forced-command script (the prior NAS copy had drifted/corrupted with no repo source of truth).
- `cli/ssh_probe.py`: embedded `_SYNOLOGY_PROBE_SCRIPT` (byte-equality test enforces sync with the deploy file) + a Synology-specific `install-instructions` branch emitting the script + the DSM nologin `/bin/sh` shell-setting step + DSM-revert caveat.
- `tests/ssh/test_synology_probe_script.py` (new): `sh -n` lint + marker-presence + byte-equality tests.
- `docs/ssh-probe-setup.md`: references the canonical deploy file.

Production fix (REG-008-024-SSH-PROBE-DOWN RESOLVED): on the NAS — created the homelab-probe home dir, set its login shell to /bin/sh (was /sbin/nologin, which blocked the forced command), and redeployed the clean script. homelab_ssh_up{target="synology"}=1; load/cpu-temp/mdstat/uptime + full per-disk SMART now flow live.

Gates: backend pytest 6678 passed, 100% branch coverage; frontend make ui-verify (eslint 0 errors, tsc strict, vitest 1847, build) green. Desktop + Mobile viewport sign-off.

---

2026-06-28 4428323 EPIC-008 / STAGE-008-025: Synology page shell + sidebar + header status strip

Built the Synology Integrations page shell (frontend) consuming the STAGE-008-024 `/api/integrations/synology/summary` endpoint. Sidebar nav entry added alphabetically (Pi-hole · Synology · Unifi); route `/integrations/synology` redirects to the Hardware tab. The page is a two-tab shell (Hardware → STAGE-008-026, Ops → STAGE-008-027 — honest empty-state placeholders) with an always-visible header status strip above the tabs. Mirrors the Unifi/Pi-hole integration-page pattern.

- `apps/ui/src/api/synology.ts` (new): `useSynologySummary()` react-query hook (`apiClient.GET('/api/integrations/synology/summary')`, `unwrap`, 30s refetch) + `synologyQueryKeys`; mirrors `api/unifi.ts`.
- `apps/ui/src/routes/integrations/SynologyLayout.tsx` (new): layout + inline status strip + 2-tab nav + `<Outlet/>`. Strip chips: System health (`dsm_up` → OK/Degraded badge — ok/warn, NEVER red/"DSM down"; resolves REG-008-024-DSM-UP-FLAG), Volume (% with <80 ok / 80-90 warn / >90 critical / null muted), UPS (composite of on-battery + charge%, critical when on-battery & charge<20), Updated (relative `last_seen`). Loading/502/error branches mirror `PiholeStatusStrip`.
- `apps/ui/src/routes/integrations/SynologyHardwareTab.tsx` + `SynologyOpsTab.tsx` (new): `EmptyState` placeholders citing STAGE-008-026 / -027.
- `apps/ui/src/components/SidebarNav.tsx`: added the "Synology" nav entry (HardDrive icon, alphabetical).
- `apps/ui/src/router.tsx`: `synologyLayoutRoute` + index redirect to `/hardware` + Hardware/Ops child routes.
- Tests (new): `SynologyLayout.test.tsx` (strip chips, threshold cases, 502 banner, never-"down", UPS on-battery, 2-tab structure) + `SynologyHardwareTab.test.tsx` + `SynologyOpsTab.test.tsx`.
- Design note: the Overview and Metrics tabs were removed during Refinement (user-directed). The Synology Grafana dashboard embeds on the GLOBAL Metrics page as a new dashboard tab (STAGE-008-030 scope correction), NOT on the Synology integration page.
