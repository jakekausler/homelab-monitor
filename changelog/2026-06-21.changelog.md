2026-06-21 3f73f97 EPIC-007/STAGE-007-023: Embedded `<LogViewer>` (UDM syslog) Logs + Security-Events tabs + device-page logs embed

New Unifi-panel **Logs tab** (`/integrations/unifi/logs`): embedded LogViewer over UDM syslog with category chips (All/Firewall/Audit/System), time-range, refresh, and client-IP filter. New **Threats → Security Events tab** (`/integrations/unifi/threats`): filter `source_type:udm (service:udm-audit OR service:udm-firewall)` with honest banner (IDS/IPS requires CyberSecure). Device page (`/integrations/unifi/devices/$device`): embedded gateway-sourced logs section + Open-in-Explorer button.

Frontend-only; consumes existing `/api/logs/query` + `<LogViewer>`. New files: `udmLogFilters.ts`, `useUdmLogViewer.ts`, `UnifiLogsTab.tsx`, `UnifiThreatsTab.tsx`, `UnifiDeviceLogsSection.tsx` + tests. Generic API errors surface LogViewer `unavailable` state.

---

2026-06-21 a3129ca EPIC-007/STAGE-007-022: Clients tab + Client page + DPI pipeline migration (UniFi v2 traffic API)

New Integrations → Network → Clients tab: registry table of ~86 clients (name/IP/MAC/network/connection/last-seen/online/lease), the monitor host pinned first-class + exempt from search filter, client-side sort + debounced search. New Client page (`/integrations/network/clients/$mac`): identity, connection (Wi-Fi+signal / switch port), bandwidth, lease, per-client DPI, and a documented DNS-enrichment extension-point slot (`<ClientDnsSection>` + contract README) that renders an honest "provided by Pi-hole" placeholder until EPIC-006 fills it.

**DPI pipeline migrated to the UniFi v2 traffic API.** The classic `stat/stadpi` endpoint returns empty on UniFi Network 10.x (Ubiquiti moved per-app traffic); the collector now polls `/proxy/network/v2/api/site/default/traffic` (new `_get_v2` helper + `v2_traffic` method in the Unifi client; epoch-millisecond window). Real per-client per-app DPI now flows (0 → live series on the prod UDM).

Fixed: the per-client DPI query filtered the wrong label (`mac` → `client`), so the per-client DPI panel was always empty even with data.

DPI app/category numeric IDs now resolve to human names via a vendored catalog (`dpi_catalog_data.py`, 2,279 apps / 22 categories, compound key `(category<<16)+application`, sourced from ubntwiki/unpoller), with a graceful raw-ID fallback for IDs not in the catalog (~89% app / 100% category coverage on the live UDM).

Removed the orphaned classic `stat/dpi` client method (empty-sentinel on 10.x, no consumer). Endpoint-staleness audit: only DPI was outdated; threats are empty because IPS is disabled (legit); all other Unifi collector endpoints verified live + accurate.

Backend + frontend; gates green: `make verify` (backend 100% branch coverage, pyright strict) + `make ui-verify` (frontend). No openapi/schema regeneration (DPI app/cat remain string labels — no response-model shape change).

---
