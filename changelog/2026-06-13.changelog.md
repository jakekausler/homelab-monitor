## [STAGE-005-023] Entity-health + battery count widgets (HA panel Health tab)

**Summary:** Fill the HA panel's Health tab (built in 022) with two live COUNT widgets consuming the 021 `/summary` endpoint. A new shared `useHomeAssistantSummary()` hook (reused by 024), two pure presentational widget components, and a tab-level connection banner. Frontend-only. COUNTS-ONLY by design: the 021 contract returns counts, not per-entity detail, so the card's drill-lists + `stale` count were dropped and the drill-down work was tracked as new stages STAGE-005-027 (backend detail endpoints) + STAGE-005-028 (frontend drill-down widgets).

**New files:**
- `apps/ui/src/api/home_assistant.ts` — `useHomeAssistantSummary()` react-query hook (mirrors `docker.ts::useListContainers`): queryKey `['integrations','home-assistant','summary']`, `apiClient.GET('/api/integrations/home-assistant/summary', {})`, `unwrap<HaSummaryResponse>`, 30s refetch. Shared — STAGE-005-024 reuses it for the Status tab.
- `apps/ui/src/routes/integrations/HaEntityHealthWidget.tsx` — pure presentational 3-cell `<dl>` (Total / Available / Unavailable; Unavailable amber-tinted when > 0).
- `apps/ui/src/routes/integrations/HaBatteryWidget.tsx` — pure presentational 2-cell `<dl>` (Low amber / Critical red when > 0); "All batteries healthy" EmptyState when both 0.
- `apps/ui/src/routes/integrations/HaEntityHealthWidget.test.tsx`, `HaBatteryWidget.test.tsx`, `HomeAssistantHealthTab.test.tsx` — render tests (widget counts + tint + EmptyState; tab states via a `vi.mock('@/api/home_assistant')` hook mock).

**Modified files:**
- `apps/ui/src/routes/integrations/HomeAssistantHealthTab.tsx` — calls `useHomeAssistantSummary()` once; renders a tab-level connection banner (isPending → Loading; 502 VM-down → yellow "metrics temporarily unavailable"; `ha_up===false` → yellow "offline" banner with counts still shown; other errors → ErrorDisplay) + the two widgets (entities/battery props).
- `apps/ui/src/routes/integrations/HomeAssistantTabs.test.tsx` — removed the old Health-tab placeholder case (Status + Logs cases kept; Health now has its own hook-mocked test).

**Tracking (new future stages from deferral):** STAGE-005-027.md + STAGE-005-028.md created; EPIC-005 stage count 26 → 28. The deferred drill-down work (and the `stale` drop) is recorded against those stages.

**Design decisions:** D-023-SCOPE = counts-only (drill-lists deferred to 027/028; `stale` dropped — no data source). D-ENTITY-WIDGET-SHAPE = 3-cell `<dl>`, amber Unavailable. D-BATTERY-WIDGET-SHAPE = 2-cell `<dl>`, red Critical / amber Low, "all healthy" EmptyState. D-023-STATES = tab-level connection banner + per-widget EmptyState. D-023-HOOK = shared `useHomeAssistantSummary()`.

**Verification:** `make verify` GREEN — frontend tsc/eslint clean (0 errors, 94 baseline warnings), vitest 1453 passed, UI build pass; backend untouched (4801 tests, 100% coverage). Refinement 3a: Desktop + Mobile both approved against the rebuilt prod-rig UI (bundle index-C2Tm-Y2q.js) showing REAL data (entities 1906/922/984, battery 0/1).

**Commit:** (commit: fcd3dfa)

## [STAGE-005-024] Updates + integration-status count widgets (HA panel Status tab)

**Summary:** Fill the HA panel's Status tab (built in 022) with two live COUNT widgets consuming the 021 `/summary` endpoint via the SHARED `useHomeAssistantSummary()` hook (reused from 023, unchanged). Frontend-only. COUNTS-ONLY by design: the 021 contract returns counts, not per-item detail, so the card's lists were deferred and tracked — updates/config-entry/repairs detail → STAGE-005-027 (backend) + STAGE-005-028 (frontend) [their scope extended this stage]; notification bodies → new STAGE-005-029 (live HA re-query + privacy review). EPIC-005 stage count 28 → 29.

**New files:**
- `apps/ui/src/routes/integrations/HaUpdatesWidget.tsx` — pure presentational. Shows a single "Available" count (amber-tinted when > 0); "All up to date" EmptyState when 0. (Per a Refinement design change, the "Total" count was DROPPED — only the actionable Available count is shown.)
- `apps/ui/src/routes/integrations/HaIntegrationStatusWidget.tsx` — pure presentational 4-cell 2×2 `<dl>` (Loaded neutral / Errors red>0 / Repairs amber>0 / Notifications amber>0); "All integrations healthy" EmptyState when `error === 0 && repairs === 0 && notifications === 0` (gated on the actionable signals; `loaded` does not suppress healthy).
- `apps/ui/src/routes/integrations/HaUpdatesWidget.test.tsx`, `HaIntegrationStatusWidget.test.tsx`, `HomeAssistantStatusTab.test.tsx` — render tests (widget counts + tints + EmptyState; tab states via a `vi.mock('@/api/home_assistant')` hook mock).

**Modified files:**
- `apps/ui/src/routes/integrations/HomeAssistantStatusTab.tsx` — calls `useHomeAssistantSummary()` once; renders the same tab-level connection banner as the Health tab (isPending → Loading; 502 → yellow "metrics temporarily unavailable"; `ha_up===false` → yellow "offline" banner with counts still shown; other errors → ErrorDisplay) + the two widgets.
- `apps/ui/src/routes/integrations/HomeAssistantTabs.test.tsx` — removed the old Status-tab placeholder case (Logs case kept; Status now has its own hook-mocked test).

**Tracking (deferred-work stages, per the "deferred work MUST become a tracked stage" rule):** STAGE-005-027 + STAGE-005-028 scope extended (updates/config-entry/repairs detail endpoints + drill-list widgets); new STAGE-005-029 created (notification body detail — live re-query + privacy + safe render). EPIC-005 26 → 28 → 29 over stages 023+024.

**Design decisions:** D-024-SCOPE = counts-only. D-024-UPDATES-WIDGET-SHAPE = single Available count (Total dropped in Refinement). D-024-INTEGRATION-STATUS-WIDGET-SHAPE = 4-cell 2×2 `<dl>` + healthy-gate EmptyState. D-024-STATES = mirror HomeAssistantHealthTab. D-NOTIF-RENDER-SAFETY = evaporates (count only; safe-render deferred to 029).

**Verification:** `make verify` GREEN — frontend tsc/eslint clean (0 errors, 94 baseline warnings), vitest 1464 passed, UI build pass; backend untouched (4800 tests pass / 2 env-skipped, 100% coverage). Refinement 3a: Desktop + Mobile both approved against the rebuilt prod-rig UI (bundle index-ifncGUNI.js).

**Commit:** (commit: ac316d2)

## [STAGE-005-025] Embedded LogViewer (HA panel Logs tab)

**Summary:** Replaced the HA panel Logs-tab placeholder with the shared EPIC-004 `<LogViewer>` embedded via the documented embedding contract (caller-provided `useLogs` adapter), scoped to the Home Assistant log stream `service:homeassistant`. The view defaults to errors-focused (`severity:error OR severity:warn`) over the last 1h with an "Errors only ↔ All lines" toggle (all-lines uses match-all `*`), plus time-range / timezone / wrap / refresh controls and an "Open in Explorer" deep-link scoped to the HA service. The `useLogs` adapter mirrors `LogsExplorerBody.tsx` (the authoritative `useLogsQuery` consumer): `LogsQueryResponse` carries no `log_status`/`truncated`, so `logStatus` is derived from line presence and VictoriaLogs-down surfaces as HTTP 502 → unavailable banner; other errors render a benign empty state. Frontend-only.

**Service-label note:** scoped to the exact value `homeassistant` (no hyphen, verified against the live rig — 139,472 HA Core lines under `service:homeassistant`; the exact value avoids the unrelated `grocy-homeassistant` collision).

**New files:**
- `apps/ui/src/routes/integrations/HomeAssistantLogsTab.test.tsx` — 8-case test: available / no_lines / 502-unavailable / generic-error-benign / errors-default+`docker:homeassistant`-scope / toggle-to-`*` / refresh / open-in-explorer.

**Modified files:**
- `apps/ui/src/routes/integrations/HomeAssistantLogsTab.tsx` — placeholder → embedded `<LogViewer>` with headerSlot controls + `useLogs` adapter.
- `apps/ui/src/routes/integrations/HomeAssistantTabs.test.tsx` — deleted obsolete placeholder test, superseded by the dedicated co-located test.

**Design decisions:** D-HALOGS-SELECTOR = scope to `docker:homeassistant` (exact `homeassistant`, no hyphen — verified against the live rig; avoids the `grocy-homeassistant` collision). D-HALOGS-DEFAULT-QUERY = errors-focused default `severity:error OR severity:warn` over 1h with "Errors only ↔ All lines" toggle (all-lines = match-all `*`), lean headerSlot, plain `<LogViewer>` (no fillHeight). D-REAL-SERVICE-LABEL = confirmed `homeassistant` (no hyphen). Adapter mirrors `LogsExplorerBody.tsx`: `logStatus` derived from line presence (no `log_status` field in `LogsQueryResponse`), VL-down → HTTP 502 unavailable banner.

**Verification:** `make verify` GREEN — pyright 0, pytest 4800 pass / 2 expected env-skips, kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1470 pass, UI build OK.

**Commit:** (commit: 7a17ab7)

## [STAGE-005-026] Grafana dashboard `home-assistant.json` (HA overview)

**Summary:** Added a default Grafana dashboard-as-code for Home Assistant, provisioned automatically via the existing provisioning folder ("Homelab", 30s scan interval — no provisioning/compose/datasource change needed). Option B sectioned operational layout: 5 `row` sections (Availability & Status / Batteries / Updates & Config Health / Automation Cadence / Anomalies & Collector Health) over a 20-entry FLAT panels array (5 rows + 10 stat + 4 timeseries + 1 bargauge). Panel shapes mirror `host-overview.json`; uid `home-assistant`, `editable:false`, schemaVersion 40, tags `["homelab-monitor","home-assistant"]`; every panel + target on the `victoriametrics` datasource. All exprs verified against the real collectors and cardinality-safe (count/sum/topk only — never raw per-series across the ~1906 entities). Battery bargauge uses INVERTED thresholds (red base / yellow 10 / green 20) matching vmalert (critical <10, low 10-20); stale `>7200`, idle automations `>86400`, updates `count(==1)`, z-scores `topk(8, abs(...))`, collector-run health filtered `name=~"ha_.*"`. Backend/OpenAPI untouched — config + test only.

**New files:**
- `deploy/grafana/dashboards/home-assistant.json` — HA Grafana dashboard-as-code, 20-panel sectioned operational layout, uid `home-assistant`, provisioned to "Homelab" folder.

**Modified files:**
- `apps/monitor/tests/integration/test_grafana_provisioning.py` — added `test_grafana_home_assistant_dashboard_provisioned` (mirrors the existing `test_grafana_host_overview_dashboard_provisioned` pattern), @integration+@slow marker. Collects and deselects with `make verify`; runs under `make integration` (full test-compose stack).

**Design decisions:** D-DASHBOARD-PANELS = Option B sectioned dashboard (rejected A compact / C tables). D-DASHBOARD-MOUNT = no provisioning/compose/datasource change (auto-provisioned via existing folder). D-DASHBOARD-AS-CODE = `editable:false`, victoriametrics datasource (locked). D-DASHBOARD-LAST = final HA stage, complete health/status/anomaly overview (locked).

**Verification:** `make verify` GREEN — ruff clean, pyright 0, pytest 4800 pass / 2 expected env-skips (new integration test collected + deselected), kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1470, UI build OK. Refinement 3a+3b on the live prod rig: Grafana provisioned the dashboard (fetchable by uid `home-assistant`, 20 panels, 0 parse errors); panels render real HA data (`homelab_ha_up=1`, 1906 entities available, 41 updates pending, z-score top 4.47).

**Commit:** (commit: be9bf8c)

## [STAGE-005-030] Metrics page tabs (System + Home Assistant)

**Summary:** Converted the Metrics screen from a single hardcoded Grafana iframe into ROUTE-BASED tabs mirroring `HomeAssistantLayout`: `/metrics` redirects to `/metrics/system`; a tab bar (System | Home Assistant) switches between two embedded Grafana dashboards. System tab embeds `host-overview`; Home Assistant tab embeds `home-assistant` (uid verified, src `/api/grafana/d/home-assistant/home-assistant?kiosk`, returns 200 through the monitor's Grafana reverse-proxy). One shared `GrafanaDashboardEmbed` component (props src + title) holds the iframe + Open-in-Grafana link; two thin tab wrappers. Lazy iframes (only the active route's component mounts). Frontend-only; no backend/OpenAPI change.

**New files:**
- `apps/ui/src/routes/metrics/GrafanaDashboardEmbed.tsx` — shared presentational component: a kiosk Grafana iframe (props `src` + `title`) inside a flex-fill wrapper + an "Open in Grafana" link. Extracted from the old Metrics.tsx embed.
- `apps/ui/src/routes/metrics/MetricsLayout.tsx` — layout route housing the tab bar (System | Home Assistant) + `<Outlet />` for active tab content; mirrors `HomeAssistantLayout` structure.
- `apps/ui/src/routes/metrics/MetricsSystemTab.tsx` — tab wrapper rendering `<GrafanaDashboardEmbed src="/api/grafana/d/host-overview/host-overview?kiosk" title="System metrics (Grafana)" />`.
- `apps/ui/src/routes/metrics/MetricsHomeAssistantTab.tsx` — tab wrapper rendering `<GrafanaDashboardEmbed src="/api/grafana/d/home-assistant/home-assistant?kiosk" title="Home Assistant metrics (Grafana)" />`.
- `apps/ui/src/routes/metrics/GrafanaDashboardEmbed.test.tsx`, `MetricsLayout.test.tsx`, `MetricsSystemTab.test.tsx`, `MetricsHomeAssistantTab.test.tsx` — render tests (embed src/title rendering, Open-in-Grafana link, layout tab navigation).

**Modified files:**
- `apps/ui/src/router.tsx` — replaced single `metricsRoute` with `metricsLayoutRoute` (layout route) + index redirect (`/metrics` → `/metrics/system`) + two child routes (`/metrics/system`, `/metrics/home-assistant`); removed `MetricsPage` import.

**Deleted files:**
- `apps/ui/src/routes/Metrics.tsx` — superseded by the tabbed layout.
- `apps/ui/src/routes/Metrics.test.tsx` — superseded by the tabbed layout.

**Refinement (3a):** User viewport review caught an iframe top-clipping bug — the embed had carried the old single-page full-bleed classes (`-m-6 h-[calc(100vh-3.5rem)]`) which broke inside the new header+tabs layout. Fixed the embed wrapper to `relative h-full w-full` (fills MetricsLayout's bounded flex Outlet region, mirroring HomeAssistantLayout). User then approved both Desktop AND Mobile against the fixed bundle.

**Design decisions:** D-METRICS-TABBED = tabbed Metrics page (System + Home Assistant), user-requested. D-METRICS-ROUTE-BASED = route-based tabs (layout route + index redirect + child routes), not state-based. D-METRICS-LAZY = lazy iframes inherent to route-based tabs. D-METRICS-HA-UID = `home-assistant` uid. D-METRICS-TABS-PATTERN = mirror HomeAssistantLayout. D-METRICS-EMBED-SHARE = one shared GrafanaDashboardEmbed component.

**Verification:** `make verify` GREEN (re-run post-fix) — ruff clean, pyright 0, pytest pass / expected env-skip, kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1477, UI build OK.

**Commit:** (commit: 62c44ea)

## [STAGE-005-027] HA entity/battery/updates/config-entry/repairs DETAIL endpoints (drill-down backend)

**Summary:** Added 5 backend GET endpoints to `integrations_home_assistant.py` returning typed per-entity row lists sourced from VictoriaMetrics PER-SERIES queries (extending the 021 `/summary` per-aggregate read path to per-series row extraction via `vm_instant_query`): `/entities?filter=unavailable`, `/batteries?filter=low_or_critical`, `/updates`, `/config-entries?filter=error`, `/repairs`. VM-only (Option C, honoring locked D-DETAIL-CONSUMES-VM — no live-HA re-query). Each `Depends(require_session())`, 502 auto-propagates via vm_instant_query, empty-vector → empty list. `/entities` joins the unavailable set with last_changed age, sorts stalest-first, and caps at top-N=100 (`total` reflects full ~962, `returned`=capped) — cardinality-safe for ~1906 entities. config-entry ships coarse `state="error"`. 10 new Pydantic models (`ConfigDict(extra="ignore")`, `total`/`returned`/`filtered_to`).

**New files:**
- `apps/monitor/tests/test_api_ha_detail.py` — 28 tests: happy-path + cap + sort + empty + 502 + 401 + non-numeric-skip + missing-label-skip per endpoint.

**Modified files:**
- `apps/monitor/homelab_monitor/kernel/api/routers/integrations_home_assistant.py` (5 endpoints + 10 models + `_sample_float` helper + module consts).
- `packages/shared-types/openapi.json` (regenerated — 5 new paths).
- `apps/monitor/tests/test_container_crash_reconciler.py` (PRE-EXISTING fix, unrelated: 5 tests were a time-bomb — hardcoded `finished_at="2026-06-07"` aged past the reconciler's 7-day prune window; replaced with rot-proof `now - 1h` anchors; production code unchanged).

**Design decisions:** D-DETAIL-VM-LABELS-PRESENT = verified NONE of the 5 are fully VM-sourceable; fields VM can't carry (battery device, update versions/release_url, repair summary, precise config-entry state) DROPPED + deferred to STAGE-005-031 (live-HA enrichment) / STAGE-005-032 (config-entry state collector change). D-DETAIL-DATA-SOURCE = VM per-series via vm_instant_query. D-DETAIL-CARDINALITY = top-N=100 + total/returned. D-DETAIL-ENDPOINT-SPLIT = 5 separate endpoints. D-DETAIL-CONSUMES-VM (locked) = VM, not live-HA.

**Refinement (3a+3b):** All 5 endpoints validated against the live prod rig with a real session — real data (962 unavailable / 100 returned cap confirmed, 1 low battery, 41 updates, 3 config-errors, 0 repairs genuine), 401 without session. `/updates` title="" confirmed correct (collector emits title, empty when HA has none — not a bug).

**Verification:** `make verify` GREEN — ruff clean, pyright 0, pytest 4829 pass / 1 expected env-skip, kernel coverage 100%, tsc 0, eslint 0 errors / 94 warnings (baseline), vitest 1477, UI build OK, openapi consistent.

**Commit:** (commit: 4a8533d)
